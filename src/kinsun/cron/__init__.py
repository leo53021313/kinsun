"""排程引擎（Cron）：什麼時候該跑什麼程式。

⚠️ 這裡**不是** `schedules/`。兩者長得像但完全是兩件事：

- `cron/`（本套件）＝技術層。cron 運算式、到期判斷、狀態持久化、長跑迴圈。
  它不知道「吃藥」是什麼，只知道有一支叫 `schedule-dispatch` 的 job 每分鐘要跑。
- `schedules/` ＝業務層。長輩的吃藥、回診、自訂提醒——**她的**行程表。
  它不知道 cron 是什麼，只產出一支要被本套件執行的 job。

（2026-07-27 由 `scheduler/` 更名而來，正是為了讓這條界線在檔案樹上看得出來。）
"""
