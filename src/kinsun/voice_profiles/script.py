"""家屬錄製參考語音時要照唸的固定稿子。

⚠️ 為什麼是「系統給稿、家屬照唸」，而不是讓家屬自由發揮再辨識逐字稿：

CosyVoice 的 zero-shot 需要「參考音檔 ＋ 那段音檔的逐字稿」成對輸入。逐字稿只要
對不上音檔，合成品質就會壞掉。三種取得逐字稿的方式裡：

- 讓家屬自己打字 → 打錯一個字就影響品質，而且是靜默的
- 上傳後用 ASR 辨識 → 多一個誤判來源，且長輩家屬的口音本來就是 ASR 最不擅長的
- **系統給稿、家屬照唸** → 逐字稿一定正確，因為它根本不是「猜」出來的

而且這個做法順帶解決另一件事：`services/tts/README.md` 的「參考語音的錄製準則」
本來要靠錄音者自律，改成固定稿之後**變成內建的**——稿子本身就設計成滿足全部四條。
"""

from __future__ import annotations

# 家屬要照唸的內容。改動前請確認仍滿足下方 `SCRIPT_RATIONALE` 列出的四項，
# 並同步 `services/tts/README.md` 的「參考語音的錄製準則」。
VOICE_PROFILE_SCRIPT = (
    "阿嬤您好，今天過得好嗎。記得按時吃飯吃藥喔，天氣冷要多穿一件。阿嬤要保重身體喔。"
)

# 錄音時要提醒家屬的注意事項（前端直接顯示，不要自行改寫）。
VOICE_PROFILE_TIPS = (
    "找個安靜的地方錄，不要有回音，旁邊也不要有人講話。",
    "用平常講話的語氣和速度就好，不用刻意像在播報新聞。",
    "整段照著唸完，不要漏字也不要多唸。唸的跟文字一模一樣，出來的聲音才會像。",
    "唸完最後一個字之後，停一秒再按停止錄音。",
)

# 這份稿子為什麼長這樣。留在程式碼裡，改稿的人才知道每一項在保護什麼。
SCRIPT_RATIONALE = {
    "含「阿嬤」兩次": (
        "「嬤」是罕用字，模型會念成一聲且氣音蓋掉、幾乎聽不見，而這是本系統出現頻率"
        "最高的稱呼。參考語音裡有人示範過就會念對（DGX 實機 A/B 實測，"
        "固定同一位說話者、只差參考語音有無此詞）。"
    ),
    "約 35 字": (
        "自然語速下約 12～13 秒，落在「10～15 秒」的區間。太短會讓輸出長度不穩定："
        "實測 4 秒參考合成同一句話會產出 5.04～6.92 秒（另有一次 11.88 秒）。"
    ),
    "逐字稿長度適中": (
        "CosyVoice 對「合成文字比參考逐字稿短」會拖慢速度：6 字回覆用 56 字參考的"
        "RTF 是 1.50，用 30 字參考是 0.64。本系統回覆上限 30 字、分段每段至少 8 字。"
    ),
    "「喔」句中與句尾各一次": (
        "「喔」在長輩照護語域極常出現（要吃藥喔、多喝水喔），但目前合成到它時約五次"
        "有四次會卡頓。推測與現行參考語音的「喔」只出現在整段最末尾、尾音可能被切掉"
        "有關（尚未證實，見 services/tts/README.md 已知限制）。兩個位置各出現一次，"
        "讓模型兩種型態都學得到。"
    ),
}
