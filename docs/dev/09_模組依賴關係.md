# 模組依賴關係分析 - 金孫 KinSun

> **版本:** v1.37 | **更新:** 2026-08-12 | **狀態:** ✅ 定稿（整合 voice_profiles、人設、地端嵌入與 App／Web 新視覺依賴）
> v1.9（2026-07-26）：基礎層新增 `background`（背景落庫），零領域依賴、不新增循環。
> 依 2026-07-08 全庫 import 掃描實證；分層定義見 [05 §1.3](05_架構與設計.md)。
> v1.5（2026-07-20）：三項自建輪子改用成熟 OSS——傳輸層 urllib→httpx、RAG 重試手刻→tenacity、設定 dataclass→pydantic-settings；httpx 由 dev 升為 runtime 直接依賴，新增 tenacity／pydantic-settings。

---

## 人設目錄的依賴方向（D-81，2026-08-05）

`personas.py` 是**葉節點**：它不 import 任何子系統，由四處反向引用——
`agent`（提示詞開頭的語氣段落）、`accounts/models`（`Elder.persona` 的預設值）、
`speech/acks`（等待語語庫的鍵）、`web/routers/elders`（值域驗證）。

刻意獨立成模組而不是住在 `agent.py`：上述四處要引用同一份 id 清單，共用出處才不會
四邊各寫一份、然後慢慢走鐘。放在 `agent.py` 會讓 `accounts` 與 `speech` 反向依賴 agent，
違反既有的 import 方向約束。

## 1. 依賴原則與落實

| 原則 | 本專案落實 |
| :--- | :--- |
| 依賴倒置（DIP） | **51 個** Protocol＋三件套（Pg／Fake，**21 組**合約測試）；Service 依賴介面不依賴實作。⚠️ 51 為 2026-08-08 以 AST 掃全庫 `class X(Protocol)` 的實數（合約測試 21 組亦為 `ls tests/test_*_store_contract.py` 實測）（逐名清單見 [10 §8](10_類別關係.md)）；先前新增 `CombinedSafetyClassifier` 一個（**不增加合約測試組數**——LLM 分類器 seam，無 Pg／Fake 對）；本次併入 `VoiceProfileStore` 一個（**增加**一組合約測試，有 Pg／Fake 對） |
| 無循環依賴（ADP） | ✅ **實證無檔案級循環**（見 §4） |
| 穩定依賴（SDP） | 高扇入的 `db`／`llm`／`config`／`transport` 零領域依賴，方向正確 |

## 2. 分層依賴圖

```mermaid
graph TD
    subgraph 組裝根
        APP[app.py] & COMP[composition.py] & WORKER[cron/worker.py]
    end
    subgraph 邊緣層
        WEB[web] & CH[channels line/app]
    end
    subgraph 編排層
        PIPE[pipeline] & AGENT[agent] & ROUTER[channels/router] & JOBS[各領域 jobs.py]
    end
    subgraph 領域層
        ACC[accounts] & SCHED[schedules] & SAFE[safety] & MEM[memory] & RAG[rag] & REP[reports] & LOC[locations] & STRAT[strategies] & NEWS[news] & PLACES[places] & VOICE[voice_profiles]
    end
    subgraph 基礎層
        DB[db] & LLM[llm] & CFG[config] & TR[transport] & SP[speech] & AU[audio] & OBS[observability] & BG[background]
    end
    組裝根 --> 邊緣層 --> 編排層 --> 領域層 --> 基礎層
    SAFE -. "runtime 經 TextSender 插座（✅ D-18 已反轉，無原始碼依賴）" .-> ROUTER
```

## 3. 扇入／扇出統計（import 邊掃描）

| 高扇入（被依賴）Top 5 | 高扇出（依賴他人）Top 5 |
| :--- | :--- |
| db（11）、accounts（8）、llm（7）、memory（6）、reports／observability／config／channels（各 5） | composition（13）、cron worker（11）、app.py（11）、web（7）、schedules（8） |

高扇出者全是**組裝根**（本來就該認識所有人）；高扇入者全是**基礎層**（穩定、零領域依賴）——方向健康。

`background`（2026-07-26 新增）落在基礎層且零領域依賴（只用 stdlib），被 `observability.store`、`pipeline`、`app.py`、**`agent`**（2026-07-30 起，`record_turn` 背景化）四者依賴，方向與 `db`／`llm` 一致，不新增循環——`agent` 在編排層、`background` 在基礎層，這條新邊仍是往下指。扇入 3→4。

## 4. 循環依賴：無

module 粒度看似有三對雙向（schedules／observability／proactive ↔ cron），實為 **cron-core 與 cron-worker 混看的假象**：

- cron 核心（`scheduler.py`／`fanout.py`／`state.py`）只依賴 `db`，**不 import 任何領域**；
- `registry.py` 只依賴 `config`（要讀鐘點設定），同樣不 import 任何領域——排程宣告是純資料，這是它能被 web 程序廉價讀取的前提；
- 領域 `jobs.py` 只 import cron 核心（`Job`／`fanout_job`／`SCHEDULE_DISPATCH_CRON`）；
- 反向邊全部來自 `worker.py`（排程進程的組裝根）import 各領域 jobs——組裝根依賴所有人是正常的。

分開看即為乾淨 DAG。文件約定：**討論依賴時 cron-core 與 worker 分開計**。

2026-07-12 起（D-73）新增一條組裝根之間的邊：`app.py → cron/worker.build_jobs`——內測手動觸發與排程 worker 共用同一份 job 清單。方向單一（web 組裝根 → 排程組裝根的純組裝函式），不引入循環；worker 扇入自 0 變 1，DAG 結論不變。

2026-07-27 另加兩條指向 `cron/registry` 的邊：`app.py`（後台排程健康檢查讀全系統宣告）與 `rag/worker.py`（取自己那支 job 的名稱與 cron）。registry 不 import 任何領域，故兩條邊都只是往下指，DAG 結論不變。

## 5. 關鍵依賴路徑（App 語音回合）

`channels/app/ws.py`（邊緣，2026-07-28 新增的對講機長連線）與 `channels/app/turns.py`（邊緣，降級路徑）→ `channels/inbound.dispatch`（編排）→ `binding/gate.ConsentGate`（領域）→ `pipeline.VoicePipeline`（編排）→ `safety.RiskDetector`＋`agent.CareAgent`（領域）→ `memory.SessionMemory`（領域）→ `llm.GeminiClient`／`speech`／`db`（基礎）。全程依賴方向單向向下；notifier 反向依賴已解除（✅ D-18，`TextSender` Protocol）。

補充（2026-07-10 群 1 深潛實證）：閘門實際被呼叫**兩次**——`turns.py:92` 在進 dispatch 前先於邊緣層解析一次做 403 守門，`inbound.py:131` 於 dispatch 內再解析一次取 elder_id（✅ A-11 已修，庚-12：dispatch 加可選 elder_id，App 路徑跳過重查）。上行線性描述為主幹示意。

補充（2026-07-17 長輩地點）：`turns.py` 於 dispatch **之前**先寫 `elder_locations`（地名＋模糊座標三者齊備才寫）；agent 端經 `SessionMemory` 的 `LocationFacts`（FactProvider）注入，依賴方向不變（locations 僅依賴 db）。

補充（2026-07-27 附近地點搜尋）：`places` 僅依賴 `db`（與 `locations` 同層、彼此不互依）；`tools/places.py` 的 handler 依賴 `places` 與 `locations` 兩者（座標讀取），這條邊只在編排層之下的 `tools`，不逆向指回領域層，DAG 結論不變。地名 → 座標的 `resolve_place` 借道 `tools/transport.geocode`，形成 `tools/places → tools/transport` 一條同層邊（工具互相依賴，非領域依賴基礎層外的新方向）。

補充（2026-07-30 延遲優化四刀，兩個新檔皆**不新增任何對外依賴方向**）：

- `safety/combined_classifier.py` 只 import `tracing`、`llm`（既有的 `LLMClient` seam）與同套件的 `classifier`／`moderation`／`tiers`——**safety 不新增對外依賴**，與 v1.8 那筆 `AbuseClassifier` 的結論完全相同（合併分類器只是把兩次 `LLMClient.generate` 併成一次，seam 沒變）。
- `channels/app/inbound_audio.py` **一個 kinsun 模組都沒有 import**（只用 stdlib 的 `contextvars`／`logging`／`threading`／`time`）：`inbound_audio`（`AudioPublisher`）與 `traces`（`TraceStore`）都是**呼叫端傳進來的鴨子型別參數**，故它對 `audio` 與 `observability` 只有執行期關係、沒有原始碼邊。⚠️ 這是刻意的——它跑在 `dispatch` 之前的背景執行緒裡，越少 import 越不會把新的啟動期耦合帶進入站路徑；型別靠 `turns.py`／`ws.py` 的組裝點保證，代價是這一檔的參數沒有型別標註。
- `channels/app/admission.py`（P3 Task 1，2026-07-31）比照上一筆 `inbound_audio.py` 的形狀：**同樣一個 kinsun 模組都沒有 import**（只用 stdlib 的 `itertools`／`threading`／`collections`／`contextlib`）。`TurnAdmission` 是純記憶體內的容量閘門，不依賴任何領域或基礎層模組。

  **接線後的依賴邊**（P3 Task 2，2026-07-31）：`channels/app/turns.py` 與 `channels/app/ws.py`（皆邊緣層）各自 `from kinsun.channels.app.admission import AdmissionTimeout, TurnAdmission`；組裝根 `app.py` 額外 import 以建立**共用的單一實例**並分別注入兩個路由器工廠（各自建一個的話，兩條路徑可以互相繞過對方的容量上限，閘門形同虛設）。三條邊皆往下指或組裝根依賴邊緣層，方向正常。

  另新增一條 `channels/app/` 子套件內部的**同層邊**：`turns.py → ws.py`（`from kinsun.channels.app.ws import _BUSY_REPLY, _DEFAULT_TURN_CONCURRENCY`）——兩條對講機路徑對長輩說的婉拒文案、以及未注入 `TurnAdmission` 時的備援併發上限，刻意共用同一個出處而非各寫一份，避免措辭日後分岔。這是 `turns.py`／`ws.py` 兩檔之間**第一次**出現互相依賴的邊；`ws.py` 不 import `turns.py`，故不構成循環，且兩者同屬邊緣層，不影響 DAG 結論。
- `agent → background`：見 §3 的說明（編排層 → 基礎層，往下指）。`pipeline` 讀 `PreparedTurn.record_handle` 走的是既有的 `pipeline → agent` 邊，不新增方向。

## 6. 外部依賴清單

### 後端（pyproject.toml）

| 依賴 | 用途 | 風險 |
| :--- | :--- | :--- |
| fastapi＋uvicorn | Web 框架＋ASGI | 低 |
| psycopg[binary,pool] | Postgres 驅動＋池 | 低 |
| google-genai | Gemini SDK | 中（API 演進快；thought_signature 等行為已封裝於 llm.py） |
| httpx | 對外 HTTP client（傳輸層 `transport.py`＝`HttpxTransport`；亦為 mem0／opik 傳遞相依） | 低（純 Python，跨平台；持久連線池） |
| pydantic-settings | 設定載入（`config.py`：欄位宣告＋型別強制＋凍結＋自訂 env 注入來源） | 低（pydantic 已隨 fastapi 存在） |
| tenacity | 有界重試／指數退避（RAG embedding＋crawler `_fetch`） | 低（純 Python，跨平台） |
| mem0ai（2.0.10） | 長期記憶 | 中（版本行為差異大——graph 移除、檢索三路實為一路；升版需重跑查證，見 ADR-003） |
| line-bot-sdk v3 | LINE 通道（凍結） | 低（隨 LINE 退場移除） |
| vecs | mem0 Supabase provider 的傳遞相依（衛教 RAG 為自寫 pgvector SQL，**不用 vecs**——2026-07-10 群 2b 實證全庫零 import） | 低 |
| croniter | cron 解析 | 低 |
| tzdata | 時區 | 低 |

dev：pytest、ruff、pre-commit。

### 前端

- `app/`：expo ~54（2026-07-12 自 57 降級，配合實機 Expo Go）、expo-router 6.0、expo-audio（對講機）、expo-secure-store（token）、expo-location（模糊定位）、expo-linear-gradient 15.0.8（長輩端暖色背景）、AsyncStorage 2.2.0（裝置本地當日對話）、`react-native-webview` 13.15.0（阿白離線 renderer）、react-native 0.81.5、react 19.1、datetimepicker 8.4.4；dev：jest-expo、eslint-config-expo、@testing-library/react-native、jsdom 20.0.3（renderer HTML 啟動測試）。
- `frontend/`：@line/liff 2.25、react 19.2.8、react-router 8.3（2026-07-25 audit 修復升級：react-router 7.x 全系列有 high 通報、8.3.0 要求 React ≥ 19.2.7，故 React 18.3→19 一併升；import 自 `react-router-dom` 改 `react-router`）、vite 6；dev：vitest 4（jsdom）、ESLint 9（typescript-eslint＋react-hooks 7）、@testing-library/react；overrides：minimatch ^10.0.3＋postcss ^8.5.23（漏洞修補，避免被迫升 eslint 10）。
- `web/`（P1 地基，2026-07-30）：react 19.2.8、react-router 8.3、vite 8、Tailwind CSS v4（`@tailwindcss/vite`）；dev：vitest 4（jsdom）、ESLint 9、@testing-library/react。**依賴 `shared/`**（`kinsun-shared` 為 `vite.config.ts` 的 resolve alias，指向 `../shared`，與 `frontend/` 同一套手法）：目前用到 `client.ts`（`createApiClient`）與 `envelope.ts`（`ApiError`）。兩個第三方套件解決 QR 生成／解讀相反方向的問題，各自獨立：`qrcode` 1.5.4（P2，家屬端 `InviteCard` 畫 QR 圖）與 `zxing-wasm` 3.1.2（P3 Task 5，長輩端 `talk/qrScanner.ts` 解碼，`zxing-wasm/reader` 子路徑；wasm 二進位以同源靜態檔 `web/public/zxing_reader.wasm` 提供，須與套件版本保持一致——`web/scripts/verify-wasm-checksum.mjs` 於 `build` 前置檢查，見 07 §7／08 §6）。**P4 Task 1（模擬系統通知橫幅，2026-07-31）未新增第三方套件**：`notify/osStyle.ts` 新增一條型別限定邊 `→ stage/PhoneFrame.tsx`（`export type { PhoneOs } from "@/stage/PhoneFrame"`，編譯期抹除、無執行期依賴），避免與既有的 `PhoneOs` 各自重新宣告一份。**審查修正（2026-07-31）**新增 `notify/NotificationBanner.tsx → stage/reducedMotion.ts` 一條執行期邊（原本每檔各自宣告一份 `prefersReducedMotion`，現抽出共用；`stage/TearTransition.tsx` 亦改為 import 同一份，屬 `stage/` 內部邊，非跨模組）；`stage/PhoneFrame.tsx` 新增 `role="status"`／`aria-live="polite"`（純屬性、非依賴變更）。**P4 Task 2（通知輪詢，2026-07-31）未新增第三方套件**：`notify/useNotificationFeed.ts` 新增四條執行期邊——`→ elder/api.ts`（`listElderNotifications`）、`→ guardian/api.ts`（`listNotifications`）、`→ notify/seen.ts`（`loadSeenAt`／`unreadCount`；⚠️ 不呼叫 `saveSeenAt`，該函式的呼叫權維持只屬於兩支 `NotificationsScreen`，見 07 §7）、`→ strings.ts`（`strings.gate.brand`，取代 brief 原始版本寫死的裸字串）；另一條型別限定邊 `→ notify/NotificationBanner.tsx`（`import type { BannerItem }`，編譯期抹除）。純新增，未被任何進入點 import，尚未接進畫面，不影響既有依賴圖的 DAG 結論。**審查修正（2026-07-31）**補一條先前漏記的執行期邊 `notify/useNotificationFeed.ts → @/api`（`import { ApiError } from "@/api"`，401 判定用；`@/api` 本身是既有模組，非新增依賴）；未新增第三方套件。**P4 Task 3（跨欄連動，2026-07-31；本行 2026-08-01 全分支審查修正時補記，`15 §2` 規則本應在 Task 3 首次交付時就同步）**：新增 `notify/bus.ts`（`emitStageEvent`／`useStageEvent`），未被任何模組 import（消費端待 Task 4，接上 `notify/useNotificationFeed.ts` 的 `reloadSignal`）；新增兩條執行期邊——`guardian/SchedulesScreen.tsx → notify/bus.ts`（`submit()`／`confirmDelete()` 成功後 `emitStageEvent("guardian-wrote")`）、`guardian/HomeScreen.tsx → notify/bus.ts`（`addElder()` 成功後同一行）；`guardian/ElderDetailScreen.tsx` 未新增此邊（`regenerateBinding()` 刻意不發此事件，理由見 07 §7）。另一條線（`prefilledCode`／`onSendCodeToElder`，搬資料）不新增跨模組依賴邊：`stage/StagePage.tsx` 既有依賴 `elder/ElderApp.tsx`／`guardian/GuardianApp.tsx`（P1 地基即有），本輪只是這兩條既有邊上多傳一個新型別的 prop，型別 `ElderCodeDelivery` 定義於 `elder/BindScreen.tsx` 並由 `elder/ElderApp.tsx`／`stage/StagePage.tsx` 匯入（新增兩條型別限定邊，編譯期抹除）。未新增第三方套件，DAG 結論不變。**P4 Task 4（兩欄接上通知，2026-08-01；本行 2026-08-01 審查修正時補第四條漏記的邊）**：`stage/StagePage.tsx` 新增**四條**執行期邊——`→ notify/useNotificationFeed.ts`（兩支呼叫，長輩／家屬各一）、`→ notify/NotificationBanner.tsx`（兩支呼叫，長輩欄 `size="big"`）、`→ notify/bus.ts`（`useStageEvent("guardian-wrote")`，`notify/bus.ts` 的 `useStageEvent` 至此才有第一個真實呼叫端）、`→ notify/osStyle.ts`（`detectOs(navigator.userAgent)`，`os` state 的惰性初始值，**執行期函式呼叫、非型別**——初版審查記錄漏記了這條）；`→ elder/ElderApp.tsx`／`→ guardian/GuardianApp.tsx` 兩條既有邊上新增 `unread` prop（不新增邊，僅既有邊多傳一個值）。未新增第三方套件，DAG 結論不變：`stage/StagePage.tsx → notify/*` 與既有的 `notify/NotificationBanner.tsx → stage/reducedMotion.ts` 是同一個目錄下兩支不同檔案、方向相反的邊，兩者間不構成循環（`reducedMotion.ts` 是不依賴任何 `stage/` 其他檔案的葉工具函式，`StagePage.tsx` 也未被 `notify/` 任何檔案匯入）。⚠️ **新增的 `stage/StagePage.tsx → notify/osStyle.ts` 執行期邊，與既有的 `notify/osStyle.ts → stage/PhoneFrame.tsx` 型別限定邊（P4 Task 1 記載），合起來會形成 `stage/ → notify/ → stage/` 的模組圖來回**：後者是 `export type { PhoneOs } from "@/stage/PhoneFrame"`，編譯期完全抹除、不產生任何執行期 import，故不構成真正的執行期循環，DAG 結論不變——與上一段 `NotificationBanner.tsx`／`reducedMotion.ts` 那組「方向相反不成環」的說明是同一種情形，這裡補記同樣的一句話。**審查修正 Minor 5 順帶新增一條型別限定邊**：`strings.ts::stage.notificationStyle` 的參數型別由 `string` 收緊為 `PhoneOs`，新增 `strings.ts → stage/PhoneFrame.tsx` 型別限定邊（`import type { PhoneOs }`，編譯期抹除）；`stage/PhoneFrame.tsx` 未匯入 `strings.ts`，不構成循環。**P4 Task 5（端到端測試，2026-08-01）**：新增開發依賴 `@playwright/test` 1.62（`web/e2e/journey.spec.ts`／`web/playwright.config.ts` 唯一 import 者）。不新增 `web/src/` 內部 import 邊——E2E 透過真瀏覽器操作已建置的 `web/dist/`，走 HTTP 打真後端，不 import 任何 `web/src/` 模組，是黑箱（black-box）測試，DAG 結論不變。**P4 全分支審查修正（2026-08-01）新增一條執行期邊**：`notify/seen.ts → session/storage.ts`（`loadSession`／`Role`）——已讀水位的儲存鍵改為「角色＋目前登入者的 `display_name`」，識別碼直接由持久化的 session 取得，不要求三個呼叫端各自多傳一路（理由見該檔檔頭）。方向是 `notify/ → session/`，`session/` 不匯入 `notify/` 任何檔案，不構成循環；`Audience` 型別自此等同 `session/storage.ts::Role`（原本是各自宣告的同形聯集）。DAG 結論不變，未新增第三方套件。
- npm overrides 補漏（2026-07-25，PR #78）：`app/` 亦加 minimatch ^10.0.3＋postcss ^8.5.23 兩個 overrides 清 npm audit 高危（不動 expo／react-native 版本）。

### App 阿白 renderer 依賴邊（2026-08-07）

- 執行期：`elder/talk.tsx → components/BearStage.tsx → components/OttoBearRenderer.tsx → lib/ottoBridge.ts`；`OttoBearRenderer` 另依賴 `expo-asset`／`expo-file-system`（既有）與 `react-native-webview`。方向由頁面指向葉呈現層，renderer 不回頭匯入頁面、session、API、錄音或播放模組，不構成循環。
- 產物：`app/scripts/build-otto-renderer.mjs → shared/otto-pet-core/* → app/assets/otto/renderer.html`。App runtime 只讀產物；`bear:check` 反向檢查產物與白名單來源一致，但生產程式碼沒有 import 腳本或 vendor JS。
- bridge 的 `TalkVisualState` 只做 TypeScript 型別依賴，沿用既有五態；`talkGesture.ts`／`talkSocket.ts`／`talkPresentation.ts` 未修改。Otto 的 `brain.js`、麥克風、LLM、TTS、定位與其他外部網路模組均不在 renderer script 白名單內。

### App 共用 UI 依賴邊（2026-08-07，批次 2）

- 執行期：`app/src/app/* → components/ui.tsx → lib/theme.ts`。共用 UI 只依賴 React、React Native 與設計 token，不回頭匯入路由、session、API、錄音或播放模組。
- 測試：`components/ui.test.tsx → components/ui.tsx`，並讀取 `lib/theme.ts` 驗證 token；使用既有 jest-expo／React Native Testing Library，未新增第三方套件。
- 焦點處理使用 React Native 內建 `onFocus`／`onBlur`，沒有新增焦點管理依賴；DAG 結論不變。

### App 對講機呈現層依賴邊（2026-08-07，批次 3）

- `elder/talk.tsx → components/BearStage.tsx → components/OttoBearRenderer.tsx`；前者只傳五態、回應情緒與 `speechCue`，舞台不回頭依賴 session／API／錄音。
- `elder/talk.tsx → lib/talkReplyPresentation.ts`（純函式葉模組）與 `→ lib/todayLog.ts → @react-native-async-storage/async-storage`。`todayLog` 不匯入 API；完整逐字稿不會沿依賴邊流向家屬模組。
- `elder/talk.tsx → expo-linear-gradient` 只畫背景；`→ lib/api.ts → shared/types.ts::TurnReply` 讀新增的 `transcript`。後端兩個長輩通道 router 共同依賴既有 `turn_context.transcript_listener`，未新增跨領域 Protocol。
- 測試邊只由 co-located 測試指向上述三個葉模組。`talkGesture.ts`／`talkSocket.ts`／`talkPresentation.ts` 未修改，DAG 結論不變。

### App 當日紀錄畫面依賴邊（2026-08-07，批次 4）

- `elder/history.tsx → lib/SessionProvider.tsx` 只做長輩角色守衛；`→ lib/todayLog.ts` 只讀當日裝置紀錄；`→ components/BearStage.tsx` 只使用小型 `BearAvatar`，不建立第二套角色狀態。
- 畫面另依賴既有 `expo-router`、`react-native-safe-area-context`、`strings.ts` 與 `theme.ts`；沒有 API import，也沒有新增第三方套件。
- `lib/elderHistoryScreen.test.tsx → elder/history.tsx` 以 mock 隔離 router、session、storage 與角色 renderer。依賴維持頁面→既有葉模組，DAG 結論不變。

### App 家屬端 Tabs 依賴邊（2026-08-07，批次 5）

- `guardian/_layout.tsx → expo-router Tabs／SessionProvider／GuardianTabsProvider／theme`，另使用 React Native `BackHandler` 處理 Android 非首頁 tab 返回；layout 不匯入通知 API。
- `GuardianTabsProvider.tsx → lib/api.ts::listElders` 與 `SessionProvider`，集中第一位長輩暫用規則與 401 登出；`GuardianTabLanding.tsx → GuardianTabsProvider／ui／expo-router`，report／profile 只重用既有資料與路由。
- `guardian/notifications.tsx → lib/api.ts／notificationsSeen.ts／expo-router navigation.setOptions`，通知頁自行設定 badge 與最新水位。根 Stack 單向包住 nested Tabs、`auth/` 與 `guardian-detail/`，深頁不反向匯入 Tabs layout。
- 測試邊只由三支 co-located 測試指向上述頁面／Provider。未新增第三方套件，執行期方向仍由路由／頁面指向資料與 UI 葉模組，DAG 結論不變。

### 新增依賴紀律（AGENTS.md）

新增前確認 **linux/aarch64 有 wheel**（DGX 是部署目標）；除非有充分理由不加第三方套件；lock 檔（uv.lock／package-lock.json）必須入庫。

## 7. 依賴風險管理

| 風險 | 策略 |
| :--- | :--- |
| mem0ai 升版行為漂移 | 釘版＋升版前重跑功能查證（2026-07-08 查證報告為基線） |
| 外部 SDK（LINE／Gemini）schema 變動 | 已以 ACL 封裝（`channels/line`、`llm.py`）；領域層零直接依賴 |
| safety→channels 反向依賴 | ✅ **已解除**（D-18；2026-07-10 實證 notifier 無 channels import，`TextSender` 插座＝notifier.py:18） |

## 變更紀錄

| 版本 | 日期 | 變更 |
| :--- | :--- | :--- |
| v1.36 | 2026-08-10 | 整合 persona／profile、地端嵌入與 App／Web 新視覺依賴；Guardian Tabs 對 persona 的新增資料流維持單向。 |
| v1.34 | 2026-08-07 | §6 補 App 批次 5 家屬 Tabs、`GuardianTabsProvider`、過渡 landing、通知頁自設 badge 與根 Stack／nested Tabs 單向依賴；未新增第三方套件，DAG 不變。 |
| v1.33 | 2026-08-07 | §6 補 App 批次 4 `/elder/history` 對 SessionProvider、todayLog、BearAvatar、router 與測試的單向依賴；未新增第三方套件，DAG 不變。 |
| v1.32 | 2026-08-07 | §6 補 App 批次 3 依賴：`BearStage` 取代舊插槽，新增 `talkReplyPresentation`／`todayLog` 葉模組，以及 AsyncStorage 2.2.0／expo-linear-gradient 15.0.8；後端沿用 transcript listener，DAG 與 Protocol 數不變。 |
| v1.31 | 2026-08-07 | §6 補 App 共用 UI 批次 2 依賴邊與測試邊；只依賴既有 React Native／theme／測試基礎，未新增第三方套件，DAG 不變。 |
| v1.30 | 2026-08-07 | §6 補 `react-native-webview` 13.15.0、jsdom 20.0.3 與 App 阿白 renderer 的執行期／產物依賴邊；方向維持頁面→葉呈現層，DAG 不變。 |
| v1.29 | 2026-08-01 | **App 內通知加 `severity`**（Leo 裁決）：§6 補一段——前端新增葉模組 `notify/severity.ts` 與三條邊；後端新增零依賴葉模組 `notifications/models.py` 與四條邊（含 `safety/notifier.py →`，已載明為何不違反 D-18）。DAG 結論不變 |
| v1.28 | 2026-08-01 | **網頁版前端 P4 全分支審查修正**（1 Critical＋1 Important＋6 Minor）：§6 `web/` 依賴清單新增執行期邊 `notify/seen.ts → session/storage.ts`（已讀水位改以「角色＋登入者」分鍵，Minor 2）。方向 `notify/ → session/`，不構成循環，DAG 結論不變 |
| v1.0 | 2026-07-08 | 初版：實證無循環；扇入扇出統計；外部依賴風險表 |
| v1.1 | 2026-07-10 | 群 1 深潛更正：D-18 已反轉（§2 圖＋§7 風險表）；§5 補閘門邊緣先行守門與二次解析事實 |
| v1.2 | 2026-07-10 | 群 2b 深潛更正：§6 vecs 描述更正（mem0 傳遞相依，RAG 自寫 pgvector SQL 不用 vecs） |
| v1.3 | 2026-07-12 | 內測基礎建設（D-73）：§4 補 `app.py → worker.build_jobs` 新邊（單向、無循環） |
| v1.4 | 2026-07-17 | 對齊程式現況：Protocol 31→42（合約 17 組）；§2 領域層補 locations／strategies；§5 補 turns 先寫地點與 LocationFacts 注入、A-11 標已修（庚-12）；§6 前端依賴對齊 Expo 54 降級與 JS 測試／lint dev 依賴 |
| v1.6 | 2026-07-25 | 話題新聞（D-74）：§2 領域層補 news、§1 Protocol 44／合約 18 組；§6 前端依賴對齊 audit 修復（frontend React 19.2.8＋react-router 8.3、frontend／app overrides minimatch＋postcss，PR #78） |
| v1.7 | 2026-07-25 | D-74 消費端：§1 Protocol 45／合約 19 組（NewsMentionStore） |
| v1.8 | 2026-07-25 | 濫用審核：§1 Protocol 45→47（重掃實數，含新增 `AbuseClassifier`）；審核走 `LLMClient` seam，safety 不新增對外依賴。 |
| v1.11 | 2026-07-27 | 新增附近地點搜尋（spec 2026-07-27）：§2 領域層補 `places`；§5 補一段依賴補充（`places` 僅依賴 `db`；`tools/places.py` 依賴 `places`＋`locations`，並經 `tools/transport.geocode` 形成 tools 內部同層邊）；§1 Protocol 47→48／合約 19→20 組（新增 `PlaceStore`）。 |
| v1.14 | 2026-07-31 | 網頁版前端 P1 地基（任務 12，spec 2026-07-30-web-demo-design）：§6 前端依賴清單新增 `web/`（react-router 8／vite 8／Tailwind v4；依賴 `shared/` 的 `client.ts`／`envelope.ts`，`kinsun-shared` alias 與 `frontend/` 同手法，非新增第三方套件）。 |
| v1.13 | 2026-07-30 | 延遲優化四刀的依賴面。動機：四刀都是「把等待搬離關鍵路徑」，最容易順帶弄壞的就是依賴方向（背景化＝多一個消費者、合併呼叫＝可能多一個外部 seam），故逐項查證後記錄結論——**三處都沒有新方向**。①`agent` 成為 `background` 的第四個消費者（B2，`record_turn` 背景化）：編排層 → 基礎層，往下指，扇入 3→4，DAG 結論不變。②`safety/combined_classifier.py` 只走既有 `LLMClient` seam，safety 不新增對外依賴（比照 v1.8 的 `AbuseClassifier`）。③`channels/app/inbound_audio.py` **零 kinsun import**，對 `audio`／`observability` 只有執行期的鴨子型別關係、無原始碼邊。⚠️ **Protocol 數以 AST 全庫重掃更正 48→50**（口徑＝`class X(Protocol)` 的實數）：原本的 48 同時犯了兩個方向的錯——多算 2 個**已退役**的（`MedicationStore`／`AppointmentStore`，D-76 隨模組移除卻留在 10 §8 名單裡），少算 3 個**現存**的（`ScheduleStore`／`PushTokenStore`／`LocationStoreLike`），兩邊部分相消才長得像只差幾個；加上本批新增的 `CombinedSafetyClassifier` 即為 50。逐名清單同步修好於 10 §8（該節標題自 v1.24 起掛著「45 vs 47 待下次全庫重掃核實」的註記，本次就是那次重掃）。合約測試維持 20 組（新 Protocol 無 Pg／Fake 對）。 |
| v1.15 | 2026-07-31 | **網頁版前端 P3 對講機容量閘門**（Task 1，複審裁決要求補上）：§5 補一段 `channels/app/admission.py`——比照 v1.13 的 `inbound_audio.py`：同樣零 kinsun import（只用 stdlib `itertools`／`threading`／`collections`／`contextlib`），且**尚未被任何模組 import**（純新增，`turns.py`／`ws.py` 接線與環境變數屬 Task 2），此刻與全庫其他模組完全沒有依賴邊。05（架構契約）與 10（類別關係）不補：`TurnAdmission` 非 Protocol、無架構層級的新 Container，複審已核對 `05`／`10` 兩份文件皆未收錄性質相近的 `inbound_audio`／`_Sender`／`_InFlight`／`AckAudioCache`，判斷一致。 |
| v1.20 | 2026-07-31 | **P4 Task 1 審查修正**：§6 `web/` 依賴清單補一句——新增 `notify/NotificationBanner.tsx → stage/reducedMotion.ts` 執行期邊，`stage/TearTransition.tsx` 亦改為 import 同一份共用函式 |
| v1.27 | 2026-08-01 | **網頁版前端 P4 Task 6：跨瀏覽器驗收與文件收尾（P1～P4 最後一項）**：對照實際 import 關係逐一覆核 §6 `web/` 依賴清單，未發現遺漏，DAG 結論不變，清單本身無需改動。詳細見 task-6-report.md |
| v1.26 | 2026-08-01 | **網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：§6 `web/` 依賴清單新增開發依賴 `@playwright/test` 1.62（僅 `web/e2e/`／`web/playwright.config.ts` 使用，`npm audit` 0 漏洞）；不新增 `web/src/` 內部依賴邊——E2E 為黑箱測試，不 import 任何 `web/src/` 模組，DAG 結論不變。詳細見 07 §7／08 §6 |
| v1.25 | 2026-08-01 | **P4 Task 4 全分支審查修正 Minor 3**：§6 `web/` 依賴清單補漏記的第四條邊 `stage/StagePage.tsx → notify/osStyle.ts`（`detectOs`，執行期非型別），與既有反向的型別限定邊形成 `stage/ → notify/ → stage/` 圖形但編譯期抹除不成環；另補 `strings.ts → stage/PhoneFrame.tsx` 型別限定邊。DAG 結論不變 |
| v1.24 | 2026-08-01 | **網頁版前端 P4 Task 4：兩欄接上通知**：§6 `web/` 依賴清單補一段——`stage/StagePage.tsx` 新增三條執行期邊（`→ notify/useNotificationFeed.ts`×2、`→ notify/NotificationBanner.tsx`×2、`→ notify/bus.ts`）；既有兩條邊（`→ elder/ElderApp.tsx`／`→ guardian/GuardianApp.tsx`）新增 `unread` prop，不新增邊。與既有 `notify/NotificationBanner.tsx → stage/reducedMotion.ts` 方向相反但不構成循環。未新增第三方套件，DAG 結論不變 |
| v1.23 | 2026-08-01 | **P4 Task 3 全分支審查修正（Important 2）**：§6 `web/` 依賴清單補漏記的 `notify/bus.ts` 及兩條執行期邊（`guardian/SchedulesScreen.tsx`／`HomeScreen.tsx → notify/bus.ts`）與兩條 `ElderCodeDelivery` 型別限定邊；`15 §2` 要求新增模組須同步 08／09，Task 3 首次交付時漏了這兩份 |
| v1.22 | 2026-07-31 | **P4 Task 2 審查修正**：§6 `web/` 依賴清單補一條先前漏記的邊 `notify/useNotificationFeed.ts → @/api`（`ApiError`，401 判定用，既有模組非新增依賴） |
| v1.21 | 2026-07-31 | **網頁版前端 P4 Task 2：通知輪詢**：§6 `web/` 依賴清單補一段——`notify/useNotificationFeed.ts` 新增四條執行期邊（`→ elder/api.ts`／`→ guardian/api.ts`／`→ notify/seen.ts`（僅讀 `loadSeenAt`／`unreadCount`，不呼叫 `saveSeenAt`）／`→ strings.ts`）與一條型別限定邊（`→ notify/NotificationBanner.tsx`，`import type { BannerItem }`）；未新增第三方套件，DAG 結論不變 |
| v1.19 | 2026-07-31 | **網頁版前端 P4 Task 1：模擬系統通知橫幅**：§6 `web/` 前端依賴清單補一句——未新增第三方套件，`notify/osStyle.ts` 新增一條型別限定邊 `→ stage/PhoneFrame.tsx`（`export type { PhoneOs }`，編譯期抹除、無執行期依賴） |
| v1.18 | 2026-07-31 | **P3 Task 5 審查修正**（Minor 8）：§6 `web/` 前端依賴清單 `qrcode`／`zxing-wasm` 補版本號（1.5.4／3.1.2），補記 wasm 版本一致性由 `verify-wasm-checksum.mjs` 檢查 |
| v1.17 | 2026-07-31 | **網頁版前端 P3 Task 5：QR 掃碼**：§6 `web/` 前端依賴清單新增 `zxing-wasm`（`talk/qrScanner.ts` 唯一 import 者，走 `zxing-wasm/reader` 子路徑，`npm audit` 0 漏洞）；順便訂正同一行既有的過期描述——`qrcode` 早在 P2 就已加入，先前寫「留待 P2／P3 才加」未回填，本輪一併更正為現況（`qrcode` 生成、`zxing-wasm` 解讀，兩者互不重疊）。 |
| v1.16 | 2026-07-31 | **網頁版前端 P3 對講機容量閘門接線**（Task 2）：§5 更正 v1.15 的「尚未被任何模組 import」——`turns.py`／`ws.py`（邊緣層）各自 import `TurnAdmission`／`AdmissionTimeout`，組裝根 `app.py` 建立共用單一實例並分別注入兩個路由器工廠，三條邊皆方向正常。另新增 `turns.py → ws.py` 一條 `channels/app/` 子套件內部的同層邊（`_BUSY_REPLY`／`_DEFAULT_TURN_CONCURRENCY` 共用），為兩檔之間第一次出現互相依賴；`ws.py` 不回頭 import `turns.py`，不構成循環，DAG 結論不變。 |
| v1.37 | 2026-08-08 | **長輩客製化聲音複製 voice_profiles 併入 main**（PR #104 整合）：§2 領域層新增 `voice_profiles`（僅依賴 `db`，與 `speech` 經 pipeline 串接）；§1 Protocol 50→**51**／合約 20→**21** 組（新增 `VoiceProfileStore`；兩個數字皆為 2026-08-08 AST 掃描與 `ls tests/test_*_store_contract.py` 的實測值，非手數）；`VoicePipeline` 新增選配依賴 `VoiceProfileStore` 與 `sign_voice_url`（未設＝全域預設聲音），依賴方向向下、不新增循環；`channels/app/ws.py` 續段推播改呼叫既有的 `pipeline.resolve_voice`，用的是已存在的 `ws.py → pipeline` 邊，不新增模組邊。本功能尚未在 00_決策清單拍板 |
