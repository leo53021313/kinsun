# 類別關係文件 - 金孫 KinSun

> **版本:** v1.14 | **更新:** 2026-07-25 | **狀態:** ✅ 定稿（話題新聞 D-74：NewsStore／NewsFetcher 入表，三件套 18 組）
> 依 2026-07-25 重掃（**44 個 Protocol**）；核心類別圖逐群補齊，全 Protocol 清單見 §8。

---

## 1. 核心類別圖：帳號與同意

（2026-07-10 群 5 深潛重畫：移除幽靈欄位 `can_view_transcript`（DDL 已 DROP，己-8）；補憑證層三類別（GuardianAccount／ElderAccount／ApiToken）與己-6 方法（`login_elder`／`register_elder_account`／`revoke_elder_device`）；補兩個 Gate 與其窄 Protocol。）

```mermaid
classDiagram
    direction LR
    class AccountStore {
        <<Protocol，27 法／9 表>>
        +save_elder() +get_elder()
        +save_channel_binding() +get_channel_binding()
        +list_channel_bindings_for_principal()
        +save_consent() +get_consent()
        +save_invite() +get_invite()
        +save_guardian_account() +get_guardian_account_by_email()
        +save_elder_account() +get_elder_account_by_phone()
        +save_api_token() +get_api_token() +remove_api_token()
        +remove_api_tokens_for_principal()
        +transaction()
    }
    class PgAccountStore
    class FakeAccountStore
    class AccountService {
        -repo: AccountStore
        +create_elder() +generate_invite() +redeem_invite()
        +register_guardian_account() +login_guardian()
        +register_elder_account() +login_elder()
        +bind_elder_device() +revoke_elder_device()
        +consented_elder_id() +bound_elder_id()
        +has_valid_consent() +authenticate_token() +logout()
    }
    class ConsentGate { -resolver: ElderResolver +resolve_elder() 查綁定＋驗同意 }
    class AllowAllGate { -resolver: BindingResolver +resolve_elder() 只查綁定（dev） }
    class ElderResolver { <<Protocol>> +consented_elder_id() }
    class BindingResolver { <<Protocol>> +bound_elder_id() }
    class passwords { <<module>> +hash_password() +verify_password() scrypt N=16384 }

    class Elder { +elder_id +name }
    class Guardian { +guardian_id +name }
    class ElderGuardian { +role: PRIMARY/GUARDIAN +escalation_order }
    class Consent { +consent_by: SELF/PROXY +version +granted_at +revoked_at }
    class ChannelBinding { +channel: LINE/APP +external_id +principal_type +principal_id }
    class Invite { +code +elder_id +role +expires_at +max_attempts +attempts +used_at }
    class GuardianAccount { +guardian_id +email +password_hash }
    class ElderAccount { +elder_id +phone +password_hash }
    class ApiToken { +token_hash +principal_type +principal_id }

    PgAccountStore ..|> AccountStore
    FakeAccountStore ..|> AccountStore
    AccountService ..> AccountStore : 依賴介面（含 transaction seam）
    AccountService ..> passwords
    ConsentGate ..> ElderResolver
    AllowAllGate ..> BindingResolver
    AccountService ..|> ElderResolver : 滿足
    AccountService ..|> BindingResolver : 滿足
    Elder "1" o-- "many" ElderGuardian
    Guardian "1" o-- "many" ElderGuardian
    Elder "1" o-- "0..1" Consent
    Elder "1" o-- "0..1" ElderAccount
    Guardian "1" o-- "0..1" GuardianAccount
    ChannelBinding ..> Elder : principal 對映
    ChannelBinding ..> Guardian : principal 對映
    ApiToken ..> Elder : principal 對映（長輩家屬共用機制）
    ApiToken ..> Guardian : principal 對映
    Invite ..> Elder
```

> `Consent.revoked_at` 與 `consent_revoked` 403 路徑全備但**無生產寫入點**（D-13 不做撤回入口，05 差距 A-51）。`AccountStore` 為刻意的寬 Repository（同一限界上下文＋共享交易 seam），拆分軸線應為子聚合而非讀寫（見 §9）。

## 2. 核心類別圖：語音管線與安全守護

（2026-07-10 群 1 深潛重畫：補 ToolRegistry／Transport seam／TextSender 反轉／Mock-Dgx 雙實作；RiskDetector 門檻更正為單一 `mid=0.4`，✅ D-72。）

```mermaid
classDiagram
    direction TB
    class VoicePipeline {
        -asr: ASRClient
        -agent: CareAgent
        -tts: TTSClient
        -detector: RiskDetector
        -notifier: Notifier
        +process(audio, elder_id) TtsResult
        +process_text(text, elder_id) TtsResult
    }
    class CareAgent { -llm: LLMClient -session: SessionMemory -tools: ToolRegistry -max_tool_iters=3 +handle() +proactive(recall) }
    class ToolRegistry { +register(spec, handler) +specs() +dispatch(name, args) }
    class RiskDetector { -classifier: RiskClassifier -mid=0.4 +assess(text) }
    class RiskClassifier { <<Protocol>> +classify(text) }
    class LlmRiskClassifier
    class Notifier { <<Protocol>> +notify(elder_id, assessment) }
    class GuardianNotifier { -directory: GuardianDirectory -router: TextSender }
    class TextSender { <<Protocol>> +send_text(principal_type, principal_id, text) }
    class ChannelRouter
    class RiskAssessment { +tier: RiskTier +confidence +signals }
    class LLMClient { <<Protocol>> +generate() +generate_tool_turn() }
    class GeminiClient
    class ASRClient { <<Protocol>> +transcribe(audio) }
    class MockAsrClient
    class DgxAsrClient
    class TTSClient { <<Protocol>> +synthesize(text) TtsResult }
    class TextBubbleTts
    class DgxTtsClient
    class Transport { <<Protocol>> +request(method, url) Response }
    class HttpxTransport

    VoicePipeline *-- CareAgent
    VoicePipeline *-- RiskDetector
    VoicePipeline ..> ASRClient
    VoicePipeline ..> TTSClient
    VoicePipeline ..> Notifier
    CareAgent ..> LLMClient
    CareAgent ..> ToolRegistry
    RiskDetector ..> RiskClassifier
    RiskDetector ..> RiskAssessment : 產生
    LlmRiskClassifier ..|> RiskClassifier
    LlmRiskClassifier ..> LLMClient
    GuardianNotifier ..|> Notifier
    GuardianNotifier ..> TextSender : ✅ D-18 插座
    ChannelRouter ..|> TextSender : 組裝處注入
    GeminiClient ..|> LLMClient
    MockAsrClient ..|> ASRClient
    DgxAsrClient ..|> ASRClient
    TextBubbleTts ..|> TTSClient
    DgxTtsClient ..|> TTSClient
    DgxAsrClient ..> Transport
    DgxTtsClient ..> Transport
    HttpxTransport ..|> Transport
```

> ✅ D-18 已完成（2026-07-10 實證）：`GuardianNotifier` 依賴 `TextSender` Protocol（`notifier.py:18`），safety 無任何 channels import；`ChannelRouter` 由組裝處注入（`composition.py:161`）。thought_signature 等 Gemini 協定細節全部封裝於 `GeminiClient`（`llm.py:115-136`），CareAgent 對簽章透明。

## 3. 核心類別圖：記憶與通道

（2026-07-10 群 2a 深潛重畫：欄位正名 `_short_term/_long_term/_facts`、`MemoryStore` 補齊五法、`TurnContext`／`InjectedContext` 結構化、通道側補 `AppOutboundChannel`。）

```mermaid
classDiagram
    direction LR
    class SessionMemory {
        -_short_term: MemoryStore
        -_long_term: LongTermStore
        -_facts: List~FactProvider~
        +assemble(elder_id, query) TurnContext
        +record_turn(elder_id, *messages)
    }
    class MemoryStore { <<Protocol>> +append() +recent() +previous_day() +sessions() +last_active() }
    class PgMemoryStore
    class FakeMemoryStore
    class LongTermStore { <<Protocol>> +add(provenance, occurred_on) +search(query, top_k) +list_for_elder() }
    class Mem0LongTermStore { -_memory: mem0 +add() +search() }
    class FactProvider { <<Protocol>> +facts(elder_id) FactSection? }
    class MedicationFacts
    class AppointmentFacts
    class TurnContext { +injected: InjectedContext +history +system_suffix() }
    class InjectedContext { +memories: List~MemoryItem~ +sections: List~FactSection~ }
    class MemoryItem { +text +provenance +date }
    class FactSection { +title +items }
    class ChannelRouter { -directory: BindingDirectory -channels: Map +send_text() +has_route() }
    class OutboundChannel { <<Protocol>> +send_text(external_id, text) }
    class LineOutboundChannel
    class AppOutboundChannel
    class InboundMessage { +channel +external_id +kind +reply() +reply_voice() }

    PgMemoryStore ..|> MemoryStore
    FakeMemoryStore ..|> MemoryStore
    Mem0LongTermStore ..|> LongTermStore
    MedicationFacts ..|> FactProvider
    AppointmentFacts ..|> FactProvider
    SessionMemory ..> MemoryStore
    SessionMemory ..> LongTermStore
    SessionMemory ..> FactProvider
    SessionMemory ..> TurnContext : 產生
    TurnContext *-- InjectedContext
    InjectedContext *-- MemoryItem
    InjectedContext *-- FactSection
    Mem0LongTermStore ..> MemoryItem : 產生
    LineOutboundChannel ..|> OutboundChannel
    AppOutboundChannel ..|> OutboundChannel
    ChannelRouter ..> OutboundChannel
```

> `Mem0LongTermStore`＝ACL＋專案自寫邏輯（user query＋HEALTH_QUERY 雙查詢合併、去重、新到舊排序、失效退化空清單）；排版統一在 `models.py:format_injected_context`（「新覆舊」recency 前言＋出處·日期註記）。

## 4. 核心類別圖：RAG 檢索鏈

（2026-07-10 群 2b 深潛新增；2026-07-17 更新——`DocumentLoader`／`StaticDocumentLoader`／`InMemoryVectorStore` 已退役刪除（✅ 庚-34），餘四 Protocol＋雙管線服務鏈。）

```mermaid
classDiagram
    direction TB
    class EmbeddingModel { <<Protocol>> +embed(text) }
    class QueryEmbeddingModel { <<Protocol>> +embed_query(text) +embed_document(text, title) }
    class GeminiEmbeddingModel { +embed_documents(texts, title) +timeout +batch_size }
    class CharacterHashEmbedding
    class HybridVectorStore { <<Protocol>> +add() +search(qv, top_k) +keyword_search(query, top_k) }
    class RagWriteStore { <<Protocol>> +save_document() +save_discovery_document() +log_ingestion() }
    class PgVectorStore
    class PgRagReleaseStore { +begin_release() +evaluate_and_publish() +rollback() +cleanup() }
    class HealthEducationRetriever { +retrieve(query, top_k) }
    class HealthEducationRagService { +answer(query, has_risk_signal) RagAnswer }
    class AnswerPolicy { +build_answer() 五分支確定性閘門 }
    class IngestionPipeline { +ingest_documents() 逐文件失敗隔離 }
    class SourceRegistry { 人工核准清冊 }
    class SourceValidator { +validate(source) 入庫前把關 }
    class HealthEducationCrawler { +crawl() allowlist 白名單 }

    EmbeddingModel <|.. QueryEmbeddingModel
    QueryEmbeddingModel <|.. GeminiEmbeddingModel
    QueryEmbeddingModel <|.. CharacterHashEmbedding
    HybridVectorStore <|.. PgVectorStore
    RagWriteStore <|.. PgVectorStore
    HealthEducationRetriever ..> QueryEmbeddingModel
    HealthEducationRetriever ..> HybridVectorStore
    HealthEducationRagService ..> HealthEducationRetriever
    HealthEducationRagService ..> AnswerPolicy
    IngestionPipeline ..> RagWriteStore
    IngestionPipeline ..> QueryEmbeddingModel
    HealthEducationCrawler ..> SourceRegistry : allowlist 來源
    SourceValidator ..> SourceRegistry
```

> 線上工具接點：`build_health_rag_handler` 包 `HealthEducationRagService`（tools/health_rag.py），經 ToolRegistry 供 CareAgent 呼叫；rerank／citation 為純函式（reranker.py／citation.py），不入類別圖。

## 5. 核心類別圖：健康領域

（2026-07-10 群 3 深潛新增。雙生領域六檔同構——可作新領域樣板；GuardianScope 屬群 5 帳號守門，此處僅示消費關係。）

```mermaid
classDiagram
    direction TB
    class MedicationStore { <<Protocol>> +save() +list_for_elder() +list_for_slot() +remove() }
    class PgMedicationStore
    class FakeMedicationStore
    class MedicationService { +save() +list_for_elder() +update() +remove() }
    class MedicationFacts { +facts(elder_id) FactSection? }
    class AppointmentStore { <<Protocol>> +save() +list_for_elder() +list_for_date() +remove() }
    class PgAppointmentStore
    class FakeAppointmentStore
    class AppointmentService { +save() +upcoming(today) +update() +remove() }
    class AppointmentFacts { +facts(elder_id) FactSection? }
    class FactProvider { <<Protocol>> +facts(elder_id) FactSection? }
    class ReminderLogStore { <<Protocol>> +record() +list_for_elder() }
    class ConversationSummaryStore { <<Protocol>> +save() +get_for_date() +list_for_elder() }
    class build_health_report { 近30天危急＋提醒純聚合 }
    class build_medication_slot_job { 4個daily cron 只推長輩 }
    class build_appointment_reminder_job { 今明兩窗 長輩＋家屬 }

    PgMedicationStore ..|> MedicationStore
    FakeMedicationStore ..|> MedicationStore
    PgAppointmentStore ..|> AppointmentStore
    FakeAppointmentStore ..|> AppointmentStore
    MedicationService ..> MedicationStore
    AppointmentService ..> AppointmentStore
    MedicationFacts ..|> FactProvider
    AppointmentFacts ..|> FactProvider
    MedicationFacts ..> MedicationService
    AppointmentFacts ..> AppointmentService
    build_medication_slot_job ..> MedicationStore : list_for_slot
    build_appointment_reminder_job ..> AppointmentStore : list_for_date
    build_medication_slot_job ..> ReminderLogStore : safe_record
    build_appointment_reminder_job ..> ReminderLogStore : safe_record
    build_health_report ..> ReminderLogStore
    build_health_report ..> RiskEventStore
    class RiskEventStore { <<Protocol，群4>> +record() +list_for_elder() }
```

> routers 消費：medications／appointments routers → 各 Service；reports router → build_health_report＋ConversationSummaryStore（daily-summaries，✅ 己-3）；三者皆經家屬雙認證＋GuardianScope 404 守門。FactProvider 注入點見 §3（SessionMemory）。

## 6. 核心類別圖：排程與主動關懷

（2026-07-10 群 4 深潛新增。安全守護類別已在 §2 重畫，此處補排程核心＋通知留痕＋App 內通知；GuardianNotifier 三插座全 Protocol 化。）

```mermaid
classDiagram
    direction TB
    class Scheduler { +run_due() -_is_due() 首見種基準＋補跑一次 }
    class Job { <<frozen>> +name +cron +run }
    class ScheduleStateStore { <<Protocol>> +get_last_run() +set_last_run() }
    class PgScheduleStateStore
    class FakeScheduleStateStore
    class fanout_job { 每日單點 cron＋逐筆隔離無重試 }

    class GuardianNotifier { +notify() 全家屬齊發無冷卻 }
    class GuardianDirectory { <<Protocol>> +guardians_of(elder_id) }
    class TextSender { <<Protocol>> +send_text(ptype, pid, text) int }
    class DeliveryLog { <<Protocol>> +record(...delivered) }
    class RiskNotificationLogStore { <<Protocol>> +record() +list_for_elder() }
    class ChannelRouter

    class AppOutboundChannel { +send_text()＝落庫 }
    class AppNotificationStore { <<Protocol>> +record() +list_for_external_ids() }

    class build_greeting_job { 每半小時掃描 各長輩偏好時刻 greeted_today 冪等 }
    class build_inactivity_job { 失聯 last_active≥2天 只計 user turn }
    class GreetingPreferenceStore { <<Protocol>> +save() +get_for_elder() +list_all() }
    class CareAgent { +proactive(elder_id, intent) 不跑工具迴圈 }

    Scheduler ..> Job
    Scheduler ..> ScheduleStateStore
    PgScheduleStateStore ..|> ScheduleStateStore
    FakeScheduleStateStore ..|> ScheduleStateStore
    fanout_job ..> Job : 產生
    build_greeting_job ..> fanout_job
    build_inactivity_job ..> fanout_job
    build_greeting_job ..> GreetingPreferenceStore : 偏好時刻（無偏好退回預設 8 時）
    build_greeting_job ..> CareAgent : _push_to_elder
    build_inactivity_job ..> CareAgent : _push_to_elder
    GuardianNotifier ..> GuardianDirectory
    GuardianNotifier ..> TextSender : ✅ D-18 插座
    GuardianNotifier ..> DeliveryLog : ✅ D-36 送達留痕
    ChannelRouter ..|> TextSender
    RiskNotificationLogStore <.. DeliveryLog : Pg 實作滿足
    ChannelRouter ..> AppOutboundChannel
    AppOutboundChannel ..> AppNotificationStore : D-12 過渡落庫
```

> worker 組裝 13 個 job（整理／問候／失聯／用藥×4／回診／音檔清理×2／觀測清理／新聞爬取＋新聞清理，D-74）；整理 job 的夜間批次為四步（Mem0 整理→每日摘要→每晚反思→問候時間計算，後三步各自 try/except 互不拖累）；A-42 已解（✅ 庚-17：`try_claim` 條件式 UPDATE 原子搶占）。危急偵測（RiskDetector 融合矩陣）見 §2 與 05 §8.5。

## 7. 設計模式

| 模式 | 應用 | 目的 |
| :--- | :--- | :--- |
| Repository（三件套） | 全部持久層：Protocol＋Pg＋Fake 同檔 | 業務不碰 SQL；Fake 支撐離線測試；合約測試保等價 |
| 依賴注入（建構子、多 keyword-only） | 所有 Service／Pipeline／Agent | 組裝集中三個根；可測性 |
| Facade | `SessionMemory`（三層記憶單一門面）、`VoicePipeline` | 呼叫端不知內部協作者 |
| Adapter／ACL | `channels/line`（LINE SDK）、`GeminiClient`、`Mem0LongTermStore`、`speech/*` | 外部 schema 變動不進領域 |
| Strategy | `ConsentGate` vs `AllowAllGate`；`MockAsrClient` vs `DgxAsrClient` | 環境切換不改呼叫端 |
| Factory | `build_asr_client`／`build_tts_client`／`build_*_job`／`build_audio_publisher` | 依設定選實作 |
| Registry | `ToolRegistry`（register／specs／dispatch，dispatch 永不拋，`tools/registry.py:13`） | 工具增減不改 agent；工具故障不中斷對話 |
| Null Object | `_NullBinding`（`channels/app/turns.py:30`，handle 恆回 None） | App 通道無綁定流程，dispatch 介面不分岔 |
| Collector | `_TurnCollector`（`channels/app/turns.py:37`，收 reply 轉同步 JSON） | 讓 App 一次 HTTP 往返重用「回呼式」共用 dispatch |
| Protocol 注入 seam | `Transport`（`transport.py`）注入各 HTTP client | 測試注入 FakeTransport（或給 `HttpxTransport` 注入 `httpx.MockTransport`），不動真網路 |
| Pipeline | `IngestionPipeline`（rag/ingestion.py）：文件→chunk→embedding→store，逐文件失敗隔離＋稽核留痕 | 單篇失敗不中斷整批 |
| Policy 閘門 | `SourceValidator`（入庫前）、`AnswerPolicy`（回答前五分支） | 確定性程式碼管安全，優先於 LLM 生成 |

## 8. Protocol 總表（**44 個**，2026-07-25 重掃；原記 42）

| Protocol（位置） | 實作 |
| :--- | :--- |
| AccountStore／MedicationStore／AppointmentStore／BindingSessionStore／MemoryStore／TraceStore／RiskEventStore／RiskNotificationLogStore／AppNotificationStore／ReminderLogStore／ConversationSummaryStore／ScheduleStateStore／ConsolidationLogStore／StrategyStore／WebSearchLookupStore／LocationStore／GreetingPreferenceStore／NewsStore | 各自 Pg＋Fake 三件套（18 組，皆有合約測試；NewsStore＝D-74） |
| LongTermStore（memory/longterm/store.py） | Mem0LongTermStore（ACL 非三件套：Fake 在 tests/fakes.py、無合約測試——刻意例外，載明於 05 差距 A-24） |
| FactProvider（memory/recall.py） | ElderProfileFacts（稱謂，首位）、MedicationFacts、AppointmentFacts、StrategyFacts、LocationFacts（末位） |
| RiskClassifier／Notifier／GuardianDirectory／TextSender／DeliveryLog（safety/） | LlmRiskClassifier；LogNotifier、GuardianNotifier；AccountService（duck）；ChannelRouter（✅ D-18 插座）；PgRiskNotificationLogStore（✅ D-36 送達留痕） |
| OutboundChannel／BindingDirectory／LineMessenger（channels/） | LineOutboundChannel＋Fake；AccountStore（duck）；LineApiMessenger |
| LLMClient（llm.py） | GeminiClient |
| ASRClient／TTSClient（speech/） | Mock＋Dgx 各二 |
| Transport（transport.py） | HttpxTransport、FakeTransport |
| Executor（db.py） | `Database`（池版，自動 commit）、`_ConnExecutor`（單交易連線）、`_Errors`（例外轉譯層）——非 psycopg cursor 直接充當（2026-07-10 群 6 更正）。註：Protocol 未宣告 `transaction()`，`_Errors` 需 type: ignore（05 差距 A-58） |
| LiffVerifier（web/auth.py） | LineIdTokenVerifier |
| AudioPublisher（audio/publisher.py） | SupabaseAudioPublisher |
| Profiles（binding/flow.py）／ElderResolver／BindingResolver（binding/gate.py） | AccountService（duck，同時滿足兩 Resolver）；消費者＝ConsentGate、AllowAllGate |
| EmbeddingModel／QueryEmbeddingModel／RagWriteStore／HybridVectorStore（rag/） | GeminiEmbeddingModel、CharacterHashEmbedding（測試）；PgVectorStore。註：RAG 持久層非三件套、無合約測試（刻意例外，A-32 載明）；DocumentLoader／InMemoryVectorStore 已退役刪除（✅ 庚-34） |
| RateLimiter（web/ratelimit.py） | PgRateLimiter（Postgres 滑動視窗，跨進程共享，正式組裝）、SlidingWindowRateLimiter（記憶體版，測試／單 worker fallback）（✅ 庚-08） |
| Reflector（strategies/reflection.py） | GeminiClient（duck，每晚反思與摘要共用 GEMINI_MODEL_SUMMARY） |
| NewsFetcher（news/fetchers/protocol.py） | MohwNewsFetcher（衛福部爬蟲，免金鑰）、NewsApiFetcher（newsapi.org，需金鑰）——新增新聞來源＝新增一個 fetcher 實作，不動 store／jobs（D-74） |

## 9. SOLID 檢核

- [x] S 單一職責：模組總表（07 §1）每模組一句話職責成立
- [x] O 開放封閉：新通道＝新增 adapter＋bindings 列，不改領域
- [x] L 里氏替換：合約測試強制 Fake／Pg 等價（18 組）
- [x] I 介面隔離：Protocol 平均 2–5 個方法；AccountStore 27 方法偏大，但屬**合理的寬 Repository**（同一限界上下文＋共享 `transaction()` seam 使 Elder 聚合跨表寫入原子完成）。若拆分，軸線應為**子聚合**（Identity／Consent／Invite／Binding／Credential）＋UnitOfWork，而非讀寫 CQRS（2026-07-10 群 5 深潛評估；現行規模維持單一 Store 為 YAGNI）
- [x] D 依賴反轉：44 Protocol；GuardianNotifier→ChannelRouter 已反轉（✅ D-18 完成，`TextSender` 插座＝notifier.py:18，2026-07-10 實證），無已知例外

## 變更紀錄

| 版本 | 日期 | 變更 |
| :--- | :--- | :--- |
| v1.0 | 2026-07-08 | 初版：三群核心類別圖＋31 Protocol 總表＋SOLID 檢核 |
| v1.1 | 2026-07-10 | 群 1 深潛：§2 類別圖重畫（ToolRegistry／Transport seam／TextSender／Mock-Dgx 雙實作；門檻更正 mid=0.4）；§4 補 Registry／Null Object／Collector／Transport seam 四模式；§6 D-18 檢核更新 |
| v1.2 | 2026-07-10 | 群 2a 深潛：§3 類別圖重畫（欄位正名、MemoryStore 五法、TurnContext／InjectedContext 結構化、AppOutboundChannel）；§5 LongTermStore 補 ACL 例外註記 |
| v1.3 | 2026-07-10 | 群 2b 深潛：新增 §4 RAG 檢索鏈類別圖（後續章節順延改號 §5～§7）；設計模式補 Pipeline／Policy 閘門；Protocol 總表 RAG 列補實作與例外註記 |
| v1.4 | 2026-07-10 | 群 3 深潛：新增 §5 健康領域類別圖（雙生領域樣板；後續章節順延 §6～§8） |
| v1.5 | 2026-07-10 | 群 4 深潛：新增 §6 排程與主動關懷類別圖（GuardianNotifier 三插座＋D-12 落庫鏈；後續章節順延 §7～§9） |
| v1.6 | 2026-07-10 | 群 5 深潛：§1 帳號類別圖重畫（移除幽靈欄位 can_view_transcript；補憑證層三類別＋己-6 方法＋兩 Gate 與窄 Protocol） |
| v1.7 | 2026-07-10 | 群 6 深潛：§8 Protocol 總表 Executor 實作列更正（Database／_ConnExecutor／_Errors，非 psycopg cursor duck） |
| v1.8 | 2026-07-10 | 階段 F 收斂：Protocol 總數重掃更正 31→**36**（分布 30 檔）；§9 SOLID DIP 同步 |
| v1.9 | 2026-07-17 | 對齊程式現況：Protocol 36→**42**（新增 LocationStore／GreetingPreferenceStore／StrategyStore／Reflector／WebSearchLookupStore／RateLimiter／ConsolidationLogStore；DocumentLoader 退役）；§4 RAG 圖移除已刪類別；§6 問候 job 改自適應偏好時刻＋夜間批次四步＋A-42 已解；§8 三件套 10→17 組 |
| v1.10 | 2026-07-17 | 辛-9：`ConversationSummaryStore` 補 `get_for_date()`（主動推播讀「她上次開口那天」的摘要用）；`LongTermStore.add` 補 `occurred_on`（對話日，修記憶日期晚一天）；`CareAgent.proactive` 補 `recall` |
| v1.11 | 2026-07-17 | 稱謂注入：FactProvider 實作清單補齊為五段（ElderProfileFacts 首位、StrategyFacts／LocationFacts 入列）；`Elder` 補 `nickname` |
| v1.12 | 2026-07-18 | RAG 類別圖補 `PgRagReleaseStore`、`save_discovery_document` 與 Gemini 批次 timeout；failed release 僅復用原子完成且模型／角色／結構相容文件。 |
| v1.14 | 2026-07-25 | 話題新聞（D-74，PR #75 Babic）：§6 worker job 數 11→13（新聞爬取＋清理）；§8 Protocol 42→44（NewsStore 入三件套 18 組＋NewsFetcher 列）；§9 SOLID 數字同步。 |
