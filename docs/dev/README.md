# docs/dev — 開發文件（重規劃版）

> 依老師的 VibeCoding_Workflow_Templates（完整流程模式）重新規劃的全套開發文件。
> 設計依據：[docs/superpowers/specs/2026-07-07-docs-dev全套文件重規劃-design.md](../superpowers/specs/2026-07-07-docs-dev全套文件重規劃-design.md)。
> 內容基準：**as-is＋to-be 對照**；to-be 為一次性重構的契約。
> 鐵律：所有決策由 Leo／團隊拍板，AI 不代填；未拍板一律 ⚠ 待議，登記於 [00_決策清單.md](00_決策清單.md)。

## 文件狀態表

| # | 文件 | 狀態 | 最後更新 |
| :---: | :--- | :--- | :--- |
| 00 | [決策清單](00_決策清單.md) | ✅ D-01～D-80 全回填（**新增 D-80 App 內通知加呈現分級欄位**（2026-08-01 專案負責人裁決，當日完工＋審查補完）；**新增 D-79 回診「前一天」提醒改由後端推算＋已過期就略過**（2026-08-01 專案負責人裁決並當日完工；`meta.warnings` 的前端顯示待裁示，12 §9 F-19）；D-77 附近地點搜尋六項核定、**八個工作項全完成**，人工真 LLM 驗證 18/18 全對並追加修復一個 `place` 距離護欄 Critical；D-76 統一排程十二項核定、**五刀全完成**；D-38 掛起、4 項擱置；D-74 消費端完成；D-75 濫用審核採納並重寫接法；旗標預設開但支撐數字同日作廢待重跑） | 2026-08-01 |
| — | [2026-07-08 會議決策議程](2026-07-08_會議決策議程.md) | ✅ 決議已回填（會議紀錄） | 2026-07-09 |
| 02 | [專案簡報與PRD](02_專案簡報與PRD.md) | ✅ v1.4（US-C1／C2 改寫為統一排程、新增 US-C3 長輩用說的建立提醒；KPI 數值⏸實測後定；US-B3 問候自適應註記） | 2026-07-17 |
| 03 | [BDD情境](03_BDD情境.md) | ✅ 定稿（D-72 三級制已落地） | 2026-07-09 |
| 04 | [ADR（04_adr/）](04_adr/README.md) | ✅ 定稿（11 筆；ADR-003 補述 庚-54） | 2026-07-13 |
| 05 | [架構與設計](05_架構與設計.md) | ✅ v1.77（**`app_notifications` 加 `severity` 欄**（2026-08-01 Leo 裁決，本輪唯一 Schema 變動；舊列一律 `notice`）；**網頁版前端 P4 全分支審查修正**（2026-08-01，1 Critical＋1 Important＋6 Minor）：§1.1.1「網頁版前端（web/）」狀態欄補審查修正註記；無新增 Container／模組／Protocol、無 Schema 變動）；**網頁版前端 P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 全部 35 個工項最後一項**：不新增程式碼，僅跑全庫回歸（6 支 pytest＋ruff＋npm 四項＋e2e 全綠）並彙整人工驗收清單；五格跨瀏覽器矩陣與容量閘門實測數字**皆未執行**，見 12 §9 F-18／task-6-report.md）；**網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：新增 web/e2e/journey.spec.ts（Playwright，兩情境：完整旅程／服務停機擋人，打真後端）＋playwright.config.ts；CI 判斷暫不接（brief 給定必要環境變數不全＋起後端無健康檢查等待，本地已實測重現會崩潰，理由見 task-5-report.md）；**P4 Task 4 全分支審查修正**（0 Critical，4 Important＋5 Minor）：長輩欄 401 出口改走 ElderApp 的 loseSession（原本搶在 useTalk 既有出口前面靜默登出）；shownUpTo 補隨 visible 重設（原本切回可見會補播隱藏期間通知，最壞連播 70 秒）；補兩條原本無測試守著的接線；web 全套測試 501→**503**；**網頁版前端 P4 Task 4：兩欄接上通知**：接上 Task 1～3 三件成品（通知輪詢／通知橫幅／跨欄事件匯流排）；新增對稱 guardianVisible；severity: "alert" 未接線待裁決；web 全套測試 488→**501**；**網頁版前端 P4 Task 3：跨欄連動**：§1.1.1「網頁版前端（web/）」狀態欄補「＋跨欄連動」（`notify/bus.ts` 事件匯流排＋`prefilledCode` 發送端接線）；修正發送端接上後才現形的 P3 缺陷（`BindScreen` 補 render 期間比對＋調整）；web 全套測試 473→**487**；**P4 Task 2 審查修正**：`bannerRef` 擋 `<StrictMode>` 雙呼叫、新增 `onTokenRevoked` 401 出口、「第一次不補播歷史」改一律從 0 起跳；web 全套測試 464→**473**；**網頁版前端 P4 Task 2：通知輪詢**：§1.1.1「網頁版前端（web/）」狀態欄補「＋通知輪詢」（`notify/useNotificationFeed.ts`，純新增尚未接進畫面，未新增或調整任何 API 契約）；web 全套測試 445→**464**；**P4 Task 1 審查修正**：Container 清單補「審查修正」註記——live region 移到 PhoneFrame、size prop 放大字級與觸控目標；web 全套測試 438→**445**；**網頁版前端 P4 Task 1：模擬系統通知橫幅**：§1.1.1「網頁版前端（web/）」狀態欄補「＋通知橫幅」（`notify/NotificationBanner.tsx`／`osStyle.ts`，純新增尚未接上資料來源）；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§1.1.1「網頁版前端（web/）」狀態欄補「＋長輩提醒列表」，長輩端四畫面全數落地，P3 收尾；**網頁版前端 P3 Task 8：對講機**：§1.1.1「網頁版前端（web/）」狀態欄補「＋對講機」，新增 `elder/useTalk.ts`／`TalkScreen.tsx`／`Avatar.tsx`／`talk/mediaErrors.ts`，未新增或調整任何 API 契約；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：§1.1.1「網頁版前端（web/）」狀態欄補「＋長輩配對與帳密重登畫面」，新增 `elder/BindScreen.tsx`／`LoginScreen.tsx`／`ElderApp.tsx`，接上 `stage/StagePage.tsx` 取代原佔位元件，對講機與提醒兩畫面待 Task 8／9；**網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**：§1.1.1「網頁版前端（web/）」狀態欄補「＋長輩端 API 呼叫端與文案」，新增 `elder/api.ts`／`elder/location.ts`，端點與欄位逐一核對後端 router 全數一致，未新增或調整任何 API 契約；**網頁版前端 P3 Task 5：QR 掃碼**：§1.1.1「網頁版前端（web/）」狀態欄補「＋長輩端 QR 掃碼」（`talk/qrScanner.ts`，走 WebAssembly 版 ZXing）；**P3 Task 4 審查修正**：§1.1.1「網頁版前端（web/）」狀態欄補「＋錄音播放與 iOS 音訊解鎖」，補齊與 08 同次同步卻漏掉的半句；**§1.1.1 web/ 過期描述更正**（P3 Task 3 審查回合二順帶修正）：狀態欄由「現行（P1 地基…）」更正為「P1＋P2＋P3 對講機協定核心皆已完成」；**網頁版前端 P3 對講機容量閘門接線審查修正**（Task 2）：§8.1 關鍵流程更正 8→**10** 個分岔（補 429 每分鐘保險絲、503 容量閘門排隊逾時）；§6.2 節流一列更正——記憶體版 `SlidingWindowRateLimiter` 正式環境**確實**注入對講機 `turn:` 這第 5 個 scope（`app.py:405`），多 worker 上限是 ×worker 數，先前描述僅涵蓋認證四個 scope、隱含「正式環境無記憶體版節流」為誤導；三個 SPA 掛載點補單頁應用回退（`_SpaStaticFiles`，`/demo/stage` 從伺服器直接開不再 404）；網頁版前端 P1 地基：前端由三端擴為四端，Container 清單新增 web/ 一列，`/demo` 靜態掛載收攏進 `_static_mounts`；C1 回覆音檔隨 WS binary 訊框直送＋H-1 合併分類器評測接線；延遲優化四刀：進站上傳背景化／assemble 三段並行／record_turn 背景化並在交出回應前收斂／分級與審核合併一次呼叫（旗標預設 false）） | 2026-08-01 |
| 06 | [API設計規範](06_API設計規範.md) | ✅ v1.27（**通知端點加 `severity` 欄**（2026-08-01 Leo 裁決，非破壞性）；**回診提醒三處補正**（D-79 審查後補完，2026-08-01）：`POST` 與 `PUT` 的 `meta.warnings` 句子分開、兩顆都過期的 400 訊息改講真正的原因、消費端更正為「`web/` 已顯示」；**回診提醒改由後端推算**（D-79，2026-08-01）：`POST`／`PUT /schedules` 對 `kind=appointment`＋`event_date` 忽略 client 的 `occurrences`，改自 `event_date` 推算「前一天＋當天」；已過期的前一天改為略過而非整筆失敗，新增 `meta.warnings` 告知家屬（⚠️ 四端未消費）；**容量上限核定後更正 `queued` 說明的舉例**（2026-08-01）：`TURN_CONCURRENCY_LIMIT` 由暫定 6 核定為 2（每 worker）；**網頁版前端 P3 對講機容量閘門接線**（Task 2）：`WS /ws/talk` 下行新增 `queued` 訊框；`POST /turns` 超限（含排隊逾時）改回 503 `too_many_requests`（與認證節流 429 同碼不同狀態碼）；兩條路徑另加每位長輩每分鐘 30 輪節流保險絲；`demo-status` 補 single-flight 與探針齊全性 fail-fast；新增 `GET /api/v1/demo-status`：網頁版前端 P1 地基，公開免認證、快取 5 秒；`WS /ws/talk` 回覆音檔改隨 binary 訊框直送——自我描述訊框省掉「上傳 Supabase→App 再下載」兩趟；header 嵌同一訊框以免併發輪交錯配錯） | 2026-08-01 |
| 07 | [模組規格與測試](07_模組規格與測試.md) | ✅ v1.99（**補播的審查修正**（2026-08-01，1 Critical 續拉錯亂＋1 Important 字幕被搶）；**插嘴後前一題的答案改為補播**（2026-08-01 專案裁決，推翻 2026-07-31 的「一律丟棄」）；**iOS 音訊解鎖時機定案**（2026-08-01 專案裁決，選項 B）；**T3 審查補完（D-80／辛-24）**；**App 內通知加 `severity`**（2026-08-01 Leo 裁決）；**回診略過提示的新增／編輯分流**（D-79 補完，2026-08-01）：編輯時前一天那顆很可能已經送出過，改為只陳述事實；**回診兩顆鬧鐘的推算收斂進 `timeparse.py`**（D-79，2026-08-01）：§2 schedules 列補 `build_appointment_reminders`；RAG 爬蟲抓取範圍實測定案（推翻 v1.83 對導覽連結的判讀）；**網頁版前端 P4 全分支審查修正**（2026-08-01，1 Critical＋1 Important＋6 Minor）：新增 §7.3 逐項記載（Critical：分頁切回前景沒重建基準、背景期間累積的提醒連播最壞 70 秒，且 Task 4 那條 `visible` 修正在寬螢幕展示設定下永遠不觸發；Important：`notify/bus.ts` 存在理由與人工驗收 G4 是假前提；6 Minor）；§7.2 就地更正人工驗收清單 G4／G13；web 全套測試 503→**509**）；**P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 最後一項**：新增 §7.2，不新增程式碼，重跑 6 支指定 pytest（143 條）＋ruff＋npm 四項（503/503）＋e2e（chromium／firefox 4/4）全綠，主要產出為彙整人工驗收清單）；**P4 Task 5 審查修正**：`playwright.config.ts` 的 CI 已接宣稱更正為未接；`baseURL` 陷阱補進程式碼三處；`journey.spec.ts` 檔頭「長輩端收到通知」更正為「提醒出現在行程清單」；`??`→`||`；typecheck／lint 補涵蓋 `e2e/`；**網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：新增 §7.1，web/e2e/journey.spec.ts（Playwright，兩情境）；修 brief 缺陷（vitest exclude 補 e2e/**，已實測重現原本會誤跑）；CI 未接（必要環境變數不全＋無健康檢查等待，本地實測會 ConfigError 崩潰）；本機隔離測試庫實測 chromium／firefox 4/4 過；**P4 Task 4 全分支審查修正**（0 Critical，4 Important＋5 Minor）：Important 1 長輩欄 401 出口改遞增 tokenRevokedSeq 交 ElderApp 統一走 loseSession；Important 2 shownUpTo 補隨 visible 重設；Important 3／4 補兩條無測試守著的接線；5 Minor；web 全套測試 501→**503**；**網頁版前端 P4 Task 4：兩欄接上通知**：新增列 `stage/StagePage.tsx`（拆兩層＋接上 useNotificationFeed×2／NotificationBanner×2／useStageEvent）；`notify/`／`guardian/`／`elder/…` 三列補接線說明；web 全套測試 488→**501**；**網頁版前端 P4 Task 3：跨欄連動**：新增列 `notify/bus.ts`（`emitStageEvent`／`useStageEvent`）＋`elder/BindScreen.tsx`（P4 Task 3 追加修正）；`guardian/` 列補 `onSendCodeToElder` 說明；變異驗證發現一條等價變異（`set.delete(listener)`，已記錄）；web 全套測試 473→**487**；**P4 Task 2 審查修正**（0 Critical，5 Important＋6 Minor）：`notify/` 一列補審查修正——「第一次不補播歷史」改一律從 0 起跳（原本從已讀水位起跳、實測掛載即播出 7 則）；`<StrictMode>` 下 `bannerRef` 擋 `setBanner` updater 雙呼叫；換人／登出補 `unread` 歸零；新增 `onTokenRevoked` 401 出口（接線待 Task 3／4）；三條資源釋放路徑改用 `vi.getTimerCount()`／spy 精確驗證（純 API 呼叫次數斷言因 `alive` 旗標而不夠精確）；新增 8 條測試（19→**27**）＋`guardian/api.test.ts` 1 條，web 全套測試 464→**473**；RAG 爬蟲改內容優先（真實 A/B 文章數 0→19）；**網頁版前端 P4 Task 2：通知輪詢**：`notify/` 一列擴充新增 `pickNewItems`／`useNotificationFeed`；修正 brief 缺陷（`unread` 恆為 0、佇列無上限）；新增修正競態（換人時舊資料晚到，`sessionRef`）；偏離 brief（分頁可見性暫停、品牌字串改讀 `strings.gate.brand`、換人清空改走 render 期間比對以符合 `eslint-plugin-react-hooks` v7 新規則）；新增 19 條測試，web 全套測試 445→**464**；**P4 Task 1 審查修正**（0 Critical，3 Important＋4 Minor）：`notify/` 一列補審查修正——`size` prop 放大長輩端字級與關閉鍵、live region 移到 `stage/PhoneFrame.tsx`、`prefersReducedMotion` 抽到新增的 `stage/reducedMotion.ts`、卡片 `pointer-events` 分工、`BannerItem` 新增 `severity` 型別欄位；全部逐條變異驗證，web 全套測試 438→**445**；**網頁版前端 P4 Task 1：模擬系統通知橫幅**：`notify/` 一列擴充新增 `NotificationBanner.tsx`／`osStyle.ts`；修正 brief 缺陷 `strings.common.close` 鍵不存在；偏離 brief 的 CSS `prefers-reduced-motion` 寫法改採 JS `matchMedia` 判斷（與 `TearTransition.tsx` 一致）；新增 14 條測試，web 全套測試 424→**438**；**網頁版前端 P3 全分支審查修正**（2 Critical＋3 Important＋5 Minor＋1 支死鍵，全部是工項之間的接縫）：§7 表後新增一段——家屬換綁定碼後長輩永久卡死（401 先於 403，長輩端零個畫面處理 401）、開錄當下索取定位權限、閘門婉拒人話被降級路徑吃掉、寬螢幕 `elderVisible` 死路、提醒載入失敗借用家屬端字串；web 全套測試 407→**424**；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§7 新增 `elder/NotificationsScreen.tsx` 一列，長輩欄四路由全數落地，`ElderApp` `default` 分支改為 `never` 窮盡檢查、`strings.common.comingSoon` 死碼已移除；修正 brief「錯誤取代空狀態」缺陷（`hasError` 獨立布林旗標取代同時 `setItems([])`＋`setError(...)`）；補齊已讀水位取最新一則與寫入失敗隔離兩項既有不變量；新增 10 條測試（含 `ElderApp.test.tsx` 路由整合測試），web 全套測試 397→**407**；**P3 Task 8 裁決回應**：收音期間抵達的回覆由「收下來、錄完補播」改為**一律丟棄**（✅ 專案裁決——同一類東西不該因抵達時機不同而待遇不同；突然冒出來的舊答案比沒有回答更讓人困惑），但**不靜默丟棄**，新增 `strings.talk.thinkingAfterSkipped`；`revokeQueuedReplyAudio` 的陣列參數隨補播機制一併收回。誠實記載：`clear()` 因此與新守門重疊、變異一度全綠，已補測試釘住它仍單獨承重的路徑。web 全套測試 396→**397**；**P3 Task 8 審查修正**（3 Critical＋4 Important＋5 Minor）：Critical 1 iOS 解鎖粒度（每頁一次→每顆播放器一次，`WeakSet`——切一次頁籤或登出重新配對之後，iPhone 只看得到字、聽不到任何聲音且本次載入內永久如此）；Critical 2 收音期間抵達的訊框先收下來、錄完補播（原本會在長輩講話時放音，金孫自己的聲音進 ASR）；Critical 3 播放回呼與「被打斷」賽跑（原本打斷一則 30 秒回覆後下一句語音要等 29 秒、期間麥克風鍵全程停用；修在 `useTalk` 這一層，`talkSocket.ts` 一個字元未動）；Important 第十八個假測試改寫、`playback.ts` 過期註解更正、登出確認叫長輩去用一組不存在的帳密、舊連線 `onclose` 競態；Minor `aria-modal` 宣告了卻沒有模態性＋三處無測試的行為補齊。新增 12 條測試，web 全套測試 384→**396**；**網頁版前端 P3 Task 8：對講機**：§7 新增三列 `talk/mediaErrors.ts`／`elder/useTalk.ts`／`elder/TalkScreen.tsx`＋`Avatar.tsx`；`talk/recorder.ts` 補 `probeMicrophone`（六種結果非布林值，測試 9→**16**）；`talk/playback.ts` 的「`queue.clear()` 尚未接上」已知缺口**收斂**；`elder/BindScreen…` 列 talk 路由不再是佔位。修正 brief **十一處**缺陷（`clear()` 未回收 blob URL、無 `visible` 概念、等不到回話與有字沒聲音兩條會永遠卡在「在想」、空錄音照送、登出無二次確認、無指標捕捉、次要按鈕低於長輩端下限、`aria-label` 裸中文字串、`size-26` 不是 Tailwind 級距、`probeMicrophone` 簽章）；實測抓到 brief **一條假測試**（「錯誤訊框」從待機餵 error 再斷言 avatar 是 idle——它本來就是 idle）與自寫測試的兩個「恰好通過」並全部改寫。新增 55 條測試，web 全套測試 329→**384**；**P3 Task 7 審查修正**（1 Critical＋3 Important＋6 Minor）：Critical——雙欄舞台窄螢幕切頁籤不會關閉相機（非活動欄僅 CSS `hidden`、`MediaStream` 軌道不受影響），新增 `visible` prop 鏈路（`StagePage`→`ElderApp`→`BindScreen`），切走停止掃描、切回自動恢復；Important 1——`LoginScreen` 422 誤講成「連線失敗」改用 `apiErrorMessage`；Important 2——手打綁定碼與帳密登入補上 method／路徑／body 斷言（審查實測「截斷送出」「欄位對調」兩變異原本全綠）；Important 3——`ErrorText`／`Field` 補 `size="big"` 放大長輩端字級；6 Minor。新增 11 條測試，web 全套測試 318→**329**；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：新增 §7 一列 `elder/BindScreen.tsx`／`LoginScreen.tsx`／`ElderApp.tsx`；修正 brief 三處缺陷（`BIND_ERRORS`／`SCANNER_ERRORS` 補齊 409 `invite_wrong_role`＋`QrScannerError` 六種全覆蓋，原始 `SCANNER_ERRORS` 型別下連 tsc 都過不了；佔位分支裸中文字串改走 `strings.common.comingSoon`；Step 1 測試斷言了 Task 8 才有的畫面已改寫）；偏離點（接線 `stage/StagePage.tsx`＋連帶修正 `App.test.tsx` 一條因按鈕同名失去辨別力的既有測試；`loginLink` 補 `size="big"`）；記載建置驗證兩個已知落差（`createQrScanner` 於 minify 建置 grep 為 0 屬識別碼重命名；`jsdelivr` 字面值屬 zxing-wasm 套件自身預設值且已被覆寫不可觸達）；新增 20 條測試，web 全套測試 298→**318**；**P3 Task 6 審查修正**（4 Important＋5 Minor，0 Critical）：`postTurn` 三條測試補 `init.body` 斷言；更正「帶空字串會讓後端寫入空地名」的錯誤敘述與「同名不同義」的不精確措辭；`elder/location.ts::currentPlace` 成功取得座標的分支改為也回 `null`（原本回傳含空地名物件，`blur()` 移除）；Task 6 淨新增測試 13→**12** 條，web 全套測試 299→**298**；**網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**：新增 §7 兩列 `elder/api.ts`（六支端點呼叫端，慣例比照 `guardian/api.ts`）與 `elder/location.ts`（`currentPlace`）；端點與欄位逐一核對後端 router 與 06 §3，全數一致，未新增或調整任何 API 契約；補齊 brief 兩處測試涵蓋缺口（`logoutSession`／`listElderNotifications` 契約測試、`elder/location.test.ts`）；新增 §9 F-17 已知限制——網頁無反查地名 API；新增 13 條測試（全部逐條變異驗證），web 全套測試 286→**299**；**P3 Task 5 審查修正**（4 Important＋8 Minor，0 Critical）：`qrScanner.ts` 補 `play()` 等待期間計時器洩漏、`inFlight` 防疊層、`QrScannerError` 六種分類、`play()` 失敗與第一幀看門狗回報 `no-signal`；另補 canvas 重複使用、`invokeCallback`、`verify-wasm-checksum.mjs`、`import.meta.env.BASE_URL`；新增 9 條測試（7→16），web 全套 277→**286**；**網頁版前端 P3 Task 5：QR 掃碼**：新增 §7 一列 `talk/qrScanner.ts`（`createQrScanner`，走 WebAssembly 版 ZXing）；修正 brief 兩處缺陷（`overrideWasmUrl` 選項不存在改用 `overrides.locateFile`；解碼飛行中被 `stop()` 仍會回報，補 `stopped` 重新檢查）；新增 `QrScannerDeps` 注入點測去重與停止後不再回報；新增 7 條測試，web 全套 270→**277**；**P3 Task 4 審查修正**（6 Important＋6 Minor，0 Critical）：`recorder.ts` 補重入保護／`stop()` try-finally＋保險逾時三項；`playback.ts` 補 iOS 解鎖無聲檔 `ended` 抑制／`revokeQueuedReplyAudio`（佇列 `clear()` 尚未接上，已知缺口）；`audioUnlock.ts` 補呼叫順序斷言；新增 7 條測試，web 全套 263→**270**；**網頁版前端 P3 Task 4：錄音、播放與 iOS 音訊解鎖**：新增 §7 三列 `talk/recorder.ts`／`talk/playback.ts`／`talk/audioUnlock.ts`；修正 brief 兩處缺陷（`MediaRecorder` 建立失敗未關麥克風軌道的資源洩漏；`writeReplyAudio` 在 TS 5.9 下 `Uint8Array`／`Blob` 型別不相容的編譯錯誤）；補齊 brief 未列的 `audioUnlock.ts` 測試與 `playback.ts` 的 `dispose()` 測試；新增 17 條測試，web 全套 246→**263**；**P3 Task 3 審查修正**（回合二）：`queued` 測試補型別常數斷言恢復編譯期鑑別力；`asArrayBuffer` docstring 自相矛盾已對齊；§7 差異數更正為四處（兩處行為＋兩處非行為）；補記 202 基準來源（`6a84df0`＋`b95ee90` 兩支 P2 收尾 commit 未回填）；**網頁版前端 P3 對講機協定核心搬移**（Task 3）：新增 §7 `talk/talkSocket.ts`／`talk/talkGesture.ts` 兩列，自 `app/src/lib/` 原樣搬移；web 全套 202→**246**；**網頁版前端 P3 對講機容量閘門接線**（Task 2）：`turns.py`／`ws.py` 共用 `app.py` 建立的同一個 `TurnAdmission`；WS 滿載送 `queued` 訊框、POST 逾時回 503；另加每位長輩每分鐘輪數保險絲；新增 `TURN_` 三鍵、`Settings` 102→**105** 欄；11 條新測試，mutation 驗證抓出 brief 原始兩條測試的假綠並改寫；**P3 Task 1**：`channels` 列新增 `app/admission.py`（`TurnAdmission`／`AdmissionTimeout`，FIFO 取號、`on_queued` 鎖外呼叫、`notify_all` 避免活鎖），channels 11→**12** 檔；**P2 全分支審查修正**：§7 `guardian/` 的「錯誤取代空狀態」升級為模組級不變量（四個清單一體適用「錯誤旗標 → 載入中 → 空狀態 → 清單」；首頁那條是 Critical——誤報空狀態會讓家屬建出刪不掉的重複長輩），另補「成功與失敗必須長得不一樣」「後端訊息照實顯示」兩條；`api.ts` 補 `revokeElderDeviceBindings`（長輩綁定碼唯一重取路徑）；`ui/` 補 `NoticeText`；新增 `GuardianApp.test.tsx`；協調者裁決追加建立成功後重打列表＋綁定碼二次確認；web 全套 167→**195**；**新增 §7 前端模組（web/）**：網頁版前端 P2 家屬端完成，`guardian/`／`ui/`／`nav/`／`notify/` 首次併入本文件；C1 回覆音檔直送＋H-1 評測接線；延遲優化四刀＋審查修正；後端測試 2594，六處 DbC 行號錨點更正） | 2026-08-01 |
| 08 | [專案結構指南](08_專案結構指南.md) | ✅ v1.48（**App 內通知加 `severity`**（2026-08-01 Leo 裁決）；**`web/guardian/api.ts` 註記補述**（D-79 補完，2026-08-01）：排程兩支寫入改走 `requestWithMeta`；**`web/guardian/schedules.ts` 註記補述**（D-79，2026-08-01）：回診的 `occurrences` 送出後會被後端忽略；**P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 最後一項**：對照 `find web/src -type f` 實際檔案系統逐一覆核 §6 ASCII 樹，未發現遺漏，樹本身無需改動）；**網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：新增 web/playwright.config.ts／web/e2e/journey.spec.ts 說明（非 src/ 樹範圍，比照 verify-wasm-checksum.mjs 慣例）；vite.config.ts 補 test.exclude: ["e2e/**"]；樹頂註記更正為「P4 全部完成」；**P4 Task 4 全分支審查修正**：elder/ 那行補 ElderApp.tsx 新增 tokenRevokedSeq? prop（401 出口改走 loseSession）；web 全套測試 501→**503**；**網頁版前端 P4 Task 4：兩欄接上通知**：樹頂註記更正為「P4 皆已完成」；`stage/`／`notify/`／`guardian/`／`elder/` 四行補接線說明；web 全套測試 488→**501**；**P4 Task 3 全分支審查修正（Important 2）**：§6 `notify/` 那行補漏更的 `bus.ts`（跨欄事件匯流排）；**P4 Task 2 審查修正**：`notify/` 那行補 `bannerRef`／`onTokenRevoked`；web 全套測試 464→**473**；**網頁版前端 P4 Task 2：通知輪詢**：§6 `web/src/` 樹 `notify/` 那行新增 `useNotificationFeed.ts`；純新增，尚未接進畫面；web 全套測試 445→**464**；**P4 Task 1 審查修正**：§6 `stage/` 新增 `reducedMotion.ts`（`TearTransition.tsx` 改為 import 共用）；`notify/` 那行更正 `NotificationBanner.tsx` 描述（role 移到 PhoneFrame、新增 size prop）；**網頁版前端 P4 Task 1：模擬系統通知橫幅**：§6 `web/src/` 樹 `notify/` 那行新增 `NotificationBanner.tsx`／`osStyle.ts`；樹頂註記補「＋通知橫幅已完成，純新增尚未接線」；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§6 `web/src/` 樹 `elder/` 那行新增 `NotificationsScreen.tsx`；`ElderApp.tsx` 描述更正為四路由全數接上真正畫面；樹頂註記更正——長輩欄四畫面全數落地，P3 收尾；**網頁版前端 P3 Task 8：對講機**：§6 `web/src/` 樹 `elder/` 那行新增 `useTalk.ts`／`TalkScreen.tsx`／`Avatar.tsx`，`talk/` 那行新增 `mediaErrors.ts`；`ElderApp.tsx` 的 talk 路由不再是佔位；樹頂註記更正為「＋對講機已完成，僅剩提醒畫面待 Task 9」；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：§6 `web/src/` 樹 `elder/` 那行新增 `ElderApp.tsx`／`BindScreen.tsx`／`LoginScreen.tsx`；`stage/` 那行補註記左欄已接上真正的 `ElderApp`；樹頂註記更正——長輩配對與帳密重登畫面已完成，對講機與提醒待 Task 8／9；**P3 Task 6 審查修正**：§6 `elder/` 那行更正 `location.ts` 為「一律回 null」（成功取得座標亦然）；**網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**：§6 `web/src/` 樹新增 `elder/`（`api.ts`／`location.ts`）；樹頂註記補「＋長輩端 API 呼叫端與文案已完成」；**P3 Task 5 審查修正**：§6 補 `web/src/vite-env.d.ts`＋新增 `web/scripts/verify-wasm-checksum.mjs` 說明；**網頁版前端 P3 Task 5：QR 掃碼**：§6 `web/src/` 樹 `talk/` 那行新增 `qrScanner.ts`；樹頂註記補「＋QR 掃碼已完成」；新增 `web/public/zxing_reader.wasm`（同源靜態 wasm 二進位，不入 `src/` 樹範圍，同 `silent.wav` 慣例）；**網頁版前端 P3 Task 4：錄音、播放與 iOS 音訊解鎖**：§6 `web/src/` 樹 `talk/` 那行新增 `recorder.ts`／`playback.ts`／`audioUnlock.ts`；樹頂註記更正為「對講機協定核心＋錄音播放與 iOS 音訊解鎖已完成」；新增 `web/public/silent.wav`（同源靜態無聲檔，不入 `src/` 樹範圍）；**網頁版前端 P3 對講機協定核心搬移**（Task 3）：§6 `web/src/` 樹新增 `talk/`（`talkSocket.ts`／`talkGesture.ts`），樹頂註記更正為「對講機協定核心已完成，長輩端畫面本身待 P3 續行」；**網頁版前端 P3 對講機容量閘門接線**（Task 2）：§3 `channels/app/admission.py` 由「純新增、尚未接線」更正為已接上 `turns.py`／`ws.py`（檔案清單不變、仍 12 檔）；新增 `TURN_` 三鍵，`Settings` 102→**105** 欄；**P3 Task 1**：`channels/app/` 新增 `admission.py`，§3 channels/app 11→**12** 檔；**P2 複審殘留修正**：§6 `web/src/ui/` 那行的 `Feedback` 補列 `NoticeText`，此元件由 P2 期間一支獨立 fix commit 新增、晚於回填本文件目錄樹的那支 commit，先前漏同步；網頁版前端 P2 家屬端完成回填：§6 `web/src/` 樹補 `guardian/`（十檔）／`ui/`／`nav/`／`notify/`，移除過期的「P2／P3 補齊」承諾；網頁版前端 P1 地基：頂層新增 web/、§6 補 web/src 目錄樹；channels/app/inbound_audio.py＋safety/combined_classifier.py 入目錄樹；safety／channels 兩行補齊漏列檔案並附檔數） | 2026-08-01 |
| 09 | [模組依賴關係](09_模組依賴關係.md) | ✅ v1.29（**App 內通知加 `severity`**（2026-08-01 Leo 裁決）；**網頁版前端 P4 全分支審查修正**（2026-08-01，1 Critical＋1 Important＋6 Minor）：§6 `web/` 依賴清單新增執行期邊 `notify/seen.ts → session/storage.ts`（已讀水位改以「角色＋登入者」分鍵），DAG 結論不變）；**P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 最後一項**：對照實際 import 關係逐一覆核 §6 `web/` 依賴清單，未發現遺漏，DAG 結論不變）；**網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：新增開發依賴 @playwright/test 1.62；不新增 web/src/ 內部依賴邊（E2E 為黑箱測試，DAG 結論不變）；**P4 Task 4 全分支審查修正 Minor 3**：補漏記的第四條邊 stage/StagePage.tsx → notify/osStyle.ts（detectOs，執行期非型別）；另補 strings.ts → stage/PhoneFrame.tsx 型別限定邊；DAG 結論不變；**網頁版前端 P4 Task 4：兩欄接上通知**：`stage/StagePage.tsx` 新增三條執行期邊（→ notify/useNotificationFeed.ts、→ notify/NotificationBanner.tsx、→ notify/bus.ts）；未新增第三方套件，DAG 結論不變；**P4 Task 3 全分支審查修正（Important 2）**：補漏記的 `notify/bus.ts` 與兩條執行期邊；**P4 Task 2 審查修正**：補一條先前漏記的邊 `notify/useNotificationFeed.ts → @/api`（`ApiError`）；**網頁版前端 P4 Task 2：通知輪詢**：`notify/useNotificationFeed.ts` 新增四條執行期邊（`→ elder/api.ts`／`→ guardian/api.ts`／`→ notify/seen.ts` 僅讀不寫／`→ strings.ts`）與一條型別限定邊（`→ notify/NotificationBanner.tsx`）；未新增第三方套件；**P4 Task 1 審查修正**：新增 `notify/NotificationBanner.tsx → stage/reducedMotion.ts` 執行期邊；**網頁版前端 P4 Task 1：模擬系統通知橫幅**：§6 `web/` 依賴清單補一句——未新增第三方套件，`notify/osStyle.ts` 新增一條型別限定邊 `→ stage/PhoneFrame.tsx`（編譯期抹除）；**P3 Task 5 審查修正**：§6 `qrcode`／`zxing-wasm` 補版本號＋wasm 一致性檢查腳本記載；**網頁版前端 P3 Task 5：QR 掃碼**：§6 `web/` 前端依賴清單新增 `zxing-wasm`（`talk/qrScanner.ts` 唯一 import，`npm audit` 0 漏洞）；順便訂正該行既有過期描述——`qrcode` 早在 P2 就已加入，先前寫「留待 P2／P3 才加」未回填；**網頁版前端 P3 對講機容量閘門接線**（Task 2）：§5 更正 `channels/app/admission.py`「尚未被任何模組 import」——現由 `turns.py`／`ws.py`／`app.py` 三處 import；新增 `turns.py → ws.py` 同層邊（`_BUSY_REPLY`／`_DEFAULT_TURN_CONCURRENCY` 共用），為兩檔首次互相依賴、不構成循環；**P3 Task 1**：§5 補 `channels/app/admission.py`，比照 `inbound_audio.py` 零 kinsun import；web/ 依賴 shared/（kinsun-shared alias），未新增第三方套件；Protocol 48→50 重掃；background 消費者 3→4（agent 因 record_turn 背景化）；合併分類器只走既有 LLMClient seam） | 2026-08-01 |
| 10 | [類別關係](10_類別關係.md) | ✅ v1.28（**`OutboundChannel` 加 `severity`**（2026-08-01 Leo 裁決）；**統一排程段落補 `AppointmentReminders`／`build_appointment_reminders`**（D-79，2026-08-01）：回診兩顆鬧鐘的唯一推算處；**網頁版前端 P3 對講機容量閘門**（Task 1 遺留、Task 2 審查補回）：§3 補 `TurnAdmission`／`AdmissionTimeout` 兩類別，前一輪判斷同級於 `_InFlight`／`_Sender`／`AckAudioCache` 不需入圖，複審後改判——它是兩條對講機路徑共用的單一物件；CombinedSafetyClassifier／LlmCombinedSafetyClassifier／CombinedSafetyResult 入圖，RiskDetector＋combine_with_llm、AbuseModerator＋apply_threshold；Protocol 總表 50 並補回漏列的 AbuseClassifier） | 2026-08-01 |
| 11 | [審查與重構指南](11_審查與重構指南.md) | ✅ v1.1（CI 已落地；docs/dev 同步鐵律入自查） | 2026-07-17 |
| 12 | [前端架構規範](12_前端架構規範.md) | ✅ v1.47（**補播的審查修正**（2026-08-01，1 Critical＋1 Important＋2 Minor，含一處錯誤自陳的更正）；**F-17 第一段：縣市座標反查模組已備妥、尚未接線**（T5，Leo 2026-08-01 裁決③）：新增 `elder/countyCoords.ts`（複製後端 22 縣市座標表＋Haversine 最近鄰、120 公里門檻擋海外座標）與 `scripts/verify-county-coords.mjs`（防兩份表漂移，掛進 `npm run build`）；`location.ts`／`useTalk.ts` 刻意未接線（T4／T6 當時仍在同一支 `useTalk.ts` 收尾），天氣／附近地點／交通路線三個症狀現況不變，§9 F-17 狀態仍是待處置；新增 10 條測試，web 563→**573**；**插嘴後前一題的答案改為補播**（2026-08-01 專案裁決）；**iOS 音訊解鎖提早到「播放器誕生後的第一個觸碰」**（2026-08-01 專案裁決，選項 B）；**T3 審查補完（D-80／辛-24）**；**危急警報接上後端 `severity`**（2026-08-01 Leo 裁決）；**F-19 已完成**（D-79 補完，2026-08-01）：網頁版排程寫入改帶回信封 `meta`，家屬終於看得到「回診前一天那顆提醒沒建成」；⚠️ 記錄 `setHint`／`resetForm` 的順序陷阱；web 全套 509→517；**F-16 改判＋新增 F-19**（D-79，2026-08-01）：F-16 建立面症狀消失、顯示面仍未修（根因未除）；F-19＝`meta.warnings` 四端皆未消費，家屬仍看不到；**網頁版前端 P4 全分支審查修正**（2026-08-01，1 Critical＋1 Important＋6 Minor）：§4 新增一大段——Critical（分頁切回前景會連播最壞 70 秒）＋Important（`notify/bus.ts` 與 G4 的假前提）＋6 Minor；同時改寫一條「釘住錯的那一邊」的測試）；**P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 全部 35 個工項最後一項**：新增 §9 F-18——五格跨瀏覽器人工驗收矩陣與容量閘門實測數字皆**未執行**（需真實裝置與人工彩排，無頭沙箱做不到），完整人工驗收清單見 task-6-report.md；`.env.example`／spec §13 暫定值未動）；**P4 Task 5 審查修正**：長輩欄通知橫幅不在 E2E 涵蓋範圍內、`baseURL` 陷阱與 webkit 未啟動就地揭露；**網頁版前端 P4 Task 5：端到端測試（P4 收尾）**：§6 新增 Playwright E2E 層說明；CI 判斷暫不接（理由見 task-5-report.md）；**P4 Task 4 全分支審查修正**（0 Critical，4 Important＋5 Minor）：長輩欄 401 出口改走 ElderApp 的 loseSession；shownUpTo 補隨 visible 重設；補兩條無測試守著的接線；5 Minor；web 全套測試 501→**503**；**網頁版前端 P4 Task 4：兩欄接上通知**：拆 StagePage／StageBody 兩層；visible／onTokenRevoked 接線；brief 缺陷（Critical，已修正，範例遺漏既有 prefilledCode／elderVisible／memo）；severity: "alert" 未接線待裁決；web 全套測試 488→**501**；**P4 Task 3 全分支審查修正**：`prefilledCode` 改為帶 `seq` 的事件（`ElderCodeDelivery`）而非單純的值，修好「配對成功又被登出時誤讀舊碼」與「同一組碼再送一次略過同步」兩種症狀；補齊漏更的 08／09；更正報告一句變異驗證宣稱；web 全套測試 487→**488**；**網頁版前端 P4 Task 3：跨欄連動**：§4 新增一段——`notify/bus.ts` 事件匯流排（家屬寫入成功發 `guardian-wrote`，消費端待 Task 4）＋`prefilledCode`／`onSendCodeToElder` 發送端接線；修正發送端接上後才現形的 P3 缺陷（`BindScreen` 補 render 期間比對＋調整）；變異驗證發現一條等價變異；web 全套測試 473→**487**；**P4 Task 2 審查修正**（0 Critical，5 Important＋6 Minor）：「第一次不補播歷史」改一律從 0 起跳；`bannerRef` 擋 `<StrictMode>` 雙呼叫；換人／登出補 `unread` 歸零；新增 `onTokenRevoked` 401 出口；三條資源釋放路徑改用 `vi.getTimerCount()`／spy 精確驗證；新增 8 條測試＋`guardian/api.test.ts` 1 條，web 全套測試 464→**473**；**網頁版前端 P4 Task 2：通知輪詢**：§4 新增一段——`notify/useNotificationFeed.ts`；修正 brief 缺陷（`unread` 恆為 0、佇列無上限）；新增修正競態（換人時舊資料晚到，`sessionRef`）；偏離 brief（分頁可見性暫停、品牌字串改讀 `strings.gate.brand`、換人清空改走 render 期間比對）；新增 19 條測試，web 全套測試 445→**464**；**P4 Task 1 審查修正**（0 Critical，3 Important＋4 Minor）：live region 移到 PhoneFrame、size prop 放大字級與觸控目標、`prefersReducedMotion` 抽到共用模組、卡片 pointer-events 分工；web 全套測試 438→**445**；**網頁版前端 P4 Task 1：模擬系統通知橫幅**：§4 新增一段——`notify/NotificationBanner.tsx`（`role="status"`、`key` 綁 `item.id`、不管理佇列）／`osStyle.ts`（`detectOs`）；修正 brief 缺陷 `strings.common.close` 鍵不存在；偏離 brief 的 CSS `prefers-reduced-motion` 寫法，改採與 `TearTransition.tsx` 一致的 JS `matchMedia` 判斷；純新增尚未接線；新增 14 條測試，web 全套測試 424→**438**；**網頁版前端 P3 全分支審查修正**：§4 新增一段四條架構結論（401 出口／權限對話框時機／`matchMedia` 可見性／降級路徑保留後端人話）；§8 與 §9 F-17 收緊為「連 `getCurrentPosition` 都不呼叫」並載明恢復時的權限時機硬約束；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§4 新增一段 `elder/NotificationsScreen.tsx`，長輩欄四畫面全數落地；修正 brief「錯誤取代空狀態」缺陷（`hasError` 獨立布林旗標，沿用 `guardian/NotificationsScreen.tsx` 既有修法）；補齊已讀水位取最新一則與寫入失敗隔離兩項不變量；新增路由整合測試補齊「鈴鐺→提醒→返回」接縫；待裁決 `elderNotifications.back` 未使用一事；新增 10 條測試，web 全套測試 397→**407**；**P3 Task 8 裁決回應**：§4 收音期間抵達的回覆改為一律丟棄（✅ 專案裁決），不靜默丟棄；**P3 Task 8 審查修正**（3 Critical）：§4 補三個 Critical 的成因與修法——三者都是「單一模組看起來都對、組合起來才出事」的類型；另更正 `unlockAudio` 的風險論證（真正讓形狀不同的是 `pause()` 發生在 `getUserMedia` 之前，不是「無聲檔只播一次」；2026-07-18 那次故障的代價是整句話沒了）。新增 12 條測試，web 全套測試 384→**396**；**網頁版前端 P3 Task 8：對講機**：§4 新增一段——`useTalk`／`TalkScreen`／`Avatar`／`mediaErrors`；記載五條資源釋放路徑、`visible` 鏈路延伸到對講機（同類坑第四次）、`unlockAudio` 掛載時機的待裁決取捨（與 2026-07-18 iOS 錄音截斷故障同形狀、無頭環境判定不了，已列人工驗收）、兩條會永遠卡在「在想」的路徑已補保險、`probeMicrophone` 六種結果、登出二次確認列。新增 55 條測試，web 全套測試 329→**384**；**P3 Task 7 審查修正**（1 Critical＋3 Important＋6 Minor）：雙欄舞台切頁籤不關相機的 Critical＋422 誤導文案＋手打路徑無線路契約斷言三項 Important 已修，新增 11 條測試，web 全套測試 318→**329**；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：§4 新增一段 `elder/BindScreen.tsx`／`LoginScreen.tsx`／`ElderApp.tsx`，接上 `stage/StagePage.tsx` 取代原佔位元件；修正 brief 三處缺陷＋兩處偏離點（詳見 07 §7）；記載建置驗證兩個已知落差；新增 20 條測試，web 全套測試 298→**318**；**P3 Task 6 審查修正**（4 Important＋5 Minor，0 Critical）：`postTurn` 補 `init.body` 斷言、`talk.queued` 改「您排第 N 位」、修正錯誤敘述、`currentPlace` 成功路徑改回 `null`；§9 F-17 影響面擴大（附近地點／交通路線）並補離線選項（`weather.py` 22 縣市表反向最近鄰）；Task 6 淨新增測試 13→**12** 條，web 全套測試 299→**298**；**網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**：新增 `elder/api.ts`（六支端點呼叫端，慣例比照 `guardian/api.ts`）與 `elder/location.ts`（`currentPlace`）；端點與欄位逐一核對後端 router 與 06 §3，全數一致，未新增或調整任何 API 契約；`strings.ts` 補 `elderBind`／`elderLogin`／`talk`／`elderNotifications` 四段（逐字沿用 App 版）；新增 §9 F-17 已知限制——網頁無反查地名 API，天氣類問題退回反問所在地；新增 13 條測試（全部逐條變異驗證），web 全套測試 286→**299**；**P3 Task 5 審查修正**（4 Important＋8 Minor，0 Critical）：§4 qrScanner 段落補齊四項行為缺陷與八項 Minor，新增 9 條測試（7→16）；**網頁版前端 P3 Task 5：QR 掃碼**：§4 補一段 `talk/qrScanner.ts`（走 WebAssembly 版 ZXing）；修正 brief 兩處缺陷（`overrideWasmUrl` 選項不存在改用 `overrides.locateFile`；解碼飛行中被 `stop()` 仍會回報）；版頭第三行補「＋長輩端 QR 掃碼」；**P3 Task 4 審查修正**：§4 補三處易漏邊界（`recorder.ts::stop()` try/finally＋保險逾時＋重入保護、`playback.ts` 解鎖無聲檔 `ended` 抑制與 `revokeQueuedReplyAudio`）；版頭過期描述「P1 地基已完成，長輩／家屬畫面待 P2／P3」更正；**網頁版前端 P3 Task 4：錄音、播放與 iOS 音訊解鎖**：§4 補 `talk/recorder.ts`／`talk/playback.ts`／`talk/audioUnlock.ts` 一段，完成 Task 3「待後續任務」的第三處差異；三個瀏覽器特有約束（容器格式各家不同、iOS 音訊解鎖須同源靜態檔、資源必須釋放）；補記 TS 5.9 `Uint8Array`／`Blob` 型別衝突；**網頁版前端 P3 對講機協定核心搬移**（Task 3）：§4 補 `talk/` 兩處必要瀏覽器差異段落＋第三處待後續任務落地的說明；§6 補 `setTimeout` 跨環境型別衝突記錄；複審修正：F-16 拿掉會爛掉的 `schedules.ts:131` 行號錨點（三份候選檔案皆對不上），改用分支語意指路；Task 7 審查修正：F-16 補上 `describeGroup` `once` 分支（自訂一次性提醒）同樣受時區 bug 影響，不只回診；**新增 §9 F-16 已知限制**：`app/`／`frontend/` 回診「前一天」提醒因 UTC/本地時區混用早算一天，`web/` P2 已修、`app/`／`frontend/` 凍結未修待裁示；`signIn` 收 `Omit<Session, "role">`，角色由工廠決定；網頁版前端 P1 地基：三端擴為四端，新增 web/ 雙 session 工廠（與 App 單例的差異）與 CSP 兩處放寬理由；App 端接內嵌音檔：新增 lib/replyAudio.ts 落地 cache 暫存 .m4a、talkSocket 以注入點保住離線可測性；新增 expo-file-system 依賴，組員需重跑 npm install） | 2026-08-01 |
| 13 | [安全與就緒檢查](13_安全與就緒檢查.md) | ✅ v1.6（日誌脫敏結案：只記系統事實、內容進 Opik；新增工具寫入授權與語音後端誤設兩列；日誌脫敏需重驗；§G 補外呼逾時與 D-22 部分落地；位置隱私列；依賴掃描與 healthz 結案） | 2026-07-27 |
| 14 | [部署與運維](14_部署與運維.md) | ✅ v1.28（**§3.6 容量上限已核定**（2026-08-01）：全場同時最多 3 人；3 除以 `WEB_WORKERS`(2) 除不盡，取 `TURN_CONCURRENCY_LIMIT=2`（全域 4）而非 1（全域 2）；**新增 §3.6 對講機容量閘門**（P3 Task 2）：`TURN_CONCURRENCY_LIMIT × WEB_WORKERS＝全域上限`，暫定值待彩排實測核定；**§3.5 pool_size 15→25＋爆線症狀補「近滿載＝排隊變慢」**；**kinsun.sh 新增 update 指令**（僅 opik）；**Opik 啟動不再問 registry**：opik.sh 本地加 `--pull missing`，registry 瞬斷不再讓整組起不來，映像改明確手動升級；**新增裝置推播章節**：五個前置步驟、iOS 需付費 Apple 會員、推播沒響的判讀順序；**連線超賣已治本：池 5→3＋CLI 小池 2**；**§3.5 連線上限更正＝15 非 60**（DATABASE_URL 走 pooler session mode，預設 4 進程×5 已超賣）；RAG Runbook 補四則實戰前提：CLI 連線額度、免費層每日嵌入約 1,000 條／金鑰、評測撞配額會偽裝成品質問題、golden set 指定來源須與內容政策一致；Opik 觀測定期重探；排程器 systemd unit；排程停擺根因＝浮點等值比對；RAG 週更 hpa 憑證 bundle 與爬深 ≥100） | 2026-08-01 |
| 15 | [文檔與維護指南](15_文檔與維護指南.md) | ✅ v1.2（同步鐵律＋前端／WBS／排程宣告三列） | 2026-07-27 |
| 16 | [WBS開發計畫](16_WBS開發計畫.md) | ✅ v1.31（**辛-25 審查後補完四項**（2026-08-01）；**新增辛-26**（F-17 第一段：縣市座標反查模組已備妥、尚未接線，Leo 八項裁決第 6 項，⏳ 部分完成——`location.ts`／`useTalk.ts` 刻意未接線，待 T4／T6 收尾後下一輪再做）；**新增辛-25**（對講機兩項裁決同時施工，已完成）；**新增辛-24**（危急警報看起來像危急警報，D-80，已完成）；辛-23 審查後補完四項（F-19 結案、`APPOINTMENT_REMINDER_HOUR` 補 0–23 範圍驗證、兩處訊息補正）；新增辛-23（修回診「前一天」提醒提早兩天響＋下午設明天的回診整筆建不起來，D-79，已完成）；庚-11（A-10 有字無聲）由「不改」改判並完成——07-28 的安撫話快取讓成本由 M 降到 S，且「螢幕有字」對純語音長輩不成立；新增辛-22 修未註冊工具導致的退化輸出已完成（B 提示詞動態化留待辦）；新增辛-21 一輪總時間預算已完成；新增辛-20 修對講機改走 WS 後定位失效已完成；辛-19 非同步工具調用與併發對話已完成；新增辛-18 附近地點搜尋已完成；辛-17 工具回合思考層級已完成；辛-15 架構對比後六項邊界補強已完成、辛-16 第二批未施工）（辛-14 三項修復完成；辛-13 全流程模擬實測完成；辛-10 濫用審核＋辛-11 觀測與評測強化＋辛-12 防呆待施工；RAG 個人庫驗收；甲～庚結案＋辛批 12 項；⚠ 庚-09 待 Leo 確認） | 2026-08-01 |
| 17 | [前端資訊架構](17_前端資訊架構.md) | ✅ v1.32（**補播的審查修正＋解鎖殘餘風險補述**（2026-08-01）；**插嘴後前一題的答案改為補播**（2026-08-01 專案裁決）；**iOS 音訊解鎖提早**（2026-08-01 專案裁決，選項 B）；**網頁版前端 P4 全分支審查修正**（2026-08-01，1 Critical＋1 Important＋6 Minor）：§6 長輩端補兩條——通知橫幅四條「只重建基準」路徑（缺的是分頁切回前景）、家屬送碼過來時要收掉被動登出的紅字；路由表不變）；**P4 Task 6：跨瀏覽器驗收與文件收尾，P1～P4 最後一項**：`web/` 兩條路由與十個框內畫面全數定稿，覆核與程式現況一致，無需改動；真機觀感項目待人工驗收，見 12 §9 F-18／task-6-report.md）；**P4 Task 4 全分支審查修正**：對講機「被動登出」段補記第三條 401 出口（通知輪詢）原本搶在既有兩條出口前面靜默登出，已改走同一支 loseSession；web 全套測試 501→**503**；**網頁版前端 P4 Task 4：兩欄接上通知**：對講機／提醒鈴鐺未讀數更正為已接上真數字；首頁補通知未讀徽章；補對稱 guardianVisible；web 全套測試 488→**501**；**網頁版前端 P4 Task 3：跨欄連動**：§6「首頁」「長輩詳情」補「送到長輩的手機」；「配對」列 `prefilledCode` 由「本 Task 未接」改為「已接線」；web 全套測試 473→**487**；**網頁版前端 P3 全分支審查修正**：§6 web/ 長輩端段末補三條（返回鍵標籤表、401 被動登出走回配對並在該畫面說明、長輩欄可見性以斷點為準）、對講機「權限」列補記 web/ 重演 2026-07-18 權限時機故障；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§3／§6／§1／末段落四處由「提醒 ⏳ 待 Task 9」改為「✅ 已完成」，長輩欄四畫面全數落地；§6 提醒列補齊「錯誤取代空狀態」不變量與已讀水位機制；新增 10 條測試，web 全套測試 397→**407**；**P3 Task 8 裁決回應**：§6 對講機列更正——收音期間抵達的回覆一律丟棄、不補播，但不靜默丟棄；**P3 Task 8 審查修正**：§6 對講機列補三條審查修正後的不變量（收音期間先收下來錄完補播、打斷後下一句立刻播、登出確認文案）；**網頁版前端 P3 Task 8：對講機**：§1 範圍敘述更正（長輩框內只剩提醒待 Task 9）；§3 `WebElder` 子圖 `elder/talk` 由佔位改為實作節點；§6「web/ 長輩端畫面」表對講機列改寫為完整規格；§7 附註與頁面總計同步；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：§1 範圍敘述更正；§3 `WebElder` 子圖改畫為配對／帳密重登（實作）＋對講機／提醒（⏳ 佔位）四節點；§6 新增「web/ 長輩端畫面」規格表；§7 附註更正、頁面總計補細分；**P2 全分支審查修正**：§6 web 家屬端六畫面表三列改寫——首頁補「空狀態只在真的沒有長輩時才顯示」與登出入口、長輩詳情補**重新產生長輩綁定碼**（綁定碼在家屬端唯一的重取路徑，無此路徑則 P3 三句「請家人重新產生一組」全是死路）與代辦帳密成功／失敗兩種樣式、行程管理補標題帶長輩稱呼與載入失敗降級；框內導覽補 `elderName` 隨路由帶入＋新增「登入狀態守衛」一段；協調者裁決追加：首頁補建立成功後重打列表、長輩詳情補綁定碼二次確認列；**Task 7 審查修正**：§3／§6／§7／頁面總計更正 D-76 統一排程後遺留 6 天的漂移（App「用藥管理」「回診管理」兩頁合併為「行程管理」單頁，12 頁；**LIFF 同一漂移一併修正**，「用藥 CRUD」「回診 CRUD」兩節點合併，4→3 頁）；**網頁版前端 P2 家屬端完成**：§3 全景圖新增 web/ 家屬欄六畫面框內導覽子圖；§6 新增 web 家屬端六畫面規格；§7 附註更新（家屬框內畫面已補齊、長輩端仍待 P3）；§7 補 `/demo/stage` 伺服器端回退、舞台與狀態 hook 掛在路由之上兩則附註；**新增 /elder/notifications 長輩提醒頁＋對講機鈴鐺未讀 badge**（X-01）；對講機資料需求對齊 WS 長連線＋位置鍵名兩路徑一律 `location`；雙手勢＋admin 7 頁 D-74 話題新聞頁） | 2026-08-01 |

狀態圖例：⬜ 未開始｜🟡 進行中／待議中｜✅ 定稿

> **2026-07-10 架構文檔深化**（設計＝[spec](../superpowers/specs/2026-07-10-架構文檔深化-design.md)、計畫＝[plan](../superpowers/plans/2026-07-10-架構文檔深化.md)）：逐子系統（7 群、8 輪）深潛重構後的程式碼，回填 05／09／10／12／17；共登記 58 項後端差距（A-1～A-58，六項 HIGH 見 [05 差距彙整](05_架構與設計.md#差距與重構項本文件貢獻給-16_wbs)）與 8 項前端差距（[12 §9](12_前端架構規範.md)）。**純文檔工作，未修改任何程式碼。**

## 寫作順序

02 → 03 → 04 → 05 → 06 → 07 → 08 → 09 → 10 → 12 → 17 → 11 → 13 → 14 → 15 → 16（WBS 最後，收斂全部差距項）

## 與舊文件的關係

`docs/mvp/`、`docs/全庫人工決策盤點-待議.md` 為過時參考，全套定稿後移入 `docs/archive/`；其結論一律不直接繼承。
