# 專案結構指南 - 金孫 KinSun

> **版本:** v1.39 | **更新:** 2026-07-31 | **狀態:** ✅ 定稿（**網頁版前端 P4 Task 2：通知輪詢**（2026-07-31）：§6 `web/src/` 樹 `notify/` 那行新增 `useNotificationFeed.ts`（`pickNewItems`／`useNotificationFeed`，把後端既有兩支 App 內通知端點變成畫面上的橫幅；`QUEUE_MAX=20` 佇列上限、`sessionRef` 擋換人時舊資料晚到、`document.hidden` 分頁背景暫停）；純新增，尚未接進畫面。詳細模組規格、brief 缺陷修正、變異驗證見 07 §7 v1.82；**P4 Task 1 審查修正**（2026-07-31，0 Critical，3 Important＋4 Minor）：§6 `stage/` 那行新增 `reducedMotion.ts`（`TearTransition.tsx` 改為 import，消除重複）；`notify/` 那行更正——`NotificationBanner.tsx` 的 `role="status"` 移到 `stage/PhoneFrame.tsx`（該行補註記），本檔改用 `data-testid`，新增 `size?: "normal"|"big"` 放大長輩端字級與關閉鍵。詳細模組規格、變異驗證見 07 §7 v1.81；**網頁版前端 P4 Task 1：模擬系統通知橫幅**（2026-07-31）：§6 `web/src/` 樹 `notify/` 那行新增 `NotificationBanner.tsx`／`osStyle.ts`；樹頂註記補「＋通知橫幅已完成，純新增尚未接線」。詳細模組規格見 07 §7 v1.80；**網頁版前端 P3 Task 9：長輩提醒列表與收尾**（2026-07-31）：§6 `web/src/` 樹 `elder/` 那行新增 `NotificationsScreen.tsx`，`ElderApp.tsx` 描述更正——四路由全數接上真正畫面（不再有 notifications 佔位分支），`default` 分支為 `never` 型別窮盡檢查；樹頂註記更正為「長輩欄四畫面全數落地，P3 收尾」（原「僅剩提醒畫面待 Task 9 續行」已完成）。詳細模組規格見 07 §7 v1.78；**網頁版前端 P3 Task 8：對講機**（2026-07-31）：§6 `web/src/` 樹 `elder/` 那行新增 `useTalk.ts`／`TalkScreen.tsx`／`Avatar.tsx`，`talk/` 那行新增 `mediaErrors.ts`；`ElderApp.tsx` 描述更正——talk 路由已接上真正畫面，僅 notifications 仍為 Task 9 佔位；樹頂註記更正為「＋對講機已完成，僅剩提醒畫面待 Task 9」；**網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**（2026-07-31）：§6 `web/src/` 樹 `elder/` 那行新增 `ElderApp.tsx`／`BindScreen.tsx`／`LoginScreen.tsx`（長輩端第一個畫面，詳見 07 §7）；`stage/` 那行補註記——左欄已接上真正的 `ElderApp`，原 `ElderPanePlaceholder` 移除；樹頂註記更正為「＋長輩配對與帳密重登畫面已完成，對講機與提醒兩個長輩端畫面待 Task 8／9 續行」（原「長輩端畫面本身待 P3 續行」已過期——P3 拆成多個 Task，本輪完成其中兩個畫面）；**P3 Task 6 審查修正**（2026-07-31）：§6 `elder/` 那行更正 `location.ts` 描述——`currentPlace` 成功取得座標的分支也回 `null`（不再回傳含空地名的物件），詳見 07 §7／12 §9 F-17；**網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**（2026-07-31）：§6 `web/src/` 樹新增 `elder/`（`api.ts`／`location.ts`，見 07 §7）；樹頂註記補「＋長輩端 API 呼叫端與文案已完成」；**P3 Task 5 審查修正**（2026-07-31，Minor 7＋8）：§6 `web/src/` 樹補 `vite-env.d.ts`（標準 Vite 型別參考，供 `import.meta.env.BASE_URL` 型別檢查）；新增 `web/scripts/verify-wasm-checksum.mjs`（純 Node.js，`build` 前置比對 wasm 版本一致性，非 `src/` 樹範圍不入 ASCII 樹）；**網頁版前端 P3 Task 5：QR 掃碼**（2026-07-31）：§6 `web/src/` 樹 `talk/` 那行新增 `qrScanner.ts`（樹頂註記同步更正為「＋QR 掃碼已完成」）；新增 `web/public/zxing_reader.wasm`（相機掃碼用的 WebAssembly 二進位，約 1MB，複製自 `zxing-wasm` 套件的 `dist/reader/zxing_reader.wasm`，Vite 建置時複製到 `web/dist/` 根目錄由 `_static_mounts` 的 `/demo` 掛載點提供，同源提供以避開套件預設的 CDN 位址被 CSP `default-src 'self'` 擋掉；路徑非 `src/` 樹範圍，與 `silent.wav` 既有省略慣例一致，不列入本節 ASCII 樹）；**網頁版前端 P3 Task 4：錄音、播放與 iOS 音訊解鎖**（2026-07-31）：§6 `web/src/` 樹 `talk/` 那行新增 `recorder.ts`／`playback.ts`／`audioUnlock.ts` 三檔（樹頂註記同步更正為「對講機協定核心＋錄音播放與 iOS 音訊解鎖已完成」）；新增 `web/public/silent.wav`（iOS 音訊解鎖用的同源靜態無聲檔，約 4KB，Vite 建置時複製到 `web/dist/` 根目錄由 `_static_mounts` 的 `/demo` 掛載點提供，路徑非 `src/` 樹範圍故不列入本節 ASCII 樹——與既有 `index.html`／`vite.config.ts` 等 `web/` 頂層檔案的既定省略慣例一致）；**網頁版前端 P3 對講機協定核心搬移**（Task 3，2026-07-31）：§6 `web/src/` 樹新增 `talk/`（`talkSocket.ts`／`talkGesture.ts`，自 `app/src/lib/` 原樣搬移，詳見 07 §7）；`web/src/` 樹頂註記更正為「對講機協定核心已完成，長輩端畫面本身待 P3 續行」（原「長輩端與對講機待 P3」已過期）；**網頁版前端 P3 對講機容量閘門接線**（Task 2，2026-07-31）：§3 `channels/app/` 那行的 `admission.py` 由「純新增、尚未接線」更正為**已接上** `turns.py`／`ws.py` 兩條路徑（共用同一個 `TurnAdmission` 實例，由 `app.py` 建立並注入）；檔案清單不變（仍 12 檔，無新增檔案）；**網頁版前端 P3 對講機容量閘門**（Task 1，2026-07-31）：`channels/app/` 新增 `admission.py`（`TurnAdmission`＋`AdmissionTimeout`，先到先服務的容量閘門，FIFO 取號、`on_queued` 鎖外呼叫）；§3 `channels/app/` 那行 11→**12** 檔；**網頁版前端 P2 家屬端複審殘留修正**（三處小殘留之一）：§6 `web/src/ui/` 那行的 `Feedback` 補列 `NoticeText`——這個元件是 P2 開發期間一支獨立 fix commit（代辦帳密成功／失敗改走兩種樣式）新增的，晚於回填本文件目錄樹的那支 commit，未被連帶同步，這裡補上；**網頁版前端 P2 家屬端完成回填**（Task 7 審查要求，同步鐵律）：§6 `web/src/` 樹補 P2 新增的 `guardian/`（十檔：六畫面＋InviteCard＋api.ts＋schedules.ts）、`ui/`、`nav/`、`notify/` 四個目錄，並移除已過期的「長輩／家屬畫面於 P2／P3 補齊」註記（家屬端已完成）；網頁版前端 P1 地基（任務 12）：頂層新增 `web/`（App／LIFF 凍結後唯一演進的前端，掛 `/demo`），§6 前端結構補 `web/src/` 樹（gate／stage／session＋api.ts／strings.ts／theme.css，長輩／家屬畫面待 P2／P3）；延遲優化四刀（2026-07-30）：`channels/app/` 新增 `inbound_audio.py`（進站音檔背景上傳，`turns.py`／`ws.py` 共用）、`safety/` 新增 `combined_classifier.py`（分級＋審核合併一次呼叫）；一併補齊 `safety/` 檔案清單先前漏列的 `moderation`／`evaluation`／`local_detector` 三檔；`locations/store.py` 新增 `is_valid_coordinate` 座標判準，WS 與 REST 共用單一出處（V-04）；`ack_audio.py` 兼供回退話術的預錄音檔（V-02）；非同步工具調用（spec 2026-07-28）：`speech/` 補 `acks.py`（安撫話語庫）與 `ack_audio.py`（音檔快取），`channels/app/` 補 `ws.py`（對講機長連線）；新增附近地點搜尋（spec 2026-07-27）：頂層新增 `places/`（POI 持久層＋類別對照＋結果後處理＋抽取 CLI）、`tools/` 補 `places.py`；`scheduler/` 更名 `cron/` 並新增 `registry.py`（全系統排程宣告的唯一真實來源）；`transport_agent/` 原型移出 `src/` 到 `docs/archive/`（仍在版控）；`speech/chunking.py` 回覆切句；頂層新增 `background.py` 背景落庫；統一排程 D-76 **五刀全完成**：schedules/ 八檔全配、medications/ 與 appointments/ 退役；頂層新增 clock.py、tools/clock.py 移除；D-42～D-44 已拍板；補工程觀測 tracing/ 與 evals/；併入 RAG 版本發布、遷移、週更與評估模組；tools 補交通 transport，transport_agent/ 原型 ruff 排除）
> 命名規則的唯一真實來源是 [AGENTS.md](../../AGENTS.md)；本文件記錄結構現況與腳手架慣例，不重複規範全文。

---

## 1. 設計原則

- **按領域組織**（package-per-domain 模組化單體）：`schedules/`、`accounts/` 各自完整，不按類型分散——刻意偏離模板的 domains/application/infrastructure 三層目錄式，分層以 import 方向約束（見 [09_模組依賴關係](09_模組依賴關係.md)），對應關係見 [05 §1.3](05_架構與設計.md)。
- **組裝集中**：物件建構只發生在 `composition.py`／`app.py`／`cron/worker.py` 三個組裝根，handler 內禁止自行 new。
- **排程宣告集中**：有哪些排程、何時跑、哪個程序跑，只寫在 `cron/registry.py`。主排程器與 RAG Worker 各自依 `owner` 挑出要跑的，後台 `/admin/jobs` 讀全集——兩邊各自宣告時，跑在別的程序的排程對後台是全盲的（2026-07-27 修）。
- **配置外部化**：一切設定走環境變數（`config.py`＋`.env.example`），程式不寫死金鑰。
- **src layout**：後端在 `src/kinsun/`，根目錄只放專案級檔案。

## 2. 頂層結構

```plaintext
kinsun/
├── src/kinsun/     Python 後端主套件
├── app/            Expo React Native App（長輩＋家屬雙角色，凍結維運）
├── frontend/       Vite＋React（LIFF 家屬頁＋admin 後台，雙 build，LIFF 凍結維運）
├── web/            Vite＋React 網頁版全功能前端（App／LIFF 凍結後唯一演進的前端，掛 /demo；spec 2026-07-30）
├── services/       DGX 重模型伺服器（asr/、tts/，環境變數獨立管理）
├── tests/          pytest（單元＋合約＋KINSUN_IT 整合）
├── scripts/        kinsun.sh（服務統一啟停，含 Expo dev server）＋backup_db.sh＋anchoring_probe.py（天氣 anchoring 行為探針）
├── data/           RAG 種子資料
├── docs/           文件（docs/dev/ 為現行 SSOT）
├── pyproject.toml  依賴／ruff／pytest 設定（uv 管理）
└── AGENTS.md / CONTEXT.md / CONTRIBUTING.md / README.md
```

## 3. 後端結構（src/kinsun/）

```plaintext
src/kinsun/
├── app.py / composition.py        # 組裝根（API）／共用組裝
├── config.py / db.py / llm.py     # 基礎層：設定／Postgres／Gemini
├── transport.py / pipeline.py / agent.py
├── turn_context.py                # 長輩原話 contextvar（工具防 anchoring）
├── clock.py                       # 台灣時間白話講法＋TimeFacts（每輪注入情境，取代舊時間工具）
├── background.py                  # 背景落庫（觀測稽核／提醒標記／本輪對話記憶移出回覆路徑；預設同步，只有 app 啟用）
│               ⚠ 自 2026-07-30（B2）起 `run()` 回傳 `Handle`：對話記憶那一筆有
│                 讀取端（分段續拉算 digest），由 `pipeline` 在交出回應前收斂
├── accounts/      帳號綁定同意（models/passwords/service/store）
├── binding/       LINE 綁定狀態機＋同意閘門（flow/gate/session）
├── channels/      通道中立層（inbound/outbound/router＋line/〔channel/messenger/richmenu/webhook〕
│                 ＋app/〔turns/ws/outbound/inbound_audio/admission〕；共 12 檔）
│               ⚠ `app/inbound_audio.py` 是 `turns.py`（HTTP）與 `ws.py`（長連線）
│                 共用的進站音檔背景上傳（延遲優化 B1，2026-07-30）：先前兩處各有
│                 一份 `_publish_inbound`，抽出來才不會只修好其中一條路
│               ⚠ `app/admission.py`（P3 Task 1，2026-07-31）：`TurnAdmission` 是
│                 全體共用的容量閘門（FIFO 取號、佇列非空不可插隊、`on_queued`
│                 鎖外呼叫避免拖住整個閘門），與 `ws.py` 既有的單連線併發上限
│                 `_InFlight` 並存而非取代；**已接線**（Task 2，2026-07-31）：
│                 `turns.py`／`ws.py` 共用同一個 `TurnAdmission`（由 `app.py`
│                 建立並注入兩條路徑），滿載時 WS 送 `queued` 訊框、POST 回 503
├── locations/     長輩目前地點（store＋facts；只存地名＋模糊座標）
│               ⚠ `store.is_valid_coordinate` 為座標判準的單一出處：WS 訊框與
│                 REST query 兩個呼叫端共用，各寫一份會重演 07-28 鍵名不合那次
│                 ——兩邊測試各斷言自己那版契約，全綠而位置整晚沒進庫
├── places/        附近地點 POI（models/geo/store/categories/refine/ingest；Overture Maps 匯入，非長輩擁有的全域查詢表，不用 PostGIS；spec 2026-07-27）
├── memory/        記憶三層（shortterm.py／longterm/／recall.py＋models.py；longterm/ 含 consolidation_log.py 整理進度標記）
├── safety/        危急偵測＋濫用審核（11 檔：tiers/keywords/classifier/combined_classifier/
│                 detector/local_detector/moderation/evaluation/events/notifier/deliveries）
│               ⚠ `combined_classifier.py`（2026-07-30）只是「一次呼叫拿兩份原始判斷」，
│                 判準本身仍住 `detector.combine_with_llm`／`moderation.apply_threshold`
│                 ——分開呼叫與合併呼叫兩條路徑共用同一份決策規則，不可各寫一份
├── schedules/     統一排程（models/store/service/jobs/facts/wording/flow/timeparse；用藥＋回診＋長輩自訂三合一，一列＝一個鬧鐘）
├── cron/          自建排程引擎（scheduler/state/fanout/jobs/registry/worker）
│               ⚠ 不是 schedules/：這裡是「何時跑」的引擎，那裡是長輩的行程表
├── proactive/     主動關懷 job＋自適應問候時間（jobs/constants/greeting_time/preferences）
├── news/          話題新聞（fetchers/ 三來源：mohw＋rss 免金鑰、news_api 需金鑰；jobs 爬取／清理；mentions.py 提及紀錄＝不重複給料；工具消費，D-74）
├── rag/           衛教 RAG（23 檔：爬取→入庫／遷移→版本發布／週更→檢索→回答政策）
├── reports/       健康報告／提醒紀錄／每日摘要
├── speech/        ASR／TTS client（呼叫 services/）＋ chunking.py 回覆切句（TTS 分段串流）
│                 ＋ acks.py 安撫話語庫（人設×工具兩維，文字為唯一真實來源）
│                 ＋ ack_audio.py 安撫話音檔快取（啟動預熱、簽章過期整批重暖）
│                    另供回退話術的預錄音檔（standby_phrases，V-02 有字無聲）
│                 ＋ tts.QueuedTtsClient TTS 優先權佇列（一次只跑一個推論）
├── audio/         音檔上傳 Supabase Storage
├── strategies/    守則與每晚反思（models/store/policy/reflection/facts）
├── tools/         LLM 工具（registry/weather/health_rag/web_search/transport/news/schedules（會寫庫，D-76 P4）/places（附近地點，讀 places＋locations 兩 store，免金鑰，spec 2026-07-27）＋lookups 查證紀錄）
├── observability/ 五階段 trace（models/store/jobs）
└── web/           REST 路由：routers/ 套件 14 檔（✅ D-28 已落地）＋auth/envelope/errors/ratelimit/security
```

## 4. 新領域腳手架（實際慣例）

新增領域（例：`exercise/` 運動提醒）依需要裁剪六件套——**models／store／service 必備**，其餘按功能：

| 檔案 | 何時需要 | 範例依據 |
| :--- | :--- | :--- |
| `models.py` | 必備：frozen dataclass＋Enum | `schedules/models.py` |
| `store.py` | 必備：三件套同檔（Protocol＋Pg＋Fake）；事件／狀態表可依語意命名（D-42 例外） | `schedules/store.py` |
| `service.py` | 必備：建構子注入 store（＋依需要 new_id／clock，✅ 庚-53 對齊現實） | `schedules/service.py` |
| `facts.py` | 要注入對話上下文才加（實作 `FactProvider`） | `schedules/facts.py` |
| `flow.py` | 有 LINE 引導選單才加 | `schedules/flow.py` |
| `jobs.py` | 有排程提醒才加（`build_*_job` 工廠，AGENTS.md 規範放領域內） | `schedules/jobs.py` |

**接線三步**：`composition.py`（建 store＋service）→ 需要排程則**先在 `cron/registry.py` 宣告**再於 `cron/worker.py` 綁執行體 → 需要 API 則 `web/`（to-be `web/routers/`）掛 router。

⚠ 只在 `worker.py` 加而沒在 registry 宣告，那支 job 根本不會被執行到（`build_jobs` 只走宣告）；只在 registry 宣告而沒人綁，啟動時會直接拋 RuntimeError——兩種漏接都不會安靜通過。

## 5. 測試結構

- 平鋪於 `tests/`（不分 unit/integration 子目錄——整合測試以 `test_pg_*`＋`KINSUN_IT=1` 區隔）。
- 命名：`test_<套件>_<檔>.py`；合約測試 `test_<領域>_store_contract.py`（Fake 每次跑＋Pg 需 IT 旗標）。
- 缺套件前綴的 9 檔已改名完成（✅ D-44，丙-12 git mv）。
- JS 測試與被測檔同目錄（co-located）：frontend 用 vitest（`*.test.tsx`）、app 用 jest-expo（`*.test.ts`），各自 `npm test`，CI 併入 frontend／app job（2026-07-17）。

## 6. 前端結構

```plaintext
app/src/
├── app/            Expo Router 檔案即路由：role → elder/{bind,login,talk}、guardian/{login,register,home,notifications,elder/[elderId]/{index,schedules}}
├── components/     AvatarPlaceholder（虛擬形象預留）、RoleSwitcher（內測雙端快切）、ui.tsx
└── lib/            api.ts（呼叫後端，snake_case 對齊）、schedules.ts（行程輸入組裝，純函式）、auth.ts＋SessionProvider.tsx（SecureStore）、location.ts（定位模糊化，精確座標不離開手機）、strings.ts、theme.ts（＋co-located 測試 location.test.ts）

frontend/src/
├── （根）          LIFF 家屬端：App/api/report/schedules/strings＋test-setup.ts（vitest）
├── pages/          EldersPage／SchedulesPage 等三頁
└── admin/          觀測後台（獨立 Vite config）：pages/ 八頁（含 NewsPage 話題新聞，D-74）＋長輩詳情五分頁 elder-tabs/＋useLoadable/usePolling 兩 hook（各附 .test.tsx）

web/src/          （P2 家屬端完成；P3 對講機協定核心＋錄音播放與 iOS 音訊解鎖＋QR 掃碼＋長輩端 API 呼叫端與文案＋長輩配對與帳密重登＋對講機＋長輩提醒列表已完成，長輩欄四畫面全數落地、P3 收尾；P4 Task 1 **模擬系統通知橫幅**已完成並經審查修正（純新增尚未接線），2026-07-31）
├── gate/           開場運營狀態頁：useDemoStatus／StatusCard／GatePage
├── stage/          撕裂展開動畫＋雙欄舞台：TearTransition／PhoneFrame／StagePage（左欄已接上真正的 ElderApp，P3 Task 7，2026-07-31；原 ElderPanePlaceholder 移除）、reducedMotion.ts（`prefersReducedMotion`，P4 Task 1 審查修正新增，2026-07-31——`TearTransition.tsx` 與 `notify/NotificationBanner.tsx` 共用同一份，原本各自宣告一份一模一樣的函式）；PhoneFrame 通知容身處新增 `role="status"`／`aria-live="polite"`（該屬性從 notify/NotificationBanner.tsx 移過來，見 07 §7 Important 3）
├── session/        雙 session 工廠（與 App 單例的根本差異，見 12 §2）：createSessionContext／storage／contexts／useSignOutOnAuthError
├── guardian/       家屬端六畫面（P2，2026-07-31）：GuardianApp（switch 路由，六案例窮盡＋never 型別檢查）、LoginScreen、RegisterScreen、HomeScreen、InviteCard（綁定碼卡＋QR＋複製）、ElderDetailScreen、SchedulesScreen、NotificationsScreen、api.ts（REST 呼叫端）、schedules.ts（欄位↔occurrences 組裝；回診前一天時區已修正，見 12 §9 F-16）
├── elder/          長輩端 API 呼叫端、定位與畫面（P3 Task 6／7／8／9，2026-07-31；審查修正見下）：api.ts（bindElderDevice／loginElder／logoutSession／postTurn／getTurnChunk／listElderNotifications，慣例比照 guardian/api.ts，見 07 §7）、location.ts（currentPlace；⚠️ 網頁無反查地名 API，**一律回 null**——含成功取得座標的分支，審查修正 2026-07-31：原本回傳含空地名的物件，與後端「缺地名整組忽略」的行為衝突且平白洩漏座標，見 12 §9 F-17）、ElderApp.tsx（switch 路由，四路由：bind／login／talk／notifications 全數接上真正畫面；default 分支為 never 型別窮盡檢查，同家屬端 GuardianApp）、BindScreen.tsx（掃 QR 或輸入綁定碼，五種綁定錯誤＋六種相機錯誤皆有長輩話，見 07 §7）、LoginScreen.tsx（帳密重登，✅ D-71 己-6）、useTalk.ts（對講機的狀態與副作用：權限、手勢、長連線、播放、分段續拉；依賴全部以 deps 注入，見 07 §7）、TalkScreen.tsx（對講機主畫面，含登出二次確認列）、Avatar.tsx（虛擬形象預留區，四種表情各配 aria-label）、NotificationsScreen.tsx（P3 Task 9，長輩端提醒列表，與家屬版刻意不共用元件；錯誤取代空狀態、已讀水位取最新一則＋分鍵＋寫入失敗隔離，見 07 §7）
├── ui/             四端共用 UI 慣例的 web 版：Button／Field／Section／Feedback（EmptyHint／ErrorText／NoticeText）
├── nav/            手機外框內部的畫面堆疊：useScreenStack（不進瀏覽器網址列）
├── notify/         通知已讀水位：seen.ts（loadSeenAt／saveSeenAt／unreadCount，兩角色分鍵，見 07 §7）＋模擬系統通知橫幅（P4 Task 1＋審查修正，2026-07-31）：NotificationBanner.tsx（key 綁 item.id；role="status" 已移到 stage/PhoneFrame.tsx，本檔改用 data-testid="notification-banner"；size?: "normal"|"big" 放大長輩端字級與關閉鍵）、osStyle.ts（detectOs；PhoneOs 型別轉出自 stage/PhoneFrame，不重新宣告）＋通知輪詢（P4 Task 2，2026-07-31）：useNotificationFeed.ts（pickNewItems 純函式＋useNotificationFeed，把後端既有兩支 App 內通知端點變成畫面上的橫幅，兩秒輪詢；QUEUE_MAX=20 佇列上限、sessionRef 擋換人時舊資料晚到、document.hidden 分頁背景暫停，見 07 §7）——純新增，PhoneFrame 既有的 notificationSlot 尚未被 StagePage 傳入，未接進畫面，見 07 §7
├── talk/           對講機（P3 Task 3／4／5／8，2026-07-31）：talkSocket.ts（協定核心，自 `app/src/lib/` 原樣搬移，網頁端差異——`connect()` 設 `binaryType = "arraybuffer"`、`TalkFrame` 追加 `queued`，見 07 §7）、talkGesture.ts（麥克風手勢狀態機，零改動）、recorder.ts（瀏覽器錄音，取代 App 的 expo-audio）、playback.ts（網頁播放＋blob URL 回收，取代 App 的 expo-audio＋expo-file-system）、audioUnlock.ts（iOS Safari 音訊解鎖）、qrScanner.ts（相機掃 QR，長輩端配對用；WebAssembly 版 ZXing，見 07 §7）、mediaErrors.ts（getUserMedia 失敗分類，相機與麥克風共用；零依賴——不可從 recorder.ts 匯入 qrScanner.ts，後者模組載入時就會拉進 1 MB 條碼 wasm 載入器）
├── App.tsx／main.tsx  路由與階段（僅 /demo、/demo/stage 兩條瀏覽器路由；框內畫面切換是元件狀態不進網址列）
├── api.ts          createApiClient（同源，不設 baseUrl；後端無 CORS middleware）
├── strings.ts      zh-TW 字串常數集中（沿用 D-50）
├── theme.css       Tailwind v4 @theme：沿用既有設計 token（9 色／22-30-40 字級）
└── vite-env.d.ts   標準 Vite 型別參考（`/// <reference types="vite/client" />`），供 `import.meta.env.BASE_URL` 型別檢查（P3 Task 5 審查修正新增，qrScanner.ts 用它組 wasm 同源位址，不寫死 "/demo/"）
```

`web/scripts/verify-wasm-checksum.mjs`（P3 Task 5 審查修正，2026-07-31，Minor 7）：純 Node.js（`fs`／`crypto`，不用 shell 的 `cmp`——本專案橫跨 Windows／macOS／DGX，AGENTS.md 環境紀律）比對 `web/public/zxing_reader.wasm` 與 `node_modules/zxing-wasm` 目前版本內附 wasm 二進位的雜湊，`npm run build` 前置執行，兩者不一致（例如 `npm update` 升級了 glue JS 卻忘記重新複製 wasm）立即失敗並印出修法；此為建置工具腳本、非 `src/` 樹範圍，故不列入上方 ASCII 樹。

詳細前端規範見 [12_前端架構規範](12_前端架構規範.md)／[17_前端資訊架構](17_前端資訊架構.md)。

## 7. 演進原則

- 頂層結構重大變更需 ADR（`docs/dev/04_adr/`）。
- 結構變更同步：05（架構契約）、09（依賴）、10（類別）、14（部署）。

## 變更紀錄

| 版本 | 日期 | 變更 |
| :--- | :--- | :--- |
| v1.39 | 2026-07-31 | **網頁版前端 P4 Task 2：通知輪詢**：§6 `web/src/` 樹 `notify/` 那行新增 `useNotificationFeed.ts`（`pickNewItems`／`useNotificationFeed`）。詳細模組規格、brief 缺陷修正（`unread` 恆為 0、佇列無上限）、新增修正（換人時舊資料晚到的競態）、偏離點（分頁可見性暫停、品牌字串改讀 `strings.gate.brand`、`render` 期間比對取代 `useEffect` 以符合 `eslint-plugin-react-hooks` v7 新規則）見 07 §7 v1.82。web 全套測試 445→**464**。純新增，尚未接進畫面 |
| v1.38 | 2026-07-31 | **P4 Task 1 審查修正**（0 Critical，3 Important＋4 Minor）：§6 `stage/` 那行新增 `reducedMotion.ts`（`prefersReducedMotion`，`TearTransition.tsx` 改為 import 共用，消除與 `notify/NotificationBanner.tsx` 原本各自宣告的重複）；`notify/` 那行更正——`NotificationBanner.tsx` 描述改為「`role="status"` 已移到 `stage/PhoneFrame.tsx`，本檔用 `data-testid`；新增 `size` prop 放大長輩端字級與關閉鍵」。詳細模組規格、brief 缺陷修正、變異驗證見 07 §7 v1.81 |
| v1.37 | 2026-07-31 | **網頁版前端 P4 Task 1：模擬系統通知橫幅**：§6 `web/src/` 樹 `notify/` 那行新增 `NotificationBanner.tsx`（`<NotificationBanner item os onDismiss />`）／`osStyle.ts`（`detectOs`）；樹頂註記補「＋通知橫幅已完成，純新增尚未接線」。詳細模組規格、brief 缺陷修正見 07 §7 v1.80 |
| v1.36 | 2026-07-31 | **網頁版前端 P3 Task 9：長輩提醒列表與收尾**：§6 `web/src/` 樹 `elder/` 那行新增 `NotificationsScreen.tsx`；`ElderApp.tsx` 描述更正為四路由全數接上真正畫面；樹頂註記更正——長輩欄四畫面全數落地，P3 收尾。詳細模組規格見 07 §7 v1.78 |
| v1.35 | 2026-07-31 | **網頁版前端 P3 Task 8：對講機**：§6 `web/src/` 樹 `elder/` 那行新增 `useTalk.ts`（狀態與副作用）／`TalkScreen.tsx`（主畫面，含登出二次確認列）／`Avatar.tsx`（虛擬形象預留區）；`talk/` 那行新增 `mediaErrors.ts`（相機與麥克風共用的 `getUserMedia` 失敗分類，零依賴）；`ElderApp.tsx` 的 talk 路由不再是佔位；樹頂註記更正。詳細模組規格、brief 缺陷修正與偏離點見 07 §7 v1.75 |
| v1.34 | 2026-07-31 | **網頁版前端 P3 Task 7：長輩配對與帳密重登畫面**：§6 `web/src/` 樹 `elder/` 那行新增 `ElderApp.tsx`／`BindScreen.tsx`／`LoginScreen.tsx`；`stage/` 那行補註記左欄已接上真正的 `ElderApp`（原 `ElderPanePlaceholder` 移除）；樹頂註記更正——長輩配對與帳密重登畫面已完成，對講機與提醒兩個長輩端畫面待 Task 8／9 續行。詳細模組規格、brief 缺陷修正與偏離點見 07 §7 v1.73 |
| v1.33 | 2026-07-31 | **P3 Task 6 審查修正**：§6 `elder/` 那行更正 `location.ts` 描述為「一律回 null」（成功取得座標亦然），移除過期的「地名固定留空」半描述。詳見 07 §7 v1.72 |
| v1.32 | 2026-07-31 | **網頁版前端 P3 Task 6：長輩端 API 呼叫端與文案**：§6 `web/src/` 樹新增 `elder/`（`api.ts`／`location.ts`）；樹頂註記更正——＋長輩端 API 呼叫端與文案已完成。詳細模組規格與測試數見 07 §7 |
| v1.31 | 2026-07-31 | **P3 Task 5 審查修正**（Minor 7＋8）：§6 `web/src/` 樹補 `vite-env.d.ts`；新增 `web/scripts/verify-wasm-checksum.mjs` 說明（非 `src/` 樹範圍）。詳見 07 §7 v1.70 |
| v1.30 | 2026-07-31 | **網頁版前端 P3 Task 5：QR 掃碼**：§6 `web/src/` 樹 `talk/` 那行新增 `qrScanner.ts`（相機掃 QR，長輩端配對用）；樹頂註記更正——＋QR 掃碼已完成；新增 `web/public/zxing_reader.wasm`（掃碼用 wasm 二進位，同源靜態檔，不入 `src/` 樹範圍，同 `silent.wav` 既有慣例）。詳細模組規格與測試數見 07 §7 |
| v1.29 | 2026-07-31 | **網頁版前端 P3 Task 4：錄音、播放與 iOS 音訊解鎖**：§6 `web/src/` 樹 `talk/` 那行新增 `recorder.ts`（瀏覽器錄音）／`playback.ts`（網頁播放＋blob URL 回收）／`audioUnlock.ts`（iOS Safari 音訊解鎖）三檔；樹頂註記更正——對講機協定核心＋錄音播放與 iOS 音訊解鎖已完成，長輩端畫面本身仍待 P3 續行；新增 `web/public/silent.wav`（同源靜態無聲檔，不入 `src/` 樹範圍）。詳細模組規格與測試數見 07 §7 |
| v1.28 | 2026-07-31 | **網頁版前端 P3 對講機協定核心搬移**（Task 3）：§6 `web/src/` 樹新增 `talk/`（`talkSocket.ts`／`talkGesture.ts`），自 `app/src/lib/` 原樣搬移；樹頂註記更正——對講機協定核心已完成，長輩端畫面本身仍待 P3 續行。詳細模組規格與測試數見 07 §7 |
| v1.27 | 2026-07-31 | **網頁版前端 P3 對講機容量閘門接線**（Task 2）：§3 `channels/app/admission.py` 那行由「純新增、尚未接線」更正為已接上 `turns.py`／`ws.py`（共用同一個 `TurnAdmission`，由 `app.py` 建立並注入）；檔案清單不變（仍 12 檔）。新增 `TURN_` 三個環境變數鍵（見 07 §4／.env.example），`config.py` 的 `Settings` 102→**105** 欄 |
| v1.26 | 2026-07-31 | **網頁版前端 P3 對講機容量閘門**（Task 1）：`channels/app/` 新增 `admission.py`（`TurnAdmission`＋`AdmissionTimeout`）；§3 `channels/app/` 檔案清單 11→**12** 檔補列 `admission`。純新增一個尚未被任何路徑呼叫的 class（`limit` 由呼叫端傳入，不讀任何環境變數／`Settings`），故本輪不涉及 `.env.example`／`config.py`／`AGENTS.md` 前綴清單——那些留給 Task 2（閘門接上 `turns.py`／`ws.py` 並帶入環境變數） |
| v1.25 | 2026-07-31 | **網頁版前端 P2 家屬端複審殘留修正**：§6 `web/src/` 樹 `ui/` 那行的 `Feedback` 補列 `NoticeText`（成功／操作指示訊息，`role="status"`）。此元件由 P2 期間一支獨立 fix commit 新增，晚於回填本文件目錄樹的那支 commit（7605940），未被連帶同步——同步鐵律的補件 |
| v1.24 | 2026-07-31 | **網頁版前端 P2 家屬端完成回填**（Task 7 審查要求）：§6 `web/src/` 樹補 `guardian/`（十檔）、`ui/`、`nav/`、`notify/` 四個 P2 新增目錄；移除過期承諾「長輩／家屬畫面於 P2／P3 補齊」（家屬端已完成，僅長輩端仍待 P3）。此為單獨 commit，與程式碼變更分開，理由：目錄樹回填與本次程式修正無邏輯相依，混在一起會讓 diff 難以個別 review |
| v1.0 | 2026-07-08 | 初版；D-42 例外成文、D-43 AGENTS.md 對齊、D-44 測試改名列 WBS |
| v1.1 | 2026-07-12 | 內測基礎建設（D-73）：web/routers 增 meta.py／admin_jobs.py、app 增 components/RoleSwitcher、admin 前端頁面樹更新 |
| v1.2 | 2026-07-17 | 對齊程式現況：後端樹補 locations/／strategies/／turn_context.py／tools web_search＋lookups；web routers 標已落地；proactive 補自適應問候時間四檔；測試結構補 JS co-located 測試與 D-44 改名完成；前端樹補 location.ts／useLoadable／guardian 子頁；scripts 補 anchoring_probe.py |
| v1.3 | 2026-07-19 | 工程觀測（Opik）：後端新增 `src/kinsun/tracing/`（client／decorators／genai，唯一 import opik 之處，外部 adapter 定位免三件套）；新增頂層 `evals/`（與 tests/ 平行的離線評測套件：datasets／experiments）；架構契約見 [05 §12](05_架構與設計.md) |
| v1.4 | 2026-07-18 | 併入 RAG 目錄 17→23 檔（自 Jerry 分支 ec12cb8，原 v1.3）：新增 evaluation／migrate／refresh／release_cli／releases／worker，結構描述補版本發布與獨立週更。 |
| v1.5 | 2026-07-20 | tools/ 補 `transport.py`（交通四工具，改寫自 Kevin 原型）；`transport_agent/`（未接線原型）加 ruff `extend-exclude`，`.gitignore` 編碼修 UTF-8。 |
| v1.6 | 2026-07-25 | 後端樹補 `news/`（話題新聞，D-74，PR #75 Babic）。 |
| v1.7 | 2026-07-25 | tools/ 補 `news.py`（get_news 工具，D-74 消費端）。 |
| v1.8 | 2026-07-25 | news/ 補 `mentions.py`（提及紀錄三件套，D-42 語意命名例外）。 |
| v1.9 | 2026-07-25 | admin pages/ 七→八頁（NewsPage 話題新聞，D-74）。 |
| v1.10 | 2026-07-25 | news/fetchers/ 補 `rss.py`（通用 RSS 來源，Google News 台灣版預設）。 |
| v1.11 | 2026-07-25 | 頂層新增 `clock.py`（台灣時間白話講法＋`TimeFacts` 每輪注入），`tools/clock.py` 移除；頂層單檔而非 `clock/` 套件——時間無資料表也無 store，比照 `turn_context.py`／`pipeline.py` 的既有慣例。 |
| v1.12 | 2026-07-25 | 新增 `schedules/`（統一排程 D-76 P1 地基：models／store 三件套／service）。與既有 `scheduler/` 分工：前者是排程**資料**，後者是排程**引擎**。 |
| v1.13 | 2026-07-25 | `schedules/` 補 jobs／facts／wording／legacy_bridge（D-76 P2 派送切換）；`legacy_bridge.py` 為過渡對帳，P3 換掉家屬寫入端當天刪除。 |
| v1.14 | 2026-07-25 | 統一排程 D-76 收尾：`medications/`／`appointments/` 兩模組移除（資料表同步 DROP）；`schedules/` 補 flow／timeparse；`tools/` 補 schedules（全庫第一組會寫庫的工具）；App 與 LIFF 各自兩頁併成一頁。 |
| v1.15 | 2026-07-26 | 頂層新增 `background.py`（背景落庫）。單檔而非套件——無資料表也無 store，比照 `turn_context.py`／`clock.py` 的既有慣例。 |
| v1.16 | 2026-07-26 | `speech/` 補 `chunking.py`（回覆切句，供 TTS 分段串流）。放 speech/ 而非頂層——它是 TTS 的前處理、與 tts.py 同一關注點。 |
| v1.17 | 2026-07-27 | `scheduler/` → `cron/`（含 `python -m kinsun.cron`、logger 名、`tests/test_cron_*.py`）：與 `schedules/` 只差一個 s 卻是兩個層次，界線改由檔名承擔。新增 `cron/registry.py`＝全系統排程宣告的唯一真實來源（跨兩個程序）。`tools/transport_agent/`（未接線原型）移出 `src/` 到 `docs/archive/transport_agent/`——`src/` 裡不該有需要靠 lint 例外才待得住的目錄；放 `docs/archive/` 而非 `ref/`，是因為 `ref/` 在 .gitignore 內，搬過去等於把 Kevin 的原型從共用 repo 刪掉。ruff 的 `extend-exclude` 改指 `docs/archive`（封存區是「當時長這樣」的快照，刻意不隨規範演進而改寫；⚠ CI 跑的是無參數的 `ruff check .`，少了這行封存進來的舊程式碼會直接把 CI 弄紅）。 |
| v1.18 | 2026-07-27 | 新增附近地點搜尋（spec 2026-07-27）：頂層新增 `places/`（models／geo／store／categories／refine／ingest，6 檔）——非長輩擁有的全域 POI 查詢表，故獨立於 `locations/` 之外；`tools/` 補 `places.py`（`search_nearby_places`，讀 `places`＋`locations` 兩 store）。 |
| v1.23 | 2026-07-31 | 網頁版前端 P1 地基（任務 12，spec 2026-07-30-web-demo-design）：頂層結構（§2）新增 `web/`；§6 前端結構補 `web/src/` 目錄樹。P1 僅完成地基（開場運營狀態頁→撕裂展開→雙欄手機外框＋雙 session 工廠），長輩／家屬實際畫面（`elder/`／`guardian/`）為 P2／P3 工項，屆時再補目錄樹。 |
| v1.22 | 2026-07-30 | 延遲優化四刀（2026-07-30）的結構面：①`channels/app/` 新增 `inbound_audio.py`——放 `channels/app/` 而非頂層或 `audio/`，因為它是**這個通道的進站順序決策**（何時上傳、要不要等），`audio/publisher.py` 仍只管「怎麼上傳」；抽成共用模組是因為 `turns.py`（HTTP）與 `ws.py`（長連線）先前各有一份 `_publish_inbound`，兩份分頭修必然只修好一條路。②`safety/` 新增 `combined_classifier.py`——判準規則刻意留在 `detector`／`moderation`，本檔只負責「一次呼叫取回兩份原始判斷」，兩條路徑（分開／合併）才不會分岔出兩份決策邏輯。⚠️ **一併補齊 `safety/` 那行漏列的三檔**（`moderation`／`evaluation`／`local_detector`）：該行自濫用審核與地端偵測器落地後就沒更新，實際 11 檔而清單只寫 7 個，於是「safety 有哪些檔」在本文件與 07 §1 都是錯的（07 同步更正為 11 檔）；`channels/` 那行同時補上 line／app 的子檔清單與 11 檔總數。⚠️ 另載明一處文件缺口、本版不代為追記：**變更紀錄缺 v1.19～v1.21 三列**（版頭直接跳號，那三批的內容只留在版頭狀態文字裡），無從確認當時的分列口徑，故僅指出缺口不補寫。 |
