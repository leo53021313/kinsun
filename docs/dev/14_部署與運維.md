# 部署與運維 - 金孫 KinSun

> **版本:** v1.4 | **更新:** 2026-07-19 | **狀態:** ✅ 定稿（D-64 備援⏸延後至發表文件期定——會-13；補工程觀測 Opik 服務、環境變數、公開隧道遠端檢視）
> 部署拓撲圖見 [05 §5](05_架構與設計.md)（不重複）；本檔為操作面：啟停、環境、備份、告警、Runbook。

---

## 1. 執行單元與啟停

`scripts/kinsun.sh` 統一管理七服務：`asr(8001) tts(8002) webhook(8000) scheduler frontend(5173) app(8081) ngrok`——setsid 獨立進程組、log 至 `logs/<name>.log`、PID 至 `.run/<name>.pid`。

| 指令 | 行為 |
| :--- | :--- |
| `scripts/kinsun.sh start|stop|restart [服務]` | 全部或指定服務（如 `restart app`） |
| `scripts/kinsun.sh status` | PID 存活檢查；`app` 那列即 Expo Go 要掃的 `exp://` 位址 |

**App（Expo dev server）**：**預設走 tunnel**（✅ 團隊測試手機與 DGX 從不在同一個 Wi-Fi）——Expo 經 `exp.direct` 反向隧道對外，任何網路的手機都連得到，`status` 顯示形如 `exp://xxx-anonymous-8081.exp.direct` 的對外位址供 Expo Go 掃描（iOS 用相機、Android 用 Expo Go 內建掃碼）。

- **位址從哪來**：Expo 只在互動終端印 QR／網址，背景啟動時 log 裡一個字都沒有——`status` 改問 Metro 的 manifest（`launchAsset.url` 的 host 即手機實際要連的位置），tunnel 與 LAN 兩種模式都準。
- **tunnel 偶發失敗**：`exp.direct` 端會出現 `remote gone away`，且失敗時 Expo 整個退出、連 Metro 都不留。故啟動時會等到 tunnel 就緒才算數，**失敗自動重試一次**（實測命中率頗高）。兩次都失敗時另有明確提示。
- **區網模式**：`KINSUN_EXPO_TUNNEL=0`（僅同網段可用、啟動較快）。此模式以 `REACT_NATIVE_PACKAGER_HOSTNAME` 釘住區網 IP——DGX 有 docker(172.17.x)／Tailscale(100.x) 等多張網卡，不釘住則 Expo 可能挑錯介面、手機抓不到 bundle。

to-be：API 改多 worker 啟動（✅ D-20）。

## 2. 環境策略

| 環境 | 組態 | 用途 |
| :--- | :--- | :--- |
| 開發（各自筆電） | `ASR_BACKEND=mock`＋`TTS_BACKEND=bubble`＋AllowAllGate（D-19 修語意後保留） | 離線開發，跨 Windows/macOS |
| 內測（DGX，D-73） | 全真模型＋`BINDING_GATE_ENABLED=true`＋**`INTERNAL_TESTING_ENABLED=true`**（重啟生效；經公開 `GET /api/v1/meta` 下發，自建 APK 免重發版；建議加開 `ASR_DEBUG_SHOW_TRANSCRIPT=true` 看辨識品質） | 內部測試：App 切換身分＋後台手動觸發 |
| DGX（demo／準正式） | 全真模型＋`BINDING_GATE_ENABLED=true` 強制；`INTERNAL_TESTING_ENABLED` **務必 false**（預設） | 實測與 8/20 展示 |

設定紀律：程式讀的每個鍵都列 `.env.example`＋預設值＋中文註解（AGENTS.md）；`NGROK_*` 與 DGX 服務環境變數不經 config.py、各自 README 管理；內測旗標集中於 `.env.example`「內部測試」區塊（D-73）。

## 3. 備份與資料保全（✅ D-63 修訂：輕量手動版，2026-07-09 已上線）

- **現況 ✅**：`scripts/backup_db.sh` 一鍵手動備份——以 Docker `postgres:17-alpine` 執行 pg_dump（DGX 未裝且伺服器為 PG 17），自訂格式存 `data/backups/`（gitignore 排除、檔案權限 600），完成後自動以 `pg_restore --list` 驗證可讀。
- **時機約定（不做排程——D-63 修訂）**：①資料庫結構大改前（如乙批 migration）②長輩實測開始前。還原指令見腳本檔頭註解。
- 2026-07-09 首次備份實測通過（328K／480 物件）。
- mem0 稽核檔：`history_db_path` 設至 `data/mem0/history.db`＋.gitignore（✅ D-65）。
- 音檔：2 天自動清理（`AUDIO_RETENTION_DAYS=2`；設 0 可停用清理——開關保留）。

## 3.5 資料庫連線總量（✅ 庚-26／A-55）

Supabase 直連（5432）上限＝免費方案約 60 條。每進程開一個連線池，
上限 `DATABASE_POOL_MAX_SIZE`（預設 5）。總量公式：

```
WEB_WORKERS × DATABASE_POOL_MAX_SIZE ＋ 排程 worker × DATABASE_POOL_MAX_SIZE ≤ 60
```

- 預設（WEB_WORKERS=2）常駐 15 條，寬裕；**WEB_WORKERS 安全上限約 8**（45 條，留 CLI 餘裕）。
- 零星加項：記憶整理 CLI、RAG ingest、備份腳本各短暫 +5。
- ⚠️ 不可為了拉高上限改用 6543 交易池化埠：認證節流（庚-08）依賴 advisory lock，
  交易池化下 session 級鎖語意不成立。
- 爆線症狀＝連線被拒（API 500），不是變慢——排障時先想到本公式。

## 4. 監控與告警

| 層 | 現況 | to-be |
| :--- | :--- | :--- |
| 健康檢查 | DGX 兩服務＋主 API `/healthz` ✅（D-67，丙-13 完成） | 結案 |
| 鏈路追蹤 | 五階段 trace＋admin 後台 ✅ | token 欄補齊（G-6） |
| 告警 | admin 告警橫幅 ✅（2026-07-09 甲-5：分級器故障近 1 小時 ≥3 次即亮） | LINE 推播告警取消（D-66 修訂 2026-07-09），本列結案 |
| 日誌 | logs/*.log 每服務一檔 | 維持（單機規模） |

## 5. 8/20 發表日 Runbook（D-64 ⏸ 延後——備援與彩排等發表文件期一起定，會-13）

**會前檢查（發表日早上）**
1. `scripts/kinsun.sh status` 七服務全綠
2. 兩個 `/healthz`＋主 API `/healthz`（✅ D-67 已落地）回 200
3. ngrok 隧道通、LINE webhook 驗證、Expo Go 掃碼可連
4. Gemini 額度餘量確認（D-16 免費層）
5. 測試長輩帳號走一輪對講機（含危急詞展示句）

**故障退路（⏸ 延後補）**：預錄 demo 影片／mock 模式退路／彩排時程——D-64 於發表文件期定案（會-13，2026-07-09）。

**已知單點**：DGX（模型）、ngrok（入口）、校網——無備援，退路靠 D-64 決議。

## 6. 部署工項（餵 16_WBS）

| # | 工項 | 依據 |
| :--- | :--- | :--- |
| DEP-1 | 週備 pg_dump job＋輪替＋還原演練一次 | ✅ D-63（P0） |
| DEP-2 | 告警管道（admin 橫幅＋LINE 推播）＋D-31 訊號接入 | ✅ D-66 |
| DEP-3 | 主 API `/healthz` | ✅ D-67 |
| DEP-4 | history_db_path 固定＋.gitignore | ✅ D-65 |
| DEP-5 | API 多 worker 啟動改造 | ✅ D-20（=A-3） |
| DEP-6 | 發表日備援與彩排 | ➡ D-64 會議 |

## 工程觀測（Opik）服務

工程／LLM 深度視角的觀測後端，自架於 DGX，**不進版控**（外部基礎設施，比照資料庫）。

- **服務位置**：`/home/leo29/opik`（git clone，非 kinsun repo）；容器名 `opik-opik-*`。
- **啟停**：`cd /home/leo29/opik && ./opik.sh`（啟動）／`./opik.sh --stop`／`./opik.sh --clean`（清資料）。
- **連結顯示**：`scripts/kinsun.sh status`／`start` 結尾會印出 Opik 觀測後台連結（由 `OPIK_URL_OVERRIDE` 去 `/api` 推得，預設 `http://localhost:5273`）、服務是否在跑、以及 `OPIK_ENABLED` 旗標。服務本身不由 kinsun.sh 管理（只顯示）。
- **遠端／隊友檢視（Opik 公開隧道，opt-in）**：`scripts/kinsun.sh start opik` 開 Cloudflare Quick Tunnel（cloudflared，已裝於 DGX，aarch64）到 `:5273`；`status` 動態顯示當前 `*.trycloudflare.com` 公開網址（每次重啟會變，故動態讀 `logs/opik.log`）。`stop opik` 或 `stop`（全部）都會關閉。**不在預設 `start` 清單**（避免誤開）。
  - ⚠️ **安全**：Quick Tunnel 免網域但【公開、無認證】——Opik 自架版本身無登入，等於任何拿到網址者可讀所有 trace。目前 app 端 `OPIK_ENABLED=false`（僅 demo/測試資料），故臨時開無妨；**真實長輩資料進入 Opik 前，切勿長時間開啟**。
  - **正式版（有 Cloudflare 網域時）**：改用 Named Tunnel＋Cloudflare Access（Email/Google 登入），提供固定子網域＋認證。需 `cloudflared tunnel login`（互動授權本人 CF 帳號）後建 tunnel／DNS／Access 政策。目前無網域，故先用 Quick Tunnel。
  - ngrok（webhook :8000）留給 App API（見 §1；`app/.env` 的 `EXPO_PUBLIC_API_URL` 指向該網域），不與 Opik 隧道互搶；免費 ngrok 只有一個網域，故 Opik 不共用 ngrok。
- **對外埠**：僅前端 `5273`（UI＋API 反代）；資料庫（clickhouse/mysql/minio/redis/zookeeper）預設不對外，與 kinsun（postgres 5433／uvicorn 8000）零衝突。
- **資料本體**：docker named volumes（rootless：`~/.local/share/docker/volumes/opik-opik_*`）；**要備份的是這些 volume**，服務程式碼可重新 clone。
- **ARM64**：核心映像原生 aarch64（已驗證）；選配 Guardrails（PII/主題，英文模型、吃 GPU）預設關閉且無 arm64 映像，需用再自 build。

### 環境變數（OPIK_ 前綴；app 端 client）

| 變數 | 預設 | 說明 |
| :--- | :--- | :--- |
| `OPIK_ENABLED` | `false` | 總開關；false＝全模組 no-op、零行為改變 |
| `OPIK_URL_OVERRIDE` | `http://localhost:5273/api` | 自架 Opik 後端位址（搬遷只改此值） |
| `OPIK_WORKSPACE` | `default` | 自架工作區 |
| `OPIK_PROJECT_NAME` | `kinsun` | Opik UI 內的分組 |

> **安全**：接真實長輩資料時，Opik 自架版預設幾無存取控制；共用或上線前須先處理存取控制與去識別化。

## 變更紀錄

| 版本 | 日期 | 變更 |
| :--- | :--- | :--- |
| v1.0 | 2026-07-08 | 初版：D-63～D-67；發表日 Runbook 骨架 |
| v1.1 | 2026-07-12 | 內測基礎建設（D-73）：§2 環境策略補「內測」列（INTERNAL_TESTING_ENABLED 開關操作）與內測旗標區塊紀律 |
| v1.2 | 2026-07-17 | 主 API /healthz 結案（丙-13）；Runbook 服務數對齊七服務（含 Expo dev server） |
| v1.3 | 2026-07-19 | 工程觀測（Opik）：新增「Opik 服務」章節（啟停／埠／資料卷／備份範圍／ARM64）與 OPIK_ 環境變數表；架構契約見 [05 §12](05_架構與設計.md) |
| v1.4 | 2026-07-19 | Opik 公開隧道遠端檢視：`kinsun.sh start/stop opik`（Cloudflare Quick Tunnel，opt-in、動態網址、公開無認證警語）；正式版 Named Tunnel＋Access 待有 CF 網域；釐清 ngrok 網域專供 App API 不共用 |
