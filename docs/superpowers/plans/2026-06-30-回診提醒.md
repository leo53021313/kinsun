# 回診提醒 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓家屬在 LINE 設定長輩的一次性回診，系統在前一天早上與當天早上各推一次提醒給長輩與家屬，並把即將到來的回診每輪注入 agent 情境。

**Architecture:** 新增 `src/kinsun/appointment/` 套件，結構鏡像既有 `medication/`（models/store/service/facts/jobs/flow）。排程沿用既有 `Scheduler/fanout/state`，只加一個每日 job，靠「今天/明天」日期選取決定兩次提醒。設定流程併入既有「設定」引導選單（第 5 項）。

**Tech Stack:** Python 3.12、uv、FastAPI、psycopg v3、pytest、ruff。長期記憶 Mem0/pgvector（本案不動）。

## Global Constraints

- 一律台灣繁體中文：文件、程式碼註解、commit 訊息。
- Python 3.12；ruff 規則 `E,W,F,I,B,UP`，line-length 100。
- 不新增第三方套件（本案只用標準庫 `datetime`/`uuid`/`logging` 與既有依賴）。
- 所有 store 走 Protocol + 注入 fake；時鐘/亂數一律注入以利 TDD。
- 全程 fail-safe：記憶/LLM/DB 失敗都退化、不中斷對話。
- 日期一律 ISO 字串 `YYYY-MM-DD`；DB 欄位名用 `appt_date`（避開保留字），model 屬性叫 `date`。
- commit 訊息型別：`feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 單元測試全離線（注入 fake，不需金鑰/GPU）；雲端整合測試以 `KINSUN_IT=1` opt-in。

---

## 檔案結構（File Structure）

**新增：**
- `src/kinsun/appointment/__init__.py` — 空套件標記。
- `src/kinsun/appointment/models.py` — `Appointment` 資料類別。
- `src/kinsun/appointment/store.py` — `AppointmentError`、`AppointmentStore`(Protocol)、`PgAppointmentStore`。
- `src/kinsun/appointment/service.py` — `AppointmentService`（add/list/upcoming/remove）。
- `src/kinsun/appointment/jobs.py` — `build_appointment_reminder_job`。
- `src/kinsun/appointment/facts.py` — `AppointmentFacts`（FactProvider）。
- `src/kinsun/appointment/flow.py` — `AppointmentMenu`（引導式設定）。
- 測試：`tests/test_appointment_store.py`、`test_appointment_service.py`、`test_appointment_jobs.py`、`test_appointment_facts.py`、`test_appointment_flow.py`。

**修改：**
- `src/kinsun/db.py` — `APPOINTMENTS_DDL` + `ensure_schema`。
- `src/kinsun/accounts/service.py` — `guardian_line_ids_of_elder`（+ `guardian_line_ids` 委派去重）。
- `src/kinsun/recall.py` — `recall` 逐 provider try/except（fail-safe）。
- `src/kinsun/binding/session.py` — `BindingState` 新增 `APPT_*`。
- `src/kinsun/binding/flow.py` — 建構子多收 `appointment`、選單第 5 項、`appt_` 分派。
- `src/kinsun/config.py` — `appointment_reminder_hour`。
- `src/kinsun/app.py`、`src/kinsun/scheduler/worker.py` — 組裝接線。
- `tests/fakes.py` — `FakeAppointmentStore`。
- `tests/test_memory_context.py`、`test_accounts_service.py`、`test_binding_flow.py`、`test_config.py` — 對應修改。
- `.env.example` — `APPOINTMENT_REMINDER_HOUR=8`。

---

## Task 1：回診資料模型 + 儲存 + DDL + Fake

**Files:**
- Create: `src/kinsun/appointment/__init__.py`、`src/kinsun/appointment/models.py`、`src/kinsun/appointment/store.py`
- Modify: `src/kinsun/db.py`、`tests/fakes.py`
- Test: `tests/test_appointment_store.py`

**Interfaces:**
- Produces:
  - `Appointment(appt_id: str, elder_id: str, date: str, label: str)`（frozen dataclass）
  - `AppointmentError(Exception)`
  - `AppointmentStore` Protocol：`add(appt)`, `list_for_elder(elder_id)->list[Appointment]`, `list_for_date(date)->list[Appointment]`, `remove(appt_id)`
  - `PgAppointmentStore(db: Database)`、`FakeAppointmentStore()`

- [ ] **Step 1：寫失敗測試** `tests/test_appointment_store.py`

```python
import os

import pytest

from kinsun.appointment.models import Appointment
from tests.fakes import FakeAppointmentStore


def _appt(appt_id, elder_id, date, label):
    return Appointment(appt_id, elder_id, date, label)


def test_fake_store_round_trip():
    store = FakeAppointmentStore()
    store.add(_appt("a1", "e1", "2026-07-15", "心臟科回診"))
    store.add(_appt("a2", "e1", "2026-07-01", "眼科回診"))
    assert [a.date for a in store.list_for_elder("e1")] == ["2026-07-01", "2026-07-15"]
    assert [a.appt_id for a in store.list_for_date("2026-07-15")] == ["a1"]
    store.remove("a1")
    assert store.list_for_date("2026-07-15") == []


@pytest.mark.skipif(os.environ.get("KINSUN_IT") != "1", reason="需雲端 key")
def test_pg_store_round_trip():
    from kinsun.appointment.store import PgAppointmentStore
    from kinsun.db import Database, ensure_schema

    url = os.environ["DATABASE_URL"]
    ensure_schema(url)
    store = PgAppointmentStore(Database.open(url))
    store.add(_appt("ap", "ep", "2026-08-20", "測試回診"))
    got = store.list_for_elder("ep")[0]
    assert got.label == "測試回診"
    assert [a.appt_id for a in store.list_for_date("2026-08-20") if a.appt_id == "ap"] == ["ap"]
    store.remove("ap")
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_appointment_store.py -v`
Expected: FAIL（`ModuleNotFoundError: kinsun.appointment` / `ImportError: FakeAppointmentStore`）

- [ ] **Step 3：建立 model、store、fake、DDL**

`src/kinsun/appointment/__init__.py`：空檔。

`src/kinsun/appointment/models.py`：
```python
"""回診提醒的資料模型。"""

from __future__ import annotations

from dataclasses import dataclass


@dataclass(frozen=True)
class Appointment:
    appt_id: str
    elder_id: str
    date: str  # ISO "YYYY-MM-DD"
    label: str  # 自由文字，例「上午10點 心臟科回診 林口長庚」
```

`src/kinsun/appointment/store.py`：
```python
"""回診提醒儲存：Protocol 與 Postgres 實作。"""

from __future__ import annotations

from typing import Protocol

from kinsun.appointment.models import Appointment
from kinsun.db import Database, _Errors


class AppointmentError(Exception):
    """回診資料讀寫失敗。"""


class AppointmentStore(Protocol):
    def add(self, appt: Appointment) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[Appointment]: ...
    def list_for_date(self, date: str) -> list[Appointment]: ...
    def remove(self, appt_id: str) -> None: ...


class PgAppointmentStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: AppointmentError(f"回診資料存取失敗：{m}"))

    def _to_appt(self, row: tuple) -> Appointment:
        appt_id, elder_id, appt_date, label = row
        return Appointment(appt_id, elder_id, appt_date, label)

    def add(self, appt: Appointment) -> None:
        self._db.execute(
            "INSERT INTO appointments (appt_id, elder_id, appt_date, label) "
            "VALUES (%s, %s, %s, %s) ON CONFLICT (appt_id) DO UPDATE SET "
            "elder_id = EXCLUDED.elder_id, appt_date = EXCLUDED.appt_date, "
            "label = EXCLUDED.label",
            (appt.appt_id, appt.elder_id, appt.date, appt.label),
        )

    def list_for_elder(self, elder_id: str) -> list[Appointment]:
        rows = self._db.query(
            "SELECT appt_id, elder_id, appt_date, label FROM appointments "
            "WHERE elder_id = %s ORDER BY appt_date",
            (elder_id,),
        )
        return [self._to_appt(r) for r in rows]

    def list_for_date(self, date: str) -> list[Appointment]:
        rows = self._db.query(
            "SELECT appt_id, elder_id, appt_date, label FROM appointments WHERE appt_date = %s",
            (date,),
        )
        return [self._to_appt(r) for r in rows]

    def remove(self, appt_id: str) -> None:
        self._db.execute("DELETE FROM appointments WHERE appt_id = %s", (appt_id,))
```

`src/kinsun/db.py`：在 `MEDICATIONS_DDL` 之後新增：
```python
APPOINTMENTS_DDL = (
    "CREATE TABLE IF NOT EXISTS appointments ("
    "appt_id TEXT PRIMARY KEY, elder_id TEXT NOT NULL, "
    "appt_date TEXT NOT NULL, label TEXT NOT NULL);"
    "CREATE INDEX IF NOT EXISTS idx_appt_date ON appointments (appt_date);"
)
```
並在 `ensure_schema` 的 `conn.execute(MEDICATIONS_DDL)` 之後加一行：
```python
        conn.execute(APPOINTMENTS_DDL)
```

`tests/fakes.py`：檔末新增（接在 `FakeMedicationStore` 之後）：
```python
class FakeAppointmentStore:
    def __init__(self) -> None:
        self._appts = {}

    def add(self, appt):
        self._appts[appt.appt_id] = appt

    def list_for_elder(self, elder_id):
        rows = [a for a in self._appts.values() if a.elder_id == elder_id]
        return sorted(rows, key=lambda a: a.date)

    def list_for_date(self, date):
        return [a for a in self._appts.values() if a.date == date]

    def remove(self, appt_id):
        self._appts.pop(appt_id, None)
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_appointment_store.py -v`
Expected: PASS（`test_fake_store_round_trip` 通過；`test_pg_store_round_trip` SKIPPED）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/appointment/__init__.py src/kinsun/appointment/models.py \
        src/kinsun/appointment/store.py src/kinsun/db.py tests/fakes.py \
        tests/test_appointment_store.py
git commit -m "feat: 回診資料模型與儲存（appointments 表 + Fake）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：AppointmentService

**Files:**
- Create: `src/kinsun/appointment/service.py`
- Test: `tests/test_appointment_service.py`

**Interfaces:**
- Consumes: `AppointmentStore`、`Appointment`（Task 1）
- Produces: `AppointmentService(store, *, new_id=None)`：`add(elder_id, date, label)->Appointment`、`list_for_elder(elder_id)`、`upcoming(elder_id, today)->list[Appointment]`、`remove(appt_id)`

- [ ] **Step 1：寫失敗測試** `tests/test_appointment_service.py`

```python
from itertools import count

from kinsun.appointment.service import AppointmentService
from tests.fakes import FakeAppointmentStore


def _svc():
    ids = (f"a{i}" for i in count(1))
    return AppointmentService(FakeAppointmentStore(), new_id=lambda: next(ids))


def test_add_assigns_id_and_persists():
    svc = _svc()
    appt = svc.add("e1", "2026-07-15", "心臟科回診")
    assert appt.appt_id == "a1"
    assert [a.label for a in svc.list_for_elder("e1")] == ["心臟科回診"]


def test_upcoming_filters_past_keeps_today():
    svc = _svc()
    svc.add("e1", "2026-07-01", "過去")
    svc.add("e1", "2026-07-10", "今天")
    svc.add("e1", "2026-07-20", "未來")
    assert [a.label for a in svc.upcoming("e1", "2026-07-10")] == ["今天", "未來"]


def test_remove():
    svc = _svc()
    appt = svc.add("e1", "2026-07-15", "x")
    svc.remove(appt.appt_id)
    assert svc.list_for_elder("e1") == []
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_appointment_service.py -v`
Expected: FAIL（`ImportError: AppointmentService`）

- [ ] **Step 3：實作** `src/kinsun/appointment/service.py`

```python
"""回診提醒服務（新增/查看/upcoming/刪除）。"""

from __future__ import annotations

import uuid
from collections.abc import Callable

from kinsun.appointment.models import Appointment
from kinsun.appointment.store import AppointmentStore


class AppointmentService:
    def __init__(
        self, store: AppointmentStore, *, new_id: Callable[[], str] | None = None
    ) -> None:
        self._store = store
        self._new_id = new_id or (lambda: uuid.uuid4().hex)

    def add(self, elder_id: str, date: str, label: str) -> Appointment:
        appt = Appointment(self._new_id(), elder_id, date, label)
        self._store.add(appt)
        return appt

    def list_for_elder(self, elder_id: str) -> list[Appointment]:
        return self._store.list_for_elder(elder_id)

    def upcoming(self, elder_id: str, today: str) -> list[Appointment]:
        return [a for a in self._store.list_for_elder(elder_id) if a.date >= today]

    def remove(self, appt_id: str) -> None:
        self._store.remove(appt_id)
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_appointment_service.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/appointment/service.py tests/test_appointment_service.py
git commit -m "feat: AppointmentService（新增/查看/upcoming/刪除）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：AccountService.guardian_line_ids_of_elder

**Files:**
- Modify: `src/kinsun/accounts/service.py`
- Test: `tests/test_accounts_service.py`

**Interfaces:**
- Produces: `AccountService.guardian_line_ids_of_elder(elder_id: str) -> list[str]`（依升級順序、含主家屬，長輩未綁 LINE 也可用）
- 既有 `guardian_line_ids(elder_line_id)` 改為委派（行為不變）

- [ ] **Step 1：寫失敗測試**（接在 `tests/test_accounts_service.py` 檔末）

```python
def test_guardian_line_ids_of_elder_by_id():
    repo = FakeAccountRepository()
    svc = _service(repo)
    elder = svc.create_elder("U-son", "兒子", "阿公")
    inv = svc.generate_invite(elder.elder_id, InviteRole.GUARDIAN)
    svc.redeem_invite(inv.code, "U-daughter", consent_by=ConsentBy.SELF)
    # 用 elder_id 直接查，不需長輩本人綁定 LINE
    assert svc.guardian_line_ids_of_elder(elder.elder_id) == ["U-son", "U-daughter"]
    assert svc.guardian_line_ids_of_elder("nope") == []
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_accounts_service.py::test_guardian_line_ids_of_elder_by_id -v`
Expected: FAIL（`AttributeError: guardian_line_ids_of_elder`）

- [ ] **Step 3：實作**（`src/kinsun/accounts/service.py`）

把既有 `guardian_line_ids` 方法（約 line 181-190）整段替換為：
```python
    def guardian_line_ids_of_elder(self, elder_id: str) -> list[str]:
        line_ids: list[str] = []
        for eg in self._repo.list_elder_guardians(elder_id):
            guardian = self._repo.get_guardian(eg.guardian_id)
            if guardian is not None and guardian.line_user_id:
                line_ids.append(guardian.line_user_id)
        return line_ids

    def guardian_line_ids(self, elder_line_id: str) -> list[str]:
        elder = self._repo.get_elder_by_line(elder_line_id)
        if elder is None:
            return []
        return self.guardian_line_ids_of_elder(elder.elder_id)
```

- [ ] **Step 4：跑測試確認通過**（含既有 guardian_line_ids 測試不回歸）

Run: `uv run pytest tests/test_accounts_service.py -v -k guardian_line_ids`
Expected: PASS（`*_in_escalation_order`、`*_unbound_elder_returns_empty`、`*_only_primary`、`*_of_elder_by_id` 全綠）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/accounts/service.py tests/test_accounts_service.py
git commit -m "feat: AccountService 以 elder_id 查家屬 LINE（guardian_line_ids 委派去重）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：回診提醒每日 job

**Files:**
- Create: `src/kinsun/appointment/jobs.py`
- Test: `tests/test_appointment_jobs.py`

**Interfaces:**
- Consumes: `Appointment`（Task 1）、`fanout_job`/`Job`（既有 `kinsun.scheduler`）
- Produces: `build_appointment_reminder_job(*, appts_on, today, tomorrow, lookup_elder, is_consented, guardian_line_ids, push, hour, minute=0, name="appointment-reminder") -> Job`
  - `appts_on: Callable[[str], list[Appointment]]`、`today/tomorrow: Callable[[], str]`、`lookup_elder: Callable[[str], object]`、`is_consented: Callable[[str], bool]`、`guardian_line_ids: Callable[[str], list[str]]`、`push: Callable[[str, str], None]`

- [ ] **Step 1：寫失敗測試** `tests/test_appointment_jobs.py`

```python
from kinsun.accounts.models import Elder
from kinsun.appointment.jobs import build_appointment_reminder_job
from kinsun.appointment.models import Appointment


def _job(appts_by_date, *, elders, consented, guardians, hour=8):
    pushed = []
    job = build_appointment_reminder_job(
        appts_on=lambda d: appts_by_date.get(d, []),
        today=lambda: "2026-07-15",
        tomorrow=lambda: "2026-07-16",
        lookup_elder=lambda eid: elders.get(eid),
        is_consented=lambda line: consented.get(line, False),
        guardian_line_ids=lambda eid: guardians.get(eid, []),
        push=lambda line, text: pushed.append((line, text)),
        hour=hour,
    )
    return job, pushed


def test_today_and_tomorrow_to_elder_and_guardians():
    elders = {"e1": Elder("e1", "阿公", "U-elder")}
    appts = {
        "2026-07-15": [Appointment("a1", "e1", "2026-07-15", "心臟科回診")],
        "2026-07-16": [Appointment("a2", "e1", "2026-07-16", "眼科回診")],
    }
    job, pushed = _job(
        appts, elders=elders, consented={"U-elder": True}, guardians={"e1": ["U-son"]}
    )
    job.run()
    assert ("U-elder", "阿公，今天要回診囉：心臟科回診。記得準時，需要的話請家人陪您去。") in pushed
    assert ("U-elder", "阿公，明天要回診囉：眼科回診。記得準時，需要的話請家人陪您去。") in pushed
    assert ("U-son", "【金孫提醒】阿公 今天要回診——心臟科回診。") in pushed
    assert ("U-son", "【金孫提醒】阿公 明天要回診——眼科回診。") in pushed
    assert job.cron == "0 8 * * *"


def test_elder_skipped_without_consent_but_guardians_notified():
    elders = {"e1": Elder("e1", "阿公", "U-elder")}
    appts = {"2026-07-15": [Appointment("a1", "e1", "2026-07-15", "回診")]}
    job, pushed = _job(
        appts, elders=elders, consented={"U-elder": False}, guardians={"e1": ["U-son"]}
    )
    job.run()
    assert pushed == [("U-son", "【金孫提醒】阿公 今天要回診——回診。")]


def test_unbound_elder_still_notifies_guardians():
    elders = {"e1": Elder("e1", "阿公", None)}
    appts = {"2026-07-16": [Appointment("a1", "e1", "2026-07-16", "回診")]}
    job, pushed = _job(
        appts, elders=elders, consented={}, guardians={"e1": ["U-son", "U-daughter"]}
    )
    job.run()
    assert pushed == [
        ("U-son", "【金孫提醒】阿公 明天要回診——回診。"),
        ("U-daughter", "【金孫提醒】阿公 明天要回診——回診。"),
    ]
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_appointment_jobs.py -v`
Expected: FAIL（`ImportError: build_appointment_reminder_job`）

- [ ] **Step 3：實作** `src/kinsun/appointment/jobs.py`

```python
"""回診提醒的每日排程 job。"""

from __future__ import annotations

import logging
from collections.abc import Callable

from kinsun.appointment.models import Appointment
from kinsun.scheduler.fanout import fanout_job
from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.appointment")


def build_appointment_reminder_job(
    *,
    appts_on: Callable[[str], list[Appointment]],
    today: Callable[[], str],
    tomorrow: Callable[[], str],
    lookup_elder: Callable[[str], object],
    is_consented: Callable[[str], bool],
    guardian_line_ids: Callable[[str], list[str]],
    push: Callable[[str, str], None],
    hour: int,
    minute: int = 0,
    name: str = "appointment-reminder",
) -> Job:
    def population() -> list[tuple[Appointment, str]]:
        items = [(a, "today") for a in appts_on(today())]
        items += [(a, "tomorrow") for a in appts_on(tomorrow())]
        return items

    def action(item: tuple[Appointment, str]) -> None:
        appt, when = item
        elder = lookup_elder(appt.elder_id)
        if elder is None:
            return
        when_word = "今天" if when == "today" else "明天"
        if elder.line_user_id and is_consented(elder.line_user_id):
            push(
                elder.line_user_id,
                f"{elder.name}，{when_word}要回診囉：{appt.label}。記得準時，需要的話請家人陪您去。",
            )
        for line_id in guardian_line_ids(appt.elder_id):
            push(line_id, f"【金孫提醒】{elder.name} {when_word}要回診——{appt.label}。")

    return fanout_job(
        name=name,
        hour=hour,
        minute=minute,
        population=population,
        action=action,
        item_id=lambda item: item[0].appt_id,
        logger=logger,
    )
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_appointment_jobs.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/appointment/jobs.py tests/test_appointment_jobs.py
git commit -m "feat: 回診提醒每日 job（今天/明天選取，推長輩與家屬）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5：MemoryContext.recall fail-safe

**Files:**
- Modify: `src/kinsun/recall.py`
- Test: `tests/test_memory_context.py`

**Interfaces:**
- 行為不變的對外介面；`recall` 內部對每個 provider 包 try/except，失敗略過該段、不上拋。

- [ ] **Step 1：寫失敗測試**（接在 `tests/test_memory_context.py` 檔末）

```python
class _BoomFacts:
    def facts(self, session_id):
        raise RuntimeError("db down")


def test_recall_skips_failing_fact_provider():
    ctx = MemoryContext(
        FakeLongTermStore(search_result="長期\n"),
        facts=[_BoomFacts(), _FakeFacts("用藥A\n")],
    )
    assert ctx.recall("s", "x") == "長期\n用藥A\n"
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_memory_context.py::test_recall_skips_failing_fact_provider -v`
Expected: FAIL（`RuntimeError: db down` 上拋）

- [ ] **Step 3：實作**（`src/kinsun/recall.py`）

在檔案頂部 import 區加入 `logging` 並建 logger；把 `recall` 改成逐 provider try/except。完整檔案：
```python
"""記憶情境聚合器：長期記憶 ＋ 各事實提供者 → system prompt 注入字串。"""

from __future__ import annotations

import logging
from typing import Protocol

from kinsun.longterm.store import LongTermStore

logger = logging.getLogger("kinsun.recall")


class FactProvider(Protocol):
    def facts(self, session_id: str) -> str: ...


class MemoryContext:
    def __init__(
        self, long_term: LongTermStore, *, facts: list[FactProvider] | None = None
    ) -> None:
        self._long_term = long_term
        self._facts = facts or []

    def recall(self, session_id: str, query: str) -> str:
        out = self._long_term.search(session_id, query)
        for provider in self._facts:
            try:
                out += provider.facts(session_id)
            except Exception:  # noqa: BLE001 - 事實提供者失敗不可中斷對話
                logger.warning("事實提供者失敗，略過該段")
        return out
```

- [ ] **Step 4：跑測試確認通過**（含既有 memory_context 測試不回歸）

Run: `uv run pytest tests/test_memory_context.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/recall.py tests/test_memory_context.py
git commit -m "fix: MemoryContext.recall 對事實提供者 fail-safe，DB 抖動不中斷對話

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6：AppointmentFacts（情境注入）

**Files:**
- Create: `src/kinsun/appointment/facts.py`
- Test: `tests/test_appointment_facts.py`

**Interfaces:**
- Consumes: `AppointmentService.upcoming`（Task 2）、`accounts.elder_by_line`（既有）
- Produces: `AppointmentFacts(accounts, appointments, *, clock)`，`facts(session_id) -> str`（符合 `FactProvider`）

- [ ] **Step 1：寫失敗測試** `tests/test_appointment_facts.py`

```python
from datetime import datetime, timedelta, timezone
from itertools import count

from kinsun.accounts.models import Elder
from kinsun.accounts.service import AccountService
from kinsun.appointment.facts import AppointmentFacts
from kinsun.appointment.service import AppointmentService
from tests.fakes import FakeAccountRepository, FakeAppointmentStore

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 10, 9, 0, tzinfo=TPE)


def _facts(appts):
    repo = FakeAccountRepository()
    repo.save_elder(Elder("e1", "阿公", "U-elder"))
    accounts = AccountService(repo, clock=lambda: NOW, new_id=lambda: "x", new_code=lambda: "c")
    aids = (f"a{i}" for i in count(1))
    appointments = AppointmentService(FakeAppointmentStore(), new_id=lambda: next(aids))
    for date, label in appts:
        appointments.add("e1", date, label)
    return AppointmentFacts(accounts, appointments, clock=lambda: NOW)


def test_injects_upcoming_sorted():
    facts = _facts([("2026-07-20", "心臟科回診"), ("2026-07-12", "眼科回診")])
    out = facts.facts("U-elder")
    assert "即將到來的回診" in out
    assert out.index("2026-07-12") < out.index("2026-07-20")
    assert "眼科回診" in out and "心臟科回診" in out


def test_empty_when_no_upcoming():
    facts = _facts([("2026-07-01", "過去")])  # 早於 NOW(07-10)
    assert facts.facts("U-elder") == ""


def test_empty_when_elder_unknown():
    facts = _facts([("2026-07-20", "x")])
    assert facts.facts("U-stranger") == ""
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_appointment_facts.py -v`
Expected: FAIL（`ImportError: AppointmentFacts`）

- [ ] **Step 3：實作** `src/kinsun/appointment/facts.py`

```python
"""回診事實提供者：把長輩即將到來的回診組成注入情境的一段文字。"""

from __future__ import annotations

from collections.abc import Callable
from datetime import datetime

_PREFIX = "\n這位長者即將到來的回診（系統設定，僅供參考）：\n"


class AppointmentFacts:
    """facts(session_id) -> 注入文字。session_id 即長輩的 LINE user_id。"""

    def __init__(self, accounts, appointments, *, clock: Callable[[], datetime]) -> None:
        self._accounts = accounts
        self._appointments = appointments
        self._clock = clock

    def facts(self, session_id: str) -> str:
        elder = self._accounts.elder_by_line(session_id)
        if elder is None:
            return ""
        today = self._clock().date().isoformat()
        ups = self._appointments.upcoming(elder.elder_id, today)
        if not ups:
            return ""
        lines = "\n".join(f"- {a.date} {a.label}" for a in ups)
        return _PREFIX + lines + "\n"
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_appointment_facts.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/appointment/facts.py tests/test_appointment_facts.py
git commit -m "feat: AppointmentFacts 每輪注入即將到來的回診

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7：BindingState + AppointmentMenu（設定流程）

**Files:**
- Modify: `src/kinsun/binding/session.py`
- Create: `src/kinsun/appointment/flow.py`
- Test: `tests/test_appointment_flow.py`

**Interfaces:**
- Consumes: `AppointmentService`（Task 2）、`BindingSession/BindingSessionStore/BindingState`（既有）、`accounts.elders_managed_by`（既有）
- Produces:
  - `BindingState` 新增 `APPT_MENU/APPT_PICK_ELDER/APPT_ADD_LABEL/APPT_ADD_DATE/APPT_DEL_PICK`（值 `appt_*`）
  - `AppointmentMenu(appointments, accounts, sessions, *, clock)`：`open(line)->str`、`step(session, text, line)->str`
  - `_parse_date(text) -> str | None`

- [ ] **Step 1：寫失敗測試** `tests/test_appointment_flow.py`

```python
from datetime import datetime, timedelta, timezone
from itertools import count

from kinsun.accounts.service import AccountService
from kinsun.appointment.flow import AppointmentMenu, _parse_date
from kinsun.appointment.service import AppointmentService
from kinsun.binding.session import BindingState
from tests.fakes import FakeAccountRepository, FakeAppointmentStore, FakeBindingSessionStore

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 10, 10, 0, tzinfo=TPE)


def _setup():
    repo = FakeAccountRepository()
    ids = (f"id{i}" for i in count(1))
    accounts = AccountService(
        repo, clock=lambda: NOW, new_id=lambda: next(ids), new_code=lambda: "c"
    )
    accounts.create_elder("U-son", "兒子", "阿公")
    aids = (f"a{i}" for i in count(1))
    appointments = AppointmentService(FakeAppointmentStore(), new_id=lambda: next(aids))
    sessions = FakeBindingSessionStore()
    menu = AppointmentMenu(appointments, accounts, sessions, clock=lambda: NOW)
    return menu, appointments, accounts, sessions


def test_parse_date():
    assert _parse_date("2026-07-15") == "2026-07-15"
    assert _parse_date(" 2026-7-5 ") == "2026-07-05"
    assert _parse_date("七月") is None


def test_add_flow_happy_path():
    menu, appointments, accounts, sessions = _setup()
    elder = accounts.elders_managed_by("U-son")[0]
    assert "新增回診" in menu.open("U-son")
    menu.step(sessions.get("U-son"), "1", "U-son")
    assert sessions.get("U-son").state == BindingState.APPT_ADD_LABEL
    menu.step(sessions.get("U-son"), "心臟科回診", "U-son")
    assert sessions.get("U-son").state == BindingState.APPT_ADD_DATE
    reply = menu.step(sessions.get("U-son"), "2026-07-20", "U-son")
    assert "心臟科回診" in reply and "2026-07-20" in reply
    appts = appointments.list_for_elder(elder.elder_id)
    assert (appts[0].date, appts[0].label) == ("2026-07-20", "心臟科回診")
    assert sessions.get("U-son") is None


def test_add_invalid_date_reprompts():
    menu, _, _, sessions = _setup()
    menu.open("U-son")
    menu.step(sessions.get("U-son"), "1", "U-son")
    menu.step(sessions.get("U-son"), "回診", "U-son")
    reply = menu.step(sessions.get("U-son"), "明天啦", "U-son")
    assert "格式" in reply
    assert sessions.get("U-son").state == BindingState.APPT_ADD_DATE


def test_add_past_date_reprompts():
    menu, _, _, sessions = _setup()
    menu.open("U-son")
    menu.step(sessions.get("U-son"), "1", "U-son")
    menu.step(sessions.get("U-son"), "回診", "U-son")
    reply = menu.step(sessions.get("U-son"), "2026-07-01", "U-son")
    assert "已經過了" in reply
    assert sessions.get("U-son").state == BindingState.APPT_ADD_DATE


def test_view_upcoming_only():
    menu, appointments, accounts, sessions = _setup()
    elder = accounts.elders_managed_by("U-son")[0]
    appointments.add(elder.elder_id, "2026-07-20", "心臟科回診")
    appointments.add(elder.elder_id, "2026-07-01", "舊回診")
    menu.open("U-son")
    reply = menu.step(sessions.get("U-son"), "2", "U-son")
    assert "心臟科回診" in reply and "舊回診" not in reply
    assert sessions.get("U-son") is None


def test_view_empty():
    menu, _, _, sessions = _setup()
    menu.open("U-son")
    assert "沒有即將到來的回診" in menu.step(sessions.get("U-son"), "2", "U-son")


def test_delete_flow():
    menu, appointments, accounts, sessions = _setup()
    elder = accounts.elders_managed_by("U-son")[0]
    appointments.add(elder.elder_id, "2026-07-20", "心臟科回診")
    menu.open("U-son")
    listing = menu.step(sessions.get("U-son"), "3", "U-son")
    assert "1. 2026-07-20 心臟科回診" in listing
    reply = menu.step(sessions.get("U-son"), "1", "U-son")
    assert "已刪除" in reply
    assert appointments.list_for_elder(elder.elder_id) == []
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_appointment_flow.py -v`
Expected: FAIL（`ImportError: AppointmentMenu`）

- [ ] **Step 3a：擴充 `BindingState`**（`src/kinsun/binding/session.py`）

在 `MED_DEL_PICK = "med_del_pick"` 之後新增：
```python
    APPT_MENU = "appt_menu"
    APPT_PICK_ELDER = "appt_pick_elder"
    APPT_ADD_LABEL = "appt_add_label"
    APPT_ADD_DATE = "appt_add_date"
    APPT_DEL_PICK = "appt_del_pick"
```

- [ ] **Step 3b：實作** `src/kinsun/appointment/flow.py`

```python
"""回診提醒的引導式設定（被引導流程委派，共用 session）。"""

from __future__ import annotations

from collections.abc import Callable
from datetime import datetime

from kinsun.appointment.service import AppointmentService
from kinsun.binding.session import BindingSession, BindingSessionStore, BindingState

_APPT_MENU = "回診提醒：請回覆數字：\n1️⃣ 新增回診\n2️⃣ 查看回診\n3️⃣ 刪除回診"
_DATE_PROMPT = "請問回診日期？請用 2026-07-15 這種格式（年-月-日）。"
_FULLWIDTH = str.maketrans("０１２３４５６７８９", "0123456789")


def _parse_date(text: str) -> str | None:
    try:
        parsed = datetime.strptime(text.strip(), "%Y-%m-%d").date()
    except ValueError:
        return None
    return parsed.isoformat()


class AppointmentMenu:
    def __init__(
        self,
        appointments: AppointmentService,
        accounts,
        sessions: BindingSessionStore,
        *,
        clock: Callable[[], datetime],
    ) -> None:
        self._appts = appointments
        self._accounts = accounts
        self._sessions = sessions
        self._clock = clock

    def _save(self, line: str, state: BindingState, data: dict) -> None:
        self._sessions.save(BindingSession(line, state, data, self._clock().timestamp()))

    def open(self, line: str) -> str:
        self._save(line, BindingState.APPT_MENU, {})
        return _APPT_MENU

    def step(self, session: BindingSession, text: str, line: str) -> str:
        state = session.state
        if state == BindingState.APPT_MENU:
            return self._menu(text, line)
        if state == BindingState.APPT_PICK_ELDER:
            return self._pick_elder(session, text, line)
        if state == BindingState.APPT_ADD_LABEL:
            return self._add_label(session, text, line)
        if state == BindingState.APPT_ADD_DATE:
            return self._add_date(session, text, line)
        return self._del_pick(session, text, line)

    def _menu(self, text: str, line: str) -> str:
        action = {"1": "add", "2": "view", "3": "del"}.get(text.translate(_FULLWIDTH))
        if action is None:
            return "請回覆 1、2 或 3。"
        elders = self._accounts.elders_managed_by(line)
        if not elders:
            return "您還沒有長輩檔案，請先回覆「設定」並選 1 建立。"
        if len(elders) == 1:
            return self._begin(action, elders[0].elder_id, elders[0].name, line)
        self._save(
            line,
            BindingState.APPT_PICK_ELDER,
            {"action": action, "elders": [[e.elder_id, e.name] for e in elders]},
        )
        listing = "\n".join(f"{i + 1}. {e.name}" for i, e in enumerate(elders))
        return "請回覆數字選擇長輩：\n" + listing

    def _pick_elder(self, session: BindingSession, text: str, line: str) -> str:
        elders = session.data["elders"]
        choice = text.translate(_FULLWIDTH)
        if not choice.isdigit() or not (1 <= int(choice) <= len(elders)):
            return "請回覆清單中的數字。"
        elder_id, elder_name = elders[int(choice) - 1]
        return self._begin(session.data["action"], elder_id, elder_name, line)

    def _begin(self, action: str, elder_id: str, elder_name: str, line: str) -> str:
        if action == "add":
            self._save(
                line, BindingState.APPT_ADD_LABEL, {"elder_id": elder_id, "elder_name": elder_name}
            )
            return f"請問要幫『{elder_name}』記哪一個回診？（例：上午10點 心臟科回診 林口長庚）"
        if action == "view":
            self._sessions.delete(line)
            return self._view(elder_id, elder_name)
        appts = self._appts.list_for_elder(elder_id)
        if not appts:
            self._sessions.delete(line)
            return f"『{elder_name}』目前沒有設定回診。"
        items = [[a.appt_id, f"{a.date} {a.label}"] for a in appts]
        self._save(line, BindingState.APPT_DEL_PICK, {"appts": items})
        listing = "\n".join(f"{i + 1}. {label}" for i, (_, label) in enumerate(items))
        return "請回覆要刪除的編號：\n" + listing

    def _view(self, elder_id: str, elder_name: str) -> str:
        today = self._clock().date().isoformat()
        ups = self._appts.upcoming(elder_id, today)
        if not ups:
            return f"『{elder_name}』目前沒有即將到來的回診。"
        lines = "\n".join(f"• {a.date} {a.label}" for a in ups)
        return f"『{elder_name}』即將到來的回診：\n" + lines

    def _add_label(self, session: BindingSession, text: str, line: str) -> str:
        data = dict(session.data)
        data["label"] = text
        self._save(line, BindingState.APPT_ADD_DATE, data)
        return _DATE_PROMPT

    def _add_date(self, session: BindingSession, text: str, line: str) -> str:
        date = _parse_date(text)
        if date is None:
            return "請用 2026-07-15 這種格式（年-月-日）。"
        if date < self._clock().date().isoformat():
            return "這個日期已經過了，請輸入今天以後的日期。"
        data = session.data
        self._appts.add(data["elder_id"], date, data["label"])
        self._sessions.delete(line)
        return f"已為『{data['elder_name']}』新增回診：{date} {data['label']}。"

    def _del_pick(self, session: BindingSession, text: str, line: str) -> str:
        appts = session.data["appts"]
        choice = text.translate(_FULLWIDTH)
        if not choice.isdigit() or not (1 <= int(choice) <= len(appts)):
            return "請回覆清單中的編號。"
        appt_id, label = appts[int(choice) - 1]
        self._appts.remove(appt_id)
        self._sessions.delete(line)
        return f"已刪除『{label}』。"
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_appointment_flow.py -v`
Expected: PASS（7 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/binding/session.py src/kinsun/appointment/flow.py \
        tests/test_appointment_flow.py
git commit -m "feat: 回診提醒引導式設定 AppointmentMenu（新增/查看/刪除 + 日期驗證）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 8：BindingFlow 接線（選單第 5 項 + 分派）

**Files:**
- Modify: `src/kinsun/binding/flow.py`
- Test: `tests/test_binding_flow.py`

**Interfaces:**
- Consumes: `AppointmentMenu`（Task 7）
- Produces: `BindingFlow.__init__(self, accounts, sessions, profiles, medication, appointment, *, clock, session_ttl_seconds=600)`；選單含「5️⃣ 回診提醒」；`appt_` 狀態委派 `appointment.step`；選 `5` 呼叫 `appointment.open`

- [ ] **Step 1：改既有測試 + 加新測試**（`tests/test_binding_flow.py`）

頂部 import 增加：
```python
from kinsun.appointment.flow import AppointmentMenu
from kinsun.appointment.service import AppointmentService
from tests.fakes import FakeAccountRepository, FakeAppointmentStore, FakeBindingSessionStore, FakeMedicationStore
```
（把原本的 `from tests.fakes import ...` 那行整行換成上面這行，補上 `FakeAppointmentStore`。）

`_build_flow` 整個換成：
```python
def _build_flow(accounts, sessions, profiles, *, clock):
    med_ids = (f"m{i}" for i in count(1))
    medications = MedicationService(FakeMedicationStore(), new_id=lambda: next(med_ids))
    menu = MedicationMenu(medications, accounts, sessions, clock=clock)
    appt_ids = (f"a{i}" for i in count(1))
    appointments = AppointmentService(FakeAppointmentStore(), new_id=lambda: next(appt_ids))
    appt_menu = AppointmentMenu(appointments, accounts, sessions, clock=clock)
    return BindingFlow(
        accounts, sessions, profiles, menu, appt_menu, clock=clock, session_ttl_seconds=600
    )
```

檔末新增：
```python
def test_menu_shows_appointment_and_delegates():
    flow, _, _ = _flow()
    assert "回診提醒" in flow.handle("U-1", "設定")
    assert "新增回診" in flow.handle("U-1", "5")
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_binding_flow.py -v`
Expected: FAIL（`BindingFlow.__init__` 參數不符 / `test_menu_shows_appointment_and_delegates` 失敗）

- [ ] **Step 3：實作**（`src/kinsun/binding/flow.py`）

(1) `_MENU` 常數改為（在「4️⃣ 用藥提醒」後加一行）：
```python
_MENU = (
    "您好，我是金孫。請回覆數字：\n"
    "1️⃣ 建立長輩檔案\n"
    "2️⃣ 邀請其他家屬\n"
    "3️⃣ 綁定（貼上邀請碼）\n"
    "4️⃣ 用藥提醒\n"
    "5️⃣ 回診提醒\n"
    "（隨時回覆「取消」可結束）"
)
```

(2) `__init__` 簽章加入 `appointment`（在 `medication` 之後），並存欄位：
```python
    def __init__(
        self,
        accounts: AccountService,
        sessions: BindingSessionStore,
        profiles: Profiles,
        medication,
        appointment,
        *,
        clock: Callable[[], datetime],
        session_ttl_seconds: int = 600,
    ) -> None:
        self._accounts = accounts
        self._sessions = sessions
        self._profiles = profiles
        self._medication = medication
        self._appointment = appointment
        self._clock = clock
        self._ttl = session_ttl_seconds
```

(3) `_step` 開頭，在 `if state.value.startswith("med_"):` 之前或之後加入 appt 分派：
```python
        if state.value.startswith("appt_"):
            return self._appointment.step(session, text, line)
```

(4) `_menu` 加入選 5、並更新無效提示：
```python
        if choice == "4":
            return self._medication.open(line)
        if choice == "5":
            return self._appointment.open(line)
        return "請回覆 1、2、3、4 或 5。"
```

- [ ] **Step 4：跑測試確認通過**（含既有 binding_flow 測試不回歸）

Run: `uv run pytest tests/test_binding_flow.py -v`
Expected: PASS（含 `test_menu_shows_medication_and_delegates`、`test_menu_shows_appointment_and_delegates`）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/binding/flow.py tests/test_binding_flow.py
git commit -m "feat: 設定選單加入回診提醒（第 5 項 + appt_ 分派）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 9：設定 appointment_reminder_hour

**Files:**
- Modify: `src/kinsun/config.py`、`.env.example`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings.appointment_reminder_hour: int`（env `APPOINTMENT_REMINDER_HOUR`，預設 8）

- [ ] **Step 1：改測試**（`tests/test_config.py` 的 `test_load_settings_reads_required_and_defaults` 內，`medication_bedtime_hour` 那行之後加一行斷言）

```python
    assert settings.appointment_reminder_hour == 8
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_config.py::test_load_settings_reads_required_and_defaults -v`
Expected: FAIL（`AttributeError: appointment_reminder_hour`）

- [ ] **Step 3：實作**

`src/kinsun/config.py`：`Settings` 在 `medication_bedtime_hour: int` 後加：
```python
    appointment_reminder_hour: int
```
`load_settings(...)` 在 `medication_bedtime_hour=...` 後加：
```python
        appointment_reminder_hour=int(env.get("APPOINTMENT_REMINDER_HOUR", "8")),
```
`.env.example`：在 `MEDICATION_BEDTIME_HOUR` 一帶附近加一行：
```
APPOINTMENT_REMINDER_HOUR=8
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 5：commit**

```bash
git add src/kinsun/config.py tests/test_config.py .env.example
git commit -m "feat: 新增 APPOINTMENT_REMINDER_HOUR 設定（預設 8）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 10：組裝接線（app.py + worker.py）+ 全量驗證

**Files:**
- Modify: `src/kinsun/app.py`、`src/kinsun/scheduler/worker.py`

**Interfaces:**
- Consumes: 前面所有 Task 的產物。
- 無新對外介面；本 Task 把元件接進 webhook app 與排程 worker。

- [ ] **Step 1：接線 `src/kinsun/app.py`**

(1) import 區（依字母序）加入：
```python
from kinsun.appointment.facts import AppointmentFacts
from kinsun.appointment.flow import AppointmentMenu
from kinsun.appointment.service import AppointmentService
from kinsun.appointment.store import PgAppointmentStore
```

(2) 在 `medications = MedicationService(PgMedicationStore(db))` 之後加：
```python
    appointments = AppointmentService(PgAppointmentStore(db))
```

(3) 把 `context = MemoryContext(long_term, facts=[MedicationFacts(accounts, medications)])` 改為：
```python
    context = MemoryContext(
        long_term,
        facts=[
            MedicationFacts(accounts, medications),
            AppointmentFacts(accounts, appointments, clock=lambda: datetime.now(tz)),
        ],
    )
```

(4) 在 `medication_menu = MedicationMenu(...)` 之後加：
```python
    appointment_menu = AppointmentMenu(
        appointments, accounts, binding_sessions, clock=lambda: datetime.now(tz)
    )
```

(5) `binding = BindingFlow(...)` 加入 `appointment_menu`（在 `medication_menu` 之後）：
```python
    binding = BindingFlow(
        accounts,
        binding_sessions,
        messenger,
        medication_menu,
        appointment_menu,
        clock=lambda: datetime.now(tz),
        session_ttl_seconds=settings.binding_session_ttl_minutes * 60,
    )
```

- [ ] **Step 2：接線 `src/kinsun/scheduler/worker.py`**

(1) `from datetime import datetime` 改為：
```python
from datetime import datetime, timedelta
```

(2) import 區加入：
```python
from kinsun.appointment.facts import AppointmentFacts
from kinsun.appointment.jobs import build_appointment_reminder_job
from kinsun.appointment.service import AppointmentService
from kinsun.appointment.store import PgAppointmentStore
```

(3) 在 `med_store = PgMedicationStore(db)` 之後加：
```python
    appt_store = PgAppointmentStore(db)
    appointments = AppointmentService(appt_store)
```

(4) 把 `context = MemoryContext(long_term, facts=[MedicationFacts(accounts, med_store)])` 改為：
```python
    context = MemoryContext(
        long_term,
        facts=[
            MedicationFacts(accounts, med_store),
            AppointmentFacts(accounts, appointments, clock=clock),
        ],
    )
```

(5) 在用藥 `med_slots` 迴圈 append job 之後（`state = PgScheduleStateStore(db, tz)` 之前）加入回診 job：
```python
    jobs.append(
        build_appointment_reminder_job(
            appts_on=appt_store.list_for_date,
            today=lambda: clock().date().isoformat(),
            tomorrow=lambda: (clock().date() + timedelta(days=1)).isoformat(),
            lookup_elder=accounts.get_elder,
            is_consented=accounts.is_consented_elder,
            guardian_line_ids=accounts.guardian_line_ids_of_elder,
            push=messenger.push_text,
            hour=settings.appointment_reminder_hour,
        )
    )
```

- [ ] **Step 3：靜態驗證可匯入（不需金鑰）**

Run: `uv run python -c "import kinsun.app, kinsun.scheduler.worker; print('import ok')"`
Expected: 印出 `import ok`（模組層匯入成功；`build_app`/`build_scheduler` 不在匯入時執行，故不需 DB/金鑰）

- [ ] **Step 4：全量品質閘門**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠（既有 + 新增；雲端整合 opt-in 為 SKIPPED）。
若 `ruff format --check` 報未格式化，跑 `uv run ruff format .` 後重跑。

- [ ] **Step 5：commit**

```bash
git add src/kinsun/app.py src/kinsun/scheduler/worker.py
git commit -m "feat: 組裝回診提醒到 webhook app 與排程 worker

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review（計畫對 spec 覆蓋檢查）

- 資料模型/儲存/DDL（spec §3,§4）→ Task 1 ✅
- 服務 add/list/upcoming/remove（§5）→ Task 2 ✅
- `guardian_line_ids_of_elder`（§2.1,§6）→ Task 3 ✅
- 提醒 job：今天/明天、長輩需同意、家屬不限、扇出隔離（§6）→ Task 4 ✅
- recall fail-safe（§9）→ Task 5 ✅
- AppointmentFacts 注入（§8）→ Task 6 ✅
- 設定流程 + 日期驗證 + BindingState（§7.1,§7.2）→ Task 7 ✅
- BindingFlow 選單第 5 項 + 分派（§7.3）→ Task 8 ✅
- config `appointment_reminder_hour` + .env.example（§10）→ Task 9 ✅
- app.py / worker.py 組裝（§11）+ 全量驗證 → Task 10 ✅

型別/命名一致性：`Appointment.date`(model) vs `appt_date`(DB 欄位) 已於 store 對映；`guardian_line_ids_of_elder` 在 Task 3 定義、Task 4/10 使用一致；`build_appointment_reminder_job` 參數名於 Task 4 定義、Task 10 套用一致。無佔位符。
