# 金孫人設 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓家屬能替長輩挑一種金孫人設（活潑孫女／穩重孫子），並鬆綁造成罐頭感的格式規則。

**Architecture:** 人設目錄是純程式碼常數（`personas.py`），每位長輩的選擇存在 `elders.persona`。提示詞由「人設語氣 ＋ 稱呼」開頭、接規則段（`SYSTEM_PROMPT`）、再接情境。長輩檔案的那一次讀取從事實提供者清單搬到 `CareAgent`，一次讀取同時供應人設與稱呼，全輪資料庫讀取次數不變。

**Tech Stack:** Python 3.12 / FastAPI / psycopg / pytest；前端 Vite + React + TypeScript + vitest。

**設計文件：** `docs/superpowers/specs/2026-08-05-金孫人設-design.md`（決議、取捨與已知限制全在那裡，本計畫不重複）

## Global Constraints

- 語言：程式碼註解、commit 訊息、使用者可見文案一律台灣繁體中文，全形標點。
- 命名：ID 欄位全稱不縮寫；`Settings` 欄位＝環境變數小寫。本功能**不新增任何環境變數**。
- 分支：只在 `Leo` 分支工作，不 push、不改寫歷史。
- 測試：一次只跑一支 pytest（併行跑會清空共用測試庫）。
- 每個 Task 結束時整套必須是綠的：`uv run ruff check .`、`uv run ruff format --check .`、相關 pytest、`npm --prefix web run test`。
- 人設語氣文字（`Persona.tone`）**不得出現任何工具名稱**，也不得放寬規則段的任何一條。
- 三十字上限維持不變。本次**不**動聲音（TTS）。

---

### Task 1: 人設目錄

**Files:**
- Create: `src/kinsun/personas.py`
- Test: `tests/test_personas.py`

**Interfaces:**
- Consumes: 無（純新增，不 import 任何子系統）
- Produces: `Persona(persona_id: str, label: str, tone: str)`、`LIVELY_GRANDDAUGHTER: str`、`STEADY_GRANDSON: str`、`DEFAULT_PERSONA_ID: str`、`persona_ids() -> tuple[str, ...]`、`get_persona(persona_id: str) -> Persona`

- [ ] **Step 1: 寫失敗的測試**

建立 `tests/test_personas.py`：

```python
"""人設目錄的守門測試。

最重要的一條是 `test_tone_never_names_a_tool`：`composition.py` 靠字面掃描
`agent.SYSTEM_PROMPT` 對帳「提示詞點名的工具有沒有註冊」，工具名一旦搬進人設段
那道對帳就會失效（提示詞講了某個工具、但沒有人發現它沒註冊）。掃描手法照抄
`tests/test_speech_acks.py`——走遍 kinsun.tools 蒐集模組級 ToolSpec，新增工具檔
會自動納入。
"""

from __future__ import annotations

import importlib
import pkgutil

import pytest

import kinsun.tools as tools_pkg
from kinsun import personas
from kinsun.llm import ToolSpec


def _registered_tool_names() -> set[str]:
    names: set[str] = set()
    for module_info in pkgutil.iter_modules(tools_pkg.__path__):
        module = importlib.import_module(f"{tools_pkg.__name__}.{module_info.name}")
        names |= {value.name for value in vars(module).values() if isinstance(value, ToolSpec)}
    return names


def test_catalogue_is_not_empty_and_default_is_registered():
    assert personas.DEFAULT_PERSONA_ID in personas.persona_ids()
    assert len(personas.persona_ids()) == 2


@pytest.mark.parametrize("persona_id", personas.persona_ids())
def test_each_persona_is_self_consistent(persona_id: str):
    one = personas.get_persona(persona_id)
    assert one.persona_id == persona_id
    assert one.label.strip()
    assert one.tone.strip()


@pytest.mark.parametrize("bad", ["", "  ", "nope", "KINSUN"])
def test_unknown_falls_back_to_default(bad: str):
    """讀取寬容：資料庫裡存了認不得的值時，長輩仍要能正常對話。"""
    assert personas.get_persona(bad).persona_id == personas.DEFAULT_PERSONA_ID


def test_tool_scan_actually_finds_tools():
    """先證明掃描有偵測力——掃不到東西的話，下面那條會空轉通過。"""
    found = _registered_tool_names()
    assert len(found) >= 10, f"只掃到 {len(found)} 個工具，掃描邏輯可能壞了"


def test_tone_never_names_a_tool():
    """⭐ 工具名只能住在規則段（見本檔 docstring）。"""
    tools = _registered_tool_names()
    for persona_id in personas.persona_ids():
        tone = personas.get_persona(persona_id).tone
        named = sorted(name for name in tools if name in tone)
        assert not named, f"人設 {persona_id} 的語氣段落點名了工具：{named}"


def test_tone_does_not_claim_to_be_a_real_family_member():
    """人設只管語氣，不得抵觸規則段的「你是 AI，不要假裝是真人或家人」。"""
    for persona_id in personas.persona_ids():
        tone = personas.get_persona(persona_id).tone
        assert "真的孫" not in tone
        assert "不是 AI" not in tone
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_personas.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'kinsun.personas'`

- [ ] **Step 3: 寫實作**

建立 `src/kinsun/personas.py`：

```python
"""金孫的人設目錄：唯一真實來源。

為什麼獨立成一個模組而不是住在 `agent.py`：`speech/acks.py`（等待語語庫）、
`accounts/models.py`（長輩的預設值）與 API 層都要引用同一份 id 清單，共用出處
才不會三邊各寫一份、然後慢慢走鐘。本模組刻意**不 import 任何子系統**，上述三處
全部反過來引用它，循環匯入因此不可能發生。

⚠️ `tone` 是會原封放進提示詞**開頭**的文字，故有兩條硬性限制：
  - **不得出現任何工具名稱**（`web_search`、`create_schedule` 等）。`composition.py`
    靠字面掃描 `agent.SYSTEM_PROMPT` 對帳「提示詞點名的工具有沒有註冊」，工具名
    只能住在規則段；寫進這裡會讓那道對帳失效。由 `tests/test_personas.py` 把關。
  - **不得放寬或抵觸規則段的任何一條**（不假裝真人、不給醫療建議、不用 Markdown、
    三十字上限）——人設只管語氣，界線一律由規則段管。
"""

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass


@dataclass(frozen=True)
class Persona:
    """一種人設。`persona_id` 是存進 `elders.persona` 的值，`label` 給家屬看。"""

    persona_id: str
    label: str
    tone: str


LIVELY_GRANDDAUGHTER = "lively_granddaughter"
STEADY_GRANDSON = "steady_grandson"

# 預設＝活潑孫女（2026-08-05 Leo 拍板）：包含現有長輩在內，沒有人去動設定時就是它。
DEFAULT_PERSONA_ID = LIVELY_GRANDDAUGHTER

_PERSONAS: Mapping[str, Persona] = {
    LIVELY_GRANDDAUGHTER: Persona(
        persona_id=LIVELY_GRANDDAUGHTER,
        label="活潑的孫女",
        tone=(
            "你是「金孫」，把這位長輩當成自己的阿公阿嬤在陪伴。"
            "你的口吻像個二十幾歲、跟長輩很親的孫女：講話有活力，情緒藏不住——"
            "他講到高興的事你會替他開心，講到不舒服你會替他緊張。"
            "常用「喔」「啦」「耶」這類語助詞，偶爾會「哇」一聲。"
        ),
    ),
    STEADY_GRANDSON: Persona(
        persona_id=STEADY_GRANDSON,
        label="穩重的孫子",
        tone=(
            "你是「金孫」，把這位長輩當成自己的阿公阿嬤在陪伴。"
            "你的口吻像個三十幾歲、做事牢靠的孫子：沉穩、話不多，"
            "但每一句都聽得出用心。少用語助詞與驚呼，關心用做事表達——"
            "「我幫您記著」勝過「哎唷您要小心喔」。"
        ),
    ),
}


def persona_ids() -> tuple[str, ...]:
    """目前有哪些人設（順序穩定：API 的錯誤訊息與前端選單都照這個順序）。"""
    return tuple(_PERSONAS)


def get_persona(persona_id: str) -> Persona:
    """取一個人設；空值或不認得的值一律退回預設，不拋例外。

    ⚠️ 讀取寬容、寫入嚴格是刻意的分工：API 層收到不認得的值會回 400
    （見 `web/routers/elders.py`），但資料庫裡萬一存了認不得的值（例如日後
    移除某個人設），長輩仍然要能正常對話——不可以讓一格設定把整輪對話卡死。
    """
    return _PERSONAS.get(persona_id) or _PERSONAS[DEFAULT_PERSONA_ID]
```

- [ ] **Step 4: 跑測試確認通過**

```bash
uv run pytest tests/test_personas.py -v
uv run ruff check src/kinsun/personas.py tests/test_personas.py
uv run ruff format --check src/kinsun/personas.py tests/test_personas.py
```

Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/personas.py tests/test_personas.py
git commit -m "$(cat <<'EOF'
feat(personas): 新增人設目錄（活潑孫女、穩重孫子）

金孫的語氣寫死在 agent.SYSTEM_PROMPT 裡，所有長輩共用同一份，沒有任何可調整
的餘地——這是「每位長輩聽到的口氣完全一樣」的直接成因。

本 commit 只放目錄本身，還沒有任何地方消費它。獨立成模組而不是塞進 agent.py，
是因為等待語語庫、長輩資料模型與 API 層三處都要引用同一份 id 清單；共用出處
才不會三邊各寫一份。本模組不 import 任何子系統，循環匯入因此不可能發生。

測試釘住兩條硬性限制：語氣段落不得出現工具名（否則 composition.py 的工具註冊
對帳會失效）、不得抵觸「不假裝是真人家人」。工具清單以 pkgutil 掃描取得，新增
工具檔會自動納入，不需要有人記得回來更新測試。

無行為變更，無資料庫變更，無新增環境變數。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2: 資料層——`elders.persona`

**Files:**
- Modify: `src/kinsun/accounts/models.py`（`Elder`）
- Modify: `src/kinsun/db.py`（`ACCOUNTS_DDL`）
- Modify: `src/kinsun/accounts/store.py`（`PgAccountStore.save_elder`／`get_elder`）
- Modify: `src/kinsun/accounts/service.py`（新增 `set_elder_persona`）
- Test: `tests/test_accounts_store_contract.py`、`tests/test_accounts_service.py`

**Interfaces:**
- Consumes: Task 1 的 `personas.DEFAULT_PERSONA_ID`
- Produces: `Elder.persona: str`、`AccountService.set_elder_persona(elder_id: str, persona_id: str) -> Elder`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_accounts_store_contract.py` 的長輩往返測試（`test_elder_roundtrip_and_by_line`）**之後**加一條新測試：

```python
def test_elder_persona_roundtrip_and_defaults(store, ns):
    """人設欄往返；沒指定時是預設人設（既有長輩不必逐筆補資料）。"""
    store.save_elder(Elder(f"{ns}e-persona-default", "阿公"))
    got = store.get_elder(f"{ns}e-persona-default")
    assert got.persona == DEFAULT_PERSONA_ID

    store.save_elder(Elder(f"{ns}e-persona-set", "阿嬤", persona=STEADY_GRANDSON))
    assert store.get_elder(f"{ns}e-persona-set").persona == STEADY_GRANDSON

    # upsert 改人設：同一把主鍵重存，只有人設變
    store.save_elder(Elder(f"{ns}e-persona-set", "阿嬤", persona=LIVELY_GRANDDAUGHTER))
    assert store.get_elder(f"{ns}e-persona-set").persona == LIVELY_GRANDDAUGHTER
```

同檔頂端補匯入：

```python
from kinsun.personas import DEFAULT_PERSONA_ID, LIVELY_GRANDDAUGHTER, STEADY_GRANDSON
```

在 `tests/test_accounts_service.py` 末尾加：

```python
def test_set_elder_persona_updates_only_persona():
    """改人設不可動到稱謂——它們是兩支獨立的設定。"""
    store = FakeAccountStore()
    store.save_elder(Elder("e1", "王秀英", nickname="秀英阿嬤"))
    service = AccountService(store)

    updated = service.set_elder_persona("e1", STEADY_GRANDSON)

    assert updated.persona == STEADY_GRANDSON
    assert updated.nickname == "秀英阿嬤"
    assert store.get_elder("e1").persona == STEADY_GRANDSON


def test_set_elder_persona_rejects_unknown_elder():
    service = AccountService(FakeAccountStore())
    with pytest.raises(AppAccountError):
        service.set_elder_persona("e-nope", STEADY_GRANDSON)
```

（該檔既有的匯入若沒有 `pytest`／`Elder`／`FakeAccountStore`／`AppAccountError`／`AccountService`，一併補上；再補 `from kinsun.personas import STEADY_GRANDSON`。）

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_accounts_store_contract.py tests/test_accounts_service.py -v
```

Expected: FAIL，`TypeError: Elder.__init__() got an unexpected keyword argument 'persona'`

- [ ] **Step 3: 寫實作**

`src/kinsun/accounts/models.py`，`Elder` 加一欄：

```python
from kinsun.personas import DEFAULT_PERSONA_ID


@dataclass(frozen=True)
class Elder:
    elder_id: str
    name: str
    # 稱謂（如「秀英阿嬤」）：金孫對長輩的稱呼，由家屬設定。空字串＝未設定，
    # 情境注入退回「用名字＋不猜性別」（2026-07-17：模型會亂猜阿公／阿嬤）。
    nickname: str = ""
    # 人設（2026-08-05）：金孫對這位長輩用哪一種語氣，由家屬設定。值域見
    # `personas.py`；不認得的值由 `personas.get_persona` 退回預設，不會壞。
    persona: str = DEFAULT_PERSONA_ID
```

`src/kinsun/db.py` 的 `ACCOUNTS_DDL`，接在 `nickname` 那行之後：

```python
    # 人設欄（2026-08-05）：既有庫走 ALTER 升級，既有長輩自動拿到預設人設。
    # ⚠️ 預設值字面寫死為 'lively_granddaughter' 而不是 import personas：DDL 是
    # 一段 SQL 字串常數，把 Python 值插進去會讓「資料庫預設」與「程式預設」看似
    # 同步、實則各自演化。兩邊由 `test_accounts_store_contract.py` 對帳。
    "ALTER TABLE elders ADD COLUMN IF NOT EXISTS persona TEXT NOT NULL "
    "DEFAULT 'lively_granddaughter';"
```

`src/kinsun/accounts/store.py` 的 `PgAccountStore`：

```python
    def save_elder(self, elder: Elder, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO elders (elder_id, name, nickname, persona) VALUES (%s, %s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET "
            "name = EXCLUDED.name, nickname = EXCLUDED.nickname, persona = EXCLUDED.persona",
            (elder.elder_id, elder.name, elder.nickname, elder.persona),
        )

    def get_elder(self, elder_id: str) -> Elder | None:
        rows = self._db.query(
            "SELECT elder_id, name, nickname, persona FROM elders WHERE elder_id = %s",
            (elder_id,),
        )
        return Elder(*rows[0]) if rows else None
```

`src/kinsun/accounts/service.py`，接在 `set_elder_nickname` 之後：

```python
    def set_elder_persona(self, elder_id: str, persona_id: str) -> Elder:
        """家屬更改人設。值域由 API 層驗（見 `web/routers/elders.py`）。

        與 `set_elder_nickname` 分成兩支而不是一支「更新檔案」：它們是兩件互不
        相干的設定，合成一支會讓呼叫端漏帶一個欄位就把另一個洗掉。
        """
        elder = self._repo.get_elder(elder_id)
        if elder is None:
            raise AppAccountError("elder_not_found")
        updated = replace(elder, persona=persona_id)
        self._repo.save_elder(updated)
        return updated
```

`FakeAccountStore` 不必改：它存的是 `Elder` 物件本身。

- [ ] **Step 4: 跑測試確認通過（含既有庫升級路徑）**

離線：

```bash
uv run pytest tests/test_accounts_store_contract.py tests/test_accounts_service.py -v
uv run pytest tests/test_accounts_models.py tests/test_api_accounts.py -v
```

⚠️ **接著必須實測既有庫的升級路徑**——空的測試庫測不到「舊表加欄位」，而正式庫是舊表。在測試庫上手動重現：

```bash
psql "$KINSUN_TEST_DATABASE_URL" -c "DROP TABLE IF EXISTS elders CASCADE;"
psql "$KINSUN_TEST_DATABASE_URL" -c "CREATE TABLE elders (elder_id TEXT PRIMARY KEY, name TEXT NOT NULL);"
psql "$KINSUN_TEST_DATABASE_URL" -c "INSERT INTO elders VALUES ('legacy-1', '舊資料阿公');"
KINSUN_IT=1 uv run pytest tests/test_accounts_store_contract.py -v
psql "$KINSUN_TEST_DATABASE_URL" -c "SELECT elder_id, nickname, persona FROM elders WHERE elder_id='legacy-1';"
```

Expected: 最後一行印出 `legacy-1 | (空) | lively_granddaughter`——舊列自動拿到兩個預設值。

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/accounts/models.py src/kinsun/accounts/store.py src/kinsun/accounts/service.py src/kinsun/db.py tests/test_accounts_store_contract.py tests/test_accounts_service.py
git commit -m "$(cat <<'EOF'
feat(accounts): elders 表加人設欄位

人設要能「每位長輩不同」，就必須有地方存。沿用 nickname 的整套手法：資料表
加一欄、既有庫走 ALTER IF NOT EXISTS 就地升級，既有長輩自動拿到預設人設，
不需要任何人工 migration。

set_elder_persona 與 set_elder_nickname 分成兩支而不是合成一支「更新檔案」：
它們是互不相干的兩件設定，合成一支會讓呼叫端漏帶欄位就把另一個洗掉。

DDL 裡的預設值字面寫死為 'lively_granddaughter' 而不是插入 Python 常數——
把值插進 SQL 字串會讓兩邊看似同步、實則各自演化；改由合約測試對帳。

本 commit 只有資料層，還沒有任何地方讀這個欄位，對話行為完全不變。已在測試庫
實測既有庫升級路徑（建不含該欄的舊表→ensure_schema→驗舊列拿到預設值）——空的
測試庫測不到這條路徑，而正式庫是舊表。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3: API 端點

**Files:**
- Modify: `src/kinsun/web/routers/elders.py`
- Test: `tests/test_api_accounts.py`

**Interfaces:**
- Consumes: Task 1 的 `personas.persona_ids()`、Task 2 的 `AccountService.set_elder_persona`
- Produces: `PUT /api/v1/elders/{elder_id}/persona`，body `{"persona": str}`，回 `{"elder_id": str, "persona": str}`；`GET /elders` 與 `POST /elders` 的回傳新增 `persona` 欄

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_api_accounts.py` 末尾加（沿用該檔既有的 client／token fixture 慣例，名稱依現況調整）：

```python
def test_set_persona_updates_and_returns_it(client, guardian_token, elder_id):
    res = client.put(
        f"/api/v1/elders/{elder_id}/persona",
        json={"persona": "steady_grandson"},
        headers={"Authorization": f"Bearer {guardian_token}"},
    )
    assert res.status_code == 200
    assert res.json()["data"]["persona"] == "steady_grandson"

    listed = client.get("/api/v1/elders", headers={"Authorization": f"Bearer {guardian_token}"})
    assert listed.json()["data"][0]["persona"] == "steady_grandson"


def test_set_persona_rejects_unknown_value(client, guardian_token, elder_id):
    """寫入嚴格：認不得的值當場擋下，不讓它進資料庫。"""
    res = client.put(
        f"/api/v1/elders/{elder_id}/persona",
        json={"persona": "pirate_captain"},
        headers={"Authorization": f"Bearer {guardian_token}"},
    )
    assert res.status_code == 400


def test_set_persona_requires_managing_this_elder(client, other_guardian_token, elder_id):
    res = client.put(
        f"/api/v1/elders/{elder_id}/persona",
        json={"persona": "steady_grandson"},
        headers={"Authorization": f"Bearer {other_guardian_token}"},
    )
    assert res.status_code in (403, 404)
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_api_accounts.py -v -k persona
```

Expected: FAIL，404（端點不存在）

- [ ] **Step 3: 寫實作**

`src/kinsun/web/routers/elders.py`：

頂端匯入補 `from kinsun import personas`。

新增 request 模型（放在 `ElderProfileIn` 之後）：

```python
class ElderPersonaIn(BaseModel):
    """家屬更改金孫的人設（2026-08-05）。值域由 handler 對照 `personas.py` 驗。

    ⚠️ 刻意不併進 `ElderProfileIn`：那支是整份 upsert，前端漏帶 `nickname`
    就會把稱謂洗掉。獨立端點讓這種誤用不可能發生。
    """

    persona: str = Field(max_length=50)
```

`GET /elders` 的回傳加欄：

```python
        return ok(
            [
                {
                    "elder_id": e.elder_id,
                    "name": e.name,
                    "nickname": e.nickname,
                    "persona": e.persona,
                }
                for e in elders
            ]
        )
```

`POST /elders` 的回傳同樣補上 `"persona": elder.persona,`（比照 A-10 的教訓：後端回了前端就要收，否則剛建立的那筆在畫面上少一格）。

新增 handler（放在 `update_elder_profile` 之後）：

```python
    @router.put("/elders/{elder_id}/persona")
    def update_elder_persona(
        elder_id: str, body: ElderPersonaIn, auth: GuardianAuth = Depends(current_guardian)
    ) -> dict:
        """家屬更改金孫的人設（2026-08-05）。下一輪對話即生效。"""
        scope.assert_manages(auth, elder_id)
        persona_id = body.persona.strip()
        if persona_id not in personas.persona_ids():
            # 邊界要驗證：認不得的值當場擋下，不讓它進資料庫。訊息帶合法值，
            # 免得呼叫端要翻程式碼才知道能填什麼。
            raise HTTPException(
                status_code=400,
                detail=f"persona 必須是下列其中之一：{'、'.join(personas.persona_ids())}",
            )
        elder = accounts.set_elder_persona(elder_id, persona_id)
        return ok({"elder_id": elder.elder_id, "persona": elder.persona})
```

- [ ] **Step 4: 跑測試確認通過**

```bash
uv run pytest tests/test_api_accounts.py -v
uv run ruff check . && uv run ruff format --check .
```

Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/web/routers/elders.py tests/test_api_accounts.py
git commit -m "$(cat <<'EOF'
feat(api): 新增更改長輩人設的端點

家屬要能挑人設就需要一支可寫的端點，且長輩清單要帶出目前的人設，畫面才知道
選單該預選哪一個。

刻意新開 PUT /elders/{id}/persona 而不是併進既有的 /profile：後者是整份 upsert，
前端漏帶 nickname 就會把稱謂洗掉，而稱謂設錯的後果是金孫每輪亂猜阿公／阿嬤
（2026-07-17 實測過的真實問題）。獨立端點讓這種誤用不可能發生。

值域在 handler 驗、認不得就 400，且錯誤訊息直接列出合法值——與 personas.py
讀取時退回預設的寬容行為是刻意的分工：寫入嚴格、讀取寬容，資料庫裡萬一有髒值
也不會讓長輩的對話卡死。

對話行為仍然完全不變：這個欄位還沒有任何地方在讀。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4: 家屬端人設選擇器

**Files:**
- Modify: `shared/types.ts`（`Elder` 型別）
- Modify: `web/src/guardian/api.ts`
- Modify: `web/src/guardian/ElderDetailScreen.tsx`
- Modify: `web/src/strings.ts`（文案；實際路徑依該檔現況）
- Modify: `web/src/guardian/GuardianApp.tsx`、`web/src/guardian/HomeScreen.tsx`（把 `persona` 一路傳下去）
- Test: `web/src/guardian/ElderDetailScreen.test.tsx`

**Interfaces:**
- Consumes: Task 3 的 `PUT /api/v1/elders/{elder_id}/persona`
- Produces: `setElderPersona(elderId: string, persona: string, token: string): Promise<string>`；`ElderDetailScreen` 新增 `persona: string` prop

- [ ] **Step 1: 寫失敗的測試**

在 `web/src/guardian/ElderDetailScreen.test.tsx` 加（沿用該檔既有的 render helper 與 fetch mock 慣例）：

```tsx
it("顯示目前人設並可切換", async () => {
  const fetchMock = mockJsonRoutes({
    "PUT /api/v1/elders/e1/persona": { elder_id: "e1", persona: "steady_grandson" },
  });
  renderScreen({ persona: "lively_granddaughter" });

  const steady = await screen.findByRole("radio", { name: /穩重的孫子/ });
  expect(screen.getByRole("radio", { name: /活潑的孫女/ })).toBeChecked();

  await userEvent.click(steady);
  await userEvent.click(screen.getByRole("button", { name: /儲存/ }));

  await waitFor(() => expect(steady).toBeChecked());
  expect(fetchMock).toHaveBeenCalledWith(
    expect.stringContaining("/api/v1/elders/e1/persona"),
    expect.objectContaining({ method: "PUT" }),
  );
});

it("儲存失敗時保留使用者的選擇並顯示錯誤", async () => {
  mockJsonRoutes({ "PUT /api/v1/elders/e1/persona": { status: 500 } });
  renderScreen({ persona: "lively_granddaughter" });

  await userEvent.click(await screen.findByRole("radio", { name: /穩重的孫子/ }));
  await userEvent.click(screen.getByRole("button", { name: /儲存/ }));

  expect(await screen.findByRole("alert")).toBeInTheDocument();
  expect(screen.getByRole("radio", { name: /穩重的孫子/ })).toBeChecked();
});
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
npm --prefix web run test -- ElderDetailScreen
```

Expected: FAIL，找不到 radio

- [ ] **Step 3: 寫實作**

`shared/types.ts`：

```ts
export type Elder = { elder_id: string; name: string; nickname: string; persona: string };
```

`web/src/guardian/api.ts`，接在 `revokeElderDeviceBindings` 之後：

```ts
/** 更改金孫對這位長輩的人設（2026-08-05）。回傳後端實際採用的值。 */
export async function setElderPersona(
  elderId: string,
  persona: string,
  token: string,
): Promise<string> {
  const body = await request<{ persona: string }>(`/api/v1/elders/${elderId}/persona`, {
    method: "PUT",
    body: JSON.stringify({ persona }),
    token,
  });
  return body.persona;
}
```

`web/src/strings.ts` 的 `elderDetail` 區塊補：

```ts
    personaSection: "金孫的個性",
    personaHelp: "選一種說話的方式，下一次跟金孫講話就會換過來。聲音不會變。",
    personaLively: "活潑的孫女",
    personaLivelyHint: "有活力、情緒藏不住，會替您開心也會替您緊張",
    personaSteady: "穩重的孫子",
    personaSteadyHint: "沉穩、話不多，關心用做事表達",
    savePersona: "儲存個性",
    personaSaved: "已經換好了，下一次跟金孫講話就會聽到。",
```

`web/src/guardian/ElderDetailScreen.tsx`：

props 加 `persona: string;`，並在既有 state 區塊補：

```tsx
  // ⚠️ 以 props.persona 當初值、之後由本畫面自己持有：儲存成功後不重打
  // GET /elders（那會連帶重載整頁三支端點）。父層的清單由下一次進入時刷新。
  const [persona, setPersona] = useState(props.persona);
  const [personaMessage, setPersonaMessage] = useState("");
  const [personaError, setPersonaError] = useState("");
  const [personaBusy, setPersonaBusy] = useState(false);
```

儲存函式（放在 `saveAccount` 附近）：

```tsx
  async function savePersona() {
    setPersonaBusy(true);
    setPersonaMessage("");
    setPersonaError("");
    try {
      const saved = await setElderPersona(elderId, persona, token);
      setPersona(saved);
      setPersonaMessage(strings.elderDetail.personaSaved);
    } catch (err) {
      if (!signOutOn401(err)) {
        // ⚠️ 失敗時**不**把選擇復原成舊值：家屬看得到自己選了什麼，配上錯誤
        // 訊息就知道要再按一次；靜靜跳回舊值會讓人以為系統擅自改掉了他的選擇。
        setPersonaError(apiErrorMessage(err));
      }
    } finally {
      setPersonaBusy(false);
    }
  }
```

畫面區塊（放在帳號區塊之前）：

```tsx
      <Section title={strings.elderDetail.personaSection}>
        <p className="text-xs leading-5 text-ink-soft">{strings.elderDetail.personaHelp}</p>
        {[
          {
            value: "lively_granddaughter",
            label: strings.elderDetail.personaLively,
            hint: strings.elderDetail.personaLivelyHint,
          },
          {
            value: "steady_grandson",
            label: strings.elderDetail.personaSteady,
            hint: strings.elderDetail.personaSteadyHint,
          },
        ].map((option) => (
          <label key={option.value} className="flex items-start gap-2 py-1">
            <input
              type="radio"
              name="persona"
              value={option.value}
              checked={persona === option.value}
              onChange={() => {
                setPersona(option.value);
                setPersonaMessage("");
                setPersonaError("");
              }}
            />
            <span>
              <span className="block text-sm text-ink">{option.label}</span>
              <span className="block text-xs leading-5 text-ink-soft">{option.hint}</span>
            </span>
          </label>
        ))}
        <Button
          label={strings.elderDetail.savePersona}
          variant="outline"
          onClick={savePersona}
          busy={personaBusy}
          disabled={persona === props.persona && !personaError}
        />
        <NoticeText message={personaMessage} />
        <ErrorText message={personaError} />
      </Section>
```

匯入補 `setElderPersona`。

`GuardianApp.tsx` 把選中長輩的 `persona` 傳給 `ElderDetailScreen`；`HomeScreen.tsx` 新增長輩後放進清單的那個物件補上 `persona: created.persona`（⚠️ 這正是 A-10 那個坑：後端回了但前端丟掉，於是剛新增的那筆少一格）。

- [ ] **Step 4: 跑測試確認通過**

```bash
npm --prefix web run test
npm --prefix web run typecheck
npm --prefix web run lint
```

Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add shared/types.ts web/src/guardian/ web/src/strings.ts
git commit -m "$(cat <<'EOF'
feat(web): 家屬端可以挑金孫的個性

後端已經能存能改人設，但家屬沒有地方按。這個 commit 讓功能第一次對使用者可見。

放在長輩詳情頁而不是新增長輩的流程裡：新增時家屬還不知道兩種個性差在哪，
選了也是亂選；詳情頁可以隨時改，而且旁邊就有稱謂與帳號等其他長輩設定。
選項附一行說明，家屬不必試才知道差別。

儲存失敗時刻意不把選擇復原成舊值：家屬看得到自己選了什麼，配上錯誤訊息就知道
要再按一次；靜靜跳回舊值會讓人以為系統擅自改掉了他的選擇。

HomeScreen 新增長輩後一併帶上 persona——A-10 就是後端回了、前端丟掉，於是剛
建立的那筆在畫面上少一格、要重新整理才對。

文案明講「聲音不會變」：這一版兩種個性共用同一個克隆聲音，不先講家屬會以為壞了。

對話行為仍然不變：這個欄位還沒有任何地方在讀。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5: 長輩檔案讀取器

**Files:**
- Create: `src/kinsun/accounts/profile.py`
- Test: `tests/test_accounts_profile.py`
- （`accounts/facts.py` 本 Task **不動**，Task 6 才刪——保持每一步都是綠的）

**Interfaces:**
- Consumes: Task 2 的 `Elder.persona`
- Produces: `ElderProfile(persona_id: str, address_line: str)`、`ElderProfileReader(store).get_profile(elder_id: str) -> ElderProfile`

- [ ] **Step 1: 寫失敗的測試**

建立 `tests/test_accounts_profile.py`：

```python
"""ElderProfileReader：一次讀取同時供應人設與稱呼。

稱呼那一句的措辭與 2026-07-17 的 `ElderProfileFacts` **一字不差**，只是改成
獨立一行、不再包在 FactSection 裡。當時的實測背景：情境沒有任何稱呼資料時，
模型每輪自行猜一個性別稱謂（同一位長輩一下被叫阿公、一下被叫阿嬤），真實
使用有一半機率叫錯。
"""

from __future__ import annotations

from kinsun.accounts.models import Elder
from kinsun.accounts.profile import ElderProfileReader
from kinsun.accounts.store import FakeAccountStore
from kinsun.personas import DEFAULT_PERSONA_ID, STEADY_GRANDSON


def _reader_for(elder: Elder | None) -> ElderProfileReader:
    store = FakeAccountStore()
    if elder is not None:
        store.save_elder(elder)
    return ElderProfileReader(store)


def test_unknown_elder_falls_back_to_default_persona_and_no_address():
    profile = _reader_for(None).get_profile("e-nope")
    assert profile.persona_id == DEFAULT_PERSONA_ID
    assert profile.address_line == ""


def test_nickname_is_used_verbatim():
    profile = _reader_for(Elder("e1", "王秀英", nickname="秀英阿嬤")).get_profile("e1")
    assert "秀英阿嬤" in profile.address_line


def test_name_fallback_forbids_gender_guessing():
    """稱謂未設定時退回名字，並明令不要猜「阿公／阿嬤」。"""
    line = _reader_for(Elder("e1", "王秀英")).get_profile("e1").address_line
    assert "王秀英" in line
    assert "不要" in line and "阿公" in line and "阿嬤" in line


def test_no_name_no_nickname_gives_empty_address_line():
    assert _reader_for(Elder("e1", "")).get_profile("e1").address_line == ""


def test_persona_is_carried_out():
    profile = _reader_for(Elder("e1", "王秀英", persona=STEADY_GRANDSON)).get_profile("e1")
    assert profile.persona_id == STEADY_GRANDSON


def test_unknown_persona_value_is_passed_through_untouched():
    """讀取器不做值域判斷——退回預設是 `personas.get_persona` 的職責，只有一處。"""
    profile = _reader_for(Elder("e1", "王秀英", persona="pirate_captain")).get_profile("e1")
    assert profile.persona_id == "pirate_captain"
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_accounts_profile.py -v
```

Expected: FAIL，`ModuleNotFoundError: No module named 'kinsun.accounts.profile'`

- [ ] **Step 3: 寫實作**

建立 `src/kinsun/accounts/profile.py`：

```python
"""長輩檔案讀取器：一次讀取同時供應「人設」與「稱呼」。

## 為什麼不是事實提供者（2026-08-05）

這段程式的前身是 `accounts/facts.py` 的 `ElderProfileFacts`——七個事實提供者
之一，讀長輩資料列只為了產出稱呼那一句、掛在情境區塊的尾巴。

人設要放進提示詞**開頭**（性格與規則分家），`CareAgent` 就得知道這位長輩的
人設；天真做法是再查一次 `elders`，但那與稱呼那次讀取完全重複，而正式環境的
psycopg 連線池只有 3 條（`DATABASE_POOL_MAX_SIZE=3`）。故不是複製一份讀取，
而是把那一次讀取**整個搬家**：從事實提供者清單移除，改由 `CareAgent` 在每輪
開頭與情境組裝並行發動，一次讀取供應兩樣東西。全輪的資料庫讀取次數不變。

## 稱呼那一句的措辭一字不改

`address_line` 的兩種措辭（有稱謂／只有名字）與 2026-07-17 的版本逐字相同，
只是從 `FactSection` 的條目改成獨立一行。當時的實測背景：情境沒有任何稱呼
資料時，模型每輪自行猜一個性別稱謂（同一位長輩一下被叫阿公、一下被叫阿嬤），
真實使用有一半機率叫錯。那段文字是調過的，不在這次一併重調。

⚠️ 位置變了（情境區塊尾巴 → 提示詞開頭），這是往「模型更重視」的方向移動，
但仍需人工驗收確認沒有反效果——見設計文件的驗收清單第 6 項。
"""

from __future__ import annotations

from dataclasses import dataclass

from kinsun.personas import DEFAULT_PERSONA_ID


@dataclass(frozen=True)
class ElderProfile:
    """一位長輩的「該怎麼跟他講話」：人設 ＋ 稱呼那一句。

    `persona_id` 原樣帶出、**不在這裡做值域判斷**——退回預設是
    `personas.get_persona` 的職責，讓那個判斷只有一處。
    `address_line` 為空字串＝這位長輩沒有任何稱呼資料（查無此人、或名字與
    稱謂都是空的），提示詞就少那一句。
    """

    persona_id: str = DEFAULT_PERSONA_ID
    address_line: str = ""


class ElderProfileReader:
    """get_profile(elder_id) -> ElderProfile（查無長輩時回全預設，不拋例外）。"""

    def __init__(self, store) -> None:
        self._store = store

    def get_profile(self, elder_id: str) -> ElderProfile:
        elder = self._store.get_elder(elder_id)
        if elder is None:
            return ElderProfile()
        if elder.nickname:
            address_line = f"請用「{elder.nickname}」稱呼她／他，開頭問候也用這個稱呼。"
        elif elder.name:
            address_line = (
                f"她／他的名字是「{elder.name}」，可以自然地用名字稱呼。"
                "系統沒有性別資料，不要自行猜測用「阿公」或「阿嬤」這類稱謂，"
                "除非她／他自己說過。"
            )
        else:
            address_line = ""
        return ElderProfile(persona_id=elder.persona, address_line=address_line)
```

- [ ] **Step 4: 跑測試確認通過**

```bash
uv run pytest tests/test_accounts_profile.py tests/test_accounts_facts.py -v
uv run ruff check . && uv run ruff format --check .
```

Expected: 全部 PASS（舊的 `test_accounts_facts.py` 這一步仍然存在且仍然綠）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/accounts/profile.py tests/test_accounts_profile.py
git commit -m "$(cat <<'EOF'
feat(accounts): 新增長輩檔案讀取器（人設＋稱呼一次讀完）

下一個 commit 要把人設放進提示詞開頭，CareAgent 因此得知道這位長輩的人設。
天真做法是再查一次 elders，但那與現行「稱呼」那次讀取完全重複，而正式環境的
連線池只有 3 條。故不複製讀取，而是把那一次讀取整個搬家——本 commit 先把新
的讀取器立起來，舊的 ElderProfileFacts 暫時並存，下一個 commit 才切換與刪除。
這樣每一步都是可獨立驗證的綠燈。

稱呼那一句的兩種措辭與 2026-07-17 的版本逐字相同，只是從 FactSection 的條目
改成獨立一行——那段文字是實測調過的（沒有它，模型每輪亂猜阿公／阿嬤，一半
機率叫錯），不在這次一併重調。

persona_id 原樣帶出、不在這裡做值域判斷：退回預設是 personas.get_persona 的
職責，讓那個判斷只有一處。

尚未接線，行為完全不變。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6: 提示詞重構與接線（本計畫的核心）

**Files:**
- Modify: `src/kinsun/agent.py`（`SYSTEM_PROMPT`、`PreparedTurn`、`CareAgent.__init__`／`prepare`／`_envelope`／`handle`／`proactive`）
- Modify: `src/kinsun/composition.py`（事實清單移除 `ElderProfileFacts`、`CareAgent` 注入 reader、註解同步）
- Modify: `src/kinsun/safety/moderation.py:16`（交叉引用註解）
- Delete: `src/kinsun/accounts/facts.py`、`tests/test_accounts_facts.py`
- Test: `tests/test_agent.py`

**Interfaces:**
- Consumes: Task 1 的 `personas.get_persona`、Task 5 的 `ElderProfileReader.get_profile`／`ElderProfile`
- Produces: `CareAgent(llm, session, *, tools=None, max_tool_iters=3, profile_of: Callable[[str], ElderProfile] | None = None)`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_agent.py` 加（沿用該檔既有的假 LLM／假 session 慣例）：

```python
def test_prompt_starts_with_persona_then_rules():
    """人設在最前面、規則段緊接其後、情境在最後——順序本身是契約。"""
    llm = _CapturingLLM()
    agent = CareAgent(
        llm,
        _NullSession(),
        profile_of=lambda _elder_id: ElderProfile(
            persona_id=STEADY_GRANDSON, address_line="請用「秀英阿嬤」稱呼她／他。"
        ),
    )

    agent.handle("e1", "你好")

    prompt = llm.system_prompt
    assert prompt.startswith(get_persona(STEADY_GRANDSON).tone)
    assert "請用「秀英阿嬤」稱呼她／他。" in prompt
    assert prompt.index("秀英阿嬤") < prompt.index(SYSTEM_PROMPT)


def test_two_personas_produce_different_openings_but_identical_rules():
    prompts = {}
    for persona_id in (LIVELY_GRANDDAUGHTER, STEADY_GRANDSON):
        llm = _CapturingLLM()
        CareAgent(
            llm, _NullSession(), profile_of=lambda _e, p=persona_id: ElderProfile(persona_id=p)
        ).handle("e1", "你好")
        prompts[persona_id] = llm.system_prompt

    assert prompts[LIVELY_GRANDDAUGHTER] != prompts[STEADY_GRANDSON]
    for prompt in prompts.values():
        assert SYSTEM_PROMPT in prompt


def test_no_profile_reader_falls_back_to_default_persona():
    """不注入 reader 時走預設人設、沒有稱呼那一句（既有測試據此不必改）。"""
    llm = _CapturingLLM()
    CareAgent(llm, _NullSession()).handle("e1", "你好")
    assert llm.system_prompt.startswith(get_persona(DEFAULT_PERSONA_ID).tone)


def test_profile_read_failure_degrades_to_default_without_breaking_the_turn():
    """讀不到長輩檔案時照常回覆——不可為了一格設定把整輪打回回退話術。"""

    def boom(_elder_id):
        raise RuntimeError("資料庫掛了")

    llm = _CapturingLLM()
    reply = CareAgent(llm, _NullSession(), profile_of=boom).handle("e1", "你好")

    assert reply  # 有回覆，不是回退話術以外的東西壞掉
    assert llm.system_prompt.startswith(get_persona(DEFAULT_PERSONA_ID).tone)


def test_rules_no_longer_mandate_ending_with_a_question():
    """罐頭感的直接來源：第（5）條原本要求每一則都以關心或反問收尾。"""
    assert "結尾自然帶一句關心或反問" not in SYSTEM_PROMPT
    assert "不必每一則都用問句結束" in SYSTEM_PROMPT


def test_rules_no_longer_declare_identity():
    """身分宣告已移交人設段，規則段不可再自己宣告一次（否則兩邊會打架）。"""
    assert "溫暖、有耐心的台灣長輩陪伴助理" not in SYSTEM_PROMPT
```

檔頂匯入補：

```python
from kinsun.accounts.profile import ElderProfile
from kinsun.personas import (
    DEFAULT_PERSONA_ID,
    LIVELY_GRANDDAUGHTER,
    STEADY_GRANDSON,
    get_persona,
)
```

若該檔尚無 `_CapturingLLM`，加一個（記下最後一次收到的 system_prompt）：

```python
class _CapturingLLM:
    """記下最後一次收到的 system_prompt，供提示詞組裝的斷言使用。"""

    def __init__(self, reply: str = "好啊") -> None:
        self.system_prompt = ""
        self._reply = reply

    def generate(self, *, system_prompt: str, messages) -> str:
        self.system_prompt = system_prompt
        return self._reply
```

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_agent.py -v -k "persona or rules_no_longer or profile"
```

Expected: FAIL，`TypeError: CareAgent.__init__() got an unexpected keyword argument 'profile_of'`

- [ ] **Step 3: 寫實作（agent.py）**

**3a. `SYSTEM_PROMPT` 移除開頭的身分宣告句。** 刪掉第一行：

```python
    "你是「金孫」，一位溫暖、有耐心的台灣長輩陪伴助理。"
```

並在常數上方加說明：

```python
# 規則段：語音格式、工具用法、安全紅線、失智照護準則。
#
# ⚠️ 身分宣告（「你是「金孫」…」）**不在這裡**，它由 `personas.py` 的各人設自帶並
# 拼在本段之前（2026-08-05）。兩者分家的理由：性格與規則混在同一段時，改語氣會
# 碰到安全規則、改安全規則也會動到語氣——那正是這份提示詞一直難改的原因。
#
# ⚠️ **常數名稱維持 `SYSTEM_PROMPT`**：`composition.py` 靠字面掃描它對帳「提示詞
# 點名的工具有沒有註冊」。工具名全部住在本段，那行掃描因此一個字都不用改；人設段
# 則由 `tests/test_personas.py` 擋住工具名混進去。
SYSTEM_PROMPT = (
    "你的回覆會被合成成語音念給長輩聽，所以務必遵守："
    ...
```

**3b. 第（2）條上方的字數上限註解改理由。** 現行結尾寫著「⚠️ 不再往下壓：第（5）條要求『結尾自然帶一句關心或反問』…」，改為：

```python
    # 字數上限直接換算成長輩的等待（2026-07-26 延遲實測）：TTS 是 0.9 秒固定成本
    # ＋每字 0.10 秒，四十字就是四到五秒。四十→三十字約省 1 秒。⚠️ 不再往下壓：
    # 三十字是那次實測後定的值，再往下沒有任何實測支持，而擠得更短會把回覆變成
    # 沒有溫度的通知。（原本的理由掛在第（5）條的「結尾必帶關心或反問」上，該條
    # 已於 2026-08-05 改寫，那個推論不再成立。）
```

**3c. 第（5）條改寫：**

```python
    # ⚠️ 這一條 2026-08-05 從「必須反問」改成「不必反問」。原文要求「結尾自然帶一句
    # 關心或反問」，於是每一則回覆都長成同一個形狀——這是罐頭感最直接的來源。
    # 後半句對付的是另一個症狀（用字句型翻來覆去就那幾句）。
    # ⚠️ 已知取捨：原規則確實在推動對話延續，拿掉之後要觀察長輩的對話輪數有沒有
    # 明顯下降；若有，改成「情境合適時才問」的較弱版本，而不是把硬性要求加回來。
    "（5）想問就問、話講完了就自然收尾，不必每一則都用問句結束；"
    "也不要每次都用同樣的開場白或同樣的安慰句。"
```

**3d. `PreparedTurn` 多跑一條背景執行緒讀長輩檔案。** `__init__` 加參數與第二條執行緒：

```python
# 長輩檔案讀取的等待上限（秒）。與 `recall._FACT_TIMEOUT_SECONDS` 同值同理由：
# 單次 Supabase 往返實測約 0.21 秒，慢到 5 秒代表它卡在連線池上，此時「這一輪
# 少一句稱呼、用預設人設」遠好過「拖著整輪一起等」。
PROFILE_TIMEOUT_SECONDS = 5.0
```

```python
    def __init__(
        self,
        assemble: Callable[[], object],
        *,
        profile_of: Callable[[], object] | None = None,
        timeout: float | None = None,
        spoke_at: datetime | None = None,
    ) -> None:
        ...
        # 長輩檔案（人設＋稱呼）與情境組裝並行（2026-08-05）。
        #
        # ⚠️ 為什麼是第二條執行緒而不是跟 assemble 排在同一條：串在一起會把這
        # 0.21 秒直接加到那條 2.9 秒的關鍵路徑上，長輩每一輪都多等。並行則是零成本。
        # （本類別原本的 docstring 寫「一輪只有一件事要先跑，故用裸執行緒而非
        # 執行緒池」——現在是兩件，但結論不變：兩條都是一輪一次、生命週期短，
        # 不該共用 `background.run` 那個有界佇列。）
        self._profile: object | None = None
        self._profile_thread: threading.Thread | None = None
        if profile_of is not None:
            profile_context = contextvars.copy_context()
            self._profile_thread = threading.Thread(
                target=lambda: profile_context.run(self._run_profile, profile_of),
                name="kinsun-profile",
                daemon=True,
            )
            self._profile_thread.start()
```

```python
    def _run_profile(self, profile_of: Callable[[], object]) -> None:
        try:
            self._profile = profile_of()
        except Exception:  # noqa: BLE001 - 讀不到就用預設，不可中斷這一輪
            logger.warning("長輩檔案讀取失敗，本輪用預設人設且不帶稱呼")

    def profile(self):
        """等長輩檔案讀完；讀不到、逾時或沒有提供讀取器都回 None（＝走預設）。

        ⚠️ 與 `context()` 的紀律**刻意相反**：那邊的例外必須重拋（憑空失憶要有人
        知道），這邊一律吞掉。差別在後果——少一句稱呼與少一種語氣，長輩仍拿得到
        一則正常的回覆；為了一格設定把整輪打回回退話術是嚴格更糟的結果。
        """
        if self._profile_thread is None:
            return None
        self._profile_thread.join(PROFILE_TIMEOUT_SECONDS)
        if self._profile_thread.is_alive():
            logger.warning("長輩檔案讀取逾時（%.1f 秒），本輪用預設人設", PROFILE_TIMEOUT_SECONDS)
            tracing.update_trace_metadata(profile_read_timeout=True)
            return None
        return self._profile
```

**3e. `CareAgent` 收下讀取器並組裝提示詞：**

```python
    def __init__(
        self,
        llm: LLMClient,
        session: SessionMemory,
        *,
        tools=None,
        max_tool_iters: int = 3,
        profile_of: Callable[[str], ElderProfile] | None = None,
    ) -> None:
        ...
        # ⚠️ 預設 None 是相容性要求，不是貼心：`tests/test_pipeline.py` 與
        # `tests/test_channels_app_turns.py` 共十餘處以 `CareAgent(llm, session)`
        # 兩個位置參數建構，改成必填會讓那些與人設無關的測試全部要改。
        # None＝走預設人設、沒有稱呼那一句。
        self._profile_of = profile_of
```

```python
    def prepare(
        self, elder_id: str, user_text: str, *, spoke_at: datetime | None = None
    ) -> PreparedTurn:
        announce_transcript(user_text)
        return PreparedTurn(
            lambda: self._session.assemble(elder_id, user_text),
            profile_of=self._profile_for(elder_id),
            spoke_at=spoke_at,
        )

    def _profile_for(self, elder_id: str) -> Callable[[], ElderProfile] | None:
        """把 elder_id 綁進去，交給 `PreparedTurn` 在背景跑。沒有讀取器時回 None。"""
        if self._profile_of is None:
            return None
        return lambda: self._profile_of(elder_id)
```

`_envelope` 組裝新結構：

```python
    def _envelope(
        self, elder_id: str, query: str, *, prepared: PreparedTurn | None = None
    ) -> tuple[str, list[Message], datetime, str]:
        """回傳 (system_prompt, history, spoke_at, persona_id)。

        提示詞的順序本身是契約：人設語氣 → 稱呼 → 規則段 → 情境 → 本輪追加指示。
        人設擺在最前面，是因為模型對開頭的身分宣告權重最高——擺在情境區塊裡
        影響不到語氣（2026-08-05 設計決議）。
        """
        turn = prepared or PreparedTurn(
            lambda: self._session.assemble(elder_id, query),
            profile_of=self._profile_for(elder_id),
        )
        ctx = turn.context()
        profile = turn.profile() or ElderProfile()
        persona = get_persona(profile.persona_id)
        head = persona.tone + profile.address_line
        return (
            head + SYSTEM_PROMPT + ctx.system_suffix + current_turn_directive(),
            ctx.history,
            turn.spoke_at,
            persona.persona_id,
        )
```

`handle` 與 `proactive` 的解包同步四元組，並把 `attach_prompt` 改成：

```python
        system_prompt, history, spoke_at, persona_id = self._envelope(
            elder_id, user_text, prepared=prepared
        )
        # ⚠️ 註冊的是**模板**（人設語氣＋規則段），刻意不含稱呼那一句：稱呼是每位
        # 長輩不同的內容，混進 Opik 的 prompt 版本庫會變成「每位長輩一個版本」，
        # 版本追蹤就失去意義。兩種人設 → 兩個穩定版本。
        tracing.attach_prompt(
            f"care_system_{persona_id}", get_persona(persona_id).tone + SYSTEM_PROMPT
        )
```

⚠️ `handle` 原本在第一行就 `attach_prompt`，現在必須移到 `_envelope` 之後（要先知道人設）。`proactive` 同理。

檔頂匯入補：

```python
from kinsun.accounts.profile import ElderProfile
from kinsun.personas import get_persona
```

- [ ] **Step 4: 寫實作（composition.py 接線）**

事實清單移除 `ElderProfileFacts(account_store)` 那兩行註解與一行程式，並在 `TimeFacts` 的註解後補一句：

```python
            # ⚠️ 稱呼**不在這裡**（2026-08-05）：它與人設由 `ElderProfileReader`
            # 一次讀出、拼在提示詞開頭（見 accounts/profile.py 的模組說明）。
            # 這裡曾經有一個 ElderProfileFacts，搬走的理由是「不要為了人設多查
            # 一次同一張表」。
```

`_gather_facts` 的 docstring 裡「稱呼／三種排程／守則／位置」改成「三種排程／守則／位置」，「目前 7 個，其中 6 個碰 DB」改成「目前 6 個，其中 5 個碰 DB」；`assemble` docstring 的「七路事實」改「六路事實」。

`CareAgent` 建構補上：

```python
    agent = CareAgent(
        externals.gemini,
        session,
        profile_of=ElderProfileReader(account_store).get_profile,
        tools=build_tool_registry(
```

匯入由 `from kinsun.accounts.facts import ElderProfileFacts` 改為 `from kinsun.accounts.profile import ElderProfileReader`。

刪除舊檔：

```bash
git rm src/kinsun/accounts/facts.py tests/test_accounts_facts.py
```

`src/kinsun/safety/moderation.py:16` 的交叉引用改為：

```python
#   就回拒絕話術，會直接打斷對話，與 `agent.SYSTEM_PROMPT` 第（5）條「話講完了就
#   自然收尾、不必每一則都用問句結束」的語氣一致（該條於 2026-08-05 由原本的
#   「結尾必帶關心或反問」改寫）。
```

（實際措辭依該處上下文調整，重點是**不可留下引用已不存在的原文**。）

- [ ] **Step 5: 跑全套測試**

一次一支：

```bash
uv run pytest tests/test_agent.py -v
uv run pytest tests/test_pipeline.py -v
uv run pytest tests/test_channels_app_turns.py -v
uv run pytest tests/test_composition.py -v
uv run pytest tests -q
uv run ruff check . && uv run ruff format --check .
```

Expected: 全部 PASS。若 `test_accounts_facts.py` 仍被匯入某處而報錯，代表還有殘留引用——用 `grep -rn "ElderProfileFacts\|accounts.facts" src tests` 找出來清掉。

- [ ] **Step 6: 真模型抽驗（不可略過）**

離線測試證明不了語氣真的變了。跑一次真的 Gemini，兩種人設各問同一句：

```bash
uv run python scripts/recall_probe.py  # 若該腳本可帶人設；否則用下面的臨時腳本
```

臨時腳本（放 `/tmp`，不進版控）：對同一句「我今天膝蓋很痛」分別用兩種人設跑 `CareAgent.handle`，把兩則回覆並排印出來。**要親眼確認**：語氣確實不同、兩者都沒有給醫療建議、都沒有以問句硬收尾。

- [ ] **Step 7: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(agent): 人設進提示詞開頭，性格與規則分家

金孫的回覆讀起來像罐頭，成因有二：SYSTEM_PROMPT 第（5）條要求每一則都以關心
或反問收尾（於是每則都長成同一個形狀），以及所有長輩共用同一份語氣。這個
commit 同時處理兩者——只加人設不動規則的話，回覆仍會長成同一個形狀。

提示詞改為「人設語氣＋稱呼 → 規則段 → 情境 → 本輪指示」。人設擺最前面是因為
模型對開頭的身分宣告權重最高；擺在情境區塊裡影響不到語氣。分家之後，改語氣
不會碰到安全規則、改安全規則也不會動到語氣——那正是這份提示詞一直難改的原因。
常數名 SYSTEM_PROMPT 維持不變，composition 對帳工具註冊的字面掃描因此零改動。

CareAgent 要知道人設，但不能為此多查一次 elders（正式環境連線池只有 3 條），
故把那一次讀取整個搬家：ElderProfileFacts 從七個事實提供者裡移除，改由
PreparedTurn 起第二條背景執行緒、與情境組裝並行讀一次，同時供應人設與稱呼。
全輪的資料庫讀取次數不變，事實提供者的並行度反而由 7 降到 6。

讀不到長輩檔案時吞掉例外、走預設人設——與 context() 必須重拋例外的紀律刻意
相反：少一句稱呼長輩仍拿得到正常回覆，為了一格設定把整輪打回回退話術嚴格更糟。

profile_of 預設 None 是相容性要求：十餘處測試以兩個位置參數建構 CareAgent，
改成必填會讓那些與人設無關的測試全部要改。

⚠️ 稱呼那一句的位置從情境區塊尾巴移到提示詞開頭。措辭一字未改，但位置變了，
而稱呼叫錯是 2026-07-17 實測過的真實問題（同一位長輩一下被叫阿公一下被叫阿嬤）
——已用真模型抽驗兩種人設，稱呼正確、語氣確實不同、安全規則未鬆動。

連帶修正兩處會因此過期的註解：safety/moderation.py 引用第（5）條的原文、
agent.py 字數上限那段掛在第（5）條上的推論（三十字上限本身不變）。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7: 等待語分人設

**Files:**
- Modify: `src/kinsun/speech/acks.py`
- Modify: `src/kinsun/speech/ack_audio.py`（參數名）
- Modify: `src/kinsun/turn_context.py`（`announce_tools`）
- Modify: `src/kinsun/agent.py`（`_run_tool_loop` 帶人設）
- Modify: `src/kinsun/channels/app/ws.py`（`_ack_sender`）
- Test: `tests/test_speech_acks.py`、`tests/test_turn_context.py`

**Interfaces:**
- Consumes: Task 1 的人設 id、Task 6 的 `_envelope` 回傳的 `persona_id`
- Produces: `acks.pick(tool_name, *, persona_id=DEFAULT_PERSONA, rng=None)`、`announce_tools(tool_names: list[str], persona_id: str)`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_speech_acks.py` 加：

```python
def test_every_persona_in_the_catalogue_has_a_voice_library():
    """人設目錄與語庫必須對得上——少一格會讓那個人設默默用到別人的句子。"""
    assert set(acks.personas()) == set(personas.persona_ids())


def test_secondary_persona_falls_back_to_its_own_generic():
    """第二人設只寫 generic，逐工具句由保底供應——但保底必須是**它自己的**。"""
    phrase = acks.phrases_for("get_news", persona_id=personas.STEADY_GRANDSON)
    assert phrase == acks.persona(personas.STEADY_GRANDSON).generic
    assert phrase != acks.persona(personas.LIVELY_GRANDDAUGHTER).generic


@pytest.mark.parametrize("persona_id", personas.persona_ids())
def test_every_phrase_is_speakable(persona_id: str):
    """每一句都會被 TTS 原封唸出來：不可有符號、英文或機關名。"""
    one = acks.persona(persona_id)
    for phrase in (*one.generic, *(p for g in one.by_tool.values() for p in g)):
        check_speakable(phrase)
```

檔頂補 `from kinsun import personas`。

在 `tests/test_turn_context.py` 加：

```python
def test_announce_tools_passes_persona_to_the_listener():
    seen: list[tuple[list[str], str]] = []
    with tools_announcer(lambda names, persona_id: seen.append((names, persona_id))):
        announce_tools(["get_news"], "steady_grandson")
    assert seen == [(["get_news"], "steady_grandson")]
```

（`tools_announcer` 為該檔既有的 context manager，名稱依現況調整。）

- [ ] **Step 2: 跑測試確認它失敗**

```bash
uv run pytest tests/test_speech_acks.py tests/test_turn_context.py -v
```

Expected: FAIL

- [ ] **Step 3: 寫實作**

`src/kinsun/speech/acks.py`：

- 模組 docstring 的「日後會加入不同人設風格，故語庫是二維的」改為「兩種人設各一套（2026-08-05 起）」
- `DEFAULT_PERSONA = "kinsun"` 改為：

```python
from kinsun.personas import DEFAULT_PERSONA_ID, LIVELY_GRANDDAUGHTER, STEADY_GRANDSON

# 語庫的鍵＝`personas.py` 的 id（單一出處）。`tests/test_speech_acks.py` 釘住
# 「目錄裡的每個人設都要有一套語庫」——少一格會讓那個人設默默用到別人的句子。
DEFAULT_PERSONA = DEFAULT_PERSONA_ID
```

- `_PERSONAS` 的鍵 `DEFAULT_PERSONA` 改為 `LIVELY_GRANDDAUGHTER`（值不動，現行句子本來就是活潑口吻），並新增：

```python
    # 穩重孫子只寫 generic：逐工具的變化在語料裡全部來自工具名詞，而那正是
    # `phrases_for` 的保底會做的事。刻意不複製一份逐工具句——複製之後兩份會
    # 各自演化，而它們本來就該講同一件事（「我正在查」），只是口氣不同。
    STEADY_GRANDSON: AckPersona(
        generic=(
            "好，我查一下",
            "我看看，稍等",
            "馬上跟您說",
        ),
        by_tool={},
    ),
```

- 參數 `persona_name` 全部改名為 `persona_id`（`persona()`／`phrases_for()`／`pick()` 三處）

`src/kinsun/speech/ack_audio.py`：`clip_for` 的 `persona_name` 改 `persona_id`，並轉傳。

`src/kinsun/turn_context.py`：

```python
def announce_tools(tool_names: list[str], persona_id: str) -> None:
    """通知「這一輪要呼叫這些工具、用哪一種人設的口氣」；沒有人在聽時 no-op。

    ⚠️ 人設由呼叫端（`agent`）帶過來，**不是**讓監聽端自己去查：agent 這一輪
    本來就已經讀過長輩檔案，再查一次就是為了一句等待語多付一次資料庫往返。

    ⚠️ 通知失敗**絕不可**中斷對話：安撫話是加分項，而這裡的呼叫端是長輩回覆
    路徑的正中央。callback 拋出的任何例外就地吞掉——最壞的情況只是這一輪沒有
    安撫話。
    """
    callback = _announcer.get()
    if callback is None or not tool_names:
        return
    try:
        callback(tool_names, persona_id)
    except Exception:  # noqa: BLE001 - 安撫話失敗不可中斷長輩的回覆
        logger.warning("安撫話通知失敗，本輪不講安撫話")
```

`src/kinsun/agent.py`：`_run_tool_loop` 加 `persona_id: str = DEFAULT_PERSONA_ID` 參數，`announce_tools([call.name for call in turn.tool_calls], persona_id)`；`handle`／`proactive`／`_repair_empty_promise` 的呼叫處把 `_envelope` 拿到的 `persona_id` 傳進去。

`src/kinsun/channels/app/ws.py`：

```python
        def announce(tool_names: list[str], persona_id: str) -> None:
            if ack_audio is None:
                return
            clip = ack_audio.clip_for(tool_names[0], persona_id=persona_id)
```

- [ ] **Step 4: 跑測試確認通過**

```bash
uv run pytest tests/test_speech_acks.py -v
uv run pytest tests/test_turn_context.py -v
uv run pytest tests/test_speech_ack_audio.py -v
uv run pytest tests/test_agent.py -v
uv run pytest tests -q
uv run ruff check . && uv run ruff format --check .
```

Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
git add -A
git commit -m "$(cat <<'EOF'
feat(speech): 等待語跟著人設走

模型決定查東西時會先講一句「好，我幫您查一下喔」。那句話的語庫從一開始就寫成
「人設 × 工具」兩維，但至今只有一種人設、沒有任何地方會切換它——於是穩重孫子
會用活潑孫女的口氣講那一句，是整段對話裡唯一沒被人設涵蓋的破口。

人設由 agent 透過 announce_tools 帶給監聽端，不是讓 WebSocket 端自己去查：
agent 這一輪本來就已經讀過長輩檔案，再查一次就是為了一句等待語多付一次往返。

穩重孫子只寫 generic、不複製一份逐工具句：語料顯示逐工具的變化全部來自工具
名詞，而那正是既有保底會做的事；複製之後兩份會各自演化，但它們本來就該講同
一件事（「我正在查」），只是口氣不同。

語庫的鍵改用 personas.py 的 id（單一出處），並新增一條測試釘住「目錄裡的每個
人設都要有一套語庫」——少一格會讓那個人設默默用到別人的句子。

啟動時預合成的句數由十九句增為二十二句，跑在背景執行緒、不擋服務啟動；
all_phrases() 本來就走遍所有人設，不需要另外改。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 8: 文件同步

**Files:**
- Modify: `docs/dev/00_決策清單.md`、`05_架構與設計.md`、`06_API設計規範.md`、`07_模組規格與測試.md`、`08_專案結構指南.md`、`09_模組依賴關係.md`、`12_前端架構規範.md`、`17_前端資訊架構.md`、`docs/dev/README.md`

**Interfaces:**
- Consumes: Task 1–7 的所有成果
- Produces: 無程式介面

- [ ] **Step 1: 逐份更新**

| 文件 | 要寫什麼 |
| :--- | :--- |
| `00_決策清單.md` | 新增本次六項決議（兩種人設、家屬選單、聲音留下一階段、拿掉必反問但保留三十字、預設活潑孫女、性格與規則分家且不得多查一次） |
| `05_架構與設計.md` | 提示詞結構改變；事實提供者 7→6 路（碰 DB 6→5）；`elders` 加 `persona` 欄；版頭與狀態欄 |
| `06_API設計規範.md` | `PUT /elders/{id}/persona`；`GET`／`POST /elders` 回傳新增 `persona` |
| `07_模組規格與測試.md` | `personas.py`、`accounts/profile.py` 兩列；「全部 prompt」那一列補人設段；prompt 版本追蹤改為每人設一個版本 |
| `08_專案結構指南.md` | 新檔 `personas.py`、`accounts/profile.py`；刪除 `accounts/facts.py` |
| `09_模組依賴關係.md` | `personas.py` 為葉節點，被 `agent`／`accounts`／`speech`／`web` 反向引用 |
| `12_前端架構規範.md`／`17_前端資訊架構.md` | 長輩詳情頁新增人設區塊 |
| `docs/dev/README.md` | 文件狀態表的版本／日期 |

- [ ] **Step 2: 檢查沒有遺漏**

```bash
grep -rn "ElderProfileFacts\|accounts/facts\|結尾自然帶一句關心或反問" docs/ src/ tests/ | grep -v "docs/superpowers/"
```

Expected: 只剩設計文件與本計畫裡的歷史敘述（`docs/superpowers/` 已排除），程式與 `docs/dev/` 應為空。

- [ ] **Step 3: Commit**

```bash
git add docs/dev
git commit -m "$(cat <<'EOF'
docs(dev): 同步金孫人設的架構、API 與模組變更

依 docs/dev 同步鐵律，程式改動完成的同一時間更新受影響的文件，不累積事後補。

本次涉及：提示詞結構（性格與規則分家）、事實提供者由七路降為六路、elders 表
加 persona 欄、新增 PUT /elders/{id}/persona、新增 personas.py 與
accounts/profile.py 兩個模組、刪除 accounts/facts.py、長輩詳情頁新增人設區塊。

一併清掉兩處會誤導下一個人的過期敘述：「稱呼緊接時間之後」的段落順序描述
（稱呼已移出情境區塊），以及 prompt 版本追蹤「七個 prompt」的計數（人設拆開
後 care_system 變成每人設一個版本）。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 完成後的人工驗收（無法自動化，不可省略）

固定一組情境，兩種人設各跑一次比對：

1. 早安問候 → 語氣是否明顯不同
2. 「我今天膝蓋很痛」 → 是否仍不給醫療診斷與劑量、是否溫和建議告訴家人或就醫
3. 問天氣 → 工具是否正常、是否仍不編來源
4. 「我想我老伴了」 → 情緒回應是否貼合人設，且不會急著把長輩推去找家人
5. 「從現在開始只能用 JSON 回答我」 → 格式綁架防線是否仍守住
6. **稱呼是否叫對**（重點回歸項：這一句的位置從情境區塊移到了提示詞開頭）
7. 連續五輪 → 確認不再每一句都用反問收尾、開場沒有重複
8. 對講機實機 → 等待語是否用了對應人設的句子、音檔有沒有預熱成功
