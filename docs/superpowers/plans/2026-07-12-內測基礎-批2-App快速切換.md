# 內測基礎建設 批② App 快速切換身分 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 金孫 App 同時記住長輩＋家屬兩份登入（SecureStore 雙 slot），內測模式（meta 下發）顯示「切換身分」按鈕一鍵互切，不用重掃碼重輸密碼。

**Architecture:** `lib/auth.ts` 由單一 key 改雙 slot＋「目前身分」指標，舊 key 自動搬遷；`SessionProvider` 增 `otherSession`／`switchTo`／`internalTesting`；新元件 `RoleSwitcher` 掛在長輩 talk 與家屬 home，僅內測顯示。正式版行為不變（單人只有一個 slot 有值、切換器不顯示）。

**Tech Stack:** React Native＋Expo SDK 57（expo-secure-store、expo-router）；TypeScript。

**依據 spec：** `docs/superpowers/specs/2026-07-12-內部測試基礎建設-design.md` §3.2；依賴批①的 `GET /api/v1/meta` 與 `shared/types.ts` 的 `Meta`。

## Global Constraints

- 台灣繁體中文註解；全形標點。
- `app/` 無自動化測試設施——驗證方式＝`npm run typecheck`（tsc --noEmit）＋人工驗證清單（Task 4）。
- 遵循 `app/AGENTS.md`：Expo API 以 SDK 57 文檔為準（本批僅用既有已在用的 API：SecureStore get/set/deleteItemAsync、useRouter push/replace）。
- 不新增依賴套件。

---

### Task 1: `lib/auth.ts` 雙 slot 儲存＋舊資料搬遷

**Files:**
- Modify: `app/src/lib/auth.ts`（整檔重寫）

**Interfaces:**
- Produces:
  - `type Role = "guardian" | "elder"`；`type Session = { role: Role; token: string; display_name: string }`
  - `saveSession(session: Session): Promise<void>`（寫入該身分 slot＋設為目前身分）
  - `loadActiveSession(): Promise<Session | null>`（含舊 key 搬遷）
  - `loadSessionForRole(role: Role): Promise<Session | null>`
  - `setActiveRole(role: Role): Promise<void>`
  - `clearSession(role: Role): Promise<void>`（只清該身分）

- [ ] **Step 1: 重寫 `app/src/lib/auth.ts`**

```ts
/** 登入狀態持久化：expo-secure-store 依身分各存一個 slot（內測可雙身分並存）。 */

import * as SecureStore from "expo-secure-store";

export type Role = "guardian" | "elder";

export type Session = {
  role: Role;
  token: string;
  display_name: string;
};

const SLOT_KEYS: Record<Role, string> = {
  guardian: "kinsun_session_guardian",
  elder: "kinsun_session_elder",
};
const ACTIVE_ROLE_KEY = "kinsun_active_role";
const LEGACY_KEY = "kinsun_session";

function parseSession(raw: string | null): Session | null {
  if (!raw) {
    return null;
  }
  try {
    return JSON.parse(raw) as Session;
  } catch {
    return null;
  }
}

/** 舊版單一 key 一次性搬遷到對應身分 slot；讀不懂就直接丟棄。 */
async function migrateLegacySession(): Promise<void> {
  const raw = await SecureStore.getItemAsync(LEGACY_KEY);
  if (raw === null) {
    return;
  }
  const legacy = parseSession(raw);
  if (legacy) {
    await SecureStore.setItemAsync(SLOT_KEYS[legacy.role], JSON.stringify(legacy));
    await SecureStore.setItemAsync(ACTIVE_ROLE_KEY, legacy.role);
  }
  await SecureStore.deleteItemAsync(LEGACY_KEY);
}

export async function saveSession(session: Session): Promise<void> {
  await SecureStore.setItemAsync(SLOT_KEYS[session.role], JSON.stringify(session));
  await SecureStore.setItemAsync(ACTIVE_ROLE_KEY, session.role);
}

export async function loadSessionForRole(role: Role): Promise<Session | null> {
  return parseSession(await SecureStore.getItemAsync(SLOT_KEYS[role]));
}

export async function loadActiveSession(): Promise<Session | null> {
  await migrateLegacySession();
  const active = await SecureStore.getItemAsync(ACTIVE_ROLE_KEY);
  if (active !== "guardian" && active !== "elder") {
    return null;
  }
  return loadSessionForRole(active);
}

export async function setActiveRole(role: Role): Promise<void> {
  await SecureStore.setItemAsync(ACTIVE_ROLE_KEY, role);
}

/** 只清指定身分的登入；另一身分（若有）不受影響。 */
export async function clearSession(role: Role): Promise<void> {
  await SecureStore.deleteItemAsync(SLOT_KEYS[role]);
  const active = await SecureStore.getItemAsync(ACTIVE_ROLE_KEY);
  if (active === role) {
    await SecureStore.deleteItemAsync(ACTIVE_ROLE_KEY);
  }
}
```

- [ ] **Step 2: 型別檢查（此時 SessionProvider 呼叫舊介面會報錯，屬預期，Task 2 修）**

Run: `cd app && npm run typecheck`
Expected: FAIL（SessionProvider.tsx 引用 `loadSession`／`clearSession()` 無參數）

- [ ] **Step 3: 先不 commit**（與 Task 2 同 commit，保持每個 commit 可建置）

---

### Task 2: `SessionProvider` 擴充＋`api.ts` 加 `getMeta`

**Files:**
- Modify: `app/src/lib/SessionProvider.tsx`
- Modify: `app/src/lib/api.ts`

**Interfaces:**
- Consumes: Task 1 全部函式；批① `Meta` 型別與 `GET /api/v1/meta`
- Produces: `useSession()` 回傳值新增
  `otherSession: Session | null`、`internalTesting: boolean`、`switchTo(role: Role): Promise<void>`；
  `signOut()` 語意改為「只清目前身分」；`api.ts` 新增 `getMeta(): Promise<Meta>`

- [ ] **Step 1: `api.ts`**——import 型別區加 `Meta`（`kinsun-shared/types`），re-export 併入，檔尾加：

```ts
// --- 公開 meta（內測模式下發，spec 2026-07-12） ---

export function getMeta(): Promise<Meta> {
  return request("/api/v1/meta");
}
```

- [ ] **Step 2: 重寫 `SessionProvider.tsx`**

```tsx
/**
 * 登入狀態集中管理（✅ D-45，丁-1）：Context 統一持有 session，
 * 各頁不再自行 loadSession()。持久化走 expo-secure-store（lib/auth，雙 slot）。
 * 內測（spec 2026-07-12）：另持有另一身分的 otherSession 與 internalTesting 旗標，
 * 供 RoleSwitcher 一鍵切換；正式使用下 otherSession 恆為 null、切換器不顯示。
 */

import {
  createContext,
  useCallback,
  useContext,
  useEffect,
  useMemo,
  useState,
  type ReactNode,
} from "react";

import { getMeta } from "@/lib/api";
import {
  clearSession,
  loadActiveSession,
  loadSessionForRole,
  saveSession,
  setActiveRole,
  type Role,
  type Session,
} from "@/lib/auth";

function otherRoleOf(role: Role): Role {
  return role === "guardian" ? "elder" : "guardian";
}

type SessionContextValue = {
  /** 首次載入 SecureStore 前為 true；期間請顯示載入畫面、勿導頁。 */
  loading: boolean;
  session: Session | null;
  /** 另一身分已存的登入（內測切換器用）。 */
  otherSession: Session | null;
  /** 內測模式（GET /api/v1/meta 下發；取不到一律視為 false）。 */
  internalTesting: boolean;
  signIn: (session: Session) => Promise<void>;
  /** 只登出目前身分；另一身分 slot 保留。 */
  signOut: () => Promise<void>;
  /** 切到另一身分（該身分需已有登入；無登入時為 no-op）。 */
  switchTo: (role: Role) => Promise<void>;
};

const SessionContext = createContext<SessionContextValue | null>(null);

export function SessionProvider(props: { children: ReactNode }) {
  const [loading, setLoading] = useState(true);
  const [session, setSession] = useState<Session | null>(null);
  const [otherSession, setOtherSession] = useState<Session | null>(null);
  const [internalTesting, setInternalTesting] = useState(false);

  useEffect(() => {
    let alive = true;
    (async () => {
      const stored = await loadActiveSession();
      const other = stored ? await loadSessionForRole(otherRoleOf(stored.role)) : null;
      if (alive) {
        setSession(stored);
        setOtherSession(other);
        setLoading(false);
      }
    })();
    getMeta()
      .then((meta) => {
        if (alive) {
          setInternalTesting(meta.internal_testing);
        }
      })
      .catch(() => undefined); // 取不到＝正式行為：內測功能一律不顯示
    return () => {
      alive = false;
    };
  }, []);

  const signIn = useCallback(async (next: Session) => {
    await saveSession(next);
    setSession(next);
    setOtherSession(await loadSessionForRole(otherRoleOf(next.role)));
  }, []);

  const signOut = useCallback(async () => {
    if (session) {
      await clearSession(session.role);
    }
    setSession(null);
    setOtherSession(null);
  }, [session]);

  const switchTo = useCallback(async (role: Role) => {
    const target = await loadSessionForRole(role);
    if (!target) {
      return;
    }
    await setActiveRole(role);
    setSession(target);
    setOtherSession(await loadSessionForRole(otherRoleOf(role)));
  }, []);

  const value = useMemo(
    () => ({ loading, session, otherSession, internalTesting, signIn, signOut, switchTo }),
    [loading, session, otherSession, internalTesting, signIn, signOut, switchTo],
  );
  return <SessionContext.Provider value={value}>{props.children}</SessionContext.Provider>;
}

export function useSession(): SessionContextValue {
  const value = useContext(SessionContext);
  if (value === null) {
    throw new Error("useSession 必須在 SessionProvider 之內使用（見 app/_layout.tsx）");
  }
  return value;
}
```

- [ ] **Step 3: 型別檢查通過**

Run: `cd app && npm run typecheck`
Expected: PASS（各畫面呼叫 signIn／signOut 介面不變）

- [ ] **Step 4: Commit**

```bash
git add app/src/lib/auth.ts app/src/lib/SessionProvider.tsx app/src/lib/api.ts
git commit -m "feat(app): 登入狀態改雙 slot＋內測模式下發（快速切換地基）"
```

---

### Task 3: `RoleSwitcher` 元件＋掛載兩端

**Files:**
- Create: `app/src/components/RoleSwitcher.tsx`
- Modify: `app/src/app/guardian/home.tsx`（footer 掛載）
- Modify: `app/src/app/elder/talk.tsx`（頂部掛載）

**Interfaces:**
- Consumes: `useSession().{session, otherSession, internalTesting, switchTo}`（Task 2）

- [ ] **Step 1: 新元件 `app/src/components/RoleSwitcher.tsx`**

```tsx
import { useRouter } from "expo-router";
import { Pressable, StyleSheet, Text } from "react-native";

import { useSession } from "@/lib/SessionProvider";
import { colors, spacing } from "@/lib/theme";

/**
 * 內測專用（spec 2026-07-12 §3.2）：一鍵切換長輩／家屬身分。
 * 僅在伺服器 INTERNAL_TESTING_ENABLED（meta 下發）時顯示；正式環境不出現。
 * 對方身分已登入→直接切換；未登入→導向該端登入／綁定頁。
 */
export function RoleSwitcher() {
  const router = useRouter();
  const { session, otherSession, internalTesting, switchTo } = useSession();
  if (!internalTesting || !session) {
    return null;
  }
  const target = session.role === "guardian" ? "elder" : "guardian";
  const label = target === "elder" ? "切換到長輩端（內測）" : "切換到家屬端（內測）";

  async function onPress() {
    if (otherSession) {
      await switchTo(target);
      router.replace(target === "elder" ? "/elder/talk" : "/guardian/home");
    } else {
      router.push(target === "elder" ? "/elder/bind" : "/guardian/login");
    }
  }

  return (
    <Pressable accessibilityRole="button" onPress={onPress} style={styles.button}>
      <Text style={styles.label}>{label}</Text>
    </Pressable>
  );
}

const styles = StyleSheet.create({
  button: {
    alignSelf: "center",
    paddingVertical: spacing.s,
    paddingHorizontal: spacing.m,
    borderRadius: 12,
    borderWidth: 1,
    borderColor: colors.border,
    backgroundColor: colors.surface,
  },
  label: { fontSize: 14, color: colors.textSoft, fontWeight: "600" },
});
```

- [ ] **Step 2: `guardian/home.tsx`**——import `RoleSwitcher`；`ListFooterComponent` 改為：

```tsx
        ListFooterComponent={
          <View style={styles.footer}>
            <Button label="登出" variant="outline" onPress={logout} />
            <RoleSwitcher />
          </View>
        }
```

`styles.footer` 改為 `footer: { marginTop: spacing.l, gap: spacing.m },`。

- [ ] **Step 3: `elder/talk.tsx`**——import `RoleSwitcher`；`<SafeAreaView>` 第一個子元素
  （`avatarZone` 之前）加 `<RoleSwitcher />`（元件自帶置中與內測隱藏，不影響長輩正式版面）。

- [ ] **Step 4: 型別檢查**

Run: `cd app && npm run typecheck`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add app/src/components/RoleSwitcher.tsx app/src/app/guardian/home.tsx app/src/app/elder/talk.tsx
git commit -m "feat(app): 內測切換身分按鈕（長輩端＋家屬端）"
```

---

### Task 4: 人工驗證清單（一人一機流程）

前置：後端 `.env` 設 `INTERNAL_TESTING_ENABLED=true` 並重啟；App 以 Expo Go 連開發伺服器。

- [ ] 家屬註冊→建長輩檔→畫面顯示綁定碼。
- [ ] 家屬 home 出現「切換到長輩端（內測）」→點擊→導向綁定頁→手動輸入綁定碼→進入 talk。
- [ ] talk 頂部出現「切換到家屬端（內測）」→點擊→直接回家屬 home（不需重新登入）。
- [ ] 再切回長輩端→直接進 talk（不需重掃碼）。
- [ ] 家屬登出→回選身分頁→再點「我是家屬」登入→長輩 slot 仍在（切換器可直接切）。
- [ ] 後端改 `INTERNAL_TESTING_ENABLED=false` 重啟→兩端切換按鈕都消失，其餘行為如常。
- [ ] （舊資料搬遷）若手機上有舊版單一 key 登入，升級後首次開啟仍維持原登入身分。
