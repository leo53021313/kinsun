# 通道綁定資料層（階段 1A）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 建立通道中立身分層的資料地基——`channel_bindings` 表＋store 三件套＋既有 LINE 綁定回填與雙寫，行為不變、可獨立交付。

**Architecture:** 擴張—收縮（expand–contract）遷移的「擴張」步：新增 `(channel, external_id) → (principal_type, principal_id)` 對應表，寫入端雙寫（`save_elder`／`save_guardian` 同步寫綁定）、既有資料冪等回填；讀取端暫不切換（維持讀 `elders.line_user_id`／`guardians.line_user_id`），由後續計畫 1B（會話主鍵遷移）、1C（邊界解析切換）接手。

**Tech Stack:** Python 3.12＋psycopg（既有 `Database`／`Executor` 基礎設施）、pytest（Fake 每次跑；Pg 合約以 `KINSUN_IT=1` 啟用）。

## Global Constraints

- 台灣繁體中文：文件、註解、commit 訊息一律繁中、全形標點（AGENTS.md）。
- 命名規範：`save` 為 upsert 語意；`get_*` 取單筆、`list_*_for_<維度>` 取清單；ID 欄位全稱不縮寫；時間戳 `<動詞過去分詞>_at`、`DOUBLE PRECISION`（epoch 秒）（AGENTS.md）。
- 持久層三件套：Protocol＋`Pg…`＋`Fake…` 同住 `store.py`；合約測試同一份斷言參數化跑兩個 adapter（AGENTS.md）。
- DDL 皆冪等（`IF NOT EXISTS`／`ON CONFLICT DO NOTHING`），集中於 `src/kinsun/db.py`，由 `ensure_schema` 依序執行；無 migration 工具。
- 不新增第三方套件、不新增環境變數。
- 每個 Task 結束時全部測試綠燈才 commit；commit 訊息遵循 feat／fix／docs／refactor／test／chore。
- 設計源頭：`docs/superpowers/specs/2026-07-07-app通道與身分中立化-design.md` §4.1。

---

### Task 1: ChannelBinding 領域模型

**Files:**
- Modify: `src/kinsun/accounts/models.py`（檔尾新增）
- Test: `tests/test_accounts_models.py`（檔尾新增）

**Interfaces:**
- Consumes: 既有 `StrEnum`／`dataclass` 匯入（檔內已有）。
- Produces: `Channel`（StrEnum：`LINE="line"`、`APP="app"`）、`PrincipalType`（StrEnum：`ELDER="elder"`、`GUARDIAN="guardian"`）、`ChannelBinding(channel, external_id, principal_type, principal_id, created_at)`（frozen dataclass）。後續 Task 2–4 與計畫 1B/1C 都依賴這三個名字。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_accounts_models.py` 檔尾新增：

```python
def test_channel_binding_model():
    binding = ChannelBinding(
        channel=Channel.LINE,
        external_id="U123",
        principal_type=PrincipalType.ELDER,
        principal_id="e1",
        created_at=1000.0,
    )
    assert binding.channel == Channel.LINE
    assert binding.channel.value == "line"
    assert Channel.APP.value == "app"
    assert binding.principal_type == PrincipalType.ELDER
    assert PrincipalType.GUARDIAN.value == "guardian"
    assert binding.principal_id == "e1"
    assert binding.created_at == 1000.0
```

並把該檔既有的 `from kinsun.accounts.models import (…)` 匯入清單補上 `Channel`、`ChannelBinding`、`PrincipalType`（依字母序插入）。

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_accounts_models.py -v`
Expected: FAIL，`ImportError: cannot import name 'Channel'`

- [ ] **Step 3: 最小實作**

在 `src/kinsun/accounts/models.py` 檔尾新增：

```python
class Channel(StrEnum):
    LINE = "line"
    APP = "app"


class PrincipalType(StrEnum):
    ELDER = "elder"
    GUARDIAN = "guardian"


@dataclass(frozen=True)
class ChannelBinding:
    """通道帳號 → 本人（長輩／家屬）的對應：一人可同時有多通道綁定。"""

    channel: Channel
    external_id: str
    principal_type: PrincipalType
    principal_id: str
    created_at: float
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_accounts_models.py -v`
Expected: PASS（全檔）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/accounts/models.py tests/test_accounts_models.py
git commit -m "feat: 新增 ChannelBinding 領域模型（通道帳號對應本人）"
```

---

### Task 2: channel_bindings 表 DDL 與 store 三件套方法

**Files:**
- Modify: `src/kinsun/db.py`（`ACCOUNTS_DDL` 之後新增常數；`ensure_schema` 掛上）
- Modify: `src/kinsun/accounts/store.py`（Protocol＋Pg＋Fake 各加三個方法）
- Test: `tests/test_accounts_store_contract.py`（檔尾新增兩個合約測試）

**Interfaces:**
- Consumes: Task 1 的 `Channel`、`ChannelBinding`、`PrincipalType`；既有 `Executor`、`_Errors`。
- Produces（AccountStore Protocol 新增，簽名一字不差）:
  - `save_channel_binding(self, binding: ChannelBinding, *, tx: Executor | None = None) -> None`（upsert；衝突時更新 principal，**不覆寫 `created_at`**——保留首次綁定時間）
  - `get_channel_binding(self, channel: Channel, external_id: str) -> ChannelBinding | None`
  - `list_channel_bindings_for_principal(self, principal_type: PrincipalType, principal_id: str) -> list[ChannelBinding]`（依 `channel, external_id` 排序）

- [ ] **Step 1: 寫失敗合約測試**

在 `tests/test_accounts_store_contract.py` 檔尾新增，並把檔頭匯入清單補上 `Channel`、`ChannelBinding`、`PrincipalType`：

```python
def test_channel_binding_roundtrip_and_upsert_keeps_created_at(store, ns):
    store.save_channel_binding(
        ChannelBinding(Channel.LINE, f"{ns}U1", PrincipalType.ELDER, f"{ns}e1", 1000.0)
    )
    got = store.get_channel_binding(Channel.LINE, f"{ns}U1")
    assert got.principal_type == PrincipalType.ELDER
    assert got.principal_id == f"{ns}e1"
    assert got.created_at == 1000.0
    # upsert：改指到別的本人，但 created_at 保留首次綁定時間。
    store.save_channel_binding(
        ChannelBinding(Channel.LINE, f"{ns}U1", PrincipalType.GUARDIAN, f"{ns}g1", 2000.0)
    )
    got = store.get_channel_binding(Channel.LINE, f"{ns}U1")
    assert got.principal_type == PrincipalType.GUARDIAN
    assert got.principal_id == f"{ns}g1"
    assert got.created_at == 1000.0
    assert store.get_channel_binding(Channel.LINE, f"{ns}nope") is None


def test_channel_bindings_listed_by_principal(store, ns):
    # 同一位長輩兩個通道綁定；另一人的綁定不得混入。
    store.save_channel_binding(
        ChannelBinding(Channel.LINE, f"{ns}U-line", PrincipalType.ELDER, f"{ns}e1", 1000.0)
    )
    store.save_channel_binding(
        ChannelBinding(Channel.APP, f"{ns}dev-1", PrincipalType.ELDER, f"{ns}e1", 1100.0)
    )
    store.save_channel_binding(
        ChannelBinding(Channel.LINE, f"{ns}U-other", PrincipalType.ELDER, f"{ns}e2", 1200.0)
    )
    got = store.list_channel_bindings_for_principal(PrincipalType.ELDER, f"{ns}e1")
    assert [(b.channel, b.external_id) for b in got] == [
        (Channel.APP, f"{ns}dev-1"),
        (Channel.LINE, f"{ns}U-line"),
    ]
    assert store.list_channel_bindings_for_principal(PrincipalType.GUARDIAN, f"{ns}e1") == []
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_accounts_store_contract.py -v -k channel_binding`
Expected: FAIL，`AttributeError: 'FakeAccountStore' object has no attribute 'save_channel_binding'`（pg 參數化在未設 `KINSUN_IT=1` 時 skip，屬預期）

- [ ] **Step 3: 實作 DDL 與三件套**

`src/kinsun/db.py`——在 `ACCOUNTS_DDL` 定義之後新增：

```python
CHANNEL_BINDINGS_DDL = (
    "CREATE TABLE IF NOT EXISTS channel_bindings ("
    "channel TEXT NOT NULL, external_id TEXT NOT NULL, "
    "principal_type TEXT NOT NULL, principal_id TEXT NOT NULL, "
    "created_at DOUBLE PRECISION NOT NULL, "
    "PRIMARY KEY (channel, external_id));"
    "CREATE INDEX IF NOT EXISTS idx_channel_bindings_principal "
    "ON channel_bindings (principal_type, principal_id);"
)
```

`ensure_schema` 內在 `conn.execute(ACCOUNTS_DDL)` 之後加一行：

```python
        conn.execute(CHANNEL_BINDINGS_DDL)
```

`src/kinsun/accounts/store.py`——檔頭匯入補 `Channel`、`ChannelBinding`、`PrincipalType`；`AccountStore` Protocol 在 `save_invite`／`get_invite` 之後、`transaction` 之前插入：

```python
    def save_channel_binding(
        self, binding: ChannelBinding, *, tx: Executor | None = None
    ) -> None: ...
    def get_channel_binding(self, channel: Channel, external_id: str) -> ChannelBinding | None: ...
    def list_channel_bindings_for_principal(
        self, principal_type: PrincipalType, principal_id: str
    ) -> list[ChannelBinding]: ...
```

`PgAccountStore` 在 `get_invite` 之後新增：

```python
    def save_channel_binding(self, binding: ChannelBinding, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO channel_bindings "
            "(channel, external_id, principal_type, principal_id, created_at) "
            "VALUES (%s, %s, %s, %s, %s) ON CONFLICT (channel, external_id) DO UPDATE SET "
            "principal_type = EXCLUDED.principal_type, principal_id = EXCLUDED.principal_id",
            (
                binding.channel.value,
                binding.external_id,
                binding.principal_type.value,
                binding.principal_id,
                binding.created_at,
            ),
        )

    def get_channel_binding(self, channel: Channel, external_id: str) -> ChannelBinding | None:
        rows = self._db.query(
            "SELECT channel, external_id, principal_type, principal_id, created_at "
            "FROM channel_bindings WHERE channel = %s AND external_id = %s",
            (channel.value, external_id),
        )
        if not rows:
            return None
        ch, ext, ptype, pid, created = rows[0]
        return ChannelBinding(Channel(ch), ext, PrincipalType(ptype), pid, created)

    def list_channel_bindings_for_principal(
        self, principal_type: PrincipalType, principal_id: str
    ) -> list[ChannelBinding]:
        rows = self._db.query(
            "SELECT channel, external_id, principal_type, principal_id, created_at "
            "FROM channel_bindings WHERE principal_type = %s AND principal_id = %s "
            "ORDER BY channel, external_id",
            (principal_type.value, principal_id),
        )
        return [ChannelBinding(Channel(c), e, PrincipalType(p), i, t) for (c, e, p, i, t) in rows]
```

`FakeAccountStore`——`__init__` 增加一個 dict，並在 `get_invite` 之後新增三個方法：

```python
        self.channel_bindings: dict[tuple[Channel, str], ChannelBinding] = {}
```

```python
    def save_channel_binding(self, binding: ChannelBinding, *, tx: Executor | None = None) -> None:
        key = (binding.channel, binding.external_id)
        existing = self.channel_bindings.get(key)
        if existing is not None:
            # 與 Pg 一致：upsert 不覆寫 created_at（保留首次綁定時間）。
            binding = ChannelBinding(
                binding.channel,
                binding.external_id,
                binding.principal_type,
                binding.principal_id,
                existing.created_at,
            )
        self.channel_bindings[key] = binding

    def get_channel_binding(self, channel: Channel, external_id: str) -> ChannelBinding | None:
        return self.channel_bindings.get((channel, external_id))

    def list_channel_bindings_for_principal(
        self, principal_type: PrincipalType, principal_id: str
    ) -> list[ChannelBinding]:
        rows = [
            b
            for b in self.channel_bindings.values()
            if b.principal_type == principal_type and b.principal_id == principal_id
        ]
        return sorted(rows, key=lambda b: (b.channel.value, b.external_id))
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_accounts_store_contract.py tests/test_db.py -v`
Expected: PASS（pg 參數化 skip 屬預期；若本機有 `KINSUN_IT=1`＋`DATABASE_URL` 則一併驗證真庫建表）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/db.py src/kinsun/accounts/store.py tests/test_accounts_store_contract.py
git commit -m "feat: 新增 channel_bindings 表與 AccountStore 綁定三方法（含合約測試）"
```

---

### Task 3: save_elder／save_guardian 通道綁定雙寫

**Files:**
- Modify: `src/kinsun/accounts/store.py`（`PgAccountStore.save_elder`／`save_guardian`、`FakeAccountStore.save_elder`／`save_guardian`）
- Test: `tests/test_accounts_store_contract.py`（檔尾新增）

**Interfaces:**
- Consumes: Task 2 的 `save_channel_binding`。
- Produces: 行為合約——`save_elder(elder)` 當 `elder.line_user_id` 非空時，同步 upsert 一筆 `ChannelBinding(Channel.LINE, elder.line_user_id, PrincipalType.ELDER, elder.elder_id, …)`；`save_guardian` 同理（`PrincipalType.GUARDIAN`）。`line_user_id` 為 `None` 或空字串時不寫綁定。呼叫端（`AccountService`、綁定流程）不需任何修改即自動雙寫。

- [ ] **Step 1: 寫失敗合約測試**

在 `tests/test_accounts_store_contract.py` 檔尾新增：

```python
def test_save_elder_with_line_writes_channel_binding(store, ns):
    store.save_elder(Elder(f"{ns}e1", "阿公", f"{ns}U-elder"))
    binding = store.get_channel_binding(Channel.LINE, f"{ns}U-elder")
    assert binding is not None
    assert binding.principal_type == PrincipalType.ELDER
    assert binding.principal_id == f"{ns}e1"


def test_save_elder_without_line_writes_no_binding(store, ns):
    store.save_elder(Elder(f"{ns}e-none", "阿嬤", None))
    assert store.list_channel_bindings_for_principal(PrincipalType.ELDER, f"{ns}e-none") == []


def test_save_guardian_writes_channel_binding(store, ns):
    store.save_guardian(Guardian(f"{ns}g1", f"{ns}U-guard", "女兒"))
    binding = store.get_channel_binding(Channel.LINE, f"{ns}U-guard")
    assert binding is not None
    assert binding.principal_type == PrincipalType.GUARDIAN
    assert binding.principal_id == f"{ns}g1"
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_accounts_store_contract.py -v -k "writes_channel_binding or writes_no_binding"`
Expected: FAIL，`assert binding is not None` 不成立（雙寫尚未實作）

- [ ] **Step 3: 實作雙寫**

`src/kinsun/accounts/store.py`——檔頭補 `import time`。`PgAccountStore.save_elder` 改為（原 upsert 保留，之後追加雙寫）：

```python
    def save_elder(self, elder: Elder, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO elders (elder_id, name, line_user_id) VALUES (%s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET "
            "name = EXCLUDED.name, line_user_id = EXCLUDED.line_user_id",
            (elder.elder_id, elder.name, elder.line_user_id),
        )
        # 擴張—收縮遷移的雙寫：欄位與 channel_bindings 同步，讀取端切換前兩邊恆一致。
        if elder.line_user_id:
            self.save_channel_binding(
                ChannelBinding(
                    Channel.LINE,
                    elder.line_user_id,
                    PrincipalType.ELDER,
                    elder.elder_id,
                    time.time(),
                ),
                tx=tx,
            )
```

`PgAccountStore.save_guardian` 同樣在原 upsert 之後追加：

```python
        if guardian.line_user_id:
            self.save_channel_binding(
                ChannelBinding(
                    Channel.LINE,
                    guardian.line_user_id,
                    PrincipalType.GUARDIAN,
                    guardian.guardian_id,
                    time.time(),
                ),
                tx=tx,
            )
```

`FakeAccountStore.save_elder`／`save_guardian` 加上完全相同的兩段 `if …: self.save_channel_binding(…)`（Fake 的 `save_channel_binding` 已忽略 `tx`）。

- [ ] **Step 4: 跑測試確認通過（全套件）**

Run: `uv run pytest -q`
Expected: 全數 PASS（pg 參數化 skip 屬預期）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/accounts/store.py tests/test_accounts_store_contract.py
git commit -m "feat: save_elder／save_guardian 雙寫 channel_bindings（擴張—收縮遷移的擴張步）"
```

---

### Task 4: 既有 LINE 綁定回填遷移（冪等）

**Files:**
- Modify: `src/kinsun/db.py`（新增回填 DDL；`ensure_schema` 於尾端執行）
- Test: `tests/test_pg_db.py`（檔尾新增；`KINSUN_IT=1` 才跑）

**Interfaces:**
- Consumes: Task 2 的 `channel_bindings` 表。
- Produces: `CHANNEL_BINDINGS_BACKFILL_DDL` 常數；部署後既有 `elders.line_user_id`／`guardians.line_user_id` 全數存在對應綁定列。已知邊界：同一 `line_user_id` 同時掛在長輩與家屬（髒資料）時，長輩優先（長輩回填先執行、`ON CONFLICT DO NOTHING`）；同一 `line_user_id` 掛多位長輩時，`elder_id` 排序最小者優先（確定性）。

- [ ] **Step 1: 寫失敗整合測試**

在 `tests/test_pg_db.py` 檔尾新增（沿用該檔既有匯入；若缺則補 `import os` 與 `from kinsun.db import ensure_schema`）：

```python
def test_channel_bindings_backfill_idempotent(pg_database, ns):
    # 模擬「雙寫上線前」的舊資料：直接寫欄位、清掉綁定列。
    pg_database.execute(
        "INSERT INTO elders (elder_id, name, line_user_id) VALUES (%s, %s, %s) "
        "ON CONFLICT (elder_id) DO UPDATE SET line_user_id = EXCLUDED.line_user_id",
        (f"{ns}e-bf", "阿公", f"{ns}U-bf"),
    )
    pg_database.execute(
        "DELETE FROM channel_bindings WHERE channel = 'line' AND external_id = %s",
        (f"{ns}U-bf",),
    )
    ensure_schema(os.environ["DATABASE_URL"])
    ensure_schema(os.environ["DATABASE_URL"])  # 冪等：跑兩次結果不變
    rows = pg_database.query(
        "SELECT principal_type, principal_id FROM channel_bindings "
        "WHERE channel = 'line' AND external_id = %s",
        (f"{ns}U-bf",),
    )
    assert rows == [("elder", f"{ns}e-bf")]
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `KINSUN_IT=1 uv run pytest tests/test_pg_db.py -v -k backfill`（需 `DATABASE_URL`；若本機無真庫，改跑 `uv run pytest tests/test_pg_db.py -v -k backfill` 確認為 skip，並於 Task 5 前補真庫驗證）
Expected: FAIL，`rows == []`（回填尚未實作）

- [ ] **Step 3: 實作回填 DDL**

`src/kinsun/db.py`——在 `RISK_EVENTS_TRACE_MIGRATION_DDL` 之後新增：

```python
# elders／guardians 既有 LINE 綁定回填 channel_bindings（冪等）：擴張—收縮遷移的回填步。
# 同一 line_user_id 髒資料的優先序：長輩先於家屬（先執行）、elder_id 排序小者先（確定性）。
CHANNEL_BINDINGS_BACKFILL_DDL = (
    "INSERT INTO channel_bindings "
    "(channel, external_id, principal_type, principal_id, created_at) "
    "SELECT 'line', line_user_id, 'elder', elder_id, EXTRACT(EPOCH FROM now()) "
    "FROM elders WHERE line_user_id IS NOT NULL AND line_user_id <> '' "
    "ORDER BY elder_id "
    "ON CONFLICT (channel, external_id) DO NOTHING;"
    "INSERT INTO channel_bindings "
    "(channel, external_id, principal_type, principal_id, created_at) "
    "SELECT 'line', line_user_id, 'guardian', guardian_id, EXTRACT(EPOCH FROM now()) "
    "FROM guardians WHERE line_user_id IS NOT NULL AND line_user_id <> '' "
    "ORDER BY guardian_id "
    "ON CONFLICT (channel, external_id) DO NOTHING;"
)
```

`ensure_schema` 內在 `conn.execute(RISK_EVENTS_TRACE_MIGRATION_DDL)` 之後加一行：

```python
        conn.execute(CHANNEL_BINDINGS_BACKFILL_DDL)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `KINSUN_IT=1 uv run pytest tests/test_pg_db.py tests/test_accounts_store_contract.py -v`（無真庫則先跑 `uv run pytest -q` 確認離線全綠）
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/db.py tests/test_pg_db.py
git commit -m "feat: ensure_schema 冪等回填既有 LINE 綁定至 channel_bindings"
```

---

### Task 5: 文件同步

**Files:**
- Modify: `docs/mvp/06_DB_Schema.md`（§1 總覽表帳號列、§2.2 帳號綁定新增逐表定義）
- Modify: `CONTEXT.md`（「通道與訊息」節補「通道綁定」語彙）

**Interfaces:**
- Consumes: Task 2／4 落地的表結構。
- Produces: 文件與 `db.py` DDL 一致（該文件性質即「逐欄反向抄錄」）。

- [ ] **Step 1: 更新 06_DB_Schema.md**

§1 總覽表「帳號」列的表清單改為 `elders`、`guardians`、`elder_guardians`、`consents`、`invites`、`channel_bindings`；§2.2「帳號綁定」小節檔尾新增（格式比照同節其他表）：

```markdown
**`channel_bindings`**（db.py CHANNEL_BINDINGS_DDL）
| 欄位 | 型別 | 說明 |
|------|------|------|
| channel | TEXT PK(1/2) | 通道：line／app |
| external_id | TEXT PK(2/2) | 通道側帳號識別（LINE userId／App 裝置帳號） |
| principal_type | TEXT | 本人類型：elder／guardian |
| principal_id | TEXT | elder_id 或 guardian_id |
| created_at | DOUBLE PRECISION | 首次綁定時間（upsert 不覆寫） |

索引：`idx_channel_bindings_principal (principal_type, principal_id)`。
遷移：`CHANNEL_BINDINGS_BACKFILL_DDL` 冪等回填 `elders`／`guardians` 的既有 `line_user_id`；
寫入端由 `save_elder`／`save_guardian` 雙寫（擴張—收縮，讀取端切換見階段 1C 計畫）。
```

- [ ] **Step 2: 更新 CONTEXT.md**

「通道與訊息」節的「出站通道（OutboundChannel）」段落之後新增：

```markdown
**通道綁定（ChannelBinding）**：
通道帳號對應本人的持久對應（`channel_bindings` 表）：`(channel, external_id) → (principal_type, principal_id)`。一人可同時有 LINE 與 App 綁定；換手機＝改一筆綁定，記憶不動。目前為擴張期——寫入端雙寫、讀取端仍走 `elders.line_user_id`／`guardians.line_user_id`，切換由階段 1B／1C 計畫接手。
```

- [ ] **Step 3: 全套件驗證**

Run: `uv run pytest -q && uv run ruff check src tests`
Expected: 測試全 PASS、ruff 無違規

- [ ] **Step 4: Commit**

```bash
git add docs/mvp/06_DB_Schema.md CONTEXT.md
git commit -m "docs: 同步 channel_bindings 表至 DB Schema 文件與領域語彙"
```

---

## 後續計畫（不在本計畫範圍）

- **1B 會話主鍵遷移**：`turns`／`conversation_summaries`／`risk_events`／Mem0 鍵由 `line_user_id` 遷為 `elder_id`，含資料回填與管線簽名改造。
- **1C 邊界解析切換**：`InboundMessage` 改帶 `(channel, external_id)`、`dispatch` 入口解析本人、`OutboundChannel` 收件人中立化、讀取端（`get_elder_by_line` 等）切換至 `channel_bindings`、欄位收縮退役。
