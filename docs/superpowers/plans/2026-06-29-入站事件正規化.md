# 入站事件正規化（InboundMessage + LineChannel）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（建議）或 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 把 webhook 裡用 `getattr` 探測 LINE SDK 事件形狀的分派邏輯，改成「LINE 轉接器正規化成通道中立的 `InboundMessage` → 中立 dispatch 分派」，讓 LINE SDK 型別止於通道接縫、第二通道只需再寫一個轉接器。

**Architecture:** 新增 `channels/inbound.py`：通道中立的 `InboundMessage(session_id, kind, text, audio, reply)`（`reply` 為綁定好的可呼叫 handle）與 `dispatch(msg, *, pipeline, binding, gate)`。新增 `channels/line/channel.py` 的 `LineChannel`：把 LINE 事件正規化成 `InboundMessage`，正規化時即抓好 audio bytes、並把 `reply` 綁到 `messenger.reply_text(reply_token, ...)`。`webhook.py` 內部建 `LineChannel`，路由對每個事件呼叫 `channel.inbound` → `dispatch`，全程在每事件 try 內（維持單一事件失敗不回 500）。

**Tech Stack:** Python 3.12、FastAPI、line-bot-sdk v3、pytest。**無新依賴。**

## Global Constraints

- src layout（`src/kinsun/`），pytest `pythonpath=["src","."]`。
- 單一事件失敗不可讓 webhook 回 500（LINE 會重送整包 → 重複跑管線、重複發危急通知）。
- `create_app(*, parser, pipeline, messenger, binding, gate, on_shutdown=None)` 對外簽名維持（既有 `test_webhook.py` 不需改、須保持綠燈）。
- 行為差異（明列）：audio bytes 於正規化時即抓取（eager），故 (a) 未同意者也會多一次被丟棄的 `get_audio`；(b) `get_audio` 失敗由每事件 try 記錄、不回 fallback 文字（原本在管線 try 內會回 fallback）。皆為通道中立化的可接受取捨。
- ruff `E,W,F,I,B,UP`、line-length 100、quote-style double。commit 結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不自動 push；只在 Leo 分支工作。

---

## File Structure

```
src/kinsun/channels/inbound.py        # 新增：InboundMessage + dispatch + 三個 prompt 常數
src/kinsun/channels/line/channel.py   # 新增：LineChannel（事件→InboundMessage）
src/kinsun/channels/line/webhook.py   # 改：建 LineChannel、每事件 inbound→dispatch、re-export prompts
tests/test_inbound.py                 # 新增：dispatch 單元測試（純 InboundMessage）
tests/test_line_channel.py            # 新增：LineChannel 正規化測試
tests/test_webhook.py                 # 既有，保持綠燈（不改）
```

---

## Task 1：InboundMessage + 中立 dispatch

**Files:**
- Create: `src/kinsun/channels/inbound.py`
- Test: `tests/test_inbound.py`

**Interfaces:**
- Consumes：`ASRError`（speech.asr）、`LLMError`（llm）、`MemoryError`（memory.store）
- Produces：
  - `InboundMessage(session_id: str, kind: str, text: str, audio: bytes, reply: Callable[[str], None])`（frozen dataclass；`kind` ∈ `"text"`/`"audio"`/`"other"`）
  - `dispatch(msg: InboundMessage, *, pipeline, binding, gate) -> None`
  - `NON_AUDIO_PROMPT`、`FALLBACK_PROMPT`、`BIND_FIRST_PROMPT`（str 常數）

- [ ] **Step 1：失敗測試** Create `tests/test_inbound.py`：
```python
from types import SimpleNamespace

from kinsun.channels.inbound import (
    BIND_FIRST_PROMPT,
    FALLBACK_PROMPT,
    NON_AUDIO_PROMPT,
    InboundMessage,
    dispatch,
)
from kinsun.speech.asr import ASRError


class _Replies:
    def __init__(self):
        self.sent = []

    def __call__(self, text):
        self.sent.append(text)


class _Pipeline:
    def __init__(self, text="管線回覆", boom=None):
        self._text = text
        self._boom = boom
        self.calls = []

    def process(self, audio, *, session_id):
        self.calls.append((audio, session_id))
        if self._boom is not None:
            raise self._boom
        return SimpleNamespace(text=self._text)


class _Binding:
    def __init__(self, reply):
        self._reply = reply
        self.calls = []

    def handle(self, session_id, text):
        self.calls.append((session_id, text))
        return self._reply


class _Gate:
    def __init__(self, allow):
        self._allow = allow

    def allows(self, session_id):
        return self._allow


def _msg(kind, *, reply, text="", audio=b"", session_id="U-1"):
    return InboundMessage(session_id, kind, text, audio, reply)


def test_text_routes_to_binding():
    r = _Replies()
    binding = _Binding("已建立")
    dispatch(_msg("text", text="設定", reply=r), pipeline=_Pipeline(), binding=binding, gate=_Gate(True))
    assert binding.calls == [("U-1", "設定")]
    assert r.sent == ["已建立"]


def test_text_none_falls_back_to_prompt():
    r = _Replies()
    dispatch(_msg("text", text="閒聊", reply=r), pipeline=_Pipeline(), binding=_Binding(None), gate=_Gate(True))
    assert r.sent == [NON_AUDIO_PROMPT]


def test_other_kind_replies_prompt():
    r = _Replies()
    dispatch(_msg("other", reply=r), pipeline=_Pipeline(), binding=_Binding(None), gate=_Gate(True))
    assert r.sent == [NON_AUDIO_PROMPT]


def test_audio_blocked_when_gate_denies():
    r = _Replies()
    pipe = _Pipeline()
    dispatch(_msg("audio", audio=b"x", reply=r), pipeline=pipe, binding=_Binding(None), gate=_Gate(False))
    assert r.sent == [BIND_FIRST_PROMPT]
    assert pipe.calls == []


def test_audio_runs_pipeline_when_allowed():
    r = _Replies()
    pipe = _Pipeline(text="你說的是：早安")
    dispatch(_msg("audio", audio=b"xy", reply=r), pipeline=pipe, binding=_Binding(None), gate=_Gate(True))
    assert pipe.calls == [(b"xy", "U-1")]
    assert r.sent == ["你說的是：早安"]


def test_audio_pipeline_error_replies_fallback():
    r = _Replies()
    dispatch(
        _msg("audio", audio=b"x", reply=r),
        pipeline=_Pipeline(boom=ASRError("boom")),
        binding=_Binding(None),
        gate=_Gate(True),
    )
    assert r.sent == [FALLBACK_PROMPT]
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_inbound.py -q`
  Expected：FAIL（`ModuleNotFoundError: kinsun.channels.inbound`）。

- [ ] **Step 3：實作** Create `src/kinsun/channels/inbound.py`：
```python
"""通道中立的入站訊息與分派（不依賴任何特定通道 SDK）。"""

from __future__ import annotations

import logging
from collections.abc import Callable
from dataclasses import dataclass

from kinsun.llm import LLMError
from kinsun.memory.store import MemoryError
from kinsun.speech.asr import ASRError

logger = logging.getLogger("kinsun.inbound")

NON_AUDIO_PROMPT = "金孫現在聽得懂語音喔，您可以按住麥克風跟我說說話。"
FALLBACK_PROMPT = "金孫剛剛沒聽清楚，您可以再說一次嗎？"
BIND_FIRST_PROMPT = (
    "金孫需要先完成綁定才能陪您聊天喔。請把家人給您的邀請碼貼到這裡，或回覆「設定」開始。"
)


@dataclass(frozen=True)
class InboundMessage:
    """通道中立的入站訊息。kind ∈ text/audio/other；reply 為綁定好的回覆 handle。"""

    session_id: str
    kind: str
    text: str
    audio: bytes
    reply: Callable[[str], None]


def dispatch(msg: InboundMessage, *, pipeline, binding, gate) -> None:
    if msg.kind == "text":
        reply = binding.handle(msg.session_id, msg.text)
        msg.reply(reply if reply is not None else NON_AUDIO_PROMPT)
        return
    if msg.kind != "audio":
        msg.reply(NON_AUDIO_PROMPT)
        return
    if not gate.allows(msg.session_id):
        msg.reply(BIND_FIRST_PROMPT)
        return
    try:
        result = pipeline.process(msg.audio, session_id=msg.session_id)
        msg.reply(result.text)
    except (ASRError, LLMError, MemoryError):
        msg.reply(FALLBACK_PROMPT)
```

- [ ] **Step 4：確認通過** Run：`uv run pytest tests/test_inbound.py -q` → PASS
- [ ] **Step 5：commit**
```bash
git add src/kinsun/channels/inbound.py tests/test_inbound.py
git commit -m "feat: 通道中立 InboundMessage 與 dispatch"
```

---

## Task 2：LineChannel 轉接器

**Files:**
- Create: `src/kinsun/channels/line/channel.py`
- Test: `tests/test_line_channel.py`

**Interfaces:**
- Consumes：`InboundMessage`、`LineMessenger`（`get_audio`/`reply_text`）
- Produces：`LineChannel(messenger: LineMessenger)`：`inbound(event) -> InboundMessage | None`

- [ ] **Step 1：失敗測試** Create `tests/test_line_channel.py`：
```python
from types import SimpleNamespace

from kinsun.channels.line.channel import LineChannel


class _Messenger:
    def __init__(self):
        self.replied = []
        self.audio_calls = []

    def get_audio(self, message_id):
        self.audio_calls.append(message_id)
        return b"\x00\x01"

    def reply_text(self, reply_token, text):
        self.replied.append((reply_token, text))


def _audio_event(uid="U-1"):
    return SimpleNamespace(
        reply_token="rt-1",
        message=SimpleNamespace(type="audio", id="m-1"),
        source=SimpleNamespace(user_id=uid),
    )


def _text_event():
    return SimpleNamespace(
        reply_token="rt-2",
        message=SimpleNamespace(type="text", id="m-2", text="設定"),
        source=SimpleNamespace(user_id="U-2"),
    )


def _sticker_event():
    return SimpleNamespace(
        reply_token="rt-3",
        message=SimpleNamespace(type="sticker", id="m-3"),
        source=SimpleNamespace(user_id="U-3"),
    )


def test_text_event_normalized():
    msg = LineChannel(_Messenger()).inbound(_text_event())
    assert msg.kind == "text"
    assert msg.text == "設定"
    assert msg.session_id == "U-2"
    assert msg.audio == b""


def test_audio_event_fetches_bytes():
    m = _Messenger()
    msg = LineChannel(m).inbound(_audio_event())
    assert msg.kind == "audio"
    assert msg.audio == b"\x00\x01"
    assert m.audio_calls == ["m-1"]


def test_sticker_is_other():
    assert LineChannel(_Messenger()).inbound(_sticker_event()).kind == "other"


def test_missing_token_returns_none():
    ev = SimpleNamespace(
        reply_token=None,
        message=SimpleNamespace(type="text", text="x"),
        source=SimpleNamespace(user_id="U"),
    )
    assert LineChannel(_Messenger()).inbound(ev) is None


def test_missing_user_id_is_unknown():
    assert LineChannel(_Messenger()).inbound(_audio_event(uid=None)).session_id == "unknown"


def test_reply_binds_to_messenger():
    m = _Messenger()
    msg = LineChannel(m).inbound(_text_event())
    msg.reply("哈囉")
    assert m.replied == [("rt-2", "哈囉")]
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_line_channel.py -q`
  Expected：FAIL（`ModuleNotFoundError: kinsun.channels.line.channel`）。

- [ ] **Step 3：實作** Create `src/kinsun/channels/line/channel.py`：
```python
"""LINE 通道轉接器：把 LINE webhook 事件正規化成 InboundMessage。"""

from __future__ import annotations

from kinsun.channels.inbound import InboundMessage
from kinsun.channels.line.messenger import LineMessenger


class LineChannel:
    def __init__(self, messenger: LineMessenger) -> None:
        self._messenger = messenger

    def inbound(self, event) -> InboundMessage | None:
        message = getattr(event, "message", None)
        reply_token = getattr(event, "reply_token", None)
        if message is None or reply_token is None:
            return None
        source = getattr(event, "source", None)
        session_id = getattr(source, "user_id", None) or "unknown"
        mtype = getattr(message, "type", None)

        def reply(text: str) -> None:
            self._messenger.reply_text(reply_token, text)

        if mtype == "text":
            return InboundMessage(
                session_id, "text", getattr(message, "text", "") or "", b"", reply
            )
        if mtype == "audio":
            return InboundMessage(
                session_id, "audio", "", self._messenger.get_audio(message.id), reply
            )
        return InboundMessage(session_id, "other", "", b"", reply)
```

- [ ] **Step 4：確認通過** Run：`uv run pytest tests/test_line_channel.py -q` → PASS
- [ ] **Step 5：commit**
```bash
git add src/kinsun/channels/line/channel.py tests/test_line_channel.py
git commit -m "feat: LineChannel 把 LINE 事件正規化為 InboundMessage"
```

---

## Task 3：webhook 改用 LineChannel + dispatch

**Files:**
- Modify: `src/kinsun/channels/line/webhook.py`

**Interfaces:**
- Consumes：`LineChannel`、`dispatch`、`NON_AUDIO_PROMPT`/`FALLBACK_PROMPT`/`BIND_FIRST_PROMPT`
- Produces：`create_app(*, parser, pipeline, messenger, binding, gate, on_shutdown=None) -> FastAPI`（簽名不變；prompts 由本模組 re-export 供既有測試 import）

- [ ] **Step 1：確認既有測試現綠** Run：`uv run pytest tests/test_webhook.py -q` → PASS（重構前基準）
- [ ] **Step 2：實作** 改 `src/kinsun/channels/line/webhook.py` 全檔為：
```python
"""LINE webhook：驗簽 → 正規化為 InboundMessage → 分派。"""

from __future__ import annotations

import logging
from collections.abc import Callable
from contextlib import asynccontextmanager

from fastapi import FastAPI, HTTPException, Request
from fastapi.concurrency import run_in_threadpool
from linebot.v3.exceptions import InvalidSignatureError

from kinsun.channels.inbound import (
    BIND_FIRST_PROMPT,
    FALLBACK_PROMPT,
    NON_AUDIO_PROMPT,
    dispatch,
)
from kinsun.channels.line.channel import LineChannel
from kinsun.channels.line.messenger import LineMessenger
from kinsun.pipeline import VoicePipeline

logger = logging.getLogger("kinsun.webhook")

# 供既有測試與呼叫端沿用既有 import 路徑。
__all__ = ["BIND_FIRST_PROMPT", "FALLBACK_PROMPT", "NON_AUDIO_PROMPT", "create_app"]


def _handle_events(events, *, channel: LineChannel, pipeline, binding, gate) -> None:
    for event in events:
        try:
            msg = channel.inbound(event)
            if msg is not None:
                dispatch(msg, pipeline=pipeline, binding=binding, gate=gate)
        except Exception:  # noqa: BLE001
            # 單一事件失敗不可讓 webhook 回 500：LINE 會重送整包事件，
            # 導致重複跑管線、重複發家屬危急通知。記錄後繼續下一個事件。
            logger.exception("處理 LINE 事件失敗")


def create_app(
    *,
    parser,
    pipeline: VoicePipeline,
    messenger: LineMessenger,
    binding,
    gate,
    on_shutdown: Callable[[], None] | None = None,
) -> FastAPI:
    channel = LineChannel(messenger)

    @asynccontextmanager
    async def lifespan(_app: FastAPI):
        yield
        if on_shutdown is not None:
            on_shutdown()

    app = FastAPI(lifespan=lifespan)

    @app.post("/line/webhook")
    async def line_webhook(request: Request) -> dict[str, bool]:
        body = await request.body()
        signature = request.headers.get("X-Line-Signature", "")
        try:
            events = parser.parse(body.decode("utf-8"), signature)
        except InvalidSignatureError as exc:
            raise HTTPException(status_code=400, detail="invalid signature") from exc
        # 事件處理是阻塞的重工作（ASR/LLM/HTTP），丟到 threadpool 以免卡住事件迴圈。
        await run_in_threadpool(
            _handle_events, events, channel=channel, pipeline=pipeline, binding=binding, gate=gate
        )
        return {"ok": True}

    return app
```
> 註：`on_shutdown`／`lifespan` 沿用「Postgres 存取深模組」計畫對 `create_app` 的擴充；若該計畫未套用，`on_shutdown` 預設 `None`、`lifespan` 無副作用，行為不變。

- [ ] **Step 3：確認通過** Run：`uv run pytest tests/test_webhook.py -q`、`uv run ruff check src/kinsun/channels/line/webhook.py` → PASS（既有 13 個測試全綠）、ruff 通過
- [ ] **Step 4：commit**
```bash
git add src/kinsun/channels/line/webhook.py
git commit -m "refactor: webhook 改用 LineChannel 正規化 + 中立 dispatch"
```

---

## Task 4：品質收尾

- [ ] **Step 1：全測試 + 品質** Run：`uv run pytest -q`、`uv run ruff check .`、`uv run ruff format --check .` → 全綠
- [ ] **Step 2：確認 `git status` 僅含預期變更**
- [ ] **Step 3：push + PR**（待使用者指示）

---

## Self-Review

**Spec coverage：** 通道中立領域型別（audio bytes + reply handle、不帶 LINE token）→ Task 1（`InboundMessage`）。中立分派 → Task 1（`dispatch`）。LINE 型別止於通道接縫 → Task 2（`LineChannel`，唯一碰 `getattr`/`message.id`/`reply_token` 之處）。第二通道只需再寫轉接器 → Task 1/2 的分工（dispatch 不認 LINE）。webhook 行為保留、既有測試保持綠 → Task 3（既有 `test_webhook.py` 不改）。

**Placeholder scan：** 無 TODO；新模組、測試與 webhook 全檔皆完整。明列的行為差異（eager 抓 audio）已於 Global Constraints 載明，非佔位。

**Type consistency：** `InboundMessage(session_id, kind, text, audio, reply)` 於 Task 1 定義，Task 2 三個分支與 Task 1 測試一致；`dispatch(msg, *, pipeline, binding, gate)` 於 Task 1 定義、Task 3 呼叫一致；`LineChannel(messenger).inbound(event) -> InboundMessage | None` 於 Task 2 定義、Task 3 使用；`create_app(..., on_shutdown=None)` 與「DB 深模組」計畫 app.py 的呼叫一致；prompts 由 `inbound.py` 定義、`webhook.py` re-export，既有測試 import 路徑不變。
```
