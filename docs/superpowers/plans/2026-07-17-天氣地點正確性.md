# 天氣地點正確性實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 金孫不再拿中國山西的天氣回答問台南的長輩；長輩問所在地天氣不再被反問；山區長輩拿到符合海拔的天氣。

**Architecture:** 定位路徑改走模糊座標（手機端四捨五入到 0.01 度）直接查預報、完全跳過地理編碼；長輩口說地名的路徑保留地理編碼但加 `countryCode=TW`，讓它從「答錯」變成「誠實說查不到」。

**Tech Stack:** Python／FastAPI／psycopg／pytest（後端）、React Native＋Expo（App）、Open-Meteo（外部 API）

設計來源：`docs/superpowers/specs/2026-07-17-天氣地點正確性-design.md`。有疑義時以 spec 為準。

## Global Constraints

- 語言：台灣繁體中文，全形標點；程式碼註解、commit 訊息皆同。
- 分支：只在 `Leo` 上工作，不切 main、不自動 push。
- ⚠️ **貫穿全文的原則：寧可答不出來，不可答錯。** 任何「提高命中率但可能比對到錯地方」的做法一律否決（spec 方案選擇 D）。
- ⚠️ **四捨五入必須在手機端做**，後端不得收到精確座標（spec 元件設計 1）。
- ⚠️ `WEATHER_SPEC.description` 與 `agent.py` 的地點三句**必須語意一致**——Bug 2 的根因就是兩者矛盾。改一處必須檢查另一處。
- ⚠️ `LocationFacts` 的 title「僅供參考——他問到的地點不一定是這裡」**一字不動**：那是 anchoring 防線，實測有效。
- ⚠️ **不做座標範圍驗證**（spec 元件設計 5，YAGNI）。
- ⚠️ 動 `app/` 前必讀 v54 文件（`app/AGENTS.md` 明令）：https://docs.expo.dev/versions/v54.0.0/
- ⚠️ 裝依賴後立刻確認 `dependencies` 沒被動到——`expo install` 今天已犯兩次（PR #56、#57）。
- Pg 整合測試以 `KINSUN_IT=1` ＋ `KINSUN_TEST_DATABASE_URL` 啟用（獨立測試庫，**嚴禁連 `DATABASE_URL`**，它指向 Supabase 正式庫）。
- 每個 Task 結束前工作區必須乾淨（`git status --short` 為空）。

---

### Task 1: 地理編碼限定台灣（Bug 1 止血）

**Files:**
- Modify: `src/kinsun/tools/weather.py`（`_GEOCODE_URL`）
- Test: `tests/test_tools_weather.py`

**Interfaces:**
- Consumes: 無
- Produces: 無介面變更

**這個 Task 獨立且最急**：它是目前唯一會主動給長輩錯誤資訊的路徑，且與定位功能無關。即使後面所有 Task 都被打回，這個也該留下。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_tools_weather.py` 檔末加入：

```python
def test_geocode_request_is_limited_to_taiwan():
    """⚠️ Bug 1：地理編碼未限定國家時，「台南」會命中中國山西省的台南
    （35.56, 113.14），金孫會用山西的氣溫回答問台南天氣的長輩，語氣毫無遲疑。

    寧可答不出來，不可答錯——限定 TW 後命中率會下降（實測全台 22 縣市僅 6 個
    查得到），但錯答無法補救、查不到至少誠實。
    """
    urls: list[str] = []

    def handler(method, url, data):
        urls.append(url)
        payload = _GEO if "geocoding" in url else _FC
        return Response(200, {}, json.dumps(payload).encode())

    build_weather_handler(FakeTransport(handler=handler))({"location": "台南"})

    geocode_url = next(u for u in urls if "geocoding" in u)
    assert "countryCode=TW" in geocode_url
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_tools_weather.py::test_geocode_request_is_limited_to_taiwan -q`
Expected: FAIL — `AssertionError`（URL 中沒有 `countryCode=TW`）

- [ ] **Step 3: 實作**

在 `src/kinsun/tools/weather.py` 中，將：

```python
_GEOCODE_URL = (
    "https://geocoding-api.open-meteo.com/v1/search?name={name}&count=1&language=zh&format=json"
)
```

替換為：

```python
# ⚠️ countryCode=TW 不可拿掉：沒有它，「台南」會命中中國山西省的台南
# （35.56, 113.14），金孫會用山西的氣溫回答問台南天氣的長輩。
#
# 代價是命中率低——實測全台 22 縣市只有 6 個查得到（Open-Meteo 的台灣地名索引
# 用「臺」不用「台」、且多數縣市只收錄不帶「市／縣」字尾的形式）。刻意不做
# 多變體 fallback 去提高命中率：實測「新竹縣」會因此命中屏東的一個「新竹」村
# （22.46, 120.47），那是把「查不到」換成「查錯」，與本行要修的 bug 同型。
#
# 寧可答不出來，不可答錯。定位路徑不走這條（它有座標，直接查預報）。
_GEOCODE_URL = (
    "https://geocoding-api.open-meteo.com/v1/search"
    "?name={name}&count=1&language=zh&format=json&countryCode=TW"
)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_tools_weather.py -q`
Expected: PASS（既有測試 ＋ 新增 1）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/tools/weather.py tests/test_tools_weather.py
git commit -F - <<'EOF'
fix(weather): 地理編碼限定台灣——問台南不該拿到中國山西的天氣

實測：地理編碼沒有限定國家，「台南」命中的是中國山西省的台南
（35.56, 113.14）。金孫會用山西七月的氣溫（陰天 19–27°C）回答問台南天氣的
長輩，而且語氣毫無遲疑。這是目前唯一會主動給長輩錯誤資訊的路徑，且從天氣
工具寫出來就存在，與今天的定位功能無關。

代價誠實記在註解裡：限定 TW 後命中率會下降——實測全台 22 縣市只有 6 個查得
到（Open-Meteo 的台灣索引用「臺」不用「台」，且多數縣市只收錄不帶「市／縣」
字尾的形式）。刻意不做多變體 fallback 去補命中率：實測「新竹縣」會因此命中
屏東的一個「新竹」村（22.46, 120.47），而新竹市在 24.80, 120.97——那是把
「查不到」換成「查錯」，與本 commit 要修的 bug 同型。

寧可答不出來，不可答錯：工具對查無結果已有優雅處理（「查不到「X」這個地點
的天氣。」），而錯答無法補救。

IMPACT：
- 長輩問台灣的地名：不再拿到別國的天氣；查不到的縣市會誠實說查不到。
- 長輩問台灣以外的地點：從此一律查不到（刻意——本產品的使用者在台灣）。
- 命中率的根治需要一份台灣地名→座標對照表或換地理編碼服務，見 spec 已知限制 1。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: `get_weather` 收座標，並改寫工具描述（Bug 2 根治）

**Files:**
- Modify: `src/kinsun/tools/weather.py`（`WEATHER_SPEC`、handler）
- Test: `tests/test_tools_weather.py`

**Interfaces:**
- Consumes: Task 1 的 `_GEOCODE_URL`
- Produces: `get_weather(location: str, latitude?: number, longitude?: number)`；handler 在 lat／lon 都有時跳過地理編碼

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_tools_weather.py` 檔末加入：

```python
def test_handler_with_coords_skips_geocoding():
    """定位路徑：有座標就直接查預報，不碰地理編碼。

    這是 Bug 3 的根治——Open-Meteo 的台灣地名索引只有 6/22 命中，而
    reverseGeocodeAsync 回的正是「台南市」這種查不到的字串。有座標就別譯了。
    """
    urls: list[str] = []

    def handler(method, url, data):
        urls.append(url)
        payload = _GEO if "geocoding" in url else _FC
        return Response(200, {}, json.dumps(payload).encode())

    out = build_weather_handler(FakeTransport(handler=handler))(
        {"location": "台南市", "latitude": 22.99, "longitude": 120.21}
    )

    assert not any("geocoding" in u for u in urls), "有座標時不該呼叫地理編碼"
    assert "22.99" in urls[0] and "120.21" in urls[0]
    assert "台南市" in out  # 地名仍用於稱呼


def test_handler_without_coords_uses_geocoding():
    """長輩口說地名的路徑：沒有座標才譯。"""
    urls: list[str] = []

    def handler(method, url, data):
        urls.append(url)
        payload = _GEO if "geocoding" in url else _FC
        return Response(200, {}, json.dumps(payload).encode())

    build_weather_handler(FakeTransport(handler=handler))({"location": "台南"})

    assert any("geocoding" in u for u in urls)


def test_handler_with_only_one_coord_uses_geocoding():
    """半套座標＝沒有座標。不猜、不半套查。"""
    urls: list[str] = []

    def handler(method, url, data):
        urls.append(url)
        payload = _GEO if "geocoding" in url else _FC
        return Response(200, {}, json.dumps(payload).encode())

    build_weather_handler(FakeTransport(handler=handler))({"location": "台南", "latitude": 22.99})

    assert any("geocoding" in u for u in urls)


def test_weather_spec_tells_model_when_to_pass_coords():
    """⚠️ Bug 2 的回歸防線：本描述與 agent.py 的地點三句必須語意一致。

    Bug 2 的根因就是兩者矛盾——工具說「不知道地點就先開口問」，system prompt
    說「直接用那個地點」。兩段都會進模型的 context，模型選了保守解，定位功能
    靜默失效。實測（真 Gemini）：位置是「3 分鐘前在台南市」，金孫照樣反問。

    ⚠️ 這只是釘字串。行為驗證做不到——假 LLM 不會推理，兩段矛盾的提示詞在它
    眼裡只是兩個字串。真正的驗證見 plan Task 7 的真 Gemini 探針。
    """
    assert "他問的就是所在地的天氣，帶上該座標與地名呼叫，不要多問" in WEATHER_SPEC.description
    assert "不要帶座標" in WEATHER_SPEC.description
    assert "兩者都不知道時，先開口問" in WEATHER_SPEC.description
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_tools_weather.py -q`
Expected: FAIL（4 個新測試失敗）

- [ ] **Step 3: 改寫 `WEATHER_SPEC`**

在 `src/kinsun/tools/weather.py` 中，將整個 `WEATHER_SPEC` 替換為：

```python
# ⚠️ 本描述與 agent.py 的地點三句必須語意一致。Bug 2 的根因就是兩者矛盾：工具
# 說「不知道地點就先開口問」、system prompt 說「直接用那個地點」，兩段都進模型
# 的 context，模型選了保守解，定位功能靜默失效整整一天。改一處必須檢查另一處。
WEATHER_SPEC = ToolSpec(
    name="get_weather",
    description=(
        "查詢指定地點今天的天氣（概況與氣溫）。"
        "情境若附上長輩目前位置與座標，而他問的就是所在地的天氣，帶上該座標與地名呼叫，不要多問。"
        "他問的是別的地方（例如等下要去哪裡），就用他說的地點名稱、不要帶座標。"
        "兩者都不知道時，先開口問他要查哪裡，不要自行假設台北。"
    ),
    parameters={
        "type": "object",
        "properties": {
            "location": {
                "type": "string",
                "description": "地點名稱，例：台南、高雄。永遠要帶——回覆時用它稱呼地點。",
            },
            "latitude": {
                "type": "number",
                "description": "情境附的座標；只有在長輩問的就是他所在地時才帶",
            },
            "longitude": {"type": "number", "description": "同 latitude"},
        },
        "required": ["location"],
    },
)
```

- [ ] **Step 4: 改寫 handler**

在 `src/kinsun/tools/weather.py` 中，將 `build_weather_handler` 內的 `handler` 函式替換為：

```python
    def handler(args: dict) -> str:
        location = (args.get("location") or "").strip()
        if not location:
            return "請告訴我您想查哪個地方的天氣。"
        lat, lon = args.get("latitude"), args.get("longitude")
        # 兩個都有才用座標：半套＝沒有。定位路徑（手機回報）必然兩個都給，
        # 半套只可能來自模型出錯，那時走地理編碼比拿半個座標去猜安全。
        # ⚠️ 刻意不驗證座標範圍（spec 元件設計 5）：擋不了模型幻覺（後果是天氣
        # 答錯，與地理編碼失準同級），卻會擋掉出國的長輩，且驗證失敗也只能說
        # 「查不到」——沒有比現況更好。
        if lat is None or lon is None:
            geo = get_json(http, _GEOCODE_URL.format(name=urllib.parse.quote(location)), timeout=10)
            results = geo.get("results") or []
            if not results:
                return f"查不到「{location}」這個地點的天氣。"
            lat, lon = results[0]["latitude"], results[0]["longitude"]
        fc = get_json(http, _FORECAST_URL.format(lat=lat, lon=lon), timeout=10)
        current = fc.get("current") or {}
        daily = fc.get("daily") or {}
        desc = _WMO.get(current.get("weather_code"), "天氣")
        now_t = current.get("temperature_2m")
        highs = (daily.get("temperature_2m_max") or [None])[0]
        lows = (daily.get("temperature_2m_min") or [None])[0]
        parts = [f"{location}今天{desc}"]
        if lows is not None and highs is not None:
            parts.append(f"氣溫約 {round(lows)}–{round(highs)}°C")
        if now_t is not None:
            parts.append(f"目前 {round(now_t)}°C")
        return "，".join(parts) + "。"
```

- [ ] **Step 5: 執行測試，確認通過**

Run: `uv run pytest tests/test_tools_weather.py -q`
Expected: PASS

- [ ] **Step 6: 全測試迴歸**

Run: `uv run pytest -q && uv run ruff check src/ tests/`
Expected: 全綠

- [ ] **Step 7: Commit**

```bash
git add src/kinsun/tools/weather.py tests/test_tools_weather.py
git commit -F - <<'EOF'
fix(weather): 工具收座標並改寫描述——定位功能終於真的會動

PR #55 改了 agent.py 的 SYSTEM_PROMPT，卻沒改 WEATHER_SPEC 的 description
（它仍寫著「只有在你已確認長輩要查哪個城市時才呼叫；若不知道地點，先開口問」）。
兩段提示詞都會進模型的 context，內容矛盾，模型選了保守解——今天早上合併的
定位功能，一整天沒有作用。

真 Gemini 實測：位置是「3 分鐘前在台南市」，金孫照樣答「我不確定您現在是在
哪個縣市呢」。改掉描述後，同一情境立刻改為去查台南的天氣。

⚠️ 連帶發現複驗 3（anchoring）原本「通過」是假象：位置沒生效時金孫在所有情境
都會反問，看起來就像防線有效。修掉本 bug 後重測，它才是真的通過。

新增選用的 latitude／longitude：兩個都有就直接查預報、跳過地理編碼。這是
Bug 3 的根治——Open-Meteo 的台灣地名索引只有 6/22 命中，而 reverseGeocodeAsync
回的正是「台南市」這種查不到的字串。有座標就別譯了；手機端本來就有座標，
是我們把它丟掉換成地名，然後再也譯不回來。

半套座標（只有 lat 或只有 lon）視同沒有：定位路徑必然兩個都給，半套只可能來自
模型出錯，那時走地理編碼比拿半個座標去猜安全。

刻意不驗證座標範圍：擋不了模型幻覺（後果是天氣答錯，與地理編碼失準同級），
卻會擋掉出國的長輩，且驗證失敗也只能說「查不到」——沒有比現況更好。

location 永遠必填：金孫要對長輩說「台南市今天…」，沒有地名它只能說「您那邊」。

IMPACT：
- 對外行為變更：長輩問所在地天氣不再被反問（PR #55 承諾但未兌現的行為）。
  此時 LocationFacts 尚未注入座標，故實際仍走地理編碼；座標路徑在後續 commit 接通。
- ⚠️ 本描述與 agent.py 的地點三句是一組，日後改一處必須檢查另一處。測試只釘
  字串——行為驗證做不到（假 LLM 不會推理），真正的驗證是真 Gemini 探針。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: `elder_locations` 加座標欄位（含既有庫升級）

**Files:**
- Modify: `src/kinsun/db.py`（新 migration DDL ＋ 更正過期註解 ＋ `ensure_schema`）
- Modify: `src/kinsun/locations/store.py`
- Test: `tests/test_locations_store_contract.py`、`tests/test_pg_db.py`

**Interfaces:**
- Consumes: 無
- Produces: `ElderLocation(elder_id: str, place: str, recorded_at: float, latitude: float | None = None, longitude: float | None = None)`

⚠️ **這張表已存在於正式庫**（PR #55 今早合併）。`CREATE TABLE IF NOT EXISTS` 對它是 no-op，把欄位加進 `ELDER_LOCATIONS_DDL` **只有新庫吃得到、既有庫永遠缺欄位**，線上會炸在 `UndefinedColumn`。

- [ ] **Step 1: 寫失敗的測試（合約）**

在 `tests/test_locations_store_contract.py` 檔末加入：

```python
def test_save_and_get_coords(store, ns):
    store.save(ElderLocation(f"{ns}e1", "台南市", 1752739200.0, 22.99, 120.21))
    got = store.get_for_elder(f"{ns}e1")
    assert got == ElderLocation(f"{ns}e1", "台南市", 1752739200.0, 22.99, 120.21)


def test_coords_may_be_absent(store, ns):
    # 既有列的情形：PR #55 寫入的資料沒有座標，ALTER 後為 NULL。
    store.save(ElderLocation(f"{ns}e1", "台南市", 1752739200.0))
    got = store.get_for_elder(f"{ns}e1")
    assert got is not None
    assert got.latitude is None
    assert got.longitude is None
```

- [ ] **Step 2: 寫失敗的測試（既有庫升級）**

在 `tests/test_pg_db.py` 檔末加入：

```python
# PR #55 的 elder_locations schema：只有地名、沒有座標。正式庫就是長這樣。
_LEGACY_ELDER_LOCATIONS_DDL = (
    "CREATE TABLE elder_locations ("
    "elder_id TEXT PRIMARY KEY, place TEXT NOT NULL, "
    "recorded_at DOUBLE PRECISION NOT NULL);"
)


def test_ensure_schema_adds_coords_to_legacy_elder_locations(pg_url):
    """既有庫升級：elder_locations 已於 PR #55 上線、沒有座標欄位，
    ensure_schema 須能就地補上，且不得掉既有資料。

    空庫路徑測不到這條——CREATE TABLE 一開始就會建出座標欄，ALTER 自動略過。
    只有帶著舊表的既有庫（正式庫就是）會走到 ALTER 那條路。
    """
    from kinsun.db import connect, ensure_schema

    with connect(pg_url) as conn:
        conn.execute("DROP TABLE IF EXISTS elder_locations CASCADE;")
        conn.execute(_LEGACY_ELDER_LOCATIONS_DDL)
        conn.execute(
            "INSERT INTO elder_locations (elder_id, place, recorded_at) VALUES (%s, %s, %s)",
            ("legacy-e1", "台南市", 1000.0),
        )
        conn.commit()

    ensure_schema(pg_url)

    with connect(pg_url) as conn:
        cols = _columns_of(conn, "elder_locations")
        assert "latitude" in cols, "未補上 latitude"
        assert "longitude" in cols, "未補上 longitude"
        row = conn.execute(
            "SELECT place, recorded_at, latitude, longitude FROM elder_locations "
            "WHERE elder_id = %s",
            ("legacy-e1",),
        ).fetchone()
        # 既有列必須留著，且座標為 NULL——語意正確（我們確實不知道它在哪）。
        assert row == ("台南市", 1000.0, None, None)
```

- [ ] **Step 3: 執行測試，確認失敗**

Run: `uv run pytest tests/test_locations_store_contract.py tests/test_pg_db.py -q`
Expected: FAIL（`ElderLocation` 不吃座標；`elder_locations` 無 latitude 欄）

- [ ] **Step 4: 加 migration DDL 並更正過期註解**

在 `src/kinsun/db.py` 中，將 `ELDER_LOCATIONS_DDL` 上方的註解與定義替換為：

```python
# 長輩目前地點（spec 2026-07-17）：一位長輩一列、覆寫式，不留行蹤軌跡。
# elder_id 為天然唯一鍵，直接當主鍵（比照 greeting_preferences）。
# 不建額外索引：主鍵即唯一查詢維度。
# ⚠️ 座標欄位不在此處——本表已於 PR #55 上線，見下方 MIGRATION_DDL。
ELDER_LOCATIONS_DDL = (
    "CREATE TABLE IF NOT EXISTS elder_locations ("
    "elder_id TEXT PRIMARY KEY, place TEXT NOT NULL, "
    "recorded_at DOUBLE PRECISION NOT NULL);"
)

# 模糊座標（spec 2026-07-17-天氣地點正確性）：手機端四捨五入到 0.01 度（約 1.1
# 公里）後上傳，天氣工具直接拿去查預報、跳過地理編碼。
#
# ⚠️ 既有庫的 elder_locations 早已存在（PR #55 今早合併），CREATE TABLE IF NOT
# EXISTS 對它是 no-op、不會生出座標欄；故新欄位必須獨立以 ALTER 補（把欄位加進
# ELDER_LOCATIONS_DDL 只有新庫吃得到，既有庫永遠缺欄位、線上炸 UndefinedColumn）。
# ensure_schema 裡本段須排在 ELDER_LOCATIONS_DDL 之後——新庫要先有表才 ALTER 得動。
#
# NULL＝我們不知道它在哪（PR #55 寫入的既有列即此情形），語意正確；LocationFacts
# 讀到 NULL 時退回只注入地名。
ELDER_LOCATIONS_COORDS_MIGRATION_DDL = (
    "ALTER TABLE elder_locations ADD COLUMN IF NOT EXISTS latitude DOUBLE PRECISION;"
    "ALTER TABLE elder_locations ADD COLUMN IF NOT EXISTS longitude DOUBLE PRECISION;"
)
```

在 `ensure_schema` 中，於 `conn.execute(ELDER_LOCATIONS_DDL)` 之後加入：

```python
        conn.execute(ELDER_LOCATIONS_COORDS_MIGRATION_DDL)
```

- [ ] **Step 5: 改 store**

在 `src/kinsun/locations/store.py` 中：

`_COLUMNS` 改為：

```python
_COLUMNS = "elder_id, place, recorded_at, latitude, longitude"
```

`ElderLocation` 改為：

```python
@dataclass(frozen=True)
class ElderLocation:
    elder_id: str
    place: str  # 地名，如「台南市」
    # ⚠️ 我們**收到**的時刻，不是 GPS 定位的時刻；兩者落差為已知誤差來源（見 spec 已知限制 3）。
    recorded_at: float
    # 模糊座標（約 0.01 度／1.1 公里，手機端四捨五入後才上傳）。None＝我們不知道
    # 它在哪：PR #55 寫入的既有列即此情形，LocationFacts 會退回只注入地名。
    latitude: float | None = None
    longitude: float | None = None
```

`PgLocationStore.save` 改為：

```python
    def save(self, location: ElderLocation) -> None:
        self._db.execute(
            f"INSERT INTO elder_locations ({_COLUMNS}) VALUES (%s, %s, %s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET "
            "place = EXCLUDED.place, recorded_at = EXCLUDED.recorded_at, "
            "latitude = EXCLUDED.latitude, longitude = EXCLUDED.longitude",
            (
                location.elder_id,
                location.place,
                location.recorded_at,
                location.latitude,
                location.longitude,
            ),
        )
```

`get_for_elder` 不動（`ElderLocation(*row)` 會自動吃到五個欄位）。

- [ ] **Step 6: 執行測試，確認通過**

Run: `uv run pytest tests/test_locations_store_contract.py tests/test_pg_db.py -q`
Expected: PASS

⚠️ 若 `test_ensure_schema_adds_coords_to_legacy_elder_locations` 未執行（skip），代表 `KINSUN_IT` 未設——**那條是本 Task 的核心斷言，必須實際跑到**。設 `KINSUN_IT=1` 並確認測試庫起著（`scripts/test_db.sh up`）。

- [ ] **Step 7: 全測試迴歸**

Run: `uv run pytest -q && uv run ruff check src/ tests/`
Expected: 全綠

- [ ] **Step 8: Commit**

```bash
git add src/kinsun/db.py src/kinsun/locations/store.py tests/test_locations_store_contract.py tests/test_pg_db.py
git commit -F - <<'EOF'
feat(locations): elder_locations 加座標欄位，並補既有庫的升級路徑

模糊座標（0.01 度／約 1.1 公里）要有地方存。手機端四捨五入後上傳，天氣工具
直接拿去查預報、跳過地理編碼——那是 Open-Meteo 台灣地名索引只有 6/22 命中的
根治，也是山區長輩能拿到正確天氣的唯一辦法（梨山與台中市中心差 8.5°C）。

⚠️ 座標欄位獨立以 ALTER 補，不加進 ELDER_LOCATIONS_DDL：這張表今早才隨 PR #55
上正式庫，CREATE TABLE IF NOT EXISTS 對它是 no-op，把欄位寫進去只有新庫吃得到、
既有庫永遠缺欄位、線上炸 UndefinedColumn。這正是 db.py 裡 reminder_logs 那段
註解記載的教訓，本 commit 是它的第二個案例。

一併更正一句今早才寫、現在就過期的註解：「elder_locations 是全新的表，既有庫
沒有舊資料要升級，故不需 ALTER 補欄位」——那句話今早為真，加了座標欄位後為假，
留著會誤導下一個加欄位的人。

既有庫升級路徑有整合測試釘住（造出 PR #55 的舊 schema、塞一列、跑 ensure_schema、
驗欄位補上且既有列的座標為 NULL）。空庫路徑測不到這條——CREATE TABLE 一開始
就會建出座標欄，ALTER 自動略過。只有帶著舊表的既有庫（正式庫就是）會踩到。

座標可為空，語意是「我們不知道它在哪」：PR #55 寫入的既有列即此情形，
LocationFacts 會退回只注入地名（行為等同 PR #55）。

IMPACT：
- 既有庫多兩個可空欄位，既有列不受影響、行為不變。
- 尚無呼叫端寫入座標（/turns 在後續 commit 接通），對外行為零變更。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: `LocationFacts` 注入座標

**Files:**
- Modify: `src/kinsun/locations/facts.py`
- Test: `tests/test_locations_facts.py`

**Interfaces:**
- Consumes: Task 3 的 `ElderLocation`（含座標）
- Produces: 無介面變更（`facts(elder_id) -> FactSection | None`）

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_locations_facts.py` 檔末加入：

```python
def test_injects_coords_for_the_weather_tool():
    section = _facts(
        ElderLocation("e1", "台南市", _at(minutes_ago=3), 22.99, 120.21)
    ).facts("e1")
    assert section is not None
    assert section.items == ["3 分鐘前在台南市（查這裡的天氣請用座標 22.99,120.21）"]


def test_falls_back_to_place_only_without_coords():
    # 既有列（PR #55 寫入的）沒有座標：退回只注入地名，行為等同 PR #55。
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=3))).facts("e1")
    assert section is not None
    assert section.items == ["3 分鐘前在台南市"]


def test_title_frames_location_as_hint_even_with_coords():
    # ⚠️ anchoring 防線不因座標而放鬆：實測證明這段措辭有效，一字不動。
    section = _facts(
        ElderLocation("e1", "台南市", _at(minutes_ago=3), 22.99, 120.21)
    ).facts("e1")
    assert section is not None
    assert "參考" in section.title
    assert "不一定" in section.title
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_locations_facts.py -q`
Expected: FAIL（`test_injects_coords_for_the_weather_tool`）

- [ ] **Step 3: 實作**

在 `src/kinsun/locations/facts.py` 中，將 `facts` 方法的 return 替換為：

```python
        return FactSection(_TITLE, [self._line(location, elapsed)])
```

並在 `LocationFacts` 類別中加入：

```python
    @staticmethod
    def _line(location, elapsed: float) -> str:
        """一行內容：相對時間 ＋ 地名（＋ 座標，若有）。

        座標給天氣工具直接查預報用，跳過地理編碼——Open-Meteo 的台灣地名索引
        只有 6/22 命中，而手機回報的正是「台南市」這種查不到的字串。
        地名仍要有：金孫得對長輩說「台南市今天…」。

        座標缺漏（PR #55 寫入的既有列）時退回只給地名，行為等同 PR #55。
        """
        base = f"{_relative(elapsed)}在{location.place}"
        if location.latitude is None or location.longitude is None:
            return base
        return f"{base}（查這裡的天氣請用座標 {location.latitude},{location.longitude}）"
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_locations_facts.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/locations/facts.py tests/test_locations_facts.py
git commit -F - <<'EOF'
feat(locations): 注入座標供天氣工具直接查，跳過地理編碼

手機端本來就有精確座標，是我們把它丟掉換成地名，然後再也譯不回來——
Open-Meteo 的台灣地名索引只有 6/22 命中，「台南市」正是查不到的那種。
本 commit 把模糊座標一起注入，讓模型能轉交給天氣工具直接查預報。

地名仍要有：金孫得對長輩說「台南市今天…」，沒有地名它只能說「您那邊」。

座標缺漏時退回只給地名——PR #55 寫入的既有列即此情形，行為等同 PR #55
（走地理編碼、可能查不到、誠實回覆）。

⚠️ title 的「僅供參考——他問到的地點不一定是這裡」一字不動：那是 anchoring
防線，真 Gemini 實測證明有效（問「等下要去別的地方吃飯」時金孫會開口問是
哪裡，不拿所在地去查）。測試明文釘住它在有座標時也不放鬆。

IMPACT：
- 尚無呼叫端寫入座標（/turns 在下一個 commit 接通），對外行為零變更。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: `/turns` 收座標

**Files:**
- Modify: `src/kinsun/channels/app/turns.py`
- Test: `tests/test_channels_app_turns.py`

**Interfaces:**
- Consumes: Task 3 的 `ElderLocation`
- Produces: `POST /turns` 新增 query param `latitude`、`longitude`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_channels_app_turns.py` 中，將 `_post_audio` 替換為：

```python
def _post_audio(client, token, body=b"\x00fake-audio", *, location=None, lat=None, lon=None):
    params = {}
    if location is not None:
        params["location"] = location
    if lat is not None:
        params["latitude"] = lat
    if lon is not None:
        params["longitude"] = lon
    return client.post(
        "/api/v1/turns",
        content=body,
        params=params,
        headers={"Authorization": f"Bearer {token}", "Content-Type": "audio/m4a"},
    )
```

並在檔末加入：

```python
def test_location_with_coords_is_saved():
    svc = _service()
    elder, token = _bound_elder_token(svc)
    locations = FakeLocationStore()
    res = _post_audio(
        _client(svc, locations=locations), token, location="台南市", lat=22.99, lon=120.21
    )
    assert res.status_code == 201
    assert locations.get_for_elder(elder.elder_id) == ElderLocation(
        elder.elder_id, "台南市", NOW.timestamp(), 22.99, 120.21
    )


def test_location_without_coords_does_not_write():
    # 半套＝沒有：App 要嘛三個都給、要嘛都不給。只有地名沒有座標的分支沒有任何
    # 呼叫端會產生，接受它只會讓下游多一條沒人走的路。
    svc = _service()
    elder, token = _bound_elder_token(svc)
    locations = FakeLocationStore()
    _post_audio(_client(svc, locations=locations), token, location="台南市")
    assert locations.get_for_elder(elder.elder_id) is None


def test_coords_without_location_does_not_write():
    svc = _service()
    elder, token = _bound_elder_token(svc)
    locations = FakeLocationStore()
    _post_audio(_client(svc, locations=locations), token, lat=22.99, lon=120.21)
    assert locations.get_for_elder(elder.elder_id) is None
```

⚠️ 既有的 `test_location_query_param_is_saved` 與 `test_blank_location_does_not_clear_existing` 會因「半套＝沒有」而失效——**它們斷言的行為已被 spec 取代**。將 `test_location_query_param_is_saved` 刪除（由上方 `test_location_with_coords_is_saved` 取代）；`test_blank_location_does_not_clear_existing` 改為帶齊三個參數但 location 為空白：

```python
def test_blank_location_does_not_clear_existing():
    svc = _service()
    elder, token = _bound_elder_token(svc)
    locations = FakeLocationStore()
    locations.save(ElderLocation(elder.elder_id, "高雄市", 1752739200.0, 22.62, 120.31))
    _post_audio(_client(svc, locations=locations), token, location="   ", lat=22.99, lon=120.21)
    assert locations.get_for_elder(elder.elder_id) == ElderLocation(
        elder.elder_id, "高雄市", 1752739200.0, 22.62, 120.31
    )
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_channels_app_turns.py -q`
Expected: FAIL

- [ ] **Step 3: 實作**

在 `src/kinsun/channels/app/turns.py` 中，將 `_save_location` 替換為：

```python
    def _save_location(elder_id: str, place: str, lat: float | None, lon: float | None) -> None:
        """記下長輩這輪回報的地點與模糊座標。

        三者必須同時具備才寫入：只有地名沒座標（或反之）視同「這輪沒有位置」。
        App 要嘛三個都給、要嘛都不給；接受半套只會讓下游多一條沒人走的分支。

        空字串／純空白的地名＝「這輪沒有位置」（未授權、室內收不到），**不是**
        「他不在任何地方」——故不寫入也不清空既有資料。
        """
        place = place.strip()
        if locations is None or not place or lat is None or lon is None:
            return
        try:
            locations.save(ElderLocation(elder_id, place, now().timestamp(), lat, lon))
        except Exception:  # noqa: BLE001 - 位置是加分項，寫入失敗不可中斷對話
            logger.warning("長輩地點寫入失敗")
```

將 `create_turn` 的簽章替換為：

```python
    async def create_turn(
        request: Request,
        location: str = "",
        latitude: float | None = None,
        longitude: float | None = None,
        elder_id: str = Depends(current_elder),
    ) -> dict:
```

將呼叫處替換為：

```python
        _save_location(elder_id, location, latitude, longitude)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_channels_app_turns.py -q`
Expected: PASS

- [ ] **Step 5: 全測試迴歸**

Run: `uv run pytest -q && uv run ruff check src/ tests/`
Expected: 全綠

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/channels/app/turns.py tests/test_channels_app_turns.py
git commit -F - <<'EOF'
feat(channels): /turns 收模糊座標，三者齊備才寫入

後端這半的最後一段：App 把地名與模糊座標一起送上來。

三者（location／latitude／longitude）必須同時具備才寫入。半套＝沒有：App 要嘛
三個都給、要嘛都不給，接受「只有地名沒有座標」只會讓下游多一條沒有任何呼叫端
會產生的分支。既有的 test_location_query_param_is_saved 斷言的正是那條半套路徑，
已由帶齊座標的版本取代。

空白地名仍不寫入也不清空——語意是「這輪沒有位置」（未授權、室內收不到），
不是「他不在任何地方」（PR #55 的決定，不變）。

IMPACT：
- 對外行為變更：App 帶齊三個參數時會寫入座標。App 端在下一個 commit 才送，
  故正式環境行為此時未變。
- locations 未注入時（scheduler 端）行為與現在完全相同，參數皆有預設。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 6: App 端送模糊座標

**Files:**
- Modify: `app/src/lib/location.ts`
- Modify: `app/src/lib/api.ts`
- Modify: `app/src/app/elder/talk.tsx`
- Test: `app/src/lib/location.test.ts`

**Interfaces:**
- Consumes: Task 5 的 `/turns` query params
- Produces: `currentPlace(): Promise<ElderPlace | null>`，`ElderPlace = { place: string; latitude: number; longitude: number }`

⚠️ **先讀 v54 文件**（`app/AGENTS.md` 明令）：https://docs.expo.dev/versions/v54.0.0/sdk/location/

- [ ] **Step 1: 改寫測試**

將 `app/src/lib/location.test.ts` 中所有 `expect(await currentPlace()).toBe("")` 改為 `expect(await currentPlace()).toBeNull()`，並將回傳城市名的斷言改為物件比對。完整替換三個「正常」相關的測試：

```ts
  it("正常時回傳地名與模糊座標", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue({
      coords: { latitude: 22.9876, longitude: 120.2134 },
    } as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([{ city: "台南市" }] as never);

    expect(await currentPlace()).toEqual({
      place: "台南市",
      latitude: 22.99,
      longitude: 120.21,
    });
  });

  it("座標四捨五入到 0.01 度（約 1.1 公里）", async () => {
    // ⚠️ 四捨五入必須在手機端做：後端一旦收到精確值，它就已經進了伺服器的
    // 記憶體與（潛在的）log。隱私邊界要劃在資料離開裝置之前。
    //
    // 0.01 度是實測出來的交會點：0.1 度（約 11 公里）會把陽明山的雷雨變成
    // 毛毛雨；0.01 度與精確座標的天氣幾乎一致（梨山差 1.3°C，天氣類型相同）。
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue({
      coords: { latitude: 25.0261234, longitude: 121.5439876 },
    } as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([{ city: "台北市" }] as never);

    const got = await currentPlace();
    expect(got?.latitude).toBe(25.03);
    expect(got?.longitude).toBe(121.54);
  });

  it("city 缺漏時退 subregion", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(POSITION as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([
      { city: null, subregion: "東區", region: "台南市" },
    ] as never);

    expect((await currentPlace())?.place).toBe("東區");
  });
```

其餘失敗路徑的測試（拒絕定位、無快取位置、譯不出地名、拋例外）將斷言由 `toBe("")` 改為 `toBeNull()`。

- [ ] **Step 2: 執行測試，確認失敗**

Run: `cd app && npm test`
Expected: FAIL

- [ ] **Step 3: 改寫 `location.ts`**

將 `app/src/lib/location.ts` 全檔替換為：

```typescript
/**
 * 取長輩目前所在的地名與模糊座標。
 *
 * ⚠️ 座標在本檔四捨五入到 0.01 度（約 1.1 公里）之後才上傳，精確值永不離開手機。
 * 隱私邊界劃在資料離開裝置之前——後端一旦收到精確值，它就已經進了伺服器的
 * 記憶體與（潛在的）log，再捨去也來不及。
 *
 * 為什麼要送座標而不是只送地名：Open-Meteo 的台灣地名索引只有 6/22 縣市查得到，
 * 而本檔回的正是「台南市」這種查不到的字串。而且真正決定天氣的是海拔不是行政區
 * ——實測台北市大安區與市中心完全一樣，但陽明山是雷雨、市中心是毛毛雨；梨山與
 * 台中市中心差 8.5°C。只有座標抓得到這件事。
 *
 * 為什麼是 0.01 度：實測出來的交會點。0.1 度（約 11 公里，Open-Meteo 全球網格
 * 的尺度）會把陽明山的雷雨變成毛毛雨；0.01 度與精確座標的天氣幾乎一致。
 *
 * 一切失敗（未授權、無快取位置、譯不出地名）都回 null，由呼叫端當成「這輪沒有
 * 位置」——金孫會照舊開口問，功能靜默降級，絕不阻擋對講機。
 */

import * as Location from "expo-location";

export type ElderPlace = { place: string; latitude: number; longitude: number };

/** 約 1.1 公里見方。市區內是上萬人的範圍，定位不到住址。 */
function blur(value: number): number {
  return Math.round(value * 100) / 100;
}

export async function currentPlace(): Promise<ElderPlace | null> {
  try {
    const { status } = await Location.requestForegroundPermissionsAsync();
    if (status !== Location.PermissionStatus.GRANTED) {
      return null;
    }
    // 用 getLastKnownPositionAsync 而非 getCurrentPositionAsync：官方文件載明後者
    // 可能要等好幾秒，而長輩按下對講機是要馬上講話的。快取位置對城市級精度綽綽有餘。
    const position = await Location.getLastKnownPositionAsync();
    if (position === null) {
      return null;
    }
    const [address] = await Location.reverseGeocodeAsync(position.coords);
    if (!address) {
      return null;
    }
    // 各家作業系統的 city 粒度不一（可能回「台南市」也可能回「東區」）；
    // 退階順序是緩解，不是保證。地名只用於稱呼，查天氣走座標，故粒度不影響準確度。
    const place = address.city ?? address.subregion ?? address.region ?? "";
    if (!place) {
      return null;
    }
    return {
      place,
      latitude: blur(position.coords.latitude),
      longitude: blur(position.coords.longitude),
    };
  } catch {
    return null;
  }
}
```

- [ ] **Step 4: 改 `api.ts`**

將 `app/src/lib/api.ts` 的 `postTurn` 替換為：

```typescript
export async function postTurn(
  audioUri: string,
  token: string,
  place: ElderPlace | null = null,
): Promise<TurnReply> {
  const audio = await (await fetch(audioUri)).blob();
  // 地名與座標走 query param：/turns 收的是裸音檔 body，它們在 body 裡沒有位置可放。
  // null＝這輪沒有位置（未授權、室內收不到），不帶參數——不是「他不在任何地方」。
  const query = place
    ? `?${new URLSearchParams({
        location: place.place,
        latitude: String(place.latitude),
        longitude: String(place.longitude),
      })}`
    : "";
  return request(`/api/v1/turns${query}`, {
    method: "POST",
    body: audio,
    headers: { "Content-Type": "audio/m4a" },
    token,
  });
}
```

並在檔案頂端加入型別匯入：

```typescript
import type { ElderPlace } from "@/lib/location";
```

⚠️ 若 `api.ts` 匯入 `location.ts` 造成循環匯入（`location.ts` 目前不匯入 `api.ts`，應不會），改為把 `ElderPlace` 型別定義移到 `api.ts` 或共用處——**以實際的錯誤為準**。

- [ ] **Step 5: 改 `talk.tsx`**

將 `placeRef` 的型別與 await 處替換為：

```tsx
  const placeRef = useRef<Promise<ElderPlace | null> | null>(null);
```

```tsx
      const place = await (placeRef.current ?? Promise.resolve(null));
      const reply = await postTurn(uri, session?.token ?? "", place);
```

並將 `import { currentPlace } from "@/lib/location";` 改為：

```tsx
import { type ElderPlace, currentPlace } from "@/lib/location";
```

- [ ] **Step 6: 執行測試與檢查**

Run: `cd app && npm test && npm run lint && npm run typecheck`
Expected: 全部 PASS

- [ ] **Step 7: Commit**

```bash
cd /home/leo29/kinsun
git add app/src/lib/location.ts app/src/lib/location.test.ts app/src/lib/api.ts app/src/app/elder/talk.tsx
git commit -F - <<'EOF'
feat(app): 對講機送出模糊座標，精確值不離開手機

管道的源頭改送座標。PR #55 只送地名，而那條路走不通：Open-Meteo 的台灣地名
索引只有 6/22 縣市查得到，本檔回的正是「台南市」這種查不到的字串。手機本來
就有座標，是我們把它丟掉換成地名，然後再也譯不回來。

更根本的是：真正決定天氣的是海拔，不是行政區。實測台北市大安區與市中心完全
一樣（32.4°C／毛毛雨），但陽明山是雷雨、梨山與台中市中心差 8.5°C。行政地名
表（不論 22 縣市或 368 鄉鎮區）都抓不到這件事，只有座標抓得到。

⚠️ 四捨五入到 0.01 度（約 1.1 公里）在本檔做，精確值永不離開手機：隱私邊界要
劃在資料離開裝置之前——後端一旦收到精確值，它就已經進了記憶體與潛在的 log，
再捨去也來不及。

0.01 度是實測出來的交會點：先試 0.1 度（約 11 公里，Open-Meteo 全球網格的
尺度，理論上無損），實測否決——它把陽明山的雷雨變成毛毛雨、梨山差 3.5°C。
0.01 度與精確座標的天氣幾乎一致（梨山差 1.3°C，天氣類型相同）。

⚠️ 這推翻了 PR #55 的「經緯度不離開手機」。Leo 當時的決定沒有錯——錯的是它
依據的前提（我們都以為地名足以查到天氣）。保留該決定的精神（不上傳住址級
精度），代價誠實記在 spec：1.1 公里在市區是上萬人的範圍，但在梨山那種地方
可能只有幾戶。接受它的理由是山區長輩恰恰是縣市表錯得最離譜的那群。

失敗路徑由回空字串改為回 null，語意相同（這輪沒有位置），型別更誠實。

IMPACT：
- App 已授權定位的長輩：問所在地天氣不再被反問，且山區也會拿到正確的天氣。
- 未授權者行為完全不變（金孫照舊開口問）。
- ⚠️ App 端的三條人工複驗仍需 Leo 拿手機執行；後端側的行為驗證見下一個 commit
  的真 Gemini 探針。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 7: 真 Gemini 探針驗證 ＋ 更新兩份 spec

**Files:**
- Create: `scripts/anchoring_probe.py`
- Modify: `docs/superpowers/specs/2026-07-17-天氣地點正確性-design.md`（狀態）
- Modify: `docs/superpowers/specs/2026-07-17-長輩目前地點-design.md`（指向本輪的更正）

**Interfaces:**
- Consumes: Task 1–6 的全部
- Produces: 可重跑的探針

**這個 Task 是整個計畫唯一能證明它真的работает的一步。** Bug 2 的本質是「兩段提示詞矛盾 → 模型選保守解」——單元測試抓不到（假 LLM 不會推理），只有真 Gemini 跑得出來。

- [ ] **Step 1: 把探針收進 repo**

把 session 中用過的探針（scratchpad 的 `anchoring_probe.py`）整理進 `scripts/anchoring_probe.py`，並依 Task 3／4 的新介面更新：`ElderLocation` 帶座標（`22.99, 120.21`）。

⚠️ 探針**不得碰資料庫**：短期／長期記憶與地點全用測試替身，只有 Gemini 與 Open-Meteo 是真的。`DATABASE_URL` 指向 Supabase 正式庫，絕不參與。

- [ ] **Step 2: 執行三條複驗**

Run: `uv run python scripts/anchoring_probe.py`

Expected：

1. **允許定位 →「今天天氣如何？」** → 直接答**台南**的天氣（氣溫應合理，七月的台南約 26–33°C），**不反問**
2. **無位置 →「今天天氣如何？」** → 開口問是哪裡
3. **允許定位 →「等下要去別的地方吃飯，那邊天氣如何？」** → 開口問是哪裡，**不拿台南去查**

⚠️ 第 1 條若仍反問，代表 Bug 2 未根治（工具描述與 system prompt 仍有矛盾）——**停下來回報**，不要靠加提示詞硬壓。
⚠️ 第 3 條若直接答台南，代表 anchoring 失效——**停下來回報**，那是設計層級的問題，正確的反應是回頭重估工具方案（PR #55 spec 已載明），不是往 prompt 疊更多句。
⚠️ 第 1 條若答出**山西的氣溫**（約 19–27°C），代表 Task 1 未生效。

- [ ] **Step 3: 更新本輪 spec 的狀態**

在 `docs/superpowers/specs/2026-07-17-天氣地點正確性-design.md` 中，將狀態行改為「**已實作**（Task 1–7）」，並比照同日三份 spec 的慣例，具體載明實作中對本文的更正（無更正才可用簡短版）。

- [ ] **Step 4: 更正 PR #55 的 spec**

在 `docs/superpowers/specs/2026-07-17-長輩目前地點-design.md` 的狀態行末尾加入：

```markdown
⚠️ **本文的「只存地名不存座標」與方案選擇 D 已由 `2026-07-17-天氣地點正確性-design.md` 推翻**：實測顯示地名路徑只有 6/22 縣市查得到，且行政區這個抽象抓不到真正決定天氣的海拔。改以模糊座標（0.01 度）實現。另：本文的三條人工複驗執行後發現定位功能因工具描述未同步而一直沒有作用，見該文 Bug 2。
```

- [ ] **Step 5: 全量驗證**

Run:

```bash
cd /home/leo29/kinsun && uv run pytest -q 2>&1 | tail -2 && uv run ruff check src/ tests/ \
  && cd frontend && npm run lint && npm run typecheck && npm test && npm run build && npm run build:admin \
  && cd ../app && npm run lint && npm run typecheck && npm test \
  && echo "全部通過"
```

Expected: 後端全綠、ruff All checks passed、印出「全部通過」

- [ ] **Step 6: Commit**

```bash
cd /home/leo29/kinsun
git add scripts/anchoring_probe.py docs/superpowers/specs/2026-07-17-天氣地點正確性-design.md docs/superpowers/specs/2026-07-17-長輩目前地點-design.md
git commit -F - <<'EOF'
test(weather): 把真 Gemini 探針收進 repo，並更正兩份 spec

本系列唯一能證明它真的會動的一步。Bug 2 的本質是「兩段提示詞矛盾 → 模型選
保守解」——單元測試抓不到（假 LLM 不會推理，兩段矛盾的提示詞在它眼裡只是
兩個字串）。今天就是靠這支探針才發現定位功能一整天沒有作用。

收進 scripts/ 而非留在 scratchpad：它是這個功能唯一的行為驗證，日後任何人改
weather.py 的描述或 agent.py 的地點三句，都該重跑它。

探針刻意不碰資料庫：短期／長期記憶與地點全用測試替身，只有 Gemini 與
Open-Meteo 是真的。DATABASE_URL 指向 Supabase 正式庫，絕不參與。

一併更正 PR #55 的 spec：它的「只存地名不存座標」與「方案選擇 D」已被本輪
推翻，狀態行指向本輪的 spec，避免日後有人照著它做出走不通的設計。

IMPACT：
- 純新增腳本與文件更正，無執行期程式碼變更。
- ⚠️ App 端的人工複驗（實機點「允許定位」、按對講機講話）仍需 Leo 執行：
  探針驗的是後端與模型的行為，繞過了 App 那層。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

## Self-Review 紀錄

- **Spec 覆蓋**：元件設計 1→Task 6、2→Task 5、3→Task 3、4→Task 4、5→Task 2、6→Task 1、7→Task 2；資料庫遷移→Task 3 Step 2／4；測試策略表→各 Task；「行為驗證只能靠真 Gemini」→Task 7；與既有設計的衝突→Task 7 Step 4。無遺漏。
- **刻意的排序**：Task 1（Bug 1 止血）獨立且排第一——即使後面全被打回，它也該留下。Task 7（探針）排最後，因為它驗的是整條路。
- **會被取代的既有測試**：Task 5 明文指出 `test_location_query_param_is_saved` 與 `test_blank_location_does_not_clear_existing` 斷言的是「半套寫入」，已被 spec 的「三者齊備」取代——這是刻意的行為變更，不是測試壞了。
- **型別一致性**：`ElderLocation(elder_id, place, recorded_at, latitude=None, longitude=None)` 於 Task 3（定義）、4、5、7 一致；`ElderPlace = { place, latitude, longitude }` 於 Task 6 定義並被 `api.ts`、`talk.tsx` 使用；`get_weather(location, latitude?, longitude?)` 於 Task 2 定義、Task 4 的注入措辭與之對應。
- **未寫死的一處**：Task 6 Step 4 的 `ElderPlace` 型別匯入若造成循環匯入，要以實際錯誤為準調整——硬給程式碼會產生與現實不符的指令（PR #55／#56／#57 的教訓）。
- **範圍外**（spec 已知限制）：口說地名的 6/22 命中率、座標範圍驗證、山區的隱私成本、`recorded_at` 的語意。
