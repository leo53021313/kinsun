# Postgres 存取深模組 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（建議）或 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 把散在 5 個 Pg* store 的「開連線／try-except／commit」樣板，收進一個深 `Database` 模組（連線池 ＋ 交易 ＋ 單一 `StoreError`），各 store 退回到「自己的 SQL ＋ 自己的 row 映射 ＋ 自己的領域錯誤」。

**Architecture:** 新增 `Database`（注入式，組合非繼承）包 `psycopg_pool.ConnectionPool`，提供 `execute/query/query_one/transaction`，失敗一律丟 `StoreError`。每個 store 在建構時把 `Database` 包進一個 `_Errors` 轉接器，自動把 `StoreError` 翻成自己的領域錯誤（`MemoryError`/`AccountError`/`BindingSessionError`/`MedicationError`/`ScheduleStateError`）——`binding`、`medication` 不再洩漏 `AccountError`。`AccountService` 的多寫操作（`create_elder`、`redeem_invite`）改走 `transaction()` 保障原子性。

**Tech Stack:** Python 3.12、`psycopg` v3、**新增 `psycopg_pool`**（`psycopg[pool]` extra；純 Python、ARM64 安全）、pytest。

## Global Constraints

- src layout（`src/kinsun/`），pytest `pythonpath=["src","."]`。
- 單元測試全離線：以 fake pool／fake executor 注入；Pg 行為以 opt-in 整合測試（`KINSUN_IT=1` + 雲端 `DATABASE_URL`）覆蓋。
- `DATABASE_URL` 為 Supabase 5432 直連 → 保留 psycopg3 prepared statement；連線池設小（`min_size=1, max_size=5`），`app` 與 `scheduler` 兩行程各一個池。
- 不寫死任何金鑰；查詢全參數化。
- StrEnum；ruff `E,W,F,I,B,UP`、line-length 100、quote-style double。commit 結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不自動 push；只在 Leo 分支工作；main 經 PR 合併。
- `ensure_schema(database_url)` 維持以一次性連線跑 DDL（不經池），簽名與呼叫端不變。

---

## File Structure

```
src/kinsun/db.py                  # + StoreError / Executor / Database / _ConnExecutor / _Errors（connect/DDL/ensure_schema 不變）
src/kinsun/memory/store.py        # PgMemoryStore(db, ...) 改用 Database
src/kinsun/scheduler/state.py     # PgScheduleStateStore(db, tz) 改用 Database
src/kinsun/medication/store.py    # PgMedicationStore(db) 改用 Database + MedicationError
src/kinsun/binding/session.py     # PgBindingSessionStore(db) 改用 Database + BindingSessionError
src/kinsun/accounts/repository.py # PgAccountRepository(db) 改用 Database + 5 個 save_* 吃 tx + transaction()
src/kinsun/accounts/service.py    # create_elder / redeem_invite 走 transaction()
src/kinsun/app.py                 # build_app 建 Database、close 池（lifespan）
src/kinsun/scheduler/worker.py    # build_scheduler 建 Database、serve 收尾 close 池
pyproject.toml                    # psycopg[binary] → psycopg[binary,pool]
tests/fakes.py                    # FakeAccountRepository + transaction()/tx=None
tests/test_database.py            # 新增：Database + _Errors 單元測試（離線）
tests/test_pg_memory_store.py     # 整合測試改建 Database
tests/test_pg_account_repository.py / test_pg_memory_store / test_pg_scheduler_state / test_pg_memory_store  # 整合測試改建 Database
```

---

## Task 1：Database 深模組 + psycopg_pool 依賴

**Files:**
- Modify: `pyproject.toml`
- Modify: `src/kinsun/db.py`
- Test: `tests/test_database.py`

**Interfaces:**
- Produces：
  - `StoreError(Exception)`
  - `Executor`(Protocol)：`execute(sql, params=()) -> None`、`query(sql, params=()) -> list[tuple]`、`query_one(sql, params=()) -> tuple | None`
  - `Database(pool)`：上述 Executor 介面 ＋ `transaction() -> ContextManager[Executor]`、classmethod `open(url, *, min_size=1, max_size=5) -> Database`、`close() -> None`；失敗丟 `StoreError`
  - `_Errors(inner: Executor, wrap: Callable[[str], Exception])`：Executor，含 `transaction()`；把 `StoreError` 翻成 `wrap(...)`

- [ ] **Step 1：加依賴** 於 `pyproject.toml` 把 `"psycopg[binary]>=3.3.4"` 改為 `"psycopg[binary,pool]>=3.3.4"`，然後 Run：`uv sync`
  Expected：安裝 `psycopg_pool`，無錯。

- [ ] **Step 2：失敗測試** Create `tests/test_database.py`：
```python
from contextlib import contextmanager

import pytest

from kinsun.db import Database, StoreError, _Errors


class _FakeCursor:
    def __init__(self, rows):
        self._rows = rows

    def fetchall(self):
        return self._rows

    def fetchone(self):
        return self._rows[0] if self._rows else None


class _FakeConn:
    def __init__(self, rows=None, boom=False):
        self.rows = rows or []
        self.boom = boom
        self.calls = []
        self.committed = False
        self.tx_entered = False

    def execute(self, sql, params=()):
        self.calls.append((sql, tuple(params)))
        if self.boom:
            raise RuntimeError("db down")
        return _FakeCursor(self.rows)

    def commit(self):
        self.committed = True

    @contextmanager
    def transaction(self):
        self.tx_entered = True
        yield


class _FakePool:
    def __init__(self, conn):
        self._conn = conn
        self.closed = False

    @contextmanager
    def connection(self):
        yield self._conn

    def close(self):
        self.closed = True


def test_execute_commits():
    conn = _FakeConn()
    db = Database(_FakePool(conn))
    db.execute("INSERT INTO t VALUES (%s)", ("a",))
    assert conn.calls == [("INSERT INTO t VALUES (%s)", ("a",))]
    assert conn.committed is True


def test_query_returns_rows():
    conn = _FakeConn(rows=[("x",), ("y",)])
    db = Database(_FakePool(conn))
    assert db.query("SELECT c FROM t") == [("x",), ("y",)]
    assert db.query_one("SELECT c FROM t") == ("x",)


def test_failure_raises_store_error():
    db = Database(_FakePool(_FakeConn(boom=True)))
    with pytest.raises(StoreError):
        db.execute("INSERT INTO t VALUES (%s)", ("a",))


def test_transaction_yields_executor():
    conn = _FakeConn(rows=[("ok",)])
    db = Database(_FakePool(conn))
    with db.transaction() as tx:
        tx.execute("INSERT INTO t VALUES (%s)", ("a",))
        assert tx.query_one("SELECT c FROM t") == ("ok",)
    assert conn.tx_entered is True


def test_close_closes_pool():
    pool = _FakePool(_FakeConn())
    Database(pool).close()
    assert pool.closed is True


def test_errors_translates_store_error():
    class _Boom:
        def execute(self, sql, params=()):
            raise StoreError("boom")

        def query(self, sql, params=()):
            raise StoreError("boom")

        def query_one(self, sql, params=()):
            raise StoreError("boom")

        @contextmanager
        def transaction(self):
            raise StoreError("boom")
            yield

    wrapped = _Errors(_Boom(), lambda m: ValueError(f"translated:{m}"))
    with pytest.raises(ValueError, match="translated:boom"):
        wrapped.execute("X")
    with pytest.raises(ValueError, match="translated:boom"):
        wrapped.query("X")
    with pytest.raises(ValueError, match="translated:boom"):
        wrapped.query_one("X")
    with pytest.raises(ValueError, match="translated:boom"):
        with wrapped.transaction():
            pass
```

- [ ] **Step 3：確認失敗** Run：`uv run pytest tests/test_database.py -q`
  Expected：FAIL（`ImportError: cannot import name 'Database'`）。

- [ ] **Step 4：實作** 於 `src/kinsun/db.py`，在檔案 import 區與 `connect` 之間加入新成員（`MEMORY_DDL`…`ensure_schema` 全部保留不動）。檔頭 import 改為：
```python
"""共用 Postgres 連線、連線池與建表 DDL（Supabase）。"""

from __future__ import annotations

from collections.abc import Callable, Iterator
from contextlib import contextmanager
from typing import Protocol

import psycopg
from psycopg_pool import ConnectionPool
```
於 `ensure_schema` 之後（檔案末尾）加：
```python
class StoreError(Exception):
    """資料庫存取失敗（連線／執行／交易）；各 store 會翻成自己的領域錯誤。"""


class Executor(Protocol):
    """可執行 SQL 的對象：Database 本身或交易內的單一連線。"""

    def execute(self, sql: str, params: tuple = ()) -> None: ...
    def query(self, sql: str, params: tuple = ()) -> list[tuple]: ...
    def query_one(self, sql: str, params: tuple = ()) -> tuple | None: ...


class _ConnExecutor:
    """包一條交易連線並提供 Executor 介面（不自行 commit；交易結束統一提交）。"""

    def __init__(self, conn: psycopg.Connection) -> None:
        self._conn = conn

    def execute(self, sql: str, params: tuple = ()) -> None:
        self._conn.execute(sql, params)

    def query(self, sql: str, params: tuple = ()) -> list[tuple]:
        return self._conn.execute(sql, params).fetchall()

    def query_one(self, sql: str, params: tuple = ()) -> tuple | None:
        return self._conn.execute(sql, params).fetchone()


class Database:
    """連線池 ＋ 交易 ＋ 錯誤翻譯。所有方法失敗丟 StoreError。"""

    def __init__(self, pool: ConnectionPool) -> None:
        self._pool = pool

    @classmethod
    def open(cls, url: str, *, min_size: int = 1, max_size: int = 5) -> Database:
        return cls(ConnectionPool(url, min_size=min_size, max_size=max_size, open=True))

    def close(self) -> None:
        self._pool.close()

    def execute(self, sql: str, params: tuple = ()) -> None:
        try:
            with self._pool.connection() as conn:
                conn.execute(sql, params)
                conn.commit()
        except Exception as exc:  # noqa: BLE001 - 一律翻成 StoreError
            raise StoreError(str(exc)) from exc

    def query(self, sql: str, params: tuple = ()) -> list[tuple]:
        try:
            with self._pool.connection() as conn:
                return conn.execute(sql, params).fetchall()
        except Exception as exc:  # noqa: BLE001
            raise StoreError(str(exc)) from exc

    def query_one(self, sql: str, params: tuple = ()) -> tuple | None:
        try:
            with self._pool.connection() as conn:
                return conn.execute(sql, params).fetchone()
        except Exception as exc:  # noqa: BLE001
            raise StoreError(str(exc)) from exc

    @contextmanager
    def transaction(self) -> Iterator[Executor]:
        try:
            with self._pool.connection() as conn, conn.transaction():
                yield _ConnExecutor(conn)
        except StoreError:
            raise
        except Exception as exc:  # noqa: BLE001
            raise StoreError(str(exc)) from exc


class _Errors:
    """把內層 Executor 丟的 StoreError 翻成某 store 的領域錯誤；本身也是 Executor。"""

    def __init__(self, inner: Executor, wrap: Callable[[str], Exception]) -> None:
        self._inner = inner
        self._wrap = wrap

    def execute(self, sql: str, params: tuple = ()) -> None:
        try:
            self._inner.execute(sql, params)
        except StoreError as exc:
            raise self._wrap(str(exc)) from exc

    def query(self, sql: str, params: tuple = ()) -> list[tuple]:
        try:
            return self._inner.query(sql, params)
        except StoreError as exc:
            raise self._wrap(str(exc)) from exc

    def query_one(self, sql: str, params: tuple = ()) -> tuple | None:
        try:
            return self._inner.query_one(sql, params)
        except StoreError as exc:
            raise self._wrap(str(exc)) from exc

    @contextmanager
    def transaction(self) -> Iterator[Executor]:
        try:
            with self._inner.transaction() as tx:  # type: ignore[attr-defined]
                yield _Errors(tx, self._wrap)
        except StoreError as exc:
            raise self._wrap(str(exc)) from exc
```

- [ ] **Step 5：確認通過** Run：`uv run pytest tests/test_database.py -q` → PASS
- [ ] **Step 6：commit**
```bash
git add pyproject.toml uv.lock src/kinsun/db.py tests/test_database.py
git commit -m "feat: Database 深模組（連線池+交易+StoreError 翻譯）"
```

---

## Task 2：PgMemoryStore 改用 Database

**Files:**
- Modify: `src/kinsun/memory/store.py`
- Modify: `tests/test_pg_memory_store.py`

**Interfaces:**
- Consumes：`Database`、`_Errors`、`StoreError`（Task 1）
- Produces：`PgMemoryStore(db: Database, clock, max_turns=20)`（建構參數由 `database_url` 改為 `db`；對外方法 `append/recent/previous_day/sessions/last_active` 不變）

- [ ] **Step 1：改整合測試** 於 `tests/test_pg_memory_store.py` 的 `_store()`：
```python
def _store():
    from kinsun.db import Database, ensure_schema
    from kinsun.memory.store import PgMemoryStore

    url = os.environ["DATABASE_URL"]
    ensure_schema(url)
    tz = ZoneInfo("Asia/Taipei")
    return PgMemoryStore(Database.open(url), clock=lambda: datetime.now(tz), max_turns=20)
```

- [ ] **Step 2：確認失敗** Run：`KINSUN_IT=0 uv run pytest tests/test_pg_memory_store.py -q`
  Expected：整個檔 SKIP（無雲端 key 時；改 store 的型別錯誤要等實作後才驗，整合測試在 CI 為 SKIP，本步僅確認改動不破壞收集）。

- [ ] **Step 3：實作** 改 `src/kinsun/memory/store.py`：import 區把 `from kinsun.db import connect` 改為 `from kinsun.db import Database`；`PgMemoryStore` 全體改為：
```python
class PgMemoryStore:
    """短期記憶的 Postgres（Supabase）實作；介面同 MemoryStore。"""

    def __init__(
        self, db: Database, clock: Callable[[], datetime], max_turns: int = 20
    ) -> None:
        self._db = _Errors(db, lambda m: MemoryError(f"記憶存取失敗：{m}"))
        self._clock = clock
        self._max_turns = max_turns

    def append(self, session_id: str, message: Message) -> None:
        created_at = self._clock().timestamp()
        self._db.execute(
            "INSERT INTO turns (session_id, role, text, created_at) VALUES (%s, %s, %s, %s)",
            (session_id, message.role, message.text, created_at),
        )

    def recent(self, session_id: str) -> list[Message]:
        start = self._start_of_today()
        rows = self._db.query(
            "SELECT role, text FROM turns WHERE session_id = %s AND created_at >= %s "
            "ORDER BY created_at DESC, id DESC LIMIT %s",
            (session_id, start, self._max_turns),
        )
        return [Message(role=r, text=t) for r, t in reversed(rows)]

    def previous_day(self, session_id: str) -> list[Message]:
        """整理批次用：回傳『剛結束的那一天』整天的對話（時序由舊到新）。"""
        start, end = previous_day_bounds(self._clock())
        rows = self._db.query(
            "SELECT role, text FROM turns "
            "WHERE session_id = %s AND created_at >= %s AND created_at < %s "
            "ORDER BY created_at ASC, id ASC LIMIT %s",
            (session_id, start, end, self._max_turns),
        )
        return [Message(role=r, text=t) for r, t in rows]

    def sessions(self) -> list[str]:
        rows = self._db.query("SELECT DISTINCT session_id FROM turns ORDER BY session_id")
        return [r[0] for r in rows]

    def last_active(self, session_id: str) -> float | None:
        row = self._db.query_one(
            "SELECT MAX(created_at) FROM turns WHERE session_id = %s AND role = 'user'",
            (session_id,),
        )
        return row[0] if row and row[0] is not None else None

    def _start_of_today(self) -> float:
        now = self._clock()
        midnight = now.replace(hour=0, minute=0, second=0, microsecond=0)
        return midnight.timestamp()
```
於 import 區補 `from kinsun.db import Database, _Errors`（`MemoryError` 已定義於本檔，保留）。

- [ ] **Step 4：確認** Run：`uv run pytest tests/test_memory_window.py tests/test_pg_memory_store.py -q`、`uv run ruff check src/kinsun/memory/store.py`
  Expected：window 測試（用 `FakeMemoryStore`，不受影響）PASS；Pg 整合 SKIP；ruff 通過。
- [ ] **Step 5：commit**
```bash
git add src/kinsun/memory/store.py tests/test_pg_memory_store.py
git commit -m "refactor: PgMemoryStore 改坐在 Database 深模組上"
```

---

## Task 3：PgScheduleStateStore 改用 Database

**Files:**
- Modify: `src/kinsun/scheduler/state.py`
- Modify: `tests/test_pg_scheduler_state.py`

**Interfaces:**
- Consumes：`Database`、`_Errors`
- Produces：`PgScheduleStateStore(db: Database, tz)`（建構由 `database_url` 改為 `db`；`get_last_run/set_last_run` 不變）

- [ ] **Step 1：改整合測試** 於 `tests/test_pg_scheduler_state.py` 找出建構 `PgScheduleStateStore(url, tz)` 之處，改為 `PgScheduleStateStore(Database.open(url), tz)`，並把 `from kinsun.db import ensure_schema` 補成 `from kinsun.db import Database, ensure_schema`。

- [ ] **Step 2：實作** 改 `src/kinsun/scheduler/state.py`：import `from kinsun.db import connect` → `from kinsun.db import Database, _Errors`；`PgScheduleStateStore` 改為：
```python
class PgScheduleStateStore:
    """排程狀態的 Postgres（Supabase）實作；介面同 ScheduleStateStore。"""

    def __init__(self, db: Database, tz: tzinfo) -> None:
        self._db = _Errors(db, lambda m: ScheduleStateError(f"排程狀態存取失敗：{m}"))
        self._tz = tz

    def get_last_run(self, job_name: str) -> datetime | None:
        row = self._db.query_one(
            "SELECT last_run_at FROM scheduler_state WHERE job_name = %s",
            (job_name,),
        )
        if row is None or row[0] is None:
            return None
        return datetime.fromtimestamp(row[0], self._tz)

    def set_last_run(self, job_name: str, when: datetime) -> None:
        self._db.execute(
            "INSERT INTO scheduler_state (job_name, last_run_at) VALUES (%s, %s) "
            "ON CONFLICT (job_name) DO UPDATE SET last_run_at = EXCLUDED.last_run_at",
            (job_name, when.timestamp()),
        )
```

- [ ] **Step 3：確認** Run：`uv run pytest tests/test_scheduler_state.py tests/test_pg_scheduler_state.py -q`、`uv run ruff check src/kinsun/scheduler/state.py`
  Expected：fake 版（`FakeScheduleStateStore`）PASS；Pg 整合 SKIP；ruff 通過。
- [ ] **Step 4：commit**
```bash
git add src/kinsun/scheduler/state.py tests/test_pg_scheduler_state.py
git commit -m "refactor: PgScheduleStateStore 改坐在 Database 深模組上"
```

---

## Task 4：PgMedicationStore 改用 Database（停丟 AccountError）

**Files:**
- Modify: `src/kinsun/medication/store.py`
- Modify: `tests/test_medication_store.py`

**Interfaces:**
- Consumes：`Database`、`_Errors`
- Produces：`MedicationError(Exception)`；`PgMedicationStore(db: Database)`（建構由 `database_url` 改為 `db`；`add/list_for_elder/list_for_slot/remove` 不變）

- [ ] **Step 1：改整合測試** 於 `tests/test_medication_store.py` 的 `test_pg_store_round_trip`：
```python
    from kinsun.db import Database, ensure_schema
    from kinsun.medication.store import PgMedicationStore

    url = os.environ["DATABASE_URL"]
    ensure_schema(url)
    store = PgMedicationStore(Database.open(url))
```

- [ ] **Step 2：實作** 改 `src/kinsun/medication/store.py`：import `from kinsun.accounts.repository import AccountError` 與 `from kinsun.db import connect` 改為 `from kinsun.db import Database, _Errors`；新增本檔自有錯誤並改實作：
```python
"""用藥提醒儲存：Protocol 與 Postgres 實作。"""

from __future__ import annotations

from typing import Protocol

from kinsun.db import Database, _Errors
from kinsun.medication.models import Medication, MedicationSlot


class MedicationError(Exception):
    """用藥資料讀寫失敗。"""


class MedicationStore(Protocol):
    def add(self, med: Medication) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[Medication]: ...
    def list_for_slot(self, slot: MedicationSlot) -> list[Medication]: ...
    def remove(self, med_id: str) -> None: ...


class PgMedicationStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: MedicationError(f"用藥資料存取失敗：{m}"))

    def _to_med(self, row: tuple) -> Medication:
        med_id, elder_id, name, slots = row
        parsed = tuple(MedicationSlot(s) for s in slots.split(","))
        return Medication(med_id, elder_id, name, parsed)

    def add(self, med: Medication) -> None:
        self._db.execute(
            "INSERT INTO medications (med_id, elder_id, name, slots) "
            "VALUES (%s, %s, %s, %s) ON CONFLICT (med_id) DO UPDATE SET "
            "elder_id = EXCLUDED.elder_id, name = EXCLUDED.name, slots = EXCLUDED.slots",
            (med.med_id, med.elder_id, med.name, ",".join(s.value for s in med.slots)),
        )

    def list_for_elder(self, elder_id: str) -> list[Medication]:
        rows = self._db.query(
            "SELECT med_id, elder_id, name, slots FROM medications "
            "WHERE elder_id = %s ORDER BY name",
            (elder_id,),
        )
        return [self._to_med(r) for r in rows]

    def list_for_slot(self, slot: MedicationSlot) -> list[Medication]:
        rows = self._db.query(
            "SELECT med_id, elder_id, name, slots FROM medications WHERE slots LIKE %s",
            (f"%{slot.value}%",),
        )
        return [self._to_med(r) for r in rows]

    def remove(self, med_id: str) -> None:
        self._db.execute("DELETE FROM medications WHERE med_id = %s", (med_id,))
```

- [ ] **Step 3：確認** Run：`uv run pytest tests/test_medication_store.py -q`、`uv run ruff check src/kinsun/medication/store.py`
  Expected：fake 版 PASS；Pg 整合 SKIP；ruff 通過。
- [ ] **Step 4：commit**
```bash
git add src/kinsun/medication/store.py tests/test_medication_store.py
git commit -m "refactor: PgMedicationStore 改坐 Database 並改丟 MedicationError"
```

---

## Task 5：PgBindingSessionStore 改用 Database（停丟 AccountError）

**Files:**
- Modify: `src/kinsun/binding/session.py`
- Modify: `tests/test_binding_session.py`

**Interfaces:**
- Consumes：`Database`、`_Errors`
- Produces：`BindingSessionError(Exception)`；`PgBindingSessionStore(db: Database)`（建構由 `database_url` 改為 `db`；`get/save/delete` 不變）

- [ ] **Step 1：改整合測試** 於 `tests/test_binding_session.py` 找出建構 `PgBindingSessionStore(url)` 之處，改為 `PgBindingSessionStore(Database.open(url))`，並補 import `from kinsun.db import Database`（保留既有 `ensure_schema` import）。

- [ ] **Step 2：實作** 改 `src/kinsun/binding/session.py`：import `from kinsun.accounts.repository import AccountError`、`from kinsun.db import connect` 改為 `from kinsun.db import Database, _Errors`；新增自有錯誤、改實作：
```python
class BindingSessionError(Exception):
    """綁定會話讀寫失敗。"""


class BindingSessionStore(Protocol):
    def get(self, line_user_id: str) -> BindingSession | None: ...
    def save(self, session: BindingSession) -> None: ...
    def delete(self, line_user_id: str) -> None: ...


class PgBindingSessionStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: BindingSessionError(f"綁定會話存取失敗：{m}"))

    def get(self, line_user_id: str) -> BindingSession | None:
        rows = self._db.query(
            "SELECT line_user_id, state, data, updated_at "
            "FROM binding_sessions WHERE line_user_id = %s",
            (line_user_id,),
        )
        if not rows:
            return None
        line, state, data, updated = rows[0]
        return BindingSession(line, BindingState(state), json.loads(data), updated)

    def save(self, session: BindingSession) -> None:
        self._db.execute(
            "INSERT INTO binding_sessions (line_user_id, state, data, updated_at) "
            "VALUES (%s, %s, %s, %s) ON CONFLICT (line_user_id) DO UPDATE SET "
            "state = EXCLUDED.state, data = EXCLUDED.data, updated_at = EXCLUDED.updated_at",
            (
                session.line_user_id,
                session.state.value,
                json.dumps(session.data),
                session.updated_at,
            ),
        )

    def delete(self, line_user_id: str) -> None:
        self._db.execute(
            "DELETE FROM binding_sessions WHERE line_user_id = %s", (line_user_id,)
        )
```

- [ ] **Step 3：確認** Run：`uv run pytest tests/test_binding_session.py -q`、`uv run ruff check src/kinsun/binding/session.py`
  Expected：fake 版 PASS；Pg 整合 SKIP；ruff 通過。
- [ ] **Step 4：commit**
```bash
git add src/kinsun/binding/session.py tests/test_binding_session.py
git commit -m "refactor: PgBindingSessionStore 改坐 Database 並改丟 BindingSessionError"
```

---

## Task 6：PgAccountRepository 改用 Database + 交易支援

**Files:**
- Modify: `src/kinsun/accounts/repository.py`
- Modify: `tests/test_pg_account_repository.py`

**Interfaces:**
- Consumes：`Database`、`Executor`、`_Errors`
- Produces：`PgAccountRepository(db: Database)`；`AccountRepository` Protocol 新增 `transaction() -> ContextManager[Executor]`，5 個寫入方法新增關鍵字參數 `*, tx: Executor | None = None`（`save_elder/save_guardian/save_elder_guardian/save_consent/save_invite`）；讀取方法簽名不變

- [ ] **Step 1：改整合測試** 於 `tests/test_pg_account_repository.py`，三個測試裡 `repo = PgAccountRepository(url)` 全改為 `repo = PgAccountRepository(Database.open(url))`，並把各自的 `from kinsun.db import ensure_schema` 補成 `from kinsun.db import Database, ensure_schema`。

- [ ] **Step 2：實作** 改 `src/kinsun/accounts/repository.py`：import 區 `from kinsun.db import connect` → `from kinsun.db import Database, Executor, _Errors`，並加 `from collections.abc import Iterator`、`from contextlib import contextmanager`。`AccountRepository` Protocol 在末尾加：
```python
    def transaction(self) -> object: ...
```
5 個 `save_*` 在 Protocol 內加 `*, tx: Executor | None = None`（如 `def save_elder(self, elder: Elder, *, tx: Executor | None = None) -> None: ...`）。`PgAccountRepository` 改為（移除 `_exec`/`_query`，改用注入的 `Database`；寫入走 `(tx or self._db)`）：
```python
class PgAccountRepository:
    """帳號綁定的 Postgres（Supabase）實作；介面同 AccountRepository。"""

    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: AccountError(f"帳號存取失敗：{m}"))

    @contextmanager
    def transaction(self) -> Iterator[Executor]:
        with self._db.transaction() as tx:
            yield tx

    def save_elder(self, elder: Elder, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO elders (elder_id, name, line_user_id) VALUES (%s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET "
            "name = EXCLUDED.name, line_user_id = EXCLUDED.line_user_id",
            (elder.elder_id, elder.name, elder.line_user_id),
        )

    def get_elder(self, elder_id: str) -> Elder | None:
        rows = self._db.query(
            "SELECT elder_id, name, line_user_id FROM elders WHERE elder_id = %s", (elder_id,)
        )
        return Elder(*rows[0]) if rows else None

    def save_guardian(self, guardian: Guardian, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO guardians (guardian_id, line_user_id, name) VALUES (%s, %s, %s) "
            "ON CONFLICT (guardian_id) DO UPDATE SET "
            "line_user_id = EXCLUDED.line_user_id, name = EXCLUDED.name",
            (guardian.guardian_id, guardian.line_user_id, guardian.name),
        )

    def get_guardian_by_line(self, line_user_id: str) -> Guardian | None:
        rows = self._db.query(
            "SELECT guardian_id, line_user_id, name FROM guardians WHERE line_user_id = %s",
            (line_user_id,),
        )
        return Guardian(*rows[0]) if rows else None

    def get_guardian(self, guardian_id: str) -> Guardian | None:
        rows = self._db.query(
            "SELECT guardian_id, line_user_id, name FROM guardians WHERE guardian_id = %s",
            (guardian_id,),
        )
        return Guardian(*rows[0]) if rows else None

    def get_elder_by_line(self, line_user_id: str) -> Elder | None:
        rows = self._db.query(
            "SELECT elder_id, name, line_user_id FROM elders WHERE line_user_id = %s",
            (line_user_id,),
        )
        return Elder(*rows[0]) if rows else None

    def save_elder_guardian(self, eg: ElderGuardian, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO elder_guardians "
            "(elder_id, guardian_id, role, escalation_order, can_view_transcript) "
            "VALUES (%s, %s, %s, %s, %s) ON CONFLICT (elder_id, guardian_id) DO UPDATE SET "
            "role = EXCLUDED.role, escalation_order = EXCLUDED.escalation_order, "
            "can_view_transcript = EXCLUDED.can_view_transcript",
            (
                eg.elder_id,
                eg.guardian_id,
                eg.role.value,
                eg.escalation_order,
                bool(eg.can_view_transcript),
            ),
        )

    def _to_eg(self, row: tuple) -> ElderGuardian:
        elder_id, guardian_id, role, order, can_view = row
        return ElderGuardian(elder_id, guardian_id, Role(role), order, bool(can_view))

    def get_elder_guardian(self, elder_id: str, guardian_id: str) -> ElderGuardian | None:
        rows = self._db.query(
            "SELECT elder_id, guardian_id, role, escalation_order, can_view_transcript "
            "FROM elder_guardians WHERE elder_id = %s AND guardian_id = %s",
            (elder_id, guardian_id),
        )
        return self._to_eg(rows[0]) if rows else None

    def list_elder_guardians(self, elder_id: str) -> list[ElderGuardian]:
        rows = self._db.query(
            "SELECT elder_id, guardian_id, role, escalation_order, can_view_transcript "
            "FROM elder_guardians WHERE elder_id = %s ORDER BY escalation_order",
            (elder_id,),
        )
        return [self._to_eg(r) for r in rows]

    def elder_ids_of_guardian(self, guardian_id: str) -> list[str]:
        rows = self._db.query(
            "SELECT elder_id FROM elder_guardians WHERE guardian_id = %s ORDER BY elder_id",
            (guardian_id,),
        )
        return [r[0] for r in rows]

    def save_consent(self, consent: Consent, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO consents (elder_id, consent_by, version, granted_at, revoked_at) "
            "VALUES (%s, %s, %s, %s, %s) ON CONFLICT (elder_id) DO UPDATE SET "
            "consent_by = EXCLUDED.consent_by, version = EXCLUDED.version, "
            "granted_at = EXCLUDED.granted_at, revoked_at = EXCLUDED.revoked_at",
            (
                consent.elder_id,
                consent.consent_by.value,
                consent.version,
                consent.granted_at,
                consent.revoked_at,
            ),
        )

    def get_consent(self, elder_id: str) -> Consent | None:
        rows = self._db.query(
            "SELECT elder_id, consent_by, version, granted_at, revoked_at "
            "FROM consents WHERE elder_id = %s",
            (elder_id,),
        )
        if not rows:
            return None
        elder, by, version, granted, revoked = rows[0]
        return Consent(elder, ConsentBy(by), version, granted, revoked)

    def save_invite(self, invite: Invite, *, tx: Executor | None = None) -> None:
        (tx or self._db).execute(
            "INSERT INTO invites "
            "(code, elder_id, role, expires_at, max_attempts, attempts, used_at) "
            "VALUES (%s, %s, %s, %s, %s, %s, %s) ON CONFLICT (code) DO UPDATE SET "
            "elder_id = EXCLUDED.elder_id, role = EXCLUDED.role, "
            "expires_at = EXCLUDED.expires_at, max_attempts = EXCLUDED.max_attempts, "
            "attempts = EXCLUDED.attempts, used_at = EXCLUDED.used_at",
            (
                invite.code,
                invite.elder_id,
                invite.role.value,
                invite.expires_at,
                invite.max_attempts,
                invite.attempts,
                invite.used_at,
            ),
        )

    def get_invite(self, code: str) -> Invite | None:
        rows = self._db.query(
            "SELECT code, elder_id, role, expires_at, max_attempts, attempts, used_at "
            "FROM invites WHERE code = %s",
            (code,),
        )
        if not rows:
            return None
        c, elder, role, expires, max_a, attempts, used = rows[0]
        return Invite(c, elder, InviteRole(role), expires, max_a, attempts, used)
```

- [ ] **Step 3：確認** Run：`uv run pytest tests/test_pg_account_repository.py -q`、`uv run ruff check src/kinsun/accounts/repository.py`
  Expected：Pg 整合 SKIP；ruff 通過。
- [ ] **Step 4：commit**
```bash
git add src/kinsun/accounts/repository.py tests/test_pg_account_repository.py
git commit -m "refactor: PgAccountRepository 改坐 Database 並加交易支援（tx 參數）"
```

---

## Task 7：AccountService 多寫走交易 + 更新 fake

**Files:**
- Modify: `src/kinsun/accounts/service.py`
- Modify: `tests/fakes.py`
- Modify: `tests/test_accounts_service.py`

**Interfaces:**
- Consumes：`AccountRepository.transaction()`、`save_*(..., tx=...)`（Task 6）
- Produces：`AccountService.create_elder` / `redeem_invite` 內以 `with self._repo.transaction() as tx` 包多寫；`FakeAccountRepository.transaction()`（yield None）與 5 個 `save_*` 接受 `*, tx=None`

- [ ] **Step 1：失敗測試** 於 `tests/test_accounts_service.py` 末尾加（驗證仍走原子寫、且 fake 提供 transaction）：
```python
def test_create_elder_uses_repo_transaction():
    repo = FakeAccountRepository()
    svc = _service(repo)
    elder = svc.create_elder("U-son", "兒子", "阿公")
    # create_elder 同時寫 elder 與 elder_guardian，且兩者皆落地
    assert repo.get_elder(elder.elder_id).name == "阿公"
    assert repo.list_elder_guardians(elder.elder_id)[0].role.value == "primary"
```
（`_service` 與既有測試共用；若 `FakeAccountRepository` 尚未支援 `transaction()`，下一步會失敗。）

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_accounts_service.py::test_create_elder_uses_repo_transaction -q`
  Expected：FAIL（`AttributeError: 'FakeAccountRepository' object has no attribute 'transaction'`，待 Step 3/4 修）。

- [ ] **Step 3：更新 fake** 於 `tests/fakes.py`，檔頭加 `from contextlib import contextmanager`；`FakeAccountRepository` 內加 `transaction()` 並把 5 個 `save_*` 加 `*, tx=None`：
```python
    @contextmanager
    def transaction(self):
        yield None

    def save_elder(self, elder, *, tx=None):
        self.elders[elder.elder_id] = elder

    def save_guardian(self, g, *, tx=None):
        self.guardians[g.guardian_id] = g
        self.guardians_by_line[g.line_user_id] = g

    def save_elder_guardian(self, eg, *, tx=None):
        self.elder_guardians[(eg.elder_id, eg.guardian_id)] = eg

    def save_consent(self, c, *, tx=None):
        self.consents[c.elder_id] = c

    def save_invite(self, i, *, tx=None):
        self.invites[i.code] = i
```
（取代原本同名的 5 個方法。）

- [ ] **Step 4：實作 service 交易** 改 `src/kinsun/accounts/service.py`：`_guardian_for` 加 `tx` 透傳；`create_elder`、`redeem_invite` 用交易包多寫：
```python
    def _guardian_for(self, line_user_id: str, name: str, *, tx=None) -> Guardian:
        existing = self._repo.get_guardian_by_line(line_user_id)
        if existing is not None:
            return existing
        guardian = Guardian(self._new_id(), line_user_id, name)
        self._repo.save_guardian(guardian, tx=tx)
        return guardian

    def create_elder(self, guardian_line_id: str, guardian_name: str, elder_name: str) -> Elder:
        elder = Elder(self._new_id(), elder_name)
        with self._repo.transaction() as tx:
            guardian = self._guardian_for(guardian_line_id, guardian_name, tx=tx)
            self._repo.save_elder(elder, tx=tx)
            self._repo.save_elder_guardian(
                ElderGuardian(elder.elder_id, guardian.guardian_id, Role.PRIMARY, 1, True), tx=tx
            )
        return elder
```
`redeem_invite` 把成功路徑的多寫包進交易（前置驗證與 `_fail` 留在交易外）：
```python
    def redeem_invite(self, code: str, line_user_id: str, *, consent_by: ConsentBy) -> None:
        invite = self._repo.get_invite(code)
        if invite is None:
            raise InviteError("not_found")
        now = self._clock()
        if invite.used_at is not None:
            raise InviteError("used")
        if invite.attempts >= invite.max_attempts:
            self._fail(invite, "too_many_attempts")
        if now.timestamp() > invite.expires_at:
            self._fail(invite, "expired")

        with self._repo.transaction() as tx:
            if invite.role == InviteRole.ELDER:
                elder = self._repo.get_elder(invite.elder_id)
                if elder is None:
                    raise InviteError("not_found")
                self._repo.save_elder(Elder(elder.elder_id, elder.name, line_user_id), tx=tx)
                self._repo.save_consent(
                    Consent(invite.elder_id, consent_by, CONSENT_VERSION, now.timestamp()), tx=tx
                )
            else:
                guardian = self._guardian_for(line_user_id, "", tx=tx)
                order = max(
                    (eg.escalation_order for eg in self._repo.list_elder_guardians(invite.elder_id)),
                    default=0,
                )
                self._repo.save_elder_guardian(
                    ElderGuardian(
                        invite.elder_id, guardian.guardian_id, Role.GUARDIAN, order + 1, False
                    ),
                    tx=tx,
                )
            self._repo.save_invite(
                Invite(
                    invite.code,
                    invite.elder_id,
                    invite.role,
                    invite.expires_at,
                    invite.max_attempts,
                    invite.attempts + 1,
                    now.timestamp(),
                ),
                tx=tx,
            )
```

- [ ] **Step 5：確認通過** Run：`uv run pytest tests/test_accounts_service.py tests/test_binding_flow.py tests/test_medication_flow.py -q`
  Expected：全部 PASS（fake repo 走 no-op transaction；綁定/用藥流程不受影響）。
- [ ] **Step 6：commit**
```bash
git add src/kinsun/accounts/service.py tests/fakes.py tests/test_accounts_service.py
git commit -m "feat: create_elder / redeem_invite 以交易保障多寫原子性"
```

---

## Task 8：組裝根建 Database + 池生命週期 + 整合測試收尾

**Files:**
- Modify: `src/kinsun/app.py`
- Modify: `src/kinsun/scheduler/worker.py`

**Interfaces:**
- Consumes：`Database.open`、`Database.close`、各 store 的新建構子（吃 `db`）
- Produces：`build_app()` 內建立單一 `Database`、注入全部 store、於 FastAPI lifespan 關閉池；`build_scheduler` 內建立單一 `Database`、注入全部 store；`serve` 結束時關池

- [ ] **Step 1：改 app.py** import 區加 `from contextlib import asynccontextmanager`、`from kinsun.db import Database, ensure_schema`（取代原 `from kinsun.db import ensure_schema`）。`build_app` 內把 `ensure_schema(...)` 之後改為先建 `db`，所有 `Pg*(settings.database_url, ...)` 改注入 `db`：
```python
    settings = load_settings(os.environ)
    tz = ZoneInfo(settings.timezone)
    ensure_schema(settings.database_url)
    db = Database.open(settings.database_url)
    memory = PgMemoryStore(db, clock=lambda: datetime.now(tz), max_turns=settings.memory_max_turns)
    ...
    accounts = AccountService(
        PgAccountRepository(db),
        clock=lambda: datetime.now(tz),
        ttl_hours=settings.invite_ttl_hours,
        max_attempts=settings.invite_max_attempts,
    )
    ...
    binding_sessions = PgBindingSessionStore(db)
    medications = MedicationService(PgMedicationStore(db))
```
`create_app(...)` 呼叫前，建立關池的 lifespan 並交給 `create_app`（見 Step 2）：把 `build_app` 結尾改為：
```python
    return create_app(
        parser=parser,
        pipeline=pipeline,
        messenger=messenger,
        binding=binding,
        gate=gate,
        on_shutdown=db.close,
    )
```

- [ ] **Step 2：webhook 接受 on_shutdown** 改 `src/kinsun/channels/line/webhook.py` 的 `create_app`，新增可選 `on_shutdown` 並掛上 FastAPI lifespan：
```python
from collections.abc import Callable
from contextlib import asynccontextmanager


def create_app(
    *, parser, pipeline, messenger, binding, gate, on_shutdown: Callable[[], None] | None = None
) -> FastAPI:
    @asynccontextmanager
    async def lifespan(_app: FastAPI):
        yield
        if on_shutdown is not None:
            on_shutdown()

    app = FastAPI(lifespan=lifespan)
    ...
```
（其餘 `create_app` 內容不變；型別註記 `pipeline: VoicePipeline` 等保留。）

- [ ] **Step 3：改 worker.py** import 區把 `from kinsun.db import ensure_schema` 改為 `from kinsun.db import Database, ensure_schema`。`build_scheduler` 內 `ensure_schema(...)` 之後建 `db`，所有 store 注入 `db`：
```python
    ensure_schema(settings.database_url)
    db = Database.open(settings.database_url)
    memory = PgMemoryStore(db, clock=lambda: datetime.now(tz), max_turns=settings.memory_max_turns)
    ...
    accounts = AccountService(PgAccountRepository(db), clock=clock)
    med_store = PgMedicationStore(db)
    ...
    state = PgScheduleStateStore(db, tz)
    return Scheduler(jobs, clock, state)
```
（`build_scheduler` 仍回傳 `Scheduler`；池由行程存活期持有，`main` 結束時關閉——見 Step 4。注意：若要顯式關池，可讓 `build_scheduler` 回傳 `(scheduler, db)`；本案採最小改動，於 `main` 以 try/finally 包 `serve` 並在 finally 關閉由 `build_scheduler` 建立的 db——為此把 `db` 提升為回傳值。）

  具體：把 `build_scheduler` 簽名與回傳改為同時回 `db`：
```python
def build_scheduler(settings: Settings, *, clock: Callable[[], datetime]) -> tuple[Scheduler, Database]:
    ...
    return Scheduler(jobs, clock, state), db
```
`main` 改為：
```python
def main() -> int:
    settings = load_settings(os.environ)
    tz = ZoneInfo(settings.timezone)
    scheduler, db = build_scheduler(settings, clock=lambda: datetime.now(tz))
    print(...)
    try:
        serve(scheduler, tick_seconds=settings.scheduler_tick_seconds)
    finally:
        db.close()
    return 0
```

- [ ] **Step 4：語法 + ruff + 全測試** Run：
  `uv run python -c "import ast, pathlib; [ast.parse(pathlib.Path(p).read_text(encoding='utf-8')) for p in ['src/kinsun/app.py','src/kinsun/scheduler/worker.py','src/kinsun/channels/line/webhook.py']]; print('ok')"`、
  `uv run ruff check .`、`uv run pytest -q`
  Expected：`ok`；ruff 通過；pytest 全綠（Pg 整合 SKIP）。
- [ ] **Step 5：commit**
```bash
git add src/kinsun/app.py src/kinsun/scheduler/worker.py src/kinsun/channels/line/webhook.py
git commit -m "feat: 組裝根建立 Database 連線池並於關閉時釋放"
```

---

## Task 9：品質收尾與上線

- [ ] **Step 1：全品質** Run：`uv run pytest -q`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run pre-commit run --all-files` → 全綠（整合 SKIP）
- [ ] **Step 2：（可選）整合驗證** 若有雲端 `DATABASE_URL`：`KINSUN_IT=1 uv run pytest tests/test_pg_memory_store.py tests/test_pg_account_repository.py tests/test_pg_scheduler_state.py tests/test_medication_store.py -q` → PASS
- [ ] **Step 3：確認 `git status` 僅含預期變更**
- [ ] **Step 4：push + PR**（沿用既有流程，待使用者指示）
```bash
git push origin Leo
gh pr create --base main --head Leo --title "refactor: Postgres 存取深模組（連線池+交易）" --body "<摘要>"
```

---

## Self-Review

**Spec coverage：** 連線池 → Task 1（`Database.open` + `psycopg_pool`）、Task 8（組裝根各建一個、關閉）。單一 `StoreError` + 各 store 翻領域錯誤 → Task 1（`_Errors`）+ Task 2–6（各 store 注入自己的 wrap）。停止 `AccountError` 洩漏 → Task 4（`MedicationError`）、Task 5（`BindingSessionError`）。交易原子性 → Task 1（`transaction`）+ Task 6（`tx` 參數/`transaction()`）+ Task 7（`create_elder`/`redeem_invite`）。樣板消除 → Task 2–6 各 store 去除 try-except/connect。

**Placeholder scan：** PR body `<摘要>` 於執行時填入；整合測試各檔的「找出建構處」於該檔僅一處，依 Step 指示替換；其餘步驟皆含完整程式碼，無 TODO。

**Type consistency：** `Database(pool)` / `Database.open(url, *, min_size, max_size)` / `Database.close()` / `execute(sql, params=())` / `query(...) -> list[tuple]` / `query_one(...) -> tuple | None` / `transaction() -> Executor`；`_Errors(inner, wrap)` 介面同 `Executor` 並含 `transaction`；各 `Pg*Store(db, ...)` 建構參數一致；`AccountRepository.save_*(..., *, tx=None)` 與 `transaction()` 跨 Task 6/7 一致；`FakeAccountRepository.transaction()`/`save_*(..., tx=None)` 與真實 repo 對齊。`create_app(..., on_shutdown=None)` 於 app.py 呼叫與 webhook 定義一致；`build_scheduler -> (Scheduler, Database)` 於 worker.main 解構一致。
