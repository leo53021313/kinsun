# 關鍵健康事實每輪必帶（固定健康查詢增補）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `Mem0LongTermStore.search` 每輪除 user 文字檢索外，固定再用健康關鍵詞檢索一次，合併去重，讓用藥/慢性病等穩定事實每輪都浮現。

**Architecture:** 改動封裝在 `Mem0LongTermStore` 內：新增 `HEALTH_QUERY` 常數、`health_top_k` 建構參數、私有 `_search_raw`（單次檢索、fail-safe 回 `[]`）與 `_dedup`（依 mem0 id 去重）。`search` 對外簽名與回傳型別不變，故 Protocol／`MemoryContext`／`CareAgent`／`FakeLongTermStore` 全部不動。

**Tech Stack:** Python 3.12、mem0ai 2.0.10、pytest。

## Global Constraints

- 一律台灣繁體中文（文件、註解、commit）。
- 最小修改：只動 `src/kinsun/longterm/store.py` 與 `tests/test_longterm_store.py`。
- fail-safe：兩次檢索各自降級回 `[]`，任一/全部失敗都不中斷對話。
- 不新增環境變數、不新增依賴；`health_top_k` 為建構參數（預設 3）。
- 不改 `LongTermStore` Protocol、`MemoryContext`、`CareAgent`、`FakeLongTermStore`。
- commit 規範：`feat/fix/docs/refactor/test/chore`，結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 品質閘門：`uv run pytest`、`uv run ruff check .`、`uv run ruff format --check .`、pre-commit 全綠。
- 設計依據：[docs/superpowers/specs/2026-06-29-關鍵健康事實每輪必帶-design.md](../specs/2026-06-29-關鍵健康事實每輪必帶-design.md)。

---

### Task 1: Mem0LongTermStore 固定健康查詢增補

**Files:**
- Modify: `src/kinsun/longterm/store.py`
- Test: `tests/test_longterm_store.py`

**Interfaces:**
- Consumes: `self._memory.search(query, user_id=..., limit=...)`（mem0；回 `{"results": [...]}` 或 list）、既有 `_format_memories_for_prompt(result: dict) -> str`。
- Produces:
  - 模組常數 `HEALTH_QUERY: str = "用藥 慢性病 過敏 回診 健康狀況"`。
  - `Mem0LongTermStore(memory, *, top_k=5, health_top_k=3)`。
  - `search(session_id, query, *, top_k=None) -> str`（簽名不變，行為增補）。

- [ ] **Step 1: Write the failing tests**

在 `tests/test_longterm_store.py`：把 import 行改為
```python
from kinsun.longterm.store import HEALTH_QUERY, Mem0LongTermStore, _format_memories_for_prompt
```
在 `_FakeMem0` 之後新增一個依 query 回應的 fake：
```python
class _ByQueryMem0:
    """依 query 回不同 results 的 fake；值為 "RAISE" 則該 query 拋例外。"""

    def __init__(self, mapping):
        self.mapping = mapping
        self.calls = []

    def search(self, query, user_id=None, limit=None, **kwargs):
        self.calls.append((query, limit))
        entry = self.mapping.get(query)
        if entry == "RAISE":
            raise RuntimeError("boom")
        return entry or {"results": []}
```
於檔尾新增四個測試：
```python
def test_search_always_includes_health_facts():
    mem = _ByQueryMem0(
        {
            "今天天氣": {"results": [{"id": "1", "memory": "喜歡晴天"}]},
            HEALTH_QUERY: {"results": [{"id": "2", "memory": "有高血壓，每天吃藥"}]},
        }
    )
    out = Mem0LongTermStore(mem).search("sess1", "今天天氣")
    assert "喜歡晴天" in out
    assert "有高血壓" in out


def test_search_dedups_overlapping_id():
    mem = _ByQueryMem0(
        {
            "頭痛": {"results": [{"id": "1", "memory": "有高血壓"}]},
            HEALTH_QUERY: {"results": [{"id": "1", "memory": "有高血壓"}]},
        }
    )
    out = Mem0LongTermStore(mem).search("sess1", "頭痛")
    assert out.count("有高血壓") == 1


def test_search_uses_top_k_and_health_top_k():
    mem = _ByQueryMem0({})
    Mem0LongTermStore(mem, top_k=5, health_top_k=2).search("sess1", "天氣")
    assert ("天氣", 5) in mem.calls
    assert (HEALTH_QUERY, 2) in mem.calls


def test_health_search_failure_keeps_user_results():
    mem = _ByQueryMem0(
        {
            "頭痛": {"results": [{"id": "1", "memory": "今天頭痛"}]},
            HEALTH_QUERY: "RAISE",
        }
    )
    out = Mem0LongTermStore(mem).search("sess1", "頭痛")
    assert "今天頭痛" in out
```

- [ ] **Step 2: Run tests to verify they fail**

Run: `uv run pytest tests/test_longterm_store.py -v`
Expected: FAIL — 新測試在 `from ... import HEALTH_QUERY` 即 `ImportError`（常數尚未存在）。

- [ ] **Step 3: Implement in store.py**

在 `src/kinsun/longterm/store.py`，於 `_PREFIX = ...` 之後新增模組常數：
```python
HEALTH_QUERY = "用藥 慢性病 過敏 回診 健康狀況"
```
把 `Mem0LongTermStore` 的 `__init__` 與 `search` 改為（`add`、`_format_memories_for_prompt`、`_PREFIX` 不變）：
```python
class Mem0LongTermStore:
    def __init__(self, memory, *, top_k: int = 5, health_top_k: int = 3) -> None:
        self._memory = memory
        self._top_k = top_k
        self._health_top_k = health_top_k

    def add(
        self, session_id, messages, *, provenance=prov.SELF_CLAIMED
    ) -> None:
        payload = [{"role": m.role, "content": m.text} for m in messages]
        self._memory.add(payload, user_id=session_id, metadata={"provenance": provenance})

    def _search_raw(self, query: str, session_id: str, top_k: int) -> list[dict]:
        try:
            result = self._memory.search(query, user_id=session_id, limit=top_k)
        except Exception as exc:  # noqa: BLE001 — 記憶壞掉不可中斷對話
            logger.warning("長期記憶檢索失敗，退化為無記憶：%s", exc)
            return []
        if isinstance(result, dict):
            return result.get("results") or []
        return result or []

    @staticmethod
    def _dedup(items: list[dict]) -> list[dict]:
        seen = set()
        out = []
        for item in items:
            key = item.get("id") or item.get("memory") or item.get("text")
            if key in seen:
                continue
            seen.add(key)
            out.append(item)
        return out

    def search(self, session_id: str, query: str, *, top_k: int | None = None) -> str:
        user_items = self._search_raw(query, session_id, top_k or self._top_k)
        health_items = self._search_raw(HEALTH_QUERY, session_id, self._health_top_k)
        merged = self._dedup(user_items + health_items)
        return _format_memories_for_prompt({"results": merged})
```

> 保留 `add` 原本的 `provenance=prov.SELF_CLAIMED` 預設與型別註記（若原檔有 `messages: list[Message]` 等標註，照原樣保留，本步只示意結構）。

- [ ] **Step 4: Run tests to verify they pass**

Run: `uv run pytest tests/test_longterm_store.py -v`
Expected: PASS（4 新測試 + 既有 5 個皆綠；`test_search_returns_formatted_string`／`test_search_failsafe_returns_blank` 向後相容）。

- [ ] **Step 5: Run full quality gate**

Run: `uv run pytest`
Expected: 全部通過（+4 passed）。

Run: `uv run ruff check . && uv run ruff format --check .`
Expected: 無錯誤、皆已格式化。

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/longterm/store.py tests/test_longterm_store.py
git commit -m "feat: 長期記憶每輪固定增補健康事實檢索（用藥/慢性病不漏）

Mem0LongTermStore.search 加 HEALTH_QUERY 檢索 + 依 id 去重，
健康關鍵事實即使與當下話題無關也浮現。fail-safe 降級。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage：**
- spec §3/§4 固定健康查詢增補（user 檢索 + HEALTH_QUERY 檢索、去重、合併格式化）→ Task 1 Step 3 ✅
- spec §4 `HEALTH_QUERY` 常數、`health_top_k=3` 建構參數、`_search_raw`/`_dedup` → Task 1 Step 3 ✅
- spec §5 fail-safe（各自降級回 []）→ `_search_raw` try/except ✅
- spec §6 測試（健康必帶、去重、上限與雙檢索、健康失敗隔離、既有相容）→ Task 1 Step 1 ✅
- spec §2.2 不動 Protocol/MemoryContext/CareAgent/FakeLongTermStore → 計畫未觸及 ✅

**2. Placeholder scan：** 無 TBD/TODO；測試與實作皆完整程式碼 ✅

**3. Type consistency：**
- `HEALTH_QUERY: str`、`Mem0LongTermStore(memory, *, top_k=5, health_top_k=3)`、`search(session_id, query, *, top_k=None) -> str`、`_search_raw(query, session_id, top_k) -> list[dict]`、`_dedup(items) -> list[dict]` 在 spec／plan／測試一致 ✅
- 既有測試呼叫 `store.search("興趣", "sess1")` 仍符合 `(session_id, query)` 位置參數、回 str ✅
