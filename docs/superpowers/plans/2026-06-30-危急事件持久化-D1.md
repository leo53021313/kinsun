# 危急事件持久化（D1）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 tier≥L2 的危急事件持久化到 `risk_events`，作為日後健康報告（E）的資料源；落庫失敗不中斷對話。

**Architecture:** 新增 `safety/events.py`（`RiskEvent` + `RiskEventStore` + `PgRiskEventStore`）與 `risk_events` 表；`VoicePipeline` 在既有 tier≥L2 通知分支內 fail-safe 記錄一筆。

**Tech Stack:** Python 3.12 / psycopg / FastAPI / pytest / ruff。

## Global Constraints

- 一律台灣繁體中文：文件、註解、commit 訊息。
- Python 3.12；ruff `E,W,F,I,B,UP`，line-length 100；不新增第三方套件。
- 落庫失敗只 log、續跑：對話不中斷、家屬通知照發（fail-safe）。
- store 走 Protocol + 注入 fake；clock/new_id 注入以利測試。
- 記錄門檻＝通知門檻 tier≥L2；以 `session_id`（長輩 LINE id）為鍵。
- commit 型別 `feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 單元測試全離線（注入 fake）；PG round-trip 以 `KINSUN_IT=1` opt-in。

---

## 檔案結構（File Structure）

**新增：** `src/kinsun/safety/events.py`、`tests/test_risk_events.py`。
**修改：** `src/kinsun/db.py`（DDL）、`src/kinsun/pipeline.py`（注入 + 落庫）、`src/kinsun/app.py`（接線）、`tests/fakes.py`（`FakeRiskEventStore`）、`tests/test_pipeline.py`（落庫/門檻/fail-safe）。

---

## Task 1：危急事件 model + 儲存 + DDL + Fake

**Files:**
- Create: `src/kinsun/safety/events.py`
- Modify: `src/kinsun/db.py`、`tests/fakes.py`
- Test: `tests/test_risk_events.py`

**Interfaces:**
- Produces:
  - `RiskEvent(event_id: str, session_id: str, tier: RiskTier, reason: str, created_at: float)`
  - `RiskEventError(Exception)`
  - `RiskEventStore` Protocol：`record(session_id, assessment)`、`list_for_session(session_id)->list[RiskEvent]`
  - `PgRiskEventStore(db, *, clock, new_id)`、`FakeRiskEventStore()`（`.recorded` 清單）

- [ ] **Step 1：建立 `src/kinsun/safety/events.py`**

```python
"""危急事件持久化：供日後健康報告查詢。"""

from __future__ import annotations

from collections.abc import Callable
from dataclasses import dataclass
from datetime import datetime
from typing import Protocol

from kinsun.db import Database, _Errors
from kinsun.safety.tiers import RiskAssessment, RiskTier


@dataclass(frozen=True)
class RiskEvent:
    event_id: str
    session_id: str
    tier: RiskTier
    reason: str
    created_at: float


class RiskEventError(Exception):
    """危急事件讀寫失敗。"""


class RiskEventStore(Protocol):
    def record(self, session_id: str, assessment: RiskAssessment) -> None: ...
    def list_for_session(self, session_id: str) -> list[RiskEvent]: ...


class PgRiskEventStore:
    def __init__(
        self, db: Database, *, clock: Callable[[], datetime], new_id: Callable[[], str]
    ) -> None:
        self._db = _Errors(db, lambda m: RiskEventError(f"危急事件存取失敗：{m}"))
        self._clock = clock
        self._new_id = new_id

    def record(self, session_id: str, assessment: RiskAssessment) -> None:
        self._db.execute(
            "INSERT INTO risk_events (event_id, session_id, tier, reason, created_at) "
            "VALUES (%s, %s, %s, %s, %s)",
            (
                self._new_id(),
                session_id,
                int(assessment.tier),
                assessment.reason,
                self._clock().timestamp(),
            ),
        )

    def list_for_session(self, session_id: str) -> list[RiskEvent]:
        rows = self._db.query(
            "SELECT event_id, session_id, tier, reason, created_at FROM risk_events "
            "WHERE session_id = %s ORDER BY created_at DESC",
            (session_id,),
        )
        return [RiskEvent(r[0], r[1], RiskTier(r[2]), r[3], r[4]) for r in rows]
```

- [ ] **Step 2：`db.py` 新增 DDL**

在 `APPOINTMENTS_DDL` 之後新增：
```python
RISK_EVENTS_DDL = (
    "CREATE TABLE IF NOT EXISTS risk_events ("
    "event_id TEXT PRIMARY KEY, session_id TEXT NOT NULL, "
    "tier INTEGER NOT NULL, reason TEXT NOT NULL, created_at DOUBLE PRECISION NOT NULL);"
    "CREATE INDEX IF NOT EXISTS idx_risk_events_session_created "
    "ON risk_events (session_id, created_at);"
)
```
並在 `ensure_schema` 的 `conn.execute(APPOINTMENTS_DDL)` 之後加：
```python
        conn.execute(RISK_EVENTS_DDL)
```

- [ ] **Step 3：`tests/fakes.py` 加 `FakeRiskEventStore`**

頂部 import 區加：
```python
from kinsun.safety.events import RiskEvent
```
檔末新增：
```python
class FakeRiskEventStore:
    def __init__(self) -> None:
        self.recorded: list[tuple] = []

    def record(self, session_id, assessment):
        self.recorded.append((session_id, assessment))

    def list_for_session(self, session_id):
        return [
            RiskEvent(str(i), s, a.tier, a.reason, float(i))
            for i, (s, a) in enumerate(self.recorded)
            if s == session_id
        ]
```

- [ ] **Step 4：寫 opt-in PG round-trip 測試** `tests/test_risk_events.py`

```python
import os
import uuid
from datetime import datetime, timedelta, timezone
from itertools import count

import pytest

from kinsun.safety.tiers import RiskAssessment, RiskTier

TPE = timezone(timedelta(hours=8))


@pytest.mark.skipif(os.environ.get("KINSUN_IT") != "1", reason="需雲端 key")
def test_pg_risk_events_round_trip():
    from kinsun.db import Database, ensure_schema
    from kinsun.safety.events import PgRiskEventStore

    url = os.environ["DATABASE_URL"]
    ensure_schema(url)
    session_id = f"it-{uuid.uuid4().hex}"
    ids = (f"re{i}" for i in count(1))
    times = iter(
        [datetime(2026, 7, 10, 9, tzinfo=TPE), datetime(2026, 7, 10, 10, tzinfo=TPE)]
    )
    store = PgRiskEventStore(
        Database.open(url), clock=lambda: next(times), new_id=lambda: next(ids)
    )
    store.record(session_id, RiskAssessment(RiskTier.L2, 0.9, "胸痛"))
    store.record(session_id, RiskAssessment(RiskTier.L3, 0.95, "昏倒"))
    events = store.list_for_session(session_id)
    assert [e.tier for e in events] == [RiskTier.L3, RiskTier.L2]
    assert events[0].reason == "昏倒"
```

- [ ] **Step 5：驗證匯入與測試收集**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.safety.events; print('ok')"
uv run pytest tests/test_risk_events.py -v
```
Expected: 印出 `ok`；`test_pg_risk_events_round_trip` SKIPPED。

- [ ] **Step 6：commit**

```bash
git add src/kinsun/safety/events.py src/kinsun/db.py tests/fakes.py tests/test_risk_events.py
git commit -m "feat: 危急事件 model 與儲存（risk_events 表 + Fake）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：pipeline 落庫（fail-safe）

**Files:**
- Modify: `src/kinsun/pipeline.py`、`tests/test_pipeline.py`

**Interfaces:**
- Consumes: `RiskEventStore`（Task 1）
- Produces: `VoicePipeline(*, asr, agent, tts, detector, notifier, risk_events)`，tier≥L2 時 fail-safe `record`

- [ ] **Step 1：改既有測試 + 加新測試**（`tests/test_pipeline.py`）

頂部 import 加：
```python
from tests.fakes import FakeRiskEventStore
```
`_pipeline` 改為（多收 `risk_events`，預設給 fake，既有兩測試不需改）：
```python
def _pipeline(detector, notifier, risk_events=None):
    return VoicePipeline(
        asr=MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM(), NullMemory(), NullContext()),
        tts=TextBubbleTts(),
        detector=detector,
        notifier=notifier,
        risk_events=risk_events or FakeRiskEventStore(),
    )
```
檔末新增：
```python
class _BoomRiskEvents:
    def record(self, session_id, assessment):
        raise RuntimeError("db down")

    def list_for_session(self, session_id):
        return []


def test_pipeline_records_risk_event_on_l2():
    notifier = SpyNotifier()
    events = FakeRiskEventStore()
    _pipeline(StubDetector(RiskTier.L2), notifier, events).process(b"\x00", session_id="u1")
    assert [s for s, _ in events.recorded] == ["u1"]
    assert notifier.calls == [("u1", RiskTier.L2)]


def test_pipeline_does_not_record_below_l2():
    events = FakeRiskEventStore()
    _pipeline(StubDetector(RiskTier.L1), SpyNotifier(), events).process(b"\x00", session_id="u1")
    assert events.recorded == []


def test_pipeline_record_failure_does_not_break():
    notifier = SpyNotifier()
    result = _pipeline(StubDetector(RiskTier.L3), notifier, _BoomRiskEvents()).process(
        b"\x00", session_id="u1"
    )
    assert result.text == "你說的是：阿公早安"
    assert notifier.calls == [("u1", RiskTier.L3)]
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_pipeline.py -v`
Expected: FAIL（`VoicePipeline` 未接受 `risk_events`）

- [ ] **Step 3：實作**（`src/kinsun/pipeline.py`，整檔取代）

```python
"""語音管線：把 ASR、偵測、Agent、TTS 串成一次處理。"""

from __future__ import annotations

import logging

from kinsun.agent import CareAgent
from kinsun.safety.detector import RiskDetector
from kinsun.safety.events import RiskEventStore
from kinsun.safety.notifier import Notifier
from kinsun.safety.tiers import RiskTier
from kinsun.speech.asr import ASRClient
from kinsun.speech.tts import TTSClient, TtsResult

logger = logging.getLogger("kinsun.pipeline")


class VoicePipeline:
    def __init__(
        self,
        *,
        asr: ASRClient,
        agent: CareAgent,
        tts: TTSClient,
        detector: RiskDetector,
        notifier: Notifier,
        risk_events: RiskEventStore,
    ) -> None:
        self._asr = asr
        self._agent = agent
        self._tts = tts
        self._detector = detector
        self._notifier = notifier
        self._risk_events = risk_events

    def process(
        self, audio: bytes, *, session_id: str, content_type: str = "audio/m4a"
    ) -> TtsResult:
        user_text = self._asr.transcribe(audio, content_type=content_type)
        assessment = self._detector.assess(user_text)
        reply_text = self._agent.handle(session_id, user_text)
        if assessment.tier >= RiskTier.L2:
            try:
                self._risk_events.record(session_id, assessment)
            except Exception:  # noqa: BLE001 - 落庫失敗不可中斷對話
                logger.warning("危急事件落庫失敗")
            self._notifier.notify(session_id, assessment)
        return self._tts.synthesize(reply_text)
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_pipeline.py -v`
Expected: PASS（既有 2 + 新增 3）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/pipeline.py tests/test_pipeline.py
git commit -m "feat: pipeline 於 tier≥L2 fail-safe 落庫危急事件

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：app.py 接線

**Files:**
- Modify: `src/kinsun/app.py`

- [ ] **Step 1：實作接線**

import 區加：
```python
import uuid
```
並在 `from kinsun.safety.notifier import LineGuardianNotifier` 一帶加：
```python
from kinsun.safety.events import PgRiskEventStore
```
把 `pipeline = VoicePipeline(...)` 改為（多傳 `risk_events`）：
```python
    pipeline = VoicePipeline(
        asr=build_asr_client(settings),
        agent=CareAgent(gemini, memory, context, tools=registry),
        tts=TextBubbleTts(),
        detector=RiskDetector(LlmRiskClassifier(gemini)),
        notifier=LineGuardianNotifier(accounts, messenger),
        risk_events=PgRiskEventStore(
            db, clock=lambda: datetime.now(tz), new_id=lambda: uuid.uuid4().hex
        ),
    )
```

- [ ] **Step 2：匯入驗證 + 全量測試**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"
uv run pytest -q
```
Expected: `import ok`；pytest 全綠。

- [ ] **Step 3：commit**

```bash
git add src/kinsun/app.py
git commit -m "feat: 組裝層把 PgRiskEventStore 注入 pipeline

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：全量品質閘門

**Files:** 無（純驗證）。

- [ ] **Step 1：後端 lint / format / 測試**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠（PG round-trip SKIPPED）。
format 報未格式化 → `uv run ruff format .` 後重跑並把變更納入收尾 commit。

- [ ] **Step 2：pre-commit 全鉤子**

Run: `uv run pre-commit run --all-files`
Expected: 全 Passed。

- [ ] **Step 3：靜態匯入驗證**

Run: `PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"`
Expected: `import ok`。

（若 Step 1 有 format 修整，補一個 `chore: 套用格式化` commit。）

---

## Self-Review（計畫對 spec 覆蓋檢查）

- `RiskEvent`/`RiskEventStore`/`PgRiskEventStore`（spec §3）→ Task 1 ✅
- `risk_events` DDL（§3）→ Task 1 Step 2 ✅
- pipeline tier≥L2 fail-safe 落庫（§4）→ Task 2 ✅
- app.py 注入（§5）→ Task 3 ✅
- 測試：pipeline 落庫/門檻/fail-safe（§6）→ Task 2；PG round-trip opt-in（§6）→ Task 1 ✅
- fail-safe 不中斷對話、通知照發（§7）→ Task 2 `_BoomRiskEvents` 測試 ✅

型別/命名一致性：`RiskEventStore.record(session_id, assessment)` / `list_for_session`、`VoicePipeline(..., risk_events=...)`、`RiskTier` int 存取還原、`FakeRiskEventStore.recorded`。無佔位符。
