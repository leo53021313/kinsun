# 續段語音 WS 直送 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 續段語音改由後端從既有 WebSocket 主動推送，消除段與段之間的下載空白；併發對話與插嘴補播的既有行為完全保留。

**Architecture:** 後端 `ws.py::_run_turn` 在第一段送出後，於同一個工作執行緒逐段合成剩餘句子並以 binary frame 推出（複用 C1 的 `encode_reply_frame`）。前端從「播完一段才去拉下一段」改為被動接收。`GET /turns/chunks/{index}` 與前端拉取邏輯退場；`POST /turns` 降級路徑改為不分段。補播的單位從「一則音檔」改成「一輪回覆」。

**Tech Stack:** Python 3.12 / FastAPI / WebSocket；TypeScript / React 19 / Vitest；pytest

**Spec:** `docs/superpowers/specs/2026-08-01-續段語音WS直送-design.md`

## Global Constraints

- 語言：台灣繁體中文，用於程式碼註解、文件與 commit 訊息（`AGENTS.md`）
- 分支：`Leo`（個人分支）。**不要 push，不要切到 main**
- 命名：ID 欄位全稱（`turn_id` 不用 `tid`）；JSON 欄位 snake_case，前後端一致
- 測試：TDD（先寫失敗測試）；每條測試須**逐條變異驗證**（拿掉對應實作後確認真的變紅）
- 後端測試指令：`uv run pytest <path> -v`。⚠️ **一次只跑一支**——併行跑會清空共用測試庫
- 前端測試指令：於 `web/` 執行 `npm test`、`npm run typecheck`、`npm run lint`
- 文件同步鐵律：程式碼改動完成時**同一時間**更新 `docs/dev/`，見 Task 8
- 併發對話（`_InFlight`、`pending_utterances`、`_LATE_REPLY_DIRECTIVE`、`playingTurnIdRef`、`canTakeOverSubtitle`）**一律保留，不得移除**

---

### Task 1: 分段決策改由投遞路徑決定（spec D-3 前半）

**背景：** `pipeline._synthesize` 目前以 `channel in self._chunked_channels` 決定分不分段，而 WS 與 `POST /turns` 都是 `Channel.APP`，分不出來。`POST` 降級路徑若仍分段，前端就得留整套 REST 續拉邏輯。

**機制選擇：** 走 contextvars，與 `turn_context.py` 既有的六個跨層事實同一形狀（該模組頂端說明了為什麼不改協定：會波及所有測試替身，而只有少數呼叫端需要）。

**Files:**
- Modify: `src/kinsun/turn_context.py`（檔末新增）
- Modify: `src/kinsun/channels/inbound.py`（`dispatch` 設定範圍）
- Modify: `src/kinsun/pipeline.py:628`
- Test: `tests/test_pipeline.py`、`tests/test_channels_inbound.py`

**Interfaces:**
- Produces: `turn_context.inline_audio_delivery(enabled: bool) -> ContextManager[None]`、`turn_context.is_inline_audio_delivery() -> bool`

- [ ] **Step 1: 寫失敗測試（pipeline 側）**

沿用 `tests/test_pipeline.py:806` 既有的 `_chunking_pipeline(tts, **kwargs)` 與 `_SpyTts`（`.spoken` 是被合成過的文字清單）、`_LONG_REPLY`。在該檔的分段測試區塊末尾新增：

```python
def test_chunking_requires_inline_audio_delivery():
    """走 POST 降級路徑（非內嵌投遞）時不分段——否則長輩只拿得到第一句。"""
    tts = _SpyTts()
    with turn_context.inline_audio_delivery(False):
        result = _chunking_pipeline(tts, chunked_channels=frozenset({"app"})).process_text(
            "我想聊天", elder_id="u1", channel="app"
        )

    assert tts.spoken == [_LONG_REPLY], "非內嵌投遞應合成完整回覆，不是第一句"
    assert result.chunk_count == 0
```

檔頂 import 補上 `from kinsun import turn_context`（若尚未 import）。

- [ ] **Step 1b: 修既有測試（它們會因這道條件變紅）**

把下列兩條的 `process_text(...)` 包進 `with turn_context.inline_audio_delivery(True):`：

- `test_chunked_channel_synthesizes_only_the_first_sentence`（第 819 行附近）——**這條會真的變紅**（斷言 `chunk_count == 2`）
- `test_short_reply_is_not_chunked_even_on_a_chunked_channel`（第 856 行附近）——⚠️ 這條**不會變紅**（斷言本來就是 0），但不包的話它會因為「沒宣告內嵌投遞」而通過，不再是在測「短回覆不分段」。**為了保住測試意義而改，不是為了讓它變綠**

⚠️ **不要改** `test_unchunked_channel_still_synthesizes_the_whole_reply` 與 `test_chunking_defaults_to_off`——它們測的是別的條件，改了就失去意義。

在改動的兩條測試各加一行註解：`# 2026-08-01：分段另需宣告內嵌投遞，見 turn_context.inline_audio_delivery`。

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_pipeline.py -k inline_audio_delivery -v`
Expected: FAIL — `AttributeError: module 'kinsun.turn_context' has no attribute 'inline_audio_delivery'`

- [ ] **Step 3: 在 `turn_context.py` 檔末新增 seam**

```python
_inline_audio: contextvars.ContextVar[bool] = contextvars.ContextVar(
    "kinsun_inline_audio_delivery", default=False
)


@contextmanager
def inline_audio_delivery(enabled: bool) -> Iterator[None]:
    """這一輪的音檔會不會直接走長連線交回通道（C1 內嵌投遞）。由 `dispatch` 設定。

    ⚠️ 為什麼 `pipeline` 需要知道（2026-08-01）：分段只在**投遞端接得住**時才有意義。
    WS 通道能逐段推，`POST /turns` 只能回一則——後者若照樣分段，長輩就只拿得到第一句，
    其餘永遠取不回來（續拉端點已隨本次改動移除）。而兩條路徑的 `channel` 同為 `app`，
    `_chunked_channels` 分不出來。

    走 contextvars 而非改 `pipeline.process` 簽章，理由與本模組其餘六個機制相同：
    改簽章會波及所有測試替身與呼叫端，而真正需要這個資訊的只有 `_synthesize` 一處。

    `default=False`＝沒有人宣告時**不分段**。這是安全側：漏標的呼叫端會拿到完整音檔
    （慢一點但完整），而不是只拿到第一句、其餘無聲消失。
    """
    token = _inline_audio.set(enabled)
    try:
        yield
    finally:
        _inline_audio.reset(token)


def is_inline_audio_delivery() -> bool:
    return _inline_audio.get()
```

- [ ] **Step 4: `pipeline.py:628` 加上這道條件**

```python
        chunks = (
            split_for_speech(reply_text)
            if channel in self._chunked_channels and turn_context.is_inline_audio_delivery()
            else []
        )
```

並在 `_synthesize` 的 docstring 補一句：「⚠️ 另需 `turn_context.is_inline_audio_delivery()`：分段只在投遞端逐段接得住時才成立，見該函式說明。」

- [ ] **Step 5: 執行測試確認通過**

Run: `uv run pytest tests/test_pipeline.py -k inline_audio_delivery -v`
Expected: 2 passed

- [ ] **Step 6: 寫 `dispatch` 側的失敗測試**

`tests/test_channels_inbound.py:87` 的 `_msg` helper 目前沒有 `reply_audio` 參數，先擴充它（保持既有呼叫端不變）：

```python
def _msg(kind, *, reply, text="", audio=b"", external_id="U-1", reply_audio=None):
    return InboundMessage(
        Channel.LINE, external_id, kind, text, audio, reply, reply_audio=reply_audio
    )
```

⚠️ 上面的建構參數順序須與 `InboundMessage` 的實際定義一致——**先讀 `src/kinsun/channels/inbound.py` 的 dataclass 欄位順序**再照抄，不要憑記憶寫。

然後新增測試（沿用既有的 `_Binding(None)`、`_Gate(True)`、`_SpyVoice()`）：

```python
def test_dispatch_declares_inline_delivery_when_reply_audio_present():
    """有 reply_audio（WS 通道）→ 宣告內嵌投遞；沒有（LINE／POST）→ 不宣告。"""
    seen: list[bool] = []

    class _Probe:
        def process(self, audio, **kwargs):
            seen.append(turn_context.is_inline_audio_delivery())
            return TtsResult(text="回覆", audio=b"x", duration_ms=1)

    dispatch(
        _msg("audio", audio=b"a", reply=_Replies(), reply_audio=lambda *a: None),
        pipeline=_Probe(),
        binding=_Binding(None),
        gate=_Gate(True),
        voice=_SpyVoice(),
    )
    assert seen == [True]

    seen.clear()
    dispatch(
        _msg("audio", audio=b"a", reply=_Replies()),
        pipeline=_Probe(),
        binding=_Binding(None),
        gate=_Gate(True),
        voice=_SpyVoice(),
    )
    assert seen == [False]
```

檔頂 import 補上 `from kinsun import turn_context` 與 `from kinsun.speech.tts import TtsResult`（若尚未 import）。

- [ ] **Step 7: 執行測試確認失敗**

Run: `uv run pytest tests/test_channels_inbound.py -k inline_delivery -v`
Expected: FAIL — `seen == [False]` 而預期 `[True]`

- [ ] **Step 8: 在 `dispatch` 包上範圍**

`src/kinsun/channels/inbound.py`：找到 `dispatch` 呼叫 `_run_pipeline` 的兩處（語音與文字），把 `_run_pipeline(...)` 整段包進：

```python
    with turn_context.inline_audio_delivery(msg.reply_audio is not None):
        return _run_pipeline(...)
```

⚠️ 必須包住 `_run_pipeline` **整段**（含 `pipeline.process`），不能只包 `voice.deliver`——分段決策發生在合成當下。

- [ ] **Step 9: 執行全檔測試**

Run: `uv run pytest tests/test_channels_inbound.py -v`
然後：`uv run pytest tests/test_pipeline.py -v`
Expected: 全部通過（既有測試不得變紅——它們沒宣告 contextvar，走 `default=False` 不分段，若有既有測試斷言 `chunk_count > 1`，需在該測試補上 `with turn_context.inline_audio_delivery(True):`）

- [ ] **Step 10: 逐條變異驗證**

把 Step 4 的 `and turn_context.is_inline_audio_delivery()` 拿掉 → Step 1 的第一條測試必須變紅。把 Step 8 的 `msg.reply_audio is not None` 改成 `False` → Step 6 的測試必須變紅。確認後改回。

- [ ] **Step 11: Commit**

```bash
git add src/kinsun/turn_context.py src/kinsun/pipeline.py src/kinsun/channels/inbound.py tests/test_pipeline.py tests/test_channels_inbound.py
git commit -F- <<'EOF'
feat(speech): 分段改由投遞路徑決定，POST 降級路徑不再分段

WHY：
續段即將改由 WebSocket 主動推送，REST 續拉端點會移除。而 `POST /turns` 降級路徑
只能回一則音檔——它若照樣分段，長輩就只拿得到第一句、其餘永遠取不回來。兩條路徑
的 channel 同為 app，`_chunked_channels` 分不出來。

WHAT：
新增 `turn_context.inline_audio_delivery` seam（走 contextvars，與本模組既有六個
跨層事實同一形狀），由 `dispatch` 依 `msg.reply_audio is not None` 設定，
`pipeline._synthesize` 據此與 `_chunked_channels` 取交集。
default=False 是安全側：漏標的呼叫端拿到完整音檔（慢但完整），而不是只有第一句。

IMPACT：
- LINE 通道不受影響（本來就不在 `_chunked_channels`）
- `POST /turns` 首聲 4.46s→5.97s（p50 實測），降級路徑的刻意取捨
- 無 API／Schema／環境變數改動
EOF
```

---

### Task 2: 後端推送續段

**Files:**
- Modify: `src/kinsun/channels/app/ws.py`
- Modify: `src/kinsun/app.py`（注入 `tts`）
- Test: `tests/test_channels_app_ws.py`

**Interfaces:**
- Consumes: Task 1 的 `turn_context.inline_audio_delivery`（`ws.py` 不直接用，但行為依賴它）
- Produces: `_Sender.send_chunk_audio(header: dict, audio: bytes) -> None`；`create_app_ws_router(..., tts=None)` 新參數

- [ ] **Step 1: 寫失敗測試**

沿用 `tests/test_channels_app_ws.py` 既有的 `_service()`、`_bound_elder_token(svc)`、`_client(svc, ...)`、`_frames(ws, n)`、`_VoiceTts`。

⚠️ `_client` 目前把 `tts` 只傳給 `VoicePipeline`，**沒有**傳給 `create_app_ws_router`。先改它（Step 4 之後）：在 `create_app_ws_router(...)` 的參數中加 `tts=tts or _VoiceTts(),`。

⚠️ `_EchoLLM` 回的是固定短句，切不出多段。需要一個回長句的 LLM 替身：

```python
class _MultiSentenceLLM:
    """回一段切得出三句的回覆，供續段測試用。每句皆 ≥ MIN_CHUNK_CHARS（8 字）。"""

    def generate(self, *, system_prompt, messages, response_schema=None):
        return "第一句話夠長可以自成一段。第二句話也夠長可以自成一段。第三句話同樣夠長。"
```

新增測試：

```python
def test_continuation_chunks_are_pushed_in_order():
    """第一段之後，剩餘段落逐一以 binary frame 推出，index 遞增、最後一段 is_last。"""
    svc = _service()
    token = _bound_elder_token(svc)
    client = _client(svc, llm=_MultiSentenceLLM())
    with client.websocket_connect(f"/api/v1/ws/talk?token={token}") as ws:
        ws.send_bytes(b"elder-audio")
        frames = _frames(ws, 3)  # reply ＋ chunk1 ＋ chunk2

    chunks = [f for f in frames if f["type"] == "chunk"]
    assert [c["index"] for c in chunks] == [1, 2]
    assert [c["is_last"] for c in chunks] == [False, True]
    # ⚠️ 併發之下前端靠 turn_id 歸屬，錯了會把 A 的段接到 B 後面（同 459051f 那類錯亂）
    assert {c["turn_id"] for c in chunks} == {"turn-1"}  # _client 的 new_id 固定回 turn-1
    assert chunks[0]["text"] == "第二句話也夠長可以自成一段。"
    assert chunks[0]["audio"] == b"fake-m4a"  # _VoiceTts 的固定音檔


def test_single_segment_reply_pushes_no_audio_chunk():
    """短回覆切不出第二段——不推任何**帶音檔**的續段訊框。"""
    svc = _service()
    token = _bound_elder_token(svc)
    client = _client(svc)  # _EchoLLM 回短句
    with client.websocket_connect(f"/api/v1/ws/talk?token={token}") as ws:
        ws.send_bytes(b"elder-audio")
        frames = _frames(ws, 1)  # 只有 reply

    audio_chunks = [f for f in frames if f["type"] == "chunk" and f["audio"]]
    assert audio_chunks == []
```

⚠️ **本任務結束時 `_frames(ws, 1)` 是對的**（此刻還沒有終止訊框）。Task 3 引入終止訊框後**必須把這條改成 `_frames(ws, 2)`**——Task 3 的 Step 5 有對應指示。不先改的話這條測試會在 Task 3 之後開始卡住等不到第二個訊框。

⚠️ `_frames(ws, n)` 的 `n` 必須與實際送出的訊框數一致，多了會卡住等不到、少了會漏檢。實作完成後若數量不符，**先確認是實作錯還是預期錯**，不要直接改數字。

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_channels_app_ws.py -k continuation -v`
Expected: FAIL — 沒有任何 `type == "chunk"` 的訊框

- [ ] **Step 3: 新增 `_Sender.send_chunk_audio`**

在 `src/kinsun/channels/app/ws.py` 的 `_Sender` 類別內，`send_reply_audio` 之後：

```python
    def send_chunk_audio(self, header: dict, audio: bytes) -> None:
        """送出續段音檔訊框（2026-08-01）。

        ⚠️ 失敗只記 warning、**不往外拋**（與 `send_reply_audio` 相反）：第一段推不出去
        代表長輩這一輪什麼都收不到，必須讓投遞層退回文字泡泡；續段推不出去時他已經
        聽到開頭了，為此把整輪打回文字反而更糟。
        """
        try:
            future = asyncio.run_coroutine_threadsafe(
                self._websocket.send_bytes(encode_reply_frame(header, audio)), self._loop
            )
            future.result(timeout=5)
        except Exception:  # noqa: BLE001 - 續段送不出去不可中斷那一輪的其餘工作
            logger.warning("續段音檔送出失敗 index=%s", header.get("index"))
```

- [ ] **Step 4: 工廠新增 `tts` 參數**

`create_app_ws_router` 的參數列加入 `tts=None`（位置放在 `ack_audio` 之後），並在 docstring 的協定段落補上 `chunk` 訊框說明：

```
- **binary frame（type="chunk"）**——續段語音（2026-08-01）。header 欄位：
  `turn_id`、`index`（從 1 起）、`text`、`duration_ms`、`is_last`。
  ⚠️ `turn_id` 是必要的：併發之下同時可能有多輪在推段，前端靠它歸屬。
```

- [ ] **Step 5: 在 `_run_turn` 加入續段迴圈**

找到 `_run_turn` 尾端 `if collector.audio_sent:` 那一段，改成：

```python
        if collector.audio_sent:
            # 續段直送（2026-08-01）：第一段已經在播了，剩下的逐段合成、逐段推出去。
            # ⚠️ 迴圈在 `turn_gate.admit()` 的 with 區塊**之內**（見上方 try 的縮排）：
            # 續段一樣打 GPU，閘門要擋的就是這個。放外面會讓 `active()` 低估實際負載。
            _push_continuation_chunks(sender, collector.text, turn_id)
            return
```

並在 `create_app_ws_router` 內（`_run_turn` 之前）新增：

```python
    def _push_continuation_chunks(sender: _Sender, reply_text: str, turn_id: str) -> None:
        """把第一段之後的句子逐段合成並推出去（spec 2026-08-01）。

        ⚠️ 自己呼叫 `split_for_speech` 而不是從 `TtsResult` 拿：它是純函式，同樣輸入
        必得同樣輸出；改 `TtsResult` 協定會波及所有測試替身，換不到任何東西。
        既有的 `turns.py::get_turn_chunk` 也是這樣做的。

        優先權 `CHUNK`：長輩正在聽第一段、續段有餘裕，別位長輩的第一段（`REPLY`）
        應該先做。
        """
        if tts is None:
            return
        chunks = split_for_speech(reply_text)
        for index, text in enumerate(chunks[1:], start=1):
            is_last = index == len(chunks) - 1
            try:
                with tts_priority(TtsPriority.CHUNK):
                    result = tts.synthesize(text)
            except TTSError:
                logger.warning("續段合成失敗 turn=%s index=%s", turn_id, index)
                return
            if result.audio is None:
                logger.warning("續段無音檔 turn=%s index=%s", turn_id, index)
                return
            sender.send_chunk_audio(
                {
                    "type": "chunk",
                    "turn_id": turn_id,
                    "index": index,
                    "text": text,
                    "duration_ms": result.duration_ms,
                    "is_last": is_last,
                },
                result.audio,
            )
```

檔頂 import 補上：

```python
from kinsun.speech.chunking import split_for_speech
from kinsun.speech.tts import TTSError, TtsPriority, tts_priority
```

- [ ] **Step 6: 執行測試確認通過**

Run: `uv run pytest tests/test_channels_app_ws.py -k continuation -v`
Expected: 2 passed

- [ ] **Step 6b: 續段納入觀測（spec §5.5）**

續段的合成目前沒有任何 span——`pipeline._synthesize` 的 `@tracing.track(name="tts")` 只涵蓋第一段。加在 `_push_continuation_chunks` 上（閉包可裝飾，`turns.py::get_turn_chunk` 就是同樣的形狀）：

```python
    @tracing.track(
        name="tts_chunks",
        type="general",
        capture_input=False,   # reply_text 已在 care_turn_voice 的 I/O 裡
        capture_output=False,  # 回傳 None
    )
    def _push_continuation_chunks(sender: _Sender, reply_text: str, turn_id: str) -> None:
```

並在迴圈內每段送出後補一行結構化 log，供日後查續段耗時分布：

```python
            logger.info("續段推出 turn=%s index=%s 字數=%s", turn_id, index, len(text))
```

檔頂 import 補 `from kinsun import tracing`（若尚未 import）。

⚠️ 這順帶修掉一件事：`turn_chunk` trace 隨 Task 4 的端點消失後，續段的耗時若無此 span 就完全看不見了。

- [ ] **Step 7: `app.py` 注入 tts**

在 `create_app_ws_router(` 的參數中，`ack_audio=ack_audio,` 之後加一行：

```python
            # 續段直送（2026-08-01）：第一段之後的句子由本 router 自己逐段合成推出。
            tts=tts_client,
```

- [ ] **Step 8: 執行全檔測試**

Run: `uv run pytest tests/test_channels_app_ws.py -v`
Expected: 全部通過

- [ ] **Step 9: 逐條變異驗證**

把 Step 5 的 `is_last = index == len(chunks) - 1` 改成 `is_last = False` → 第一條測試變紅。把 `"turn_id": turn_id` 改成 `""` → `turn_id` 那條斷言變紅。確認後改回。

- [ ] **Step 10: Commit**

```bash
git add src/kinsun/channels/app/ws.py src/kinsun/app.py tests/test_channels_app_ws.py
git commit -F- <<'EOF'
feat(ws): 續段語音改由後端主動推送，不再繞 Supabase

WHY：
Leo 回報分段輸出效果不好。根因是前端 `advanceQueue` 的 `prefetchNext` 預取的是
**網址不是音檔位元組**，真正的下載發生在 `player.replace()` 設 src 之後——而那一刻
前一段已經播完，每一段之間都有一個下載空白。Opik n=57 顯示續拉伺服器端中位 3.13s
（含 shortterm_recent 0.21s＋TTS 2.49s＋audio_upload 0.40s），還沒算兩趟隧道往返
與前端下載；而段 2 的預算只有 3.16 秒（實測）。

WHAT：
第一段送出後，於同一個工作執行緒逐段合成剩餘句子並以 binary frame 推出，複用 C1 的
`encode_reply_frame`。`split_for_speech` 自己呼叫（純函式、同輸入同輸出），不改
`TtsResult` 協定。優先權維持 CHUNK——別位長輩的第一段仍優先。
續段迴圈在容量閘門**之內**：續段一樣打 GPU，放外面會讓閘門低估實際負載。
`send_chunk_audio` 失敗只記 warning 不往外拋（與第一段相反）：長輩已經聽到開頭了，
為此把整輪打回文字反而更糟。

IMPACT：
- `chunk` frame 的 `turn_id` 是必要欄位：併發之下前端靠它歸屬，錯了會把 A 的段接到
  B 後面（同 459051f 修過的那類錯亂）
- 一輪佔用閘門的時間從約 4.46s 變成約 6.7s（p50）／11.2s（p90），`queued` 訊框會
  更常出現。這是刻意的
- 前端尚未接線（Task 5），本 commit 之後續段會推出去但前端還在走 REST 續拉
EOF
```

---

### Task 3: 續段合成失敗時仍送終止訊號

**背景（spec §5.2）：** 前端靠 `is_last` 判斷這一輪的語音結束。合成中途失敗時 Task 2 的迴圈直接 `return`，`is_last` 永遠不會送出，前端會把這一輪當成還沒結束。

**Files:**
- Modify: `src/kinsun/channels/app/ws.py`（`_push_continuation_chunks`）
- Test: `tests/test_channels_app_ws.py`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_channels_app_ws.py` 新增替身與測試：

```python
class _FailAfterFirstTts:
    """第一次合成成功（那是回覆的第一段），第二次起擲 TTSError。"""

    def __init__(self) -> None:
        self.calls = 0

    def synthesize(self, text: str) -> TtsResult:
        self.calls += 1
        if self.calls > 1:
            raise TTSError("模擬合成失敗")
        return TtsResult(text=text, audio=b"fake-m4a", duration_ms=1200)


def test_chunk_synthesis_failure_still_sends_terminator():
    """續段合成失敗 → 補送空音檔的 is_last 訊框，前端才不會把該輪當成還沒結束。"""
    svc = _service()
    token = _bound_elder_token(svc)
    client = _client(svc, llm=_MultiSentenceLLM(), tts=_FailAfterFirstTts())
    with client.websocket_connect(f"/api/v1/ws/talk?token={token}") as ws:
        ws.send_bytes(b"elder-audio")
        frames = _frames(ws, 2)  # reply ＋ 終止訊框

    terminator = frames[-1]
    assert terminator["type"] == "chunk"
    assert terminator["is_last"] is True, "無論如何都要有終止訊號"
    assert terminator["text"] == "", "終止訊框不帶文字"
    assert terminator["audio"] == b"", "終止訊框不帶音檔"
```

檔頂 import 補上 `from kinsun.speech.tts import TTSError`。

⚠️ `_FailAfterFirstTts` 同時被 `VoicePipeline`（第一段）與續段迴圈使用——`_client` 把同一顆 tts 傳給兩邊，這正是要測的情境。

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_channels_app_ws.py -k terminator -v`
Expected: FAIL — `chunks[-1]["is_last"]` 是 `False`（成功的那一段）

- [ ] **Step 3: 改成 try/finally 保證送出終止訊號**

把 `_push_continuation_chunks` 的迴圈改為：

```python
        sent_terminator = False
        for index, text in enumerate(chunks[1:], start=1):
            is_last = index == len(chunks) - 1
            try:
                with tts_priority(TtsPriority.CHUNK):
                    result = tts.synthesize(text)
            except TTSError:
                logger.warning("續段合成失敗 turn=%s index=%s", turn_id, index)
                break
            if result.audio is None:
                logger.warning("續段無音檔 turn=%s index=%s", turn_id, index)
                break
            sender.send_chunk_audio({...同上..., "is_last": is_last}, result.audio)
            sent_terminator = is_last
        if not sent_terminator:
            # ⚠️ 無論如何都要有終止訊號：前端靠 `is_last` 知道這一輪講完了，
            # 缺了會把該輪當成還沒結束（見 spec §5.2）。
            sender.send_chunk_audio(
                {
                    "type": "chunk",
                    "turn_id": turn_id,
                    "index": 0,
                    "text": "",
                    "duration_ms": 0,
                    "is_last": True,
                },
                b"",
            )
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_channels_app_ws.py -k terminator -v`
Expected: PASS

- [ ] **Step 4b: 更新 Task 2 留下的那條測試（必做）**

單段回覆現在也會收到終止訊框（迴圈不執行、`sent_terminator` 維持 False，走補送路徑）。把 Task 2 建立的 `test_single_segment_reply_pushes_no_audio_chunk` 從 `_frames(ws, 1)` 改成 `_frames(ws, 2)`，並在 docstring 補一句：

```python
    """短回覆切不出第二段——不推任何**帶音檔**的續段訊框。

    ⚠️ 仍會收到一個空音檔的終止訊框：前端靠 is_last 結束該輪，缺了會把該輪當成
    還沒結束（見 spec §5.2）。故此處收兩個訊框而不是一個。
    """
```

⚠️ **不改的話這條測試會卡住等不到第二個訊框**（`_frames` 會一直等）。

- [ ] **Step 5: 執行全檔測試並變異驗證**

Run: `uv run pytest tests/test_channels_app_ws.py -v`
變異：把 `if not sent_terminator:` 整段拿掉 → 本 Task 的測試變紅、且 Step 4b 改過的那條也變紅（等不到第二個訊框而逾時）。把 `sent_terminator = is_last` 改成 `sent_terminator = True` → 多段情境不再補送終止訊框，本 Task 的測試變紅。確認後改回。

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/channels/app/ws.py tests/test_channels_app_ws.py
git commit -F- <<'EOF'
fix(ws): 續段合成失敗時仍送出終止訊框

WHY：
前端靠 `is_last` 判斷這一輪的語音結束。續段合成中途失敗時迴圈直接 return，
`is_last` 永遠不會送出，前端會把該輪當成還沒結束——畫面停在「說話中」直到
75 秒的 THINKING_TIMEOUT_MS 保險才放行。

WHAT：
迴圈改為 break＋事後補送：正常結束時最後一段帶 is_last；中途失敗（TTSError 或
audio 為 None）時補送一個空音檔、空文字的 is_last 訊框。

IMPACT：
單段回覆現在也會收到一個終止訊框（迴圈不執行、走補送路徑）。前端據 is_last 結束
該輪，空音檔不進播放佇列——Task 5 會處理。
EOF
```

---

### Task 4: 移除 REST 續拉端點

**Files:**
- Modify: `src/kinsun/channels/app/turns.py`（移除 `get_turn_chunk`）
- Modify: `src/kinsun/web/errors.py`（移除兩個 ErrorCode）
- Modify: `src/kinsun/app.py`（移除 `create_app_turns_router` 不再需要的參數）
- Test: `tests/test_channels_app_turns.py`

⚠️ **`speech/chunking.py::reply_digest` 保留、不移除**：`inbound.py::chunk_info` 仍在算它、`reply` frame 與 `POST /turns` 回應仍帶它。只有 `turns.py` 內的 digest 比對隨端點消失。

- [ ] **Step 1: 刪除既有測試中針對該端點的案例**

在 `tests/test_channels_app_turns.py` 找出所有 `get_turn_chunk` / `/turns/chunks/` 相關測試並刪除。**先執行一次記下數量**：

Run: `uv run pytest tests/test_channels_app_turns.py -v 2>&1 | tail -3`

- [ ] **Step 2: 移除端點**

刪除 `src/kinsun/channels/app/turns.py` 的 `get_turn_chunk` 函式（含 `@router.get` 與 `@tracing.track` 裝飾器）與其專用 import（`reply_digest`、`split_for_speech`、`tts_priority`、`TtsPriority`、`TTSError` 中不再被用到者——**逐一確認該檔是否還有其他用途再刪**）。

`create_app_turns_router` 的 `memory` / `tts` / `audio_publisher` 參數若已無其他消費者則一併移除，並同步 `app.py` 的呼叫端。

- [ ] **Step 3: 移除 ErrorCode**

`src/kinsun/web/errors.py`：刪除 `CHUNK_NOT_FOUND` 與 `CHUNK_SUPERSEDED` 兩列。

- [ ] **Step 4: 執行測試**

Run: `uv run pytest tests/test_channels_app_turns.py -v`
然後全庫：`uv run pytest -q 2>&1 | tail -5`
Expected: 全綠。若有其他檔案引用被刪的符號會在此暴露。

- [ ] **Step 5: Commit**

```bash
git add -A src/kinsun tests
git commit -F- <<'EOF'
refactor(api): 移除 REST 續拉端點，續段全走 WebSocket

WHY：
續段已改由 WebSocket 主動推送（前一個 commit），而 POST 降級路徑改為不分段
（Task 1），`GET /turns/chunks/{index}` 沒有任何呼叫端了。留著只會讓前端得繼續
維護整套 ChunkQueue 拉取邏輯。

WHAT：
移除端點、`ErrorCode.CHUNK_NOT_FOUND`／`CHUNK_SUPERSEDED`，以及 turns router 中
只為該端點注入的 memory／tts／audio_publisher 參數。

⚠️ `speech/chunking.py::reply_digest` 保留：`inbound.py::chunk_info` 仍在算它、
`reply` frame 與 POST 回應仍帶它（POST 降級路徑用它當輪次識別，因 TurnReply 無
turn_id）。只有端點內的 digest 比對隨端點消失。

IMPACT：
- 破壞性變更：`GET /api/v1/turns/chunks/{index}` 不再存在。前端於 Task 6 同步移除
- 順帶修掉「續拉在 Opik 生出孤兒 root trace」的另一半（turn_chunk trace 隨端點消失，
  續段耗時改由 care_turn_voice 這棵樹涵蓋）
EOF
```

---

### Task 5: 前端接收 `chunk` 訊框

**Files:**
- Modify: `web/src/talk/talkSocket.ts`（`TalkFrame` 型別）
- Modify: `web/src/elder/useTalk.ts`（`onFrame`）
- Test: `web/src/talk/talkSocket.test.ts`、`web/src/elder/useTalk.test.ts`

**Interfaces:**
- Consumes: Task 2／3 的 `chunk` 訊框 header（`type`、`turn_id`、`index`、`text`、`duration_ms`、`is_last`）

⚠️ `talkSocket.ts` 的 binary 路徑**已經是通用的**（`onFrame({...parsed.header, audio_url: uri})`），除型別外不需改動。

- [ ] **Step 1: 加型別**

`web/src/talk/talkSocket.ts` 的 `TalkFrame` union 加一個 variant：

```ts
  | {
      /** 續段語音（2026-08-01）。第 0 段隨 `reply` 送出，之後由後端主動推。 */
      type: "chunk";
      turn_id: string;
      index: number;
      text: string;
      audio_url: string;
      duration_ms: number;
      /** 這一輪的語音到此結束。合成失敗時後端補送空音檔的終止訊框。 */
      is_last: boolean;
    }
```

- [ ] **Step 2: 寫失敗測試**

在 `web/src/elder/useTalk.test.ts` 新增（沿用該檔既有的 `renderHook` ＋假 socket 建構方式）：

```ts
it("收到 chunk 訊框就進播放佇列，依 index 順序播", async () => {
  const { emitFrame, played } = setup();
  emitFrame({ type: "reply", turn_id: "t1", text: "第一句。", audio_url: "blob:a",
              duration_ms: 100, chunk_count: 3, reply_digest: "d" });
  emitFrame({ type: "chunk", turn_id: "t1", index: 1, text: "第二句。",
              audio_url: "blob:b", duration_ms: 100, is_last: false });
  emitFrame({ type: "chunk", turn_id: "t1", index: 2, text: "第三句。",
              audio_url: "blob:c", duration_ms: 100, is_last: true });
  await flush();
  expect(played.map((p) => p.audioUrl)).toEqual(["blob:a", "blob:b", "blob:c"]);
});

it("空音檔的終止訊框不進播放佇列", async () => {
  const { emitFrame, played } = setup();
  emitFrame({ type: "reply", turn_id: "t1", text: "第一句。", audio_url: "blob:a",
              duration_ms: 100, chunk_count: 2, reply_digest: "d" });
  emitFrame({ type: "chunk", turn_id: "t1", index: 0, text: "",
              audio_url: "", duration_ms: 0, is_last: true });
  await flush();
  expect(played.map((p) => p.audioUrl)).toEqual(["blob:a"]);
});
```

- [ ] **Step 3: 執行測試確認失敗**

Run（於 `web/`）：`npm test -- useTalk`
Expected: FAIL — chunk 訊框沒有進佇列

- [ ] **Step 4: 在 `onFrame` 處理 chunk**

`useTalk.ts` 的 `onFrame` 內，`if (frame.type === "reply")` 那一段**之前**插入：

```ts
        if (frame.type === "chunk") {
          // 續段直送（2026-08-01）：後端主動推，不再由前端去拉。
          // ⚠️ 空音檔＝終止訊框（後端合成失敗時補送），只用來結束這一輪，不進佇列。
          if (frame.audio_url) {
            queue.push({
              turnId: frame.turn_id,
              audioUrl: frame.audio_url,
              text: frame.text,
              durationMs: frame.duration_ms,
            });
          }
          return;
        }
```

⚠️ 放在 `canTakeOverScreen` 的字幕更新**之後**、`reply` 分支之前——續段的字幕要跟著它自己播出來的那一刻走（既有的播放回呼已經處理，見該處註解）。

- [ ] **Step 5: 執行測試確認通過**

Run: `npm test -- useTalk`
Expected: PASS

- [ ] **Step 6: 型別與 lint**

Run: `npm run typecheck && npm run lint`
Expected: 無錯誤

- [ ] **Step 7: 變異驗證**

把 Step 4 的 `if (frame.audio_url)` 拿掉 → 第二條測試變紅。把 `return` 拿掉 → chunk 會掉進 `reply` 分支，第一條測試變紅。確認後改回。

- [ ] **Step 8: Commit**

```bash
git add web/src/talk/talkSocket.ts web/src/elder/useTalk.ts web/src/elder/useTalk.test.ts
git commit -F- <<'EOF'
feat(web): 長輩端接收後端主動推來的續段語音

WHY：
後端已改為在第一段送出後逐段推續段（前面的 commit）。前端此前是「播完一段才去拉
下一段」，而 `prefetchNext` 預取到的只是網址、音檔要等 `player.replace()` 才開始
下載——那正是段間空白的來源。

WHAT：
`TalkFrame` 加 `chunk` variant；`onFrame` 收到就推進播放佇列。空音檔的終止訊框
（後端合成失敗時補送）只用來結束該輪，不進佇列。

⚠️ `talkSocket.ts` 的 binary 解析路徑本來就是通用的（`{...parsed.header, audio_url}`
直接交給 onFrame），除型別外一行未改。

IMPACT：
- 舊的 REST 續拉尚未移除（Task 6），此 commit 之後兩條路徑並存：`chunk_count > 1`
  仍會觸發 `prefetchNext`，同一段可能被播兩次。**Task 6 必須緊接著做**
EOF
```

⚠️ **這個 commit 讓兩條路徑並存、同一段可能播兩次。Task 6 必須緊接著完成，不可停在此處交付。**

---

### Task 6: 移除前端 REST 續拉

**Files:**
- Modify: `web/src/elder/useTalk.ts`
- Modify: `web/src/elder/api.ts`
- Test: `web/src/elder/useTalk.test.ts`、`web/src/elder/api.test.ts`

- [ ] **Step 1: 寫失敗測試**

```ts
it("chunk_count > 1 不再觸發任何 REST 請求", async () => {
  const getTurnChunk = vi.fn();
  const { emitFrame } = setup({ deps: { getTurnChunk } });
  emitFrame({ type: "reply", turn_id: "t1", text: "第一句。", audio_url: "blob:a",
              duration_ms: 100, chunk_count: 3, reply_digest: "d" });
  await flush();
  expect(getTurnChunk).not.toHaveBeenCalled();
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `npm test -- useTalk`
Expected: FAIL — `getTurnChunk` 被呼叫了

- [ ] **Step 3: 移除拉取邏輯**

`useTalk.ts`：
- 刪除 `prefetchNext`
- 刪除 `advanceQueue` 中「取下一段」的整段邏輯；該函式的職責只剩「這一則播完了，若佇列空了就回到待機」——**保留 `playingTurnIdRef` 的輪次比對**（併發之下仍需要，見該處註解）
- 刪除 `ChunkQueue` 型別的 `total` / `nextIndex` / `pending` / `digest` 欄位與 `chunkQueueRef` 的續拉用途；若移除後 `chunkQueueRef` 已無消費者則整個刪除
- `onFrame` 的 `reply` 分支中，刪除 `if (frame.chunk_count > 1 && frame.reply_digest) { ... }` 整段
- `TalkDeps` 移除 `getTurnChunk`
- 移除相關 import

`api.ts`：刪除 `getTurnChunk` 與 `TurnChunk` 型別。

- [ ] **Step 4: 執行測試**

Run: `npm test`
Expected: 全綠。既有測試若斷言續拉行為需一併刪除（它們測的是已移除的功能）。

- [ ] **Step 5: 型別與 lint**

Run: `npm run typecheck && npm run lint`

- [ ] **Step 6: Commit**

```bash
git add web/src/elder/useTalk.ts web/src/elder/api.ts web/src/elder/useTalk.test.ts web/src/elder/api.test.ts
git commit -F- <<'EOF'
refactor(web): 移除 REST 續拉，續段全由 WebSocket 被動接收

WHY：
續段已改為後端主動推（前一個 commit）。舊的拉取路徑留著會讓同一段播兩次，
而且後端的 `GET /turns/chunks/{index}` 端點已經移除，拉也拉不到。

WHAT：
移除 `prefetchNext`、`advanceQueue` 的取下一段邏輯、`ChunkQueue` 的拉取欄位、
`onFrame` 中 `chunk_count > 1` 的觸發、`elder/api.ts::getTurnChunk`。

⚠️ **`playingTurnIdRef` 的輪次比對保留**：併發對話仍在，補播舊答案時新一輪的訊框
仍可能交錯抵達（459051f 修過的那類錯亂），這道守門依然承重。

IMPACT：
無 API 呼叫端變更以外的行為差異；`POST /turns` 降級路徑因後端不再分段，回的是完整
音檔（Task 1）。
EOF
```

---

### Task 7: 補播的單位改成「一輪」（spec D-4）

**背景（spec §6.3）：** `deferredRepliesRef` 上限 2 **則**，理由是「一輪最多 ack＋reply」。續段直送後一輪會產生 ack＋reply＋chunk1＋chunk2 四則以上——上限 2 會在**單一輪之內**被擠爆，症狀是補播時只聽到那一輪的最後兩則、答案開頭被擠掉。

**Files:**
- Modify: `web/src/elder/useTalk.ts`
- Test: `web/src/elder/useTalk.test.ts`

- [ ] **Step 1: 寫失敗測試**

```ts
it("收音期間同一輪的四則全部收進同一個補播單位", async () => {
  const { emitFrame, pressMic, releaseMic, played } = setup();
  pressMic();
  emitFrame({ type: "ack", turn_id: "t1", text: "好，我幫您查", audio_url: "blob:ack",
              duration_ms: 100 });
  emitFrame({ type: "reply", turn_id: "t1", text: "第一句。", audio_url: "blob:a",
              duration_ms: 100, chunk_count: 3, reply_digest: "d" });
  emitFrame({ type: "chunk", turn_id: "t1", index: 1, text: "第二句。",
              audio_url: "blob:b", duration_ms: 100, is_last: false });
  emitFrame({ type: "chunk", turn_id: "t1", index: 2, text: "第三句。",
              audio_url: "blob:c", duration_ms: 100, is_last: true });
  await releaseMic();
  expect(played.map((p) => p.audioUrl)).toEqual(["blob:ack", "blob:a", "blob:b", "blob:c"]);
});

it("補播佇列滿時擠掉最舊那一輪（整輪，不是最舊那一則）", async () => {
  const { emitFrame, pressMic, releaseMic, played, revoked } = setup();
  pressMic();
  for (const t of ["t1", "t2", "t3"]) {
    emitFrame({ type: "reply", turn_id: t, text: `${t} 的答案。`, audio_url: `blob:${t}`,
                duration_ms: 100, chunk_count: 0, reply_digest: "" });
  }
  await releaseMic();
  expect(played.map((p) => p.audioUrl)).toEqual(["blob:t2", "blob:t3"]);
  expect(revoked).toContain("blob:t1");
});
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `npm test -- useTalk`
Expected: 第一條 FAIL — 只播到最後兩則（`blob:b`、`blob:c`）

- [ ] **Step 3: 改成以輪為單位**

`useTalk.ts`：把 `deferredRepliesRef` 換成：

```ts
/** 收音期間收下來、等他講完再補播的那幾**輪**回覆。 */
type DeferredTurn = { turnId: string; items: PlaybackItem[] };

/**
 * ⚠️ **單位是「輪」不是「則」**（2026-08-01，續段直送引入）：續段改由後端主動推之後，
 * 一輪會產生 ack＋reply＋chunk1＋chunk2… 四則以上。原本的「上限 2 則」會在**單一輪
 * 之內**就被擠爆——長輩補播時只聽到那一輪的最後兩則，答案的開頭被擠掉了，比「整題
 * 沒有答案」更難理解。
 *
 * 以輪為單位之後，觸發門檻反而比原本更寬（原本 2 則、現在 2 輪），而 27b9c7c 當初
 * 要防的事（連續插嘴累積成一串舊語音）仍然被擋住。
 */
const MAX_DEFERRED_TURNS = 2;
const deferredTurnsRef = useRef<DeferredTurn[]>([]);
```

收集端（播放佇列回呼中 `micActiveRef.current` 為真的分支）改為：

```ts
      if (micActiveRef.current) {
        const turns = deferredTurnsRef.current;
        // ⚠️ 同一輪的續段要歸到同一個單位，不可另開一輪——收音期間該輪的續段仍會
        // 繼續推過來（後端不知道長輩在講話）。
        let target = turns.find((t) => t.turnId === item.turnId);
        if (!target) {
          target = { turnId: item.turnId, items: [] };
          turns.push(target);
          while (turns.length > MAX_DEFERRED_TURNS) {
            const dropped = turns.shift();
            // 整輪回收：`revokeQueuedReplyAudio` 是「除了這一則以外全部回收」、
            // 只留得住一個例外，用它會把還要播的那幾則一起毀掉（27b9c7c 的教訓）。
            dropped?.items.forEach((i) => deps.revokeReplyAudio(i.audioUrl));
          }
        }
        target.items.push(item);
        return;
      }
```

補播端 `flushDeferredReplies` 改為：

```ts
  const flushDeferredReplies = useCallback(() => {
    const queue = playQueueRef.current;
    if (queue === null || deferredTurnsRef.current.length === 0) {
      return;
    }
    const pending = deferredTurnsRef.current;
    deferredTurnsRef.current = [];
    // FIFO：舊的那一輪先播（理由見既有註解），輪內依抵達順序。
    for (const turn of pending) {
      for (const item of turn.items) {
        queue.push(item);
      }
    }
  }, []);
```

⚠️ 三處清空暫存的出口（開錄失敗、effect cleanup、`stopAndSend` 的 finally）全部把 `deferredRepliesRef.current = []` 改成 `deferredTurnsRef.current = []`。**逐一確認，漏一處就會留下失效的 blob URL。**

- [ ] **Step 4: 執行測試確認通過**

Run: `npm test -- useTalk`
Expected: PASS

- [ ] **Step 5: 全套測試 ＋ 型別 ＋ lint**

Run: `npm test && npm run typecheck && npm run lint`

- [ ] **Step 6: 變異驗證**

把 `MAX_DEFERRED_TURNS` 改成 1 → 第二條測試變紅（只剩 t3）。把 `turns.find(...)` 改成永遠 `undefined`（每則各開一輪）→ 第一條測試變紅。確認後改回。

- [ ] **Step 7: Commit**

```bash
git add web/src/elder/useTalk.ts web/src/elder/useTalk.test.ts
git commit -F- <<'EOF'
fix(web): 補播的單位從「一則音檔」改成「一輪回覆」

WHY：
`deferredRepliesRef` 上限 2 則，27b9c7c 當時的理由是「一輪最多 ack＋reply」。
續段改由後端主動推之後，一輪會產生 ack＋reply＋chunk1＋chunk2 四則以上——上限 2
會在**單一輪之內**就被擠爆。症狀：長輩插嘴後補播，只聽到那一輪的最後兩則，答案的
開頭被擠掉了。那比「整題沒有答案」更難理解。

WHAT：
`deferredTurnsRef: DeferredTurn[]`（`{turnId, items}`），上限 2 輪。收音期間抵達的
訊框依 turn_id 歸到對應的輪；滿了擠掉最舊那一輪並整輪回收其音檔。補播時 FIFO，
輪內依抵達順序。

⚠️ 同一輪的續段要歸到同一個單位、不可另開一輪：收音期間該輪的續段仍會繼續推過來，
後端不知道長輩在講話。

IMPACT：
- 觸發門檻比原本更寬（原本 2 則、現在 2 輪），27b9c7c 要防的「連續插嘴累積成一串
  舊語音」仍然被擋住
- 三處清空暫存的出口（開錄失敗、effect cleanup、stopAndSend 的 finally）同步改名，
  漏一處會留下失效的 blob URL
EOF
```

---

### Task 8: 文件同步

**Files:**
- Modify: `docs/dev/06_API設計規範.md`
- Modify: `docs/dev/07_模組規格與測試.md`
- Modify: `docs/dev/12_前端架構規範.md`
- Modify: `docs/dev/17_前端資訊架構.md`
- Modify: `docs/dev/README.md`

- [ ] **Step 1: 讀同步規則**

Read: `docs/dev/15_文檔與維護指南.md` §2 對照表，確認本次改動命中的每一列。

- [ ] **Step 2: 更新 06（API）**

WS 協定新增 `chunk` 下行訊框（欄位表同 spec §4.1）；`GET /api/v1/turns/chunks/{index}` 標記為**已移除**（含移除日期與原因一句）。

- [ ] **Step 3: 更新 07（模組規格與測試）**

`channels/app/ws.py`：新增 `_push_continuation_chunks`、`_Sender.send_chunk_audio`、`tts` 參數。
`turn_context.py`：新增 `inline_audio_delivery`。
`useTalk.ts` / `talkSocket.ts`：續段改被動接收、補播單位改輪。
各模組的測試清單補上本次新增的案例。

- [ ] **Step 4: 更新 12（前端架構規範）**

續段播放路徑改為被動接收；補播單位改為「輪」。

- [ ] **Step 5: 更新 17（前端資訊架構）**

人工驗收清單新增：
- 🔴 一則多段的回覆，段與段之間**沒有可察覺的空白**（跨三家瀏覽器實測）
- 🔴 長輩在多段回覆播到一半時插嘴，講完後**整輪從頭補播完整**
- 連續插嘴三輪，補播只保留最近兩輪，且沒有音檔洩漏（開發者工具看 blob URL 數量）

- [ ] **Step 6: 更新 README 文件狀態表**

各更動文件的版本號 +0.01、日期改 2026-08-01。

- [ ] **Step 7: Commit**

```bash
git add docs/dev
git commit -F- <<'EOF'
docs(dev): 同步續段 WS 直送的契約與驗收清單

WHY：
docs/dev 同步鐵律——程式碼改動完成時必須同一時間更新受影響的文件。本次改動命中
15 §2 對照表的 API 契約、模組規格、前端架構、資訊架構四列。

WHAT：
06 補 `chunk` 下行訊框欄位表、標記 `GET /turns/chunks/{index}` 已移除；
07 補 `_push_continuation_chunks`／`send_chunk_audio`／`inline_audio_delivery`
與各自的測試清單；12 記續段改被動接收與補播單位改輪；
17 新增三條人工驗收（段間無空白、插嘴後整輪補播、連續插嘴的音檔回收）。

IMPACT：
文件變更，無行為影響。README 文件狀態表版本與日期同步。
EOF
```

---

## 完成後的人工驗收（無法自動化）

⚠️ 以下三項腳本測不到，**必須真的用瀏覽器聽**：

1. **段間空白消失**——問一個會產生三段回覆的問題（例如「明天高雄天氣如何，我要去市場」），確認聽起來是連續一句話。⚠️ 跨 Chrome／Firefox／Safari，容器格式不同
2. **插嘴後整輪補播**——在第二段播放中按住麥克風講話，放開後確認**從第一段開始**完整播完舊答案，再播新問題的答案
3. **併發兩題各自有答案**——問新聞（慢）→ 立刻問天氣（快），確認兩題都有 ack、天氣先回、新聞後回且帶回指

驗收在真的 ngrok 網址上做（麥克風需要 HTTPS，localhost 的安全例外會給出假的安全感）。
