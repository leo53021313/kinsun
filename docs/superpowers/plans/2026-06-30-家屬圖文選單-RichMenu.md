# 家屬圖文選單（Rich Menu 開 LIFF）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 佈建一個開啟 LIFF 的家屬 Rich Menu，並在家屬綁定時 link 給該使用者。

**Architecture:** 新 `channels/line/richmenu.py`（佈建腳本）+ `LineApiMessenger.link_rich_menu` + `BindingFlow` 的 `on_guardian_bound` fail-safe hook；`app.py` 把 hook 接成「link 選單」，`RICH_MENU_ID` 未設則停用。

**Tech Stack:** Python 3.12 / linebot.v3.messaging（既有）/ pytest / ruff。

## Global Constraints

- 一律台灣繁體中文：文件、註解、commit 訊息。
- Python 3.12；ruff `E,W,F,I,B,UP`，line-length 100；不新增第三方套件（用既有 linebot v3）。
- **不可 `x = lambda`**（ruff E731）：需要函式時用 `def`。
- link 選單失敗只 log、不中斷綁定（fail-safe）；`RICH_MENU_ID` 未設 → 停用 link。
- 選單只給已綁定家屬（link-per-user，不設預設選單）；長輩綁定不觸發。
- commit 型別 `feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 單元測試全離線；SDK 的建立/上傳/link 屬 I/O、不單元測（沿用 messenger 慣例）。

---

## 檔案結構（File Structure）

**新增：** `src/kinsun/channels/line/richmenu.py`、`tests/test_richmenu.py`。
**修改：** `src/kinsun/channels/line/messenger.py`（`link_rich_menu`）、`src/kinsun/binding/flow.py`（hook）、`src/kinsun/config.py`（`rich_menu_id`）、`src/kinsun/app.py`（接線）、`.env.example`、`README.md`、`tests/test_binding_flow.py`、`tests/test_config.py`。

---

## Task 1：Rich Menu 佈建模組（richmenu.py）

**Files:**
- Create: `src/kinsun/channels/line/richmenu.py`
- Test: `tests/test_richmenu.py`

**Interfaces:**
- Produces: `build_rich_menu_request(liff_id: str) -> RichMenuRequest`、`setup_rich_menu(access_token, liff_id, image_path) -> str`、`main(argv=None) -> int`

- [ ] **Step 1：寫失敗測試** `tests/test_richmenu.py`

```python
from kinsun.channels.line.richmenu import build_rich_menu_request


def test_build_rich_menu_request_opens_liff():
    req = build_rich_menu_request("1234-ab")
    assert req.size.width == 2500
    assert req.size.height == 843
    assert req.chat_bar_text == "家屬選單"
    assert len(req.areas) == 1
    assert req.areas[0].action.uri == "https://liff.line.me/1234-ab"
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_richmenu.py -v`
Expected: FAIL（`ModuleNotFoundError: kinsun.channels.line.richmenu`）

- [ ] **Step 3：建立 `src/kinsun/channels/line/richmenu.py`**

```python
"""家屬圖文選單佈建：建立開啟 LIFF 的 Rich Menu（一次性，對真 LINE 執行）。

CLI：
  LINE_CHANNEL_ACCESS_TOKEN=... LIFF_ID=... \
  PYTHONPATH=src uv run python -m kinsun.channels.line.richmenu <image_path>
印出 rich_menu_id；設為環境變數 RICH_MENU_ID，家屬綁定時會 link 給該使用者。
圖片規格：2500x843、<=1MB、png/jpeg（png 上傳 content-type image/png）。
"""

from __future__ import annotations

import os
import sys

from linebot.v3.messaging import (
    ApiClient,
    Configuration,
    MessagingApi,
    MessagingApiBlob,
    RichMenuArea,
    RichMenuBounds,
    RichMenuRequest,
    RichMenuSize,
    URIAction,
)

_WIDTH = 2500
_HEIGHT = 843


def build_rich_menu_request(liff_id: str) -> RichMenuRequest:
    return RichMenuRequest(
        size=RichMenuSize(width=_WIDTH, height=_HEIGHT),
        selected=True,
        name="家屬選單",
        chat_bar_text="家屬選單",
        areas=[
            RichMenuArea(
                bounds=RichMenuBounds(x=0, y=0, width=_WIDTH, height=_HEIGHT),
                action=URIAction(uri=f"https://liff.line.me/{liff_id}", label="開啟家屬儀表板"),
            )
        ],
    )


def setup_rich_menu(access_token: str, liff_id: str, image_path: str) -> str:
    config = Configuration(access_token=access_token)
    with ApiClient(config) as client:
        rich_menu_id = MessagingApi(client).create_rich_menu(
            build_rich_menu_request(liff_id)
        ).rich_menu_id
        with open(image_path, "rb") as image:
            MessagingApiBlob(client).set_rich_menu_image(rich_menu_id, body=image.read())
    return rich_menu_id


def main(argv: list[str] | None = None) -> int:
    args = sys.argv[1:] if argv is None else argv
    if not args:
        print("用法：python -m kinsun.channels.line.richmenu <image_path>")
        return 1
    token = os.environ["LINE_CHANNEL_ACCESS_TOKEN"]
    liff_id = os.environ["LIFF_ID"]
    rich_menu_id = setup_rich_menu(token, liff_id, args[0])
    print(f"rich_menu_id={rich_menu_id}")
    print("請把它設為環境變數 RICH_MENU_ID，家屬綁定時會自動 link。")
    return 0


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_richmenu.py -v`
Expected: PASS（若 linebot 欄位名不同導致 AttributeError，改用對應 v3 欄位並保持測試斷言的語意）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/channels/line/richmenu.py tests/test_richmenu.py
git commit -m "feat: Rich Menu 佈建模組（build_rich_menu_request + setup + CLI）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：messenger.link_rich_menu

**Files:**
- Modify: `src/kinsun/channels/line/messenger.py`

**Interfaces:**
- Produces: `LineMessenger.link_rich_menu(user_id, rich_menu_id)`、`LineApiMessenger.link_rich_menu`

- [ ] **Step 1：實作**（`src/kinsun/channels/line/messenger.py`）

`LineMessenger` Protocol 加：
```python
    def link_rich_menu(self, user_id: str, rich_menu_id: str) -> None: ...
```
`LineApiMessenger` 加方法（在 `display_name` 之後）：
```python
    def link_rich_menu(self, user_id: str, rich_menu_id: str) -> None:
        with self._ApiClient(self._configuration) as api_client:
            api = self._MessagingApi(api_client)
            api.link_rich_menu_id_to_user(user_id, rich_menu_id)
```

- [ ] **Step 2：匯入驗證**

Run: `PYTHONPATH=src uv run python -c "import kinsun.channels.line.messenger; print('ok')"`
Expected: 印出 `ok`。

- [ ] **Step 3：commit**

```bash
git add src/kinsun/channels/line/messenger.py
git commit -m "feat: LineApiMessenger.link_rich_menu（link 圖文選單給使用者）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：BindingFlow on_guardian_bound hook

**Files:**
- Modify: `src/kinsun/binding/flow.py`、`tests/test_binding_flow.py`

**Interfaces:**
- Produces: `BindingFlow.__init__(..., on_guardian_bound: Callable[[str], None] | None = None)`；成為家屬時 fail-safe 呼叫

- [ ] **Step 1：改測試**（`tests/test_binding_flow.py`）

`_build_flow` 改為多收 `on_guardian_bound`：
```python
def _build_flow(accounts, sessions, profiles, *, clock, on_guardian_bound=None):
    med_ids = (f"m{i}" for i in count(1))
    medications = MedicationService(FakeMedicationStore(), new_id=lambda: next(med_ids))
    menu = MedicationMenu(medications, accounts, sessions, clock=clock)
    appt_ids = (f"a{i}" for i in count(1))
    appointments = AppointmentService(FakeAppointmentStore(), new_id=lambda: next(appt_ids))
    appt_menu = AppointmentMenu(appointments, accounts, sessions, clock=clock)
    return BindingFlow(
        accounts,
        sessions,
        profiles,
        menu,
        appt_menu,
        clock=clock,
        session_ttl_seconds=600,
        on_guardian_bound=on_guardian_bound,
    )
```
`_flow` 改為多收並傳遞 `on_guardian_bound`：
```python
def _flow(repo=None, *, now=NOW, profiles=None, code="ABCDEFGHIJKLMNOP", on_guardian_bound=None):
    repo = repo or FakeAccountRepository()
    ids = (f"id{i}" for i in count(1))
    accounts = AccountService(
        repo, clock=lambda: now, new_id=lambda: next(ids), new_code=lambda: code
    )
    sessions = FakeBindingSessionStore()
    flow = _build_flow(
        accounts,
        sessions,
        profiles or _FakeProfiles(),
        clock=lambda: now,
        on_guardian_bound=on_guardian_bound,
    )
    return flow, accounts, repo
```
檔末新增：
```python
def test_create_elder_links_menu():
    bound = []
    flow, _, _ = _flow(profiles=_FakeProfiles("林小明"), on_guardian_bound=bound.append)
    flow.handle("U-son", "設定")
    flow.handle("U-son", "1")
    flow.handle("U-son", "阿公")
    assert bound == ["U-son"]


def test_guardian_redeem_links_menu():
    flow, accounts, _ = _flow()
    bound = []
    flow2, accounts2, _ = _flow(on_guardian_bound=bound.append)
    elder = accounts2.create_elder("U-son", "兒子", "阿公")
    inv = accounts2.generate_invite(elder.elder_id, InviteRole.GUARDIAN)
    flow2.handle("U-daughter", inv.code)
    flow2.handle("U-daughter", "是")
    assert bound == ["U-daughter"]


def test_elder_redeem_does_not_link_menu():
    bound = []
    flow, accounts, _ = _flow(on_guardian_bound=bound.append)
    elder = accounts.create_elder("U-son", "兒子", "阿公")
    inv = accounts.generate_invite(elder.elder_id, InviteRole.ELDER)
    flow.handle("U-elder", "設定")
    flow.handle("U-elder", "3")
    flow.handle("U-elder", inv.code)
    flow.handle("U-elder", "是")
    assert bound == []


def test_link_failure_does_not_break_binding():
    def boom(line):
        raise RuntimeError("link fail")

    flow, accounts, _ = _flow(on_guardian_bound=boom)
    elder = accounts.create_elder("U-son", "兒子", "阿公")
    inv = accounts.generate_invite(elder.elder_id, InviteRole.GUARDIAN)
    flow.handle("U-daughter", inv.code)
    assert "綁定成功" in flow.handle("U-daughter", "是")
```
（`test_guardian_redeem_links_menu` 只用 flow2/accounts2；第一行 `_flow()` 可省略——實作時只保留用到的那組。）

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_binding_flow.py -v`
Expected: FAIL（`BindingFlow.__init__` 不接受 `on_guardian_bound`）

- [ ] **Step 3：實作**（`src/kinsun/binding/flow.py`）

`__init__` 簽章在 `session_ttl_seconds: int = 600,` 之後加：
```python
        on_guardian_bound: Callable[[str], None] | None = None,
```
建構子內存欄位（在 `self._ttl = session_ttl_seconds` 之後）：
```python
        self._on_guardian_bound = on_guardian_bound
```
新增私有方法（放在 `_handle` 之前或之後）：
```python
    def _guardian_bound(self, line: str) -> None:
        if self._on_guardian_bound is None:
            return
        try:
            self._on_guardian_bound(line)
        except Exception:  # noqa: BLE001 - link 選單失敗不可中斷綁定
            logger.exception("link 圖文選單失敗 line=%s", line)
```
`_create_elder`：在 `invite = self._accounts.generate_invite(...)` 之後、`return` 之前加：
```python
        self._guardian_bound(line)
```
`_confirm` 家屬分支：把
```python
            return "綁定成功！長輩有狀況時，金孫會通知您。"
```
改為
```python
            self._guardian_bound(line)
            return "綁定成功！長輩有狀況時，金孫會通知您。"
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_binding_flow.py -v`
Expected: PASS（既有 + 新增 4）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/binding/flow.py tests/test_binding_flow.py
git commit -m "feat: BindingFlow on_guardian_bound hook（成為家屬時 fail-safe 觸發）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：config + app.py 接線 + .env.example

**Files:**
- Modify: `src/kinsun/config.py`、`src/kinsun/app.py`、`.env.example`、`tests/test_config.py`

**Interfaces:**
- Consumes: `messenger.link_rich_menu`（Task 2）、`on_guardian_bound`（Task 3）
- Produces: `Settings.rich_menu_id: str`

- [ ] **Step 1：改測試**（`tests/test_config.py` 的 defaults 測試加一行）

```python
    assert settings.rich_menu_id == ""
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_config.py::test_load_settings_reads_required_and_defaults -v`
Expected: FAIL（`AttributeError: rich_menu_id`）

- [ ] **Step 3：實作**

`src/kinsun/config.py`：`Settings` 在 `liff_timeout_seconds: float` 之後加：
```python
    rich_menu_id: str
```
`load_settings` 在 `liff_timeout_seconds=...` 之後加：
```python
        rich_menu_id=env.get("RICH_MENU_ID", ""),
```
`.env.example`：在 LIFF 區塊加：
```
# 家屬圖文選單（Rich Menu）
LIFF_ID=
RICH_MENU_ID=
```
`src/kinsun/app.py`：在 `binding = BindingFlow(...)` 之前加（**用 def，勿用 `x = lambda`**）：
```python
    def _link_menu(line: str) -> None:
        messenger.link_rich_menu(line, settings.rich_menu_id)

    on_guardian_bound = _link_menu if settings.rich_menu_id else None
```
`BindingFlow(...)` 呼叫在 `session_ttl_seconds=...` 之後加：
```python
        on_guardian_bound=on_guardian_bound,
```

- [ ] **Step 4：匯入驗證 + 全量測試**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"
uv run pytest -q
```
Expected: `import ok`；pytest 全綠。

- [ ] **Step 5：commit**

```bash
git add src/kinsun/config.py src/kinsun/app.py .env.example tests/test_config.py
git commit -m "feat: 組裝層把 RICH_MENU_ID 接成綁定時 link 選單

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5：README + 全量品質閘門

**Files:**
- Modify: `README.md`

- [ ] **Step 1：README 補「家屬圖文選單（Rich Menu）」一節**

在「家屬端 LIFF」一節之後加入：
```markdown
### 家屬圖文選單（Rich Menu，可選）

讓已綁定家屬在 LINE 底部有「開啟家屬儀表板」按鈕。

1. 準備一張選單圖（2500×843、≤1MB、png/jpeg）。
2. 佈建（對真 LINE 執行一次）：
   ```bash
   LINE_CHANNEL_ACCESS_TOKEN=... LIFF_ID=<你的 LIFF ID> \
   PYTHONPATH=src uv run python -m kinsun.channels.line.richmenu <image_path>
   ```
   會印出 `rich_menu_id`。
3. 把它設為後端環境變數 `RICH_MENU_ID`。之後家屬一綁定（建立長輩或兌換家屬邀請碼）即自動獲得選單。
   `RICH_MENU_ID` 未設則此功能停用、綁定照常。
```

- [ ] **Step 2：後端 lint / format / 測試**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠。format 報未格式化 → `uv run ruff format .` 後重跑並把變更納入本 commit。

- [ ] **Step 3：pre-commit 全鉤子 + 匯入驗證**

Run:
```bash
uv run pre-commit run --all-files
PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"
```
Expected: 全 Passed；`import ok`。

- [ ] **Step 4：commit**

```bash
git add README.md
git commit -m "docs: README 補家屬圖文選單（Rich Menu）佈建說明

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review（計畫對 spec 覆蓋檢查）

- 佈建模組 `build_rich_menu_request`/`setup_rich_menu`/`main`（spec §3）→ Task 1 ✅
- `messenger.link_rich_menu`（§4）→ Task 2 ✅
- `BindingFlow.on_guardian_bound` + 兩點觸發 + fail-safe（§4）→ Task 3 ✅
- config `rich_menu_id` + app 接線（def 非 lambda）+ .env.example（§5）→ Task 4 ✅
- 測試：payload 建構、hook 觸發/不觸發/fail-safe（§6）→ Task 1、3 ✅
- README 佈建說明（§2.1）→ Task 5 ✅

型別/命名一致性：`build_rich_menu_request(liff_id) -> RichMenuRequest`、`link_rich_menu(user_id, rich_menu_id)`、`on_guardian_bound: Callable[[str], None]`、`Settings.rich_menu_id`；URI `https://liff.line.me/{liff_id}`。避免 E731（app 用 `def _link_menu`）。無佔位符。
