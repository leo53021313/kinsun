# 綁定閘門 v1 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 只有「綁定且同意有效」的長輩語音才走完整 AI 對話；其餘語音改回綁定引導訊息。

**Architecture:** `AccountService.is_consented_elder` 做同意判斷；`ConsentGate`（只依賴小介面 `ConsentChecker`，fail-open）包住它；webhook 語音路徑先過閘門。文字與貼圖路徑不變。

**Tech Stack:** Python 3.12、pytest。**無新依賴、無新資料表、無新環境變數。**

## Global Constraints

- src layout（`src/kinsun/`），pytest `pythonpath=["src","."]`。
- 單元測試全離線：注入 `FakeAccountRepository`、fake gate/checker。
- fail-open：`ConsentGate.allows` 任何例外 → 回 `True`（記 log），不鎖長輩。
- 只擋語音；文字維持走綁定流程。
- ruff `E,W,F,I,B,UP`、line-length 100。commit 結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不自動 push；只在 Leo 分支工作；main 經 PR 合併（沿用既有 `--admin` + 同步流程，使用者已授權）。

---

## File Structure

```
src/kinsun/accounts/service.py     # + is_consented_elder
src/kinsun/binding/gate.py         # 新增：ConsentChecker(Protocol)、ConsentGate
src/kinsun/channels/line/webhook.py# 語音路徑加閘門；create_app 加 gate
src/kinsun/app.py                  # 組 ConsentGate 注入 create_app
tests/test_accounts_service.py     # + is_consented_elder
tests/test_binding_gate.py         # 新增
tests/test_webhook.py              # + 閘門擋語音；_make_client 加 gate
```

---

## Task 1：AccountService.is_consented_elder

**Files:** Modify `src/kinsun/accounts/service.py`、`tests/test_accounts_service.py`

**Interfaces:**
- Produces：`AccountService.is_consented_elder(line_user_id: str) -> bool`

- [ ] **Step 1：失敗測試** 於 `tests/test_accounts_service.py` 末尾追加：

```python
def test_is_consented_elder_lifecycle():
    repo = FakeAccountRepository()
    svc = _service(repo)
    assert svc.is_consented_elder("U-elder") is False
    elder = svc.create_elder("U-son", "兒子", "阿公")
    inv = svc.generate_invite(elder.elder_id, InviteRole.ELDER)
    svc.redeem_invite(inv.code, "U-elder", consent_by=ConsentBy.SELF)
    assert svc.is_consented_elder("U-elder") is True
    svc.revoke_consent(elder.elder_id)
    assert svc.is_consented_elder("U-elder") is False


def test_is_consented_elder_bound_without_consent():
    from kinsun.accounts.models import Elder

    repo = FakeAccountRepository()
    svc = _service(repo)
    repo.save_elder(Elder("e1", "阿公", "U-elder"))
    assert svc.is_consented_elder("U-elder") is False
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_accounts_service.py -q -k is_consented_elder`
  Expected：FAIL（`AttributeError: ... 'is_consented_elder'`）。

- [ ] **Step 3：實作** 於 `src/kinsun/accounts/service.py` 的 `AccountService`，在 `guardians_of` 後加：

```python
    def is_consented_elder(self, line_user_id: str) -> bool:
        elder = self._repo.get_elder_by_line(line_user_id)
        if elder is None:
            return False
        consent = self._repo.get_consent(elder.elder_id)
        return consent is not None and consent.revoked_at is None
```

- [ ] **Step 4：確認通過** Run：`uv run pytest tests/test_accounts_service.py -q`
  Expected：PASS。

- [ ] **Step 5：commit**
```bash
git add src/kinsun/accounts/service.py tests/test_accounts_service.py
git commit -m "feat: AccountService.is_consented_elder（綁定且同意有效）"
```

---

## Task 2：ConsentGate

**Files:** Create `src/kinsun/binding/gate.py`、`tests/test_binding_gate.py`

**Interfaces:**
- Consumes：`AccountService.is_consented_elder`（duck-typed 滿足 `ConsentChecker`）
- Produces：`ConsentChecker`(Protocol：`is_consented_elder(line_user_id) -> bool`)、`ConsentGate(checker)`，`allows(line_user_id) -> bool`

- [ ] **Step 1：失敗測試** Create `tests/test_binding_gate.py`：

```python
from kinsun.binding.gate import ConsentGate


class _Checker:
    def __init__(self, result=True, boom=False):
        self._result = result
        self._boom = boom

    def is_consented_elder(self, line_user_id):
        if self._boom:
            raise RuntimeError("db down")
        return self._result


def test_gate_delegates_to_checker():
    assert ConsentGate(_Checker(True)).allows("U-1") is True
    assert ConsentGate(_Checker(False)).allows("U-1") is False


def test_gate_fail_open_on_error():
    assert ConsentGate(_Checker(boom=True)).allows("U-1") is True
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_binding_gate.py -q`
  Expected：FAIL（`ModuleNotFoundError: kinsun.binding.gate`）。

- [ ] **Step 3：實作** Create `src/kinsun/binding/gate.py`：

```python
"""綁定閘門：只有同意有效的長輩語音才走完整對話。fail-open（故障放行）。"""

from __future__ import annotations

import logging
from typing import Protocol

logger = logging.getLogger("kinsun.binding")


class ConsentChecker(Protocol):
    def is_consented_elder(self, line_user_id: str) -> bool: ...


class ConsentGate:
    def __init__(self, checker: ConsentChecker) -> None:
        self._checker = checker

    def allows(self, line_user_id: str) -> bool:
        try:
            return self._checker.is_consented_elder(line_user_id)
        except Exception:  # noqa: BLE001
            logger.exception("同意檢查失敗，放行 line=%s", line_user_id)
            return True
```

- [ ] **Step 4：確認通過** Run：`uv run pytest tests/test_binding_gate.py -q`
  Expected：PASS。

- [ ] **Step 5：commit**
```bash
git add src/kinsun/binding/gate.py tests/test_binding_gate.py
git commit -m "feat: ConsentGate（綁定閘門，fail-open）"
```

---

## Task 3：webhook 接閘門 + app 組裝

**Files:** Modify `src/kinsun/channels/line/webhook.py`、`tests/test_webhook.py`、`src/kinsun/app.py`

**Interfaces:**
- Consumes：`ConsentGate.allows(session_id) -> bool`
- Produces：`create_app(*, parser, pipeline, messenger, binding, gate)`；常數 `BIND_FIRST_PROMPT`

- [ ] **Step 1：失敗測試** 於 `tests/test_webhook.py`：

import 加 `BIND_FIRST_PROMPT`：
```python
from kinsun.channels.line.webhook import (
    BIND_FIRST_PROMPT,
    FALLBACK_PROMPT,
    NON_AUDIO_PROMPT,
    create_app,
)
```
在 `FakeParser` 後加閘門替身：
```python
class _AllowGate:
    def allows(self, line_user_id):
        return True


class _DenyGate:
    def allows(self, line_user_id):
        return False
```
`_make_client` 加 `gate` 參數並傳入 `create_app`：
```python
def _make_client(parser, messenger, asr=None, memory=None, binding=None, gate=None):
    pipeline = VoicePipeline(
        asr=asr or MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM(), memory or NullMemory(), NullContext()),
        tts=TextBubbleTts(),
        detector=_NullDetector(),
        notifier=_NullNotifier(),
    )
    app = create_app(
        parser=parser,
        pipeline=pipeline,
        messenger=messenger,
        binding=binding or _NullBinding(),
        gate=gate or _AllowGate(),
    )
    return TestClient(app)
```
追加測試：
```python
def test_unconsented_voice_is_blocked():
    messenger = FakeMessenger()
    client = _make_client(FakeParser([_audio_event("U-x")]), messenger, gate=_DenyGate())
    client.post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert messenger.replies == [("rt-1", BIND_FIRST_PROMPT)]


def test_consented_voice_runs_pipeline():
    messenger = FakeMessenger()
    client = _make_client(FakeParser([_audio_event()]), messenger, gate=_AllowGate())
    client.post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert messenger.replies == [("rt-1", "你說的是：阿公早安")]
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_webhook.py -q`
  Expected：FAIL（`ImportError: BIND_FIRST_PROMPT` 或 `create_app() ... 'gate'`）。

- [ ] **Step 3：實作 webhook** 改 `src/kinsun/channels/line/webhook.py`：

常數區加：
```python
BIND_FIRST_PROMPT = (
    "金孫需要先完成綁定才能陪您聊天喔。"
    "請把家人給您的邀請碼貼到這裡，或回覆「設定」開始。"
)
```
`_handle_events` 加 `gate` 參數，語音分支於 `get_audio` 前加閘門：
```python
def _handle_events(events, pipeline: VoicePipeline, messenger: LineMessenger, binding, gate) -> None:
    for event in events:
        message = getattr(event, "message", None)
        reply_token = getattr(event, "reply_token", None)
        if message is None or reply_token is None:
            continue
        source = getattr(event, "source", None)
        session_id = getattr(source, "user_id", None) or "unknown"
        mtype = getattr(message, "type", None)
        if mtype == "text":
            reply = binding.handle(session_id, getattr(message, "text", "") or "")
            messenger.reply_text(reply_token, reply if reply is not None else NON_AUDIO_PROMPT)
            continue
        if mtype != "audio":
            messenger.reply_text(reply_token, NON_AUDIO_PROMPT)
            continue
        if not gate.allows(session_id):
            messenger.reply_text(reply_token, BIND_FIRST_PROMPT)
            continue
        try:
            audio = messenger.get_audio(message.id)
            result = pipeline.process(audio, session_id=session_id)
            messenger.reply_text(reply_token, result.text)
        except (ASRError, LLMError, MemoryError):
            messenger.reply_text(reply_token, FALLBACK_PROMPT)
```
`create_app` 簽章與呼叫加 `gate`：
```python
def create_app(*, parser, pipeline: VoicePipeline, messenger: LineMessenger, binding, gate) -> FastAPI:
    ...
        _handle_events(events, pipeline, messenger, binding, gate)
```

- [ ] **Step 4：確認 webhook 通過** Run：`uv run pytest tests/test_webhook.py -q`
  Expected：PASS（語音/文字/閘門皆通過）。

- [ ] **Step 5：改 app.py** 於 `build_app`，在 `binding = BindingFlow(...)` 後加：
```python
    gate = ConsentGate(accounts)
```
import 段加 `from kinsun.binding.gate import ConsentGate`；`return create_app(...)` 改為帶 `gate`：
```python
    return create_app(
        parser=parser, pipeline=pipeline, messenger=messenger, binding=binding, gate=gate
    )
```

- [ ] **Step 6：語法 + 品質** Run：
  `uv run python -c "import ast, pathlib; ast.parse(pathlib.Path('src/kinsun/app.py').read_text(encoding='utf-8')); print('ok')"`、
  `uv run pytest -q`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run pre-commit run --all-files`
  Expected：全綠（整合測試 SKIP）。

- [ ] **Step 7：commit + 上線**
```bash
git add src/kinsun/channels/line/webhook.py tests/test_webhook.py src/kinsun/app.py
git commit -m "feat: webhook 語音路徑接綁定閘門；app 組裝"
git push origin Leo
gh pr create --base main --head Leo --title "feat: 綁定閘門 v1（未同意語音→引導綁定）" --body "<摘要>"
gh pr merge <n> --merge --admin
git fetch origin
git push origin origin/main:Babic origin/main:Brian origin/main:Kevin origin/main:MA origin/main:Otto
git merge --ff-only origin/main && git push origin Leo
```
> Jerry 分支有自有提交，FF 會被拒，**不強推**。

---

## Self-Review

**Spec coverage：** §4.1 `is_consented_elder` → Task 1。§4.2 `ConsentGate`/`ConsentChecker` + fail-open → Task 2。§4.3 webhook 語音閘門 + `BIND_FIRST_PROMPT` → Task 3。§4.4 app 組裝 → Task 3。§5 fail-open → Task 2 測試。§6 測試 → 各 Task。

**Placeholder scan：** PR body `<摘要>` 執行時填入；其餘步驟皆含實際程式碼。

**Type consistency：** `is_consented_elder(line_user_id) -> bool`、`ConsentGate(checker).allows(line_user_id) -> bool`、`create_app(*, parser, pipeline, messenger, binding, gate)`、`_handle_events(events, pipeline, messenger, binding, gate)`、`BIND_FIRST_PROMPT` 跨任務一致。
