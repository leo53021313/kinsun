# JS 端測試基建實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `app/` 與 `frontend/` 各有可跑的測試框架、CI 會擋，且今天真正燒到的兩處（App 定位、admin 資料載入）從此有自動化驗證守著。

**Architecture:** 兩套 runner（frontend 用 vitest 複用既有 vite.config.ts；app 用 Expo 官方的 jest-expo）。順序刻意解掉雞生蛋：先立 vitest 並對現有的 MemoryTab 寫測試釘住現況，再 TDD 寫 `useLoadable`，遷移七頁後那份測試仍綠即為重構無副作用的證據。

**Tech Stack:** vitest、@testing-library/react、jsdom（frontend）；jest-expo、@testing-library/react-native（app）

設計來源：`docs/superpowers/specs/2026-07-17-js端測試基建-design.md`。有疑義時以 spec 為準。

## Global Constraints

- 語言：台灣繁體中文，全形標點；程式碼註解、commit 訊息皆同。
- 分支：只在 `Leo` 上工作，不切 main、不自動 push。
- ⚠️ **本輪不追 80% 覆蓋率**（Leo 核定，spec「與既有規則的抵觸」節）。只寫 spec 第 4 節列的四個測試檔，不為 `theme.ts`／`strings.ts` 這類常數字典補測試。
- ⚠️ **`useLoadable` 不得改變任何畫面行為**：它是把七頁現有行為原樣收攏。任何「順便改善」都會讓 Task 5 的證據失效。
- ⚠️ **`setError` 一律在非同步回呼中呼叫**，不得寫在函式開頭——那是 PR #56 修掉的 `set-state-in-effect`，且 CI 現在會擋。
- ⚠️ 動 `app/` 前必讀 v54 版本化文件（`app/AGENTS.md` 明令）：https://docs.expo.dev/versions/v54.0.0/
- ⚠️ 裝完依賴立刻確認 `dependencies` 沒被動到——只有 `devDependencies` 該變。`expo install` 有把套件塞進 `dependencies` 的前科（PR #56）。
- 每個 Task 結束前工作區必須乾淨（`git status --short` 為空）。

---

### Task 1: `frontend/` 的 vitest ＋ 釘住 MemoryTab 現況

**Files:**
- Modify: `frontend/package.json`（devDeps ＋ test script）、`frontend/package-lock.json`
- Modify: `frontend/vite.config.ts`（test 區塊）
- Create: `frontend/src/test-setup.ts`
- Create: `frontend/src/admin/pages/elder-tabs/MemoryTab.test.tsx`

**Interfaces:**
- Consumes: 無
- Produces: `cd frontend && npm test` 這個指令；`MemoryTab.test.tsx` 為 Task 5 驗證重構無副作用的樁

⚠️ 本 Task 的測試是**釘住現有行為**（characterization test），不是 TDD——它針對已存在的 `MemoryTab` 而寫，寫完當下就該綠。這是 spec 核定的例外，用來解掉「重構前沒有測試守著」的雞生蛋。

- [ ] **Step 1: 安裝依賴**

```bash
cd frontend && npm i -D vitest @testing-library/react @testing-library/jest-dom jsdom
```

裝完立刻確認 `frontend/package.json` 的 `dependencies` 未被動到：

```bash
cd /home/leo29/kinsun && git diff -- frontend/package.json
```

Expected: 只有 `devDependencies` 多四個項目。

- [ ] **Step 2: 加入 test script**

在 `frontend/package.json` 的 `scripts` 中，於 `"lint": "eslint src"` 之後加入：

```json
    "test": "vitest run"
```

⚠️ 用 `vitest run` 而非 `vitest`：後者是 watch 模式，CI 會卡住不結束。

- [ ] **Step 3: 建立測試環境設定**

建立 `frontend/src/test-setup.ts`：

```ts
/** vitest 的全域設定：把 @testing-library/jest-dom 的斷言（如 toBeInTheDocument）掛上。 */

import "@testing-library/jest-dom/vitest";
```

- [ ] **Step 4: 把 test 區塊併進既有的 vite config**

在 `frontend/vite.config.ts` 中，將：

```ts
export default defineConfig({
  base: "/liff/",
  plugins: [react()],
  resolve: {
    alias: { "kinsun-shared": path.resolve(__dirname, "../shared") },
  },
```

替換為：

```ts
export default defineConfig({
  base: "/liff/",
  plugins: [react()],
  // 測試設定併入既有 config 而非另開一份：kinsun-shared 的 alias 已在下方 resolve
  // 中，複用即為單一真相；另開設定檔就得維護兩份會漂移的 alias。
  test: {
    environment: "jsdom",
    setupFiles: ["./src/test-setup.ts"],
    globals: true,
  },
  resolve: {
    alias: { "kinsun-shared": path.resolve(__dirname, "../shared") },
  },
```

⚠️ `defineConfig` 需自 `vite` 匯入才吃得到 `test` 欄位的型別。若 `tsc --noEmit` 抱怨 `test` 不存在於型別上，把匯入改為 `import { defineConfig } from "vitest/config";`（它的型別涵蓋 vite 全部欄位再加 `test`）——以實際跑出來的錯誤為準。

- [ ] **Step 5: 寫釘住現況的測試**

建立 `frontend/src/admin/pages/elder-tabs/MemoryTab.test.tsx`：

```tsx
/**
 * MemoryTab 的釘樁測試（characterization test）。
 *
 * ⚠️ 它的用途不只是測 MemoryTab：Task 5 把七頁遷移到 useLoadable 之後，本檔
 * 仍須綠——那就是「重構沒有改壞行為」的證據。故本檔刻意只斷言使用者看得到的
 * 東西（畫面上的字），不碰任何內部實作，才能跨越重構存活。
 */

import { render, screen } from "@testing-library/react";
import { MemoryRouter, Route, Routes } from "react-router-dom";
import { beforeEach, describe, expect, it, vi } from "vitest";

import { MemoryTab } from "./MemoryTab";

vi.mock("../../api", () => ({
  getElderMemory: vi.fn(),
}));

const { getElderMemory } = await import("../../api");

function renderTab() {
  return render(
    <MemoryRouter initialEntries={["/elders/e1/memory"]}>
      <Routes>
        <Route path="/elders/:elderId/memory" element={<MemoryTab />} />
      </Routes>
    </MemoryRouter>,
  );
}

describe("MemoryTab", () => {
  beforeEach(() => {
    vi.mocked(getElderMemory).mockReset();
  });

  it("載入成功時顯示長期記憶內容", async () => {
    vi.mocked(getElderMemory).mockResolvedValue({
      memories: [{ text: "阿公喜歡吃滷肉飯", provenance: "自述", date: "2026-07-10" }],
      summaries: [],
    });

    renderTab();

    expect(await screen.findByText("阿公喜歡吃滷肉飯")).toBeInTheDocument();
  });

  it("載入失敗時顯示錯誤橫幅", async () => {
    vi.mocked(getElderMemory).mockRejectedValue(new Error("boom"));

    renderTab();

    expect(await screen.findByText("載入失敗，請重新整理。")).toBeInTheDocument();
  });

  it("載入完成前顯示載入中", () => {
    vi.mocked(getElderMemory).mockReturnValue(new Promise(() => {}));

    renderTab();

    expect(screen.getByText("載入中…")).toBeInTheDocument();
  });
});
```

- [ ] **Step 6: 執行測試，確認全綠**

Run: `cd frontend && npm test`
Expected: PASS（3 passed）

⚠️ 這裡**不該**紅——它釘的是已經存在且正常的行為。若紅了，代表測試寫錯（或 MemoryTab 真的有 bug），**停下來回報**，不要改 MemoryTab 去遷就測試。

- [ ] **Step 7: 確認測試真的會抓（閘門有效性）**

暫時把 `MemoryTab.tsx` 第 28 行的錯誤橫幅字串改掉：

```bash
cd /home/leo29/kinsun/frontend
sed -i 's/{strings.common.loadFailedRefresh}/{"故意改壞"}/' src/admin/pages/elder-tabs/MemoryTab.tsx
npm test    # 應該紅：找不到「載入失敗，請重新整理。」
cd /home/leo29/kinsun && git checkout -- frontend/src/admin/pages/elder-tabs/MemoryTab.tsx
cd frontend && npm test    # 應該綠
```

Expected: 第一次 FAIL、還原後 PASS。⚠️ 確認 `git status --short` 在還原後不含 `MemoryTab.tsx`。

- [ ] **Step 8: 確認未破壞既有檢查**

Run: `cd frontend && npm run lint && npm run typecheck && npm run build && npm run build:admin`
Expected: 全部 PASS

⚠️ 新的測試檔會被 eslint 掃到。若 `vi`／`describe`／`it` 被判 `no-undef`，是因為 eslint 不知道 vitest 的全域——在 `frontend/eslint.config.js` 補一段針對 `**/*.test.{ts,tsx}` 的設定，把它們宣告為 globals（或改為在測試檔明確 `import { describe, it, expect } from "vitest"`，本計畫的測試碼已如此匯入，優先靠匯入解決）。

- [ ] **Step 9: Commit**

```bash
cd /home/leo29/kinsun
git add frontend/package.json frontend/package-lock.json frontend/vite.config.ts frontend/src/test-setup.ts frontend/src/admin/pages/elder-tabs/MemoryTab.test.tsx frontend/eslint.config.js
git commit -F - <<'EOF'
test(frontend): 立起 vitest，並釘住 MemoryTab 的現有行為

frontend/ 從來沒有測試框架。昨天改 7 頁 admin 時，守著它們的只有 tsc 與
build——兩者都證明不了畫面還能運作，最後只能靠人點一遍。本 commit 補上第
一塊：測試跑得起來，而且真的看得到畫面。

測試設定併進既有的 vite.config.ts 而非另開一份：kinsun-shared 的 alias 已在
那裡，複用即為單一真相；另開設定檔就得維護兩份會漂移的 alias。

MemoryTab.test.tsx 是釘樁測試（characterization test），不是 TDD——它針對
已存在且正常的行為而寫，寫完當下就綠。用途是解掉下一步的雞生蛋：七頁要抽
共用 hook，但重構當下沒有測試守著。本檔遷移後仍須綠，那就是重構沒改壞行為
的證據。故它刻意只斷言使用者看得到的字，不碰任何內部實作，才跨得過重構。

已驗證閘門真的會抓、不是空轉：把錯誤橫幅的字串改掉 → 測試紅 → 還原 → 綠。

IMPACT：
- 純新增測試與工具設定，無執行期程式碼變更、無行為變更。
- CI 尚未跑 npm test（排在本系列最後），故此時不影響任何人。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: `useLoadable`（TDD）

**Files:**
- Create: `frontend/src/admin/useLoadable.ts`
- Create: `frontend/src/admin/useLoadable.test.tsx`

**Interfaces:**
- Consumes: Task 1 的 vitest
- Produces:

```ts
export function useLoadable<T, E = true>(
  fetcher: () => Promise<T> | null,
  mapError?: (exc: unknown) => E,
): { data: T | null; error: E | null; reload: () => void };
```

- [ ] **Step 1: 寫失敗的測試**

建立 `frontend/src/admin/useLoadable.test.tsx`：

```tsx
/** useLoadable：把七頁 admin 重複的「載入 → 成功顯示資料／失敗顯示錯誤」收成一處。 */

import { act, renderHook, waitFor } from "@testing-library/react";
import { describe, expect, it, vi } from "vitest";

import { useLoadable } from "./useLoadable";

describe("useLoadable", () => {
  it("載入成功時回傳資料、error 為 null", async () => {
    const fetcher = vi.fn().mockResolvedValue("阿公");

    const { result } = renderHook(() => useLoadable(fetcher));

    await waitFor(() => expect(result.current.data).toBe("阿公"));
    expect(result.current.error).toBeNull();
  });

  it("載入失敗時 error 預設為 true、data 維持 null", async () => {
    const fetcher = vi.fn().mockRejectedValue(new Error("boom"));

    const { result } = renderHook(() => useLoadable(fetcher));

    await waitFor(() => expect(result.current.error).toBe(true));
    expect(result.current.data).toBeNull();
  });

  it("錯誤後重載成功，error 才消失", async () => {
    // ⚠️ 這條正是 PR #56 刻意改變的行為：錯誤橫幅留到成功為止，而非一按重載
    // 就立刻消失。抽成 hook 後，這個語意只有這裡守得住。
    const fetcher = vi
      .fn()
      .mockRejectedValueOnce(new Error("boom"))
      .mockResolvedValueOnce("阿公");

    const { result } = renderHook(() => useLoadable(fetcher));
    await waitFor(() => expect(result.current.error).toBe(true));

    act(() => result.current.reload());

    await waitFor(() => expect(result.current.data).toBe("阿公"));
    expect(result.current.error).toBeNull();
  });

  it("fetcher 回 null 時不載入（條件未滿足）", async () => {
    // 呼叫端的 elderId 還沒從路由解析出來時，回 null 表示「這輪不載入」。
    const fetcher = vi.fn().mockReturnValue(null);

    const { result } = renderHook(() => useLoadable(fetcher));

    await waitFor(() => expect(fetcher).toHaveBeenCalled());
    expect(result.current.data).toBeNull();
    expect(result.current.error).toBeNull();
  });

  it("mapError 可把例外譯成自訂錯誤值", async () => {
    // TraceDetailPage 要區分 404「查無此筆」與一般失敗，它的 error 是字串——
    // 錯誤型別必須可由呼叫端決定，不能寫死成 boolean。
    const fetcher = vi.fn().mockRejectedValue({ status: 404 });
    const mapError = (exc: unknown) =>
      (exc as { status?: number })?.status === 404 ? "查無此筆" : "載入失敗";

    const { result } = renderHook(() => useLoadable(fetcher, mapError));

    await waitFor(() => expect(result.current.error).toBe("查無此筆"));
  });

  it("fetcher 換新參考時重新載入", async () => {
    const first = vi.fn().mockResolvedValue("第一次");
    const second = vi.fn().mockResolvedValue("第二次");

    const { result, rerender } = renderHook(({ f }) => useLoadable(f), {
      initialProps: { f: first },
    });
    await waitFor(() => expect(result.current.data).toBe("第一次"));

    rerender({ f: second });

    await waitFor(() => expect(result.current.data).toBe("第二次"));
  });

  it("mapError 換新參考不會觸發重新載入", async () => {
    // mapError 由 hook 內部以 ref 保存，故呼叫端不必 useCallback 包它。
    // 理由與 usePolling 相同：它是每次 render 都可能是新參考的回呼，若列入
    // 相依會讓載入無限重跑。
    const fetcher = vi.fn().mockResolvedValue("阿公");

    const { rerender } = renderHook(({ m }) => useLoadable(fetcher, m), {
      initialProps: { m: (_e: unknown) => true },
    });
    await waitFor(() => expect(fetcher).toHaveBeenCalledTimes(1));

    rerender({ m: (_e: unknown) => true }); // 新參考、同語意

    expect(fetcher).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `cd frontend && npx vitest run src/admin/useLoadable.test.tsx`
Expected: FAIL — 找不到模組 `./useLoadable`

- [ ] **Step 3: 實作 hook**

建立 `frontend/src/admin/useLoadable.ts`：

```ts
/**
 * 把 admin 七頁重複的「載入 → 成功顯示資料／失敗顯示錯誤」模式收成一處。
 *
 * 這七頁原本各自手寫 useState 資料 ＋ useState 錯誤 ＋ useCallback 載入 ＋
 * useEffect(load, [load])，改一次載入行為要改七處（PR #56 就一次修了七遍）。
 */

import { useCallback, useEffect, useRef, useState } from "react";

export function useLoadable<T, E = true>(
  /**
   * 取資料。**必須是穩定參考**（以 useCallback 包過），否則每次 render 都會重載。
   * 回傳 null ＝ 條件未滿足（如 elderId 尚未從路由解析出來），這輪不載入。
   */
  fetcher: () => Promise<T> | null,
  /** 把例外譯成錯誤值。預設回 true。**不需要**穩定參考（見下方 mapErrorRef）。 */
  mapError?: (exc: unknown) => E,
): { data: T | null; error: E | null; reload: () => void } {
  const [data, setData] = useState<T | null>(null);
  const [error, setError] = useState<E | null>(null);

  // mapError 以 ref 保存，故呼叫端不必 useCallback 包它：它是每次 render 都可能
  // 是新參考的回呼，若列入下方 reload 的相依，載入會無限重跑。ref 的更新落在
  // render 之後（render 期間改 ref 會讓並發渲染讀到不一致的值）。
  // 同款模式見 usePolling.ts。
  const mapErrorRef = useRef(mapError);
  useEffect(() => {
    mapErrorRef.current = mapError;
  });

  const reload = useCallback(() => {
    const pending = fetcher();
    if (pending === null) return;
    // setState 一律在非同步回呼中：寫在函式開頭會讓下方 useEffect 觸發連鎖重繪
    // （react-hooks/set-state-in-effect，PR #56 為此修了七頁）。
    // 錯誤也刻意留到成功才清——那是 PR #56 核定的語意：不會閃一下「載入中」
    // 再跳回錯誤。
    pending.then(
      (value) => {
        setData(value);
        setError(null);
      },
      (exc: unknown) => {
        const map = mapErrorRef.current ?? (() => true as unknown as E);
        setError(map(exc));
      },
    );
  }, [fetcher]);

  useEffect(reload, [reload]);

  return { data, error, reload };
}
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `cd frontend && npx vitest run src/admin/useLoadable.test.tsx`
Expected: PASS（7 passed）

- [ ] **Step 5: 確認 lint 與型別**

Run: `cd frontend && npm run lint && npm run typecheck`
Expected: 皆 PASS

⚠️ 若 `react-hooks/set-state-in-effect` 對 `useEffect(reload, [reload])` 報錯，**停下來回報**——那代表 hook 的寫法仍有同步 setState，不是加 disable 就好的事。

- [ ] **Step 6: Commit**

```bash
cd /home/leo29/kinsun
git add frontend/src/admin/useLoadable.ts frontend/src/admin/useLoadable.test.tsx
git commit -F - <<'EOF'
feat(frontend): 新增 useLoadable，把七頁重複的載入模式收成一處

admin 七頁各自手寫 useState 資料 ＋ useState 錯誤 ＋ useCallback 載入 ＋
useEffect(load, [load])。改一次載入行為要改七處——PR #56 就一次修了七遍。
而現在要為它寫測試，七頁會變成七份幾乎一樣的測試，重複從程式碼搬到測試裡。

錯誤型別可由呼叫端決定（E = true 為預設），不寫死成 boolean：六頁用布林就
夠，但 TraceDetailPage 要區分 404「查無此筆」與一般失敗、它的 error 是字串。
把七頁裡的六頁當成全部，是抽象時的典型失誤。

mapError 由 hook 內部以 ref 保存，呼叫端不必 useCallback 包它：它是每次
render 都可能是新參考的回呼，列入相依會讓載入無限重跑。同款模式見 usePolling。

setError 一律在非同步回呼中，且錯誤留到成功才清——前者是 PR #56 修掉的
set-state-in-effect（CI 現在會擋），後者是該 PR 核定的語意（不會閃一下
「載入中」再跳回錯誤）。抽成 hook 後這兩個決定只有一處會犯，也只有一處
守得住，故測試明文釘住。

IMPACT：
- 純新增，尚無呼叫端，對外行為零變更。七頁的遷移在下一個 commit。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: `usePolling` 的測試

**Files:**
- Create: `frontend/src/admin/usePolling.test.tsx`

**Interfaces:**
- Consumes: Task 1 的 vitest；既有的 `usePolling(callback, intervalMs)`
- Produces: 無

- [ ] **Step 1: 寫測試**

建立 `frontend/src/admin/usePolling.test.tsx`：

```tsx
/**
 * usePolling：訊息流每 5 秒輪詢的核心。PR #56 動過它的 ref 更新時機，
 * 而它當時沒有任何測試守著——本檔補上。
 */

import { act, renderHook } from "@testing-library/react";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

import { usePolling } from "./usePolling";

function setHidden(hidden: boolean) {
  Object.defineProperty(document, "hidden", { value: hidden, configurable: true });
  document.dispatchEvent(new Event("visibilitychange"));
}

describe("usePolling", () => {
  beforeEach(() => {
    vi.useFakeTimers();
    setHidden(false);
  });

  afterEach(() => {
    vi.useRealTimers();
  });

  it("掛載時立刻跑一次", () => {
    const callback = vi.fn();

    renderHook(() => usePolling(callback, 5000));

    expect(callback).toHaveBeenCalledTimes(1);
  });

  it("依間隔重複執行", () => {
    const callback = vi.fn();
    renderHook(() => usePolling(callback, 5000));

    act(() => void vi.advanceTimersByTime(15_000));

    expect(callback).toHaveBeenCalledTimes(4); // 掛載 1 次 ＋ 三次 tick
  });

  it("分頁隱藏時暫停、回到前景立刻補跑一次", () => {
    const callback = vi.fn();
    renderHook(() => usePolling(callback, 5000));
    callback.mockClear();

    act(() => setHidden(true));
    act(() => void vi.advanceTimersByTime(15_000));
    expect(callback).not.toHaveBeenCalled();

    act(() => setHidden(false));
    expect(callback).toHaveBeenCalledTimes(1);
  });

  it("卸載後不再執行", () => {
    const callback = vi.fn();
    const { unmount } = renderHook(() => usePolling(callback, 5000));
    callback.mockClear();

    unmount();
    act(() => void vi.advanceTimersByTime(15_000));

    expect(callback).not.toHaveBeenCalled();
  });

  it("callback 換新後，下一次 tick 用新的", () => {
    // ⚠️ 這條釘住 PR #56 改的 ref 更新時機：saved.current 從 render 期間指派
    // 改成落在 useEffect 裡。若那個 effect 被刪掉，輪詢會永遠呼叫第一次的
    // callback（拿著過期的閉包），而畫面看起來完全正常——這種 bug 沒有測試
    // 抓不到。
    const first = vi.fn();
    const second = vi.fn();
    const { rerender } = renderHook(({ cb }) => usePolling(cb, 5000), {
      initialProps: { cb: first },
    });

    rerender({ cb: second });
    first.mockClear();
    second.mockClear();

    act(() => void vi.advanceTimersByTime(5000));

    expect(second).toHaveBeenCalledTimes(1);
    expect(first).not.toHaveBeenCalled();
  });

  it("callback 換新不會重啟計時器", () => {
    // 計時器只相依 intervalMs——這正是本 hook 用 ref 保存 callback 的初衷。
    const first = vi.fn();
    const second = vi.fn();
    const { rerender } = renderHook(({ cb }) => usePolling(cb, 5000), {
      initialProps: { cb: first },
    });

    act(() => void vi.advanceTimersByTime(3000)); // 距下次 tick 剩 2 秒
    rerender({ cb: second });
    second.mockClear();

    act(() => void vi.advanceTimersByTime(2000)); // 若重啟，這裡不會 tick

    expect(second).toHaveBeenCalledTimes(1);
  });
});
```

- [ ] **Step 2: 執行測試**

Run: `cd frontend && npx vitest run src/admin/usePolling.test.tsx`
Expected: PASS（6 passed）

⚠️ 這是對既有正確程式碼寫的測試，該綠。若紅了，**停下來回報**——那代表 `usePolling` 真的有 bug（或測試對它的行為理解有誤），不要改 `usePolling` 去遷就測試。

- [ ] **Step 3: 確認測試真的會抓**

暫時把 `usePolling.ts` 中更新 ref 的那個 effect 刪掉（回到 PR #56 之前的寫法），確認「callback 換新後下一次 tick 用新的」那條會紅：

```bash
cd /home/leo29/kinsun/frontend
cp src/admin/usePolling.ts /tmp/usePolling.bak
python3 - <<'PY'
import pathlib
p = pathlib.Path("src/admin/usePolling.ts")
s = p.read_text()
i, j = s.index("  const saved = useRef(callback);"), s.index("  useEffect(() => {\n    let timer")
s = s[:i] + "  const saved = useRef(callback);\n  saved.current = callback;\n\n" + s[j:]
p.write_text(s)
PY
npx vitest run src/admin/usePolling.test.tsx    # 「callback 換新」那條仍會綠（舊寫法功能上也對）
cp /tmp/usePolling.bak src/admin/usePolling.ts
```

⚠️ **預期這裡兩種寫法都綠**——舊寫法在功能上也會更新 ref，它壞的是並發渲染下的正確性，fake timer 測不出來。這一步的用途是**誠實記錄這個界線**：本測試釘的是「callback 換新後會用新的」這個行為，不是「ref 在正確的時機更新」這個實作。若兩者都綠，照樣進行，並在 commit 訊息中載明此限制。

還原後確認 `git status --short` 不含 `usePolling.ts`。

- [ ] **Step 4: Commit**

```bash
cd /home/leo29/kinsun
git add frontend/src/admin/usePolling.test.tsx
git commit -F - <<'EOF'
test(frontend): 補上 usePolling 的測試——PR #56 動過它卻沒東西守著

usePolling 是訊息流每 5 秒輪詢的核心。PR #56 把 saved.current 的指派從 render
期間搬進 useEffect，當時它沒有任何測試——改對改錯都沒人知道。

六條測試釘住的是使用者看得到的行為：掛載立刻跑一次、依間隔重複、分頁隱藏
時暫停、回到前景補跑、卸載後停止、callback 換新後下一次 tick 用新的、
callback 換新不重啟計時器。

⚠️ 載明一條界線：本檔釘的是「callback 換新後會用新的」這個行為，不是「ref
在正確的時機更新」這個實作。實測 PR #56 之前的寫法（render 期間指派）也能
讓這些測試全綠——它壞的是並發渲染下的正確性，fake timer 測不出來。與其假裝
測到了，不如把界線寫下來。

IMPACT：
- 純新增測試，無執行期程式碼變更、無行為變更。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: `app/` 的 jest-expo ＋ `location.ts` 的測試

**Files:**
- Modify: `app/package.json`（devDeps ＋ test script ＋ jest 區塊）、`app/package-lock.json`
- Create: `app/src/lib/location.test.ts`

**Interfaces:**
- Consumes: 既有的 `currentPlace(): Promise<string>`（`app/src/lib/location.ts`）
- Produces: `cd app && npm test` 這個指令

⚠️ **先讀 v54 文件**（`app/AGENTS.md` 明令）：https://docs.expo.dev/versions/v54.0.0/ 與 https://docs.expo.dev/develop/unit-testing/ 。以實際安裝到的版本為準，不要照抄本計畫的設定。

- [ ] **Step 1: 安裝依賴**

```bash
cd app && npx expo install jest-expo jest @types/jest @testing-library/react-native -- --save-dev
```

⚠️ 裝完**立刻**確認 `dependencies` 未被動到——`expo install` 有把套件塞進 `dependencies` 的前科（PR #56 的 `eslint-config-expo`）：

```bash
cd /home/leo29/kinsun && git diff -- app/package.json
```

Expected: 只有 `devDependencies` 變動。若有套件跑進 `dependencies`，手動搬回 `devDependencies` 後重跑 `npm install --package-lock-only`。

- [ ] **Step 2: 加入 test script 與 jest 設定**

在 `app/package.json` 的 `scripts` 中加入：

```json
    "test": "jest"
```

⚠️ 用 `jest` 而非 Expo 文件範例的 `jest --watchAll`：後者是 watch 模式，CI 會卡住不結束。

在 `app/package.json` 頂層加入 `jest` 區塊：

```json
  "jest": {
    "preset": "jest-expo",
    "moduleNameMapper": {
      "^@/(.*)$": "<rootDir>/src/$1"
    }
  }
```

`moduleNameMapper` 對應 `tsconfig.json` 既有的 `paths`（`@/*` → `./src/*`）。`kinsun-shared` 不需要對應——它是 `file:../shared` 的真實依賴，node 解析得到。

- [ ] **Step 3: 寫測試**

建立 `app/src/lib/location.test.ts`：

```ts
/**
 * currentPlace 的四條失敗路徑全是「靜默降級」——錯了不會拋例外、不會有人發現，
 * 只會讓金孫默默失去「問天氣不反問所在地」的能力。這種 bug 沒有測試永遠抓不到。
 */

import * as Location from "expo-location";

import { currentPlace } from "./location";

jest.mock("expo-location", () => ({
  PermissionStatus: { GRANTED: "granted", DENIED: "denied" },
  requestForegroundPermissionsAsync: jest.fn(),
  getLastKnownPositionAsync: jest.fn(),
  reverseGeocodeAsync: jest.fn(),
}));

const mocked = Location as jest.Mocked<typeof Location>;

const POSITION = { coords: { latitude: 22.99, longitude: 120.21 } };

function grantPermission() {
  mocked.requestForegroundPermissionsAsync.mockResolvedValue({
    status: Location.PermissionStatus.GRANTED,
  } as never);
}

describe("currentPlace", () => {
  beforeEach(() => {
    jest.resetAllMocks();
  });

  it("正常時回傳城市名", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(POSITION as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([{ city: "台南市" }] as never);

    expect(await currentPlace()).toBe("台南市");
  });

  it("長輩拒絕定位時回空字串（靜默降級，不可阻擋對講機）", async () => {
    mocked.requestForegroundPermissionsAsync.mockResolvedValue({
      status: Location.PermissionStatus.DENIED,
    } as never);

    expect(await currentPlace()).toBe("");
    expect(mocked.getLastKnownPositionAsync).not.toHaveBeenCalled();
  });

  it("沒有快取位置時回空字串（室內收不到訊號）", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(null);

    expect(await currentPlace()).toBe("");
  });

  it("譯不出地名時回空字串", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(POSITION as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([]);

    expect(await currentPlace()).toBe("");
  });

  it("拋例外時回空字串，不往外拋", async () => {
    mocked.requestForegroundPermissionsAsync.mockRejectedValue(new Error("boom"));

    await expect(currentPlace()).resolves.toBe("");
  });

  it("city 缺漏時退 subregion", async () => {
    // 各家作業系統的 city 粒度不一，退階順序是緩解（spec 已知限制 4）。
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(POSITION as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([
      { city: null, subregion: "東區", region: "台南市" },
    ] as never);

    expect(await currentPlace()).toBe("東區");
  });

  it("city 與 subregion 皆缺時退 region", async () => {
    grantPermission();
    mocked.getLastKnownPositionAsync.mockResolvedValue(POSITION as never);
    mocked.reverseGeocodeAsync.mockResolvedValue([
      { city: null, subregion: null, region: "台南市" },
    ] as never);

    expect(await currentPlace()).toBe("台南市");
  });
});
```

- [ ] **Step 4: 執行測試**

Run: `cd app && npm test`
Expected: PASS（7 passed）

⚠️ 若 jest 抱怨 transform 或無法解析 `expo-location`，依 Expo 文件在 `jest` 區塊補 `transformIgnorePatterns`。**以實際的錯誤訊息為準**，不要預先加上不需要的設定。

- [ ] **Step 5: 確認測試真的會抓**

```bash
cd /home/leo29/kinsun/app
cp src/lib/location.ts /tmp/location.bak
sed -i 's/return "";/return "台北";/' src/lib/location.ts
npm test    # 應該紅：多條期待空字串的測試會失敗
cp /tmp/location.bak src/lib/location.ts
npm test    # 應該綠
```

Expected: 第一次 FAIL、還原後 PASS。⚠️ 確認 `git status --short` 不含 `location.ts`。

這正是 spec 說的那個情境：「有人把 `return ""` 改成 `return "台北"`，跑遍全專案的測試都不會紅」——本 Task 之後它會紅。

- [ ] **Step 6: 確認未破壞既有檢查**

Run: `cd app && npm run lint && npm run typecheck`
Expected: 皆 PASS

⚠️ 新測試檔會被 eslint 掃到。若 `jest`／`describe`／`it` 被判未定義，在 `app/eslint.config.js` 針對 `**/*.test.{ts,tsx}` 補 jest globals。

- [ ] **Step 7: Commit**

```bash
cd /home/leo29/kinsun
git add app/package.json app/package-lock.json app/src/lib/location.test.ts app/eslint.config.js
git commit -F - <<'EOF'
test(app): 立起 jest-expo，並蓋住 location.ts 的四條靜默降級路徑

app/ 從來沒有測試框架。PR #55 加的 location.ts 處理定位權限、座標反查地名、
四條失敗降級路徑——一條自動化驗證都沒有。

用 jest-expo 而非 vitest：Expo v54 文件推薦它並明列 @testing-library/react-native，
vitest 隻字未提。React Native 的 transform 與原生模組 mock 由 jest-expo preset
處理，硬用 vitest 是跟生態打架。

四條失敗路徑是本 commit 的重點：未授權、無快取位置、譯不出地名、拋例外——
全都靜默回空字串。它們錯了不會拋例外、不會有人發現，只會讓金孫默默失去
「問天氣不反問所在地」的能力。

已驗證閘門真的會抓：把 return "" 改成 return "台北" → 測試紅 → 還原 → 綠。
spec 說的正是這個情境，本 commit 之後它會紅。

script 用 jest 而非 Expo 文件範例的 jest --watchAll：後者是 watch 模式，
CI 會卡住不結束。

IMPACT：
- 純新增測試與工具設定，無執行期程式碼變更、無行為變更。
- CI 尚未跑 npm test（排在下一個 commit）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: 七頁遷移到 `useLoadable`

**Files:**
- Modify: `frontend/src/admin/pages/SystemPage.tsx`
- Modify: `frontend/src/admin/pages/ElderTimelinePage.tsx`
- Modify: `frontend/src/admin/pages/TraceDetailPage.tsx`
- Modify: `frontend/src/admin/pages/elder-tabs/AccountTab.tsx`
- Modify: `frontend/src/admin/pages/elder-tabs/MemoryTab.tsx`
- Modify: `frontend/src/admin/pages/elder-tabs/RemindersTab.tsx`
- Modify: `frontend/src/admin/pages/elder-tabs/RiskNotificationsTab.tsx`

**Interfaces:**
- Consumes: Task 2 的 `useLoadable`；Task 1 的 `MemoryTab.test.tsx`
- Produces: 無

⚠️ **本 Task 是純重構，不得改變任何畫面行為。** Task 1 那份 `MemoryTab.test.tsx` 在本 Task 之後**仍須綠**——那就是重構無副作用的證據。任何「順便改善」都會讓這個證據失效。

⚠️ **先讀完每一頁再改**：七頁形狀相近但不相同（`SystemPage` 沒有 `if (!id) return` 前綴、`TraceDetailPage` 的 error 是字串、`RemindersTab` 另有 `notice` state、多頁的 `load`／`reload` 會被事件處理器呼叫）。本計畫只給 `MemoryTab` 與 `TraceDetailPage` 的完整改法作為兩種形狀的範本；其餘五頁依其既有形狀套用，**不要照抄**。

- [ ] **Step 1: 確認起點是綠的**

Run: `cd frontend && npm test`
Expected: PASS（Task 1–3 的測試全綠）

- [ ] **Step 2: 遷移 `MemoryTab.tsx`（布林錯誤的範本）**

將：

```tsx
import { useCallback, useEffect, useState } from "react";
import { useParams } from "react-router-dom";

import { type AdminElderMemory, getElderMemory } from "../../api";
import { strings } from "../../strings";

/** 記憶與摘要分頁：AI 幫長輩記住的事（Mem0）＋每日對話摘要。 */
export function MemoryTab() {
  const { elderId } = useParams<{ elderId: string }>();
  const [data, setData] = useState<AdminElderMemory | null>(null);
  const [error, setError] = useState(false);

  const load = useCallback(() => {
    if (!elderId) return;
    // setError(false) 放進成功處理器：開頭的同步 setState 會在 useEffect 中
    // 觸發連鎖重繪。代價是錯誤橫幅留到成功才消失。
    getElderMemory(elderId).then(
      (data) => {
        setData(data);
        setError(false);
      },
      () => setError(true),
    );
  }, [elderId]);

  useEffect(load, [load]);

  if (error) return <p className="error-banner">{strings.common.loadFailedRefresh}</p>;
```

替換為：

```tsx
import { useCallback } from "react";
import { useParams } from "react-router-dom";

import { getElderMemory } from "../../api";
import { strings } from "../../strings";
import { useLoadable } from "../../useLoadable";

/** 記憶與摘要分頁：AI 幫長輩記住的事（Mem0）＋每日對話摘要。 */
export function MemoryTab() {
  const { elderId } = useParams<{ elderId: string }>();
  const { data, error } = useLoadable(
    useCallback(() => (elderId ? getElderMemory(elderId) : null), [elderId]),
  );

  if (error) return <p className="error-banner">{strings.common.loadFailedRefresh}</p>;
```

⚠️ `AdminElderMemory` 的型別匯入若已無其他用途則一併移除（`useLoadable` 會從 `getElderMemory` 的回傳型別推斷），否則 lint 會報未使用。

- [ ] **Step 3: 確認 MemoryTab 的釘樁測試仍綠**

Run: `cd frontend && npx vitest run src/admin/pages/elder-tabs/MemoryTab.test.tsx`
Expected: PASS（3 passed）

**這是本 Task 的核心斷言。** 若紅了，代表遷移改變了行為——**停下來回報**，不要改測試去遷就新的實作。

- [ ] **Step 4: 遷移 `TraceDetailPage.tsx`（字串錯誤的範本）**

將：

```tsx
  const [trace, setTrace] = useState<TraceDetail | null>(null);
  const [error, setError] = useState<string | null>(null);

  const load = useCallback(() => {
    if (!traceId) return;
    // setError(null) 放進成功處理器：開頭的同步 setState 會在 useEffect 中
    // 觸發連鎖重繪。代價是錯誤橫幅留到成功才消失。
    getTrace(traceId).then(
      (trace) => {
        setTrace(trace);
        setError(null);
      },
      (e) =>
        setError(e?.status === 404 ? strings.trace.notFound : strings.common.loadFailedRefresh),
    );
  }, [traceId]);

  useEffect(load, [load]);
```

替換為：

```tsx
  const { data: trace, error } = useLoadable(
    useCallback(() => (traceId ? getTrace(traceId) : null), [traceId]),
    // 錯誤型別由呼叫端決定：本頁要區分 404「查無此筆」與一般失敗，故為字串
    // 而非其餘六頁的布林。
    (e: unknown) =>
      (e as { status?: number })?.status === 404
        ? strings.trace.notFound
        : strings.common.loadFailedRefresh,
  );
```

並在檔案頂端加入 `import { useLoadable } from "../useLoadable";`，移除不再使用的 `useEffect`／`useState`／`TraceDetail` 型別匯入。

- [ ] **Step 5: 遷移其餘五頁**

依 Step 2（布林）或 Step 4（字串）的範本，套用到：`SystemPage.tsx`、`ElderTimelinePage.tsx`、`AccountTab.tsx`、`RemindersTab.tsx`、`RiskNotificationsTab.tsx`。

⚠️ 逐頁先讀完再改。已知差異：

- `SystemPage` 的 `load` 沒有 `if (!id) return` 前綴（它不依賴路由參數）→ fetcher 直接回 `listJobs()`，永不回 null
- `SystemPage`、`RemindersTab` 的 `load`／`reload` 會被事件處理器（執行 job、標記已讀之類）呼叫 → 改用 `useLoadable` 回傳的 `reload`
- `RemindersTab` 另有 `notice` state，與載入無關 → **保持原樣**，不要一併收進 hook

- [ ] **Step 6: 確認全綠**

Run: `cd frontend && npm test && npm run lint && npm run typecheck && npm run build && npm run build:admin`
Expected: 全部 PASS

- [ ] **Step 7: Commit**

```bash
cd /home/leo29/kinsun
git add frontend/src/admin/pages/
git commit -F - <<'EOF'
refactor(frontend): admin 七頁改用 useLoadable，載入邏輯只剩一份

七頁原本各自手寫同一套 useState 資料 ＋ useState 錯誤 ＋ useCallback 載入 ＋
useEffect(load, [load])。PR #56 為了修 set-state-in-effect 一次改了七遍——
下一次要動載入行為還是七遍，而為它寫測試會變成七份幾乎一樣的測試。

純重構，不改變任何畫面行為。證據不是「我看過了」：MemoryTab.test.tsx 是在
重構之前寫的（釘住當時的行為、只斷言使用者看得到的字），本 commit 之後它
仍然綠。那才是證據。

TraceDetailPage 用字串錯誤、其餘六頁用布林——useLoadable 的錯誤型別可由
呼叫端決定，正是為此。RemindersTab 的 notice state 與載入無關，保持原樣
沒有收進 hook：hook 只該收「載入」這一件事。

IMPACT：
- 觀測後台七個畫面，行為完全不變（MemoryTab.test.tsx 佐證）。
- 未來要改載入行為只需改 useLoadable 一處，測試也只需改一處。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 6: CI 加上 test 步驟

**Files:**
- Modify: `.github/workflows/ci.yml`

**Interfaces:**
- Consumes: Task 1–5 的兩個 `npm test` 指令
- Produces: 無

**這個 Task 是整個計畫唯一的交付價值所在。** 前五個 Task 若沒有它，測試只是本機跑得動的擺設——沒有 CI 擋，壞掉的測試照樣進 main。

- [ ] **Step 1: 加入 frontend 的 test 步驟**

在 `.github/workflows/ci.yml` 的 frontend job 中，將：

```yaml
      - name: Lint
        run: npm run lint
        working-directory: frontend

      - name: 建置
        run: npm run build
        working-directory: frontend
```

替換為：

```yaml
      - name: Lint
        run: npm run lint
        working-directory: frontend

      - name: 測試
        run: npm test
        working-directory: frontend

      - name: 建置
        run: npm run build
        working-directory: frontend
```

- [ ] **Step 2: 加入 app 的 test 步驟**

在 `.github/workflows/ci.yml` 的 app job 中，將：

```yaml
      - name: Lint
        run: npm run lint
        working-directory: app
```

替換為：

```yaml
      - name: Lint
        run: npm run lint
        working-directory: app

      - name: 測試
        run: npm test
        working-directory: app
```

兩處皆擺在 Lint 之後：型別 → lint → 測試，更根本的錯誤先報。

- [ ] **Step 3: 全量驗證**

Run:

```bash
cd /home/leo29/kinsun && uv run pytest -q 2>&1 | tail -2 && uv run ruff check src/ tests/ \
  && cd frontend && npm run lint && npm run typecheck && npm test && npm run build && npm run build:admin \
  && cd ../app && npm run lint && npm run typecheck && npm test \
  && echo "全部通過"
```

Expected: 後端 1221 passed、ruff All checks passed、印出「全部通過」

- [ ] **Step 4: 更新 spec 狀態**

在 `docs/superpowers/specs/2026-07-17-js端測試基建-design.md` 中，將：

```markdown
- 狀態：**設計中**（尚未實作）
```

改為（比照 `2026-07-16-每位長輩的問候時間` 與同日兩份 spec 的慣例，並載明實作中對本文的更正）：

```markdown
- 狀態：**已實作**（Task 1–6）。實作結果與本文的差異（若有）記於本行；無差異則僅標已實作。
```

⚠️ 若實作過程中對 spec 有任何更正（如 `useLoadable` 介面與第 3 節不符、測試檔數量與第 4 節不符），**必須在此行具體寫出更正了什麼**，比照 `2026-07-17-長輩目前地點-design.md` 的寫法。無更正才可用上述簡短版。

- [ ] **Step 5: Commit**

```bash
cd /home/leo29/kinsun
git add .github/workflows/ci.yml docs/superpowers/specs/2026-07-17-js端測試基建-design.md
git commit -F - <<'EOF'
ci: JS 端測試納入 CI，讓前五個 commit 不是擺設

前五個 commit 立起兩套測試框架、蓋住今天真正燒到的兩處、把七頁的載入邏輯
收成一份。但 CI 沒跑 npm test——沒有這一步，測試只是本機跑得動的擺設，壞掉
的測試照樣進 main。

兩處皆擺在 Lint 之後：型別 → lint → 測試，更根本的錯誤先報。

一併把 spec 狀態改為已實作。

IMPACT：
- 之後任何 PR 若 JS 端測試紅，CI 會紅。這是本系列的目的。
- frontend/ 與 app/ 從此各有一道自動化護欄；後端不受影響（仍為 1221 passed）。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

## Self-Review 紀錄

- **Spec 覆蓋**：元件設計 1→Task 1、2→Task 4、3→Task 2＋Task 5、4→Task 1／2／3／4、5→Task 6；「順序：先釘住現有行為，再重構」→ Task 1 Step 5 ＋ Task 5 Step 3；測試策略表的驗證項→各 Task 的「確認測試真的會抓」步驟；與既有規則的抵觸→Global Constraints。無遺漏。
- **刻意的順序**：Task 1 的釘樁測試必須早於 Task 5 的重構，否則「重構無副作用」沒有證據。Task 6（CI）排最後，故 Task 1–5 的中間狀態不擋任何人。
- **未寫死的地方**（皆附「以實際錯誤為準」的指示，硬給程式碼會產生與現實不符的指令，這是 PR #55／#56 學到的教訓）：Task 1 Step 4 的 `defineConfig` 匯入來源、Task 1 Step 8／Task 4 Step 6 的 eslint globals、Task 4 Step 2 的 jest 設定與 `transformIgnorePatterns`、Task 5 Step 5 其餘五頁的遷移。
- **誠實記錄的界線**：Task 3 Step 3 預期新舊寫法都綠——本測試釘的是行為不是實作，這一步的用途是把界線寫下來而非假裝測到了。
- **型別一致性**：`useLoadable<T, E = true>(fetcher, mapError?) => { data, error, reload }` 在 Task 2（定義）、Task 5（使用）一致；`currentPlace(): Promise<string>` 與既有實作一致。
- **範圍外**（spec 已知限制）：其餘六頁不寫元件測試、`app/` 不測畫面、`shared/` 無測試、無 E2E。
