# 家屬端 LIFF 入口 B1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 打通「LIFF 前端 → 帶 ID token 的 REST API → 驗證 → DB → 顯示家屬管理的長輩清單」端到端薄切片，作為完整 LIFF 儀表板的地基。

**Architecture:** 後端新增 `src/kinsun/web/` 套件（LIFF ID token 驗證 + `GET /api/me/elders`），組裝層接線、webhook 不動；前端 `frontend/`（React+Vite+TS）由 FastAPI 同源供應 `/liff`，免 CORS。

**Tech Stack:** Python 3.12 / FastAPI / uv / pytest / ruff（後端）；React 18 + Vite + TypeScript（前端，node 工具鏈）。

## Global Constraints

- 一律台灣繁體中文：文件、程式碼註解、commit 訊息。
- Python 3.12；ruff `E,W,F,I,B,UP`，line-length 100。
- 後端**不新增第三方套件**（驗證走標準庫 `urllib`，沿用 `DgxAsrClient` 模式）。
- 所有外部相依走 Protocol + 注入 fake；驗證器、accounts 皆注入以利測試。
- 全程 fail-safe：驗證失敗一律 401、不洩漏細節；web 層異常不影響 webhook 對話主流程。
- 同源供應，**不需 CORS**。前端用絕對路徑 `/api/...`。
- LINE Login channel 與 Messaging API **同 provider**（`userId` 才等於綁定時存的 `line_user_id`）—— 部署設定前提，非程式邏輯。
- `frontend/dist` 不存在則後端不掛 `/liff`、照常啟動（測試/純 webhook 開發不需 node build）。
- commit 型別 `feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 單元測試全離線（注入 fake / monkeypatch，不需金鑰/網路）。

---

## 檔案結構（File Structure）

**新增（後端）：**
- `src/kinsun/web/__init__.py` — 空套件標記。
- `src/kinsun/web/auth.py` — `AuthError`、`LiffVerifier`(Protocol)、`LineIdTokenVerifier`。
- `src/kinsun/web/api.py` — `create_api_router`。
- `tests/test_web_auth.py`、`tests/test_api_elders.py`。

**新增（前端 `frontend/`）：** `package.json`、`package-lock.json`、`vite.config.ts`、`tsconfig.json`、`index.html`、`.env.example`、`src/vite-env.d.ts`、`src/main.tsx`、`src/App.tsx`。

**修改：**
- `src/kinsun/config.py` — `liff_channel_id`、`liff_timeout_seconds`。
- `src/kinsun/app.py` — 掛 API router + 條件式掛 `/liff` 靜態。
- `tests/test_config.py` — 斷言新預設。
- `.env.example` — `LIFF_CHANNEL_ID`、`LIFF_TIMEOUT_SECONDS`。
- `.gitignore` — `frontend/node_modules/`、`frontend/dist/`。
- `.github/workflows/ci.yml` — 新增 frontend job。
- `README.md` — 家屬端 LIFF（開發/部署）一節。

---

## Task 1：LIFF 身分驗證（web/auth.py）

**Files:**
- Create: `src/kinsun/web/__init__.py`、`src/kinsun/web/auth.py`
- Test: `tests/test_web_auth.py`

**Interfaces:**
- Produces:
  - `AuthError(Exception)`
  - `LiffVerifier` Protocol：`verify(id_token: str) -> str`
  - `LineIdTokenVerifier(channel_id: str, timeout: float)`，`verify` 回 LINE `sub`（userId）

- [ ] **Step 1：寫失敗測試** `tests/test_web_auth.py`

```python
import json
import urllib.error

import pytest

from kinsun.web.auth import AuthError, LineIdTokenVerifier


class _FakeResp:
    def __init__(self, payload):
        self._data = json.dumps(payload).encode()

    def read(self):
        return self._data

    def __enter__(self):
        return self

    def __exit__(self, *exc):
        return False


def test_verify_returns_sub(monkeypatch):
    monkeypatch.setattr(
        "urllib.request.urlopen", lambda req, timeout=None: _FakeResp({"sub": "U-123"})
    )
    assert LineIdTokenVerifier("ch", 10).verify("tok") == "U-123"


def test_verify_missing_sub_raises(monkeypatch):
    monkeypatch.setattr(
        "urllib.request.urlopen", lambda req, timeout=None: _FakeResp({"aud": "ch"})
    )
    with pytest.raises(AuthError):
        LineIdTokenVerifier("ch", 10).verify("tok")


def test_verify_http_error_raises(monkeypatch):
    def boom(req, timeout=None):
        raise urllib.error.URLError("bad")

    monkeypatch.setattr("urllib.request.urlopen", boom)
    with pytest.raises(AuthError):
        LineIdTokenVerifier("ch", 10).verify("tok")
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_web_auth.py -v`
Expected: FAIL（`ModuleNotFoundError: kinsun.web`）

- [ ] **Step 3：實作**

`src/kinsun/web/__init__.py`：空檔。

`src/kinsun/web/auth.py`：
```python
"""LIFF 身分驗證：驗 ID token 取 LINE userId。"""

from __future__ import annotations

import json
import urllib.error
import urllib.parse
import urllib.request
from typing import Protocol

_VERIFY_URL = "https://api.line.me/oauth2/v2.1/verify"


class AuthError(Exception):
    """LIFF 身分驗證失敗。"""


class LiffVerifier(Protocol):
    def verify(self, id_token: str) -> str: ...


class LineIdTokenVerifier:
    """POST id_token 到 LINE verify 端點，回傳 LINE userId（sub）。"""

    def __init__(self, channel_id: str, timeout: float) -> None:
        self._channel_id = channel_id
        self._timeout = timeout

    def verify(self, id_token: str) -> str:
        data = urllib.parse.urlencode(
            {"id_token": id_token, "client_id": self._channel_id}
        ).encode()
        request = urllib.request.Request(
            _VERIFY_URL,
            data=data,
            headers={"Content-Type": "application/x-www-form-urlencoded"},
            method="POST",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                payload = json.loads(response.read().decode("utf-8"))
        except (urllib.error.URLError, OSError, json.JSONDecodeError) as exc:
            raise AuthError(f"LIFF 驗證呼叫失敗：{exc}") from exc
        sub = payload.get("sub")
        if not isinstance(sub, str):
            raise AuthError("LIFF 驗證回應缺少 sub")
        return sub
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_web_auth.py -v`
Expected: PASS（3 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/web/__init__.py src/kinsun/web/auth.py tests/test_web_auth.py
git commit -m "feat: LIFF ID token 驗證器（LineIdTokenVerifier）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：REST API（web/api.py）

**Files:**
- Create: `src/kinsun/web/api.py`
- Test: `tests/test_api_elders.py`

**Interfaces:**
- Consumes: `LiffVerifier`、`AuthError`（Task 1）、`AccountService.elders_managed_by`（既有）
- Produces: `create_api_router(*, verifier: LiffVerifier, accounts: AccountService) -> APIRouter`
  - `GET /api/me/elders` → `{"elders": [{"elder_id": str, "name": str}, ...]}`；缺/無效 token → 401

- [ ] **Step 1：寫失敗測試** `tests/test_api_elders.py`

```python
from datetime import datetime, timedelta, timezone
from itertools import count

from fastapi import FastAPI
from fastapi.testclient import TestClient

from kinsun.accounts.service import AccountService
from kinsun.web.api import create_api_router
from kinsun.web.auth import AuthError
from tests.fakes import FakeAccountRepository

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 10, tzinfo=TPE)


class _FakeVerifier:
    def __init__(self, user_id="U-son", boom=False):
        self._user_id = user_id
        self._boom = boom

    def verify(self, id_token):
        if self._boom:
            raise AuthError("bad")
        return self._user_id


def _accounts():
    repo = FakeAccountRepository()
    ids = (f"id{i}" for i in count(1))
    svc = AccountService(repo, clock=lambda: NOW, new_id=lambda: next(ids), new_code=lambda: "c")
    svc.create_elder("U-son", "兒子", "阿公")
    return svc


def _client(verifier, accounts):
    app = FastAPI()
    app.include_router(create_api_router(verifier=verifier, accounts=accounts))
    return TestClient(app)


def test_lists_elders_for_authenticated_guardian():
    client = _client(_FakeVerifier("U-son"), _accounts())
    res = client.get("/api/me/elders", headers={"Authorization": "Bearer tok"})
    assert res.status_code == 200
    assert [e["name"] for e in res.json()["elders"]] == ["阿公"]


def test_missing_token_returns_401():
    client = _client(_FakeVerifier(), _accounts())
    assert client.get("/api/me/elders").status_code == 401


def test_non_bearer_returns_401():
    client = _client(_FakeVerifier(), _accounts())
    assert client.get("/api/me/elders", headers={"Authorization": "Basic x"}).status_code == 401


def test_invalid_token_returns_401():
    client = _client(_FakeVerifier(boom=True), _accounts())
    assert client.get("/api/me/elders", headers={"Authorization": "Bearer tok"}).status_code == 401


def test_guardian_without_elders_returns_empty():
    client = _client(_FakeVerifier("U-stranger"), _accounts())
    res = client.get("/api/me/elders", headers={"Authorization": "Bearer tok"})
    assert res.status_code == 200
    assert res.json() == {"elders": []}
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_api_elders.py -v`
Expected: FAIL（`ImportError: create_api_router`）

- [ ] **Step 3：實作** `src/kinsun/web/api.py`

```python
"""家屬端 REST API（LIFF）。"""

from __future__ import annotations

from fastapi import APIRouter, Depends, Header, HTTPException

from kinsun.accounts.service import AccountService
from kinsun.web.auth import AuthError, LiffVerifier


def create_api_router(*, verifier: LiffVerifier, accounts: AccountService) -> APIRouter:
    router = APIRouter(prefix="/api")

    def current_guardian(authorization: str = Header(default="")) -> str:
        scheme, _, token = authorization.partition(" ")
        if scheme.lower() != "bearer" or not token:
            raise HTTPException(status_code=401, detail="missing bearer token")
        try:
            return verifier.verify(token)
        except AuthError as exc:
            raise HTTPException(status_code=401, detail="invalid token") from exc

    @router.get("/me/elders")
    def my_elders(line_user_id: str = Depends(current_guardian)) -> dict:
        elders = accounts.elders_managed_by(line_user_id)
        return {"elders": [{"elder_id": e.elder_id, "name": e.name} for e in elders]}

    return router
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_api_elders.py -v`
Expected: PASS（5 passed）

- [ ] **Step 5：commit**

```bash
git add src/kinsun/web/api.py tests/test_api_elders.py
git commit -m "feat: 家屬端 REST API GET /api/me/elders（LIFF 驗證守門）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：設定（config + .env.example）

**Files:**
- Modify: `src/kinsun/config.py`、`.env.example`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings.liff_channel_id: str`（預設 `""`）、`Settings.liff_timeout_seconds: float`（預設 `10`）

- [ ] **Step 1：改測試**（`tests/test_config.py` 的 `test_load_settings_reads_required_and_defaults` 內，`appointment_reminder_hour` 斷言之後加兩行）

```python
    assert settings.liff_channel_id == ""
    assert settings.liff_timeout_seconds == 10
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_config.py::test_load_settings_reads_required_and_defaults -v`
Expected: FAIL（`AttributeError: liff_channel_id`）

- [ ] **Step 3：實作**

`src/kinsun/config.py`：`Settings` 在 `appointment_reminder_hour: int` 後加：
```python
    liff_channel_id: str
    liff_timeout_seconds: float
```
`load_settings(...)` 在 `appointment_reminder_hour=...` 後加：
```python
        liff_channel_id=env.get("LIFF_CHANNEL_ID", ""),
        liff_timeout_seconds=float(env.get("LIFF_TIMEOUT_SECONDS", "10")),
```
`.env.example`：在 `APPOINTMENT_REMINDER_HOUR=8` 之後加：
```
# 家屬端 LIFF（LINE Login channel 與 Messaging API 同 provider）
LIFF_CHANNEL_ID=
LIFF_TIMEOUT_SECONDS=10
```

- [ ] **Step 4：跑測試確認通過**

Run: `uv run pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 5：commit**

```bash
git add src/kinsun/config.py tests/test_config.py .env.example
git commit -m "feat: 新增 LIFF_CHANNEL_ID 與 LIFF_TIMEOUT_SECONDS 設定

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：組裝接線（app.py）

**Files:**
- Modify: `src/kinsun/app.py`

**Interfaces:**
- Consumes: `create_api_router`（Task 2）、`LineIdTokenVerifier`（Task 1）、`settings.liff_*`（Task 3）

- [ ] **Step 1：實作接線**

`src/kinsun/app.py` import 區（依字母序）加入：
```python
from pathlib import Path

from fastapi.staticfiles import StaticFiles
```
並在既有 `from kinsun.tools...` 一帶加入：
```python
from kinsun.web.api import create_api_router
from kinsun.web.auth import LineIdTokenVerifier
```
把結尾的
```python
    return create_app(
        parser=parser,
        pipeline=pipeline,
        messenger=messenger,
        binding=binding,
        gate=gate,
        on_shutdown=db.close,
    )
```
改為：
```python
    app = create_app(
        parser=parser,
        pipeline=pipeline,
        messenger=messenger,
        binding=binding,
        gate=gate,
        on_shutdown=db.close,
    )
    verifier = LineIdTokenVerifier(settings.liff_channel_id, settings.liff_timeout_seconds)
    app.include_router(create_api_router(verifier=verifier, accounts=accounts))
    dist = Path(__file__).resolve().parents[2] / "frontend" / "dist"
    if dist.is_dir():
        app.mount("/liff", StaticFiles(directory=dist, html=True), name="liff")
    return app
```

- [ ] **Step 2：靜態驗證可匯入 + 全量測試**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"
uv run pytest
```
Expected: 印出 `import ok`；pytest 全綠（既有 + 新增）。

- [ ] **Step 3：commit**

```bash
git add src/kinsun/app.py
git commit -m "feat: 組裝層接上家屬端 REST API 與 /liff 靜態供應

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 5：前端 React + Vite + TypeScript（frontend/）

**Files:**
- Modify: `.gitignore`
- Create: `frontend/package.json`、`frontend/vite.config.ts`、`frontend/tsconfig.json`、`frontend/index.html`、`frontend/.env.example`、`frontend/src/vite-env.d.ts`、`frontend/src/main.tsx`、`frontend/src/App.tsx`、`frontend/package-lock.json`（由 npm 產生）

**Interfaces:**
- Consumes: `GET /api/me/elders`（Task 2）回 `{"elders":[{elder_id,name}]}`
- 交付物：`npm --prefix frontend run typecheck` 與 `npm --prefix frontend run build` 皆成功，產出 `frontend/dist`

- [ ] **Step 1：先擋掉前端產物**（`.gitignore` 檔末追加）

```
# 前端（LIFF）
frontend/node_modules/
frontend/dist/
```

- [ ] **Step 2：建立前端檔案**

`frontend/package.json`：
```json
{
  "name": "kinsun-liff",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@line/liff": "^2.25.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
```

`frontend/vite.config.ts`：
```ts
import react from "@vitejs/plugin-react";
import { defineConfig } from "vite";

export default defineConfig({
  base: "/liff/",
  plugins: [react()],
  server: {
    proxy: {
      "/api": "http://localhost:8000",
    },
  },
});
```

`frontend/tsconfig.json`：
```json
{
  "compilerOptions": {
    "target": "ES2020",
    "lib": ["ES2020", "DOM", "DOM.Iterable"],
    "module": "ESNext",
    "moduleResolution": "bundler",
    "jsx": "react-jsx",
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "skipLibCheck": true,
    "noEmit": true
  },
  "include": ["src"]
}
```

`frontend/index.html`：
```html
<!doctype html>
<html lang="zh-Hant">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>金孫 — 家屬端</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/main.tsx"></script>
  </body>
</html>
```

`frontend/.env.example`：
```
VITE_LIFF_ID=
```

`frontend/src/vite-env.d.ts`：
```ts
/// <reference types="vite/client" />

interface ImportMetaEnv {
  readonly VITE_LIFF_ID: string;
}

interface ImportMeta {
  readonly env: ImportMetaEnv;
}
```

`frontend/src/main.tsx`：
```tsx
import React from "react";
import ReactDOM from "react-dom/client";

import { App } from "./App";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
);
```

`frontend/src/App.tsx`：
```tsx
import { useEffect, useState } from "react";
import liff from "@line/liff";

type Elder = { elder_id: string; name: string };
type State =
  | { kind: "loading" }
  | { kind: "error"; message: string }
  | { kind: "ready"; elders: Elder[] };

export function App() {
  const [state, setState] = useState<State>({ kind: "loading" });

  useEffect(() => {
    void (async () => {
      try {
        await liff.init({ liffId: import.meta.env.VITE_LIFF_ID });
        if (!liff.isLoggedIn()) {
          liff.login();
          return;
        }
        const token = liff.getIDToken();
        if (!token) {
          setState({ kind: "error", message: "請重新登入" });
          return;
        }
        const res = await fetch("/api/me/elders", {
          headers: { Authorization: `Bearer ${token}` },
        });
        if (res.status === 401) {
          setState({ kind: "error", message: "請重新登入" });
          return;
        }
        if (!res.ok) {
          setState({ kind: "error", message: "載入失敗，請稍後再試" });
          return;
        }
        const data = (await res.json()) as { elders: Elder[] };
        setState({ kind: "ready", elders: data.elders });
      } catch {
        setState({ kind: "error", message: "載入失敗，請稍後再試" });
      }
    })();
  }, []);

  if (state.kind === "loading") return <p>載入中…</p>;
  if (state.kind === "error") return <p>{state.message}</p>;
  if (state.elders.length === 0) {
    return <p>您還沒有長輩檔案。請在 LINE 回覆「設定」建立。</p>;
  }
  return (
    <main>
      <h1>您管理的長輩</h1>
      <ul>
        {state.elders.map((e) => (
          <li key={e.elder_id}>{e.name}</li>
        ))}
      </ul>
    </main>
  );
}
```

- [ ] **Step 3：安裝依賴（產生 lockfile + node_modules）**

Run: `npm --prefix frontend install`
Expected: 產生 `frontend/package-lock.json` 與 `frontend/node_modules/`（已被 .gitignore 擋）。
（若某 caret 版本無法解析，改用 `npm --prefix frontend install <套件>@latest` 重裝對應套件。）

- [ ] **Step 4：型別檢查 + 建置**

Run:
```bash
npm --prefix frontend run typecheck
npm --prefix frontend run build
```
Expected: typecheck 無錯；build 成功並產出 `frontend/dist/`（含 `index.html` 與 `assets/`）。

- [ ] **Step 5：commit**（不含 node_modules / dist）

```bash
git add frontend/package.json frontend/package-lock.json frontend/vite.config.ts \
        frontend/tsconfig.json frontend/index.html frontend/.env.example \
        frontend/src/vite-env.d.ts frontend/src/main.tsx frontend/src/App.tsx .gitignore
git commit -m "feat: 家屬端 LIFF 前端薄切片（React+Vite+TS，登入後列長輩）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 6：CI frontend job + README

**Files:**
- Modify: `.github/workflows/ci.yml`、`README.md`

**Interfaces:** 無程式介面；CI 新增前端 typecheck+build 把關。

- [ ] **Step 1：CI 新增 frontend job**

`.github/workflows/ci.yml` 在 `jobs:` 底下、`test:` job 之後加入：
```yaml
  frontend:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: 安裝 Node
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: 安裝前端依賴
        run: npm ci
        working-directory: frontend

      - name: 型別檢查
        run: npm run typecheck
        working-directory: frontend

      - name: 建置
        run: npm run build
        working-directory: frontend
```

- [ ] **Step 2：README 補一節**

`README.md` 末尾加入：
```markdown
## 家屬端 LIFF（開發 / 部署）

家屬端網頁採 React + Vite + TypeScript，置於 [`frontend/`](frontend/)，由後端 FastAPI 同源供應於 `/liff`。

### 開發
1. 後端：`uv run uvicorn --app-dir src "kinsun.app:build_app" --factory --reload --port 8000`
2. 前端：`npm --prefix frontend install` 後 `npm --prefix frontend run dev`（dev 會把 `/api` proxy 到本機後端）。
3. 在 `frontend/.env` 填 `VITE_LIFF_ID`（複製自 `frontend/.env.example`）。

### 部署
1. `npm --prefix frontend install && npm --prefix frontend run build` 產出 `frontend/dist`，後端才會供應 `/liff`。
2. 在 LINE Developers 建一個與 Messaging API **同 provider** 的 LINE Login channel + LIFF app，endpoint 指向 `https://<host>/liff`。
3. 後端 `.env` 設 `LIFF_CHANNEL_ID`（該 Login channel ID）；前端 `VITE_LIFF_ID` 設 LIFF ID。
```

- [ ] **Step 3：驗證 YAML 與既有測試**

Run:
```bash
uv run pre-commit run check-yaml --files .github/workflows/ci.yml
uv run pytest
```
Expected: check-yaml Passed；pytest 全綠。

- [ ] **Step 4：commit**

```bash
git add .github/workflows/ci.yml README.md
git commit -m "ci: 新增前端 typecheck+build job；docs: 家屬端 LIFF 開發部署說明

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 7：全量品質閘門

**Files:** 無（純驗證）。

- [ ] **Step 1：後端 lint / format / 測試**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠（雲端整合 opt-in SKIPPED）。
若 format 報未格式化 → `uv run ruff format .` 後重跑並把變更納入最後 commit。

- [ ] **Step 2：前端 typecheck + build**

Run:
```bash
npm --prefix frontend run typecheck
npm --prefix frontend run build
```
Expected: 皆成功。

- [ ] **Step 3：pre-commit 全鉤子**

Run: `uv run pre-commit run --all-files`
Expected: 全 Passed（含 check-yaml、ruff、ruff format、check-added-large-files）。
若 `check-added-large-files` 擋 `frontend/package-lock.json`，於最後 commit 階段處理（lockfile 為必要檔，必要時調整 hook 的 `--maxkb` 引數）。

- [ ] **Step 4：靜態匯入驗證**

Run: `PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"`
Expected: `import ok`。

（本 Task 不一定產生變更；若 Step 1 有 format 修整，補一個
`chore: 套用格式化` commit。）

---

## Self-Review（計畫對 spec 覆蓋檢查）

- 驗證器 `LineIdTokenVerifier`（spec §3）→ Task 1 ✅
- `GET /api/me/elders` + 驗證 dependency（§4）→ Task 2 ✅
- app.py 接線 + 條件式靜態掛載（§5）→ Task 4 ✅
- `liff_channel_id` / `liff_timeout_seconds` + .env.example（§6）→ Task 3 ✅
- 前端 React+Vite+TS、`base:/liff/`、App 流程、VITE_LIFF_ID（§7）→ Task 5 ✅
- 後端離線測試（§8）→ Task 1、2 ✅；前端 typecheck+build（§8）→ Task 5、7 ✅
- fail-safe 401 / dist 不存在不掛載（§9）→ Task 2、4 ✅
- CI frontend job、.gitignore、部署/LINE 設定、README（§10）→ Task 5、6 ✅

型別/命名一致性：`create_api_router(*, verifier, accounts)`、`LiffVerifier.verify(id_token)->str`、回應 `{"elders":[{elder_id,name}]}`、`base:"/liff/"` ↔ 後端 mount `/liff` ↔ App `fetch("/api/me/elders")` 全對齊。無佔位符。
