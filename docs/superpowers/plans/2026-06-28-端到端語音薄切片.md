# 端到端語音薄切片 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 打通一條最薄但端到端的語音管線：LINE 收語音 → ASR → Care Agent → Gemini → TTS placeholder → LINE 回覆。

**Architecture:** 應用層為純 Python、OS-agnostic、位置無關（呼叫端只透過環境變數設定的 endpoint 連 DGX 上的重模型）。ASR/TTS/LLM 都先定義抽象介面，提供 mock/placeholder 實作，讓 7 人能並行、真模型可零痛感替換。webhook 用 FastAPI，dev 期不需 GPU、不需真實憑證即可跑單元測試。

**Tech Stack:** Python 3.12、uv、FastAPI、uvicorn、line-bot-sdk v3、google-genai、pytest、ruff。ASR 的 DGX client 用標準庫 `urllib.request`（不增依賴）。

## Global Constraints

- Python `>=3.12`；以 uv 管理依賴與環境。
- **OS-agnostic**：一律 `pathlib`、不寫死路徑、設定／金鑰全走環境變數（[AGENTS.md](../../../AGENTS.md) 開發與部署環境）。
- **ARM64 相容**：新增依賴須在 `linux/aarch64` 有 wheel；吃 GPU 的重模型只跑在 DGX。
- **位置無關**：ASR/TTS 為網路服務，應用層只是 client，endpoint 由環境變數設定。
- 全程文字＝**繁體國語漢字**；本切片 TTS 為 placeholder（回文字泡泡，`audio=None`）。
- 不寫死任何金鑰；ruff 規則 `E,W,F,I,B,UP`，line-length 100。
- 採**扁平套件佈局**：套件目錄 `kinsun/` 置於專案根目錄（本專案為應用程式、非套件，扁平佈局讓 pytest 與 uvicorn 都能直接 `import kinsun`，免裝免路徑 hack）。此處微調 spec 的 `src/kinsun/` 草圖。
- commit 訊息遵循 CONTRIBUTING.md（feat/fix/docs/refactor/test/chore），結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## File Structure

```
kinsun/
  __init__.py
  config.py              # Settings（frozen dataclass）+ load_settings(env) + ConfigError
  llm.py                 # LLMClient(Protocol) + LLMError + GeminiClient
  agent.py               # CareAgent(llm)：SYSTEM_PROMPT、handle(text)->str
  pipeline.py            # VoicePipeline(asr, agent, tts)：process(audio,content_type)->TtsResult
  app.py                 # 組裝根（composition root）：build_app()->FastAPI（uvicorn --factory）
  speech/
    __init__.py
    asr.py               # ASRClient(Protocol) + ASRError + MockAsrClient + DgxAsrClient + build_asr_client
    tts.py               # TTSClient(Protocol) + TtsResult + TextBubbleTts
  channels/
    __init__.py
    line/
      __init__.py
      messenger.py       # LineMessenger(Protocol) + LineApiMessenger（包 line-bot-sdk）
      webhook.py         # create_app(*, parser, pipeline, messenger)：驗簽→路由→回覆
services/
  asr/
    server.py            # DGX 端 FastAPI：載入 Breeze-ASR-26，POST /transcribe（不進 dev 測試）
    requirements.txt     # torch/transformers…（僅在 DGX 安裝）
    README.md            # DGX 部署說明
tests/
  test_config.py
  test_tts.py
  test_asr.py
  test_llm.py
  test_agent.py
  test_pipeline.py
  test_webhook.py
.env.example
```

---

## Task 1：依賴與設定（config）

**Files:**
- Modify: `pyproject.toml`（加依賴、pytest 設定）
- Create: `kinsun/__init__.py`, `kinsun/config.py`, `tests/test_config.py`, `.env.example`

**Interfaces:**
- Produces:
  - `ConfigError(Exception)`
  - `@dataclass(frozen=True) Settings`：欄位 `line_channel_secret: str`、`line_channel_access_token: str`、`gemini_api_key: str`、`gemini_model: str`、`asr_backend: str`、`asr_endpoint: str`、`asr_timeout_seconds: float`、`llm_timeout_seconds: float`
  - `load_settings(env: Mapping[str, str]) -> Settings`：缺必填（secret/token/key）拋 `ConfigError`；`asr_backend` 預設 `"mock"`、`gemini_model` 預設 `"gemini-2.5-flash"`、`asr_timeout_seconds` 預設 `15.0`、`llm_timeout_seconds` 預設 `30.0`。

- [ ] **Step 1：加依賴與 pytest 設定**

```bash
uv add fastapi uvicorn "line-bot-sdk>=3" google-genai
uv add --dev pytest httpx
```

在 `pyproject.toml` 末尾加：

```toml
[tool.pytest.ini_options]
testpaths = ["tests"]
```

- [ ] **Step 2：寫失敗測試** `tests/test_config.py`

```python
import pytest

from kinsun.config import ConfigError, Settings, load_settings

BASE_ENV = {
    "LINE_CHANNEL_SECRET": "secret",
    "LINE_CHANNEL_ACCESS_TOKEN": "token",
    "GEMINI_API_KEY": "key",
}


def test_load_settings_reads_required_and_defaults():
    settings = load_settings(BASE_ENV)
    assert isinstance(settings, Settings)
    assert settings.line_channel_secret == "secret"
    assert settings.asr_backend == "mock"
    assert settings.gemini_model == "gemini-2.5-flash"


def test_load_settings_missing_required_raises():
    with pytest.raises(ConfigError):
        load_settings({})


def test_load_settings_overrides_from_env():
    env = {**BASE_ENV, "ASR_BACKEND": "dgx", "ASR_ENDPOINT": "http://dgx:8001"}
    settings = load_settings(env)
    assert settings.asr_backend == "dgx"
    assert settings.asr_endpoint == "http://dgx:8001"
```

- [ ] **Step 3：執行確認失敗**：`uv run pytest tests/test_config.py -v` → FAIL（ModuleNotFoundError: kinsun.config）

- [ ] **Step 4：實作** `kinsun/__init__.py`（空檔）與 `kinsun/config.py`

```python
"""集中讀取環境變數的設定。不寫死任何金鑰。"""

from __future__ import annotations

from collections.abc import Mapping
from dataclasses import dataclass


class ConfigError(Exception):
    """設定錯誤（缺必填環境變數等）。"""


@dataclass(frozen=True)
class Settings:
    line_channel_secret: str
    line_channel_access_token: str
    gemini_api_key: str
    gemini_model: str
    asr_backend: str
    asr_endpoint: str
    asr_timeout_seconds: float
    llm_timeout_seconds: float


def _require(env: Mapping[str, str], key: str) -> str:
    value = env.get(key)
    if not value:
        raise ConfigError(f"缺少必要環境變數：{key}")
    return value


def load_settings(env: Mapping[str, str]) -> Settings:
    return Settings(
        line_channel_secret=_require(env, "LINE_CHANNEL_SECRET"),
        line_channel_access_token=_require(env, "LINE_CHANNEL_ACCESS_TOKEN"),
        gemini_api_key=_require(env, "GEMINI_API_KEY"),
        gemini_model=env.get("GEMINI_MODEL", "gemini-2.5-flash"),
        asr_backend=env.get("ASR_BACKEND", "mock"),
        asr_endpoint=env.get("ASR_ENDPOINT", ""),
        asr_timeout_seconds=float(env.get("ASR_TIMEOUT_SECONDS", "15")),
        llm_timeout_seconds=float(env.get("LLM_TIMEOUT_SECONDS", "30")),
    )
```

`.env.example`：

```dotenv
# LINE Messaging API
LINE_CHANNEL_SECRET=
LINE_CHANNEL_ACCESS_TOKEN=
# Gemini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
# ASR：dev 用 mock；整合測試改 dgx 並填 endpoint
ASR_BACKEND=mock
ASR_ENDPOINT=
```

- [ ] **Step 5：執行確認通過**：`uv run pytest tests/test_config.py -v` → PASS

- [ ] **Step 6：commit**

```bash
git add pyproject.toml uv.lock kinsun/__init__.py kinsun/config.py tests/test_config.py .env.example
git commit -m "feat: 加入專案依賴與設定模組（config）"
```

---

## Task 2：TTS placeholder（文字泡泡）

**Files:** Create `kinsun/speech/__init__.py`, `kinsun/speech/tts.py`, `tests/test_tts.py`

**Interfaces:**
- Produces:
  - `@dataclass(frozen=True) TtsResult`：`text: str`、`audio: bytes | None`
  - `class TTSClient(Protocol)`：`synthesize(self, text: str) -> TtsResult`
  - `class TextBubbleTts`：`synthesize(text)` 回 `TtsResult(text=text, audio=None)`

- [ ] **Step 1：失敗測試** `tests/test_tts.py`

```python
from kinsun.speech.tts import TextBubbleTts, TtsResult


def test_text_bubble_returns_text_without_audio():
    result = TextBubbleTts().synthesize("阿公您好")
    assert isinstance(result, TtsResult)
    assert result.text == "阿公您好"
    assert result.audio is None
```

- [ ] **Step 2：確認失敗**：`uv run pytest tests/test_tts.py -v` → FAIL

- [ ] **Step 3：實作** `kinsun/speech/__init__.py`（空）與 `kinsun/speech/tts.py`

```python
"""TTS 介面與 placeholder。真台語 TTS 練好後替換實作，呼叫端不變。"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Protocol


@dataclass(frozen=True)
class TtsResult:
    text: str
    audio: bytes | None = None


class TTSClient(Protocol):
    def synthesize(self, text: str) -> TtsResult: ...


class TextBubbleTts:
    """placeholder：回文字泡泡，不產音檔（audio=None）。"""

    def synthesize(self, text: str) -> TtsResult:
        return TtsResult(text=text, audio=None)
```

- [ ] **Step 4：確認通過**：`uv run pytest tests/test_tts.py -v` → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/speech/__init__.py kinsun/speech/tts.py tests/test_tts.py
git commit -m "feat: 加入 TTS 介面與文字泡泡 placeholder"
```

---

## Task 3：ASR client（介面 + Mock + DGX）

**Files:** Create `kinsun/speech/asr.py`, `tests/test_asr.py`

**Interfaces:**
- Consumes：`Settings`（Task 1）
- Produces:
  - `ASRError(Exception)`
  - `class ASRClient(Protocol)`：`transcribe(self, audio: bytes, *, content_type: str) -> str`
  - `class MockAsrClient`：`__init__(self, transcript: str = "今天天氣真好")`；`transcribe(...)` 回該文字
  - `class DgxAsrClient`：`__init__(self, endpoint: str, timeout: float)`；`transcribe` 以 `urllib.request` POST 原始 bytes 到 `endpoint`，讀回 JSON `{"text": ...}`；失敗包成 `ASRError`
  - `build_asr_client(settings: Settings) -> ASRClient`：`asr_backend=="dgx"` 回 `DgxAsrClient`（缺 endpoint 拋 `ASRError`），否則 `MockAsrClient`

- [ ] **Step 1：失敗測試** `tests/test_asr.py`

```python
import pytest

from kinsun.config import load_settings
from kinsun.speech.asr import ASRClient, MockAsrClient, build_asr_client

BASE_ENV = {
    "LINE_CHANNEL_SECRET": "s",
    "LINE_CHANNEL_ACCESS_TOKEN": "t",
    "GEMINI_API_KEY": "k",
}


def test_mock_returns_configured_transcript():
    client = MockAsrClient("阿公早安")
    assert client.transcribe(b"\x00\x01", content_type="audio/m4a") == "阿公早安"


def test_build_returns_mock_for_mock_backend():
    settings = load_settings(BASE_ENV)
    client = build_asr_client(settings)
    assert isinstance(client, MockAsrClient)


def test_build_dgx_without_endpoint_raises():
    from kinsun.speech.asr import ASRError

    settings = load_settings({**BASE_ENV, "ASR_BACKEND": "dgx"})
    with pytest.raises(ASRError):
        build_asr_client(settings)


def test_mock_satisfies_protocol():
    client: ASRClient = MockAsrClient()
    assert client.transcribe(b"x", content_type="audio/m4a")
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `kinsun/speech/asr.py`

```python
"""ASR 介面與實作。dev 用 Mock；正式呼叫 DGX 上的 services/asr。"""

from __future__ import annotations

import json
import urllib.error
import urllib.request
from typing import Protocol

from kinsun.config import Settings


class ASRError(Exception):
    """語音辨識失敗。"""


class ASRClient(Protocol):
    def transcribe(self, audio: bytes, *, content_type: str) -> str: ...


class MockAsrClient:
    """dev 用：回固定文字，不連網、不需 GPU。"""

    def __init__(self, transcript: str = "今天天氣真好") -> None:
        self._transcript = transcript

    def transcribe(self, audio: bytes, *, content_type: str) -> str:
        return self._transcript


class DgxAsrClient:
    """正式：POST 原始音檔 bytes 到 DGX 上的 ASR 服務，取回辨識文字。"""

    def __init__(self, endpoint: str, timeout: float) -> None:
        self._endpoint = endpoint
        self._timeout = timeout

    def transcribe(self, audio: bytes, *, content_type: str) -> str:
        request = urllib.request.Request(
            self._endpoint,
            data=audio,
            headers={"Content-Type": content_type},
            method="POST",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                payload = json.loads(response.read().decode("utf-8"))
        except (urllib.error.URLError, OSError, json.JSONDecodeError) as exc:
            raise ASRError(f"DGX ASR 呼叫失敗：{exc}") from exc
        text = payload.get("text")
        if not isinstance(text, str):
            raise ASRError("DGX ASR 回應缺少 text 欄位")
        return text


def build_asr_client(settings: Settings) -> ASRClient:
    if settings.asr_backend == "dgx":
        if not settings.asr_endpoint:
            raise ASRError("ASR_BACKEND=dgx 但未設定 ASR_ENDPOINT")
        return DgxAsrClient(settings.asr_endpoint, settings.asr_timeout_seconds)
    return MockAsrClient()
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/speech/asr.py tests/test_asr.py
git commit -m "feat: 加入 ASR 介面、Mock 與 DGX client"
```

---

## Task 4：LLM client（Gemini）

**Files:** Create `kinsun/llm.py`, `tests/test_llm.py`

**Interfaces:**
- Produces:
  - `LLMError(Exception)`
  - `class LLMClient(Protocol)`：`generate(self, *, system_prompt: str, user_text: str) -> str`
  - `class GeminiClient`：`__init__(self, *, api_key: str, model: str, timeout: float)`（`api_key` 空字串拋 `LLMError`）；`generate(...)` 呼叫 google-genai，回 `response.text`，例外包 `LLMError`

> 註：不在 dev 單元測試打真實 Gemini。測試只驗證「空金鑰拋錯」與「Protocol 可被簡單 fake 滿足」。

- [ ] **Step 1：失敗測試** `tests/test_llm.py`

```python
import pytest

from kinsun.llm import GeminiClient, LLMClient, LLMError


def test_empty_api_key_raises():
    with pytest.raises(LLMError):
        GeminiClient(api_key="", model="gemini-2.5-flash", timeout=30.0)


def test_fake_satisfies_protocol():
    class FakeLLM:
        def generate(self, *, system_prompt: str, user_text: str) -> str:
            return f"回應：{user_text}"

    client: LLMClient = FakeLLM()
    assert client.generate(system_prompt="s", user_text="嗨") == "回應：嗨"
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `kinsun/llm.py`

```python
"""LLM 介面與 Gemini 實作。系統指令＋使用者文字 → 繁體國語漢字回應。"""

from __future__ import annotations

from typing import Protocol


class LLMError(Exception):
    """LLM 呼叫失敗。"""


class LLMClient(Protocol):
    def generate(self, *, system_prompt: str, user_text: str) -> str: ...


class GeminiClient:
    def __init__(self, *, api_key: str, model: str, timeout: float) -> None:
        if not api_key:
            raise LLMError("缺少 GEMINI_API_KEY")
        from google import genai

        self._client = genai.Client(api_key=api_key)
        self._model = model
        self._timeout = timeout

    def generate(self, *, system_prompt: str, user_text: str) -> str:
        from google.genai import types

        try:
            response = self._client.models.generate_content(
                model=self._model,
                contents=user_text,
                config=types.GenerateContentConfig(system_instruction=system_prompt),
            )
        except Exception as exc:  # noqa: BLE001 - 統一轉成可辨識的 LLMError
            raise LLMError(f"Gemini 呼叫失敗：{exc}") from exc
        text = response.text
        if not text:
            raise LLMError("Gemini 回應為空")
        return text
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/llm.py tests/test_llm.py
git commit -m "feat: 加入 LLM 介面與 Gemini client"
```

---

## Task 5：Care Agent

**Files:** Create `kinsun/agent.py`, `tests/test_agent.py`

**Interfaces:**
- Consumes：`LLMClient`（Task 4）
- Produces：
  - `SYSTEM_PROMPT: str`（金孫人設、繁體國語、不做醫療診斷）
  - `class CareAgent`：`__init__(self, llm: LLMClient)`；`handle(self, user_text: str) -> str`：以 `SYSTEM_PROMPT` 呼叫 `llm.generate`，回其輸出

- [ ] **Step 1：失敗測試** `tests/test_agent.py`

```python
from kinsun.agent import SYSTEM_PROMPT, CareAgent


class SpyLLM:
    def __init__(self) -> None:
        self.calls: list[tuple[str, str]] = []

    def generate(self, *, system_prompt: str, user_text: str) -> str:
        self.calls.append((system_prompt, user_text))
        return "金孫回您：好的"


def test_handle_calls_llm_with_system_prompt_and_returns_reply():
    llm = SpyLLM()
    agent = CareAgent(llm)
    reply = agent.handle("我今天有點累")
    assert reply == "金孫回您：好的"
    assert llm.calls == [(SYSTEM_PROMPT, "我今天有點累")]
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `kinsun/agent.py`

```python
"""Care Agent 樞紐：用系統指令呼叫 LLM 產生回覆。本切片僅最小職責。"""

from __future__ import annotations

from kinsun.llm import LLMClient

SYSTEM_PROMPT = (
    "你是「金孫」，一位溫暖、有耐心的台灣長輩陪伴助理。"
    "一律用台灣繁體中文、口語、簡短地回應，語氣像晚輩關心長輩。"
    "你不是醫師，絕不提供醫療診斷或處方；遇到健康疑慮，溫柔建議對方告訴家人或就醫。"
)


class CareAgent:
    def __init__(self, llm: LLMClient) -> None:
        self._llm = llm

    def handle(self, user_text: str) -> str:
        return self._llm.generate(system_prompt=SYSTEM_PROMPT, user_text=user_text)
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/agent.py tests/test_agent.py
git commit -m "feat: 加入 Care Agent 樞紐"
```

---

## Task 6：Voice Pipeline

**Files:** Create `kinsun/pipeline.py`, `tests/test_pipeline.py`

**Interfaces:**
- Consumes：`ASRClient`、`CareAgent`、`TTSClient`
- Produces：
  - `class VoicePipeline`：`__init__(self, *, asr: ASRClient, agent: CareAgent, tts: TTSClient)`；`process(self, audio: bytes, content_type: str = "audio/m4a") -> TtsResult`：asr→agent→tts

- [ ] **Step 1：失敗測試** `tests/test_pipeline.py`

```python
from kinsun.agent import CareAgent
from kinsun.pipeline import VoicePipeline
from kinsun.speech.asr import MockAsrClient
from kinsun.speech.tts import TextBubbleTts


class EchoLLM:
    def generate(self, *, system_prompt: str, user_text: str) -> str:
        return f"你說的是：{user_text}"


def test_pipeline_runs_asr_agent_tts():
    pipeline = VoicePipeline(
        asr=MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM()),
        tts=TextBubbleTts(),
    )
    result = pipeline.process(b"\x00\x01")
    assert result.text == "你說的是：阿公早安"
    assert result.audio is None
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `kinsun/pipeline.py`

```python
"""語音管線：把 ASR、Agent、TTS 串成一次處理。"""

from __future__ import annotations

from kinsun.agent import CareAgent
from kinsun.speech.asr import ASRClient
from kinsun.speech.tts import TtsResult, TTSClient


class VoicePipeline:
    def __init__(self, *, asr: ASRClient, agent: CareAgent, tts: TTSClient) -> None:
        self._asr = asr
        self._agent = agent
        self._tts = tts

    def process(self, audio: bytes, content_type: str = "audio/m4a") -> TtsResult:
        user_text = self._asr.transcribe(audio, content_type=content_type)
        reply_text = self._agent.handle(user_text)
        return self._tts.synthesize(reply_text)
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/pipeline.py tests/test_pipeline.py
git commit -m "feat: 加入語音管線 VoicePipeline"
```

---

## Task 7：LINE webhook 與 messenger

**Files:** Create `kinsun/channels/__init__.py`, `kinsun/channels/line/__init__.py`, `kinsun/channels/line/messenger.py`, `kinsun/channels/line/webhook.py`, `tests/test_webhook.py`

**Interfaces:**
- Consumes：`VoicePipeline`（Task 6）
- Produces：
  - `class LineMessenger(Protocol)`：`get_audio(self, message_id: str) -> bytes`、`reply_text(self, reply_token: str, text: str) -> None`
  - `class LineApiMessenger`（包 line-bot-sdk v3，正式用；不進 dev 測試）
  - `NON_AUDIO_PROMPT: str`、`FALLBACK_PROMPT: str`
  - `create_app(*, parser, pipeline: VoicePipeline, messenger: LineMessenger) -> FastAPI`：POST `/line/webhook`，以 `parser.parse(body, signature)` 驗簽與解析（`InvalidSignatureError` → 400）；對 audio 訊息跑 pipeline 並回覆，非 audio 回提示，`ASRError`/`LLMError` 回 fallback。

- [ ] **Step 1：失敗測試** `tests/test_webhook.py`

```python
import hashlib
import hmac
import base64
from types import SimpleNamespace

from fastapi.testclient import TestClient

from kinsun.agent import CareAgent
from kinsun.channels.line.webhook import FALLBACK_PROMPT, NON_AUDIO_PROMPT, create_app
from kinsun.pipeline import VoicePipeline
from kinsun.speech.asr import ASRError, MockAsrClient
from kinsun.speech.tts import TextBubbleTts


class EchoLLM:
    def generate(self, *, system_prompt: str, user_text: str) -> str:
        return f"你說的是：{user_text}"


class FakeMessenger:
    def __init__(self) -> None:
        self.replies: list[tuple[str, str]] = []

    def get_audio(self, message_id: str) -> bytes:
        return b"\x00\x01"

    def reply_text(self, reply_token: str, text: str) -> None:
        self.replies.append((reply_token, text))


def _audio_event():
    return SimpleNamespace(
        reply_token="rt-1",
        message=SimpleNamespace(type="audio", id="m-1"),
    )


def _text_event():
    return SimpleNamespace(
        reply_token="rt-2",
        message=SimpleNamespace(type="text", id="m-2"),
    )


class FakeParser:
    def __init__(self, events):
        self._events = events

    def parse(self, body: str, signature: str):
        return self._events


def _make_client(parser, messenger, asr=None):
    pipeline = VoicePipeline(
        asr=asr or MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM()),
        tts=TextBubbleTts(),
    )
    app = create_app(parser=parser, pipeline=pipeline, messenger=messenger)
    return TestClient(app)


def test_audio_message_replies_agent_output():
    messenger = FakeMessenger()
    client = _make_client(FakeParser([_audio_event()]), messenger)
    resp = client.post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert resp.status_code == 200
    assert messenger.replies == [("rt-1", "你說的是：阿公早安")]


def test_non_audio_message_replies_prompt():
    messenger = FakeMessenger()
    client = _make_client(FakeParser([_text_event()]), messenger)
    client.post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert messenger.replies == [("rt-2", NON_AUDIO_PROMPT)]


class BoomAsr:
    def transcribe(self, audio: bytes, *, content_type: str) -> str:
        raise ASRError("boom")


def test_pipeline_error_replies_fallback():
    messenger = FakeMessenger()
    client = _make_client(FakeParser([_audio_event()]), messenger, asr=BoomAsr())
    client.post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert messenger.replies == [("rt-1", FALLBACK_PROMPT)]


def test_invalid_signature_returns_400():
    from linebot.v3 import WebhookParser

    real_parser = WebhookParser("real-secret")
    messenger = FakeMessenger()
    client = _make_client(real_parser, messenger)
    resp = client.post(
        "/line/webhook", content=b'{"events":[]}', headers={"X-Line-Signature": "wrong"}
    )
    assert resp.status_code == 400
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** messenger 與 webhook

`kinsun/channels/__init__.py`、`kinsun/channels/line/__init__.py`：空檔。

`kinsun/channels/line/messenger.py`：

```python
"""LINE 訊息埠：抽象出「下載語音」「回覆文字」，方便測試與替換。"""

from __future__ import annotations

from typing import Protocol


class LineMessenger(Protocol):
    def get_audio(self, message_id: str) -> bytes: ...
    def reply_text(self, reply_token: str, text: str) -> None: ...


class LineApiMessenger:
    """正式實作：包 line-bot-sdk v3。不進 dev 單元測試（需真實憑證）。"""

    def __init__(self, access_token: str) -> None:
        from linebot.v3.messaging import (
            ApiClient,
            Configuration,
            MessagingApi,
            MessagingApiBlob,
            ReplyMessageRequest,
            TextMessage,
        )

        self._configuration = Configuration(access_token=access_token)
        self._ApiClient = ApiClient
        self._MessagingApi = MessagingApi
        self._MessagingApiBlob = MessagingApiBlob
        self._ReplyMessageRequest = ReplyMessageRequest
        self._TextMessage = TextMessage

    def get_audio(self, message_id: str) -> bytes:
        with self._ApiClient(self._configuration) as api_client:
            blob = self._MessagingApiBlob(api_client)
            return blob.get_message_content(message_id)

    def reply_text(self, reply_token: str, text: str) -> None:
        with self._ApiClient(self._configuration) as api_client:
            api = self._MessagingApi(api_client)
            api.reply_message(
                self._ReplyMessageRequest(
                    reply_token=reply_token,
                    messages=[self._TextMessage(text=text)],
                )
            )
```

`kinsun/channels/line/webhook.py`：

```python
"""LINE webhook：驗簽 → 解析事件 → 跑語音管線 → 回覆。"""

from __future__ import annotations

from fastapi import FastAPI, HTTPException, Request
from linebot.v3.exceptions import InvalidSignatureError

from kinsun.channels.line.messenger import LineMessenger
from kinsun.pipeline import VoicePipeline
from kinsun.speech.asr import ASRError
from kinsun.llm import LLMError

NON_AUDIO_PROMPT = "金孫現在聽得懂語音喔，您可以按住麥克風跟我說說話。"
FALLBACK_PROMPT = "金孫剛剛沒聽清楚，您可以再說一次嗎？"


def _handle_events(events, pipeline: VoicePipeline, messenger: LineMessenger) -> None:
    for event in events:
        message = getattr(event, "message", None)
        reply_token = getattr(event, "reply_token", None)
        if message is None or reply_token is None:
            continue
        if getattr(message, "type", None) != "audio":
            messenger.reply_text(reply_token, NON_AUDIO_PROMPT)
            continue
        try:
            audio = messenger.get_audio(message.id)
            result = pipeline.process(audio)
            messenger.reply_text(reply_token, result.text)
        except (ASRError, LLMError):
            messenger.reply_text(reply_token, FALLBACK_PROMPT)


def create_app(*, parser, pipeline: VoicePipeline, messenger: LineMessenger) -> FastAPI:
    app = FastAPI()

    @app.post("/line/webhook")
    async def line_webhook(request: Request) -> dict[str, bool]:
        body = await request.body()
        signature = request.headers.get("X-Line-Signature", "")
        try:
            events = parser.parse(body.decode("utf-8"), signature)
        except InvalidSignatureError as exc:
            raise HTTPException(status_code=400, detail="invalid signature") from exc
        _handle_events(events, pipeline, messenger)
        return {"ok": True}

    return app
```

- [ ] **Step 4：確認通過**：`uv run pytest tests/test_webhook.py -v` → PASS
- [ ] **Step 5：commit**

```bash
git add kinsun/channels tests/test_webhook.py
git commit -m "feat: 加入 LINE webhook 與訊息埠"
```

---

## Task 8：組裝根與啟動文件

**Files:** Create `kinsun/app.py`；Modify `README.md`（加「啟動本機開發」一節）

**Interfaces:**
- Consumes：全部
- Produces：`build_app() -> FastAPI`：`load_settings(os.environ)` → 組真實 messenger、`WebhookParser`、`VoicePipeline`（asr 由 `build_asr_client`，llm 為 `GeminiClient`）→ `create_app(...)`

- [ ] **Step 1：實作** `kinsun/app.py`（組裝根，無單元測試——它只是接線，且需真實環境變數）

```python
"""組裝根：把設定與各元件接成可服務的 FastAPI app。

啟動：uv run uvicorn "kinsun.app:build_app" --factory --reload
"""

from __future__ import annotations

import os

from fastapi import FastAPI
from linebot.v3 import WebhookParser

from kinsun.agent import CareAgent
from kinsun.channels.line.messenger import LineApiMessenger
from kinsun.channels.line.webhook import create_app
from kinsun.config import load_settings
from kinsun.llm import GeminiClient
from kinsun.pipeline import VoicePipeline
from kinsun.speech.asr import build_asr_client
from kinsun.speech.tts import TextBubbleTts


def build_app() -> FastAPI:
    settings = load_settings(os.environ)
    pipeline = VoicePipeline(
        asr=build_asr_client(settings),
        agent=CareAgent(
            GeminiClient(
                api_key=settings.gemini_api_key,
                model=settings.gemini_model,
                timeout=settings.llm_timeout_seconds,
            )
        ),
        tts=TextBubbleTts(),
    )
    messenger = LineApiMessenger(settings.line_channel_access_token)
    parser = WebhookParser(settings.line_channel_secret)
    return create_app(parser=parser, pipeline=pipeline, messenger=messenger)
```

- [ ] **Step 2：驗證可匯入**（不啟服務，避免需要真實金鑰）：

Run: `uv run python -c "import kinsun.app; print('ok')"`
Expected: 印出 `ok`（只匯入模組，不呼叫 build_app）

- [ ] **Step 3：更新 README**，在「快速開始」後加一節：

```markdown
### 啟動本機開發（第一刀薄切片）

1. 複製 `.env.example` 為 `.env`，填入 LINE 與 Gemini 憑證（ASR 先用 `mock`）。
2. 啟動 webhook：

   ```bash
   uv run uvicorn "kinsun.app:build_app" --factory --reload --port 8000
   ```

3. 用 ngrok 之類工具把 `https://<你的網域>/line/webhook` 設為 LINE 的 Webhook URL。
4. 對 LINE 官方帳號傳語音訊息，即可收到金孫回覆（dev 期 ASR 為 mock 文字）。

> 真實 ASR：在 DGX 啟動 `services/asr`，並把 `.env` 改為 `ASR_BACKEND=dgx`、`ASR_ENDPOINT=http://<dgx>:8001/transcribe`。
```

- [ ] **Step 4：commit**

```bash
git add kinsun/app.py README.md
git commit -m "feat: 加入組裝根與本機啟動說明"
```

---

## Task 9：DGX 端 ASR 服務（不進 dev 測試）

**Files:** Create `services/asr/server.py`, `services/asr/requirements.txt`, `services/asr/README.md`

> 此服務只跑在 DGX（需 GPU 與 transformers/torch）。**不**加入根專案依賴、**不**進 dev 測試套件。重模型以延遲匯入（lazy import）載入，使檔案在無 GPU 環境仍可被 ruff 檢查。

- [ ] **Step 1：實作** `services/asr/server.py`

```python
"""DGX 端 ASR 推論服務：載入 Breeze-ASR-26，提供 POST /transcribe。

僅在 DGX（Linux + ARM64 + GPU）執行。安裝見 services/asr/requirements.txt。
啟動：uv run uvicorn services.asr.server:app --host 0.0.0.0 --port 8001
（或於 DGX 既有環境以 python -m uvicorn 啟動）
"""

from __future__ import annotations

from fastapi import FastAPI, Request

app = FastAPI(title="KinSun ASR (Breeze-ASR-26)")
_model = None


def _get_model():
    global _model
    if _model is None:
        # 延遲匯入：無 GPU 的開發機不需安裝這些重依賴
        from transformers import pipeline as hf_pipeline

        _model = hf_pipeline(
            "automatic-speech-recognition",
            model="MediaTek-Research/Breeze-ASR-26",
        )
    return _model


@app.post("/transcribe")
async def transcribe(request: Request) -> dict[str, str]:
    audio = await request.body()
    model = _get_model()
    result = model(audio)
    return {"text": result["text"]}
```

- [ ] **Step 2：寫** `services/asr/requirements.txt`

```text
# 僅在 DGX 安裝。版本於 DGX 上以實機驗證後鎖定。
fastapi
uvicorn
transformers
torch
```

- [ ] **Step 3：寫** `services/asr/README.md`（DGX 部署步驟、與 `DgxAsrClient` 的契約：輸入原始音檔 bytes、輸出 `{"text": ...}`）。

- [ ] **Step 4：確認不影響 dev 測試**：`uv run pytest -q` 仍全綠（services/ 不被匯入）。

- [ ] **Step 5：commit**

```bash
git add services/asr
git commit -m "feat: 加入 DGX 端 Breeze-ASR-26 推論服務（不進 dev 測試）"
```

---

## Task 10：全測試與品質收尾

- [ ] **Step 1：全測試**：`uv run pytest -v` → 全 PASS
- [ ] **Step 2：lint/format**：`uv run ruff check .` 與 `uv run ruff format --check .` → 乾淨
- [ ] **Step 3：pre-commit**：`uv run pre-commit run --all-files` → 全綠
- [ ] **Step 4：若有自動修正，commit**

```bash
git add -A
git commit -m "chore: 通過 ruff 與 pre-commit 檢查"
```

---

## Self-Review

**Spec coverage：**
- §2.1 LINE/ASR/Agent/LLM/TTS/設定 → Task 1–8 全覆蓋。
- §3.1 位置無關 endpoint → `DgxAsrClient` + `build_asr_client`（Task 3）、`config`（Task 1）。
- §3.2 跨平台 → 扁平佈局、`pathlib` 不涉檔案、依賴皆純 Python。
- §3.3 介面可替換 → ASRClient/TTSClient/LLMClient 皆 Protocol。
- §5 資料流 → `VoicePipeline`（Task 6）。
- §6 介面定義 → Task 2/3/4 的 Protocol 與簽章一致。
- §7 環境變數 → Task 1 `.env.example`。
- §8 錯誤處理 → webhook fallback（Task 7）、自訂例外。
- §9 測試 → mock 為主、不需 GPU/憑證；整合測試（dgx）為手動。
- §10 起步分工 → 由 Leo 先做骨架（本計畫即骨架）。

**Placeholder scan：** 無 TBD/TODO；每步皆含實際程式碼或指令。

**Type consistency：** `transcribe(audio, *, content_type)`、`generate(*, system_prompt, user_text)`、`synthesize(text) -> TtsResult`、`process(audio, content_type) -> TtsResult`、`handle(user_text) -> str` 跨任務一致。
