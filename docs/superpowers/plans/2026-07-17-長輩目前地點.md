# 長輩目前地點（App 定位）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 長輩在 App 允許定位後，問天氣不再被反問「您人在哪個城市」——但金孫仍會在他問到別處時開口確認。

**Architecture:** App 在長輩按下對講機時取快取座標、在手機端譯成地名，只把地名隨 `POST /turns?location=台南市` 上傳；後端在 `dispatch` 前以 upsert 存進 `elder_locations`（一位長輩一列、不留軌跡）；`LocationFacts` 作為既有 `FactProvider` 之一，每輪把「N 分鐘前在台南市」注入 system prompt，超過門檻則完全不注入。

**Tech Stack:** Python 3／FastAPI／psycopg／pytest（後端）、React Native＋Expo Router／expo-location（App）

設計來源：`docs/superpowers/specs/2026-07-17-長輩目前地點-design.md`。有疑義時以 spec 為準。

## Global Constraints

- 語言：台灣繁體中文，全形標點；程式碼註解、commit 訊息皆同。
- 命名：`elder_id` 為業務主鍵；ID 欄位全稱不縮寫；`save` ＝ upsert；`get_*` 取單筆；布林前綴 `is_`／`has_`／`can_`。
- 環境變數：`Settings` 欄位名＝環境變數鍵的小寫，一一對應、無別名；每個鍵都必須列進 `.env.example` 並附一行中文註解。
- 時間戳欄位：`<動詞過去分詞>_at`，型別 `DOUBLE PRECISION`（epoch 秒）。
- 持久層三件套：`store.py` ＋ `LocationStore`（Protocol）＋ `PgLocationStore` ＋ `FakeLocationStore`。
- 分支：只在 `Leo` 上工作，不切 main、不自動 push。
- ⚠️ **經緯度永不進入後端**：任何步驟都不得新增接收、記錄或儲存座標的程式碼。
- ⚠️ **`elder_id` 不得成為工具參數**（本計畫不動工具層，此為防止實作時「順手」擴張的紅線）。
- Pg 整合測試以 `KINSUN_IT=1` ＋ `KINSUN_TEST_DATABASE_URL` 啟用（獨立測試庫，嚴禁連正式庫）。

---

### Task 1: 持久層與資料表

**Files:**
- Create: `src/kinsun/locations/__init__.py`
- Create: `src/kinsun/locations/store.py`
- Modify: `src/kinsun/db.py`（新增 `ELDER_LOCATIONS_DDL`；`ensure_schema` 加一行）
- Modify: `tests/fakes.py`（轉出 `FakeLocationStore`）
- Test: `tests/test_locations_store_contract.py`

**Interfaces:**
- Consumes: `kinsun.db.Database`、`kinsun.db._Errors`（既有）
- Produces: `ElderLocation(elder_id: str, place: str, recorded_at: float)`（frozen dataclass）、`LocationStore` Protocol（`save(location) -> None`、`get_for_elder(elder_id) -> ElderLocation | None`）、`PgLocationStore(db)`、`FakeLocationStore()`、`LocationError`

- [ ] **Step 1: 寫失敗的測試**

建立 `tests/test_locations_store_contract.py`：

```python
"""LocationStore 合約：Fake 與 Pg 兩個 adapter 必須對同一情境給出相同結果。

Fake 每次都跑；Pg 需 `KINSUN_IT=1`（連獨立測試庫）。斷言一律以 `ns` 前綴 scope 到
本測試自己的資料，才能在共用真庫上以「成員／排除」關係斷言而互不干擾。
"""

from __future__ import annotations

import pytest

from kinsun.locations.store import ElderLocation, FakeLocationStore, PgLocationStore


@pytest.fixture(params=["fake", "pg"])
def store(request):
    if request.param == "pg":
        return PgLocationStore(request.getfixturevalue("pg_database"))
    return FakeLocationStore()


def test_get_for_elder_returns_none_when_never_recorded(store, ns):
    assert store.get_for_elder(f"{ns}e1") is None


def test_save_then_get_round_trips(store, ns):
    store.save(ElderLocation(f"{ns}e1", "台南市", 1752739200.0))
    assert store.get_for_elder(f"{ns}e1") == ElderLocation(f"{ns}e1", "台南市", 1752739200.0)


def test_save_is_upsert_keeping_only_latest(store, ns):
    # 「覆寫式」的核心斷言：同一位長輩存兩次只留最新一筆，不累積行蹤軌跡。
    store.save(ElderLocation(f"{ns}e1", "台南市", 1752739200.0))
    store.save(ElderLocation(f"{ns}e1", "高雄市", 1752742800.0))
    assert store.get_for_elder(f"{ns}e1") == ElderLocation(f"{ns}e1", "高雄市", 1752742800.0)


def test_one_elder_location_does_not_leak_to_another(store, ns):
    store.save(ElderLocation(f"{ns}e1", "台南市", 1752739200.0))
    assert store.get_for_elder(f"{ns}e2") is None
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_locations_store_contract.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.locations'`

- [ ] **Step 3: 建立模組與持久層**

建立 `src/kinsun/locations/__init__.py`（空檔）。

建立 `src/kinsun/locations/store.py`：

```python
"""長輩目前地點：Protocol、Postgres 實作與測試替身。

單一狀態表（一位長輩一列），故 `save` 為 upsert 而非 append-only 事件流水帳
（D-42 例外：`ElderLocation` 與三件套同住本檔，比照 `proactive/preferences.py`）。

⚠️ 只存地名（如「台南市」），**不存經緯度**——座標在手機端就譯成地名，永不上傳。

刻意不提供 `list_all`：目前無任何呼叫端需要，而「一次撈出全體長輩現在在哪」正是
這份資料最不該有的介面。YAGNI 與最小權限在此同向。
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Protocol

from kinsun.db import Database, _Errors

_COLUMNS = "elder_id, place, recorded_at"


@dataclass(frozen=True)
class ElderLocation:
    elder_id: str
    place: str  # 地名，如「台南市」
    # ⚠️ 我們**收到**的時刻，不是 GPS 定位的時刻；兩者落差為已知誤差來源（見 spec 已知限制 3）。
    recorded_at: float


class LocationError(Exception):
    """地點資料讀寫失敗。"""


class LocationStore(Protocol):
    def save(self, location: ElderLocation) -> None: ...
    def get_for_elder(self, elder_id: str) -> ElderLocation | None: ...


class PgLocationStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: LocationError(f"地點資料存取失敗：{m}"))

    def save(self, location: ElderLocation) -> None:
        self._db.execute(
            f"INSERT INTO elder_locations ({_COLUMNS}) VALUES (%s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET "
            "place = EXCLUDED.place, recorded_at = EXCLUDED.recorded_at",
            (location.elder_id, location.place, location.recorded_at),
        )

    def get_for_elder(self, elder_id: str) -> ElderLocation | None:
        row = self._db.query_one(
            f"SELECT {_COLUMNS} FROM elder_locations WHERE elder_id = %s", (elder_id,)
        )
        return ElderLocation(*row) if row else None


class FakeLocationStore:
    """LocationStore 的記憶體替身（測試用，不碰 DB）。"""

    def __init__(self) -> None:
        self._locations: dict[str, ElderLocation] = {}

    def save(self, location: ElderLocation) -> None:
        self._locations[location.elder_id] = location

    def get_for_elder(self, elder_id: str) -> ElderLocation | None:
        return self._locations.get(elder_id)
```

- [ ] **Step 4: 新增 DDL**

在 `src/kinsun/db.py` 的 `GREETING_PREFERENCES_DDL` 定義之後，加入：

```python
# 長輩目前地點（spec 2026-07-17）：一位長輩一列、覆寫式，不留行蹤軌跡。
# 只存地名不存座標。elder_id 為天然唯一鍵，直接當主鍵（比照 greeting_preferences）。
# 全新的表：既有庫沒有舊資料要升級，CREATE TABLE IF NOT EXISTS 在新舊庫結果相同，
# 故不需 ALTER 補欄位、在 ensure_schema 裡也沒有排序約束。
# 不建額外索引：主鍵即唯一查詢維度。
ELDER_LOCATIONS_DDL = (
    "CREATE TABLE IF NOT EXISTS elder_locations ("
    "elder_id TEXT PRIMARY KEY, place TEXT NOT NULL, "
    "recorded_at DOUBLE PRECISION NOT NULL);"
)
```

在 `ensure_schema` 中，於 `conn.execute(GREETING_PREFERENCES_DDL)` 之後加入一行：

```python
        conn.execute(ELDER_LOCATIONS_DDL)
```

- [ ] **Step 5: 轉出 Fake**

在 `tests/fakes.py` 的 import 區（依字母序，`FakeMedicationStore` 之前）加入：

```python
from kinsun.locations.store import FakeLocationStore as FakeLocationStore
```

- [ ] **Step 6: 執行測試，確認通過**

Run: `uv run pytest tests/test_locations_store_contract.py -q`
Expected: PASS（8 passed ＝ 4 個測試 × fake／pg 兩個 adapter；若 `KINSUN_IT` 未設則 4 passed、4 skipped）

- [ ] **Step 7: 全測試迴歸**

Run: `uv run pytest -q`
Expected: 全綠（既有測試數 ＋ 新增數）

- [ ] **Step 8: Commit**

```bash
git add src/kinsun/locations/ src/kinsun/db.py tests/fakes.py tests/test_locations_store_contract.py
git commit -F - <<'EOF'
feat(locations): 長輩目前地點的持久層——一位長輩一列，只存地名不存座標

天氣工具吃城市名，系統提示又要求不知道地點就先問——而系統從來沒有管道知道
長輩在哪，所以那句「先問清楚」每次都觸發，長輩每問一次天氣就被反問一次。
本 commit 是補上這個管道的第一步：存放處。

save 為 upsert（ON CONFLICT DO UPDATE），即「存最新一筆、覆寫式」（Leo 核定）：
一位長輩一列，新的蓋掉舊的，不累積「他每天幾點到過哪裡」的歷史軌跡——那份
資料外洩或被濾用的後果，比本專案其他任何資料都嚴重。

只存地名（如「台南市」）不存經緯度：座標在手機端就譯成地名，永不上傳。
刻意不提供 list_all——無呼叫端需要，且「一次撈出全體長輩現在在哪」正是這份
資料最不該有的介面。

elder_locations 為全新的表，既有庫沒有舊資料要升級，CREATE TABLE IF NOT EXISTS
在新舊庫結果相同，故不需 ALTER 遷移、在 ensure_schema 裡也無排序約束。

IMPACT：
- 新增 locations/ 模組與 elder_locations 表；尚無任何呼叫端，對外行為零變更。
- 合約測試釘住 Fake 與 Pg 等價，含 upsert 只留最新、跨長輩不外洩兩條核心斷言。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 2: 過期門檻設定

**Files:**
- Modify: `src/kinsun/config.py`（`Settings` 新欄位；`load_settings` 新讀取）
- Modify: `.env.example`
- Test: `tests/test_config.py`

**Interfaces:**
- Consumes: `_require_positive_int(env, key, default)`（既有）
- Produces: `Settings.location_stale_after_hours: int`（預設 `2`）

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_config.py` 的 `test_load_settings_reads_required_and_defaults` 中，於既有斷言之後加入一行：

```python
    assert settings.location_stale_after_hours == 2  # 保守門檻：只影響主動問候路徑
```

並在該測試函式之後新增：

```python
def test_location_stale_after_hours_is_overridable():
    settings = load_settings({**BASE_ENV, "LOCATION_STALE_AFTER_HOURS": "6"})
    assert settings.location_stale_after_hours == 6


def test_location_stale_after_hours_rejects_zero():
    # 0 會讓位置永遠過期、功能靜默失效；要關閉功能應該不給 App 定位權限，不是設 0。
    with pytest.raises(ConfigError):
        load_settings({**BASE_ENV, "LOCATION_STALE_AFTER_HOURS": "0"})
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_config.py -q`
Expected: FAIL — `AttributeError: 'Settings' object has no attribute 'location_stale_after_hours'`

- [ ] **Step 3: 加入設定**

在 `src/kinsun/config.py` 的 `Settings` 類別中（`rag_top_k: int` 之後）加入：

```python
    location_stale_after_hours: int
```

在 `load_settings` 的 return 中（`reflection_max_turns=...` 之後）加入：

```python
        # 位置超過幾小時就不再採信、不注入 prompt（spec 2026-07-17）。
        # 只影響主動問候路徑——長輩講話時才抓位置，對話中的位置必然是幾秒前的。
        location_stale_after_hours=_require_positive_int(env, "LOCATION_STALE_AFTER_HOURS", "2"),
```

- [ ] **Step 4: 補 .env.example**

在 `.env.example` 的 `BINDING_GATE_ENABLED=true` 那行之後、`# 佈署用` 區塊之前，加入：

```
# ────── 長輩目前地點（LOCATION_ 前綴，spec 2026-07-17） ──────
# 位置超過幾小時就不再採信、金孫不會提地名。只影響主動問候：長輩講話時才抓位置，
# 對話中的位置必然是幾秒前的、一定新鮮。2＝保守（昨晚講的話，隔天早上的問候不提地點）。
LOCATION_STALE_AFTER_HOURS=2
```

- [ ] **Step 5: 執行測試，確認通過**

Run: `uv run pytest tests/test_config.py -q`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/config.py .env.example tests/test_config.py
git commit -F - <<'EOF'
feat(config): 位置過期門檻 LOCATION_STALE_AFTER_HOURS，預設 2 小時

過期的位置比沒有位置更糟：阿公昨天回高雄了、資料庫還寫台南，金孫會很有
自信地報錯天氣。本 commit 加入採信期限，讓下一步的注入層有依據可判斷。

門檻只影響一條路徑：金孫主動開口的那一刻，讀到的是上次講話時的位置。長輩
講話時才抓位置，所以對話中的位置永遠是幾秒前的、必然新鮮，門檻對它無作用。

2 小時為 Leo 核定的保守值：昨晚八點講的話，隔天早上八點的問候不會提地點
（已過 12 小時）；早上八點講的話，十點的失聯關心還用得到。代價是主動問候
多數時候不會提地名——這正是保守的用意。

走 _require_positive_int 而非 _require_hour：0 不是合法值。設 0 會讓位置
永遠過期、功能靜默失效；要關掉功能應該是不給 App 定位權限，不是設 0——
讓它啟動即炸，比讓維運的人對著一個沒作用的功能查半天好。

IMPACT：
- 新增 LOCATION_ 前綴，加入既有前綴清單。新鍵有預設值，既有部署零改動。
- 尚無呼叫端讀取此設定，對外行為零變更。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 3: 注入層 `LocationFacts`

**Files:**
- Create: `src/kinsun/locations/facts.py`
- Test: `tests/test_locations_facts.py`

**Interfaces:**
- Consumes: `LocationStore`、`ElderLocation`（Task 1）、`kinsun.memory.models.FactSection`（既有）
- Produces: `LocationFacts(store, *, clock, stale_after_hours)`，實作既有 `FactProvider` 協定 `facts(elder_id) -> FactSection | None`

`FactSection` 的排版規則（既有 `memory/models.py`）：`title` 自帶前後換行，`items` 每行會自動加上 `- ` 前綴。

- [ ] **Step 1: 寫失敗的測試**

建立 `tests/test_locations_facts.py`：

```python
"""LocationFacts：把長輩目前地點注入情境的一段，過期則完全不注入。"""

from datetime import datetime, timedelta, timezone

from kinsun.locations.facts import LocationFacts
from kinsun.locations.store import ElderLocation, FakeLocationStore

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 17, 12, 0, tzinfo=TPE)


def _facts(location: ElderLocation | None, *, stale_after_hours: int = 2) -> LocationFacts:
    store = FakeLocationStore()
    if location is not None:
        store.save(location)
    return LocationFacts(store, clock=lambda: NOW, stale_after_hours=stale_after_hours)


def _at(*, minutes_ago: float) -> float:
    return (NOW - timedelta(minutes=minutes_ago)).timestamp()


def test_returns_none_when_no_location_recorded():
    assert _facts(None).facts("e1") is None


def test_injects_place_with_relative_time():
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=3))).facts("e1")
    assert section is not None
    assert section.items == ["3 分鐘前在台南市"]


def test_title_frames_location_as_hint_not_answer():
    # ⚠️ anchoring 防線：措辭是功能本體，不是文案。刪掉「參考」等於讓模型
    # 把所在地當答案，正是本設計要防的事。
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=3))).facts("e1")
    assert section is not None
    assert "參考" in section.title
    assert "不一定" in section.title


def test_returns_none_when_stale():
    assert _facts(ElderLocation("e1", "台南市", _at(minutes_ago=121))).facts("e1") is None


def test_boundary_exactly_at_threshold_is_still_trusted():
    # 剛好 2 小時仍採信；門檻是「超過才丟」，不是「到了就丟」。
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=120))).facts("e1")
    assert section is not None
    assert section.items == ["2 小時前在台南市"]


def test_just_now_reads_naturally():
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=0.5))).facts("e1")
    assert section is not None
    assert section.items == ["剛剛在台南市"]


def test_over_an_hour_reads_in_hours():
    section = _facts(ElderLocation("e1", "高雄市", _at(minutes_ago=90))).facts("e1")
    assert section is not None
    assert section.items == ["1 小時前在高雄市"]


def test_future_timestamp_is_treated_as_just_now():
    # 手機時鐘快於伺服器時，recorded_at 可能落在未來。負數的「-1 分鐘前」是
    # 胡說八道；退成「剛剛」是誠實的近似（誤差必然小於時鐘偏差本身）。
    section = _facts(ElderLocation("e1", "台南市", _at(minutes_ago=-5))).facts("e1")
    assert section is not None
    assert section.items == ["剛剛在台南市"]
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_locations_facts.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.locations.facts'`

- [ ] **Step 3: 實作注入層**

建立 `src/kinsun/locations/facts.py`：

```python
"""地點事實提供者：把長輩目前地點注入情境的一段（FactSection）。

⚠️ 本檔的措辭是功能本體，不是文案。

自動注入的結構性弱點是 anchoring：位置每輪無條件進 prompt，模型看到「他在
台南」，問到天氣時就容易順手查台南——而長輩問的地點不必然是他站著的地點
（「等下要去哪吃飯」）。工具方案沒有這個問題（模型得主動要），我們用注入
就得靠措辭扛。故 title 必須把「參考、不是答案」寫死在字面上。

見 spec `2026-07-17-長輩目前地點-design.md` 的「⚠️ anchoring」節。
"""

from __future__ import annotations

from collections.abc import Callable
from datetime import datetime

from kinsun.locations.store import LocationStore
from kinsun.memory.models import FactSection

_TITLE = "\n這位長者的手機回報的目前位置（僅供參考——他問到的地點不一定是這裡）：\n"

_SECONDS_PER_MINUTE = 60
_SECONDS_PER_HOUR = 3600


def _relative(elapsed_seconds: float) -> str:
    """把「多久以前」講成長輩聽得懂的白話。

    模型對 epoch 秒無感，對「3 分鐘前」有感：過期硬門檻擋掉最糟的情況，
    相對時間讓模型在門檻內的邊界情況上仍有判斷依據。

    未滿一分鐘與**未來**（手機時鐘快於伺服器）都講「剛剛」：負數的「-5 分鐘前」
    是胡說八道，退成「剛剛」是誠實的近似。
    """
    if elapsed_seconds < _SECONDS_PER_MINUTE:
        return "剛剛"
    if elapsed_seconds < _SECONDS_PER_HOUR:
        return f"{int(elapsed_seconds // _SECONDS_PER_MINUTE)} 分鐘前"
    return f"{int(elapsed_seconds // _SECONDS_PER_HOUR)} 小時前"


class LocationFacts:
    """facts(elder_id) -> FactSection | None（無資料或已過期回 None）。"""

    def __init__(
        self,
        store: LocationStore,
        *,
        clock: Callable[[], datetime],
        stale_after_hours: int,
    ) -> None:
        self._store = store
        self._clock = clock
        self._stale_after_seconds = stale_after_hours * _SECONDS_PER_HOUR

    def facts(self, elder_id: str) -> FactSection | None:
        location = self._store.get_for_elder(elder_id)
        if location is None:
            return None
        elapsed = self._clock().timestamp() - location.recorded_at
        # 超過門檻才丟，剛好等於門檻仍採信。過期的位置比沒有位置更糟——
        # 與其讓金孫很有自信地報錯天氣，不如讓它照舊開口問。
        if elapsed > self._stale_after_seconds:
            return None
        return FactSection(_TITLE, [f"{_relative(elapsed)}在{location.place}"])
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_locations_facts.py -q`
Expected: PASS（8 passed）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/locations/facts.py tests/test_locations_facts.py
git commit -F - <<'EOF'
feat(locations): 地點注入層——措辭把位置框成線索，而非答案

本 commit 的措辭是功能本體，不是文案潤飾。

自動注入有個結構性弱點：anchoring。位置每輪無條件進 prompt，模型看到
「他在台南」，問到天氣時就容易順手查台南——但長輩問的地點不必然是他站著
的地點（「等下要去哪吃飯」）。工具方案沒有這個問題（模型得主動開口要），
我們選了注入，就得靠 title 的措辭扛：「僅供參考——他問到的地點不一定是
這裡」把線索與答案的分野寫死在字面上。測試釘住「參考」與「不一定」兩個
詞，刪掉任何一個都會紅。

相對時間（「3 分鐘前」）而非絕對時間戳：模型對 epoch 秒無感，對「3 分鐘前」
有感。過期硬門檻擋掉最糟的情況，相對時間讓模型在門檻內的邊界上仍有判斷依據。

未來時刻退成「剛剛」：手機時鐘快於伺服器時 recorded_at 會落在未來，而
「-5 分鐘前」是胡說八道；退成「剛剛」的誤差必然小於時鐘偏差本身，是誠實
的近似。邊界取「超過才丟」而非「到了就丟」，剛好 2 小時仍採信。

IMPACT：
- 尚未接進 composition，對外行為零變更。
- ⚠️ 若日後實測發現措辭扛不住 anchoring，正確的反應是回頭重估工具方案
  （並一併解決 elder_id 進工具層），而不是往 prompt 疊更多句提示詞。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 4: 系統提示改寫

**Files:**
- Modify: `src/kinsun/agent.py`（`SYSTEM_PROMPT` 中的地點那句）
- Test: `tests/test_agent.py`

**Interfaces:**
- Consumes: 無（純字串常數）
- Produces: 無新介面

⚠️ **這個 Task 的測試只能是回歸防線，不是行為驗證。** 單元測試用的是腳本化的假 LLM，它不會推理，斷言「模型不會拿所在地去查別處的天氣」在假替身上是做不到的——那需要真的 Gemini。故本 Task 的測試只釘住「這三句話還在 prompt 裡」，防止日後有人刪改；**anchoring 的真正驗證是 Task 7 的人工複驗步驟 3**。spec 的「測試策略」表把這條列在 `tests/test_agent.py` 下，措辭過度承諾，實作時一併更正（見 Step 5）。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_agent.py` 檔末加入：

```python
def test_system_prompt_frames_location_as_hint_not_answer():
    """⚠️ 回歸防線，非行為驗證。

    假 LLM 不會推理，「模型不拿所在地去查別處」在此測不出來——那需要真的
    Gemini，屬人工複驗（見 plan Task 7）。本測試只確保這三句話沒被刪改：
    第一句消滅「每次都反問」，第二句擋 anchoring，第三句保住沒位置時的行為。
    """
    assert "他明確在問所在地的天氣，就直接用那個地點，不要多問" in SYSTEM_PROMPT
    assert "不可拿他目前的位置去查" in SYSTEM_PROMPT
    assert "情境沒有附位置時，一律先問" in SYSTEM_PROMPT
```

確認檔案頂端的 import 含 `SYSTEM_PROMPT`：

```python
from kinsun.agent import SYSTEM_PROMPT
```

（若既有 import 行為 `from kinsun.agent import CareAgent`，改成 `from kinsun.agent import SYSTEM_PROMPT, CareAgent`。）

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_agent.py::test_system_prompt_frames_location_as_hint_not_answer -q`
Expected: FAIL — `AssertionError`

- [ ] **Step 3: 改寫系統提示**

在 `src/kinsun/agent.py` 的 `SYSTEM_PROMPT` 中，將這一行：

```python
    "查天氣前若不知道長輩人在哪個城市，先親口問清楚，不要自己猜地點。"
```

替換為：

```python
    # 地點三句（spec 2026-07-17）：三種情形各一句，缺一不可。第一句消滅「每次都反問」
    # （本功能的目的），第二句擋 anchoring（本功能最大的坑——位置每輪無條件進 prompt，
    # 模型容易看到「他在台南」就順手查台南），第三句保住沒有位置時的現行行為。
    "情境有時會附上長輩手機回報的目前位置。那是參考，不是答案——他問到的地點不一定是他人在的地方。"
    "他明確在問所在地的天氣，就直接用那個地點，不要多問；"
    "他提到要去別的地方（例如等下要去哪裡吃飯），就問清楚是哪裡，不可拿他目前的位置去查。"
    "情境沒有附位置時，一律先問，不要自己猜。"
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_agent.py -q`
Expected: PASS（既有 agent 測試全綠 ＋ 新增 1）

- [ ] **Step 5: 更正 spec 的過度承諾**

在 `docs/superpowers/specs/2026-07-17-長輩目前地點-design.md` 的「測試策略（TDD）」表中，將 `tests/test_agent.py` 那列的覆蓋描述由：

> ⚠️ anchoring 防線：長輩問別處的天氣時，不拿所在地去查

改為：

> ⚠️ anchoring 的**回歸防線**：釘住地點三句仍在 `SYSTEM_PROMPT` 中。行為本身測不出來——假 LLM 不會推理——故 anchoring 的真正驗證在下方人工複驗步驟 3

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/agent.py tests/test_agent.py docs/superpowers/specs/2026-07-17-長輩目前地點-design.md
git commit -F - <<'EOF'
feat(agent): 系統提示改為「位置是參考不是答案」，並更正 spec 的過度承諾

原本那句「查天氣前若不知道長輩人在哪個城市，先親口問清楚」的實際效果是
每次都觸發——系統從來沒有管道知道長輩在哪，所以條件永遠成立。位置管道
接上後，這句話要改成能分辨三種情形。

三句話缺一不可：他明確問所在地就直接用（消滅每次反問，本功能的目的）；
他提到要去別的地方就問清楚、不可拿目前位置去查（擋 anchoring，本功能最大
的坑）；沒有位置資訊時一律先問（保住現行行為，LINE 通道永遠走這條）。

一併更正 spec「測試策略」表對本檔的過度承諾。原文寫測試會釘住「長輩問別處
的天氣時，不拿所在地去查」——那是行為斷言，而單元測試用的是腳本化的假 LLM，
它不會推理，這條在假替身上做不到。本 commit 的測試只釘住三句話還在 prompt
裡，是回歸防線而非行為驗證；anchoring 的真正驗證是 App 上線後的人工複驗。
與其留一條測不出東西卻讓人以為測過了的斷言，不如把界線寫清楚。

IMPACT：
- 對外行為變更：僅在情境附上位置時生效；情境無位置時（LINE 全部、App 未
  授權定位）行為與現在完全相同。
- 此時尚無任何路徑會注入位置（composition 未接線），故實際行為仍未改變。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 5: `/turns` 收地名並寫入

**Files:**
- Modify: `src/kinsun/channels/app/turns.py`
- Test: `tests/test_channels_app_turns.py`

**Interfaces:**
- Consumes: `LocationStore`、`ElderLocation`（Task 1）
- Produces: `create_app_turns_router(..., locations=None, clock=None)` 兩個新的關鍵字參數；`POST /turns` 新增 query param `location: str = ""`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_channels_app_turns.py` 檔末加入（`FakeLocationStore` 需加進頂端的 `tests.fakes` import）：

```python
def test_location_query_param_is_saved_before_dispatch():
    locations = FakeLocationStore()
    client = _client(locations=locations)
    resp = client.post(
        "/api/app/turns",
        params={"location": "台南市"},
        content=b"audio-bytes",
        headers={"Content-Type": "audio/m4a", "Authorization": f"Bearer {_TOKEN}"},
    )
    assert resp.status_code == 201
    saved = locations.get_for_elder(_ELDER_ID)
    assert saved is not None
    assert saved.place == "台南市"
    assert saved.recorded_at == NOW.timestamp()


def test_missing_location_does_not_write():
    locations = FakeLocationStore()
    client = _client(locations=locations)
    client.post(
        "/api/app/turns",
        content=b"audio-bytes",
        headers={"Content-Type": "audio/m4a", "Authorization": f"Bearer {_TOKEN}"},
    )
    assert locations.get_for_elder(_ELDER_ID) is None


def test_blank_location_does_not_clear_existing():
    # 長輩這次沒授權／室內收不到 → 不該把上次的位置抹掉。空字串＝「這輪沒有位置」，
    # 不是「他不在任何地方」。
    locations = FakeLocationStore()
    locations.save(ElderLocation(_ELDER_ID, "高雄市", 1752739200.0))
    client = _client(locations=locations)
    client.post(
        "/api/app/turns",
        params={"location": "   "},
        content=b"audio-bytes",
        headers={"Content-Type": "audio/m4a", "Authorization": f"Bearer {_TOKEN}"},
    )
    assert locations.get_for_elder(_ELDER_ID) == ElderLocation(_ELDER_ID, "高雄市", 1752739200.0)


def test_location_write_failure_does_not_break_the_turn():
    # 位置是加分項，不是對話的前提。比照工具失敗的既有政策：記 log、對話照走。
    class _ExplodingStore:
        def save(self, location):
            raise RuntimeError("boom")

        def get_for_elder(self, elder_id):
            return None

    client = _client(locations=_ExplodingStore())
    resp = client.post(
        "/api/app/turns",
        params={"location": "台南市"},
        content=b"audio-bytes",
        headers={"Content-Type": "audio/m4a", "Authorization": f"Bearer {_TOKEN}"},
    )
    assert resp.status_code == 201
```

⚠️ 上述測試用到 `_client(locations=...)`、`_TOKEN`、`_ELDER_ID`、`NOW`。既有測試檔已有等價的建置流程但名稱未必相同——**實作時先讀完 `tests/test_channels_app_turns.py`**，將既有的 client 建置抽成接受 `locations` 參數的 helper，沿用檔案既有的常數名，不要新造平行的一套。

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_channels_app_turns.py -q`
Expected: FAIL — `TypeError: create_app_turns_router() got an unexpected keyword argument 'locations'`

- [ ] **Step 3: 實作**

在 `src/kinsun/channels/app/turns.py`：

頂端 import 區加入：

```python
from datetime import datetime

from kinsun.locations.store import ElderLocation
```

`create_app_turns_router` 簽章加入兩個關鍵字參數（放在 `max_audio_bytes` 之前）：

```python
    locations=None,
    clock: Callable[[], datetime] | None = None,
```

在 `make_id = new_id or (lambda: uuid.uuid4().hex)` 之後加入：

```python
    now = clock or (lambda: datetime.now(UTC))

    def _save_location(elder_id: str, place: str) -> None:
        """記下長輩這輪回報的地點。⚠️ 只收地名，不收也不記座標。

        空字串／純空白＝「這輪沒有位置」（未授權、室內收不到），**不是**「他不在
        任何地方」——故不寫入也不清空既有資料。
        """
        place = place.strip()
        if locations is None or not place:
            return
        try:
            locations.save(ElderLocation(elder_id, place, now().timestamp()))
        except Exception:  # noqa: BLE001 - 位置是加分項，寫入失敗不可中斷對話
            logger.warning("長輩地點寫入失敗")
```

`UTC` 需自 `datetime` 匯入：`from datetime import UTC, datetime`。

`create_turn` 的簽章加入 query param：

```python
    async def create_turn(
        request: Request, location: str = "", elder_id: str = Depends(current_elder)
    ) -> dict:
```

在 `collector = _TurnCollector()` 之前、`audio` 讀取之後，加入呼叫：

```python
        # ⚠️ 必須排在 dispatch 之前：長輩這句話問的就是天氣時，這一輪就得用得到；
        # 排在後面等於永遠慢一輪。
        _save_location(elder_id, location)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_channels_app_turns.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/channels/app/turns.py tests/test_channels_app_turns.py
git commit -F - <<'EOF'
feat(channels): /turns 收長輩回報的地名，寫入排在 dispatch 之前

位置管道的最後一段：App 把地名送上來。

走 query param 而非 header 或 multipart。/turns 收的是裸音檔 body
（await request.body()，且以 content_type 擋非音訊回 415），body 裡沒有
地方放地名，只剩 header 或 query param 兩條路。header 預設 latin-1，
「台南市」得兩端各自記得 percent-encode；query param 由 FastAPI 原生解
UTF-8。改 multipart 要動 415 檢查與 App 既有上傳程式碼，代價最大、收益為零。

寫入必須排在 dispatch 之前：長輩這句話問的就是天氣時，這一輪就得用得到，
排在後面等於永遠慢一輪——而「慢一輪」在對講機上的表現就是他問第一次還是
被反問，功能等於沒做。

空字串／純空白不寫入也不清空。它的語意是「這輪沒有位置」（未授權、室內
收不到快取），不是「他不在任何地方」；把上次的位置抹掉沒有任何好處。

寫入失敗記 log、對話照走：位置是加分項，不是對話的前提。比照工具 dispatch
永不拋的既有政策。

IMPACT：
- locations 未注入時（scheduler 端、既有測試）行為與現在完全相同，參數皆有預設。
- 對外行為變更：App 帶 location 時會寫入 elder_locations。此時 composition
  尚未接線，實務上仍無呼叫端傳入 locations，故正式環境行為未變。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 6: 接線 `composition.py`

**Files:**
- Modify: `src/kinsun/composition.py`
- Modify: `src/kinsun/web/app.py`（把 `Core.locations` 傳進 `create_app_turns_router`）
- Test: `tests/test_composition.py`

**Interfaces:**
- Consumes: 全部前述 Task
- Produces: `Core.locations: PgLocationStore`

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_composition.py` 檔末加入：

```python
def test_core_wires_location_facts_into_session():
    core = _core()  # 沿用本檔既有的組裝 helper
    providers = core.agent._session._facts
    assert any(type(p).__name__ == "LocationFacts" for p in providers)
```

⚠️ **先讀完 `tests/test_composition.py`**，沿用它既有的 `Core` 建置 helper 與斷言風格，不要新造一套。

- [ ] **Step 2: 執行測試，確認失敗**

Run: `uv run pytest tests/test_composition.py -q`
Expected: FAIL — `AssertionError`

- [ ] **Step 3: 接線**

在 `src/kinsun/composition.py`：

import 區加入（依既有字母序）：

```python
from kinsun.locations.facts import LocationFacts
from kinsun.locations.store import PgLocationStore
```

`Core` dataclass 加入欄位（`strategies` 之後、`agent` 之前）：

```python
    locations: PgLocationStore
```

在 `assemble_core` 中，`strategies = PgStrategyStore(...)` 之後加入：

```python
    locations = PgLocationStore(db)
```

`SessionMemory` 的 `facts` 清單末尾加入：

```python
            # 地點注入（spec 2026-07-17）：位置是線索不是答案，措辭見 locations/facts.py。
            LocationFacts(
                locations,
                clock=clock,
                stale_after_hours=settings.location_stale_after_hours,
            ),
```

`Core(...)` 的建構加入：

```python
        locations=locations,
```

在 `src/kinsun/web/app.py` 中，`create_app_turns_router(...)` 的呼叫加入：

```python
        locations=core.locations,
        clock=clock,
```

⚠️ **先讀 `src/kinsun/web/app.py`** 確認該處 `clock` 的既有名稱與取得方式，沿用之。

- [ ] **Step 4: 執行測試，確認通過**

Run: `uv run pytest tests/test_composition.py -q`
Expected: PASS

- [ ] **Step 5: 全測試迴歸**

Run: `uv run pytest -q`
Expected: 全綠

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/composition.py src/kinsun/web/app.py tests/test_composition.py
git commit -F - <<'EOF'
feat(composition): 接上地點管道，後端整條路通了

把前四個 commit 的零件接成一條線：/turns 寫入 → elder_locations →
LocationFacts → system prompt → 金孫判斷。本 commit 之後，長輩在 App 允許
定位並問天氣，就不會再被反問「您人在哪個城市」。

LocationFacts 排在 facts 清單末尾（用藥、回診、策略之後）：注入順序即
prompt 中的段落順序，位置是這幾段裡最不重要的一段，放最後。

locations 收進 Core 而非讓 app.py 自己 new：clock 接線必須與 LocationFacts
的 clock 同源，否則寫入時刻與過期判斷會用到兩個不同的時鐘——這種錯誤在
測試裡看不出來（兩邊都是真實時鐘、差幾毫秒），上線後也不會炸，只會讓門檻
悄悄偏移。庚-44／A-57 已為 risk_events 與 summaries 踩過同一個坑。

IMPACT：
- 對外行為變更：App 已授權定位的長輩，問天氣不再被反問所在地。
- LINE 通道完全不受影響（沒有位置來源，走「情境沒有附位置時一律先問」）。
- scheduler 端的 CareAgent 同樣會注入位置，但 proactive 走 generate 而非
  工具迴圈、無天氣工具可用，位置至多影響措辭——見 spec 已知限制 2。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

### Task 7: App 端取位與人工驗證

**Files:**
- Modify: `app/package.json`（新增 `expo-location`）
- Modify: `app/app.json`（iOS／Android 定位權限說明）
- Modify: `app/src/app/elder/talk.tsx`
- Create: `app/src/lib/location.ts`

**Interfaces:**
- Consumes: `POST /turns` 的 `location` query param（Task 5）
- Produces: `currentPlace(): Promise<string>`（取不到一律回 `""`，永不拋）

⚠️ **App 端無測試框架**（`app/` 與 `frontend/` 皆無 jest／vitest、無 `.test.*` 檔、`package.json` 無 test script），本 Task 只能人工驗證。為 App 建測試基建是獨立任務，不併入本次。

- [ ] **Step 1: 安裝依賴**

```bash
cd app && npx expo install expo-location
```

用 `expo install` 而非 `npm install`：它會挑與本專案 Expo SDK 相容的版本。

- [ ] **Step 2: 宣告權限用途**

在 `app/app.json` 中，加入 `expo-location` plugin 與權限說明字串（iOS 上架會審這段文字）：

```json
    "plugins": [
      [
        "expo-location",
        {
          "locationAlwaysAndWhenInUsePermission": "金孫需要知道您在哪個城市，才能告訴您當地的天氣。"
        }
      ]
    ]
```

⚠️ `app.json` 已有 `plugins` 陣列時，把上述項目**加進**既有陣列，不要覆蓋。只申請前景權限——不要加 `isAndroidBackgroundLocationEnabled`。

- [ ] **Step 3: 實作取位模組**

建立 `app/src/lib/location.ts`：

```typescript
/**
 * 取長輩目前所在的地名（如「台南市」）。
 *
 * ⚠️ 經緯度永不離開手機：座標在本檔就譯成地名，只有地名字串會上傳。
 *
 * 一切失敗（未授權、無快取位置、譯不出地名）都回空字串，由呼叫端當成
 * 「這輪沒有位置」——金孫會照舊開口問，功能靜默降級，絕不阻擋對講機。
 */

import * as Location from "expo-location";

export async function currentPlace(): Promise<string> {
  try {
    const { status } = await Location.requestForegroundPermissionsAsync();
    if (status !== Location.PermissionStatus.GRANTED) {
      return "";
    }
    // 用 getLastKnownPositionAsync 而非 getCurrentPositionAsync：官方文件載明後者
    // 可能要等好幾秒，而長輩按下對講機是要馬上講話的。快取位置對城市級精度綽綽有餘。
    const position = await Location.getLastKnownPositionAsync();
    if (position === null) {
      return "";
    }
    const [address] = await Location.reverseGeocodeAsync(position.coords);
    if (!address) {
      return "";
    }
    // 各家作業系統的 city 粒度不一（可能回「台南市」也可能回「東區」）；
    // 退階順序是緩解，不是保證。見 spec 已知限制 4。
    return address.city ?? address.subregion ?? address.region ?? "";
  } catch {
    return "";
  }
}
```

- [ ] **Step 4: 接進對講機**

⚠️ **先讀完 `app/src/app/elder/talk.tsx` 與 `app/src/lib/api.ts`**，確認送出錄音的既有函式名稱與簽章，再依既有風格接線。要達成的行為：

1. 送出錄音前呼叫 `currentPlace()`。
2. 回傳非空字串時，把它當 `location` query param 附加到 `POST /turns`。
3. 回傳空字串時，**不帶** query param（不要送 `?location=`）。
4. `currentPlace()` 永不拋，故不需 try/catch 包它；但它的耗時不可阻塞錄音 UI 的回饋。

- [ ] **Step 5: 人工驗證（三條，缺一不可）**

啟動 App 與後端，逐條確認：

1. **允許定位 → 問「今天天氣如何」** → 金孫直接回答所在地天氣，**不反問**「您人在哪」。
2. **拒絕定位 → 問「今天天氣如何」** → 金孫照舊反問所在地，**不報錯、不卡住**（靜默降級）。
3. ⚠️ **允許定位 → 說「等下要去別的地方吃飯，那邊天氣如何」** → 金孫**會問**是哪裡，**不會**拿目前所在地去查。

第 3 條是 anchoring 防線的真正驗證——單元測試做不到（假 LLM 不會推理）。**若第 3 條失敗**，正確的反應是回頭重估工具方案（並一併解決 `elder_id` 進工具層），而不是往 prompt 疊更多句提示詞。

- [ ] **Step 6: Commit**

```bash
git add app/package.json app/package-lock.json app/app.json app/src/lib/location.ts app/src/app/elder/talk.tsx
git commit -F - <<'EOF'
feat(app): 對講機送出錄音時附上地名，經緯度不離開手機

管道的源頭：長輩按下對講機時取一次位置。

在手機端就把座標譯成地名（reverseGeocodeAsync），只上傳「台南市」五個字。
經緯度永不離開手機、不進資料庫——這讓後端少一個外部反查 API 要串，也讓
外洩情境下的損害從「阿公的精確座標」降為「阿公在台南」。

用 getLastKnownPositionAsync 而非 getCurrentPositionAsync：官方文件載明後者
可能要等好幾秒，而長輩按下對講機是要馬上講話的，等不起。快取位置對城市級
精度綽綽有餘——天氣查的是整個城市，他在哪一區不影響結果。

只申請前景權限，不申請背景定位：背景要長輩多點一次同意、iOS 上架要交代
理由、很耗電，而且最接近「監控長輩」的觀感。長輩沒在講話時，我們不需要
也不想知道他在哪。

一切失敗（未授權、無快取位置、譯不出地名）都回空字串，呼叫端不帶 query
param，金孫照舊開口問——功能靜默降級，絕不阻擋對講機。

IMPACT：
- 新增依賴 expo-location（Expo 官方套件，僅 Android／iOS）。
- app.json 新增前景定位權限說明字串，iOS 上架會審這段文字。
- ⚠️ App 端無測試框架，本 commit 經三條人工複驗（允許／拒絕／問別處），
  其中「問別處不拿所在地去查」是 anchoring 防線的唯一真正驗證——單元測試
  做不到，假 LLM 不會推理。

Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>
EOF
```

---

## Self-Review 紀錄

- **Spec 覆蓋**：元件設計 1→Task 7、2→Task 5、3→Task 1、4→Task 3、5→Task 4；錯誤處理表→Task 5 Step 3、Task 7 Step 3；環境變數→Task 2；資料庫遷移→Task 1 Step 4；測試策略→各 Task；App 人工驗證→Task 7 Step 5。無遺漏。
- **已修正的不一致**：spec「測試策略」表對 `tests/test_agent.py` 過度承諾（宣稱測得出 anchoring 行為，實際上假 LLM 做不到）→ Task 4 Step 5 一併更正 spec。
- **型別一致性**：`ElderLocation(elder_id, place, recorded_at)` 三欄位於 Task 1／3／5 用法一致；`LocationStore` 只有 `save`／`get_for_elder` 兩個方法，各 Task 未用到未定義的方法。
