# 真模型接 DGX 實作計畫（真 ASR 硬化＋CosyVoice 3 TTS 全鏈路）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓長輩傳 LINE 語音 → DGX 真 ASR 辨識 → 回覆經 DGX 真 TTS（CosyVoice 3）合成 → 以 LINE 語音訊息＋文字泡泡回覆，任一段失敗都退化純文字、對話不中斷。

**Architecture:** 應用層只當 client（標準庫 urllib 呼叫 DGX 服務、Supabase Storage REST 上傳音檔），重模型與轉檔全在 DGX。TTS 走 `DgxTtsClient` → `TtsResult(audio, duration_ms)` → `SupabaseAudioPublisher` 取公開 URL → `VoiceReplyDelivery` 發 LINE 語音。ASR 服務修阻塞 event loop 的 bug 並補健康檢查/併發。

**Tech Stack:** Python 3.12、FastAPI、line-bot-sdk v3（`AudioMessage`）、urllib（標準庫）、pytest、CosyVoice 3（DGX 端）、Breeze-ASR-26（DGX 端）、ffmpeg（DGX 端 wav→m4a）。

## Global Constraints

- 一律台灣繁體中文（文件/註解/commit）。
- 應用層 HTTP 一律用標準庫 `urllib`，**不新增** supabase SDK／httpx／requests 依賴。
- 應用層須跨 Windows / macOS / DGX 三平台可跑；ffmpeg 與重模型只在 DGX，不得成為應用層依賴。
- 全程 fail-safe：記憶/LLM/ASR/TTS/上傳/LINE 任一失敗都退化、不中斷對話、不讓 webhook 回 500。
- 依賴注入時鐘（`clock`）與亂數（`new_id`）以利 TDD，不在被測程式內呼叫 `datetime.now`／`uuid`。
- `services/` 不進開發機測試套件（既有原則）；服務端行為以 DGX 實機清單驗證。
- commit 訊息結尾加 `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`；只在 `Leo` 分支工作、不自動 push。
- 每個 config 新欄位的預設值一律沿用 spec §8 表格：`TTS_BACKEND=bubble`、`TTS_TIMEOUT_SECONDS=30`、`TTS_REPLY_TEXT=true`、`AUDIO_BUCKET=tts-audio`、`AUDIO_RETENTION_DAYS=2`、`AUDIO_UPLOAD_TIMEOUT_SECONDS=10`。

---

## 檔案結構（新增／修改）

| 檔案 | 職責 | 動作 |
|------|------|------|
| `src/kinsun/speech/tts.py` | `TtsResult.duration_ms`、`TTSError`、`DgxTtsClient`、`build_tts_client` | 修改 |
| `src/kinsun/config.py` | 新增 TTS／音檔設定欄位 | 修改 |
| `src/kinsun/audio/__init__.py` | 新套件 | 新增 |
| `src/kinsun/audio/publisher.py` | `AudioPublisher`、`SupabaseAudioPublisher`、`AudioPublishError`、`build_audio_publisher`、`build_cleanup`（刪過期資料夾） | 新增 |
| `src/kinsun/channels/line/messenger.py` | `LineMessenger.reply_voice`（Protocol＋真實作） | 修改 |
| `src/kinsun/channels/line/channel.py` | 綁定 `reply_voice` 到 reply_token | 修改 |
| `src/kinsun/channels/inbound.py` | `InboundMessage.reply_voice` 欄位、`VoiceReplyDelivery`、`dispatch(voice=)` | 修改 |
| `src/kinsun/pipeline.py` | TTS fail-safe（`TTSError`→純文字） | 修改 |
| `src/kinsun/channels/line/webhook.py` | 把 `voice` 串進 `_handle_events`→`dispatch` | 修改 |
| `src/kinsun/app.py` | 組裝 `build_tts_client`／publisher／`VoiceReplyDelivery` | 修改 |
| `src/kinsun/scheduler/worker.py` | `TTS_BACKEND=dgx` 時註冊音檔清理每日 job | 修改 |
| `services/tts/server.py` | CosyVoice 3 載入＋合成＋wav→m4a＋healthz＋併發 | 修改（DGX） |
| `services/asr/server.py` | threadpool＋semaphore 修阻塞、healthz、預載、超載 503 | 修改（DGX） |
| `services/{asr,tts}/requirements.txt`、`services/{asr,tts}/README.md`、`README.md`、`progress.md` | 文件同步 | 修改 |
| `tests/test_tts.py`、`tests/test_config.py`、`tests/test_audio_publisher.py`（新）、`tests/test_inbound.py`、`tests/test_line_channel.py`、`tests/test_pipeline.py`、`tests/test_webhook.py` | 測試 | 新增／修改 |

---

## Task 1: TTS client（TtsResult.duration_ms、TTSError、DgxTtsClient、build_tts_client）

**Files:**
- Modify: `src/kinsun/speech/tts.py`
- Test: `tests/test_tts.py`

**Interfaces:**
- Consumes: `kinsun.config.Settings`（Task 2 才加新欄位；本 Task 只讀 `tts_backend`、`tts_endpoint`、`tts_timeout_seconds`，測試用 stub settings 物件，不依賴 Task 2）。
- Produces:
  - `TtsResult(text: str, audio: bytes | None = None, duration_ms: int = 0)`
  - `class TTSError(Exception)`
  - `class DgxTtsClient: __init__(self, endpoint: str, timeout: float); synthesize(self, text: str) -> TtsResult`
  - `def build_tts_client(settings) -> TTSClient`

- [ ] **Step 1: 寫失敗測試（DgxTtsClient 成功 / 缺 header / HTTP 錯誤 / build 開關）**

在 `tests/test_tts.py` 末尾追加（保留既有 `test_text_bubble_returns_text_without_audio`）：

```python
import io
import urllib.error

import pytest

from kinsun.speech.tts import DgxTtsClient, TTSError, build_tts_client


class _FakeResp:
    def __init__(self, body: bytes, duration_ms: str | None):
        self._body = body
        self.headers = {} if duration_ms is None else {"X-Duration-Ms": duration_ms}

    def read(self):
        return self._body

    def __enter__(self):
        return self

    def __exit__(self, *a):
        return False


class _StubSettings:
    def __init__(self, backend="bubble", endpoint="", timeout=30.0):
        self.tts_backend = backend
        self.tts_endpoint = endpoint
        self.tts_timeout_seconds = timeout


def test_dgx_tts_returns_audio_and_duration(monkeypatch):
    captured = {}

    def fake_urlopen(req, timeout=None):
        captured["url"] = req.full_url
        captured["body"] = req.data
        captured["ctype"] = req.headers.get("Content-type")
        return _FakeResp(b"AUDIOBYTES", "1234")

    monkeypatch.setattr("urllib.request.urlopen", fake_urlopen)
    client = DgxTtsClient("http://dgx:8002/synthesize", 30.0)
    result = client.synthesize("阿公您好")
    assert result.text == "阿公您好"
    assert result.audio == b"AUDIOBYTES"
    assert result.duration_ms == 1234
    assert captured["url"] == "http://dgx:8002/synthesize"
    assert b"阿公" in captured["body"]  # JSON body 帶 text
    assert captured["ctype"] == "application/json"


def test_dgx_tts_missing_duration_header_raises(monkeypatch):
    monkeypatch.setattr(
        "urllib.request.urlopen", lambda req, timeout=None: _FakeResp(b"A", None)
    )
    with pytest.raises(TTSError):
        DgxTtsClient("http://dgx:8002/synthesize", 30.0).synthesize("嗨")


def test_dgx_tts_http_error_raises(monkeypatch):
    def boom(req, timeout=None):
        raise urllib.error.URLError("connection refused")

    monkeypatch.setattr("urllib.request.urlopen", boom)
    with pytest.raises(TTSError):
        DgxTtsClient("http://dgx:8002/synthesize", 30.0).synthesize("嗨")


def test_build_returns_bubble_by_default():
    from kinsun.speech.tts import TextBubbleTts

    assert isinstance(build_tts_client(_StubSettings()), TextBubbleTts)


def test_build_dgx_without_endpoint_raises():
    with pytest.raises(TTSError):
        build_tts_client(_StubSettings(backend="dgx", endpoint=""))


def test_build_dgx_returns_client():
    client = build_tts_client(_StubSettings(backend="dgx", endpoint="http://dgx:8002/synthesize"))
    assert isinstance(client, DgxTtsClient)
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_tts.py -q`
Expected: FAIL（`ImportError: cannot import name 'DgxTtsClient'`）

- [ ] **Step 3: 實作 tts.py**

把 `src/kinsun/speech/tts.py` 改為：

```python
"""TTS 介面與實作。dev 用文字泡泡 placeholder；正式呼叫 DGX 上的 services/tts。"""

from __future__ import annotations

import json
import urllib.error
import urllib.request
from dataclasses import dataclass
from typing import Protocol


class TTSError(Exception):
    """語音合成失敗。"""


@dataclass(frozen=True)
class TtsResult:
    text: str
    audio: bytes | None = None
    duration_ms: int = 0


class TTSClient(Protocol):
    def synthesize(self, text: str) -> TtsResult: ...


class TextBubbleTts:
    """placeholder：回文字泡泡，不產音檔（audio=None）。"""

    def synthesize(self, text: str) -> TtsResult:
        return TtsResult(text=text, audio=None)


class DgxTtsClient:
    """正式：POST {"text"} 到 DGX 上的 TTS 服務，取回 m4a bytes 與時長。"""

    def __init__(self, endpoint: str, timeout: float) -> None:
        self._endpoint = endpoint
        self._timeout = timeout

    def synthesize(self, text: str) -> TtsResult:
        body = json.dumps({"text": text}).encode("utf-8")
        request = urllib.request.Request(
            self._endpoint,
            data=body,
            headers={"Content-Type": "application/json"},
            method="POST",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                audio = response.read()
                raw_ms = response.headers.get("X-Duration-Ms")
        except (urllib.error.URLError, OSError) as exc:
            raise TTSError(f"DGX TTS 呼叫失敗：{exc}") from exc
        if raw_ms is None:
            raise TTSError("DGX TTS 回應缺少 X-Duration-Ms 標頭")
        try:
            duration_ms = int(raw_ms)
        except ValueError as exc:
            raise TTSError(f"DGX TTS 回應 X-Duration-Ms 非整數：{raw_ms!r}") from exc
        return TtsResult(text=text, audio=audio, duration_ms=duration_ms)


def build_tts_client(settings) -> TTSClient:
    if settings.tts_backend == "dgx":
        if not settings.tts_endpoint:
            raise TTSError("TTS_BACKEND=dgx 但未設定 TTS_ENDPOINT")
        return DgxTtsClient(settings.tts_endpoint, settings.tts_timeout_seconds)
    return TextBubbleTts()
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_tts.py -q`
Expected: PASS（含既有 `test_text_bubble_returns_text_without_audio`）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/speech/tts.py tests/test_tts.py
git commit -m "feat: DgxTtsClient 與 build_tts_client（真 TTS client + duration_ms）"
```

---

## Task 2: config 新增 TTS／音檔設定欄位

**Files:**
- Modify: `src/kinsun/config.py:35-67`（`Settings`）、`src/kinsun/config.py:80-112`（`load_settings`）
- Test: `tests/test_config.py`

**Interfaces:**
- Produces（`Settings` 新欄位）：`tts_backend: str`、`tts_endpoint: str`、`tts_timeout_seconds: float`、`tts_reply_text: bool`、`supabase_url: str`、`supabase_service_key: str`、`audio_bucket: str`、`audio_retention_days: int`、`audio_upload_timeout_seconds: float`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_config.py` 的 `test_load_settings_reads_required_and_defaults` 末尾追加斷言：

```python
    assert settings.tts_backend == "bubble"
    assert settings.tts_endpoint == ""
    assert settings.tts_timeout_seconds == 30
    assert settings.tts_reply_text is True
    assert settings.supabase_url == ""
    assert settings.supabase_service_key == ""
    assert settings.audio_bucket == "tts-audio"
    assert settings.audio_retention_days == 2
    assert settings.audio_upload_timeout_seconds == 10
```

並新增：

```python
def test_load_settings_tts_reply_text_false():
    for raw in ("false", "0", "no", "False"):
        settings = load_settings({**BASE_ENV, "TTS_REPLY_TEXT": raw})
        assert settings.tts_reply_text is False, raw


def test_load_settings_tts_overrides():
    env = {**BASE_ENV, "TTS_BACKEND": "dgx", "TTS_ENDPOINT": "http://dgx:8002/synthesize"}
    settings = load_settings(env)
    assert settings.tts_backend == "dgx"
    assert settings.tts_endpoint == "http://dgx:8002/synthesize"
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_config.py -q`
Expected: FAIL（`AttributeError: 'Settings' object has no attribute 'tts_backend'`）

- [ ] **Step 3: 實作 config.py**

在 `Settings`（frozen dataclass）末尾（`binding_gate_enabled: bool` 之後）加欄位：

```python
    tts_backend: str
    tts_endpoint: str
    tts_timeout_seconds: float
    tts_reply_text: bool
    supabase_url: str
    supabase_service_key: str
    audio_bucket: str
    audio_retention_days: int
    audio_upload_timeout_seconds: float
```

在 `load_settings` 的 `Settings(...)` 呼叫末尾（`binding_gate_enabled=...` 之後）加：

```python
        tts_backend=env.get("TTS_BACKEND", "bubble"),
        tts_endpoint=env.get("TTS_ENDPOINT", ""),
        tts_timeout_seconds=float(env.get("TTS_TIMEOUT_SECONDS", "30")),
        tts_reply_text=_parse_bool(env.get("TTS_REPLY_TEXT", "true")),
        supabase_url=env.get("SUPABASE_URL", ""),
        supabase_service_key=env.get("SUPABASE_SERVICE_KEY", ""),
        audio_bucket=env.get("AUDIO_BUCKET", "tts-audio"),
        audio_retention_days=int(env.get("AUDIO_RETENTION_DAYS", "2")),
        audio_upload_timeout_seconds=float(env.get("AUDIO_UPLOAD_TIMEOUT_SECONDS", "10")),
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_config.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/config.py tests/test_config.py
git commit -m "feat: config 新增 TTS 與音檔託管設定欄位"
```

---

## Task 3: 音檔發佈與清理（audio/publisher.py，新套件）

**Files:**
- Create: `src/kinsun/audio/__init__.py`（空檔）
- Create: `src/kinsun/audio/publisher.py`
- Test: `tests/test_audio_publisher.py`

**Interfaces:**
- Consumes: `kinsun.config.Settings`（Task 2 的 `supabase_url`、`supabase_service_key`、`audio_bucket`、`audio_upload_timeout_seconds`）。測試以 stub settings 提供。
- Produces:
  - `class AudioPublishError(Exception)`
  - `class AudioPublisher(Protocol): def publish(self, audio: bytes, *, content_type: str) -> str`
  - `class SupabaseAudioPublisher: __init__(self, base_url, service_key, bucket, *, timeout, clock, new_id); publish(...) -> str; cleanup(self, *, retention_days: int) -> None`
  - `def build_audio_publisher(settings, *, clock, new_id) -> SupabaseAudioPublisher`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_audio_publisher.py`：

```python
from datetime import datetime, timedelta, timezone

import pytest

from kinsun.audio.publisher import (
    AudioPublishError,
    SupabaseAudioPublisher,
    build_audio_publisher,
)

_TPE = timezone(timedelta(hours=8))
_NOW = datetime(2026, 7, 2, 9, 0, tzinfo=_TPE)


def _publisher(monkeypatch, capture):
    def fake_urlopen(req, timeout=None):
        capture["url"] = req.full_url
        capture["method"] = req.get_method()
        capture["auth"] = req.headers.get("Authorization")
        capture["ctype"] = req.headers.get("Content-type")
        capture["body"] = req.data

        class _R:
            def read(self):
                return b"{}"

            def __enter__(self):
                return self

            def __exit__(self, *a):
                return False

        return _R()

    monkeypatch.setattr("urllib.request.urlopen", fake_urlopen)
    return SupabaseAudioPublisher(
        "https://proj.supabase.co",
        "service-key",
        "tts-audio",
        timeout=10.0,
        clock=lambda: _NOW,
        new_id=lambda: "abc123",
    )


def test_publish_uploads_and_returns_public_url(monkeypatch):
    capture = {}
    url = _publisher(monkeypatch, capture).publish(b"AUDIO", content_type="audio/mp4")
    assert url == (
        "https://proj.supabase.co/storage/v1/object/public/tts-audio/tts/20260702/abc123.m4a"
    )
    assert capture["url"] == (
        "https://proj.supabase.co/storage/v1/object/tts-audio/tts/20260702/abc123.m4a"
    )
    assert capture["method"] == "POST"
    assert capture["auth"] == "Bearer service-key"
    assert capture["ctype"] == "audio/mp4"
    assert capture["body"] == b"AUDIO"


def test_publish_http_error_raises(monkeypatch):
    import urllib.error

    def boom(req, timeout=None):
        raise urllib.error.URLError("boom")

    monkeypatch.setattr("urllib.request.urlopen", boom)
    pub = SupabaseAudioPublisher(
        "https://proj.supabase.co", "k", "b", timeout=10.0, clock=lambda: _NOW, new_id=lambda: "x"
    )
    with pytest.raises(AudioPublishError):
        pub.publish(b"A", content_type="audio/mp4")


def test_cleanup_deletes_expired_date_folders(monkeypatch):
    calls = []

    def fake_urlopen(req, timeout=None):
        calls.append((req.get_method(), req.full_url, req.data))

        class _R:
            def read(self):
                # list bucket 回三個日期資料夾
                return (
                    b'[{"name":"20260628"},{"name":"20260630"},{"name":"20260702"}]'
                )

            def __enter__(self):
                return self

            def __exit__(self, *a):
                return False

        return _R()

    monkeypatch.setattr("urllib.request.urlopen", fake_urlopen)
    pub = SupabaseAudioPublisher(
        "https://proj.supabase.co",
        "k",
        "tts-audio",
        timeout=10.0,
        clock=lambda: _NOW,
        new_id=lambda: "x",
    )
    pub.cleanup(retention_days=2)  # 保留 20260701~ ；刪 0628、0630
    deleted = [url for method, url, _ in calls if method == "DELETE"]
    assert any("20260628" in u for u in deleted)
    assert any("20260630" in u for u in deleted)
    assert not any("20260702" in u for u in deleted)


def test_build_requires_supabase_config():
    class _S:
        supabase_url = ""
        supabase_service_key = ""
        audio_bucket = "tts-audio"
        audio_upload_timeout_seconds = 10.0

    with pytest.raises(AudioPublishError):
        build_audio_publisher(_S(), clock=lambda: _NOW, new_id=lambda: "x")
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_audio_publisher.py -q`
Expected: FAIL（`ModuleNotFoundError: No module named 'kinsun.audio'`）

- [ ] **Step 3: 實作 audio/publisher.py**

建立 `src/kinsun/audio/__init__.py`（空檔）與 `src/kinsun/audio/publisher.py`：

```python
"""音檔發佈：把 TTS 音檔上傳 Supabase Storage 公開 bucket，回公開 URL。

標準庫 urllib（不加 supabase SDK）；service key 走環境變數。
路徑帶日期資料夾 tts/{yyyymmdd}/，清理只刪過期資料夾。
"""

from __future__ import annotations

import json
import logging
import urllib.error
import urllib.request
from collections.abc import Callable
from datetime import datetime, timedelta
from typing import Protocol

logger = logging.getLogger("kinsun.audio")


class AudioPublishError(Exception):
    """音檔上傳／清理失敗。"""


class AudioPublisher(Protocol):
    def publish(self, audio: bytes, *, content_type: str) -> str: ...


class SupabaseAudioPublisher:
    def __init__(
        self,
        base_url: str,
        service_key: str,
        bucket: str,
        *,
        timeout: float,
        clock: Callable[[], datetime],
        new_id: Callable[[], str],
    ) -> None:
        self._base = base_url.rstrip("/")
        self._key = service_key
        self._bucket = bucket
        self._timeout = timeout
        self._clock = clock
        self._new_id = new_id

    def _object_path(self, name: str) -> str:
        return f"tts/{self._clock().strftime('%Y%m%d')}/{name}"

    def publish(self, audio: bytes, *, content_type: str) -> str:
        path = self._object_path(f"{self._new_id()}.m4a")
        upload_url = f"{self._base}/storage/v1/object/{self._bucket}/{path}"
        request = urllib.request.Request(
            upload_url,
            data=audio,
            headers={
                "Authorization": f"Bearer {self._key}",
                "Content-Type": content_type,
            },
            method="POST",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                response.read()
        except (urllib.error.URLError, OSError) as exc:
            raise AudioPublishError(f"音檔上傳失敗：{exc}") from exc
        return f"{self._base}/storage/v1/object/public/{self._bucket}/{path}"

    def cleanup(self, *, retention_days: int) -> None:
        cutoff = (self._clock() - timedelta(days=retention_days)).strftime("%Y%m%d")
        for folder in self._list_date_folders():
            if folder < cutoff:
                self._delete_folder(folder)

    def _list_date_folders(self) -> list[str]:
        list_url = f"{self._base}/storage/v1/object/list/{self._bucket}"
        body = json.dumps({"prefix": "tts/", "limit": 1000}).encode("utf-8")
        request = urllib.request.Request(
            list_url,
            data=body,
            headers={"Authorization": f"Bearer {self._key}", "Content-Type": "application/json"},
            method="POST",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                rows = json.loads(response.read().decode("utf-8"))
        except (urllib.error.URLError, OSError, json.JSONDecodeError) as exc:
            raise AudioPublishError(f"音檔清單讀取失敗：{exc}") from exc
        return [r["name"] for r in rows if isinstance(r, dict) and "name" in r]

    def _delete_folder(self, folder: str) -> None:
        del_url = f"{self._base}/storage/v1/object/{self._bucket}/tts/{folder}"
        request = urllib.request.Request(
            del_url,
            headers={"Authorization": f"Bearer {self._key}"},
            method="DELETE",
        )
        try:
            with urllib.request.urlopen(request, timeout=self._timeout) as response:
                response.read()
        except (urllib.error.URLError, OSError) as exc:
            logger.warning("音檔資料夾刪除失敗 folder=%s：%s", folder, exc)


def build_audio_publisher(
    settings, *, clock: Callable[[], datetime], new_id: Callable[[], str]
) -> SupabaseAudioPublisher:
    if not settings.supabase_url or not settings.supabase_service_key:
        raise AudioPublishError("TTS_BACKEND=dgx 需設定 SUPABASE_URL 與 SUPABASE_SERVICE_KEY")
    return SupabaseAudioPublisher(
        settings.supabase_url,
        settings.supabase_service_key,
        settings.audio_bucket,
        timeout=settings.audio_upload_timeout_seconds,
        clock=clock,
        new_id=new_id,
    )
```

> 註：`cleanup` 的 list/delete 端點以 Supabase Storage REST 為準，於接線後在真專案驗證（見 Task 11 說明）；`_delete_folder` 失敗只 log，符合清理 job fail-safe。

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_audio_publisher.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/audio/ tests/test_audio_publisher.py
git commit -m "feat: SupabaseAudioPublisher 音檔上傳與過期清理"
```

---

## Task 4: LineMessenger.reply_voice（Protocol＋真實作）

**Files:**
- Modify: `src/kinsun/channels/line/messenger.py`
- Test: 無（`LineApiMessenger` 需真憑證、既有原則不進單元測試；Protocol 由 Task 5/6 的 fake 覆蓋）

**Interfaces:**
- Produces: `LineMessenger.reply_voice(self, reply_token: str, audio_url: str, duration_ms: int, text: str | None) -> None`（Protocol 方法）、`LineApiMessenger.reply_voice(...)` 真實作。

- [ ] **Step 1: 在 Protocol 加方法**

`src/kinsun/channels/line/messenger.py` 的 `LineMessenger` Protocol（約 8-13 行）加一行：

```python
    def reply_voice(
        self, reply_token: str, audio_url: str, duration_ms: int, text: str | None
    ) -> None: ...
```

- [ ] **Step 2: 在 LineApiMessenger 加真實作**

`LineApiMessenger.__init__` 的 import 區塊加入 `AudioMessage`（與既有 `TextMessage` 同一個 `from linebot.v3.messaging import (...)`），並存成 `self._AudioMessage = AudioMessage`。於類別末尾（`link_rich_menu` 之後）加：

```python
    def reply_voice(
        self, reply_token: str, audio_url: str, duration_ms: int, text: str | None
    ) -> None:
        messages = [
            self._AudioMessage(original_content_url=audio_url, duration=duration_ms)
        ]
        if text is not None:
            messages.append(self._TextMessage(text=text))
        with self._ApiClient(self._configuration) as api_client:
            api = self._MessagingApi(api_client)
            api.reply_message(
                self._ReplyMessageRequest(reply_token=reply_token, messages=messages)
            )
```

- [ ] **Step 3: 確認既有測試不受影響**

Run: `uv run pytest tests/test_webhook.py tests/test_line_channel.py -q`
Expected: PASS（未觸及 `AudioMessage`，僅新增方法）

- [ ] **Step 4: Commit**

```bash
git add src/kinsun/channels/line/messenger.py
git commit -m "feat: LineMessenger.reply_voice（LINE 語音訊息＋可選文字）"
```

---

## Task 5: InboundMessage.reply_voice 欄位 ＋ LineChannel 綁定

**Files:**
- Modify: `src/kinsun/channels/inbound.py:22-30`（`InboundMessage`）
- Modify: `src/kinsun/channels/line/channel.py`
- Test: `tests/test_line_channel.py`

**Interfaces:**
- Consumes: `LineMessenger.reply_voice`（Task 4）。
- Produces: `InboundMessage` 新增欄位 `reply_voice: Callable[[str, int, str | None], None] | None = None`（url、duration_ms、text）；`LineChannel.inbound` 對每則訊息綁定 `reply_voice`。

- [ ] **Step 1: 寫失敗測試**

`tests/test_line_channel.py` 既有 `_Messenger`（含 `get_audio`／`reply_text`）與 `_audio_event(uid)` 幫手。先給 `_Messenger.__init__` 補 `self.voice = []` 並加方法：

```python
    def reply_voice(self, reply_token, audio_url, duration_ms, text):
        self.voice.append((reply_token, audio_url, duration_ms, text))
```

再追加測試（`_audio_event` 的 `reply_token` 為 `"rt-1"`）：

```python
def test_inbound_binds_reply_voice_to_reply_token():
    messenger = _Messenger()
    msg = LineChannel(messenger).inbound(_audio_event())
    msg.reply_voice("http://x/a.m4a", 500, "文字")
    assert messenger.voice == [("rt-1", "http://x/a.m4a", 500, "文字")]
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_line_channel.py -q`
Expected: FAIL（`InboundMessage` 無 `reply_voice`／為 None）

- [ ] **Step 3: 實作**

`src/kinsun/channels/inbound.py` 的 `InboundMessage` 加欄位（放最後、給預設值，避免破壞既有 5 位置參數建構）：

```python
@dataclass(frozen=True)
class InboundMessage:
    """通道中立的入站訊息。kind ∈ text/audio/other；reply 為文字回覆 handle，
    reply_voice 為語音回覆 handle（url、duration_ms、text）。"""

    session_id: str
    kind: str
    text: str
    audio: bytes
    reply: Callable[[str], None]
    reply_voice: Callable[[str, int, str | None], None] | None = None
```

`src/kinsun/channels/line/channel.py` 的 `inbound` 內，於 `reply` closure 之後加 `reply_voice` closure，並把它帶進三個 `InboundMessage(...)`：

```python
        def reply(text: str) -> None:
            self._messenger.reply_text(reply_token, text)

        def reply_voice(audio_url: str, duration_ms: int, text: str | None) -> None:
            self._messenger.reply_voice(reply_token, audio_url, duration_ms, text)

        if mtype == "text":
            return InboundMessage(
                session_id, "text", getattr(message, "text", "") or "", b"", reply, reply_voice
            )
        if mtype == "audio":
            return InboundMessage(
                session_id, "audio", "", self._messenger.get_audio(message.id), reply, reply_voice
            )
        return InboundMessage(session_id, "other", "", b"", reply, reply_voice)
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_line_channel.py tests/test_inbound.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/channels/inbound.py src/kinsun/channels/line/channel.py tests/test_line_channel.py
git commit -m "feat: InboundMessage.reply_voice 欄位與 LineChannel 綁定"
```

---

## Task 6: VoiceReplyDelivery（音檔發佈→語音回覆，含 fail-safe）

**Files:**
- Modify: `src/kinsun/channels/inbound.py`
- Test: `tests/test_inbound.py`

**Interfaces:**
- Consumes: `kinsun.audio.publisher.AudioPublisher`（Task 3）、`kinsun.speech.tts.TtsResult`（Task 1）、`InboundMessage.reply_voice`（Task 5）。
- Produces: `class VoiceReplyDelivery: __init__(self, publisher: AudioPublisher | None, include_text: bool); deliver(self, msg: InboundMessage, result: TtsResult) -> None`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_inbound.py` 追加：

```python
from kinsun.channels.inbound import VoiceReplyDelivery
from kinsun.speech.tts import TtsResult


class _VoiceCapture:
    def __init__(self):
        self.text_sent = []
        self.voice_sent = []

    def reply(self, text):
        self.text_sent.append(text)

    def reply_voice(self, url, duration_ms, text):
        self.voice_sent.append((url, duration_ms, text))


class _Publisher:
    def __init__(self, url="http://x/a.m4a", boom=False):
        self._url = url
        self._boom = boom

    def publish(self, audio, *, content_type):
        if self._boom:
            from kinsun.audio.publisher import AudioPublishError

            raise AudioPublishError("boom")
        return self._url


def _voice_msg(cap):
    return InboundMessage("U-1", "audio", "", b"x", cap.reply, cap.reply_voice)


def test_deliver_text_when_no_audio():
    cap = _VoiceCapture()
    VoiceReplyDelivery(_Publisher(), include_text=True).deliver(
        _voice_msg(cap), TtsResult(text="純文字", audio=None)
    )
    assert cap.text_sent == ["純文字"]
    assert cap.voice_sent == []


def test_deliver_voice_with_text():
    cap = _VoiceCapture()
    VoiceReplyDelivery(_Publisher(), include_text=True).deliver(
        _voice_msg(cap), TtsResult(text="嗨", audio=b"A", duration_ms=800)
    )
    assert cap.voice_sent == [("http://x/a.m4a", 800, "嗨")]
    assert cap.text_sent == []


def test_deliver_voice_without_text_when_disabled():
    cap = _VoiceCapture()
    VoiceReplyDelivery(_Publisher(), include_text=False).deliver(
        _voice_msg(cap), TtsResult(text="嗨", audio=b"A", duration_ms=800)
    )
    assert cap.voice_sent == [("http://x/a.m4a", 800, None)]


def test_deliver_falls_back_to_text_on_publish_error():
    cap = _VoiceCapture()
    VoiceReplyDelivery(_Publisher(boom=True), include_text=True).deliver(
        _voice_msg(cap), TtsResult(text="退化文字", audio=b"A", duration_ms=800)
    )
    assert cap.text_sent == ["退化文字"]
    assert cap.voice_sent == []


def test_deliver_text_when_publisher_none():
    cap = _VoiceCapture()
    VoiceReplyDelivery(None, include_text=True).deliver(
        _voice_msg(cap), TtsResult(text="泡泡", audio=None)
    )
    assert cap.text_sent == ["泡泡"]
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_inbound.py -q`
Expected: FAIL（`ImportError: cannot import name 'VoiceReplyDelivery'`）

- [ ] **Step 3: 實作 VoiceReplyDelivery**

在 `src/kinsun/channels/inbound.py` 頂部 import 區加 `from kinsun.audio.publisher import AudioPublishError`、`from kinsun.speech.tts import TtsResult`，並新增類別（放在 `dispatch` 之前）：

```python
class VoiceReplyDelivery:
    """把 TtsResult 發成 LINE 回覆：有音檔→上傳→語音（可附文字）；否則→文字泡泡。
    上傳或語音回覆失敗一律退回文字，絕不讓回覆消失。"""

    def __init__(self, publisher, include_text: bool) -> None:
        self._publisher = publisher
        self._include_text = include_text

    def deliver(self, msg: InboundMessage, result: TtsResult) -> None:
        if result.audio is None or self._publisher is None or msg.reply_voice is None:
            msg.reply(result.text)
            return
        try:
            url = self._publisher.publish(result.audio, content_type="audio/mp4")
            msg.reply_voice(url, result.duration_ms, result.text if self._include_text else None)
        except (AudioPublishError, Exception):  # noqa: BLE001 - 任何失敗都退回文字
            logger.warning("語音回覆失敗，退回文字泡泡")
            msg.reply(result.text)
```

> 註：`reply_voice` 失敗時若 reply_token 已被消耗，補發的 `msg.reply` 會再失敗並向上拋，由 webhook 事件層 catch（見 Task 9 既有 `_handle_events`），不會回 500。

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_inbound.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/channels/inbound.py tests/test_inbound.py
git commit -m "feat: VoiceReplyDelivery（音檔上傳→LINE 語音回覆，失敗退化文字）"
```

---

## Task 7: dispatch 接 voice

**Files:**
- Modify: `src/kinsun/channels/inbound.py:33-48`（`dispatch`）
- Test: `tests/test_inbound.py`

**Interfaces:**
- Consumes: `VoiceReplyDelivery`（Task 6）。
- Produces: `dispatch(msg, *, pipeline, binding, gate, voice=None) -> None`；audio 成功路徑改呼叫 `voice.deliver(msg, result)`（`voice` 為 None 時維持 `msg.reply(result.text)`，向後相容）。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_inbound.py` 追加（`_Pipeline.process` 目前回 `SimpleNamespace(text=...)`；voice 路徑需要 `TtsResult`，故新增一個回 `TtsResult` 的 pipeline）：

```python
class _VoicePipeline:
    def __init__(self, result):
        self._result = result

    def process(self, audio, *, session_id):
        return self._result


class _SpyVoice:
    def __init__(self):
        self.delivered = []

    def deliver(self, msg, result):
        self.delivered.append((msg.session_id, result.text))


def test_audio_success_routes_to_voice_when_present():
    voice = _SpyVoice()
    dispatch(
        _msg("audio", audio=b"x", reply=_Replies()),
        pipeline=_VoicePipeline(TtsResult(text="語音回覆", audio=b"A", duration_ms=100)),
        binding=_Binding(None),
        gate=_Gate(True),
        voice=voice,
    )
    assert voice.delivered == [("U-1", "語音回覆")]
```

（`TtsResult` 已在 Task 6 於本檔 import。）

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_inbound.py::test_audio_success_routes_to_voice_when_present -q`
Expected: FAIL（`dispatch` 不接受 `voice`）

- [ ] **Step 3: 實作 dispatch**

把 `dispatch` 改為：

```python
def dispatch(msg: InboundMessage, *, pipeline, binding, gate, voice=None) -> None:
    if msg.kind == "text":
        reply = binding.handle(msg.session_id, msg.text)
        msg.reply(reply if reply is not None else NON_AUDIO_PROMPT)
        return
    if msg.kind != "audio":
        msg.reply(NON_AUDIO_PROMPT)
        return
    if not gate.allows(msg.session_id):
        msg.reply(BIND_FIRST_PROMPT)
        return
    try:
        result = pipeline.process(msg.audio, session_id=msg.session_id)
        if voice is not None:
            voice.deliver(msg, result)
        else:
            msg.reply(result.text)
    except (ASRError, LLMError, MemoryError):
        msg.reply(FALLBACK_PROMPT)
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_inbound.py -q`
Expected: PASS（既有 `test_audio_runs_pipeline_when_allowed` 等仍綠：voice 預設 None）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/channels/inbound.py tests/test_inbound.py
git commit -m "feat: dispatch 接 voice，audio 成功路徑走 VoiceReplyDelivery"
```

---

## Task 8: VoicePipeline TTS fail-safe

**Files:**
- Modify: `src/kinsun/pipeline.py:36-50`
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: `kinsun.speech.tts.TTSError`（Task 1）。
- Produces: `VoicePipeline.process` 在 `self._tts.synthesize` 拋 `TTSError` 時回 `TtsResult(text=reply_text, audio=None)`。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_pipeline.py` 追加：

```python
from kinsun.speech.tts import TTSError, TtsResult


class _BoomTts:
    def synthesize(self, text):
        raise TTSError("tts down")


def test_pipeline_tts_failure_degrades_to_text():
    pipeline = VoicePipeline(
        asr=MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM(), NullMemory(), NullContext()),
        tts=_BoomTts(),
        detector=StubDetector(RiskTier.L0),
        notifier=SpyNotifier(),
        risk_events=FakeRiskEventStore(),
    )
    result = pipeline.process(b"\x00", session_id="u1")
    assert isinstance(result, TtsResult)
    assert result.text == "你說的是：阿公早安"
    assert result.audio is None
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_pipeline.py::test_pipeline_tts_failure_degrades_to_text -q`
Expected: FAIL（`TTSError` 未被捕捉，向上拋出）

- [ ] **Step 3: 實作 pipeline fail-safe**

`src/kinsun/pipeline.py`：`from kinsun.speech.tts import TTSClient, TtsResult` 改為 `from kinsun.speech.tts import TTSClient, TtsResult, TTSError`；`process` 最後一行改為：

```python
        reply_text = self._agent.handle(session_id, user_text)
        try:
            return self._tts.synthesize(reply_text)
        except TTSError:
            logger.warning("TTS 合成失敗，退化為純文字回覆")
            return TtsResult(text=reply_text, audio=None)
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_pipeline.py -q`
Expected: PASS

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/pipeline.py tests/test_pipeline.py
git commit -m "fix: VoicePipeline TTS fail-safe（TTSError 退化純文字，回覆不消失）"
```

---

## Task 9: webhook 串接 voice

**Files:**
- Modify: `src/kinsun/channels/line/webhook.py:29-72`
- Test: `tests/test_webhook.py`

**Interfaces:**
- Consumes: `VoiceReplyDelivery`（Task 6）。
- Produces: `create_app(*, parser, pipeline, messenger, binding, gate, voice=None, on_shutdown=None)`；`_handle_events` 多收並轉傳 `voice` 給 `dispatch`。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_webhook.py` 追加（用 `TtsResult` 音檔跑通語音路徑；`FakeMessenger` 需補 `reply_voice`）：

```python
from kinsun.channels.inbound import VoiceReplyDelivery
from kinsun.speech.tts import TtsResult


class _VoiceMessenger(FakeMessenger):
    def __init__(self):
        super().__init__()
        self.voice = []

    def reply_voice(self, reply_token, audio_url, duration_ms, text):
        self.voice.append((reply_token, audio_url, duration_ms, text))


class _StubTts:
    def synthesize(self, text):
        return TtsResult(text=text, audio=b"AUDIO", duration_ms=700)


class _StubPublisher:
    def publish(self, audio, *, content_type):
        return "http://cdn/a.m4a"


def test_voice_reply_end_to_end():
    messenger = _VoiceMessenger()
    pipeline = VoicePipeline(
        asr=MockAsrClient("阿公早安"),
        agent=CareAgent(EchoLLM(), NullMemory(), NullContext()),
        tts=_StubTts(),
        detector=_NullDetector(),
        notifier=_NullNotifier(),
        risk_events=FakeRiskEventStore(),
    )
    app = create_app(
        parser=FakeParser([_audio_event()]),
        pipeline=pipeline,
        messenger=messenger,
        binding=_NullBinding(),
        gate=_AllowGate(),
        voice=VoiceReplyDelivery(_StubPublisher(), include_text=True),
    )
    from fastapi.testclient import TestClient

    TestClient(app).post("/line/webhook", content=b"{}", headers={"X-Line-Signature": "x"})
    assert messenger.voice == [("rt-1", "http://cdn/a.m4a", 700, "你說的是：阿公早安")]
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_webhook.py::test_voice_reply_end_to_end -q`
Expected: FAIL（`create_app` 不接受 `voice`）

- [ ] **Step 3: 實作 webhook 串接**

`src/kinsun/channels/line/webhook.py`：`_handle_events` 簽章加 `voice`，並傳給 `dispatch`：

```python
def _handle_events(events, *, channel: LineChannel, pipeline, binding, gate, voice) -> None:
    for event in events:
        try:
            msg = channel.inbound(event)
            if msg is not None:
                dispatch(msg, pipeline=pipeline, binding=binding, gate=gate, voice=voice)
        except Exception:  # noqa: BLE001
            logger.exception("處理 LINE 事件失敗")
```

`create_app` 簽章加 `voice=None`，並把它帶進 `run_in_threadpool`：

```python
def create_app(
    *,
    parser,
    pipeline: VoicePipeline,
    messenger: LineMessenger,
    binding,
    gate,
    voice=None,
    on_shutdown: Callable[[], None] | None = None,
) -> FastAPI:
```

```python
        await run_in_threadpool(
            _handle_events,
            events,
            channel=channel,
            pipeline=pipeline,
            binding=binding,
            gate=gate,
            voice=voice,
        )
```

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_webhook.py -q`
Expected: PASS（既有測試 voice 預設 None，行為不變）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/channels/line/webhook.py tests/test_webhook.py
git commit -m "feat: webhook 串接 VoiceReplyDelivery（voice 貫穿事件處理）"
```

---

## Task 10: app.py 組裝真 TTS 全鏈路

**Files:**
- Modify: `src/kinsun/app.py`
- Test: 無新單元測試（組裝碼；以既有 `test_webhook`／`test_config` 與 §12 實機清單覆蓋）。以 import 冒煙確認組裝不炸。

**Interfaces:**
- Consumes: `build_tts_client`（Task 1）、`build_audio_publisher`（Task 3）、`VoiceReplyDelivery`（Task 6）、`create_app(voice=)`（Task 9）、config 新欄位（Task 2）。

- [ ] **Step 1: 改 import 與組裝**

`src/kinsun/app.py` 的 import：
- `from kinsun.speech.tts import TextBubbleTts` 改為 `from kinsun.speech.tts import build_tts_client`
- 加 `from kinsun.audio.publisher import build_audio_publisher`
- 加 `from kinsun.channels.inbound import VoiceReplyDelivery`

`build_app` 內，`pipeline = VoicePipeline(...)` 的 `tts=TextBubbleTts()` 改為 `tts=build_tts_client(settings)`。

在 `pipeline = VoicePipeline(...)` 之後、`create_app(...)` 之前加：

```python
    publisher = (
        build_audio_publisher(settings, clock=lambda: datetime.now(tz), new_id=lambda: uuid.uuid4().hex)
        if settings.tts_backend == "dgx"
        else None
    )
    voice = VoiceReplyDelivery(publisher, settings.tts_reply_text)
```

`create_app(...)` 呼叫加參數 `voice=voice`（放在 `gate=gate` 之後、`on_shutdown=db.close` 之前）。

- [ ] **Step 2: import 冒煙測試**

Run: `uv run python -c "import ast; ast.parse(open('src/kinsun/app.py').read()); print('ok')"`
Expected: 輸出 `ok`（語法正確；`build_app()` 需雲端金鑰不在此執行）

Run: `uv run pytest -q`
Expected: 全綠（既有測試不受組裝碼影響）

- [ ] **Step 3: Commit**

```bash
git add src/kinsun/app.py
git commit -m "feat: app 組裝真 TTS 全鏈路（build_tts_client＋publisher＋VoiceReplyDelivery）"
```

---

## Task 11: 音檔清理每日 job（scheduler/worker.py）

**Files:**
- Modify: `src/kinsun/scheduler/worker.py`
- Test: `tests/test_scheduler_jobs.py`（用 `build_audio_cleanup_job` 的排程時間與動作）

**Interfaces:**
- Consumes: `SupabaseAudioPublisher.cleanup`（Task 3）、`build_audio_publisher`（Task 3）、`kinsun.scheduler.scheduler.Job`。
- Produces: `def build_audio_cleanup_job(*, cleanup: Callable[[], None], hour: int, minute: int = 30, name: str = "audio-cleanup") -> Job`（放 `src/kinsun/scheduler/jobs.py`）。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_scheduler_jobs.py`（若無則新建，沿用既有 job 測試風格）追加：

```python
from kinsun.scheduler.jobs import build_audio_cleanup_job


def test_audio_cleanup_job_runs_cleanup():
    ran = []
    job = build_audio_cleanup_job(cleanup=lambda: ran.append(True), hour=4)
    assert job.name == "audio-cleanup"
    assert job.cron == "30 4 * * *"
    job.run()
    assert ran == [True]
```

- [ ] **Step 2: 執行測試確認失敗**

Run: `uv run pytest tests/test_scheduler_jobs.py -q`
Expected: FAIL（`cannot import name 'build_audio_cleanup_job'`）

- [ ] **Step 3: 實作 job builder**

在 `src/kinsun/scheduler/jobs.py` 加：

```python
from kinsun.scheduler.scheduler import Job


def build_audio_cleanup_job(
    *,
    cleanup: Callable[[], None],
    hour: int,
    minute: int = 30,
    name: str = "audio-cleanup",
) -> Job:
    return Job(name=name, cron=f"{minute} {hour} * * *", run=cleanup)
```

（`Callable` 已在檔案頂部 import；`Job` 若未 import 則補上。）

- [ ] **Step 4: 執行測試確認通過**

Run: `uv run pytest tests/test_scheduler_jobs.py -q`
Expected: PASS

- [ ] **Step 5: 接進 worker**

`src/kinsun/scheduler/worker.py`：
- import 加 `from kinsun.audio.publisher import build_audio_publisher`、`from kinsun.scheduler.jobs import build_audio_cleanup_job`。
- 在 `build_scheduler` 組出 `jobs` 之後、`state = PgScheduleStateStore(...)` 之前加：

```python
    if settings.tts_backend == "dgx":
        publisher = build_audio_publisher(
            settings, clock=clock, new_id=lambda: uuid.uuid4().hex
        )
        jobs.append(
            build_audio_cleanup_job(
                cleanup=lambda: publisher.cleanup(retention_days=settings.audio_retention_days),
                hour=settings.consolidation_hour,
            )
        )
```

（`uuid` 已在 worker 頂部 import。清理排在整理時段、錯誤由 `Scheduler.run_due` 既有隔離吞掉。）

- [ ] **Step 6: 執行測試確認通過**

Run: `uv run pytest -q`
Expected: 全綠

- [ ] **Step 7: Commit**

```bash
git add src/kinsun/scheduler/jobs.py src/kinsun/scheduler/worker.py tests/test_scheduler_jobs.py
git commit -m "feat: 音檔清理每日 job（TTS_BACKEND=dgx 時刪過期音檔資料夾）"
```

---

## Task 12: DGX TTS 服務——CosyVoice 3 合成（services/tts/server.py）

> **僅在 DGX 執行、不進 CI。** 本 Task 無 pytest 循環；以 §12 DGX 實機清單驗證。CosyVoice 3 的載入／合成 API 以 [FunAudioLLM/CosyVoice](https://github.com/FunAudioLLM/CosyVoice) repo 為準，於 DGX 鎖定。

**Files:**
- Modify: `services/tts/server.py`
- Modify: `services/tts/requirements.txt`

- [ ] **Step 1: 改寫 server.py**

契約：`POST /synthesize` JSON `{"text"}` → m4a bytes、`Content-Type: audio/mp4`、header `X-Duration-Ms`；`GET /healthz`；併發序列化（semaphore）＋佇列上限 503；`TTS_PRELOAD` 可啟動即載入。

```python
"""DGX 端 TTS 推論服務（CosyVoice 3）：提供 POST /synthesize、GET /healthz。

僅在 DGX（Linux + ARM64 + GPU）執行；安裝見 services/tts/requirements.txt。
啟動：uvicorn services.tts.server:app --host 0.0.0.0 --port 8002

與 kinsun.speech.tts.DgxTtsClient 的契約：
- 輸入：JSON {"text": "繁體國語漢字"}。
- 輸出：m4a（AAC）bytes、Content-Type: audio/mp4、header X-Duration-Ms。

zero-shot 聲音複製：TTS_PROMPT_WAV（參考音檔）＋ TTS_PROMPT_TEXT（其逐字稿）。
"""

from __future__ import annotations

import asyncio
import io
import os
import subprocess
from contextlib import asynccontextmanager

from fastapi import FastAPI, HTTPException
from fastapi.concurrency import run_in_threadpool
from fastapi.responses import Response

TTS_MODEL_ID = os.environ.get("TTS_MODEL_ID", "FunAudioLLM/Fun-CosyVoice3-0.5B-2512")
TTS_PROMPT_WAV = os.environ.get("TTS_PROMPT_WAV", "")
TTS_PROMPT_TEXT = os.environ.get("TTS_PROMPT_TEXT", "")
TTS_MAX_CONCURRENCY = int(os.environ.get("TTS_MAX_CONCURRENCY", "1"))
TTS_MAX_QUEUE = int(os.environ.get("TTS_MAX_QUEUE", "8"))
TTS_PRELOAD = os.environ.get("TTS_PRELOAD", "0") not in {"0", "false", "no"}

_model = None
_prompt_16k = None
_sem = asyncio.Semaphore(TTS_MAX_CONCURRENCY)
_inflight = 0


def _get_model():
    """延遲載入 CosyVoice 3 與參考語音；缺參考語音設定即明確報錯。"""
    global _model, _prompt_16k
    if _model is None:
        if not TTS_PROMPT_WAV or not TTS_PROMPT_TEXT:
            raise RuntimeError("需設定 TTS_PROMPT_WAV 與 TTS_PROMPT_TEXT（金孫參考語音）")
        # DGX 實機鎖定：類別名稱／建構參數依 CosyVoice repo 版本調整。
        from cosyvoice.cli.cosyvoice import CosyVoice2
        from cosyvoice.utils.file_utils import load_wav

        _model = CosyVoice2(TTS_MODEL_ID, load_jit=False, load_trt=False, fp16=True)
        _prompt_16k = load_wav(TTS_PROMPT_WAV, 16000)
    return _model


def _synthesize(text: str) -> tuple[bytes, int]:
    import torch
    import torchaudio

    model = _get_model()
    chunks = [
        out["tts_speech"]
        for out in model.inference_zero_shot(text, TTS_PROMPT_TEXT, _prompt_16k, stream=False)
    ]
    waveform = torch.cat(chunks, dim=1)  # [1, N]
    sample_rate = model.sample_rate
    duration_ms = int(waveform.shape[1] / sample_rate * 1000)

    wav_buf = io.BytesIO()
    torchaudio.save(wav_buf, waveform, sample_rate, format="wav")
    return _wav_to_m4a(wav_buf.getvalue()), duration_ms


def _wav_to_m4a(wav_bytes: bytes) -> bytes:
    """DGX 端 ffmpeg：wav → AAC/m4a（應用層跨平台、不裝 ffmpeg）。"""
    proc = subprocess.run(
        ["ffmpeg", "-f", "wav", "-i", "pipe:0", "-f", "ipod", "-c:a", "aac", "pipe:1"],
        input=wav_bytes,
        stdout=subprocess.PIPE,
        stderr=subprocess.PIPE,
        check=True,
    )
    return proc.stdout


@asynccontextmanager
async def lifespan(_app: FastAPI):
    if TTS_PRELOAD:
        _get_model()
    yield


app = FastAPI(title="KinSun TTS (CosyVoice 3)", lifespan=lifespan)


@app.get("/healthz")
async def healthz() -> dict:
    return {"status": "ok", "model_loaded": _model is not None}


@app.post("/synthesize")
async def synthesize(payload: dict) -> Response:
    global _inflight
    if _inflight >= TTS_MAX_CONCURRENCY + TTS_MAX_QUEUE:
        raise HTTPException(status_code=503, detail="TTS 過載，請稍後再試")
    _inflight += 1
    try:
        async with _sem:
            audio, duration_ms = await run_in_threadpool(_synthesize, payload.get("text", ""))
    finally:
        _inflight -= 1
    return Response(
        content=audio,
        media_type="audio/mp4",
        headers={"X-Duration-Ms": str(duration_ms)},
    )
```

- [ ] **Step 2: 更新 requirements.txt**

```text
# 僅在 DGX Spark（Linux + ARM64/aarch64 + GPU）安裝。
# 版本於 DGX 上以實機驗證後鎖定；torch 用 NVIDIA 對應 aarch64/CUDA 版本。
# CosyVoice 3 依 FunAudioLLM/CosyVoice repo 安裝（含 cosyvoice 套件與其依賴）。
# 系統需 ffmpeg（wav→m4a）。
fastapi
uvicorn
torch
torchaudio
```

- [ ] **Step 3: 語法檢查（本機，不載模型）**

Run: `uv run python -c "import ast; ast.parse(open('services/tts/server.py').read()); print('ok')"`
Expected: 輸出 `ok`

- [ ] **Step 4: DGX 實機驗證（見 §12 清單第 1–3、6 項）**

在 DGX 上依序：裝 CosyVoice 3 → 錄製參考語音設 `TTS_PROMPT_WAV/TEXT` → 啟動服務 → `curl -s localhost:8002/healthz` → `curl -s -X POST localhost:8002/synthesize -H 'Content-Type: application/json' -d '{"text":"阿公早安"}' -D - -o out.m4a` 確認回 `X-Duration-Ms` 且 `out.m4a` 可播放。若 CosyVoice 3 類別／方法名不同，於此鎖定並回填程式。

- [ ] **Step 5: Commit**

```bash
git add services/tts/server.py services/tts/requirements.txt
git commit -m "feat: DGX TTS 服務接 CosyVoice 3（合成＋wav→m4a＋healthz＋併發）"
```

---

## Task 13: DGX ASR 服務硬化（services/asr/server.py）

> **僅在 DGX 執行、不進 CI。** 修阻塞 event loop 的 bug、補 healthz／併發／預載。

**Files:**
- Modify: `services/asr/server.py`
- Modify: `services/asr/requirements.txt`（補系統需求註記 ffmpeg）

- [ ] **Step 1: 改寫 server.py**

保留既有模型載入（`_get_model`：GPU＋fp16，已實機驗證），把 `transcribe` 改為 threadpool＋semaphore、加 healthz、超載 503、`ASR_PRELOAD`：

```python
"""DGX 端 ASR 推論服務：載入 Breeze-ASR-26，提供 POST /transcribe、GET /healthz。

僅在 DGX（Linux + ARM64 + GPU）執行；安裝見 services/asr/requirements.txt。
啟動：uvicorn services.asr.server:app --host 0.0.0.0 --port 8001

與 kinsun.speech.asr.DgxAsrClient 的契約：
- 輸入：HTTP body 為原始音檔 bytes（Content-Type 由呼叫端帶入）。
- 輸出：JSON {"text": "繁體國語漢字"}。
"""

from __future__ import annotations

import asyncio
import os
from contextlib import asynccontextmanager

from fastapi import FastAPI, HTTPException, Request
from fastapi.concurrency import run_in_threadpool

ASR_MODEL_ID = os.environ.get("ASR_MODEL_ID", "MediaTek-Research/Breeze-ASR-26")
ASR_MAX_CONCURRENCY = int(os.environ.get("ASR_MAX_CONCURRENCY", "1"))
ASR_MAX_QUEUE = int(os.environ.get("ASR_MAX_QUEUE", "8"))
ASR_PRELOAD = os.environ.get("ASR_PRELOAD", "0") not in {"0", "false", "no"}

_model = None
_sem = asyncio.Semaphore(ASR_MAX_CONCURRENCY)
_inflight = 0


def _get_model():
    """延遲載入：無 GPU 的開發機不需安裝 transformers/torch。"""
    global _model
    if _model is None:
        import torch
        from transformers import pipeline as hf_pipeline

        # DGX 實機驗證（GB10）：不指定 device 會落 CPU、一句數十秒；GPU + fp16 才夠即時。
        use_cuda = torch.cuda.is_available()
        _model = hf_pipeline(
            "automatic-speech-recognition",
            model=ASR_MODEL_ID,
            device=0 if use_cuda else -1,
            torch_dtype=torch.float16 if use_cuda else torch.float32,
        )
    return _model


def _transcribe(audio: bytes) -> str:
    return _get_model()(audio)["text"]


@asynccontextmanager
async def lifespan(_app: FastAPI):
    if ASR_PRELOAD:
        _get_model()
    yield


app = FastAPI(title="KinSun ASR (Breeze-ASR-26)", lifespan=lifespan)


@app.get("/healthz")
async def healthz() -> dict:
    return {"status": "ok", "model_loaded": _model is not None}


@app.post("/transcribe")
async def transcribe(request: Request) -> dict[str, str]:
    global _inflight
    audio = await request.body()
    if _inflight >= ASR_MAX_CONCURRENCY + ASR_MAX_QUEUE:
        raise HTTPException(status_code=503, detail="ASR 過載，請稍後再試")
    _inflight += 1
    try:
        async with _sem:
            text = await run_in_threadpool(_transcribe, audio)
    finally:
        _inflight -= 1
    return {"text": text}
```

- [ ] **Step 2: requirements.txt 補 ffmpeg 系統需求註記**

在 `services/asr/requirements.txt` 註解區加一行說明 HF pipeline 解 m4a 需系統安裝 ffmpeg（不改套件清單本身）：

```text
# 系統需 ffmpeg（HF pipeline 解 m4a/非 wav 音檔的依賴）。
```

- [ ] **Step 3: 語法檢查（本機，不載模型）**

Run: `uv run python -c "import ast; ast.parse(open('services/asr/server.py').read()); print('ok')"`
Expected: 輸出 `ok`

- [ ] **Step 4: DGX 實機驗證（見 §12 清單第 4 項）**

DGX 上啟動 ASR 服務 → `curl -s localhost:8001/healthz` → 併發送多筆確認不卡死、超載回 503。

- [ ] **Step 5: Commit**

```bash
git add services/asr/server.py services/asr/requirements.txt
git commit -m "fix: ASR 服務硬化（threadpool 修阻塞、healthz、併發/超載 503、預載）"
```

---

## Task 14: 文件同步（READMEs、README 環境變數、progress.md）

**Files:**
- Modify: `services/tts/README.md`、`services/asr/README.md`、`README.md`、`progress.md`

- [ ] **Step 1: services/tts/README.md**

更新契約表（m4a/audio/mp4、X-Duration-Ms、healthz）、模型改 CosyVoice 3、部署補 `TTS_PROMPT_WAV/TEXT`、`TTS_MAX_CONCURRENCY/QUEUE/PRELOAD` 與系統需 ffmpeg；接應用層段落補 `TTS_BACKEND=dgx`、`TTS_ENDPOINT`；待辦勾掉已完成項。

- [ ] **Step 2: services/asr/README.md**

待辦勾掉「加上 healthz／併發」與「音檔前處理（補 ffmpeg 系統需求）」；部署段補 `ASR_MAX_CONCURRENCY/QUEUE`、`ASR_PRELOAD`、`/healthz`。

- [ ] **Step 3: README.md 環境變數**

在環境變數說明處補：`TTS_BACKEND`、`TTS_ENDPOINT`、`TTS_TIMEOUT_SECONDS`、`TTS_REPLY_TEXT`、`SUPABASE_URL`、`SUPABASE_SERVICE_KEY`、`AUDIO_BUCKET`、`AUDIO_RETENTION_DAYS`、`AUDIO_UPLOAD_TIMEOUT_SECONDS`，並註明需在 Supabase 後台手動建立公開 bucket `tts-audio`（一次性）。

- [ ] **Step 4: progress.md**

§7 待開發表把「真 ASR 整合」「真台語 TTS」列改為：真 ASR 已硬化、TTS 已接國語 CosyVoice 3（台語模型後續換 services/tts 實作即可）；§9 或新增段落記錄本次全鏈路與 DGX 實機清單狀態。

- [ ] **Step 5: 全套件回歸與 Commit**

Run: `uv run pytest -q && uv run ruff check . && uv run ruff format --check .`
Expected: 測試全綠、ruff 無錯

```bash
git add services/tts/README.md services/asr/README.md README.md progress.md
git commit -m "docs: 真模型接 DGX 文件同步（TTS 契約/環境變數/progress 校正）"
```

---

## Self-Review（對照 spec）

- **§3 DGX TTS 服務**：Task 12（CosyVoice 3、參考語音、契約表、healthz、併發、wav→m4a、時長）✓
- **§4 ASR 硬化**：Task 13（threadpool 修阻塞、healthz、併發/503、預載、ffmpeg 註記）✓
- **§5 應用層 TTS client**：Task 1（`TtsResult.duration_ms`、`TTSError`、`DgxTtsClient`、`build_tts_client`）＋ Task 8（pipeline fail-safe）✓
- **§6 音檔發佈**：Task 3（`SupabaseAudioPublisher`、`build_audio_publisher`、`cleanup`、日期資料夾）＋ Task 11（每日清理 job）✓
- **§7 LINE 語音回覆**：Task 4（messenger.reply_voice）＋ Task 5（InboundMessage 欄位＋channel）＋ Task 6（VoiceReplyDelivery）＋ Task 7（dispatch）✓
- **§8 設定與接線**：Task 2（config 欄位）＋ Task 10（app 組裝）＋ Task 11（worker）✓
- **§9 測試策略**：各 app-layer Task 皆含離線 fake 單元測試；services 不進 CI ✓
- **§10 fail-safe 總表**：ASR（既有）／TTS（Task 8）／上傳（Task 6）／LINE 語音（Task 6）／清理 job（Task 11＋Scheduler 既有隔離）✓
- **§11 DGX 實機清單**：Task 12/13 的實機驗證步驟對應 ✓
- **§12 已定決策**：CosyVoice 3（Task 12）、Supabase Storage（Task 3）、DGX 端轉檔（Task 12）、`TTS_REPLY_TEXT`（Task 2/6）、urllib 零新依賴（Task 1/3）、日期資料夾清理（Task 3/11）、services 不進 CI（Task 12/13）✓

**型別一致性**：`TtsResult(text, audio, duration_ms)`、`DgxTtsClient.synthesize(text)->TtsResult`、`SupabaseAudioPublisher.publish(audio,*,content_type)->str`／`.cleanup(*,retention_days)`、`VoiceReplyDelivery(publisher,include_text).deliver(msg,result)`、`dispatch(...,voice=None)`、`create_app(...,voice=None)`、`build_audio_cleanup_job(*,cleanup,hour,minute,name)->Job` — 跨 Task 一致。

**佔位符掃描**：無 TBD／TODO；服務 Task 的模型 API 為真實起點程式碼＋DGX 鎖定步驟（非佔位符，屬 on-device 整合本質）。
