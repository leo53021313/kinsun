# Agent 自我進化：每晚反思與策略記憶 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓金孫每晚自動反思過去七天的互動，把「這位長輩的相處之道」沉澱成守則並自動注入 system prompt，形成「觀測 → 反思 → 策略 → 行為」的閉環。

**Architecture:** 新增 `strategies/` 領域（三件套持久層＋反思＋事實提供者）。反思掛進既有夜間批次 `run_one()`，讀過去七天逐字稿與提醒回應紀錄，產出守則直接生效（`adopted`）。守則經既有的 `FactProvider` 協定注入 system prompt——`agent.py` 核心迴圈零改動。三道濾網（醫療黑名單、證據門檻、上限汰換）取代人工審核。

**Tech Stack:** Python 3.12＋uv、FastAPI、psycopg（Postgres/Supabase）、Gemini（沿用 `GEMINI_MODEL_SUMMARY`）、pytest。

**設計文件：** `docs/superpowers/specs/2026-07-14-agent自我進化-每晚反思與策略記憶-design.md`

## Global Constraints

- 台灣繁體中文：文件、程式碼註解、commit 訊息、面向使用者的字串一律台灣繁體中文，全形標點。
- 命名規範（`AGENTS.md`）：資料表複數名詞、主鍵 `<表單數>_id` 全稱、時間戳 `<動詞過去分詞>_at` 且型別 `DOUBLE PRECISION`（epoch 秒）。
- 持久層三件套：`store.py` 內含 `StrategyStore`（Protocol）＋`PgStrategyStore`＋`FakeStrategyStore`。
- 動詞慣例：`record` 為 append-only 事件語意；查詢用 `list_for_<維度>`。
- 環境變數：`Settings` 欄位名＝環境變數鍵小寫，一一對應、不設別名；每個鍵都要列進 `.env.example` 並附預設值與一行中文註解。本次前綴一律 `REFLECTION_`。
- TDD：先寫測試（RED）→ 最小實作（GREEN）→ 重構。覆蓋率目標 80%＋。
- 整合測試以 `KINSUN_IT=1`＋`KINSUN_TEST_DATABASE_URL` 啟用，禁連正式庫。
- 安全紅線：反思產出的守則永遠不得涉及用藥、劑量、就醫決策、危急判斷。`safety/` 完全不受本次變更影響。
- 分支：`feat/agent-reflection`（自 `Leo` 開出）。不 push、不改寫歷史，除非 Leo 明確指示。

## 檔案結構

| 檔案 | 職責 |
| :--- | :--- |
| `src/kinsun/strategies/__init__.py` | 套件宣告（空） |
| `src/kinsun/strategies/models.py` | `Strategy` dataclass、分類與狀態列舉 |
| `src/kinsun/strategies/store.py` | 三件套：`StrategyStore`／`PgStrategyStore`／`FakeStrategyStore` |
| `src/kinsun/strategies/policy.py` | 醫療動作詞黑名單＋三道濾網 `is_admissible()` |
| `src/kinsun/strategies/reflection.py` | `REFLECTION_PROMPT`、`reflect_days()`、LLM 回傳解析 |
| `src/kinsun/strategies/facts.py` | `StrategyFacts`（`FactProvider` 實作，注入 prompt） |
| `src/kinsun/db.py` | 加 `STRATEGIES_DDL`、`REMINDER_LOGS_RESPONDED_MIGRATION_DDL` |
| `src/kinsun/config.py` | 加 5 個 `REFLECTION_*` 設定 |
| `src/kinsun/reports/reminders.py` | `ReminderLog.responded_at`、`mark_responded`、`list_for_range` |
| `src/kinsun/pipeline.py` | 進站訊息標記提醒已回應 |
| `src/kinsun/composition.py` | 組 `PgStrategyStore`、掛 `StrategyFacts`、傳 `reminder_logs` 給 pipeline |
| `src/kinsun/scheduler/worker.py` | `run_one()` 加反思第三步 |
| `src/kinsun/web/routers/admin_strategies.py` | 後台：列出生效中守則、撤銷 |

---

### Task 1: 策略資料模型與持久層

**Files:**
- Create: `src/kinsun/strategies/__init__.py`
- Create: `src/kinsun/strategies/models.py`
- Create: `src/kinsun/strategies/store.py`
- Modify: `src/kinsun/db.py`（`REMINDER_LOGS_DDL` 之後加 `STRATEGIES_DDL`；`ensure_schema` 內加一行 `conn.execute(STRATEGIES_DDL)`）
- Modify: `tests/fakes.py`（加一行匯入轉出）
- Test: `tests/test_strategies_store_contract.py`

**Interfaces:**
- Produces: `Strategy` dataclass；`STRATEGY_CATEGORIES`／`STRATEGY_STATUSES` 常數；`StrategyStore` Protocol，方法 `record(elder_id: str, content: str, category: str, evidence: str, observed_days: int, supersedes_strategy_id: str | None) -> None`、`list_for_elder(elder_id: str, *, status: str | None = None) -> list[Strategy]`、`list_for_status(status: str) -> list[Strategy]`、`revoke(strategy_id: str) -> None`。

- [ ] **Step 1: 寫失敗的合約測試**

`tests/test_strategies_store_contract.py`：

```python
"""StrategyStore 合約：Fake 與 Pg 兩個 adapter 必須對同一情境給出相同結果。

Fake 每次都跑；Pg 需 `KINSUN_IT=1`（連真庫）。斷言一律以 `ns` 前綴 scope 到
本測試自己的資料，才能在共用真庫上以「成員／排除」關係斷言而互不干擾。

strategy_id 與 created_at 兩欄由各 adapter 自行產生（Pg 用 new_id／clock，
Fake 用索引虛構），故合約只斷言雙方都會產生的欄位。
"""

from __future__ import annotations

from datetime import datetime, timedelta, timezone
from itertools import count

import pytest

from kinsun.strategies.models import (
    STRATEGY_CATEGORY_ROUTINE,
    STRATEGY_CATEGORY_TONE,
    STRATEGY_STATUS_ADOPTED,
    STRATEGY_STATUS_REVOKED,
    STRATEGY_STATUS_SUPERSEDED,
)
from kinsun.strategies.store import FakeStrategyStore, PgStrategyStore

TPE = timezone(timedelta(hours=8))
FIXED = datetime(2026, 7, 14, 3, tzinfo=TPE)


@pytest.fixture(params=["fake", "pg"])
def store(request, ns):
    if request.param == "pg":
        ids = (f"{ns}st{i}" for i in count(1))
        return PgStrategyStore(
            request.getfixturevalue("pg_database"),
            clock=lambda: FIXED,
            new_id=lambda: next(ids),
        )
    return FakeStrategyStore()


def test_record_writes_an_adopted_strategy(store, ns):
    store.record(f"{ns}e1", "早上七點半再問候", STRATEGY_CATEGORY_ROUTINE, "連三天八點沒回", 3, None)
    rows = store.list_for_elder(f"{ns}e1")
    assert [(r.content, r.category, r.observed_days, r.status) for r in rows] == [
        ("早上七點半再問候", STRATEGY_CATEGORY_ROUTINE, 3, STRATEGY_STATUS_ADOPTED)
    ]


def test_list_for_elder_can_filter_by_status(store, ns):
    store.record(f"{ns}e1", "回話簡短些", STRATEGY_CATEGORY_TONE, "長句多半沒回", 4, None)
    adopted = store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_ADOPTED)
    assert [r.content for r in adopted] == ["回話簡短些"]
    assert store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_REVOKED) == []


def test_list_is_scoped_to_the_given_elder(store, ns):
    store.record(f"{ns}e1", "給 e1 的", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    store.record(f"{ns}e2", "給 e2 的", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    contents = {r.content for r in store.list_for_elder(f"{ns}e1")}
    assert "給 e1 的" in contents
    assert "給 e2 的" not in contents


def test_list_for_status_spans_elders(store, ns):
    store.record(f"{ns}e1", "e1 的守則", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    store.record(f"{ns}e2", "e2 的守則", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    contents = {r.content for r in store.list_for_status(STRATEGY_STATUS_ADOPTED)}
    assert {"e1 的守則", "e2 的守則"} <= contents


def test_record_with_supersedes_retires_the_old_one_atomically(store, ns):
    store.record(f"{ns}e1", "舊守則", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    old = store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_ADOPTED)[0]

    store.record(f"{ns}e1", "新守則", STRATEGY_CATEGORY_TONE, "更新的證據", 5, old.strategy_id)

    adopted = {r.content for r in store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_ADOPTED)}
    superseded = {
        r.content for r in store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_SUPERSEDED)
    }
    assert adopted == {"新守則"}
    assert superseded == {"舊守則"}


def test_revoke_takes_a_strategy_out_of_effect(store, ns):
    store.record(f"{ns}e1", "要撤掉的", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    target = store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_ADOPTED)[0]

    store.revoke(target.strategy_id)

    assert store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_ADOPTED) == []
    revoked = store.list_for_elder(f"{ns}e1", status=STRATEGY_STATUS_REVOKED)
    assert [r.content for r in revoked] == ["要撤掉的"]
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_store_contract.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.strategies'`

- [ ] **Step 3: 建資料模型**

`src/kinsun/strategies/__init__.py`：空檔。

`src/kinsun/strategies/models.py`：

```python
"""策略記憶（守則）的資料模型：金孫從互動中學到的「這位長輩的相處之道」。"""

from __future__ import annotations

from dataclasses import dataclass

# 守則分類白名單：反思只准產出這四類；不在此列者一律丟棄（見 policy.py）。
# 這是安全設計的一環——用藥、就醫、危急判斷永遠不是可學習的對象。
STRATEGY_CATEGORY_ADDRESS = "address"  # 稱呼：不愛被叫阿婆
STRATEGY_CATEGORY_TONE = "tone"  # 語氣：講太長會沒反應
STRATEGY_CATEGORY_ROUTINE = "routine"  # 作息：早上八點還在睡
STRATEGY_CATEGORY_TOPIC = "topic"  # 話題：不愛聊孫子
STRATEGY_CATEGORIES = (
    STRATEGY_CATEGORY_ADDRESS,
    STRATEGY_CATEGORY_TONE,
    STRATEGY_CATEGORY_ROUTINE,
    STRATEGY_CATEGORY_TOPIC,
)

# 守則自動生效（無人審佇列），故無 pending 狀態。
STRATEGY_STATUS_ADOPTED = "adopted"  # 生效中，會注入 system prompt
STRATEGY_STATUS_REVOKED = "revoked"  # 人工於後台撤銷
STRATEGY_STATUS_SUPERSEDED = "superseded"  # 被新守則取代
STRATEGY_STATUSES = (
    STRATEGY_STATUS_ADOPTED,
    STRATEGY_STATUS_REVOKED,
    STRATEGY_STATUS_SUPERSEDED,
)


@dataclass(frozen=True)
class Strategy:
    strategy_id: str
    elder_id: str
    content: str
    category: str  # ∈ STRATEGY_CATEGORIES
    evidence: str  # 反思當下引用的證據（後台檢視用）
    observed_days: int  # 此模式在過去幾天中出現過（證據門檻）
    status: str  # ∈ STRATEGY_STATUSES
    supersedes_strategy_id: str | None
    created_at: float
    revoked_at: float | None
```

- [ ] **Step 4: 建持久層三件套**

`src/kinsun/strategies/store.py`：

```python
"""策略記憶持久層：守則的寫入、查詢、取代與撤銷。

`record` 為 append-only（新守則永遠是新一筆），寫入時 status 直接為 adopted——
守則自動生效，無人審佇列。帶 supersedes 時，新守則生效與舊守則退場必須在同一
交易內完成，否則 15 條上限會被突破（新的進來了、舊的還沒退）。
"""

from __future__ import annotations

from collections.abc import Callable
from datetime import datetime
from typing import Protocol

from kinsun.db import Database, _Errors
from kinsun.strategies.models import (
    STRATEGY_STATUS_ADOPTED,
    STRATEGY_STATUS_REVOKED,
    STRATEGY_STATUS_SUPERSEDED,
    Strategy,
)

_COLUMNS = (
    "strategy_id, elder_id, content, category, evidence, observed_days, "
    "status, supersedes_strategy_id, created_at, revoked_at"
)


class StrategyError(Exception):
    """策略記憶讀寫失敗。"""


class StrategyStore(Protocol):
    def record(
        self,
        elder_id: str,
        content: str,
        category: str,
        evidence: str,
        observed_days: int,
        supersedes_strategy_id: str | None,
    ) -> None: ...
    def list_for_elder(self, elder_id: str, *, status: str | None = None) -> list[Strategy]: ...
    def list_for_status(self, status: str) -> list[Strategy]: ...
    def revoke(self, strategy_id: str) -> None: ...


class PgStrategyStore:
    def __init__(
        self, db: Database, *, clock: Callable[[], datetime], new_id: Callable[[], str]
    ) -> None:
        self._db = db
        self._errors = _Errors(db, lambda m: StrategyError(f"策略記憶存取失敗：{m}"))
        self._clock = clock
        self._new_id = new_id

    def record(
        self,
        elder_id: str,
        content: str,
        category: str,
        evidence: str,
        observed_days: int,
        supersedes_strategy_id: str | None,
    ) -> None:
        now = self._clock().timestamp()
        try:
            with self._db.transaction() as tx:
                tx.execute(
                    "INSERT INTO strategies "
                    "(strategy_id, elder_id, content, category, evidence, observed_days, "
                    "status, supersedes_strategy_id, created_at, revoked_at) "
                    "VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, NULL)",
                    (
                        self._new_id(),
                        elder_id,
                        content,
                        category,
                        evidence,
                        observed_days,
                        STRATEGY_STATUS_ADOPTED,
                        supersedes_strategy_id,
                        now,
                    ),
                )
                if supersedes_strategy_id:
                    tx.execute(
                        "UPDATE strategies SET status = %s WHERE strategy_id = %s "
                        "AND elder_id = %s AND status = %s",
                        (
                            STRATEGY_STATUS_SUPERSEDED,
                            supersedes_strategy_id,
                            elder_id,
                            STRATEGY_STATUS_ADOPTED,
                        ),
                    )
        except Exception as exc:  # noqa: BLE001 - 一律翻成領域錯誤
            raise StrategyError(f"策略記憶存取失敗：{exc}") from exc

    def list_for_elder(self, elder_id: str, *, status: str | None = None) -> list[Strategy]:
        if status is None:
            rows = self._errors.query(
                f"SELECT {_COLUMNS} FROM strategies WHERE elder_id = %s "
                "ORDER BY created_at DESC",
                (elder_id,),
            )
        else:
            rows = self._errors.query(
                f"SELECT {_COLUMNS} FROM strategies WHERE elder_id = %s AND status = %s "
                "ORDER BY created_at DESC",
                (elder_id, status),
            )
        return [Strategy(*r) for r in rows]

    def list_for_status(self, status: str) -> list[Strategy]:
        rows = self._errors.query(
            f"SELECT {_COLUMNS} FROM strategies WHERE status = %s ORDER BY created_at DESC",
            (status,),
        )
        return [Strategy(*r) for r in rows]

    def revoke(self, strategy_id: str) -> None:
        self._errors.execute(
            "UPDATE strategies SET status = %s, revoked_at = %s "
            "WHERE strategy_id = %s AND status = %s",
            (STRATEGY_STATUS_REVOKED, self._clock().timestamp(), strategy_id,
             STRATEGY_STATUS_ADOPTED),
        )


class FakeStrategyStore:
    """StrategyStore 的記憶體替身（測試用，不碰 DB）。

    忠實複製 Pg 的行為：record 寫入即 adopted、帶 supersedes 時原子性退場舊守則、
    revoke 只對 adopted 生效。strategy_id 與 created_at 由索引虛構、僅供排序，
    故合約不斷言其值。回傳依 created_at 由新到舊排序，對齊 Pg 的 ORDER BY DESC。
    """

    def __init__(self) -> None:
        self._rows: list[Strategy] = []

    def record(
        self,
        elder_id: str,
        content: str,
        category: str,
        evidence: str,
        observed_days: int,
        supersedes_strategy_id: str | None,
    ) -> None:
        seq = float(len(self._rows))
        self._rows.append(
            Strategy(
                strategy_id=f"s{len(self._rows)}",
                elder_id=elder_id,
                content=content,
                category=category,
                evidence=evidence,
                observed_days=observed_days,
                status=STRATEGY_STATUS_ADOPTED,
                supersedes_strategy_id=supersedes_strategy_id,
                created_at=seq,
                revoked_at=None,
            )
        )
        if supersedes_strategy_id:
            self._replace(
                supersedes_strategy_id,
                STRATEGY_STATUS_SUPERSEDED,
                revoked_at=None,
                elder_id=elder_id,
            )

    def list_for_elder(self, elder_id: str, *, status: str | None = None) -> list[Strategy]:
        rows = [
            r
            for r in self._rows
            if r.elder_id == elder_id and (status is None or r.status == status)
        ]
        return sorted(rows, key=lambda r: r.created_at, reverse=True)

    def list_for_status(self, status: str) -> list[Strategy]:
        rows = [r for r in self._rows if r.status == status]
        return sorted(rows, key=lambda r: r.created_at, reverse=True)

    def revoke(self, strategy_id: str) -> None:
        self._replace(strategy_id, STRATEGY_STATUS_REVOKED, revoked_at=0.0, elder_id=None)

    def _replace(
        self, strategy_id: str, status: str, *, revoked_at: float | None, elder_id: str | None
    ) -> None:
        from dataclasses import replace

        for i, row in enumerate(self._rows):
            matches_elder = elder_id is None or row.elder_id == elder_id
            if (
                row.strategy_id == strategy_id
                and matches_elder
                and row.status == STRATEGY_STATUS_ADOPTED
            ):
                self._rows[i] = replace(row, status=status, revoked_at=revoked_at)
```

- [ ] **Step 5: 加 DDL**

`src/kinsun/db.py`，在 `REMINDER_LOGS_DDL`（第 149 行結束）之後插入：

```python
# 策略記憶（spec 2026-07-14）：金孫每晚反思學到的「相處之道」守則。
# 全新表、無舊欄位要遷移，故建表與建索引可同批（既有庫首次跑時一起建起來）。
STRATEGIES_DDL = (
    "CREATE TABLE IF NOT EXISTS strategies ("
    "strategy_id TEXT PRIMARY KEY, elder_id TEXT NOT NULL, content TEXT NOT NULL, "
    "category TEXT NOT NULL, evidence TEXT NOT NULL, observed_days INTEGER NOT NULL, "
    "status TEXT NOT NULL, supersedes_strategy_id TEXT, "
    "created_at DOUBLE PRECISION NOT NULL, revoked_at DOUBLE PRECISION);"
    "CREATE INDEX IF NOT EXISTS idx_strategies_elder_status "
    "ON strategies (elder_id, status);"
)
```

在 `ensure_schema` 內，`conn.execute(REMINDER_LOGS_DDL)` 之後加一行：

```python
        conn.execute(STRATEGIES_DDL)
```

- [ ] **Step 6: `tests/fakes.py` 加匯入轉出**

在 `from kinsun.scheduler.state import FakeScheduleStateStore as FakeScheduleStateStore` 之後加：

```python
from kinsun.strategies.store import FakeStrategyStore as FakeStrategyStore
```

- [ ] **Step 7: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_store_contract.py -v`
Expected: PASS（6 個測試，`fake` 全過；`pg` 在未設 `KINSUN_IT=1` 時 skip）

整合測試（需 `KINSUN_TEST_DATABASE_URL` 指向獨立測試庫）：
Run: `KINSUN_IT=1 PYTHONPATH=src uv run pytest tests/test_strategies_store_contract.py -v`
Expected: PASS（12 個，fake＋pg 各 6）

- [ ] **Step 8: Commit**

```bash
git add src/kinsun/strategies/ src/kinsun/db.py tests/fakes.py tests/test_strategies_store_contract.py
git commit -m "feat(strategies): 新增策略記憶資料模型與持久層

金孫沒有任何地方可以存放「從互動中學到的相處之道」——Mem0 長期記憶被明文
限定只存長輩的穩定事實，且是向量檢索（依當下問題撈），而守則需要每輪全取、
且有生效／撤銷的狀態，語意完全不同。先把持久層打好。

守則自動生效（record 寫入即 adopted），無 pending 狀態——人審那道防線由後續
的證據門檻取代。帶 supersedes 的寫入走 Database.transaction()：新守則生效與
舊守則退場必須原子，否則 15 條上限會被突破（新的進來了、舊的還沒退）。

影響：新增 strategies 表與 strategies/ 套件；db.py 的 ensure_schema 多一行。
尚未有任何呼叫端，對現行行為零影響。"
```

---

### Task 2: 提醒回應訊號（`reminder_logs.responded_at`）

**Files:**
- Modify: `src/kinsun/reports/reminders.py`
- Modify: `src/kinsun/db.py`（加 `REMINDER_LOGS_RESPONDED_MIGRATION_DDL`，並在 `ensure_schema` 中排在 `REMINDER_LOGS_DDL` 之後）
- Test: `tests/test_reports_reminders_store_contract.py`（擴充既有檔）

**Interfaces:**
- Consumes: Task 1 無相依（可平行）。
- Produces: `ReminderLog.responded_at: float | None`；`ReminderLogStore.mark_responded(elder_id: str, *, now: float, within_seconds: int) -> None`；`ReminderLogStore.list_for_range(elder_id: str, *, start: float, end: float) -> list[ReminderLog]`。

**為什麼**：`reminder_logs` 現在只記「我推了什麼」，完全沒有「長輩有沒有理我」。沒有這個訊號，反思只剩模型對逐字稿的主觀解讀，沒有硬數據當錨。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_reports_reminders_store_contract.py` 末尾加：

```python
def test_mark_responded_flags_a_reminder_inside_the_window(store, ns):
    store.record(f"{ns}e1", "medication", "早上用藥：A")
    logged_at = FIXED.timestamp()

    store.mark_responded(f"{ns}e1", now=logged_at + 600, within_seconds=3600)

    rows = [r for r in store.list_for_elder(f"{ns}e1") if r.content == "早上用藥：A"]
    assert rows[0].responded_at is not None


def test_mark_responded_ignores_a_reminder_outside_the_window(store, ns):
    store.record(f"{ns}e1", "medication", "太久以前的提醒")
    logged_at = FIXED.timestamp()

    store.mark_responded(f"{ns}e1", now=logged_at + 7200, within_seconds=3600)

    rows = [r for r in store.list_for_elder(f"{ns}e1") if r.content == "太久以前的提醒"]
    assert rows[0].responded_at is None


def test_list_for_range_returns_only_reminders_inside_the_bounds(store, ns):
    store.record(f"{ns}e1", "medication", "區間內")
    at = FIXED.timestamp()

    inside = store.list_for_range(f"{ns}e1", start=at - 1, end=at + 1)
    outside = store.list_for_range(f"{ns}e1", start=at + 10, end=at + 20)

    assert "區間內" in {r.content for r in inside}
    assert "區間內" not in {r.content for r in outside}
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_reports_reminders_store_contract.py -v`
Expected: FAIL — `AttributeError: 'FakeReminderLogStore' object has no attribute 'mark_responded'`

- [ ] **Step 3: 加 DDL 遷移**

`src/kinsun/db.py`，在 `STRATEGIES_DDL` 之後加：

```python
# 提醒回應訊號（spec 2026-07-14）：長輩在提醒發出後的時間窗內有發言即標記。
# ⚠️ 既有庫的 reminder_logs 早已存在，CREATE TABLE IF NOT EXISTS 對它是 no-op、
# 不會生出 responded_at；故本遷移必須獨立成一段，排在 REMINDER_LOGS_DDL 之後、
# 索引之前（見 ensure_schema）。NULL＝未回應，既有列自動為 NULL，語意正確。
REMINDER_LOGS_RESPONDED_MIGRATION_DDL = (
    "ALTER TABLE reminder_logs ADD COLUMN IF NOT EXISTS responded_at DOUBLE PRECISION;"
    "CREATE INDEX IF NOT EXISTS idx_reminder_logs_elder_responded "
    "ON reminder_logs (elder_id, responded_at);"
)
```

`ensure_schema` 內，`conn.execute(STRATEGIES_DDL)` 之後加：

```python
        conn.execute(REMINDER_LOGS_RESPONDED_MIGRATION_DDL)
```

- [ ] **Step 4: 改 `reports/reminders.py`**

`ReminderLog` 加欄位：

```python
@dataclass(frozen=True)
class ReminderLog:
    reminder_log_id: str
    elder_id: str
    kind: str  # ∈ REMINDER_KINDS
    content: str
    created_at: float
    responded_at: float | None = None  # 長輩在時間窗內有發言即標記；None＝未回應
```

`ReminderLogStore` Protocol 加兩個方法：

```python
class ReminderLogStore(Protocol):
    def record(self, elder_id: str, kind: str, content: str) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[ReminderLog]: ...
    def list_for_range(self, elder_id: str, *, start: float, end: float) -> list[ReminderLog]: ...
    def mark_responded(self, elder_id: str, *, now: float, within_seconds: int) -> None: ...
```

`PgReminderLogStore` 的 `list_for_elder` 補回 `responded_at` 欄位，並加兩個新方法：

```python
    def list_for_elder(self, elder_id: str) -> list[ReminderLog]:
        rows = self._db.query(
            "SELECT reminder_log_id, elder_id, kind, content, created_at, responded_at "
            "FROM reminder_logs WHERE elder_id = %s ORDER BY created_at DESC",
            (elder_id,),
        )
        return [ReminderLog(*r) for r in rows]

    def list_for_range(self, elder_id: str, *, start: float, end: float) -> list[ReminderLog]:
        rows = self._db.query(
            "SELECT reminder_log_id, elder_id, kind, content, created_at, responded_at "
            "FROM reminder_logs WHERE elder_id = %s AND created_at >= %s AND created_at < %s "
            "ORDER BY created_at ASC",
            (elder_id, start, end),
        )
        return [ReminderLog(*r) for r in rows]

    def mark_responded(self, elder_id: str, *, now: float, within_seconds: int) -> None:
        """把時間窗內、尚未標記的最近一則提醒標為已回應。

        只標最近一則：長輩一句話不該同時「回應」早上與中午的兩則提醒。
        """
        self._db.execute(
            "UPDATE reminder_logs SET responded_at = %s WHERE reminder_log_id = ("
            "SELECT reminder_log_id FROM reminder_logs "
            "WHERE elder_id = %s AND responded_at IS NULL AND created_at >= %s "
            "AND created_at <= %s ORDER BY created_at DESC LIMIT 1)",
            (now, elder_id, now - within_seconds, now),
        )
```

`FakeReminderLogStore` 改為存完整 `ReminderLog`（不再存 tuple），以支援狀態變更：

```python
class FakeReminderLogStore:
    """ReminderLogStore 的記憶體替身（測試用，不碰 DB）。

    reminder_log_id 由索引虛構、僅供排序與標記；created_at 沿用注入的 clock，
    因回應時間窗的判定需要真實的時間語意（不能再用索引虛構）。
    """

    def __init__(self, clock: Callable[[], datetime] | None = None) -> None:
        self._clock = clock or (lambda: datetime.now(timezone.utc))
        self._rows: list[ReminderLog] = []

    @property
    def recorded(self) -> list[tuple[str, str, str]]:
        """相容既有測試：只看 (elder_id, kind, content) 三元組。"""
        return [(r.elder_id, r.kind, r.content) for r in self._rows]

    def record(self, elder_id: str, kind: str, content: str) -> None:
        self._rows.append(
            ReminderLog(
                reminder_log_id=f"r{len(self._rows)}",
                elder_id=elder_id,
                kind=kind,
                content=content,
                created_at=self._clock().timestamp(),
                responded_at=None,
            )
        )

    def list_for_elder(self, elder_id: str) -> list[ReminderLog]:
        rows = [r for r in self._rows if r.elder_id == elder_id]
        return sorted(rows, key=lambda r: r.created_at, reverse=True)

    def list_for_range(self, elder_id: str, *, start: float, end: float) -> list[ReminderLog]:
        rows = [
            r for r in self._rows if r.elder_id == elder_id and start <= r.created_at < end
        ]
        return sorted(rows, key=lambda r: r.created_at)

    def mark_responded(self, elder_id: str, *, now: float, within_seconds: int) -> None:
        candidates = [
            (i, r)
            for i, r in enumerate(self._rows)
            if r.elder_id == elder_id
            and r.responded_at is None
            and now - within_seconds <= r.created_at <= now
        ]
        if not candidates:
            return
        i, row = max(candidates, key=lambda pair: pair[1].created_at)
        self._rows[i] = replace(row, responded_at=now)
```

檔頭 import 補 `from dataclasses import dataclass, replace` 與 `from datetime import datetime, timezone`。

`FakeReminderLogStore` 的建構子現在收 `clock`，合約測試的 fixture 要同步傳入固定時鐘：

```python
    return FakeReminderLogStore(clock=lambda: FIXED)
```

- [ ] **Step 5: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_reports_reminders_store_contract.py -v`
Expected: PASS（既有 3 個＋新增 3 個）

- [ ] **Step 6: 跑全套測試，確認沒打壞既有呼叫端**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: PASS。若有測試因 `FakeReminderLogStore(clock=...)` 建構子改變而失敗，補上時鐘參數（`recorded` 屬性已保留相容）。

- [ ] **Step 7: Commit**

```bash
git add src/kinsun/reports/reminders.py src/kinsun/db.py tests/test_reports_reminders_store_contract.py
git commit -m "feat(reports): reminder_logs 加 responded_at，記錄提醒有沒有被回應

反思若只讀逐字稿，等於讓模型憑主觀解讀猜「長輩喜不喜歡這樣」——沒有硬數據
當錨，學到的東西是幻想。提醒的回應率是騙不了人的訊號，但 reminder_logs 目前
只記「我推了什麼」，完全沒有「長輩有沒有理我」。

判定刻意做笨的：只用時間窗（提醒後 N 秒內長輩有發言即算回應），不做內容比對。
比對「這句是不是在回剛剛那則提醒」極易誤判，而反思只需要粗略的回應率訊號。
只標最近一則——長輩一句話不該同時回應早上與中午的兩則提醒。

DDL 走獨立遷移段：既有庫的 reminder_logs 早已存在，CREATE TABLE IF NOT EXISTS
對它是 no-op、不會生出 responded_at，故 ALTER 必須另立一段排在建表之後。

影響：ReminderLog 加欄位（預設 None，既有呼叫端不受影響）；FakeReminderLogStore
建構子多收 clock（回應時間窗需要真實時間語意），recorded 屬性保留相容。"
```

---

### Task 3: 反思設定

**Files:**
- Modify: `src/kinsun/config.py`（`Settings` 加 5 個欄位；`load_settings` 加 5 個讀取）
- Modify: `.env.example`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings.reflection_enabled: bool`、`reflection_lookback_days: int`、`reflection_min_observed_days: int`、`reflection_max_strategies: int`、`reflection_response_window_minutes: int`。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_config.py` 加：

```python
def test_reflection_settings_have_defaults():
    settings = load_settings({"DATABASE_URL": "postgresql://x"})
    assert settings.reflection_enabled is True
    assert settings.reflection_lookback_days == 7
    assert settings.reflection_min_observed_days == 3
    assert settings.reflection_max_strategies == 15
    assert settings.reflection_response_window_minutes == 60


def test_reflection_can_be_switched_off():
    settings = load_settings({"DATABASE_URL": "postgresql://x", "REFLECTION_ENABLED": "false"})
    assert settings.reflection_enabled is False
```

（若既有 `test_config.py` 的 `load_settings` 需要更多必填鍵，比照該檔既有測試的最小環境字典。）

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_config.py -k reflection -v`
Expected: FAIL — `AttributeError: 'Settings' object has no attribute 'reflection_enabled'`

- [ ] **Step 3: 改 `config.py`**

`Settings` dataclass 加（依既有欄位順序與風格擺放）：

```python
    reflection_enabled: bool
    reflection_lookback_days: int
    reflection_min_observed_days: int
    reflection_max_strategies: int
    reflection_response_window_minutes: int
```

`load_settings` 加：

```python
        # 每晚反思（spec 2026-07-14）：預設開——反思是自動主線行為，此旗標為緊急關閉開關。
        reflection_enabled=_parse_bool(env.get("REFLECTION_ENABLED", "true")),
        # 回顧天數：證據門檻要求「跨多天重複出現」，反思就必須看得到多天。
        reflection_lookback_days=int(env.get("REFLECTION_LOOKBACK_DAYS", "7")),
        # 證據門檻：一條守則至少要在幾天中被觀察到才成立（擋掉單日噪音）。
        reflection_min_observed_days=int(env.get("REFLECTION_MIN_OBSERVED_DAYS", "3")),
        # 守則上限＝注入 prompt 的條數上限；滿了必須指定取代對象。
        reflection_max_strategies=int(env.get("REFLECTION_MAX_STRATEGIES", "15")),
        # 提醒發出後多久內長輩有發言即算「已回應」。
        reflection_response_window_minutes=int(env.get("REFLECTION_RESPONSE_WINDOW_MINUTES", "60")),
```

- [ ] **Step 4: 補 `.env.example`**

```bash
# --- 每晚反思與策略記憶（spec 2026-07-14）---
# 反思總開關；預設開（反思為每晚自動主線行為，本旗標僅供異常時緊急停用）
REFLECTION_ENABLED=true
# 反思每晚回顧幾天的對話與提醒紀錄（證據門檻需要跨多天視野）
REFLECTION_LOOKBACK_DAYS=7
# 一條守則至少要在幾天中被觀察到才成立；低於此值一律丟棄（擋掉單日噪音）
REFLECTION_MIN_OBSERVED_DAYS=3
# 每位長輩生效中的守則上限，同時是注入 system prompt 的條數上限
REFLECTION_MAX_STRATEGIES=15
# 提醒發出後多久內長輩有發言即算「已回應」（分鐘）
REFLECTION_RESPONSE_WINDOW_MINUTES=60
```

- [ ] **Step 5: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/config.py .env.example tests/test_config.py
git commit -m "feat(config): 新增 REFLECTION_ 五個設定

每晚反思需要四個可調參數與一個緊急關閉開關。REFLECTION_ENABLED 預設 true——
反思是自動的主線行為，不是選配；此旗標存在的唯一理由是反思行為異常時能立刻
停用，而不必回滾程式碼。

REFLECTION_LOOKBACK_DAYS=7 與 REFLECTION_MIN_OBSERVED_DAYS=3 是一組：證據門檻
要求守則「跨多天重複出現」，反思就必須看得到多天，兩者必須一起調。

影響：Settings 加 5 個欄位；.env.example 同步。尚未有讀取端。"
```

---

### Task 4: 進站訊息標記提醒已回應

> **執行順序**：本任務必須排在 Task 3（反思設定）之後——`composition.py` 要讀 `settings.reflection_response_window_minutes`。

**Files:**
- Modify: `src/kinsun/pipeline.py`（建構子加 `reminder_logs`／`response_window_seconds`；`_process_transcribed` 加標記）
- Modify: `src/kinsun/composition.py`（把 `reminder_logs` 傳進 pipeline）
- Test: `tests/test_pipeline.py`

**Interfaces:**
- Consumes: Task 2 的 `ReminderLogStore.mark_responded(elder_id, *, now, within_seconds)`；Task 3 的 `Settings.reflection_response_window_minutes`。
- Produces: 無新介面（行為變更）。

- [ ] **Step 1: 寫失敗的測試**

在 `tests/test_pipeline.py` 加（依既有檔案的 pipeline 建構 helper 調整參數；若既有測試用 fixture 組 pipeline，沿用之並補 `reminder_logs=`）：

```python
def test_incoming_message_marks_a_recent_reminder_as_responded():
    reminders = FakeReminderLogStore(clock=lambda: FIXED)
    reminders.record("e1", "medication", "早上用藥：A")
    pipeline = build_pipeline(reminder_logs=reminders, response_window_seconds=3600)

    pipeline.process_text("我吃過了", elder_id="e1")

    assert reminders.list_for_elder("e1")[0].responded_at is not None


def test_marking_failure_does_not_break_the_reply():
    class Exploding:
        def mark_responded(self, elder_id, *, now, within_seconds):
            raise RuntimeError("db down")

    pipeline = build_pipeline(reminder_logs=Exploding(), response_window_seconds=3600)

    result = pipeline.process_text("你好", elder_id="e1")

    assert result.transcript == "你好"
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_pipeline.py -k responded -v`
Expected: FAIL — `TypeError: __init__() got an unexpected keyword argument 'reminder_logs'`

- [ ] **Step 3: 改 `pipeline.py`**

建構子加兩個具預設值的參數（既有呼叫端不傳＝不標記，行為不變）：

```python
        reminder_logs: ReminderLogStore | None = None,
        response_window_seconds: int = 3600,
```

```python
        self._reminder_logs = reminder_logs
        self._response_window_seconds = response_window_seconds
```

`_process_transcribed` 開頭（危急偵測之前）加：

```python
        self._mark_reminder_responded(elder_id)
```

新增私有方法：

```python
    def _mark_reminder_responded(self, elder_id: str) -> None:
        """長輩開口＝可能在回應剛推的提醒（時間窗判定，不做內容比對）。

        這是反思的行為訊號來源；標記失敗不可影響對話。
        """
        if self._reminder_logs is None:
            return
        try:
            self._reminder_logs.mark_responded(
                elder_id,
                now=time.time(),
                within_seconds=self._response_window_seconds,
            )
        except Exception:  # noqa: BLE001 - 訊號落庫失敗不可中斷對話
            logger.warning("提醒回應標記失敗 elder=%s", elder_id)
```

檔頭補 `from kinsun.reports.reminders import ReminderLogStore`。

> 注意：`self._timer` 預設是 `time.monotonic`（僅供量延遲，非牆鐘時間），**不可**拿來當 `now`。此處用 `time.time()`（epoch 秒），與 `reminder_logs.created_at` 的語意一致。

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_pipeline.py -v`
Expected: PASS

- [ ] **Step 5: 接上 `composition.py`**

在組 pipeline 之處把已建好的 `reminder_logs` store 與設定傳入：

```python
        reminder_logs=reminder_logs,
        response_window_seconds=settings.reflection_response_window_minutes * 60,
```

（`reflection_response_window_minutes` 由 Task 3 加入 `Settings`，故 Task 3 必須先完成。）

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/pipeline.py src/kinsun/composition.py tests/test_pipeline.py
git commit -m "feat(pipeline): 長輩開口時標記提醒已回應

提醒的回應率是反思唯一騙不了人的訊號，但沒有人在寫它。管線是長輩每一句話
的必經之路，標記的正確位置就在這裡。

用 time.time() 而非 self._timer：後者預設是 time.monotonic，只能量延遲、不是
牆鐘時間，拿來跟 reminder_logs.created_at（epoch 秒）比較會得到垃圾。

reminder_logs 為選填（預設 None＝不標記），既有呼叫端與測試不受影響；標記失敗
只記 warning，絕不中斷對話——訊號可以掉，長輩的話不能掉。

影響：pipeline 建構子加兩個具預設值的參數；composition 傳入既有的 reminder_logs。"
```

---

### Task 5: 三道濾網（`strategies/policy.py`）

**Files:**
- Create: `src/kinsun/strategies/policy.py`
- Test: `tests/test_strategies_policy.py`

**Interfaces:**
- Consumes: Task 1 的 `STRATEGY_CATEGORIES`。
- Produces: `Candidate` dataclass（`content`／`category`／`evidence`／`observed_days`／`supersedes`）；`is_admissible(candidate: Candidate, *, min_observed_days: int, adopted_count: int, max_strategies: int, adopted_ids: set[str]) -> str | None`（回傳拒絕理由，`None`＝通過）。

**為什麼**：這是取代人審的那道防線。**不能信任模型會乖乖遵守 prompt 裡的禁令**——prompt 說「不准產出用藥相關守則」是第一道，程式碼過濾是第二道。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_strategies_policy.py`：

```python
"""三道濾網：醫療黑名單、證據門檻、上限與取代。

這是守則自動生效的安全基礎——人審被拿掉之後，擋住壞守則的就只剩這裡。
"""

from __future__ import annotations

import pytest

from kinsun.strategies.models import STRATEGY_CATEGORY_ROUTINE, STRATEGY_CATEGORY_TONE
from kinsun.strategies.policy import Candidate, is_admissible

OK = dict(min_observed_days=3, adopted_count=0, max_strategies=15, adopted_ids=set())


def _candidate(**overrides) -> Candidate:
    base = dict(
        content="早上七點半再問候",
        category=STRATEGY_CATEGORY_ROUTINE,
        evidence="連三天八點的問候都沒回",
        observed_days=3,
        supersedes=None,
    )
    return Candidate(**{**base, **overrides})


def test_a_well_formed_candidate_passes():
    assert is_admissible(_candidate(), **OK) is None


@pytest.mark.parametrize(
    "content",
    [
        "不要再提醒她吃藥",
        "她的降血壓藥可以減量",
        "不用叫她去看醫生",
        "她說喘不過氣時不用緊張",
    ],
)
def test_medical_content_is_always_rejected(content):
    reason = is_admissible(_candidate(content=content), **OK)
    assert reason is not None
    assert "醫療" in reason


def test_a_category_outside_the_whitelist_is_rejected():
    reason = is_admissible(_candidate(category="medication"), **OK)
    assert reason is not None
    assert "分類" in reason


def test_evidence_below_the_threshold_is_rejected():
    reason = is_admissible(_candidate(observed_days=2), **OK)
    assert reason is not None
    assert "證據" in reason


def test_at_capacity_a_candidate_without_supersedes_is_rejected():
    reason = is_admissible(
        _candidate(),
        min_observed_days=3,
        adopted_count=15,
        max_strategies=15,
        adopted_ids={"s1"},
    )
    assert reason is not None
    assert "上限" in reason


def test_at_capacity_a_candidate_superseding_a_real_strategy_passes():
    reason = is_admissible(
        _candidate(supersedes="s1"),
        min_observed_days=3,
        adopted_count=15,
        max_strategies=15,
        adopted_ids={"s1"},
    )
    assert reason is None


def test_superseding_an_unknown_strategy_is_rejected():
    reason = is_admissible(
        _candidate(category=STRATEGY_CATEGORY_TONE, supersedes="does-not-exist"),
        min_observed_days=3,
        adopted_count=15,
        max_strategies=15,
        adopted_ids={"s1"},
    )
    assert reason is not None
    assert "取代" in reason
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_policy.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.strategies.policy'`

- [ ] **Step 3: 實作**

`src/kinsun/strategies/policy.py`：

```python
"""守則的三道濾網：醫療黑名單、證據門檻、上限與取代。

守則自動生效（無人審），擋住壞守則的就只剩這裡。第一道尤其重要——反思的
prompt 雖已明文禁止產出醫療類守則，但**不能信任模型會乖乖聽話**，程式碼必須
自己再擋一次。金孫可以學會不叫長輩阿婆，但永遠不准學會不提醒她吃藥。

本詞表刻意與 rag/answer_policy.py 的 _MEDICAL_ACTION_TERMS 分開維護：兩者用途
不同（那邊擋 RAG 回答、這邊擋守則），共用常數會讓兩邊互相牽動。
"""

from __future__ import annotations

from dataclasses import dataclass

from kinsun.strategies.models import STRATEGY_CATEGORIES

# 醫療動作詞：守則內容命中任一即丟棄。寧可錯殺，不可放行。
_MEDICAL_TERMS = (
    "藥",
    "劑量",
    "服用",
    "吃藥",
    "用藥",
    "看醫生",
    "就醫",
    "回診",
    "門診",
    "掛號",
    "醫院",
    "診斷",
    "病情",
    "血壓",
    "血糖",
    "疼痛",
    "喘",
    "跌倒",
    "急診",
    "救護車",
    "危急",
    "求救",
)


@dataclass(frozen=True)
class Candidate:
    """反思產出的候選守則（尚未過濾）。"""

    content: str
    category: str
    evidence: str
    observed_days: int
    supersedes: str | None


def is_admissible(
    candidate: Candidate,
    *,
    min_observed_days: int,
    adopted_count: int,
    max_strategies: int,
    adopted_ids: set[str],
) -> str | None:
    """通過回 None；未通過回一句中文拒絕理由（供 log）。"""
    if candidate.category not in STRATEGY_CATEGORIES:
        return f"分類不在白名單內：{candidate.category}"
    hit = next((t for t in _MEDICAL_TERMS if t in candidate.content), None)
    if hit is not None:
        return f"醫療相關內容不得成為守則（命中「{hit}」）"
    if candidate.observed_days < min_observed_days:
        return f"證據不足：只觀察到 {candidate.observed_days} 天，需 {min_observed_days} 天"
    if candidate.supersedes is not None and candidate.supersedes not in adopted_ids:
        return f"要取代的守則不存在或未生效：{candidate.supersedes}"
    if adopted_count >= max_strategies and candidate.supersedes is None:
        return f"已達守則上限（{max_strategies}）且未指定取代對象"
    return None
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_policy.py -v`
Expected: PASS（11 個，含 4 個 parametrize）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/strategies/policy.py tests/test_strategies_policy.py
git commit -m "feat(strategies): 新增守則三道濾網（醫療黑名單、證據門檻、上限汰換）

人審被拿掉之後，擋住壞守則的就只剩這裡。反思的 prompt 雖已明文禁止產出醫療類
守則，但不能信任模型會乖乖聽話——程式碼必須自己再擋一次。

證據門檻（observed_days ≥ 3）擋的是最危險的失效模式：長輩某天心情差不想聊，
金孫就永久學成「她不愛聊這個」。單日噪音無法通過門檻，只有穩定的模式才會沉澱。

醫療詞表刻意不與 rag/answer_policy.py 共用：兩者用途不同（那邊擋 RAG 回答、這邊
擋守則），共用常數會讓兩邊互相牽動。詞表寧可錯殺不可放行。

影響：新增純函式模組，無副作用、無相依 DB。尚未有呼叫端。"
```

---

### Task 6: 反思核心（`strategies/reflection.py`）

**Files:**
- Create: `src/kinsun/strategies/reflection.py`
- Test: `tests/test_strategies_reflection.py`

**Interfaces:**
- Consumes: Task 1 的 `StrategyStore`／`Strategy`；Task 2 的 `ReminderLogStore.list_for_range`；Task 5 的 `Candidate`／`is_admissible`；既有的 `MemoryStore.list_for_range`、`memory.shortterm.previous_day_bounds`。
- Produces: `REFLECTION_PROMPT`；`reflect_days(elder_id: str, *, short_term, reminder_logs, strategies, reflector, clock, lookback_days: int, min_observed_days: int, max_strategies: int) -> None`。`reflector` 只需有 `generate(system_prompt: str, messages: list[Message]) -> str`（與 `summarize_day` 的 `summarizer` 同一形狀）。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_strategies_reflection.py`：

```python
"""每晚反思：讀過去 N 天的逐字稿與提醒回應，產出守則。

反思讀「多天」而非只讀昨天——證據門檻要求守則跨多天重複出現，反思就必須看得到
多天。這是它與 summarize_day（只讀昨天）的關鍵差異，不可照抄。
"""

from __future__ import annotations

import json
from datetime import datetime, timedelta, timezone

from kinsun.llm import Message
from kinsun.memory.shortterm import FakeMemoryStore
from kinsun.reports.reminders import FakeReminderLogStore
from kinsun.strategies.models import (
    STRATEGY_CATEGORY_ROUTINE,
    STRATEGY_STATUS_ADOPTED,
)
from kinsun.strategies.reflection import reflect_days
from kinsun.strategies.store import FakeStrategyStore

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 14, 3, tzinfo=TPE)  # 凌晨三點跑批次
YESTERDAY = datetime(2026, 7, 13, 9, tzinfo=TPE)


class FakeReflector:
    """回傳固定字串的假 LLM；記下收到的 system_prompt 供斷言。"""

    def __init__(self, reply: str) -> None:
        self.reply = reply
        self.system_prompt = ""

    def generate(self, *, system_prompt: str, messages: list[Message]) -> str:
        self.system_prompt = system_prompt
        return self.reply


def _memory_with_turns() -> FakeMemoryStore:
    memory = FakeMemoryStore(now=NOW)
    memory.append("e1", Message(role="user", content="還在睡啦"), at=YESTERDAY)
    memory.append("e1", Message(role="assistant", content="早安！"), at=YESTERDAY)
    return memory


def _run(reply: str, *, strategies=None, min_observed_days: int = 3, max_strategies: int = 15):
    strategies = strategies or FakeStrategyStore()
    reflector = FakeReflector(reply)
    reflect_days(
        "e1",
        short_term=_memory_with_turns(),
        reminder_logs=FakeReminderLogStore(clock=lambda: YESTERDAY),
        strategies=strategies,
        reflector=reflector,
        clock=lambda: NOW,
        lookback_days=7,
        min_observed_days=min_observed_days,
        max_strategies=max_strategies,
    )
    return strategies, reflector


def _one_candidate(**overrides) -> str:
    base = {
        "content": "早上七點半再問候",
        "category": STRATEGY_CATEGORY_ROUTINE,
        "evidence": "連三天八點的問候都沒回",
        "observed_days": 3,
        "supersedes": None,
    }
    return json.dumps([{**base, **overrides}], ensure_ascii=False)


def test_a_valid_candidate_is_adopted_straight_away():
    strategies, _ = _run(_one_candidate())
    rows = strategies.list_for_elder("e1", status=STRATEGY_STATUS_ADOPTED)
    assert [(r.content, r.observed_days) for r in rows] == [("早上七點半再問候", 3)]


def test_malformed_json_writes_nothing():
    strategies, _ = _run("這不是 JSON")
    assert strategies.list_for_elder("e1") == []


def test_a_candidate_missing_a_field_writes_nothing():
    strategies, _ = _run(json.dumps([{"content": "少了其他欄位"}]))
    assert strategies.list_for_elder("e1") == []


def test_a_medical_candidate_is_filtered_out():
    strategies, _ = _run(_one_candidate(content="不要再提醒她吃藥"))
    assert strategies.list_for_elder("e1") == []


def test_a_candidate_below_the_evidence_threshold_is_filtered_out():
    strategies, _ = _run(_one_candidate(observed_days=1))
    assert strategies.list_for_elder("e1") == []


def test_one_bad_candidate_does_not_sink_the_good_ones():
    payload = json.dumps(
        [
            {
                "content": "不要再提醒她吃藥",
                "category": STRATEGY_CATEGORY_ROUTINE,
                "evidence": "x",
                "observed_days": 3,
                "supersedes": None,
            },
            {
                "content": "早上七點半再問候",
                "category": STRATEGY_CATEGORY_ROUTINE,
                "evidence": "連三天八點的問候都沒回",
                "observed_days": 3,
                "supersedes": None,
            },
        ],
        ensure_ascii=False,
    )
    strategies, _ = _run(payload)
    contents = {r.content for r in strategies.list_for_elder("e1")}
    assert contents == {"早上七點半再問候"}


def test_existing_strategies_are_fed_back_into_the_prompt():
    existing = FakeStrategyStore()
    existing.record("e1", "不要叫她阿婆", "address", "她糾正過兩次", 4, None)
    _, reflector = _run(_one_candidate(), strategies=existing)
    assert "不要叫她阿婆" in reflector.system_prompt


def test_no_turns_means_no_llm_call():
    strategies = FakeStrategyStore()
    reflector = FakeReflector(_one_candidate())
    reflect_days(
        "nobody",
        short_term=FakeMemoryStore(now=NOW),
        reminder_logs=FakeReminderLogStore(clock=lambda: YESTERDAY),
        strategies=strategies,
        reflector=reflector,
        clock=lambda: NOW,
        lookback_days=7,
        min_observed_days=3,
        max_strategies=15,
    )
    assert reflector.system_prompt == ""
    assert strategies.list_for_elder("nobody") == []
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_reflection.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.strategies.reflection'`

- [ ] **Step 3: 實作**

`src/kinsun/strategies/reflection.py`：

```python
"""每晚反思：讀過去 N 天的互動，沉澱出「這位長輩的相處之道」。

與 reports/summaries.py 的 summarize_day 是姊妹批次，但有兩個關鍵差異：
1. 讀「過去 N 天」而非只讀昨天——證據門檻要求守則跨多天重複出現，反思就必須
   看得到多天。不可照抄 previous_day()。
2. 產出寫回系統自己（strategies 表 → 注入 prompt），而非寫給家屬看。
   摘要是報告，反思是學習。
"""

from __future__ import annotations

import json
import logging
from collections.abc import Callable
from datetime import datetime

from kinsun.llm import Message
from kinsun.memory.shortterm import previous_day_bounds
from kinsun.strategies.models import STRATEGY_STATUS_ADOPTED, Strategy
from kinsun.strategies.policy import Candidate, is_admissible
from kinsun.strategies.store import StrategyStore

logger = logging.getLogger("kinsun.strategies.reflection")

_REQUIRED_FIELDS = ("content", "category", "evidence", "observed_days")

REFLECTION_PROMPT = (
    "你是「金孫」——一位陪伴長輩的 AI 夥伴——的反思模組。以下是這位長者過去幾天"
    "與金孫的對話，以及系統推送的提醒與長者是否回應的紀錄。\n\n"
    "請找出「與這位長者相處時應該遵守的守則」，讓金孫下次做得更好。\n\n"
    "【嚴格限制，違反者一律作廢】\n"
    "1. 只能產出四類守則：address（稱呼）、tone（語氣與長短）、routine（作息與時間）、"
    "topic（話題偏好）。\n"
    "2. 絕對禁止產出任何涉及用藥、劑量、就醫、回診、危急判斷、健康數值的守則。"
    "金孫的用藥提醒與安全機制不是你能調整的對象。\n"
    "3. 每條守則必須有跨多天、重複出現的證據。單一天的一次觀察不算數"
    "（長者可能只是那天心情不好），此類一律不要產出。\n"
    "4. 已經存在的守則不要重複產出。\n\n"
    "【回傳格式】只回傳 JSON 陣列，不要任何其他文字或 markdown 標記。每個元素：\n"
    '{"content": "一句話的守則", "category": "四類之一", '
    '"evidence": "你依據的觀察", "observed_days": 這個模式在幾天中出現過的整數, '
    '"supersedes": null 或要取代的既有守則 id}\n'
    "沒有值得沉澱的守則時，回傳空陣列 []。\n"
)


def _format_reminders(logs) -> str:
    if not logs:
        return "（這段期間沒有推送任何提醒）"
    lines = []
    for log in logs:
        answered = "有回應" if log.responded_at is not None else "沒有回應"
        lines.append(f"- [{log.kind}]{log.content}（長者{answered}）")
    return "\n".join(lines)


def _format_adopted(rows: list[Strategy], max_strategies: int) -> str:
    if not rows:
        return f"（目前沒有任何守則；上限 {max_strategies} 條）"
    lines = [f"- id={r.strategy_id}：{r.content}" for r in rows]
    body = "\n".join(lines)
    return (
        f"目前已有 {len(rows)} 條守則（上限 {max_strategies} 條）：\n{body}\n"
        "若已達上限而你認為新守則更重要，必須在 supersedes 欄填入要取代的那條 id；"
        "未指定取代對象的新守則會被丟棄。"
    )


def _parse(reply: str) -> list[Candidate] | None:
    """解析 LLM 回傳；任一環節不合格式即回 None（整批丟棄，不寫入任何守則）。"""
    text = reply.strip()
    if text.startswith("```"):  # 模型偶爾會包 markdown code fence
        text = text.strip("`")
        text = text[4:] if text.lower().startswith("json") else text
    try:
        raw = json.loads(text)
    except json.JSONDecodeError:
        return None
    if not isinstance(raw, list):
        return None
    out = []
    for item in raw:
        if not isinstance(item, dict) or any(f not in item for f in _REQUIRED_FIELDS):
            return None
        try:
            observed_days = int(item["observed_days"])
        except (TypeError, ValueError):
            return None
        out.append(
            Candidate(
                content=str(item["content"]),
                category=str(item["category"]),
                evidence=str(item["evidence"]),
                observed_days=observed_days,
                supersedes=item.get("supersedes") or None,
            )
        )
    return out


def reflect_days(
    elder_id: str,
    *,
    short_term,
    reminder_logs,
    strategies: StrategyStore,
    reflector,
    clock: Callable[[], datetime],
    lookback_days: int,
    min_observed_days: int,
    max_strategies: int,
) -> None:
    now = clock()
    _, end = previous_day_bounds(now)  # 迄點＝今日零時；只反思已完整結束的日子
    start = end - lookback_days * 86400.0  # 台灣無日光節約，一天固定 86400 秒
    turns = short_term.list_for_range(elder_id, start=start, end=end)
    if not turns:
        return

    logs = reminder_logs.list_for_range(elder_id, start=start, end=end)
    adopted = strategies.list_for_elder(elder_id, status=STRATEGY_STATUS_ADOPTED)

    system_prompt = (
        REFLECTION_PROMPT
        + "\n【這段期間的提醒與回應】\n"
        + _format_reminders(logs)
        + "\n\n【目前已生效的守則】\n"
        + _format_adopted(adopted, max_strategies)
    )
    reply = reflector.generate(system_prompt=system_prompt, messages=turns)

    candidates = _parse(reply)
    if candidates is None:
        logger.warning("反思回傳格式不合，整批丟棄 elder=%s", elder_id)
        return

    adopted_ids = {r.strategy_id for r in adopted}
    adopted_count = len(adopted)
    for candidate in candidates:
        reason = is_admissible(
            candidate,
            min_observed_days=min_observed_days,
            adopted_count=adopted_count,
            max_strategies=max_strategies,
            adopted_ids=adopted_ids,
        )
        if reason is not None:
            logger.warning("守則被濾網擋下 elder=%s 理由=%s", elder_id, reason)
            continue
        strategies.record(
            elder_id,
            candidate.content,
            candidate.category,
            candidate.evidence,
            candidate.observed_days,
            candidate.supersedes,
        )
        # 同一批內的後續候選要看到最新的容量與 id 集合（取代會讓舊 id 失效）。
        if candidate.supersedes is not None:
            adopted_ids.discard(candidate.supersedes)
        else:
            adopted_count += 1


# Message 供型別提示與測試匯入；此處不直接使用。
__all__ = ["REFLECTION_PROMPT", "reflect_days", "Message"]
```

> `Message` 匯入是為了讓 `reflector.generate` 的 `messages` 型別語意明確；若 lint 抱怨未使用，改為在函式簽章加型別註記 `messages: list[Message]` 或移除該匯入與 `__all__`。

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_reflection.py -v`
Expected: PASS（8 個）

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/strategies/reflection.py tests/test_strategies_reflection.py
git commit -m "feat(strategies): 新增每晚反思，從過去七天的互動沉澱守則

閉環的核心。讀過去 N 天的逐字稿與提醒回應紀錄，加上目前生效中的守則，讓 LLM
產出「與這位長者相處應遵守的守則」，過三道濾網後直接生效。

與 summarize_day 是姊妹批次，但兩個關鍵差異不可照抄：
1. 讀「過去 N 天」而非 previous_day()——證據門檻要求守則跨多天重複出現，反思就
   必須看得到多天。這是整個設計成立的前提。
2. 產出寫回系統自己（strategies → 注入 prompt），而非寫給家屬看。摘要是報告，
   反思是學習——這是金孫第一條把觀測結果送回自身決策的路徑。

把生效中守則餵回 prompt 解決了兩件事：重疊讀取造成的重複產出（明令不要重複），
以及矛盾守則的汰換（要求指定 supersedes）。

格式不合一律整批丟棄——寧可今晚不學，不可學進垃圾。單條被濾網擋下則只丟該條，
其餘照寫。

影響：新增反思模組。尚未掛進排程（Task 8），對現行行為零影響。"
```

---

### Task 7: 守則注入 system prompt（`strategies/facts.py`）

**Files:**
- Create: `src/kinsun/strategies/facts.py`
- Modify: `src/kinsun/composition.py`（組 `PgStrategyStore`、掛 `StrategyFacts`）
- Test: `tests/test_strategies_facts.py`

**Interfaces:**
- Consumes: Task 1 的 `StrategyStore`；既有的 `memory.models.FactSection`、`memory.recall.FactProvider`。
- Produces: `StrategyFacts(strategies: StrategyStore, *, max_strategies: int)`，實作 `facts(elder_id) -> FactSection | None`。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_strategies_facts.py`：

```python
"""守則注入 system prompt：只有生效中的守則會進去，且永不凌駕安全提醒。"""

from __future__ import annotations

from kinsun.strategies.facts import StrategyFacts
from kinsun.strategies.models import STRATEGY_CATEGORY_ADDRESS, STRATEGY_CATEGORY_TONE
from kinsun.strategies.store import FakeStrategyStore


def test_no_strategies_yields_no_section():
    assert StrategyFacts(FakeStrategyStore(), max_strategies=15).facts("e1") is None


def test_adopted_strategies_become_section_items():
    store = FakeStrategyStore()
    store.record("e1", "不要叫她阿婆", STRATEGY_CATEGORY_ADDRESS, "她糾正過兩次", 4, None)
    store.record("e1", "回話簡短些", STRATEGY_CATEGORY_TONE, "長句多半沒回", 3, None)

    section = StrategyFacts(store, max_strategies=15).facts("e1")

    assert set(section.items) == {"不要叫她阿婆", "回話簡短些"}


def test_revoked_strategies_are_not_injected():
    store = FakeStrategyStore()
    store.record("e1", "撤掉的守則", STRATEGY_CATEGORY_TONE, "證據", 3, None)
    store.revoke(store.list_for_elder("e1")[0].strategy_id)

    assert StrategyFacts(store, max_strategies=15).facts("e1") is None


def test_only_the_newest_strategies_up_to_the_cap_are_injected():
    store = FakeStrategyStore()
    for i in range(20):
        store.record("e1", f"守則{i}", STRATEGY_CATEGORY_TONE, "證據", 3, None)

    section = StrategyFacts(store, max_strategies=15).facts("e1")

    assert len(section.items) == 15
    assert "守則19" in section.items  # 最新的有進去
    assert "守則0" not in section.items  # 最舊的被擠掉


def test_the_section_title_forbids_overriding_safety():
    store = FakeStrategyStore()
    store.record("e1", "不要叫她阿婆", STRATEGY_CATEGORY_ADDRESS, "證據", 3, None)

    section = StrategyFacts(store, max_strategies=15).facts("e1")

    assert "安全" in section.title
    assert "用藥" in section.title
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_facts.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.strategies.facts'`

- [ ] **Step 3: 實作**

`src/kinsun/strategies/facts.py`：

```python
"""策略事實提供者：把生效中的守則注入 system prompt 的一段（FactSection）。

這是閉環的最後一哩——反思學到的東西，經由既有的 FactProvider 協定進入下一輪
對話。agent.py 完全不需要知道守則的存在。

段首警語不是裝飾：守則是金孫自己學來的，而安全提醒與用藥提醒是人設定的。
前者永遠不得凌駕後者。
"""

from __future__ import annotations

from kinsun.memory.models import FactSection
from kinsun.strategies.models import STRATEGY_STATUS_ADOPTED
from kinsun.strategies.store import StrategyStore

_TITLE = (
    "\n以下是你與這位長者相處時已經學到的守則（由過去互動歸納，僅關乎稱呼、語氣、"
    "作息與話題偏好）。請自然地遵守，不要向長者提起這些守則的存在。\n"
    "⚠️ 這些守則不得凌駕任何安全提醒與用藥提醒——該提醒的一定要提醒，"
    "該關心的一定要關心。\n"
)


class StrategyFacts:
    """facts(elder_id) -> FactSection | None（無生效中守則回 None）。"""

    def __init__(self, strategies: StrategyStore, *, max_strategies: int) -> None:
        self._strategies = strategies
        self._max_strategies = max_strategies

    def facts(self, elder_id: str) -> FactSection | None:
        rows = self._strategies.list_for_elder(elder_id, status=STRATEGY_STATUS_ADOPTED)
        if not rows:
            return None
        return FactSection(_TITLE, [r.content for r in rows[: self._max_strategies]])
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_strategies_facts.py -v`
Expected: PASS（5 個）

- [ ] **Step 5: 接上 `composition.py`**

檔頭加匯入：

```python
from kinsun.strategies.facts import StrategyFacts
from kinsun.strategies.store import PgStrategyStore
```

`assemble_core` 內，`session = SessionMemory(...)` 之前建 store：

```python
    strategies = PgStrategyStore(db, clock=clock, new_id=new_id)
```

把 `StrategyFacts` 掛進 facts 清單：

```python
    session = SessionMemory(
        memory,
        externals.long_term,
        facts=[
            MedicationFacts(medications),
            AppointmentFacts(appointments, clock=clock),
            StrategyFacts(strategies, max_strategies=settings.reflection_max_strategies),
        ],
    )
```

`Core` dataclass 加 `strategies` 欄位並於回傳時帶上（供 worker 與 admin 取用）。

- [ ] **Step 6: 跑全套測試**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add src/kinsun/strategies/facts.py src/kinsun/composition.py tests/test_strategies_facts.py
git commit -m "feat(strategies): 守則注入 system prompt，閉環的最後一哩

反思學到的守則經由既有的 FactProvider 協定進入下一輪對話——agent.py 完全不需要
知道守則的存在，核心迴圈零改動。這是選 FactProvider 這個接縫的全部理由。

段首警語不是裝飾：守則是金孫自己學來的，安全提醒與用藥提醒是人設定的，前者永遠
不得凌駕後者。這句話會出現在每一次注入的 prompt 裡。

上限在注入端也再擋一次（取最新 15 條）：即使資料庫因故存了更多生效中的守則，
prompt 長度仍有硬上限，不會變貴變慢。

影響：composition 的 facts 清單多第三個提供者；Core 多帶 strategies store。
守則表目前是空的，因此實際注入內容為空——反思尚未掛進排程（Task 8）。"
```

---

### Task 8: 掛進每晚自動批次

**Files:**
- Modify: `src/kinsun/scheduler/worker.py`（`build_jobs` 取 `strategies`；`run_one` 加第三步）
- Test: `tests/test_scheduler_worker.py`

**Interfaces:**
- Consumes: Task 6 的 `reflect_days`；Task 7 的 `Core.strategies`；Task 4 的 5 個設定。
- Produces: 無新介面（行為變更）。

**關鍵**：`run_one` 由 `build_consolidation_job` 驅動，**該 job 本來就是每晚 cron 自動執行**。反思因此自動成為主線行為，不需新增 cron。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_scheduler_worker.py` 加（依既有檔的 `build_jobs` 測試 helper 調整）：

```python
def test_run_one_reflects_after_summarising(monkeypatch):
    calls = []
    monkeypatch.setattr(
        "kinsun.scheduler.worker.reflect_days",
        lambda elder_id, **kw: calls.append(elder_id),
    )
    settings = _settings(reflection_enabled=True)
    jobs = build_jobs(settings, _core(), clock=lambda: NOW)
    _run_consolidation_job(jobs, elder_id="e1")
    assert calls == ["e1"]


def test_reflection_can_be_switched_off(monkeypatch):
    calls = []
    monkeypatch.setattr(
        "kinsun.scheduler.worker.reflect_days",
        lambda elder_id, **kw: calls.append(elder_id),
    )
    settings = _settings(reflection_enabled=False)
    jobs = build_jobs(settings, _core(), clock=lambda: NOW)
    _run_consolidation_job(jobs, elder_id="e1")
    assert calls == []


def test_reflection_failure_does_not_break_the_batch(monkeypatch):
    def explode(elder_id, **kw):
        raise RuntimeError("gemini down")

    monkeypatch.setattr("kinsun.scheduler.worker.reflect_days", explode)
    settings = _settings(reflection_enabled=True)
    jobs = build_jobs(settings, _core(), clock=lambda: NOW)
    _run_consolidation_job(jobs, elder_id="e1")  # 不應拋出
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_scheduler_worker.py -k reflect -v`
Expected: FAIL — `AttributeError: module 'kinsun.scheduler.worker' has no attribute 'reflect_days'`

- [ ] **Step 3: 改 `worker.py`**

檔頭加匯入：

```python
from kinsun.strategies.reflection import reflect_days
```

`build_jobs` 內取出 store（在 `summaries = core.summaries` 附近）：

```python
    strategies = core.strategies
```

`run_one` 加第三步：

```python
    def run_one(elder_id: str) -> None:
        run_consolidation(
            elder_id,
            short_term=memory,
            long_term=long_term,
            log=consolidation_log,
            now=clock(),
        )
        try:
            summarize_day(
                elder_id,
                short_term=memory,
                summarizer=gemini,
                summaries=summaries,
                clock=clock,
                risk_events=risk_events,
            )
        except Exception:  # noqa: BLE001 - 摘要失敗不影響整理與其他長輩
            logger.warning("對話摘要失敗 elder=%s", elder_id)
        # 每晚反思（spec 2026-07-14）：與摘要共用 GEMINI_MODEL_SUMMARY 這顆模型。
        # 反思失敗不影響整理與摘要——三者互不拖累。
        if settings.reflection_enabled:
            try:
                reflect_days(
                    elder_id,
                    short_term=memory,
                    reminder_logs=reminder_logs,
                    strategies=strategies,
                    reflector=gemini,
                    clock=clock,
                    lookback_days=settings.reflection_lookback_days,
                    min_observed_days=settings.reflection_min_observed_days,
                    max_strategies=settings.reflection_max_strategies,
                )
            except Exception:  # noqa: BLE001 - 反思失敗不影響整理與摘要
                logger.warning("每晚反思失敗 elder=%s", elder_id)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_scheduler_worker.py -v`
Expected: PASS

- [ ] **Step 5: 跑全套測試**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/scheduler/worker.py tests/test_scheduler_worker.py
git commit -m "feat(scheduler): 每晚自動反思，閉環正式接通

反思掛進 run_one——那是既有的夜間批次，已經在跑長期記憶整理與家屬摘要，由
build_consolidation_job 每晚 cron 自動觸發。反思因此是自動的主線行為，不是
手動工具；/admin 的手動觸發只是附帶能力（admin 遍歷同一份 jobs 清單）。

不新增 cron、不新增 worker、不新增模型設定（與摘要共用 GEMINI_MODEL_SUMMARY）。

三個批次互不拖累：反思失敗不影響長期記憶整理與家屬摘要，反之亦然。單一長輩的
失敗不影響其他長輩。

影響：金孫從今晚開始會學習。第一條守則需要三天以上的重複證據才會生成，因此
不會在啟用當晚就出現。"
```

---

### Task 9: 後台檢視與撤銷

**Files:**
- Create: `src/kinsun/web/routers/admin_strategies.py`
- Modify: `src/kinsun/web/app.py`（掛 router；依既有 router 註冊方式）
- Test: `tests/test_web_admin_strategies.py`

**Interfaces:**
- Consumes: Task 1 的 `StrategyStore.list_for_status`／`revoke`；既有的 `web/routers/deps.py` 金鑰驗證與 `web/envelope.py` 的 `ok()`。
- Produces: `GET /admin/strategies?status=adopted`、`PATCH /admin/strategies/{strategy_id}`。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_web_admin_strategies.py`：

```python
"""後台守則檢視與撤銷：角色是事後撤銷（opt-out），不是事前批准。"""

from __future__ import annotations

from kinsun.strategies.models import (
    STRATEGY_CATEGORY_ADDRESS,
    STRATEGY_STATUS_ADOPTED,
    STRATEGY_STATUS_REVOKED,
)
from kinsun.strategies.store import FakeStrategyStore

# client／admin 金鑰的組法沿用既有 tests/test_web_admin*.py 的 fixture。


def test_list_returns_adopted_strategies(admin_client, strategies):
    strategies.record("e1", "不要叫她阿婆", STRATEGY_CATEGORY_ADDRESS, "她糾正過兩次", 4, None)

    res = admin_client.get("/admin/strategies?status=adopted")

    assert res.status_code == 200
    items = res.json()["data"]
    assert [i["content"] for i in items] == ["不要叫她阿婆"]
    assert items[0]["observed_days"] == 4
    assert items[0]["evidence"] == "她糾正過兩次"


def test_revoke_takes_a_strategy_out_of_effect(admin_client, strategies):
    strategies.record("e1", "要撤掉的", STRATEGY_CATEGORY_ADDRESS, "證據", 3, None)
    target = strategies.list_for_elder("e1")[0]

    res = admin_client.patch(
        f"/admin/strategies/{target.strategy_id}", json={"action": "revoke"}
    )

    assert res.status_code == 200
    assert strategies.list_for_elder("e1", status=STRATEGY_STATUS_ADOPTED) == []
    assert len(strategies.list_for_elder("e1", status=STRATEGY_STATUS_REVOKED)) == 1


def test_an_unknown_action_is_rejected(admin_client, strategies):
    strategies.record("e1", "守則", STRATEGY_CATEGORY_ADDRESS, "證據", 3, None)
    target = strategies.list_for_elder("e1")[0]

    res = admin_client.patch(f"/admin/strategies/{target.strategy_id}", json={"action": "adopt"})

    assert res.status_code == 400


def test_admin_key_is_required(client, strategies):
    res = client.get("/admin/strategies?status=adopted")
    assert res.status_code in (401, 403)
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_web_admin_strategies.py -v`
Expected: FAIL — 404（路由不存在）

- [ ] **Step 3: 實作 router**

`src/kinsun/web/routers/admin_strategies.py`：

```python
"""後台守則資源：檢視生效中的守則、撤銷學歪的守則。

角色是事後撤銷（opt-out），不是事前批准——守則由反思自動生效，這裡只是逃生口。
金鑰驗證與回應信封沿用既有 admin 路由的做法。
"""

from __future__ import annotations

from fastapi import APIRouter, Body, Depends, HTTPException, Query
from pydantic import BaseModel

from kinsun.strategies.models import STRATEGY_STATUSES
from kinsun.strategies.store import StrategyStore
from kinsun.web.envelope import ok
from kinsun.web.errors import ErrorCode

router = APIRouter(prefix="/admin", tags=["admin"])


class StrategyAction(BaseModel):
    action: str  # 目前只支援 "revoke"


def build_admin_strategies_router(
    strategies: StrategyStore, *, require_admin
) -> APIRouter:
    api = APIRouter(prefix="/admin", tags=["admin"], dependencies=[Depends(require_admin)])

    @api.get("/strategies")
    def list_strategies(status: str = Query("adopted")):
        if status not in STRATEGY_STATUSES:
            raise HTTPException(status_code=400, detail=ErrorCode.INVALID_ARGUMENT)
        rows = strategies.list_for_status(status)
        return ok(
            [
                {
                    "strategy_id": r.strategy_id,
                    "elder_id": r.elder_id,
                    "content": r.content,
                    "category": r.category,
                    "evidence": r.evidence,
                    "observed_days": r.observed_days,
                    "status": r.status,
                    "supersedes_strategy_id": r.supersedes_strategy_id,
                    "created_at": r.created_at,
                    "revoked_at": r.revoked_at,
                }
                for r in rows
            ]
        )

    @api.patch("/strategies/{strategy_id}")
    def update_strategy(strategy_id: str, body: StrategyAction = Body(...)):
        if body.action != "revoke":
            raise HTTPException(status_code=400, detail=ErrorCode.INVALID_ARGUMENT)
        strategies.revoke(strategy_id)
        return ok({"strategy_id": strategy_id, "status": "revoked"})

    return api
```

> 實作時**務必比對** `web/routers/admin.py` 既有的金鑰驗證寫法（`Header` 或 `Depends`）與 `ok()` 信封的實際簽章，照抄該檔的模式；上面的 `require_admin` 是佔位介面名，要換成該檔實際使用的相依。

- [ ] **Step 4: 掛上 router**

`src/kinsun/web/app.py` 依既有 admin router 的註冊方式加入 `build_admin_strategies_router(core.strategies, ...)`。

- [ ] **Step 5: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_web_admin_strategies.py -v`
Expected: PASS（4 個）

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/web/routers/admin_strategies.py src/kinsun/web/app.py tests/test_web_admin_strategies.py
git commit -m "feat(web): 後台檢視生效中守則並可撤銷

守則自動生效，但必須有逃生口——學歪了要能立刻撤掉，而不是等下次部署。角色是
事後撤銷（opt-out），不是事前批准：後台不提供「採用」動作，因為根本沒有待審
佇列。

回傳帶 evidence 與 observed_days：要判斷一條守則該不該撤，得看得到金孫是憑什麼
學到它的。

影響：新增兩個 admin 端點，需 ADMIN_API_KEY。不影響長輩端行為。"
```

---

### Task 10: 文件同步

**Files:**
- Modify: `AGENTS.md`（環境變數命名表加 `REFLECTION_` 前綴）
- Modify: `CONTEXT.md`（新增 `strategies/` 領域說明）

- [ ] **Step 1: 更新 `AGENTS.md`**

在「環境變數」小節的前綴清單中，把 `REFLECTION_` 加入：

> 鍵一律掛子系統前綴：`GEMINI_`、`LONGTERM_`、`PROACTIVE_`、`REFLECTION_`、`SCHEDULER_`、`ASR_`、`TTS_`、`AUDIO_`、`SUPABASE_`（專案層憑證）、`LIFF_`、`LINE_`、`INTERNAL_TESTING_`（內測模式旗標）。

- [ ] **Step 2: 更新 `CONTEXT.md`**

依該檔既有的領域說明格式，加入 `strategies/`：策略記憶（守則）的三件套持久層、三道濾網、每晚反思、經 `FactProvider` 注入 system prompt；並註明「反思只學相處風格，安全與用藥規則永遠由人設定」。

- [ ] **Step 3: 跑全套測試與 lint**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: PASS

Run: `uv run ruff check src tests`
Expected: 無錯誤

- [ ] **Step 4: Commit**

```bash
git add AGENTS.md CONTEXT.md
git commit -m "docs: 同步 strategies 領域與 REFLECTION_ 環境變數前綴

新增子系統就要同步命名規範與架構脈絡文件，否則下一個人（或下一個 AI）會不知道
strategies/ 是什麼、REFLECTION_ 前綴憑什麼存在。

影響：純文件。"
```

---

## 完成標準（Definition of Done）

- [ ] 全套測試通過：`PYTHONPATH=src uv run pytest -q`
- [ ] 整合測試通過：`KINSUN_IT=1 PYTHONPATH=src uv run pytest -q`（需 `KINSUN_TEST_DATABASE_URL`）
- [ ] Lint 通過：`uv run ruff check src tests`
- [ ] **既有庫升級路徑已驗證**：對一個「已存在舊版 `reminder_logs`（無 `responded_at`）」的資料庫跑 `ensure_schema()`，確認不炸 `UndefinedColumn`。空測試庫測不到這條路徑。
- [ ] `agent.py` 未被修改（`git diff main -- src/kinsun/agent.py` 為空）
- [ ] `safety/` 未被修改（`git diff main -- src/kinsun/safety/` 為空）
- [ ] 手動驗證：在 `/admin` 手動觸發 consolidation job，確認守則表有寫入（或 log 顯示「證據不足」等預期的濾網理由）
