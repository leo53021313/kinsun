# 提醒紀錄持久化（D2）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在用藥/回診提醒推播點落一筆提醒紀錄到 `reminder_logs`，作為健康報告（E）資料源；記錄失敗不影響推播。

**Architecture:** 新 `reports/reminders.py`（`ReminderLog` + `PgReminderLogStore` + `safe_record`）+ `reminder_logs` 表；兩個 job builder 加可選 `record` callable，在送出提醒處 `safe_record`；worker 接線。

**Tech Stack:** Python 3.12 / psycopg / pytest / ruff。

## Global Constraints

- 一律台灣繁體中文：文件、註解、commit 訊息。
- Python 3.12；ruff `E,W,F,I,B,UP`，line-length 100；不新增第三方套件。
- 每次提醒一筆、以 `elder_id` 為鍵；記錄代表「實際送出」。
- `safe_record` 吞例外只 log：提醒已送出，記錄失敗不影響推播（fanout 本就逐筆隔離）。
- store 走 Protocol + 注入 fake；clock/new_id 注入。
- commit 型別 `feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 單元測試全離線（注入 fake/spy）；PG round-trip 以 `KINSUN_IT=1` opt-in。

---

## 檔案結構（File Structure）

**新增：** `src/kinsun/reports/__init__.py`、`src/kinsun/reports/reminders.py`、`tests/test_reminder_logs.py`。
**修改：** `src/kinsun/db.py`（DDL）、`src/kinsun/medication/jobs.py`、`src/kinsun/appointment/jobs.py`、`src/kinsun/scheduler/worker.py`、`tests/fakes.py`、`tests/test_medication_jobs.py`、`tests/test_appointment_jobs.py`。

---

## Task 1：提醒紀錄 model + 儲存 + DDL + safe_record + Fake

**Files:**
- Create: `src/kinsun/reports/__init__.py`、`src/kinsun/reports/reminders.py`
- Modify: `src/kinsun/db.py`、`tests/fakes.py`
- Test: `tests/test_reminder_logs.py`

**Interfaces:**
- Produces: `ReminderLog`、`ReminderLogError`、`ReminderLogStore` Protocol、`PgReminderLogStore(db, *, clock, new_id)`、`safe_record(record, elder_id, kind, content)`、`FakeReminderLogStore`

- [ ] **Step 1：建立 `src/kinsun/reports/__init__.py`（空）與 `src/kinsun/reports/reminders.py`**

```python
"""提醒紀錄持久化：供日後健康報告查詢。"""

from __future__ import annotations

import logging
from collections.abc import Callable
from dataclasses import dataclass
from datetime import datetime
from typing import Protocol

from kinsun.db import Database, _Errors

logger = logging.getLogger("kinsun.reports.reminders")


@dataclass(frozen=True)
class ReminderLog:
    log_id: str
    elder_id: str
    kind: str
    content: str
    created_at: float


class ReminderLogError(Exception):
    """提醒紀錄讀寫失敗。"""


class ReminderLogStore(Protocol):
    def record(self, elder_id: str, kind: str, content: str) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[ReminderLog]: ...


class PgReminderLogStore:
    def __init__(
        self, db: Database, *, clock: Callable[[], datetime], new_id: Callable[[], str]
    ) -> None:
        self._db = _Errors(db, lambda m: ReminderLogError(f"提醒紀錄存取失敗：{m}"))
        self._clock = clock
        self._new_id = new_id

    def record(self, elder_id: str, kind: str, content: str) -> None:
        self._db.execute(
            "INSERT INTO reminder_logs (log_id, elder_id, kind, content, created_at) "
            "VALUES (%s, %s, %s, %s, %s)",
            (self._new_id(), elder_id, kind, content, self._clock().timestamp()),
        )

    def list_for_elder(self, elder_id: str) -> list[ReminderLog]:
        rows = self._db.query(
            "SELECT log_id, elder_id, kind, content, created_at FROM reminder_logs "
            "WHERE elder_id = %s ORDER BY created_at DESC",
            (elder_id,),
        )
        return [ReminderLog(*r) for r in rows]


def safe_record(
    record: Callable[[str, str, str], None] | None, elder_id: str, kind: str, content: str
) -> None:
    if record is None:
        return
    try:
        record(elder_id, kind, content)
    except Exception:  # noqa: BLE001 - 記錄失敗不影響推播
        logger.warning("提醒紀錄落庫失敗 elder=%s kind=%s", elder_id, kind)
```

- [ ] **Step 2：`db.py` 新增 DDL**

在 `RISK_EVENTS_DDL` 之後新增：
```python
REMINDER_LOGS_DDL = (
    "CREATE TABLE IF NOT EXISTS reminder_logs ("
    "log_id TEXT PRIMARY KEY, elder_id TEXT NOT NULL, "
    "kind TEXT NOT NULL, content TEXT NOT NULL, created_at DOUBLE PRECISION NOT NULL);"
    "CREATE INDEX IF NOT EXISTS idx_reminder_logs_elder_created "
    "ON reminder_logs (elder_id, created_at);"
)
```
並在 `ensure_schema` 的 `conn.execute(RISK_EVENTS_DDL)` 之後加：
```python
        conn.execute(REMINDER_LOGS_DDL)
```

- [ ] **Step 3：`tests/fakes.py` 加 `FakeReminderLogStore`**

頂部 import 加：
```python
from kinsun.reports.reminders import ReminderLog
```
檔末新增：
```python
class FakeReminderLogStore:
    def __init__(self) -> None:
        self.recorded: list[tuple] = []

    def record(self, elder_id, kind, content):
        self.recorded.append((elder_id, kind, content))

    def list_for_elder(self, elder_id):
        return [
            ReminderLog(str(i), e, k, c, float(i))
            for i, (e, k, c) in enumerate(self.recorded)
            if e == elder_id
        ]
```

- [ ] **Step 4：opt-in PG 測試** `tests/test_reminder_logs.py`

```python
import os
import uuid
from datetime import datetime, timedelta, timezone
from itertools import count

import pytest

TPE = timezone(timedelta(hours=8))


@pytest.mark.skipif(os.environ.get("KINSUN_IT") != "1", reason="需雲端 key")
def test_pg_reminder_logs_round_trip():
    from kinsun.db import Database, ensure_schema
    from kinsun.reports.reminders import PgReminderLogStore

    url = os.environ["DATABASE_URL"]
    ensure_schema(url)
    elder_id = f"it-{uuid.uuid4().hex}"
    ids = (f"rl{i}" for i in count(1))
    times = iter([datetime(2026, 7, 10, 9, tzinfo=TPE), datetime(2026, 7, 10, 10, tzinfo=TPE)])
    store = PgReminderLogStore(
        Database.open(url), clock=lambda: next(times), new_id=lambda: next(ids)
    )
    store.record(elder_id, "medication", "早上用藥：A")
    store.record(elder_id, "appointment", "明天回診：B")
    rows = store.list_for_elder(elder_id)
    assert [r.kind for r in rows] == ["appointment", "medication"]
    assert rows[0].content == "明天回診：B"
```

- [ ] **Step 5：驗證匯入與測試收集**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.reports.reminders; print('ok')"
uv run pytest tests/test_reminder_logs.py -v
```
Expected: 印出 `ok`；PG 測試 SKIPPED。

- [ ] **Step 6：commit**

```bash
git add src/kinsun/reports/__init__.py src/kinsun/reports/reminders.py src/kinsun/db.py \
        tests/fakes.py tests/test_reminder_logs.py
git commit -m "feat: 提醒紀錄 model 與儲存（reminder_logs 表 + safe_record + Fake）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：用藥 + 回診 job 落庫紀錄

**Files:**
- Modify: `src/kinsun/medication/jobs.py`、`src/kinsun/appointment/jobs.py`、`tests/test_medication_jobs.py`、`tests/test_appointment_jobs.py`

**Interfaces:**
- Consumes: `safe_record`（Task 1）
- Produces: 兩個 builder 多收可選 `record: Callable[[str, str, str], None] | None = None`

- [ ] **Step 1：改測試**（兩個 job 測試）

`tests/test_medication_jobs.py`：`_job` 改為多收 `record`：
```python
def _job(meds, *, elders, consented, hour=8, record=None):
    pushed = []
    job = build_medication_slot_job(
        slot=MedicationSlot.MORNING,
        meds_at_slot=lambda: meds,
        lookup_elder=lambda eid: elders.get(eid),
        is_consented=lambda line: consented.get(line, False),
        push=lambda line, text: pushed.append((line, text)),
        hour=hour,
        name="medication-morning",
        record=record,
    )
    return job, pushed
```
檔末新增：
```python
def test_records_reminder_when_pushed():
    elders = {"e1": Elder("e1", "阿公", "U-elder")}
    recorded = []
    job, _ = _job(
        [_med("e1", "降血壓藥", (MedicationSlot.MORNING,))],
        elders=elders,
        consented={"U-elder": True},
        record=lambda e, k, c: recorded.append((e, k, c)),
    )
    job.run()
    assert recorded == [("e1", "medication", "早上用藥：降血壓藥")]


def test_does_not_record_when_unconsented():
    elders = {"e1": Elder("e1", "阿公", "U-elder")}
    recorded = []
    job, _ = _job(
        [_med("e1", "藥", (MedicationSlot.MORNING,))],
        elders=elders,
        consented={"U-elder": False},
        record=lambda e, k, c: recorded.append((e, k, c)),
    )
    job.run()
    assert recorded == []
```

`tests/test_appointment_jobs.py`：`_job` 改為多收 `record`（在 `build_appointment_reminder_job(...)` 加 `record=record`、helper 簽章加 `record=None`）；檔末新增：
```python
def test_records_reminder_per_event():
    elders = {"e1": Elder("e1", "阿公", "U-elder")}
    appts = {"2026-07-15": [Appointment("a1", "e1", "2026-07-15", "心臟科回診")]}
    recorded = []
    job, _ = _job(
        appts,
        elders=elders,
        consented={"U-elder": True},
        guardians={"e1": ["U-son"]},
        record=lambda e, k, c: recorded.append((e, k, c)),
    )
    job.run()
    assert recorded == [("e1", "appointment", "今天回診：心臟科回診")]
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_medication_jobs.py tests/test_appointment_jobs.py -v`
Expected: FAIL（builder 不接受 `record`）

- [ ] **Step 3：實作 `medication/jobs.py`**

import 加：
```python
from kinsun.reports.reminders import safe_record
```
`build_medication_slot_job` 簽章在 `name: str,` 之後加：
```python
    record: Callable[[str, str, str], None] | None = None,
```
action 在 `push(...)` 那行之後加：
```python
        safe_record(record, elder_id, "medication", f"{label}用藥：{'、'.join(names)}")
```

- [ ] **Step 4：實作 `appointment/jobs.py`**

import 加：
```python
from kinsun.reports.reminders import safe_record
```
`build_appointment_reminder_job` 簽章在 `name: str = "appointment-reminder",` 之後加：
```python
    record: Callable[[str, str, str], None] | None = None,
```
action 在 guardian `for` 迴圈之後加：
```python
        safe_record(record, appt.elder_id, "appointment", f"{when_word}回診：{appt.label}")
```

- [ ] **Step 5：跑測試確認通過**

Run: `uv run pytest tests/test_medication_jobs.py tests/test_appointment_jobs.py -v`
Expected: PASS（既有 + 新增）

- [ ] **Step 6：commit**

```bash
git add src/kinsun/medication/jobs.py src/kinsun/appointment/jobs.py \
        tests/test_medication_jobs.py tests/test_appointment_jobs.py
git commit -m "feat: 用藥/回診 job 於送出提醒時落庫紀錄（safe_record）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：worker 接線

**Files:**
- Modify: `src/kinsun/scheduler/worker.py`

- [ ] **Step 1：實作接線**

import 區加：
```python
import uuid
```
並加：
```python
from kinsun.reports.reminders import PgReminderLogStore
```
在 `appt_store = PgAppointmentStore(db)` 一帶之後建：
```python
    reminder_logs = PgReminderLogStore(db, clock=clock, new_id=lambda: uuid.uuid4().hex)
```
用藥 job 迴圈內 `build_medication_slot_job(...)` 加 `record=reminder_logs.record`；
回診 job `build_appointment_reminder_job(...)` 加 `record=reminder_logs.record`。

- [ ] **Step 2：匯入驗證 + 全量測試**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.scheduler.worker; print('import ok')"
uv run pytest -q
```
Expected: `import ok`；pytest 全綠。

- [ ] **Step 3：commit**

```bash
git add src/kinsun/scheduler/worker.py
git commit -m "feat: worker 把 PgReminderLogStore 接進用藥/回診 job

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：全量品質閘門

**Files:** 無（純驗證）。

- [ ] **Step 1：後端 lint / format / 測試**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠（PG round-trip SKIPPED）。
format 報未格式化 → `uv run ruff format .` 後重跑並把變更納入收尾 commit。

- [ ] **Step 2：pre-commit 全鉤子**

Run: `uv run pre-commit run --all-files`
Expected: 全 Passed。

- [ ] **Step 3：靜態匯入驗證**

Run: `PYTHONPATH=src uv run python -c "import kinsun.app, kinsun.scheduler.worker; print('import ok')"`
Expected: `import ok`。

（若 Step 1 有 format 修整，補一個 `chore: 套用格式化` commit。）

---

## Self-Review（計畫對 spec 覆蓋檢查）

- `ReminderLog`/`ReminderLogStore`/`PgReminderLogStore`/`safe_record`（spec §3）→ Task 1 ✅
- `reminder_logs` DDL（§3）→ Task 1 Step 2 ✅
- 用藥/回診 job 加 `record`、推播點 `safe_record`（§4）→ Task 2 ✅
- worker 接線（§5）→ Task 3 ✅
- 測試：med 記錄/未同意不記、appt 每事件一筆、PG round-trip（§6）→ Task 1、2 ✅
- fail-safe（§7）→ `safe_record` 吞例外 ✅

型別/命名一致性：`record(elder_id, kind, content)`、`safe_record(record, elder_id, kind, content)`、`PgReminderLogStore(db, *, clock, new_id)`、content 格式（「早上用藥：…」/「今天回診：…」）測試與實作一致；`kind ∈ {"medication","appointment"}`。無佔位符。
