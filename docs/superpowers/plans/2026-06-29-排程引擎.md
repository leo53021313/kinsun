# 排程引擎（scheduler）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 自建輕量排程器，每日到點自動枚舉所有長者 session、各跑一次骨幹整理。

**Architecture:** `Scheduler.run_due()`（時鐘注入、每日到點且當天未跑才執行、錯誤隔離）+ `build_consolidation_job`（對所有 session 跑 `run_consolidation`）+ 獨立 worker CLI。`MemoryStore.sessions()` 枚舉 session。

**Tech Stack:** Python 3.12、標準庫 `datetime`/`logging`/`time`/`sqlite3`、pytest。**無新依賴**。

## Global Constraints

- 採 src layout（`src/kinsun/`），pytest `pythonpath=["src"]`。
- 排程錯誤隔離：單一 job／單一 session 失敗 → 記錄並續跑，排程器不崩。
- in-memory 記「當天已跑」（不另存狀態 DB）；每日單一時點。
- 重用 `run_consolidation`、既有 store 與 `Settings`。
- ruff `E,W,F,I,B,UP`、line-length 100。commit 遵循 CONTRIBUTING.md，結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。

---

## File Structure

```
src/kinsun/scheduler/
  __init__.py
  scheduler.py   # Job + Scheduler
  jobs.py        # build_consolidation_job
  worker.py      # build_scheduler + serve + main
  __main__.py
src/kinsun/memory/store.py   # 加 sessions()
src/kinsun/config.py         # 加 consolidation_hour / scheduler_tick_seconds
tests/
  test_scheduler.py / test_scheduler_jobs.py / test_memory.py(追加) / test_config.py(追加)
.env.example
```

---

## Task 1：memory.sessions() + config

**Files:** Modify `src/kinsun/memory/store.py`, `src/kinsun/config.py`, `tests/test_memory.py`, `tests/test_config.py`, `.env.example`

**Interfaces:**
- Produces：`MemoryStore.sessions() -> list[str]`；`Settings.consolidation_hour: int`（3）、`scheduler_tick_seconds: int`（60）

- [ ] **Step 1：追加測試**

`tests/test_memory.py` 末尾加：

```python
def test_sessions_lists_distinct_sorted():
    store = _store(datetime(2026, 6, 29, 10, 0, tzinfo=TPE))
    store.append("u2", Message("user", "B"))
    store.append("u1", Message("user", "A"))
    store.append("u1", Message("assistant", "a"))
    assert store.sessions() == ["u1", "u2"]


def test_sessions_empty():
    store = _store(datetime(2026, 6, 29, 10, 0, tzinfo=TPE))
    assert store.sessions() == []
```

`tests/test_config.py` 的 defaults 測試末尾加：

```python
    assert settings.consolidation_hour == 3
    assert settings.scheduler_tick_seconds == 60
```

- [ ] **Step 2：確認失敗**：`uv run pytest tests/test_memory.py tests/test_config.py -q` → FAIL

- [ ] **Step 3：改 `src/kinsun/memory/store.py`**

在 `MemoryStore(Protocol)` 加一行：

```python
    def sessions(self) -> list[str]: ...
```

在 `SqliteMemoryStore` 加方法（置於 `recent` 之後）：

```python
    def sessions(self) -> list[str]:
        try:
            rows = self._conn.execute(
                "SELECT DISTINCT session_id FROM turns ORDER BY session_id"
            ).fetchall()
        except sqlite3.Error as exc:
            raise MemoryError(f"列出 session 失敗：{exc}") from exc
        return [row[0] for row in rows]
```

- [ ] **Step 4：改 `src/kinsun/config.py`**

`Settings` 末尾加：

```python
    consolidation_hour: int
    scheduler_tick_seconds: int
```
`load_settings` 末尾加：

```python
        consolidation_hour=int(env.get("CONSOLIDATION_HOUR", "3")),
        scheduler_tick_seconds=int(env.get("SCHEDULER_TICK_SECONDS", "60")),
```
`.env.example` 末尾加：

```dotenv
# 排程引擎
CONSOLIDATION_HOUR=3
SCHEDULER_TICK_SECONDS=60
```

- [ ] **Step 5：確認通過**：`uv run pytest tests/test_memory.py tests/test_config.py -q` → PASS
- [ ] **Step 6：commit**

```bash
git add src/kinsun/memory/store.py src/kinsun/config.py tests/test_memory.py tests/test_config.py .env.example
git commit -m "feat: memory 加 sessions() 與排程設定"
```

---

## Task 2：Scheduler（到期判斷 + 錯誤隔離）

**Files:** Create `src/kinsun/scheduler/__init__.py`, `src/kinsun/scheduler/scheduler.py`, `tests/test_scheduler.py`

**Interfaces:**
- Produces：
  - `@dataclass(frozen=True) Job`：`name: str`、`hour: int`、`minute: int`、`run: Callable[[], None]`
  - `class Scheduler(jobs: list[Job], clock: Callable[[], datetime])`：`run_due() -> list[str]`

- [ ] **Step 1：失敗測試** `tests/test_scheduler.py`

```python
from datetime import datetime, timedelta, timezone

from kinsun.scheduler.scheduler import Job, Scheduler

TPE = timezone(timedelta(hours=8))


class FakeClock:
    def __init__(self, dt):
        self.dt = dt

    def __call__(self):
        return self.dt


def _job(name, hour, calls):
    return Job(name=name, hour=hour, minute=0, run=lambda: calls.append(name))


def test_runs_when_due():
    calls = []
    clock = FakeClock(datetime(2026, 6, 29, 3, 0, tzinfo=TPE))
    sched = Scheduler([_job("a", 3, calls)], clock)
    assert sched.run_due() == ["a"]
    assert calls == ["a"]


def test_not_due_before_time():
    calls = []
    clock = FakeClock(datetime(2026, 6, 29, 2, 59, tzinfo=TPE))
    sched = Scheduler([_job("a", 3, calls)], clock)
    assert sched.run_due() == []
    assert calls == []


def test_runs_once_per_day():
    calls = []
    clock = FakeClock(datetime(2026, 6, 29, 5, 0, tzinfo=TPE))
    sched = Scheduler([_job("a", 3, calls)], clock)
    sched.run_due()
    assert sched.run_due() == []
    assert calls == ["a"]


def test_runs_again_next_day():
    calls = []
    clock = FakeClock(datetime(2026, 6, 29, 5, 0, tzinfo=TPE))
    sched = Scheduler([_job("a", 3, calls)], clock)
    sched.run_due()
    clock.dt = datetime(2026, 6, 30, 5, 0, tzinfo=TPE)
    assert sched.run_due() == ["a"]
    assert calls == ["a", "a"]


def test_one_job_failure_does_not_block_others():
    calls = []

    def boom():
        raise RuntimeError("boom")

    clock = FakeClock(datetime(2026, 6, 29, 3, 0, tzinfo=TPE))
    sched = Scheduler(
        [Job("boom", 3, 0, boom), _job("ok", 3, calls)], clock
    )
    assert sched.run_due() == ["boom", "ok"]
    assert calls == ["ok"]
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `src/kinsun/scheduler/__init__.py`（`"""排程引擎（Cron）。"""\n`）與 `src/kinsun/scheduler/scheduler.py`

```python
"""自建輕量排程器。"""

from __future__ import annotations

import logging
from collections.abc import Callable
from dataclasses import dataclass
from datetime import date, datetime

logger = logging.getLogger("kinsun.scheduler")


@dataclass(frozen=True)
class Job:
    name: str
    hour: int
    minute: int
    run: Callable[[], None]


class Scheduler:
    def __init__(self, jobs: list[Job], clock: Callable[[], datetime]) -> None:
        self._jobs = jobs
        self._clock = clock
        self._last_run: dict[str, date] = {}

    def run_due(self) -> list[str]:
        now = self._clock()
        ran: list[str] = []
        for job in self._jobs:
            if not self._is_due(job, now):
                continue
            try:
                job.run()
            except Exception:  # noqa: BLE001 - 排程不可因單一 job 崩潰
                logger.exception("排程 job 失敗：%s", job.name)
            self._last_run[job.name] = now.date()
            ran.append(job.name)
        return ran

    def _is_due(self, job: Job, now: datetime) -> bool:
        if self._last_run.get(job.name) == now.date():
            return False
        return (now.hour, now.minute) >= (job.hour, job.minute)
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add src/kinsun/scheduler/__init__.py src/kinsun/scheduler/scheduler.py tests/test_scheduler.py
git commit -m "feat: 加入自建輕量排程器（Scheduler）"
```

---

## Task 3：整理 job（build_consolidation_job）

**Files:** Create `src/kinsun/scheduler/jobs.py`, `tests/test_scheduler_jobs.py`

**Interfaces:**
- Consumes：`Job`
- Produces：`build_consolidation_job(*, sessions, run_one, hour, minute=0, name="daily-consolidation") -> Job`

- [ ] **Step 1：失敗測試** `tests/test_scheduler_jobs.py`

```python
from kinsun.scheduler.jobs import build_consolidation_job


def test_runs_for_each_session():
    done = []
    job = build_consolidation_job(
        sessions=lambda: ["u1", "u2"], run_one=done.append, hour=3
    )
    job.run()
    assert done == ["u1", "u2"]
    assert job.name == "daily-consolidation"
    assert job.hour == 3


def test_one_session_failure_does_not_block_others():
    done = []

    def run_one(session_id):
        if session_id == "u1":
            raise RuntimeError("boom")
        done.append(session_id)

    job = build_consolidation_job(sessions=lambda: ["u1", "u2"], run_one=run_one, hour=3)
    job.run()
    assert done == ["u2"]
```

- [ ] **Step 2：確認失敗** → FAIL
- [ ] **Step 3：實作** `src/kinsun/scheduler/jobs.py`

```python
"""排程 job 組裝。"""

from __future__ import annotations

import logging
from collections.abc import Callable

from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.scheduler")


def build_consolidation_job(
    *,
    sessions: Callable[[], list[str]],
    run_one: Callable[[str], object],
    hour: int,
    minute: int = 0,
    name: str = "daily-consolidation",
) -> Job:
    def run() -> None:
        for session_id in sessions():
            try:
                run_one(session_id)
            except Exception:  # noqa: BLE001 - 單一 session 失敗不影響其他
                logger.exception("整理 session 失敗：%s", session_id)

    return Job(name=name, hour=hour, minute=minute, run=run)
```

- [ ] **Step 4：確認通過** → PASS
- [ ] **Step 5：commit**

```bash
git add src/kinsun/scheduler/jobs.py tests/test_scheduler_jobs.py
git commit -m "feat: 加入每日整理 job（對所有 session）"
```

---

## Task 4：worker + CLI

**Files:** Create `src/kinsun/scheduler/worker.py`, `src/kinsun/scheduler/__main__.py`

**Interfaces:**
- Produces：`build_scheduler(settings, *, clock) -> Scheduler`；`serve(scheduler, *, tick_seconds)`；`main() -> int`

- [ ] **Step 1：實作** `src/kinsun/scheduler/worker.py`

```python
"""排程 worker：長跑迴圈，定時 run_due。

CLI：PYTHONPATH=src uv run python -m kinsun.scheduler
"""

from __future__ import annotations

import os
import time
from collections.abc import Callable
from datetime import datetime
from zoneinfo import ZoneInfo

from kinsun.config import Settings, load_settings
from kinsun.episodic.embeddings import GeminiEmbedder
from kinsun.episodic.store import SqliteVectorStore
from kinsun.knowledge.store import SqliteFactStore
from kinsun.llm import GeminiClient
from kinsun.longterm.consolidation import run_consolidation
from kinsun.longterm.extractor import ConsolidationExtractor
from kinsun.memory.store import SqliteMemoryStore
from kinsun.scheduler.jobs import build_consolidation_job
from kinsun.scheduler.scheduler import Scheduler


def build_scheduler(settings: Settings, *, clock: Callable[[], datetime]) -> Scheduler:
    tz = ZoneInfo(settings.timezone)
    memory = SqliteMemoryStore(
        settings.memory_db_path,
        clock=lambda: datetime.now(tz),
        max_turns=settings.memory_max_turns,
    )
    extractor = ConsolidationExtractor(
        GeminiClient(
            api_key=settings.gemini_api_key,
            model=settings.gemini_model,
            timeout=settings.llm_timeout_seconds,
        )
    )
    embedder = GeminiEmbedder(api_key=settings.gemini_api_key, model=settings.embedding_model)
    fact_store = SqliteFactStore(settings.knowledge_db_path)
    vector_store = SqliteVectorStore(settings.episodic_db_path)

    def run_one(session_id: str) -> None:
        run_consolidation(
            session_id,
            short_term=memory,
            extractor=extractor,
            embedder=embedder,
            fact_store=fact_store,
            vector_store=vector_store,
        )

    job = build_consolidation_job(
        sessions=memory.sessions, run_one=run_one, hour=settings.consolidation_hour
    )
    return Scheduler([job], clock)


def serve(scheduler: Scheduler, *, tick_seconds: int) -> None:
    while True:
        scheduler.run_due()
        time.sleep(tick_seconds)


def main() -> int:
    settings = load_settings(os.environ)
    tz = ZoneInfo(settings.timezone)
    scheduler = build_scheduler(settings, clock=lambda: datetime.now(tz))
    print(
        f"排程器啟動：每 {settings.scheduler_tick_seconds}s 檢查，"
        f"每日 {settings.consolidation_hour}:00 整理。"
    )
    serve(scheduler, tick_seconds=settings.scheduler_tick_seconds)
    return 0
```

`src/kinsun/scheduler/__main__.py`：

```python
"""`python -m kinsun.scheduler` 入口。"""

from kinsun.scheduler.worker import main

raise SystemExit(main())
```

- [ ] **Step 2：smoke 驗證**（import + 組裝，不啟動無窮迴圈）

Run:
```bash
LINE_CHANNEL_SECRET=d LINE_CHANNEL_ACCESS_TOKEN=d GEMINI_API_KEY=d uv run python -c "import sys; sys.path.insert(0,'src'); from datetime import datetime; from kinsun.config import load_settings; from kinsun.scheduler.worker import build_scheduler; s=build_scheduler(load_settings({'LINE_CHANNEL_SECRET':'d','LINE_CHANNEL_ACCESS_TOKEN':'d','GEMINI_API_KEY':'d'}), clock=lambda: datetime(2026,1,1,0,0)); print('scheduler ok:', type(s).__name__)"
```
Expected: `scheduler ok: Scheduler`

- [ ] **Step 3：commit**

```bash
git add src/kinsun/scheduler/worker.py src/kinsun/scheduler/__main__.py
git commit -m "feat: 加入排程 worker 與 CLI（python -m kinsun.scheduler）"
```

---

## Task 5：品質收尾與上線

- [ ] **Step 1：全測試 + 品質**：`uv run pytest -v`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run pre-commit run --all-files` → 全綠
- [ ] **Step 2：push + PR + 合併 + 同步分支**

```bash
git push origin Leo
gh pr create --base main --head Leo --title "feat: 排程引擎（自建輕量 Scheduler）" --body "..."
gh pr merge <n> --merge --admin
git fetch origin
git push origin origin/main:Babic origin/main:Brian origin/main:Jerry origin/main:Kevin origin/main:MA origin/main:Otto
git merge --ff-only origin/main && git push origin Leo
```

---

## Self-Review

**Spec coverage：** §3.1 Scheduler → Task 2。§3.2 整理 job → Task 3。§3.3 worker → Task 4。§3.4 sessions()/config → Task 1。§5 錯誤隔離 → Task 2（job）、Task 3（session）。§6 測試 → 各 Task。

**Placeholder scan：** PR body 的 "..." 於執行時填入；worker 無單測（wiring＋無窮迴圈），以 smoke 驗證。其餘皆含實際程式碼。

**Type consistency：** `Job(name, hour, minute, run)`、`Scheduler(jobs, clock).run_due()->list[str]`、`build_consolidation_job(*, sessions, run_one, hour, minute, name)->Job`、`MemoryStore.sessions()->list[str]` 一致；重用 `run_consolidation`、`Settings`。
