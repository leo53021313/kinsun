# 會話主鍵遷移（階段 1B）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 對話管線的會話主鍵由 `line_user_id` 改為 `elder_id`——記憶（短期／長期）、危急事件、對話摘要全部跟「人」走，不跟通道走。

**Architecture:** 資料層結構性擴張（三表加 `elder_id`、盡力回填）→ store 換鍵（合約測試先行）→ 核心簽名鏈換鍵（SessionMemory→CareAgent→VoicePipeline→Notifier→facts）→ 邊界解析（gate 由「允許與否」加深為「解析出已同意長輩的 elder_id」）→ 排程與報告收尾。觀測五表本階段**不動**（仍記 `line_user_id`，pipeline 同時攜帶兩個識別：`elder_id` 作會話鍵、`line_user_id` 僅供觀測標記）。

**Tech Stack:** 同 1A（Python 3.12、psycopg、pytest；`KINSUN_IT=1` 跑真庫）。

## Global Constraints

- 同 1A 計畫（繁中全形標點、命名規範、三件套、冪等 DDL、不加套件、每 Task 全綠才 commit）。
- 設計源頭：`docs/superpowers/specs/2026-07-07-app通道與身分中立化-design.md` §4.1；前置：1A 已落地（`channel_bindings`＋雙寫＋回填）。
- **實測前提（2026-07-07 查真庫）**：`turns` 129 筆、`conversation_summaries` 5 筆、`risk_events` 3 筆**全數為孤兒**（`line_user_id` 對不到任何 `elders`），即無真實資料可搬——回填 UPDATE 為「盡力而為」（現況 0 筆），孤兒列保留原樣、對新讀取路徑不可見；Mem0 舊記憶（鍵為舊 line id）同理**不遷移**。
- 綁定前流程（`binding_sessions`、`binding/flow.py`）與家屬 LINE 文字選單（`medications/flow.py`、`appointments/flow.py`）為**通道專屬對話**，本階段維持 `line_user_id` 鍵，不動。
- `web/api.py` 家屬認證（LIFF）與觀測五表：不動（1C／階段 2 接手）。

---

### Task 1: 三表 DDL 遷移（加 elder_id＋盡力回填）

**Files:**
- Modify: `src/kinsun/db.py`
- Test: `tests/test_pg_db.py`（檔尾新增）

**Interfaces:**
- Produces: `SESSION_KEY_MIGRATION_DDL` 常數，`ensure_schema` 於 `CHANNEL_BINDINGS_BACKFILL_DDL` 之後執行。遷移後三表皆有可空 `elder_id` 欄；`turns`／`risk_events` 的 `line_user_id` 改可空（新寫入不再填）；`conversation_summaries` 卸下 `(line_user_id, date)` 主鍵、改掛部分唯一索引 `(elder_id, date) WHERE elder_id IS NOT NULL`。

- [ ] **Step 1: 失敗整合測試**（`tests/test_pg_db.py` 檔尾）

```python
def test_session_key_migration_columns_and_backfill(pg_database, ns):
    from kinsun.db import ensure_schema

    # 造一筆「可對應長輩」的舊制對話（直接寫 line_user_id 欄），與一位已綁定長輩。
    pg_database.execute(
        "INSERT INTO elders (elder_id, name, line_user_id) VALUES (%s, %s, %s) "
        "ON CONFLICT (elder_id) DO UPDATE SET line_user_id = EXCLUDED.line_user_id",
        (f"{ns}e-mig", "阿公", f"{ns}U-mig"),
    )
    pg_database.execute(
        "INSERT INTO turns (line_user_id, role, content, created_at) VALUES (%s, %s, %s, %s)",
        (f"{ns}U-mig", "user", "舊制訊息", 1000.0),
    )
    ensure_schema(os.environ["DATABASE_URL"])
    ensure_schema(os.environ["DATABASE_URL"])  # 冪等
    rows = pg_database.query(
        "SELECT elder_id FROM turns WHERE line_user_id = %s", (f"{ns}U-mig",)
    )
    assert rows == [(f"{ns}e-mig",)]
    # 新制寫入：不帶 line_user_id 也能落列（欄位已可空）。
    pg_database.execute(
        "INSERT INTO turns (elder_id, role, content, created_at) VALUES (%s, %s, %s, %s)",
        (f"{ns}e-mig", "user", "新制訊息", 2000.0),
    )
    # 摘要：新制以 (elder_id, date) upsert（部分唯一索引生效）。
    for content in ("v1", "v2"):
        pg_database.execute(
            "INSERT INTO conversation_summaries (elder_id, date, content, created_at) "
            "VALUES (%s, %s, %s, %s) "
            "ON CONFLICT (elder_id, date) WHERE elder_id IS NOT NULL "
            "DO UPDATE SET content = EXCLUDED.content, created_at = EXCLUDED.created_at",
            (f"{ns}e-mig", "2026-07-07", content, 3000.0),
        )
    rows = pg_database.query(
        "SELECT content FROM conversation_summaries WHERE elder_id = %s AND date = %s",
        (f"{ns}e-mig", "2026-07-07"),
    )
    assert rows == [("v2",)]
```

- [ ] **Step 2: 確認失敗**：`KINSUN_IT=1 DATABASE_URL=… uv run pytest tests/test_pg_db.py -q -k session_key`，預期 FAIL（`column "elder_id" does not exist`）。

- [ ] **Step 3: 實作**（`db.py`，加在 `CHANNEL_BINDINGS_BACKFILL_DDL` 之後；`ensure_schema` 在其後執行）

```python
# 會話主鍵遷移（冪等）：turns／risk_events／conversation_summaries 加 elder_id、
# 舊 line_user_id 欄改可空（新寫入不再填）；摘要卸下 (line_user_id, date) 主鍵、
# 改掛 (elder_id, date) 部分唯一索引。既有孤兒列（對不到 elders）保留原樣、不回填。
SESSION_KEY_MIGRATION_DDL = (
    "ALTER TABLE turns ADD COLUMN IF NOT EXISTS elder_id TEXT;"
    "ALTER TABLE turns ALTER COLUMN line_user_id DROP NOT NULL;"
    "CREATE INDEX IF NOT EXISTS idx_turns_elder_created ON turns (elder_id, created_at);"
    "UPDATE turns SET elder_id = e.elder_id FROM elders e "
    "WHERE turns.elder_id IS NULL AND turns.line_user_id = e.line_user_id;"
    "ALTER TABLE risk_events ADD COLUMN IF NOT EXISTS elder_id TEXT;"
    "ALTER TABLE risk_events ALTER COLUMN line_user_id DROP NOT NULL;"
    "CREATE INDEX IF NOT EXISTS idx_risk_events_elder_created "
    "ON risk_events (elder_id, created_at);"
    "UPDATE risk_events SET elder_id = e.elder_id FROM elders e "
    "WHERE risk_events.elder_id IS NULL AND risk_events.line_user_id = e.line_user_id;"
    "ALTER TABLE conversation_summaries ADD COLUMN IF NOT EXISTS elder_id TEXT;"
    "ALTER TABLE conversation_summaries DROP CONSTRAINT IF EXISTS conversation_summaries_pkey;"
    "ALTER TABLE conversation_summaries ALTER COLUMN line_user_id DROP NOT NULL;"
    "UPDATE conversation_summaries SET elder_id = e.elder_id FROM elders e "
    "WHERE conversation_summaries.elder_id IS NULL "
    "AND conversation_summaries.line_user_id = e.line_user_id;"
    "CREATE UNIQUE INDEX IF NOT EXISTS uq_conversation_summaries_elder_date "
    "ON conversation_summaries (elder_id, date) WHERE elder_id IS NOT NULL;"
)
```

同步更新 `MEMORY_DDL`／`RISK_EVENTS_DDL`／`CONVERSATION_SUMMARIES_DDL` 的**新建**定義（新庫直接長成新制）：`turns`／`risk_events` 的 `line_user_id TEXT NOT NULL` 改 `line_user_id TEXT`＋補 `elder_id TEXT` 欄與上述索引；`conversation_summaries` 改為無主鍵＋`elder_id TEXT`＋部分唯一索引（`SESSION_KEY_MIGRATION_DDL` 皆冪等，對新庫等於 no-op）。

- [ ] **Step 4: 確認通過**（同 Step 2 指令，PASS）
- [ ] **Step 5: Commit**：`git commit -m "feat: 會話三表加 elder_id 遷移（盡力回填，孤兒保留）"`

---

### Task 2: 短期記憶 store 換鍵

**Files:** Modify `src/kinsun/memory/shortterm.py`；Test `tests/test_memory_shortterm.py`（若名不同以 `grep -l shortterm tests` 為準）＋短期記憶合約測試檔。

**Interfaces（Produces，簽名一字不差）:**
```python
class MemoryStore(Protocol):
    def append(self, elder_id: str, message: Message) -> None: ...
    def recent(self, elder_id: str) -> list[Message]: ...
    def previous_day(self, elder_id: str) -> list[Message]: ...
    def sessions(self) -> list[str]: ...          # 回傳 DISTINCT elder_id（排除 NULL）
    def last_active(self, elder_id: str) -> float | None: ...
```
Pg 實作 SQL 全部 `line_user_id` → `elder_id`（含 `sessions()` 的 `WHERE elder_id IS NOT NULL`、索引走 `idx_turns_elder_created`）；Fake 同名參數改名。

- [ ] Step 1: 先改合約測試與單元測試（參數改傳 elder_id、斷言不變）→ Step 2: 跑（FAIL：SQL 仍查舊欄）→ Step 3: 換 SQL 與簽名 → Step 4: `uv run pytest -q -k "shortterm or memory"` PASS → Step 5: Commit `refactor: 短期記憶會話鍵改 elder_id`

---

### Task 3: 對話摘要 store 換鍵

**Files:** Modify `src/kinsun/reports/summaries.py`；Test `tests/test_reports_summaries_store_contract.py`、`tests/test_summaries.py`。

**Interfaces（Produces）:**
```python
@dataclass(frozen=True)
class ConversationSummary:
    elder_id: str
    date: str
    content: str
    created_at: float

class ConversationSummaryStore(Protocol):
    def save(self, elder_id: str, date: str, content: str) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[ConversationSummary]: ...
```
Pg `save` 的 upsert 改：
```sql
INSERT INTO conversation_summaries (elder_id, date, content, created_at)
VALUES (%s, %s, %s, %s)
ON CONFLICT (elder_id, date) WHERE elder_id IS NOT NULL
DO UPDATE SET content = EXCLUDED.content, created_at = EXCLUDED.created_at
```
`summarize_day(elder_id, *, short_term, summarizer, summaries, clock)` 第一參數改名並下傳。

- [ ] TDD 同 Task 2 節奏；Commit `refactor: 對話摘要會話鍵改 elder_id（含 upsert 換部分唯一索引）`

---

### Task 4: 危急事件 store 換鍵

**Files:** Modify `src/kinsun/safety/events.py`；Test `tests/test_safety_events_store_contract.py`。

**Interfaces（Produces）:**
```python
@dataclass(frozen=True)
class RiskEvent:
    risk_event_id: str
    elder_id: str
    tier: RiskTier
    reason: str
    created_at: float

class RiskEventStore(Protocol):
    def record(
        self, elder_id: str, assessment: RiskAssessment, *, trace_id: str | None = None
    ) -> None: ...
    def list_for_elder(self, elder_id: str) -> list[RiskEvent]: ...
```
Pg SQL 換欄（INSERT 填 `elder_id`，不再填 `line_user_id`）；`list_for_line_user` 更名 `list_for_elder`；Fake 對齊。

- [ ] TDD 同前；Commit `refactor: 危急事件會話鍵改 elder_id，list_for_line_user 更名 list_for_elder`

---

### Task 5: 長期記憶（Mem0）鍵與 consolidation CLI

**Files:** Modify `src/kinsun/memory/longterm/store.py`、`src/kinsun/memory/longterm/consolidation.py`；Test `tests/test_longterm_store.py`、`tests/test_longterm_consolidation.py`。

**Interfaces（Produces）:**
```python
class LongTermStore(Protocol):
    def add(
        self, elder_id: str, messages: list[Message], *, provenance: str = "self_claimed"
    ) -> None: ...
    def search(self, elder_id: str, query: str, *, top_k: int = 5) -> list[MemoryItem]: ...
```
`Mem0LongTermStore` 內 `user_id=elder_id`、`filters={"user_id": elder_id}`；`run_consolidation(elder_id, *, short_term, long_term)`；CLI 用法字串改 `<elder_id>`。舊 Mem0 記憶（舊 line id 鍵）不遷移（見 Global Constraints 實測前提），改鍵後自然不再命中。

- [ ] TDD 同前；Commit `refactor: 長期記憶鍵改 elder_id，consolidation CLI 參數同步`

---

### Task 6: 核心簽名鏈（recall→agent→pipeline→notifier→facts）

**Files:** Modify `src/kinsun/memory/recall.py`、`src/kinsun/agent.py`、`src/kinsun/pipeline.py`、`src/kinsun/safety/notifier.py`、`src/kinsun/medications/facts.py`、`src/kinsun/appointments/facts.py`；Test 對應測試檔（`test_agent.py`、`test_pipeline*.py`、`test_safety_notifier.py`、`test_medications_facts.py`、`test_appointments_facts.py`、`test_recall*.py`）。

**Interfaces（Produces，關鍵簽名）:**
```python
class FactProvider(Protocol):
    def facts(self, elder_id: str) -> FactSection | None: ...

class SessionMemory:
    def assemble(self, elder_id: str, query: str) -> TurnContext: ...
    def record_turn(self, elder_id: str, *messages: Message) -> None: ...

class CareAgent:
    def handle(self, elder_id: str, user_text: str) -> str: ...
    def proactive(self, elder_id: str, intent: str) -> str: ...

class VoicePipeline:
    def process(self, audio: bytes, *, elder_id: str, line_user_id: str = "",
                content_type: str = "audio/m4a", trace_id: str = "", audio_url: str = "") -> TtsResult: ...
    def process_text(self, text: str, *, elder_id: str, line_user_id: str = "",
                     trace_id: str = "") -> TtsResult: ...

class Notifier(Protocol):
    def notify(self, elder_id: str, assessment: RiskAssessment) -> None: ...

class GuardianDirectory(Protocol):
    def guardian_line_ids_of_elder(self, elder_id: str) -> list[str]: ...
```
要點：pipeline 內部會話呼叫（`agent.handle`、`risk_events.record`、`notifier.notify`）改用 `elder_id`；觀測 `record_asr/llm/tts_call` 維持傳 `line_user_id`（僅標記用）。`LineGuardianNotifier` 改呼叫 `directory.guardian_line_ids_of_elder(elder_id)`（`AccountService` 已有此方法）。facts providers 由「`elders_managed_by`／`elder_by_line` 解析」簡化為直接吃 `elder_id`（實作時以現檔為準、保留原格式輸出）。

- [ ] 先改測試（傳 elder_id）→ 跑 FAIL → 實作換簽名 → `uv run pytest -q` PASS → Commit `refactor: 對話核心簽名鏈會話鍵改 elder_id（觀測仍記 line_user_id）`

---

### Task 7: 邊界解析與排程收尾

**Files:** Modify `src/kinsun/binding/gate.py`、`src/kinsun/channels/inbound.py`、`src/kinsun/scheduler/worker.py`、`src/kinsun/reports/health.py`、`src/kinsun/accounts/service.py`、`src/kinsun/app.py`（gate 建構）；Test `test_binding_gate.py`、`test_channels_inbound.py`、`test_scheduler_fanout.py`、`test_reports_health.py`（以實檔名為準）。

**Interfaces（Produces）:**
```python
class ElderResolver(Protocol):          # binding/gate.py，取代 ConsentChecker
    def consented_elder_id(self, line_user_id: str) -> str | None: ...

class ConsentGate:
    def resolve_elder(self, line_user_id: str) -> str | None: ...  # 例外→None＋log

class AllowAllGate:
    def resolve_elder(self, line_user_id: str) -> str | None: ...  # 直接回傳 line_user_id（dev）
```
- `AccountService` 新增：
```python
def consented_elder_id(self, line_user_id: str) -> str | None:
    elder = self._repo.get_elder_by_line(line_user_id)
    if elder is None:
        return None
    consent = self._repo.get_consent(elder.elder_id)
    return elder.elder_id if consent is not None and consent.revoked_at is None else None
```
（`is_consented_elder` 改為 `return self.consented_elder_id(line_user_id) is not None`，介面保留給用藥／回診 jobs。）
- **行為變更（明確記錄）**：舊 gate 故障時 fail-open 放行；新制管線必須有 `elder_id` 才能落記憶，故障改回 `None` → 回覆綁定引導語（`BIND_FIRST_PROMPT`）。dev 旗標 `BINDING_GATE_ENABLED=false` 時 `AllowAllGate` 以 `line_user_id` 充當會話鍵（僅 demo，正式環境不啟用）。
- `channels/inbound.py::dispatch`：`gate.allows(...)` 兩處改
```python
elder_id = gate.resolve_elder(msg.line_user_id)
if elder_id is None:
    msg.reply(BIND_FIRST_PROMPT)
    return
```
並把 `pipeline.process(...)`／`process_text(...)` 改傳 `elder_id=elder_id, line_user_id=msg.line_user_id`。
- `scheduler/worker.py`：`run_one`／`greet_one`／`care_one` 參數改 `elder_id`；推播前 `elder = accounts.get_elder(elder_id)`，`elder.line_user_id` 為空即 skip＋log；`_record_push(elder_id, kind, content)` 直接記（不再 `elder_by_line` 反查）。
- `reports/health.py`：刪除 `elder_line_user_id` 解析，直接 `risk_events.list_for_elder(elder_id)`；docstring 同步。
- `AccountService.guardian_line_ids`（by-line 版）若無其他呼叫端即刪除（`guardian_line_ids_of_elder` 保留）。

- [ ] 先改測試 → FAIL → 實作 → `uv run pytest -q` PASS → Commit `refactor: 入站閘門加深為長輩解析，排程與健康報告改 elder_id 會話鍵`

---

### Task 8: 全套件驗證與文件同步

**Files:** Modify `docs/mvp/06_DB_Schema.md`（turns／risk_events／conversation_summaries 三表定義與 ⚠ 標記）、`CONTEXT.md`（「會話（Session）」定義改「以 elder_id 識別；通道識別只活在邊界」）、`tests/fakes.py`（若有轉出舊名）。

- [ ] Step 1: `uv run pytest -q`＋`KINSUN_IT=1 DATABASE_URL=… uv run pytest -q`（全綠）＋`uv run ruff check src tests`＋`uv run ruff format --check src tests`
- [ ] Step 2: 全庫 `grep -rn "list_for_line_user\|is_consented_elder\|guardian_line_ids(" src` 確認無殘留舊呼叫（`is_consented_elder` 僅剩 jobs 允許的用途）
- [ ] Step 3: 文件更新＋Commit `docs: 會話主鍵遷移文件同步（DB Schema／領域語彙）`

---

## 後續（1C，不在本計畫）

`InboundMessage` 改帶 `(channel, external_id)`、`OutboundChannel` 收件人中立化＋依綁定路由、帳號讀取端（`get_elder_by_line` 等）切換 `channel_bindings`、`elders.line_user_id`／`guardians.line_user_id` 欄位退役、觀測五表補 `elder_id`。
