# 統一排程 P2（派送切換）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 用單一每分鐘的 `schedule-dispatch` job 取代現行四個用藥 job ＋ 一個回診 job，並把既有 `medications`／`appointments` 資料遷入 `schedules`；長輩收到的訊息與時刻**一字不變**。

**Architecture:** 派送 job 每分鐘向 store 問兩個問題（哪些一次性到點了、哪些重複型在這一分鐘該響），過期判定與聚合留在 job 層（store 保持 dumb）。訊息措辭依 kind 分派、程式組字不經 LLM。對話注入由 `ScheduleFacts` 取代 `MedicationFacts`／`AppointmentFacts`，**段落標題逐字沿用**以免動到 prompt。舊表舊模組**保留不刪**，回滾＝revert worker 接線。

**Tech Stack:** Python 3.12+、psycopg3、croniter、pytest、uv。

## Global Constraints

- 台灣繁體中文註解／docstring／commit；全形標點。
- 命名規範見 `AGENTS.md`；設計真相見 `docs/superpowers/specs/2026-07-25-統一排程系統-design.md`。
- TDD 強制：失敗測試 → 跑到失敗 → 最小實作 → 跑到通過 → commit。
- 分支 `Leo`，不 push、不改寫歷史。
- 每個 task 結束前跑 `uv run ruff check` 與 `ruff format --check`（**只對本刀碰到的檔案**——工作區另有他人未提交的 RAG 變更）。
- 全測試 `uv run pytest -q` 必須全綠才進下一個 task。

## 必須逐字保住的既有行為（本刀的驗收核心）

| 行為 | 出處 | 本刀怎麼保 |
| :-- | :-- | :-- |
| 用藥訊息 `{name}，{時段}該吃藥囉：{藥名、藥名}` | `medications/jobs.py:44` | 同字串；時段詞由**時刻**推算，四個預設鐘點落回原詞 |
| 多顆藥合併成一則 | `medications/jobs.py:31-35` | 按（長輩＋提醒時刻）聚合 |
| 回診長輩訊息、家屬訊息 | `appointments/jobs.py:48,55` | 同字串 |
| 回診發兩次（前一天＋當天） | `appointments/jobs.py:32-34` | 遷移時展開成兩列 |
| 出站不查同意（D-30 己-1） | 兩個 job | 不引入任何 consent 檢查 |
| 用藥／回診 `sent == 0` 不記 log（庚-14） | `medications/jobs.py:46`、`appointments/jobs.py:57` | 同政策 |
| 家屬通知不受長輩可達性影響 | `appointments/jobs.py:51-56` | 先發家屬再判長輩送達數 |
| 逐筆隔離失敗 | `scheduler/fanout.py` | 續用 `fanout_job` |

## File Structure

| 檔案 | 職責 |
| :-- | :-- |
| `src/kinsun/schedules/wording.py` | 時段詞對應、三種 kind 的訊息組字（純函式，無 I/O） |
| `src/kinsun/schedules/jobs.py` | `build_schedule_dispatch_job`：撈到期→過期作廢→聚合→送出→記帳 |
| `src/kinsun/schedules/facts.py` | `ScheduleFacts`（依 kind 產一段，標題逐字沿用舊 facts） |
| `src/kinsun/schedules/migration.py` | `backfill_from_legacy`：舊兩表→schedules，冪等可重跑 |
| `src/kinsun/reports/reminders.py`（修改） | 新增 `REMINDER_KIND_CUSTOM` |
| `src/kinsun/composition.py`（修改） | 建 `ScheduleService`、facts 三段換掉舊兩段、收進 `Core` |
| `src/kinsun/scheduler/worker.py`（修改） | 拔掉五個舊 job，掛上 `schedule-dispatch` |
| `src/kinsun/db.py`（修改） | `ensure_schema` 尾段呼叫遷移（fail-open） |

**不動**：`medications/`、`appointments/` 兩個模組與其表、API、App、LINE 選單（P3／P5 才動）。

---

### Task 1：訊息措辭（純函式）

**Files:**
- Create: `src/kinsun/schedules/wording.py`
- Test: `tests/test_schedules_wording.py`

**Interfaces:**
- Consumes: `Schedule`、`ScheduleKind`（P1）
- Produces:
  - `slot_label(hour: int) -> str`——早上／中午／晚上／睡前
  - `medication_text(elder_name: str, hour: int, titles: list[str]) -> str`
  - `appointment_texts(elder_name: str, title: str, when_word: str, event_time: str) -> tuple[str, str]`——(長輩版, 家屬版)
  - `custom_text(elder_name: str, title: str, minutes_ahead: int) -> str`

- [ ] **Step 1: 寫失敗測試**

```python
"""排程訊息措辭：時段詞對應與三種 kind 的組字（純函式，無 I/O）。"""

from __future__ import annotations

import pytest

from kinsun.schedules.wording import (
    appointment_texts,
    custom_text,
    medication_text,
    slot_label,
)


@pytest.mark.parametrize(
    ("hour", "expected"),
    [(8, "早上"), (12, "中午"), (18, "晚上"), (21, "睡前")],
)
def test_default_medication_hours_keep_their_original_words(hour, expected):
    # 四個預設鐘點必須落回原本的詞——家屬不改設定，長輩聽到的字就一字不變。
    assert slot_label(hour) == expected


@pytest.mark.parametrize(
    ("hour", "expected"),
    [(0, "早上"), (7, "早上"), (11, "中午"), (14, "中午"), (15, "晚上"), (19, "晚上"), (20, "睡前"), (23, "睡前")],
)
def test_slot_label_covers_every_hour(hour, expected):
    assert slot_label(hour) == expected


def test_medication_text_matches_the_legacy_wording():
    # 與 medications/jobs.py:44 逐字相同。
    assert medication_text("阿嬤", 8, ["血壓藥", "胃藥"]) == "阿嬤，早上該吃藥囉：血壓藥、胃藥"


def test_appointment_texts_match_the_legacy_wording_with_time():
    elder, guardian = appointment_texts("阿公", "心臟科回診 林口長庚", "明天", "10:30")
    assert elder == "阿公，明天 10:30 要回診囉：心臟科回診 林口長庚。記得準時，需要的話請家人陪您去。"
    assert guardian == "【金孫提醒】阿公 明天 10:30 要回診——心臟科回診 林口長庚。"


def test_appointment_texts_omit_the_time_when_unknown():
    # 舊行為：time 為空＝提醒不帶時間，且「今天」後面不留多餘空格。
    elder, guardian = appointment_texts("阿公", "牙科", "今天", "")
    assert elder == "阿公，今天要回診囉：牙科。記得準時，需要的話請家人陪您去。"
    assert guardian == "【金孫提醒】阿公 今天要回診——牙科。"


def test_custom_text_without_lead_time():
    assert custom_text("阿嬤", "去吃飯", 0) == "阿嬤，提醒您：去吃飯。"


def test_custom_text_with_lead_time():
    assert custom_text("阿嬤", "出門", 15) == "阿嬤，再過 15 分鐘要出門囉。"
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_wording.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.schedules.wording'`

- [ ] **Step 3: 最小實作**

```python
"""排程提醒的措辭：純函式、無 I/O、不經 LLM。

不經 LLM 與現行用藥／回診 job 一致：措辭可預期、零成本，也不會因模型漂移而變調。

時段詞刻意**不重用** `clock.py` 的 `_period_and_hour12`：那套詞彙是凌晨／上午／
中午／下午／晚上（報時用），而用藥講的是早上／中午／晚上／睡前。混用會讓 08:00
從「早上該吃藥」變成「上午該吃藥」、21:00 從「睡前」變成「晚上」。下面的分界點
挑成讓四個預設鐘點（8／12／18／21）落回原本的詞——家屬不改設定，長輩聽到的字
就一字不變。
"""

from __future__ import annotations

_MORNING_UNTIL = 11
_NOON_UNTIL = 15
_EVENING_UNTIL = 20


def slot_label(hour: int) -> str:
    if hour < _MORNING_UNTIL:
        return "早上"
    if hour < _NOON_UNTIL:
        return "中午"
    if hour < _EVENING_UNTIL:
        return "晚上"
    return "睡前"


def medication_text(elder_name: str, hour: int, titles: list[str]) -> str:
    """與 medications/jobs.py 的舊訊息逐字相同。"""
    return f"{elder_name}，{slot_label(hour)}該吃藥囉：{'、'.join(titles)}"


def appointment_texts(
    elder_name: str, title: str, when_word: str, event_time: str
) -> tuple[str, str]:
    """回傳（長輩版, 家屬版），與 appointments/jobs.py 的舊訊息逐字相同。

    event_time 為空＝未指定看診時刻，提醒不帶時間（庚-15 的舊語意）。
    """
    when_phrase = f"{when_word} {event_time} " if event_time else when_word
    elder = (
        f"{elder_name}，{when_phrase}要回診囉：{title}。記得準時，需要的話請家人陪您去。"
    )
    guardian = f"【金孫提醒】{elder_name} {when_phrase}要回診——{title}。"
    return elder, guardian


def custom_text(elder_name: str, title: str, minutes_ahead: int) -> str:
    """提前提醒才講「再過幾分鐘」；準時提醒講「提醒您」。"""
    if minutes_ahead > 0:
        return f"{elder_name}，再過 {minutes_ahead} 分鐘要{title}囉。"
    return f"{elder_name}，提醒您：{title}。"
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_wording.py -q`
Expected: 16 passed

- [ ] **Step 5: Commit**

```bash
uv run ruff check src/kinsun/schedules tests/test_schedules_wording.py
uv run ruff format --check src/kinsun/schedules tests/test_schedules_wording.py
git add src/kinsun/schedules/wording.py tests/test_schedules_wording.py
git commit -m "feat(schedules): 提醒措辭純函式（時段詞沿用用藥詞彙）"
```

---

### Task 2：新增 custom 提醒種類

**Files:**
- Modify: `src/kinsun/reports/reminders.py:49-58`
- Test: `tests/test_reports_reminders.py`（追加）

**Interfaces:**
- Produces: `REMINDER_KIND_CUSTOM = "custom"`，並加入 `REMINDER_KINDS`

- [ ] **Step 1: 寫失敗測試**

```python
def test_custom_is_a_known_reminder_kind():
    # 長輩用說的建的提醒送出後記這個 kind；健康報告與 admin 共用同一份列舉。
    from kinsun.reports.reminders import REMINDER_KIND_CUSTOM, REMINDER_KINDS

    assert REMINDER_KIND_CUSTOM == "custom"
    assert REMINDER_KIND_CUSTOM in REMINDER_KINDS
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_reports_reminders.py -q -k custom`
Expected: FAIL — `ImportError: cannot import name 'REMINDER_KIND_CUSTOM'`

- [ ] **Step 3: 最小實作**

在 `reports/reminders.py` 加：

```python
REMINDER_KIND_CUSTOM = "custom"
```

並加進 `REMINDER_KINDS` 元組（放在 appointment 之後）。同時在該檔頂端的長註解裡，把 `custom` 歸入「純觀測」那一類（與 medication／appointment 同組、同樣遵守庚-14 的 `sent == 0` 不記）。

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_reports_reminders.py -q`
Expected: 全過

- [ ] **Step 5: Commit**

```bash
git add src/kinsun/reports/reminders.py tests/test_reports_reminders.py
git commit -m "feat(reports): 提醒種類新增 custom"
```

---

### Task 3：派送 job

**Files:**
- Create: `src/kinsun/schedules/jobs.py`
- Test: `tests/test_schedules_jobs.py`

**Interfaces:**
- Consumes: `ScheduleStore`（P1）、`ChannelRouter`、`fanout_job`、Task 1 的措辭函式、Task 2 的 kind 常數
- Produces:
  - `build_schedule_dispatch_job(*, store, lookup_elder, guardians_of, router, clock, window_seconds=90, record=None, name="schedule-dispatch") -> Job`

**設計要點（實作前先讀）：**

- cron 固定 `* * * * *`（每分鐘），走 `fanout_job(cron=...)` 而非 `hour=`。
- `population()` 回傳**已聚合的送出單位**，`action()` 只負責送一則。聚合在母體端完成是 `fanout_job` 的既有約定。
- 母體組法：
  1. `now = clock()`，`window_start = now - window_seconds`。
  2. 重複型：把 `(window_start, now]` 內的每一分鐘轉成 `HH:MM` 清單（最多 `window_seconds // 60 + 1` 個），連同 `now.weekday()` 與今日零時 timestamp 呼叫 `list_due_repeating`。
  3. 一次性：`list_due_once(until=now.timestamp())`；逐筆判斷 `scheduled_at >= window_start` 才送，**早於窗的只呼叫 `mark_settled` 不送**（過期作廢，記 `logger.info`）。
  4. `kind=medication` 的到期列按 `(elder_id, 提醒小時)` 聚合成一筆；其餘逐筆一筆。
- 送出後：`mark_fired`；一次性另外 `mark_settled`。
- `item_id` 用 group_id／首列 schedule_id，供 Opik trace 標記。

- [ ] **Step 1: 寫失敗測試**

```python
"""schedule-dispatch job：到期送出、過期作廢、當日冪等、聚合與家屬同送。"""

from __future__ import annotations

from datetime import datetime
from zoneinfo import ZoneInfo

from kinsun.accounts.models import PrincipalType
from kinsun.schedules.jobs import build_schedule_dispatch_job
from kinsun.schedules.models import (
    Audience,
    CreatedBy,
    RepeatKind,
    Schedule,
    ScheduleKind,
)
from kinsun.schedules.store import FakeScheduleStore

TZ = ZoneInfo("Asia/Taipei")


class _Elder:
    def __init__(self, name: str) -> None:
        self.name = name


class _Router:
    """記錄送出的（principal_type, principal_id, text）；sent 數可控。"""

    def __init__(self, *, sent: int = 1) -> None:
        self.messages: list[tuple] = []
        self._sent = sent

    def send_text(self, principal_type, principal_id, text) -> int:
        self.messages.append((principal_type, principal_id, text))
        return self._sent


def _job(store, router, *, now: datetime, record=None, window_seconds: int = 90):
    return build_schedule_dispatch_job(
        store=store,
        lookup_elder=lambda elder_id: _Elder("阿嬤") if elder_id == "e1" else None,
        guardians_of=lambda elder_id: [],
        router=router,
        clock=lambda: now,
        window_seconds=window_seconds,
        record=record,
    )


def _daily(schedule_id, elder_id, hhmm, *, title="血壓藥", kind=ScheduleKind.MEDICATION):
    return Schedule(
        schedule_id=schedule_id,
        group_id=schedule_id,
        elder_id=elder_id,
        kind=kind,
        title=title,
        repeat_kind=RepeatKind.DAILY,
        repeat_time=hhmm,
        created_at=1.0,
    )


def _once(schedule_id, elder_id, at, *, title="去吃飯", kind=ScheduleKind.CUSTOM, **kw):
    return Schedule(
        schedule_id=schedule_id,
        group_id=schedule_id,
        elder_id=elder_id,
        kind=kind,
        title=title,
        repeat_kind=RepeatKind.ONCE,
        scheduled_at=at,
        created_at=1.0,
        **kw,
    )


def test_job_runs_every_minute():
    store, router = FakeScheduleStore(), _Router()
    job = _job(store, router, now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ))
    assert job.cron == "* * * * *"


def test_daily_medication_fires_at_its_minute():
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "e1", "08:00"))
    _job(store, router, now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ)).run()
    assert router.messages == [(PrincipalType.ELDER, "e1", "阿嬤，早上該吃藥囉：血壓藥")]


def test_multiple_medications_at_the_same_time_become_one_message():
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "e1", "08:00", title="血壓藥"))
    store.save(_daily("s2", "e1", "08:00", title="胃藥"))
    _job(store, router, now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ)).run()
    assert len(router.messages) == 1
    assert "血壓藥" in router.messages[0][2]
    assert "胃藥" in router.messages[0][2]


def test_medications_in_the_same_hour_merge_even_across_window_minutes():
    # 判定窗涵蓋 08:00 與 08:01 兩分鐘，聚合鍵是「長輩＋時段小時」，故仍為一則。
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "e1", "08:00", title="血壓藥"))
    store.save(_daily("s2", "e1", "08:01", title="胃藥"))
    _job(store, router, now=datetime(2026, 7, 25, 8, 1, tzinfo=TZ), window_seconds=90).run()
    assert len(router.messages) == 1


def test_medications_of_different_elders_never_merge():
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "e1", "08:00", title="血壓藥"))
    store.save(_daily("s2", "e2", "08:00", title="胃藥"))
    job = build_schedule_dispatch_job(
        store=store,
        lookup_elder=lambda _: _Elder("阿嬤"),
        guardians_of=lambda _: [],
        router=router,
        clock=lambda: datetime(2026, 7, 25, 8, 0, tzinfo=TZ),
    )
    job.run()
    assert {m[1] for m in router.messages} == {"e1", "e2"}


def test_repeating_schedule_is_not_sent_twice_in_one_day():
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "e1", "08:00"))
    now = datetime(2026, 7, 25, 8, 0, tzinfo=TZ)
    _job(store, router, now=now).run()
    _job(store, router, now=now).run()
    assert len(router.messages) == 1


def test_once_schedule_within_the_window_is_sent_and_settled():
    store, router = FakeScheduleStore(), _Router()
    now = datetime(2026, 7, 25, 20, 45, tzinfo=TZ)
    store.save(_once("s1", "e1", at=now.timestamp()))
    _job(store, router, now=now).run()
    assert router.messages == [(PrincipalType.ELDER, "e1", "阿嬤，提醒您：去吃飯。")]
    got = store.get("s1")
    assert got.settled_at is not None
    assert got.fired_at is not None


def test_once_schedule_older_than_the_window_is_settled_without_sending():
    # 過期就不發：只結案、不送，否則長輩吃完飯之後才被叫去吃飯。
    store, router = FakeScheduleStore(), _Router()
    now = datetime(2026, 7, 25, 21, 0, tzinfo=TZ)
    store.save(_once("s1", "e1", at=now.timestamp() - 600))
    _job(store, router, now=now, window_seconds=90).run()
    assert router.messages == []
    got = store.get("s1")
    assert got.settled_at is not None
    assert got.fired_at is None  # 沒送出就不可謊稱送過


def test_custom_with_lead_time_says_how_long_until_the_event():
    store, router = FakeScheduleStore(), _Router()
    now = datetime(2026, 7, 25, 20, 45, tzinfo=TZ)
    event = datetime(2026, 7, 25, 21, 0, tzinfo=TZ)
    store.save(_once("s1", "e1", at=now.timestamp(), title="出門", event_at=event.timestamp()))
    _job(store, router, now=now).run()
    assert router.messages[0][2] == "阿嬤，再過 15 分鐘要出門囉。"


def test_appointment_notifies_the_elder_and_the_family():
    store = FakeScheduleStore()
    router = _Router()
    now = datetime(2026, 7, 29, 9, 0, tzinfo=TZ)
    event = datetime(2026, 7, 30, 10, 30, tzinfo=TZ)
    store.save(
        _once(
            "s1",
            "e1",
            at=now.timestamp(),
            title="心臟科回診",
            kind=ScheduleKind.APPOINTMENT,
            event_at=event.timestamp(),
            audience=Audience.ELDER_AND_GUARDIAN,
        )
    )
    job = build_schedule_dispatch_job(
        store=store,
        lookup_elder=lambda _: _Elder("阿公"),
        guardians_of=lambda _: [type("G", (), {"guardian_id": "g1"})()],
        router=router,
        clock=lambda: now,
    )
    job.run()
    kinds = [m[0] for m in router.messages]
    assert PrincipalType.ELDER in kinds
    assert PrincipalType.GUARDIAN in kinds
    elder_msg = next(m[2] for m in router.messages if m[0] == PrincipalType.ELDER)
    assert elder_msg == (
        "阿公，明天 10:30 要回診囉：心臟科回診。記得準時，需要的話請家人陪您去。"
    )


def test_cancelled_schedule_is_never_sent():
    store, router = FakeScheduleStore(), _Router()
    now = datetime(2026, 7, 25, 8, 0, tzinfo=TZ)
    schedule = _daily("s1", "e1", "08:00")
    store.save(schedule)
    store.cancel_group("s1", now=now.timestamp() - 100)
    _job(store, router, now=now).run()
    assert router.messages == []


def test_unknown_elder_is_skipped():
    store, router = FakeScheduleStore(), _Router()
    store.save(_daily("s1", "ghost", "08:00"))
    _job(store, router, now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ)).run()
    assert router.messages == []


def test_nothing_is_logged_when_no_channel_delivered():
    # 庚-14：沒送到的提醒不該出現在家屬的健康報告裡。
    store, router = FakeScheduleStore(), _Router(sent=0)
    recorded = []
    store.save(_daily("s1", "e1", "08:00"))
    _job(
        store,
        router,
        now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ),
        record=lambda *a: recorded.append(a),
    ).run()
    assert recorded == []


def test_a_delivered_reminder_is_logged_with_its_kind():
    store, router = FakeScheduleStore(), _Router()
    recorded = []
    store.save(_daily("s1", "e1", "08:00"))
    _job(
        store,
        router,
        now=datetime(2026, 7, 25, 8, 0, tzinfo=TZ),
        record=lambda *a: recorded.append(a),
    ).run()
    assert len(recorded) == 1
    assert recorded[0][0] == "e1"
    assert recorded[0][1] == "medication"


def test_one_failing_item_does_not_stop_the_others():
    # 逐筆隔離（fanout 既有語意）：兩位長輩同時到期，其中一位送出時炸掉，
    # 另一位仍須收到。用不同長輩而非不同時刻，兩筆才會真的同輪到期。
    class _FlakyRouter(_Router):
        def send_text(self, principal_type, principal_id, text):
            if principal_id == "boom":
                raise RuntimeError("送不出去")
            return super().send_text(principal_type, principal_id, text)

    store, router = FakeScheduleStore(), _FlakyRouter()
    store.save(_daily("s1", "boom", "08:00", title="壞掉藥"))
    store.save(_daily("s2", "e1", "08:00", title="好藥"))
    job = build_schedule_dispatch_job(
        store=store,
        lookup_elder=lambda _: _Elder("阿嬤"),
        guardians_of=lambda _: [],
        router=router,
        clock=lambda: datetime(2026, 7, 25, 8, 0, tzinfo=TZ),
    )
    job.run()
    assert [m[1] for m in router.messages] == ["e1"]
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_jobs.py -q`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.schedules.jobs'`

- [ ] **Step 3: 最小實作**

依「設計要點」實作 `src/kinsun/schedules/jobs.py`。務必在 docstring 寫明：判定窗非補發窗；過期只結案不送的理由；聚合鍵為（長輩＋時段小時）。

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_jobs.py -q`
Expected: 全過

- [ ] **Step 5: Commit**

---

### Task 4：對話注入 ScheduleFacts

**Files:**
- Create: `src/kinsun/schedules/facts.py`
- Test: `tests/test_schedules_facts.py`

**Interfaces:**
- Consumes: `ScheduleStore`、`Schedule`
- Produces: `ScheduleFacts(store, *, kind: ScheduleKind, clock)`，實作 `FactProvider`（`facts(elder_id) -> FactSection | None`）

**段落標題逐字沿用舊 facts**（prompt 敏感，不可順手改措辭）：

- `medication`：`"\n這位長者目前固定服用的藥（系統設定的提醒時段，僅供參考、非醫療指示）：\n"`
- `appointment`：`"\n這位長者即將到來的回診（系統設定，僅供參考）：\n"`
- `custom`（新增）：`"\n這位長者自己交代要提醒的事：\n"`

項目格式：medication＝`{title}（{時段詞}）`（同 group 的多個時刻用「、」串）；appointment＝`{YYYY-MM-DD} {title}`；custom＝`{時間白話} {title}`。

- [ ] **Step 1～5**：比照前述 TDD 五步。測試至少涵蓋：無資料回 `None`／標題逐字相符／已取消不列入／過期的一次性不列入／同 group 多時刻併成一行。

---

### Task 5：資料遷移

**Files:**
- Create: `src/kinsun/schedules/migration.py`
- Modify: `src/kinsun/db.py`（`ensure_schema` 尾段呼叫，fail-open）
- Test: `tests/test_pg_schedules_migration.py`

**Interfaces:**
- Produces: `backfill_from_legacy(db, *, slot_hours: dict[str, int], appointment_hour: int, clock) -> int`（回傳遷入列數）

**規則：**

- `medications`：每筆的每個 slot 一列，`repeat_kind=daily`、`repeat_time` 取該 slot 的鐘點、`group_id`＝`medication_id`、`kind=medication`、`created_by=guardian`、`audience=elder`。
- `appointments`：每筆兩列（前一天、當天），`repeat_kind=once`、`scheduled_at`＝該日期的 `appointment_hour`、`event_at`＝`date`＋`time`（`time` 空則 None）、`group_id`＝`appointment_id`、`kind=appointment`、`created_by=guardian`、`audience=elder_and_guardian`。**已過期的回診不遷移。**
- 冪等：以 `group_id` 已存在判定跳過，可重跑。
- schedule_id 決定性：`f"{group_id}-{slot 或 offset}"`，重跑不會生出新列。

⚠ **既有庫升級路徑必測**：測試要先在舊表塞資料、再跑 `ensure_schema`，斷言遷移結果；並重跑一次驗冪等。空測試庫測不到這條路徑。

- [ ] **Step 1～5**：比照 TDD 五步。

---

### Task 6：接線 composition

**Files:**
- Modify: `src/kinsun/composition.py`
- Test: `tests/test_composition.py`（修改既有斷言）

**內容：**
- `assemble_core` 建 `PgScheduleStore` ＋ `ScheduleService`（吃三個 `SCHEDULE_*` 設定）。
- `Core` 新增 `schedule_store`、`schedules` 兩欄。
- facts 清單：`MedicationFacts`／`AppointmentFacts` **兩段換成三段** `ScheduleFacts`（medication／appointment／custom），順序沿用原位置（時間→稱呼→用藥→回診→自訂→守則→地點）。

⚠ 這是本刀第一個會改到既有測試的 task（`test_composition.py` 斷言 facts 組成）。改既有測試是預期內的，但只改與 facts 組成相關的斷言。

- [ ] **Step 1～5**：比照 TDD 五步。

---

### Task 7：切換 worker

**Files:**
- Modify: `src/kinsun/scheduler/worker.py`
- Test: `tests/test_scheduler_worker.py`（修改既有斷言）

**內容：**
- 拔掉 `build_medication_slot_job` ×4 與 `build_appointment_reminder_job` ×1 的接線與 import。
- 掛上 `build_schedule_dispatch_job`（吃 `SCHEDULE_DISPATCH_WINDOW_SECONDS`）。
- 啟動訊息（`main()` 的 print）更新：不再列四個用藥鐘點與回診鐘點，改列「排程提醒 每分鐘掃描」。

- [ ] **Step 1～5**：比照 TDD 五步。**最後跑一次全測試**：`uv run pytest -q` 必須全綠。

---

### Task 8：文件同步

**Files:** `docs/dev/00`（D-76 進度）、`05`（提醒管線段改寫、群組圖）、`07`（模組列、測試數）、`10`（類別圖補 jobs／facts）、`README`、`.env.example`（若有新鍵）。

- [ ] 更新上述文件與各自版頭；`docs/dev/README.md` 狀態表同步。
- [ ] 全測試 `uv run pytest -q` 全綠。
- [ ] Commit。

---

## 完成標準（P2 Definition of Done）

- [ ] `uv run pytest -q` 全綠（含 Pg）
- [ ] 「必須逐字保住的既有行為」八條各有測試守住
- [ ] 舊表 `medications`／`appointments` **仍在**、舊模組**仍在**（回滾靠 revert worker 接線）
- [ ] 遷移在「既有庫有舊資料」的情境下實測通過，且重跑冪等
- [ ] `docs/dev` 六份文件與 README 已同步
- [ ] 每個 commit 有 WHY／WHAT／IMPACT body
