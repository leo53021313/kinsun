# 延遲觀測細拆（memory_assemble 子 span）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or the Execute Plan phase of superpowers:sunnydata-design to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把情境組裝（memory_assemble，~2.9s）的內部耗時拆成可見的 Opik 子 span：mem0 內部三段（embedding／向量查詢／rerank）、逐路檢索、七路事實提供者、短期記憶查詢。

**Architecture:** 全部沿用既有 `kinsun.tracing.track`（停用即 identity、lazy 包 opik）。mem0 的部分在工廠建構後包**實例 bound method**；其餘直接加裝飾器。純觀測，不動任何決策路徑。

**Tech Stack:** Python 3.12、opik SDK（經 kinsun.tracing 間接使用）、mem0ai 2.0.10、pytest。

**Spec:** `docs/superpowers/specs/2026-07-30-延遲觀測細拆-design.md`

---

### Task 1: mem0 內部三段子 span

**Files:**
- Modify: `src/kinsun/memory/longterm/mem0_factory.py`
- Test: `tests/test_memory_longterm_mem0_factory.py`

- [ ] **Step 1: 寫失敗測試**（stub Memory 驗證三個包裝點、無 reranker、屬性缺席、停用 passthrough、`build_mem0_memory` 接線）

```python
# 追加到 tests/test_memory_longterm_mem0_factory.py
from kinsun.memory.longterm.mem0_factory import _instrument_tracing
from kinsun.tracing import client as tracing_client
from kinsun.tracing import decorators as tracing_decorators


class _StubEmbedder:
    def embed(self, text, memory_action=None):
        return [0.0]


class _StubVectorStore:
    def search(self, query, vectors, top_k=5, filters=None):
        return ["hit"]


class _StubReranker:
    def rerank(self, query, documents, top_k=None):
        return documents


class _StubMemory:
    def __init__(self, reranker=None):
        self.embedding_model = _StubEmbedder()
        self.vector_store = _StubVectorStore()
        self.reranker = reranker


def test_instrument_tracing_passthrough_when_disabled():
    tracing_client.reset_for_test()
    memory = _StubMemory(reranker=_StubReranker())
    assert _instrument_tracing(memory) is memory
    assert memory.embedding_model.embed("你好", "search") == [0.0]
    assert memory.vector_store.search(query="q", vectors=[0.1]) == ["hit"]
    assert memory.reranker.rerank("q", ["d"], 1) == ["d"]


def test_instrument_tracing_span_names_and_capture_policy(monkeypatch):
    import opik

    tracing_client.reset_for_test()
    seen: list[dict] = []
    monkeypatch.setattr(opik, "track", lambda **kw: (seen.append(kw), lambda f: f)[1])
    monkeypatch.setattr(tracing_decorators, "is_enabled", lambda: True)
    memory = _instrument_tracing(_StubMemory(reranker=_StubReranker()))
    memory.embedding_model.embed("你好", "search")
    memory.vector_store.search(query="q", vectors=[0.1])
    memory.reranker.rerank("q", ["doc"], 1)
    by_name = {kw["name"]: kw for kw in seen}
    assert by_name["mem0_embed"]["capture_output"] is False
    assert by_name["mem0_vector_search"]["ignore_arguments"] == ["vectors"]
    assert by_name["mem0_rerank"]["ignore_arguments"] == ["documents"]


def test_instrument_tracing_skips_absent_reranker(monkeypatch):
    import opik

    tracing_client.reset_for_test()
    seen: list[dict] = []
    monkeypatch.setattr(opik, "track", lambda **kw: (seen.append(kw), lambda f: f)[1])
    monkeypatch.setattr(tracing_decorators, "is_enabled", lambda: True)
    memory = _instrument_tracing(_StubMemory(reranker=None))
    memory.embedding_model.embed("你好", "search")
    memory.vector_store.search(query="q", vectors=[0.1])
    assert {kw["name"] for kw in seen} == {"mem0_embed", "mem0_vector_search"}


def test_instrument_tracing_survives_missing_attributes(caplog):
    class _Weird:  # 模擬 mem0 升版屬性改名
        pass

    weird = _Weird()
    assert _instrument_tracing(weird) is weird  # 不炸、原樣回傳


def test_build_mem0_memory_instruments_instance(monkeypatch):
    import mem0

    stub = _StubMemory()
    monkeypatch.setattr(mem0.Memory, "from_config", staticmethod(lambda cfg: stub))
    from kinsun.memory.longterm.mem0_factory import build_mem0_memory

    assert build_mem0_memory(load_settings(_ENV)) is stub
```

- [ ] **Step 2: 跑測試確認失敗**：`pytest tests/test_memory_longterm_mem0_factory.py -x -q` → ImportError `_instrument_tracing`

- [ ] **Step 3: 實作**

```python
# mem0_factory.py：新增 import 與函式；build_mem0_memory 尾行改為
#   return _instrument_tracing(Memory.from_config(build_mem0_config(settings)))
import logging

from kinsun import tracing

logger = logging.getLogger(__name__)


def _instrument_tracing(memory):
    """把 mem0 內部三段（embedding／向量查詢／rerank）包上 Opik span。

    延遲數據要拆到「rerank 佔多少」才有辦法決定它的去留（2026-07-30 spec）。
    包在**實例屬性**上、不動 mem0 類別；`keyword_search` 不包——Supabase provider
    繼承基底的 `return None`，零網路成本。屬性缺席（mem0 升版改名）記 warning
    後原樣回傳，比照 wrap_genai：觀測絕不可壞掉記憶功能。
    """
    try:
        memory.embedding_model.embed = tracing.track(
            name="mem0_embed", capture_input=True, capture_output=False
        )(memory.embedding_model.embed)
        memory.vector_store.search = tracing.track(
            name="mem0_vector_search",
            capture_input=True,
            capture_output=False,
            ignore_arguments=["vectors"],
        )(memory.vector_store.search)
        if getattr(memory, "reranker", None) is not None:
            memory.reranker.rerank = tracing.track(
                name="mem0_rerank",
                capture_input=True,
                capture_output=False,
                ignore_arguments=["documents"],
            )(memory.reranker.rerank)
    except AttributeError:
        logger.warning("mem0 實例屬性不符（升版改名？），內部子 span 略過")
    return memory
```

- [ ] **Step 4: 跑測試確認通過**：同 Step 2 → 全綠
- [ ] **Step 5: Commit**：`feat(tracing): mem0 內部三段（embed／向量查詢／rerank）子 span`

### Task 2: 逐路檢索 span

**Files:**
- Modify: `src/kinsun/memory/longterm/store.py:111`
- Test: `tests/test_memory_longterm_store.py`

- [ ] **Step 1: 寫失敗測試**（span 名註冊、行為不變）

```python
def test_search_raw_wrapped_with_span(monkeypatch):
    import opik

    from kinsun.tracing import client as tracing_client
    from kinsun.tracing import decorators as tracing_decorators

    tracing_client.reset_for_test()
    seen: list[dict] = []
    monkeypatch.setattr(opik, "track", lambda **kw: (seen.append(kw), lambda f: f)[1])
    monkeypatch.setattr(tracing_decorators, "is_enabled", lambda: True)
    store = Mem0LongTermStore(_FakeMemory([{"memory": "m"}]))  # 依既有測試的 fake 調整
    assert [i.text for i in store.search("e", "q")] == ["m"]
    assert any(kw["name"] == "mem0_search_raw" and kw["capture_output"] is False for kw in seen)
```

- [ ] **Step 2: 跑測試確認失敗**：`pytest tests/test_memory_longterm_store.py -x -q`
- [ ] **Step 3: 實作**：`_search_raw` 前加

```python
@tracing.track(name="mem0_search_raw", capture_input=True, capture_output=False)
def _search_raw(self, query: str, elder_id: str, top_k: int) -> list[dict]:
```

- [ ] **Step 4: 跑測試確認通過**（含既有並行／dedup／fail-safe 測試）
- [ ] **Step 5: Commit**：`feat(tracing): 長期記憶逐路檢索 span（原話／健康增補分開可見）`

### Task 3: gather_facts＋逐提供者 span

**Files:**
- Modify: `src/kinsun/memory/recall.py`
- Test: `tests/test_memory_recall.py`

- [ ] **Step 1: 寫失敗測試**

```python
def test_gather_facts_spans_carry_injection_index_and_class_name(monkeypatch):
    import opik

    from kinsun.tracing import client as tracing_client
    from kinsun.tracing import decorators as tracing_decorators

    tracing_client.reset_for_test()
    seen: list[dict] = []
    monkeypatch.setattr(opik, "track", lambda **kw: (seen.append(kw), lambda f: f)[1])
    monkeypatch.setattr(tracing_decorators, "is_enabled", lambda: True)
    s1 = FactSection("\nA：\n", ["a"])
    s2 = FactSection("\nB：\n", ["b"])
    ctx = _session(facts=[_FakeFacts(s1), _FakeFacts(s2)]).assemble("s", "x")
    assert ctx.injected.sections == [s1, s2]  # 順序契約不受包裝影響
    names = {kw["name"] for kw in seen}
    assert {"gather_facts", "facts_0__FakeFacts", "facts_1__FakeFacts"} <= names
```

- [ ] **Step 2: 跑測試確認失敗**：`pytest tests/test_memory_recall.py -x -q`
- [ ] **Step 3: 實作**：`__init__` 一次性包 provider；`_gather_facts` 改用包裝後
  callable 並加 `@tracing.track(name="gather_facts", capture_input=True,
  capture_output=False)`；`fetch` 改收 callable。既有 docstring 與並行結構保留。

```python
# __init__ 內，self._facts 之後：
# 逐提供者包 span（索引＝注入順序；ScheduleFacts 註冊三次、純類名會撞名）。
# 一次性包在建構時，wrapper 內的 lazy opik 快取才能跨輪重用。
self._tracked_facts = [
    tracing.track(
        name=f"facts_{index}_{type(provider).__name__}",
        capture_input=True,
        capture_output=True,
    )(provider.facts)
    for index, provider in enumerate(self._facts)
]

# _gather_facts 內：providers 改為 self._tracked_facts；
# fetch(provider) 改 fetch(provider_facts)，呼叫 provider_facts(elder_id)。
```

- [ ] **Step 4: 跑測試確認通過**（含既有順序回填／失敗略過測試）
- [ ] **Step 5: Commit**：`feat(tracing): 事實提供者七路逐一可見（gather_facts 子 span）`

### Task 4: 短期記憶 span

**Files:**
- Modify: `src/kinsun/memory/shortterm.py:90`
- Test: `tests/test_memory_shortterm.py`

- [ ] **Step 1: 寫失敗測試**（比照 Task 2 的模式；fake db 沿用該檔既有 fake）

```python
def test_recent_wrapped_with_span(monkeypatch):
    import opik

    from kinsun.tracing import client as tracing_client
    from kinsun.tracing import decorators as tracing_decorators

    tracing_client.reset_for_test()
    seen: list[dict] = []
    monkeypatch.setattr(opik, "track", lambda **kw: (seen.append(kw), lambda f: f)[1])
    monkeypatch.setattr(tracing_decorators, "is_enabled", lambda: True)
    store = PgMemoryStore(_db_returning([]), clock=_noon)  # 依該檔既有 fake 調整
    assert store.recent("e") == []
    assert any(kw["name"] == "shortterm_recent" for kw in seen)
```

- [ ] **Step 2: 跑測試確認失敗**：`pytest tests/test_memory_shortterm.py -x -q`
- [ ] **Step 3: 實作**：`shortterm.py` 加 `from kinsun import tracing`；`recent` 前加

```python
@tracing.track(name="shortterm_recent", capture_input=True, capture_output=False)
def recent(self, elder_id: str) -> list[Message]:
```

- [ ] **Step 4: 跑測試確認通過**
- [ ] **Step 5: Commit**：`feat(tracing): 短期記憶今日對話查詢 span`

### Task 5: 全量驗證與文件同步

- [ ] **Step 1**：`pytest tests/ -q -x --ignore=tests/e2e` 相關檔全跑（單機不併行，PG 整合測試不啟用）
- [ ] **Step 2**：`ruff check src tests && ruff format --check src tests`
- [ ] **Step 3**：docs/dev 同步——`05_架構與設計.md`（SessionMemory／Mem0LongTermStore 行補 span 記載、§12 Opik span 樹若有）、`07_模組規格與測試.md`（memory 列）、兩檔版頭與 `docs/dev/README.md` 狀態表
- [ ] **Step 4**：Commit：`docs(dev): 延遲觀測細拆——span 樹記載同步`
