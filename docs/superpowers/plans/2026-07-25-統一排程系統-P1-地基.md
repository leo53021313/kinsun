# 統一排程系統 P1（地基）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 立起 `schedules` 領域的資料層——模型、資料表、三件套 store、service——供後續四刀使用；本刀**不接線任何既有流程**，系統行為零變更。

**Architecture:** 一列資料 ＝ 一個鬧鐘（不是一件事），同一件事的多個鬧鐘以 `group_id` 串起。`event_at`（事件何時發生）與 `scheduled_at`／`repeat_time`（何時提醒）分離，話術才講得出「明天 10:30 要回診」「再過十五分鐘要去吃飯」。Store 保持 dumb（只做查詢與狀態轉移，不判斷過期）；過期判定屬派送邏輯，留給 P2。

**Tech Stack:** Python 3.12+（`StrEnum`、`from __future__ import annotations`）、psycopg3、pytest、pydantic-settings、uv。

## Global Constraints

- 全部程式碼註解、docstring、commit 訊息一律**台灣繁體中文**，全形標點。
- 命名規範（`AGENTS.md`）：模組目錄複數 `schedules/`；持久層三件套 `store.py` ＋ `ScheduleStore`（Protocol）＋ `PgScheduleStore` ＋ `FakeScheduleStore`；領域模型 `models.py`；主鍵全稱 `schedule_id`；表名複數 `schedules`；時間戳 `<動詞過去分詞>_at`、型別 `DOUBLE PRECISION`（epoch 秒）；`save`＝upsert、`list_for_<維度>`＝取清單、`get_*`＝取單筆、`remove`＝Service／Store 層刪除。
- 環境變數：`Settings` 欄位名＝環境變數鍵的小寫，一一對應無別名；每個程式讀取的鍵都必須列進 `.env.example`，附預設值與一行中文註解。
- 測試檔名 `test_<套件>_<檔>.py`；一個 seam 兩個 adapter 的等價合約用 `test_schedules_store_contract.py`，同一份斷言參數化跑 Fake（離線、每次跑）與 Pg（`KINSUN_IT=1` 才跑）。
- TDD 強制：先寫失敗測試 → 跑到確實失敗 → 最小實作 → 跑到通過 → commit。
- 分支：`Leo`（個人分支）。不 push、不改寫歷史。
- 每個 task 結束前跑 `uv run ruff check src tests && uv run ruff format --check src tests`。
- 設計真相：`docs/superpowers/specs/2026-07-25-統一排程系統-design.md`。

## File Structure

| 檔案 | 職責 |
| :-- | :-- |
| `src/kinsun/schedules/__init__.py` | 空套件標記（比照 `medications/__init__.py`） |
| `src/kinsun/schedules/models.py` | `ScheduleKind`／`RepeatKind`／`Audience`／`CreatedBy` 列舉、`Schedule` 與 `Occurrence` dataclass、`audience_for` |
| `src/kinsun/schedules/store.py` | `ScheduleError`、`ScheduleStore`（Protocol）、`PgScheduleStore`、`FakeScheduleStore` |
| `src/kinsun/schedules/service.py` | `ScheduleValidationError`、`ScheduleService`（建立展開 group、驗證、上限、取消授權） |
| `src/kinsun/db.py`（修改） | `SCHEDULES_DDL` ＋ `ensure_schema` 接線 |
| `src/kinsun/config.py`（修改） | 三個 `SCHEDULE_*` 設定鍵 |
| `.env.example`（修改） | 三個鍵的預設值與中文註解 |
| `tests/test_schedules_models.py` | 列舉值、`audience_for` |
| `tests/test_schedules_store_contract.py` | Fake／Pg 等價合約 |
| `tests/test_schedules_service.py` | 驗證、上限、group 展開、取消授權 |

**P1 不做**（留給後續刀）：派送 job、過期判定、訊息措辭、`ScheduleFacts`、工具、API、LINE 選單、App、資料遷移、清理 job。

---

### Task 1：領域模型與列舉

**Files:**
- Create: `src/kinsun/schedules/__init__.py`
- Create: `src/kinsun/schedules/models.py`
- Test: `tests/test_schedules_models.py`

**Interfaces:**
- Consumes: 無（本刀起點）
- Produces: `ScheduleKind`（`MEDICATION`／`APPOINTMENT`／`CUSTOM`）、`RepeatKind`（`ONCE`／`DAILY`／`WEEKLY`）、`Audience`（`ELDER`／`ELDER_AND_GUARDIAN`）、`CreatedBy`（`ELDER`／`GUARDIAN`）、`Schedule` frozen dataclass、`Occurrence` frozen dataclass、`audience_for(kind: ScheduleKind) -> Audience`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_schedules_models.py`：

```python
"""排程領域模型：列舉字面值與收件對象規則。"""

from __future__ import annotations

from kinsun.schedules.models import (
    Audience,
    CreatedBy,
    Occurrence,
    RepeatKind,
    Schedule,
    ScheduleKind,
    audience_for,
)


def test_kind_values_match_reminder_log_kinds():
    # 這三個字面值同時是 reminder_logs 的 kind（P2 送出時直接沿用），不可改。
    assert ScheduleKind.MEDICATION.value == "medication"
    assert ScheduleKind.APPOINTMENT.value == "appointment"
    assert ScheduleKind.CUSTOM.value == "custom"


def test_repeat_and_audience_and_created_by_values():
    assert [r.value for r in RepeatKind] == ["once", "daily", "weekly"]
    assert [a.value for a in Audience] == ["elder", "elder_and_guardian"]
    assert [c.value for c in CreatedBy] == ["elder", "guardian"]


def test_appointment_notifies_guardians_regardless_of_author():
    # 收件對象由 kind 決定、與誰建立無關：長輩自己用說的登記回診，家屬一樣該知道。
    assert audience_for(ScheduleKind.APPOINTMENT) == Audience.ELDER_AND_GUARDIAN


def test_other_kinds_notify_elder_only():
    assert audience_for(ScheduleKind.MEDICATION) == Audience.ELDER
    assert audience_for(ScheduleKind.CUSTOM) == Audience.ELDER


def test_schedule_defaults_are_inactive_markers():
    schedule = Schedule(
        schedule_id="s1",
        group_id="g1",
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="去吃飯",
        repeat_kind=RepeatKind.ONCE,
        scheduled_at=100.0,
        created_at=1.0,
    )
    assert schedule.cancelled_at is None
    assert schedule.settled_at is None
    assert schedule.fired_at is None
    assert schedule.audience == Audience.ELDER
    assert schedule.created_by == CreatedBy.GUARDIAN


def test_occurrence_carries_one_alarm():
    once = Occurrence(RepeatKind.ONCE, scheduled_at=100.0)
    weekly = Occurrence(RepeatKind.WEEKLY, repeat_time="15:00", repeat_weekday=2)
    assert once.repeat_time == ""
    assert once.repeat_weekday is None
    assert weekly.scheduled_at is None
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_models.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.schedules'`

- [ ] **Step 3: 最小實作**

建立 `src/kinsun/schedules/__init__.py`（空檔）與 `src/kinsun/schedules/models.py`：

```python
"""統一排程的資料模型。

一列 ＝ 一個鬧鐘，不是一件事（spec 2026-07-25-統一排程系統）。回診之所以前一天
與當天各響一次、用藥之所以早上與晚上各響一次，本質是同一件事有多個提醒時刻；
一列若硬要承載一件事，就得在欄位裡再長出一套提醒規則語法——複雜度只會搬家、
不會消失。同一件事的多個鬧鐘以 group_id 串起，家屬刪「這個藥」＝刪整組。
"""

from __future__ import annotations

from dataclasses import dataclass
from enum import StrEnum


class ScheduleKind(StrEnum):
    """排程類別。字面值同時是 reminder_logs 的 kind，送出時直接沿用。"""

    MEDICATION = "medication"
    APPOINTMENT = "appointment"
    CUSTOM = "custom"


class RepeatKind(StrEnum):
    ONCE = "once"
    DAILY = "daily"
    WEEKLY = "weekly"


class Audience(StrEnum):
    """收件對象。用列舉而非布林 notify_guardians：布林命名規範要求 is_／has_／can_
    前綴，硬套會得到 has_guardian_notice 這種彆扭名字；列舉也保留日後新增對象的空間。"""

    ELDER = "elder"
    ELDER_AND_GUARDIAN = "elder_and_guardian"


class CreatedBy(StrEnum):
    ELDER = "elder"
    GUARDIAN = "guardian"


def audience_for(kind: ScheduleKind) -> Audience:
    """收件對象由 kind 決定、與誰建立無關。

    長輩自己用說的登記回診，家屬一樣收到——那正是家屬最想被告知的一類事。
    """
    if kind == ScheduleKind.APPOINTMENT:
        return Audience.ELDER_AND_GUARDIAN
    return Audience.ELDER


@dataclass(frozen=True)
class Occurrence:
    """建立排程時描述「一個鬧鐘」的輸入：一次性給 scheduled_at，重複型給 repeat_time。"""

    repeat_kind: RepeatKind
    scheduled_at: float | None = None
    repeat_time: str = ""  # 'HH:MM'
    repeat_weekday: int | None = None  # 0–6，週一＝0（同 datetime.weekday()）


@dataclass(frozen=True)
class Schedule:
    """一個鬧鐘。時刻一律 epoch 秒（DOUBLE PRECISION）。

    event_at 與提醒時刻分離是話術能活過來的關鍵：回診事件在 10:30、提醒在前一天
    九點，訊息才講得出「明天 10:30 要回診」。None ＝ 事件與提醒同刻。

    settled_at 與 fired_at 刻意分開：過期的一次性排程必須結案，否則每分鐘掃描都
    會重複撈到同一批殭屍列；但它並沒有真的送出，記成 fired_at 會讓「最後送出時刻」
    說謊。已送出者兩欄同寫，過期作廢者只寫 settled_at。
    """

    schedule_id: str
    group_id: str
    elder_id: str
    kind: ScheduleKind
    title: str
    repeat_kind: RepeatKind
    scheduled_at: float | None = None  # once 的提醒時刻；重複型為 None
    repeat_time: str = ""  # 重複型的 'HH:MM'
    repeat_weekday: int | None = None  # weekly 的 0–6
    event_at: float | None = None
    audience: Audience = Audience.ELDER
    created_by: CreatedBy = CreatedBy.GUARDIAN
    created_at: float = 0.0
    cancelled_at: float | None = None
    settled_at: float | None = None
    fired_at: float | None = None
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_models.py -v`
Expected: 6 passed

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/schedules/__init__.py src/kinsun/schedules/models.py tests/test_schedules_models.py
git commit -m "$(cat <<'EOF'
feat(schedules): 統一排程領域模型——一列一個鬧鐘

WHY:
用藥、回診、語音自訂提醒本質都是「什麼時候該提醒誰」，卻各有一張表與一組
job。合併的第一步是先有一個能同時容納三者的模型。

WHAT:
核心取捨是「一列＝一個鬧鐘，不是一件事」：回診的前一天＋當天、用藥的早上＋
晚上，都是同一件事有多個提醒時刻；一列硬要承載一件事就得再長出一套提醒規則
語法，複雜度只會搬家。改以 group_id 串起同一件事的多個鬧鐘。

audience 用列舉而非布林 notify_guardians——布林命名規範要求 is_／has_／can_
前綴，硬套會得到 has_guardian_notice 這種彆扭名字；且由 kind 決定、與誰建立
無關：長輩自己登記的回診，家屬一樣該收到。

settled_at 與 fired_at 分開：過期的一次性排程必須結案否則每分鐘重複撈到，但
它沒有真的送出，記成 fired_at 會讓「最後送出時刻」說謊。

IMPACT:
純新增，未接線任何流程，系統行為零變更。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 2：資料表與 schema 接線

**Files:**
- Modify: `src/kinsun/db.py`（`APPOINTMENTS_NAMING_MIGRATION_DDL` 之後加 `SCHEDULES_DDL`；`ensure_schema` 內加一行）
- Test: `tests/test_pg_schedules_schema.py`

**Interfaces:**
- Consumes: 無
- Produces: 資料表 `schedules` 與四個索引；`SCHEDULES_DDL` 常數

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_pg_schedules_schema.py`：

```python
"""schedules 表的 schema 合約（連真庫；KINSUN_IT=1 才跑）。

既有庫升級路徑必測：空的測試庫測不到舊表升級，故斷言以「欄位齊備＋索引存在」
表述，重跑 ensure_schema 必須冪等。
"""

from __future__ import annotations

_EXPECTED_COLUMNS = {
    "schedule_id",
    "group_id",
    "elder_id",
    "kind",
    "title",
    "repeat_kind",
    "scheduled_at",
    "repeat_time",
    "repeat_weekday",
    "event_at",
    "audience",
    "created_by",
    "created_at",
    "cancelled_at",
    "settled_at",
    "fired_at",
}


def test_schedules_table_has_all_columns(pg_database):
    rows = pg_database.query(
        "SELECT column_name FROM information_schema.columns "
        "WHERE table_schema = 'public' AND table_name = 'schedules'"
    )
    assert _EXPECTED_COLUMNS <= {row[0] for row in rows}


def test_schedules_indexes_exist(pg_database):
    rows = pg_database.query(
        "SELECT indexname FROM pg_indexes "
        "WHERE schemaname = 'public' AND tablename = 'schedules'"
    )
    names = {row[0] for row in rows}
    assert "idx_schedules_elder_active" in names
    assert "idx_schedules_group" in names
    assert "idx_schedules_repeat" in names
    assert "idx_schedules_due_once" in names


def test_ensure_schema_is_idempotent(pg_database):
    from kinsun.config import load_dotenv, load_settings
    from kinsun.db import ensure_schema
    import os

    load_dotenv()
    url = os.environ["KINSUN_TEST_DATABASE_URL"]
    ensure_schema(url)  # 重跑一次不得拋例外
    ensure_schema(url)
    rows = pg_database.query("SELECT count(*) FROM schedules")
    assert rows[0][0] >= 0
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `KINSUN_IT=1 uv run pytest tests/test_pg_schedules_schema.py -v`
Expected: FAIL — `schedules` 表不存在（欄位集合為空、索引不存在）

若沒有測試庫可用（`KINSUN_IT` 未設）：此檔會被跳過，改在 Step 4 以 `test_schedules_store_contract.py` 的 Fake 參數驗證邏輯，Pg 部分待有測試庫時補跑。**不可因此略過建表**。

- [ ] **Step 3: 最小實作**

在 `src/kinsun/db.py` 的 `APPOINTMENTS_NAMING_MIGRATION_DDL` 區塊之後插入：

```python
# 統一排程（spec 2026-07-25-統一排程系統）：一列＝一個鬧鐘，group_id 串起同一件事。
# 全新表、無舊欄位要遷移，故建表與建索引可同批。三個部分索引都帶 WHERE：派送每分鐘
# 掃一次，只有「未取消」的列有意義，讓索引跟著縮小。
SCHEDULES_DDL = (
    "CREATE TABLE IF NOT EXISTS schedules ("
    "schedule_id TEXT PRIMARY KEY, group_id TEXT NOT NULL, elder_id TEXT NOT NULL, "
    "kind TEXT NOT NULL, title TEXT NOT NULL, repeat_kind TEXT NOT NULL, "
    "scheduled_at DOUBLE PRECISION, repeat_time TEXT NOT NULL DEFAULT '', "
    "repeat_weekday INTEGER, event_at DOUBLE PRECISION, "
    "audience TEXT NOT NULL DEFAULT 'elder', created_by TEXT NOT NULL, "
    "created_at DOUBLE PRECISION NOT NULL, cancelled_at DOUBLE PRECISION, "
    "settled_at DOUBLE PRECISION, fired_at DOUBLE PRECISION);"
    "CREATE INDEX IF NOT EXISTS idx_schedules_elder_active "
    "ON schedules (elder_id, cancelled_at);"
    "CREATE INDEX IF NOT EXISTS idx_schedules_group ON schedules (group_id);"
    "CREATE INDEX IF NOT EXISTS idx_schedules_repeat ON schedules (repeat_time) "
    "WHERE cancelled_at IS NULL;"
    "CREATE INDEX IF NOT EXISTS idx_schedules_due_once ON schedules (scheduled_at) "
    "WHERE cancelled_at IS NULL AND settled_at IS NULL;"
)
```

在 `ensure_schema` 內，`conn.execute(APPOINTMENTS_NAMING_MIGRATION_DDL)` 之後加一行：

```python
        conn.execute(SCHEDULES_DDL)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `KINSUN_IT=1 uv run pytest tests/test_pg_schedules_schema.py -v`
Expected: 3 passed

同時確認未破壞既有 schema 測試：
Run: `KINSUN_IT=1 uv run pytest tests/test_pg_db.py -v`
Expected: 全數 passed

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/db.py tests/test_pg_schedules_schema.py
git commit -m "$(cat <<'EOF'
feat(schedules): 建 schedules 資料表與四個索引

WHY:
統一排程的資料層需要落地的表。全新表、無既有欄位要遷移，故建表與建索引可以
同批——與 appointments 當年得先改名再建索引的情形不同。

WHAT:
三個部分索引都帶 WHERE cancelled_at IS NULL：派送每分鐘掃一次，只有未取消的
列有意義，讓索引跟著縮小而不是隨歷史資料無限長大。

IMPACT:
新表加入 ensure_schema，既有表與流程不受影響；此時尚無任何程式讀寫這張表。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 3：Store 三件套——寫入與查詢

**Files:**
- Create: `src/kinsun/schedules/store.py`
- Test: `tests/test_schedules_store_contract.py`

**Interfaces:**
- Consumes: `Schedule`、`ScheduleKind`、`RepeatKind`、`Audience`、`CreatedBy`（Task 1）；`Database`、`_Errors`（`kinsun.db`）
- Produces:
  - `ScheduleError(Exception)`
  - `ScheduleStore` Protocol：`save(schedule) -> None`、`get(schedule_id) -> Schedule | None`、`list_for_elder(elder_id) -> list[Schedule]`、`list_for_group(group_id) -> list[Schedule]`、`cancel_group(group_id, *, now: float) -> None`
  - `PgScheduleStore(db: Database)`、`FakeScheduleStore()`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_schedules_store_contract.py`：

```python
"""ScheduleStore 合約：Fake 與 Pg 兩個 adapter 必須對同一情境給出相同結果。

Fake 每次都跑；Pg 需 KINSUN_IT=1（連真庫）。斷言一律以 ns 前綴 scope 到本測試
自己的資料，才能在共用真庫上以「成員／排除」關係斷言而互不干擾。
"""

from __future__ import annotations

import pytest

from kinsun.schedules.models import (
    Audience,
    CreatedBy,
    RepeatKind,
    Schedule,
    ScheduleKind,
)
from kinsun.schedules.store import FakeScheduleStore, PgScheduleStore


@pytest.fixture(params=["fake", "pg"])
def store(request):
    if request.param == "pg":
        return PgScheduleStore(request.getfixturevalue("pg_database"))
    return FakeScheduleStore()


def _once(schedule_id: str, group_id: str, elder_id: str, at: float, **kw) -> Schedule:
    return Schedule(
        schedule_id=schedule_id,
        group_id=group_id,
        elder_id=elder_id,
        kind=kw.pop("kind", ScheduleKind.CUSTOM),
        title=kw.pop("title", "去吃飯"),
        repeat_kind=RepeatKind.ONCE,
        scheduled_at=at,
        created_at=1.0,
        **kw,
    )


def _daily(schedule_id: str, group_id: str, elder_id: str, hhmm: str, **kw) -> Schedule:
    return Schedule(
        schedule_id=schedule_id,
        group_id=group_id,
        elder_id=elder_id,
        kind=kw.pop("kind", ScheduleKind.MEDICATION),
        title=kw.pop("title", "血壓藥"),
        repeat_kind=RepeatKind.DAILY,
        repeat_time=hhmm,
        created_at=1.0,
        **kw,
    )


def test_get_returns_saved_schedule_with_all_fields(store, ns):
    saved = _once(
        f"{ns}s1",
        f"{ns}g1",
        f"{ns}e1",
        at=1000.0,
        kind=ScheduleKind.APPOINTMENT,
        title="心臟科回診",
        event_at=2000.0,
        audience=Audience.ELDER_AND_GUARDIAN,
        created_by=CreatedBy.ELDER,
    )
    store.save(saved)
    got = store.get(f"{ns}s1")
    assert got == saved


def test_get_returns_none_when_absent(store, ns):
    assert store.get(f"{ns}nope") is None


def test_save_upserts_on_conflict(store, ns):
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0, title="原本"))
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=2000.0, title="改過"))
    got = store.get(f"{ns}s1")
    assert got is not None
    assert got.title == "改過"
    assert got.scheduled_at == 2000.0
    assert len(store.list_for_elder(f"{ns}e1")) == 1


def test_list_for_elder_excludes_cancelled(store, ns):
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0))
    store.save(_once(f"{ns}s2", f"{ns}g2", f"{ns}e1", at=2000.0, cancelled_at=50.0))
    ids = {s.schedule_id for s in store.list_for_elder(f"{ns}e1")}
    assert ids == {f"{ns}s1"}


def test_list_for_elder_excludes_settled(store, ns):
    # 已結案的一次性排程不是「目前有效的事」，上限計算與清單都不該算進去。
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0))
    store.save(_once(f"{ns}s2", f"{ns}g2", f"{ns}e1", at=2000.0, settled_at=50.0))
    ids = {s.schedule_id for s in store.list_for_elder(f"{ns}e1")}
    assert ids == {f"{ns}s1"}


def test_list_for_elder_is_scoped_to_that_elder(store, ns):
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0))
    store.save(_once(f"{ns}s2", f"{ns}g2", f"{ns}e2", at=1000.0))
    ids = {s.schedule_id for s in store.list_for_elder(f"{ns}e1")}
    assert ids == {f"{ns}s1"}


def test_list_for_group_returns_every_alarm_of_one_thing(store, ns):
    # 同一顆藥早晚各一個鬧鐘：兩列、同 group。
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00"))
    store.save(_daily(f"{ns}s2", f"{ns}g1", f"{ns}e1", "21:00"))
    store.save(_daily(f"{ns}s3", f"{ns}g2", f"{ns}e1", "12:00"))
    ids = {s.schedule_id for s in store.list_for_group(f"{ns}g1")}
    assert ids == {f"{ns}s1", f"{ns}s2"}


def test_cancel_group_cancels_every_alarm_in_it(store, ns):
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00"))
    store.save(_daily(f"{ns}s2", f"{ns}g1", f"{ns}e1", "21:00"))
    store.cancel_group(f"{ns}g1", now=99.0)
    assert store.list_for_elder(f"{ns}e1") == []
    assert all(s.cancelled_at == 99.0 for s in store.list_for_group(f"{ns}g1"))


def test_cancel_group_leaves_other_groups_untouched(store, ns):
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00"))
    store.save(_daily(f"{ns}s2", f"{ns}g2", f"{ns}e1", "21:00"))
    store.cancel_group(f"{ns}g1", now=99.0)
    ids = {s.schedule_id for s in store.list_for_elder(f"{ns}e1")}
    assert ids == {f"{ns}s2"}
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_store_contract.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.schedules.store'`

- [ ] **Step 3: 最小實作**

建立 `src/kinsun/schedules/store.py`：

```python
"""統一排程儲存：Protocol、Postgres 實作與記憶體替身。

Store 保持 dumb：只做查詢與狀態轉移，不判斷「過期」——那是派送邏輯（需要
判定窗），留在 jobs 層。這道界線讓合約測試能對兩個 adapter 用同一份斷言。
"""

from __future__ import annotations

from dataclasses import replace
from typing import Protocol

from kinsun.db import Database, _Errors
from kinsun.schedules.models import (
    Audience,
    CreatedBy,
    RepeatKind,
    Schedule,
    ScheduleKind,
)


class ScheduleError(Exception):
    """排程資料讀寫失敗。"""


_COLUMNS = (
    "schedule_id, group_id, elder_id, kind, title, repeat_kind, scheduled_at, "
    "repeat_time, repeat_weekday, event_at, audience, created_by, created_at, "
    "cancelled_at, settled_at, fired_at"
)

# 「目前有效」＝未取消且未結案。上限計算、清單、對話注入全部共用這個定義。
_ACTIVE = "cancelled_at IS NULL AND settled_at IS NULL"


class ScheduleStore(Protocol):
    def save(self, schedule: Schedule) -> None: ...
    def get(self, schedule_id: str) -> Schedule | None: ...
    def list_for_elder(self, elder_id: str) -> list[Schedule]: ...
    def list_for_group(self, group_id: str) -> list[Schedule]: ...
    def cancel_group(self, group_id: str, *, now: float) -> None: ...


def _to_schedule(row: tuple) -> Schedule:
    return Schedule(
        schedule_id=row[0],
        group_id=row[1],
        elder_id=row[2],
        kind=ScheduleKind(row[3]),
        title=row[4],
        repeat_kind=RepeatKind(row[5]),
        scheduled_at=row[6],
        repeat_time=row[7],
        repeat_weekday=row[8],
        event_at=row[9],
        audience=Audience(row[10]),
        created_by=CreatedBy(row[11]),
        created_at=row[12],
        cancelled_at=row[13],
        settled_at=row[14],
        fired_at=row[15],
    )


def _is_active(schedule: Schedule) -> bool:
    return schedule.cancelled_at is None and schedule.settled_at is None


class PgScheduleStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: ScheduleError(f"排程資料存取失敗：{m}"))

    def save(self, schedule: Schedule) -> None:
        self._db.execute(
            f"INSERT INTO schedules ({_COLUMNS}) "
            "VALUES (%s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s, %s) "
            "ON CONFLICT (schedule_id) DO UPDATE SET "
            "group_id = EXCLUDED.group_id, elder_id = EXCLUDED.elder_id, "
            "kind = EXCLUDED.kind, title = EXCLUDED.title, "
            "repeat_kind = EXCLUDED.repeat_kind, scheduled_at = EXCLUDED.scheduled_at, "
            "repeat_time = EXCLUDED.repeat_time, repeat_weekday = EXCLUDED.repeat_weekday, "
            "event_at = EXCLUDED.event_at, audience = EXCLUDED.audience, "
            "created_by = EXCLUDED.created_by, created_at = EXCLUDED.created_at, "
            "cancelled_at = EXCLUDED.cancelled_at, settled_at = EXCLUDED.settled_at, "
            "fired_at = EXCLUDED.fired_at",
            (
                schedule.schedule_id,
                schedule.group_id,
                schedule.elder_id,
                schedule.kind.value,
                schedule.title,
                schedule.repeat_kind.value,
                schedule.scheduled_at,
                schedule.repeat_time,
                schedule.repeat_weekday,
                schedule.event_at,
                schedule.audience.value,
                schedule.created_by.value,
                schedule.created_at,
                schedule.cancelled_at,
                schedule.settled_at,
                schedule.fired_at,
            ),
        )

    def get(self, schedule_id: str) -> Schedule | None:
        row = self._db.query_one(
            f"SELECT {_COLUMNS} FROM schedules WHERE schedule_id = %s", (schedule_id,)
        )
        return _to_schedule(row) if row else None

    def list_for_elder(self, elder_id: str) -> list[Schedule]:
        rows = self._db.query(
            f"SELECT {_COLUMNS} FROM schedules WHERE elder_id = %s AND {_ACTIVE} "
            "ORDER BY scheduled_at NULLS LAST, repeat_time, title",
            (elder_id,),
        )
        return [_to_schedule(r) for r in rows]

    def list_for_group(self, group_id: str) -> list[Schedule]:
        rows = self._db.query(
            f"SELECT {_COLUMNS} FROM schedules WHERE group_id = %s "
            "ORDER BY scheduled_at NULLS LAST, repeat_time",
            (group_id,),
        )
        return [_to_schedule(r) for r in rows]

    def cancel_group(self, group_id: str, *, now: float) -> None:
        # 只蓋還沒取消的列：重複呼叫不會把第一次的取消時刻改掉。
        self._db.execute(
            "UPDATE schedules SET cancelled_at = %s "
            "WHERE group_id = %s AND cancelled_at IS NULL",
            (now, group_id),
        )


class FakeScheduleStore:
    """ScheduleStore 的記憶體替身（測試用，不碰 DB）。"""

    def __init__(self) -> None:
        self._rows: dict[str, Schedule] = {}

    def save(self, schedule: Schedule) -> None:
        self._rows[schedule.schedule_id] = schedule

    def get(self, schedule_id: str) -> Schedule | None:
        return self._rows.get(schedule_id)

    def list_for_elder(self, elder_id: str) -> list[Schedule]:
        rows = [
            s for s in self._rows.values() if s.elder_id == elder_id and _is_active(s)
        ]
        return sorted(rows, key=_sort_key)

    def list_for_group(self, group_id: str) -> list[Schedule]:
        rows = [s for s in self._rows.values() if s.group_id == group_id]
        return sorted(rows, key=_sort_key)

    def cancel_group(self, group_id: str, *, now: float) -> None:
        for schedule_id, schedule in list(self._rows.items()):
            if schedule.group_id == group_id and schedule.cancelled_at is None:
                self._rows[schedule_id] = replace(schedule, cancelled_at=now)


def _sort_key(schedule: Schedule) -> tuple:
    """與 Pg 的 ORDER BY scheduled_at NULLS LAST, repeat_time, title 等價。"""
    has_no_time = schedule.scheduled_at is None
    return (has_no_time, schedule.scheduled_at or 0.0, schedule.repeat_time, schedule.title)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_store_contract.py -v`
Expected: 9 passed（Fake 參數）；Pg 參數在無 `KINSUN_IT` 時 skip

有測試庫時另跑：
Run: `KINSUN_IT=1 uv run pytest tests/test_schedules_store_contract.py -v`
Expected: 18 passed

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/schedules/store.py tests/test_schedules_store_contract.py
git commit -m "$(cat <<'EOF'
feat(schedules): ScheduleStore 三件套——寫入、查詢與整組取消

WHY:
統一排程需要一個持久層，且必須讓 Fake 與 Pg 在同一份斷言下等價，否則離線
測試通過但真庫行為不同，等於沒測。

WHAT:
Store 刻意保持 dumb：只做查詢與狀態轉移，不判斷「過期」——過期需要判定窗，
那是派送邏輯。這道界線正是合約測試能對兩個 adapter 用同一份斷言的前提。

取消一律以 group 為單位：長輩說「明天不去了」時心裡想的是那件事，只取消其中
一個鬧鐘的話另一個還是會響，比沒取消更糟。UPDATE 帶 cancelled_at IS NULL
條件，重複呼叫不會把第一次的取消時刻改掉。

「目前有效」統一定義為未取消且未結案（_ACTIVE），上限計算、清單與日後的對話
注入全部共用，避免三處各自定義而漂移。

IMPACT:
純新增，尚無呼叫端。合約測試 9 條，Fake 每次跑、Pg 需 KINSUN_IT=1。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 4：Store 到期查詢與送出狀態轉移

**Files:**
- Modify: `src/kinsun/schedules/store.py`（Protocol 加四個方法；兩個實作各補四個）
- Modify: `tests/test_schedules_store_contract.py`（追加測試）

**Interfaces:**
- Consumes: Task 3 的 `ScheduleStore`、`PgScheduleStore`、`FakeScheduleStore`
- Produces（追加到 Protocol）：
  - `list_due_once(*, until: float) -> list[Schedule]`——未取消、未結案且 `scheduled_at <= until`
  - `list_due_repeating(*, times: tuple[str, ...], weekday: int, not_fired_since: float) -> list[Schedule]`——未取消、`repeat_time ∈ times`、weekly 另比對 `repeat_weekday`、且 `fired_at` 為 None 或早於 `not_fired_since`
  - `mark_fired(schedule_id: str, *, now: float) -> None`
  - `mark_settled(schedule_id: str, *, now: float) -> None`

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_schedules_store_contract.py` 末尾追加：

```python
def test_list_due_once_returns_only_reached_alarms(store, ns):
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0))
    store.save(_once(f"{ns}s2", f"{ns}g2", f"{ns}e1", at=3000.0))
    ids = {s.schedule_id for s in store.list_due_once(until=2000.0)}
    assert f"{ns}s1" in ids
    assert f"{ns}s2" not in ids


def test_list_due_once_excludes_settled_and_cancelled(store, ns):
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0, settled_at=1500.0))
    store.save(_once(f"{ns}s2", f"{ns}g2", f"{ns}e1", at=1000.0, cancelled_at=1500.0))
    ids = {s.schedule_id for s in store.list_due_once(until=2000.0)}
    assert f"{ns}s1" not in ids
    assert f"{ns}s2" not in ids


def test_list_due_repeating_matches_any_minute_in_window(store, ns):
    # 判定窗可能橫跨兩個分鐘值（掃描抖動），故傳入的是一組 HH:MM。
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00"))
    store.save(_daily(f"{ns}s2", f"{ns}g2", f"{ns}e1", "08:01"))
    store.save(_daily(f"{ns}s3", f"{ns}g3", f"{ns}e1", "09:00"))
    ids = {
        s.schedule_id
        for s in store.list_due_repeating(
            times=("08:00", "08:01"), weekday=2, not_fired_since=0.0
        )
    }
    assert ids == {f"{ns}s1", f"{ns}s2"}


def test_list_due_repeating_respects_weekday_for_weekly(store, ns):
    weekly = Schedule(
        schedule_id=f"{ns}s1",
        group_id=f"{ns}g1",
        elder_id=f"{ns}e1",
        kind=ScheduleKind.CUSTOM,
        title="上課",
        repeat_kind=RepeatKind.WEEKLY,
        repeat_time="15:00",
        repeat_weekday=2,
        created_at=1.0,
    )
    store.save(weekly)
    matched = store.list_due_repeating(times=("15:00",), weekday=2, not_fired_since=0.0)
    missed = store.list_due_repeating(times=("15:00",), weekday=3, not_fired_since=0.0)
    assert {s.schedule_id for s in matched} == {f"{ns}s1"}
    assert {s.schedule_id for s in missed} == set()


def test_list_due_repeating_skips_already_fired_today(store, ns):
    # 當日冪等：今天送過的重複型排程不再出現在到期清單。
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00", fired_at=5000.0))
    store.save(_daily(f"{ns}s2", f"{ns}g2", f"{ns}e1", "08:00", fired_at=100.0))
    ids = {
        s.schedule_id
        for s in store.list_due_repeating(
            times=("08:00",), weekday=2, not_fired_since=1000.0
        )
    }
    assert f"{ns}s1" not in ids
    assert f"{ns}s2" in ids


def test_list_due_repeating_excludes_cancelled(store, ns):
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00", cancelled_at=10.0))
    ids = {
        s.schedule_id
        for s in store.list_due_repeating(times=("08:00",), weekday=2, not_fired_since=0.0)
    }
    assert f"{ns}s1" not in ids


def test_mark_fired_records_send_time_without_settling(store, ns):
    store.save(_daily(f"{ns}s1", f"{ns}g1", f"{ns}e1", "08:00"))
    store.mark_fired(f"{ns}s1", now=777.0)
    got = store.get(f"{ns}s1")
    assert got is not None
    assert got.fired_at == 777.0
    assert got.settled_at is None  # 重複型永遠不結案


def test_mark_settled_closes_a_once_alarm_without_claiming_it_was_sent(store, ns):
    # 過期作廢的路徑：結案但沒送出，fired_at 必須維持 None，否則「最後送出時刻」說謊。
    store.save(_once(f"{ns}s1", f"{ns}g1", f"{ns}e1", at=1000.0))
    store.mark_settled(f"{ns}s1", now=888.0)
    got = store.get(f"{ns}s1")
    assert got is not None
    assert got.settled_at == 888.0
    assert got.fired_at is None
    assert store.list_due_once(until=9999.0) == []
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_store_contract.py -v`
Expected: 8 個新測試 FAIL with `AttributeError: 'FakeScheduleStore' object has no attribute 'list_due_once'`（前 9 個仍 PASS）

- [ ] **Step 3: 最小實作**

在 `src/kinsun/schedules/store.py` 的 `ScheduleStore` Protocol 內追加：

```python
    def list_due_once(self, *, until: float) -> list[Schedule]: ...
    def list_due_repeating(
        self, *, times: tuple[str, ...], weekday: int, not_fired_since: float
    ) -> list[Schedule]: ...
    def mark_fired(self, schedule_id: str, *, now: float) -> None: ...
    def mark_settled(self, schedule_id: str, *, now: float) -> None: ...
```

在 `PgScheduleStore` 內追加：

```python
    def list_due_once(self, *, until: float) -> list[Schedule]:
        rows = self._db.query(
            f"SELECT {_COLUMNS} FROM schedules "
            f"WHERE repeat_kind = %s AND {_ACTIVE} AND scheduled_at <= %s "
            "ORDER BY scheduled_at",
            (RepeatKind.ONCE.value, until),
        )
        return [_to_schedule(r) for r in rows]

    def list_due_repeating(
        self, *, times: tuple[str, ...], weekday: int, not_fired_since: float
    ) -> list[Schedule]:
        # weekly 比對星期、daily 不限；當日冪等以 fired_at 早於今日零時判定。
        rows = self._db.query(
            f"SELECT {_COLUMNS} FROM schedules "
            "WHERE cancelled_at IS NULL AND repeat_time = ANY(%s) "
            "AND (repeat_kind = %s OR (repeat_kind = %s AND repeat_weekday = %s)) "
            "AND (fired_at IS NULL OR fired_at < %s) "
            "ORDER BY repeat_time, title",
            (
                list(times),
                RepeatKind.DAILY.value,
                RepeatKind.WEEKLY.value,
                weekday,
                not_fired_since,
            ),
        )
        return [_to_schedule(r) for r in rows]

    def mark_fired(self, schedule_id: str, *, now: float) -> None:
        self._db.execute(
            "UPDATE schedules SET fired_at = %s WHERE schedule_id = %s", (now, schedule_id)
        )

    def mark_settled(self, schedule_id: str, *, now: float) -> None:
        self._db.execute(
            "UPDATE schedules SET settled_at = %s WHERE schedule_id = %s", (now, schedule_id)
        )
```

在 `FakeScheduleStore` 內追加（`replace` 已在 Task 3 的檔頭 import）：

```python
    def list_due_once(self, *, until: float) -> list[Schedule]:
        rows = [
            s
            for s in self._rows.values()
            if s.repeat_kind == RepeatKind.ONCE
            and _is_active(s)
            and s.scheduled_at is not None
            and s.scheduled_at <= until
        ]
        return sorted(rows, key=_sort_key)

    def list_due_repeating(
        self, *, times: tuple[str, ...], weekday: int, not_fired_since: float
    ) -> list[Schedule]:
        rows = [
            s
            for s in self._rows.values()
            if s.cancelled_at is None
            and s.repeat_time in times
            and (
                s.repeat_kind == RepeatKind.DAILY
                or (s.repeat_kind == RepeatKind.WEEKLY and s.repeat_weekday == weekday)
            )
            and (s.fired_at is None or s.fired_at < not_fired_since)
        ]
        return sorted(rows, key=_sort_key)

    def mark_fired(self, schedule_id: str, *, now: float) -> None:
        schedule = self._rows.get(schedule_id)
        if schedule is not None:
            self._rows[schedule_id] = replace(schedule, fired_at=now)

    def mark_settled(self, schedule_id: str, *, now: float) -> None:
        schedule = self._rows.get(schedule_id)
        if schedule is not None:
            self._rows[schedule_id] = replace(schedule, settled_at=now)
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_store_contract.py -v`
Expected: 17 passed（Fake）

Run: `KINSUN_IT=1 uv run pytest tests/test_schedules_store_contract.py -v`
Expected: 34 passed

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/schedules/store.py tests/test_schedules_store_contract.py
git commit -m "$(cat <<'EOF'
feat(schedules): 到期查詢與送出狀態轉移

WHY:
P2 的派送 job 每分鐘要問資料層兩個問題：哪些一次性排程到點了、哪些重複型
排程在這一分鐘該響。這兩個查詢的正確性直接決定長輩收不收得到提醒。

WHAT:
list_due_repeating 收的是一組 HH:MM 而非單一值：判定窗（吸收每分鐘掃描的
抖動）可能橫跨兩個分鐘值，只比對單一分鐘會讓抖動變成漏發。

當日冪等交給 not_fired_since 參數（呼叫端傳今日零時），而不是在 store 裡算
「今天」——store 沒有時區也不該有，時間語意一律由呼叫端注入的 clock 決定。

mark_fired 與 mark_settled 分成兩個方法而非一個帶旗標：重複型只會 fired、
過期作廢只會 settled、正常送出的一次性兩者都寫。旗標寫法會讓呼叫端每次都得
回想「True 是哪個意思」。

IMPACT:
純新增方法，既有合約測試不受影響。仍無呼叫端。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 5：設定鍵

**Files:**
- Modify: `src/kinsun/config.py:244`（`appointment_reminder_hour` 之後）
- Modify: `.env.example:149`（`APPOINTMENT_REMINDER_HOUR` 之後）
- Test: `tests/test_config.py`（追加）

**Interfaces:**
- Consumes: 無
- Produces: `Settings.schedule_max_active_per_elder: int`（預設 20）、`Settings.schedule_dispatch_window_seconds: int`（預設 90）、`Settings.schedule_max_days_ahead: int`（預設 365）

`SCHEDULE_RETENTION_DAYS` **不在本刀**——它只服務 P5 的清理 job，現在加等於加一個沒有讀取端的鍵。

- [ ] **Step 1: 寫失敗測試**

在 `tests/test_config.py` 末尾追加：

```python
def test_schedule_settings_defaults():
    settings = load_settings(BASE_ENV)
    assert settings.schedule_max_active_per_elder == 20
    assert settings.schedule_dispatch_window_seconds == 90
    assert settings.schedule_max_days_ahead == 365


def test_schedule_settings_overridable():
    settings = load_settings(
        {
            **BASE_ENV,
            "SCHEDULE_MAX_ACTIVE_PER_ELDER": "5",
            "SCHEDULE_DISPATCH_WINDOW_SECONDS": "120",
            "SCHEDULE_MAX_DAYS_AHEAD": "30",
        }
    )
    assert settings.schedule_max_active_per_elder == 5
    assert settings.schedule_dispatch_window_seconds == 120
    assert settings.schedule_max_days_ahead == 30


def test_schedule_dispatch_window_rejects_zero():
    with pytest.raises(ConfigError, match="SCHEDULE_DISPATCH_WINDOW_SECONDS"):
        load_settings({**BASE_ENV, "SCHEDULE_DISPATCH_WINDOW_SECONDS": "0"})
```

寫法對齊本檔既有慣例：`BASE_ENV` 字典 ＋ `load_settings({**BASE_ENV, "KEY": "值"})`，不用 monkeypatch、不另建輔助函式。

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_config.py -k schedule -v`
Expected: FAIL — `AttributeError: 'Settings' object has no attribute 'schedule_max_active_per_elder'`

- [ ] **Step 3: 最小實作**

在 `src/kinsun/config.py` 的 `appointment_reminder_hour: int = 8` 之後插入：

```python
    # 統一排程（spec 2026-07-25）：每位長輩的有效排程上限，防模型連續誤判灌爆行程表。
    schedule_max_active_per_elder: Annotated[
        int, BeforeValidator(_positive("SCHEDULE_MAX_ACTIVE_PER_ELDER"))
    ] = 20
    # 到期判定窗（秒）。⚠ 不是補發窗：決策 8 為「過期就不發」，此窗只吸收「每分鐘
    # 掃描」本身的抖動——scheduled_at 是秒級、掃描是分鐘級，沒有容許量的話每一則
    # 提醒都會晚到最多一分鐘、甚至因 tick 漂移整筆漏掉。停機超過此窗即作廢。
    schedule_dispatch_window_seconds: Annotated[
        int, BeforeValidator(_positive("SCHEDULE_DISPATCH_WINDOW_SECONDS"))
    ] = 90
    # 允許排到多久以後（天）：擋模型把「下週三」算成明年的荒謬時刻。
    schedule_max_days_ahead: Annotated[
        int, BeforeValidator(_positive("SCHEDULE_MAX_DAYS_AHEAD"))
    ] = 365
```

在 `.env.example` 的 `APPOINTMENT_REMINDER_HOUR=8` 之後插入：

```bash
# ────── 統一排程（spec 2026-07-25） ──────
# 每位長輩的有效排程上限（防止誤判把行程表灌爆）
SCHEDULE_MAX_ACTIVE_PER_ELDER=20
# 到期判定窗（秒）；⚠ 非補發窗，只吸收每分鐘掃描的抖動，停機超過即作廢
SCHEDULE_DISPATCH_WINDOW_SECONDS=90
# 允許排到多久以後（天）
SCHEDULE_MAX_DAYS_AHEAD=365
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_config.py -v`
Expected: 全數 passed（含 3 個新測試）

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/config.py .env.example tests/test_config.py
git commit -m "$(cat <<'EOF'
feat(config): 統一排程的三個設定鍵

WHY:
排程 service 需要兩個防呆上限（每人筆數、排程可到多遠），派送 job 需要一個
到期判定窗。三者都該可調而非寫死——內測期間很可能要收緊或放寬。

WHAT:
SCHEDULE_DISPATCH_WINDOW_SECONDS 的註解花了三行講「它不是補發窗」：決策 8
明定過期不補發，但每分鐘掃描本身就有抖動，沒有容許量的話每一則提醒都會晚到
最多一分鐘。這個鍵最容易被後人誤解成「停機補發」而擅自調大，故在程式碼與
.env.example 兩處都寫死語意。

SCHEDULE_RETENTION_DAYS 刻意不在本刀加入——它只服務 P5 的清理 job，現在加
等於留一個沒有讀取端的鍵。

IMPACT:
三個新鍵皆有預設值，既有部署不需要改 .env 就能運作。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 6：ScheduleService

**Files:**
- Create: `src/kinsun/schedules/service.py`
- Test: `tests/test_schedules_service.py`

**Interfaces:**
- Consumes: `Schedule`、`Occurrence`、`ScheduleKind`、`RepeatKind`、`CreatedBy`、`audience_for`（Task 1）；`ScheduleStore`（Task 3、4）
- Produces:
  - `ScheduleValidationError(Exception)`
  - `ScheduleService(store, *, clock: Callable[[], datetime], new_id: Callable[[], str] | None = None, max_active_per_elder: int = 20, max_days_ahead: int = 365)`
  - `create(*, elder_id: str, kind: ScheduleKind, title: str, created_by: CreatedBy, occurrences: tuple[Occurrence, ...], event_at: float | None = None) -> list[Schedule]`
  - `list_for_elder(elder_id: str) -> list[Schedule]`
  - `cancel_group(group_id: str, *, requested_by: CreatedBy) -> None`

- [ ] **Step 1: 寫失敗測試**

建立 `tests/test_schedules_service.py`：

```python
"""ScheduleService：group 展開、輸入驗證、上限與取消授權。"""

from __future__ import annotations

from datetime import datetime
from zoneinfo import ZoneInfo

import pytest

from kinsun.schedules.models import CreatedBy, Occurrence, RepeatKind, ScheduleKind
from kinsun.schedules.service import ScheduleService, ScheduleValidationError
from kinsun.schedules.store import FakeScheduleStore

TZ = ZoneInfo("Asia/Taipei")
NOW = datetime(2026, 7, 25, 20, 0, tzinfo=TZ)


def _service(**kw) -> ScheduleService:
    counter = {"n": 0}

    def new_id() -> str:
        counter["n"] += 1
        return f"id{counter['n']}"

    return ScheduleService(
        kw.pop("store", FakeScheduleStore()),
        clock=lambda: NOW,
        new_id=new_id,
        **kw,
    )


def _at(hour: int, minute: int = 0, day: int = 25) -> float:
    return datetime(2026, 7, day, hour, minute, tzinfo=TZ).timestamp()


def test_create_one_alarm_returns_one_row_grouped_to_itself():
    service = _service()
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="去吃飯",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(20, 45)),),
    )
    assert len(rows) == 1
    assert rows[0].group_id == rows[0].schedule_id
    assert rows[0].created_by == CreatedBy.ELDER


def test_create_many_alarms_share_one_group():
    # 同一顆藥早晚各一個鬧鐘：兩列、同 group，家屬刪「這個藥」才能一次刪乾淨。
    service = _service()
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.MEDICATION,
        title="血壓藥",
        created_by=CreatedBy.GUARDIAN,
        occurrences=(
            Occurrence(RepeatKind.DAILY, repeat_time="08:00"),
            Occurrence(RepeatKind.DAILY, repeat_time="21:00"),
        ),
    )
    assert len(rows) == 2
    assert len({r.group_id for r in rows}) == 1
    assert {r.repeat_time for r in rows} == {"08:00", "21:00"}


def test_create_sets_audience_from_kind_not_author():
    service = _service()
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.APPOINTMENT,
        title="心臟科回診",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(9, 0, day=30)),),
        event_at=_at(10, 30, day=30),
    )
    assert rows[0].audience.value == "elder_and_guardian"
    assert rows[0].event_at == _at(10, 30, day=30)


def test_create_persists_to_store():
    store = FakeScheduleStore()
    service = _service(store=store)
    service.create(
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="散步",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.DAILY, repeat_time="17:00"),),
    )
    assert len(store.list_for_elder("e1")) == 1


def test_create_rejects_blank_title():
    service = _service()
    with pytest.raises(ScheduleValidationError, match="事情"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="   ",
            created_by=CreatedBy.ELDER,
            occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(21)),),
        )


def test_create_rejects_empty_occurrences():
    service = _service()
    with pytest.raises(ScheduleValidationError, match="時間"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="散步",
            created_by=CreatedBy.ELDER,
            occurrences=(),
        )


def test_create_rejects_time_in_the_past():
    service = _service()
    with pytest.raises(ScheduleValidationError, match="過去"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="去吃飯",
            created_by=CreatedBy.ELDER,
            occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(19)),),
        )


def test_create_rejects_time_too_far_ahead():
    service = _service(max_days_ahead=7)
    with pytest.raises(ScheduleValidationError, match="太遠"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="去吃飯",
            created_by=CreatedBy.ELDER,
            occurrences=(
                Occurrence(RepeatKind.ONCE, scheduled_at=_at(20, 0, day=25) + 30 * 86400),
            ),
        )


def test_create_rejects_malformed_repeat_time():
    service = _service()
    with pytest.raises(ScheduleValidationError, match="時刻"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="散步",
            created_by=CreatedBy.ELDER,
            occurrences=(Occurrence(RepeatKind.DAILY, repeat_time="25:99"),),
        )


def test_create_rejects_weekly_without_weekday():
    service = _service()
    with pytest.raises(ScheduleValidationError, match="星期"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="上課",
            created_by=CreatedBy.ELDER,
            occurrences=(Occurrence(RepeatKind.WEEKLY, repeat_time="15:00"),),
        )


def test_create_rejects_when_over_active_limit():
    service = _service(max_active_per_elder=2)
    service.create(
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="散步",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.DAILY, repeat_time="17:00"),),
    )
    with pytest.raises(ScheduleValidationError, match="太多"):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.CUSTOM,
            title="喝水",
            created_by=CreatedBy.ELDER,
            occurrences=(
                Occurrence(RepeatKind.DAILY, repeat_time="10:00"),
                Occurrence(RepeatKind.DAILY, repeat_time="14:00"),
            ),
        )


def test_create_is_atomic_when_limit_exceeded():
    # 超過上限時一列都不該寫進去，否則長輩會拿到「半組」鬧鐘。
    store = FakeScheduleStore()
    service = _service(store=store, max_active_per_elder=1)
    with pytest.raises(ScheduleValidationError):
        service.create(
            elder_id="e1",
            kind=ScheduleKind.MEDICATION,
            title="血壓藥",
            created_by=CreatedBy.GUARDIAN,
            occurrences=(
                Occurrence(RepeatKind.DAILY, repeat_time="08:00"),
                Occurrence(RepeatKind.DAILY, repeat_time="21:00"),
            ),
        )
    assert store.list_for_elder("e1") == []


def test_elder_cannot_cancel_what_the_family_set_up():
    store = FakeScheduleStore()
    service = _service(store=store)
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.MEDICATION,
        title="血壓藥",
        created_by=CreatedBy.GUARDIAN,
        occurrences=(Occurrence(RepeatKind.DAILY, repeat_time="08:00"),),
    )
    with pytest.raises(ScheduleValidationError, match="家人"):
        service.cancel_group(rows[0].group_id, requested_by=CreatedBy.ELDER)
    assert len(store.list_for_elder("e1")) == 1


def test_elder_can_cancel_what_they_set_themselves():
    store = FakeScheduleStore()
    service = _service(store=store)
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="去吃飯",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(20, 45)),),
    )
    service.cancel_group(rows[0].group_id, requested_by=CreatedBy.ELDER)
    assert store.list_for_elder("e1") == []


def test_guardian_can_cancel_what_the_elder_set():
    store = FakeScheduleStore()
    service = _service(store=store)
    rows = service.create(
        elder_id="e1",
        kind=ScheduleKind.CUSTOM,
        title="去吃飯",
        created_by=CreatedBy.ELDER,
        occurrences=(Occurrence(RepeatKind.ONCE, scheduled_at=_at(20, 45)),),
    )
    service.cancel_group(rows[0].group_id, requested_by=CreatedBy.GUARDIAN)
    assert store.list_for_elder("e1") == []


def test_cancel_unknown_group_is_silent():
    service = _service()
    service.cancel_group("nope", requested_by=CreatedBy.GUARDIAN)  # 不得拋
```

- [ ] **Step 2: 跑測試確認失敗**

Run: `uv run pytest tests/test_schedules_service.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.schedules.service'`

- [ ] **Step 3: 最小實作**

建立 `src/kinsun/schedules/service.py`：

```python
"""統一排程服務：把「一件事」展開成一組鬧鐘，並在寫入前擋掉壞輸入。

驗證放在 service 而非各呼叫端：建立排程有三個入口（家屬 App、LINE 選單、長輩
語音工具），規則若散在三處，最先漂移的一定是最少人看的那處——而那處正是模型
在講話。
"""

from __future__ import annotations

import re
import uuid
from collections.abc import Callable
from datetime import datetime

from kinsun.schedules.models import (
    CreatedBy,
    Occurrence,
    RepeatKind,
    Schedule,
    ScheduleKind,
    audience_for,
)
from kinsun.schedules.store import ScheduleStore


class ScheduleValidationError(Exception):
    """排程輸入不合法。訊息為可直接講給人聽的白話（工具層原樣轉述給長輩）。"""


_HHMM = re.compile(r"^([01]\d|2[0-3]):([0-5]\d)$")


class ScheduleService:
    def __init__(
        self,
        store: ScheduleStore,
        *,
        clock: Callable[[], datetime],
        new_id: Callable[[], str] | None = None,
        max_active_per_elder: int = 20,
        max_days_ahead: int = 365,
    ) -> None:
        self._store = store
        self._clock = clock
        self._new_id = new_id or (lambda: uuid.uuid4().hex)
        self._max_active = max_active_per_elder
        self._max_days_ahead = max_days_ahead

    def create(
        self,
        *,
        elder_id: str,
        kind: ScheduleKind,
        title: str,
        created_by: CreatedBy,
        occurrences: tuple[Occurrence, ...],
        event_at: float | None = None,
    ) -> list[Schedule]:
        """把一件事展開成一組鬧鐘寫入。全部驗證通過才寫，不寫半組。"""
        cleaned = title.strip()
        if not cleaned:
            raise ScheduleValidationError("要提醒的事情不能是空的。")
        if not occurrences:
            raise ScheduleValidationError("至少要有一個提醒時間。")
        now = self._clock().timestamp()
        for occurrence in occurrences:
            self._validate(occurrence, now=now)
        self._check_limit(elder_id, adding=len(occurrences))

        group_id = self._new_id()
        rows = [
            Schedule(
                schedule_id=group_id if index == 0 else self._new_id(),
                group_id=group_id,
                elder_id=elder_id,
                kind=kind,
                title=cleaned,
                repeat_kind=occurrence.repeat_kind,
                scheduled_at=occurrence.scheduled_at,
                repeat_time=occurrence.repeat_time,
                repeat_weekday=occurrence.repeat_weekday,
                event_at=event_at,
                audience=audience_for(kind),
                created_by=created_by,
                created_at=now,
            )
            for index, occurrence in enumerate(occurrences)
        ]
        for row in rows:
            self._store.save(row)
        return rows

    def list_for_elder(self, elder_id: str) -> list[Schedule]:
        return self._store.list_for_elder(elder_id)

    def cancel_group(self, group_id: str, *, requested_by: CreatedBy) -> None:
        """取消一件事的全部鬧鐘。

        長輩不能取消家屬設的排程（spec 安全條 2）：吃藥與回診是家人替他把關的
        事，一句「不要再提醒我吃藥了」就刪掉，等於讓系統幫忙他停藥。查無此組
        則靜默——重複取消不該炸，呼叫端也不必先查一次。
        """
        rows = self._store.list_for_group(group_id)
        if not rows:
            return
        if requested_by == CreatedBy.ELDER and rows[0].created_by == CreatedBy.GUARDIAN:
            raise ScheduleValidationError("這是家人幫您設定的，要取消的話我跟他們說一聲好嗎？")
        self._store.cancel_group(group_id, now=self._clock().timestamp())

    def _validate(self, occurrence: Occurrence, *, now: float) -> None:
        if occurrence.repeat_kind == RepeatKind.ONCE:
            at = occurrence.scheduled_at
            if at is None:
                raise ScheduleValidationError("一次性的提醒要有確切時間。")
            if at <= now:
                raise ScheduleValidationError("那個時間已經過去了。")
            if at > now + self._max_days_ahead * 86400:
                raise ScheduleValidationError("那個時間太遠了。")
            return
        if not _HHMM.match(occurrence.repeat_time):
            raise ScheduleValidationError("重複提醒要有正確的時刻。")
        if occurrence.repeat_kind == RepeatKind.WEEKLY and occurrence.repeat_weekday is None:
            raise ScheduleValidationError("每週提醒要說是星期幾。")

    def _check_limit(self, elder_id: str, *, adding: int) -> None:
        active = len(self._store.list_for_elder(elder_id))
        if active + adding > self._max_active:
            raise ScheduleValidationError("您記的事情有點太多了，要不要先取消幾件？")
```

- [ ] **Step 4: 跑測試確認通過**

Run: `uv run pytest tests/test_schedules_service.py -v`
Expected: 16 passed

- [ ] **Step 5: Lint 與 commit**

```bash
uv run ruff check src tests && uv run ruff format --check src tests
git add src/kinsun/schedules/service.py tests/test_schedules_service.py
git commit -m "$(cat <<'EOF'
feat(schedules): ScheduleService——group 展開、驗證與取消授權

WHY:
建立排程有三個入口（家屬 App、LINE 選單、長輩語音工具）。驗證規則若散在三處，
最先漂移的一定是最少人看的那處——而那處正是模型在講話。故全部收斂到 service。

WHAT:
建立是全有全無：所有 occurrence 驗證通過、且不超過上限，才開始寫。超限時寫了
一半會讓長輩拿到「半組」鬧鐘——早上會響、晚上不會，比整組沒建更難察覺。

長輩不能取消家屬設的排程：吃藥與回診是家人替他把關的事，一句「不要再提醒我吃
藥了」就刪掉，等於讓系統幫忙他停藥。反向（家屬取消長輩設的）則放行。

錯誤訊息一律寫成可以直接講給長輩聽的白話，工具層原樣轉述即可，不必再翻譯一次
——翻譯就是又一個會漂移的地方。

取消查無此組時靜默：重複取消不該炸，呼叫端也不必先查一次。

IMPACT:
純新增，尚未接進 composition。P3 的 API 與 P4 的語音工具都會走這一層。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

### Task 7：文件同步

**Files:**
- Modify: `docs/dev/00_決策清單.md`（新增本次決策）
- Modify: `docs/dev/05_架構與設計.md`（ER 圖加 `schedules` 表、模組表加 `schedules/`、版頭）
- Modify: `docs/dev/08_專案結構指南.md`（新增 `schedules/` 目錄說明、版頭）
- Modify: `docs/dev/10_類別關係.md`（`Schedule`／`ScheduleStore`／`ScheduleService` 入圖、版頭）
- Modify: `docs/dev/README.md`（文件狀態表）
- Modify: `docs/superpowers/specs/2026-07-25-統一排程系統-design.md`（刪除語意改軟刪）

**Interfaces:**
- Consumes: Task 1–6 的全部產出
- Produces: 無程式介面

- [ ] **Step 1: 修正設計文件的刪除語意**

設計文件目前寫「DELETE `/api/v1/schedules/{schedule_id}` 刪除（`delete_schedule` handler → Service `remove`）」，但 Task 6 實作的是**軟刪**（`cancel_group` 寫 `cancelled_at`）。改為：

```markdown
| DELETE | `/api/v1/schedules/{schedule_id}` | 取消（`delete_schedule` handler → Service `cancel_group`，軟刪寫 `cancelled_at`） |
```

並在該表下方補一句：

```markdown
刪除一律為**軟刪**：`cancelled_at` 寫入時刻而非移除列。健康報告與每晚反思會回看歷史，硬刪會讓「他上週取消了回診提醒」這件事憑空消失；保留期由 P5 的清理 job 依 `SCHEDULE_RETENTION_DAYS` 處理。
```

- [ ] **Step 2: 更新 `docs/dev` 四份文件與 README**

依 `docs/dev/15_文檔與維護指南.md` §2 對照表逐項更新：

- `00_決策清單.md`：新增一條決策，記錄「用藥／回診／自訂三表合一、一列一個鬧鐘、入口合一」與十二項核定內容，並指向本 spec。
- `05_架構與設計.md`：ER 圖加入 `elders ||--o{ schedules`；模組表新增 `schedules/`；更新版頭版本與日期。
- `08_專案結構指南.md`：新增 `src/kinsun/schedules/` 段落，列出四個檔案的職責；更新版頭。
- `10_類別關係.md`：加入 `Schedule`、`ScheduleStore`（Protocol）、`PgScheduleStore`、`FakeScheduleStore`、`ScheduleService` 與其相依線；更新版頭。
- `README.md`：文件狀態表更新上述四份的版本／日期。

**明確不改**（P1 尚未動到）：`02_專案簡報與PRD.md`、`03_BDD情境.md`、`06_API設計規範.md`——用藥與回診的 US、BDD 情境與 API 在 P1 完全沒變，現在改會讓文件描述一個還不存在的狀態。

- [ ] **Step 3: 全測試回歸**

Run: `uv run pytest -q`
Expected: 全數 passed（既有測試一條都不能紅——P1 未接線任何既有流程）

Run: `KINSUN_IT=1 uv run pytest -q`
Expected: 全數 passed

- [ ] **Step 4: Lint**

Run: `uv run ruff check src tests && uv run ruff format --check src tests`
Expected: 無錯誤

- [ ] **Step 5: Commit**

```bash
git add docs/
git commit -m "$(cat <<'EOF'
docs: 統一排程 P1 的文檔同步與刪除語意修正

WHY:
docs/dev 同步鐵律要求程式改動當下就更新受影響文件，不可事後補。同時 P1 實作
過程發現設計文件與實作不一致：文件寫 DELETE 走 Service remove（硬刪），但實作
選了軟刪。

WHAT:
刪除定為軟刪並在設計文件寫明理由——健康報告與每晚反思會回看歷史，硬刪會讓
「他上週取消了回診提醒」這件事憑空消失。

02_PRD、03_BDD、06_API 三份刻意不動：用藥與回診在 P1 完全沒變，現在改會讓
文件描述一個還不存在的狀態。

IMPACT:
文件變更，無程式碼影響。

Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>
EOF
)"
```

---

## 完成標準（P1 Definition of Done）

- [ ] `uv run pytest -q` 全綠；`KINSUN_IT=1 uv run pytest -q` 全綠
- [ ] `uv run ruff check src tests` 與 `ruff format --check` 無錯誤
- [ ] `schedules` 表在 `ensure_schema` 中建立，重跑冪等
- [ ] 合約測試 17 條在 Fake 與 Pg 兩個 adapter 上等價通過
- [ ] 既有測試**一條都沒改**（P1 未接線任何既有流程，改到既有測試即代表越界）
- [ ] `docs/dev` 四份文件與 README 狀態表已更新
- [ ] 七個 commit 皆有 WHY／WHAT／IMPACT body
