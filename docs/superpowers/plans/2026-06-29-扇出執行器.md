# 扇出執行器（fanout_job）實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: 用 superpowers:subagent-driven-development（建議）或 superpowers:executing-plans 逐任務實作。步驟用核取方塊（`- [ ]`）追蹤。

**Goal:** 把 4 個 job builder 各自重抄的「每天某點 → 遍歷母體 → 逐筆 try/except + log」收進單一深 `fanout_job`，每個 job 退回到只提供「母體來源」與「單一對象動作」。

**Architecture:** 新增 `scheduler/fanout.py` 的 `fanout_job(*, name, hour, population, action, ...)`，吸收 cron 組裝、遍歷、逐筆失敗隔離與 log，回傳既有 `Job`。四個 builder（consolidation／greeting／inactivity／medication）改為薄包裝：inactivity 的門檻過濾放進 `action` 提早 return、medication 的「按 elder 分組」放進 `population`、同意守門放進 `action`。

**Tech Stack:** Python 3.12、`croniter`（已有）、pytest。**無新依賴。**

## Global Constraints

- src layout（`src/kinsun/`），pytest `pythonpath=["src","."]`。
- 失敗隔離：單一對象失敗不可中斷整批（沿用既有語意；重複通知/重跑是高成本錯誤）。
- 各 builder 公開簽名**不變**（呼叫端 `scheduler/worker.py` 不需改），既有 job 測試需保持綠燈。
- ruff `E,W,F,I,B,UP`、line-length 100、quote-style double。commit 結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 不自動 push；只在 Leo 分支工作。

---

## File Structure

```
src/kinsun/scheduler/fanout.py     # 新增：fanout_job（深執行器）
src/kinsun/scheduler/jobs.py       # build_consolidation_job 改用 fanout_job
src/kinsun/proactive/jobs.py       # build_greeting_job / build_inactivity_job 改用 fanout_job
src/kinsun/medication/jobs.py      # build_medication_slot_job 改用 fanout_job
tests/test_fanout.py               # 新增：fanout_job 單元測試
tests/test_scheduler_jobs.py       # 既有，保持綠燈
tests/test_proactive_jobs.py       # 既有，保持綠燈
tests/test_medication_jobs.py      # 既有，保持綠燈
```

---

## Task 1：fanout_job 深執行器

**Files:**
- Create: `src/kinsun/scheduler/fanout.py`
- Test: `tests/test_fanout.py`

**Interfaces:**
- Consumes：`Job`（`kinsun.scheduler.scheduler`）
- Produces：`fanout_job(*, name: str, hour: int, population: Callable[[], Iterable[T]], action: Callable[[T], None], minute: int = 0, item_id: Callable[[T], str] = str, logger: logging.Logger = <module logger>) -> Job`

- [ ] **Step 1：失敗測試** Create `tests/test_fanout.py`：
```python
from kinsun.scheduler.fanout import fanout_job


def test_runs_action_for_each_item_and_builds_cron():
    done = []
    job = fanout_job(
        name="t", hour=8, population=lambda: ["a", "b"], action=done.append
    )
    job.run()
    assert done == ["a", "b"]
    assert job.name == "t"
    assert job.cron == "0 8 * * *"


def test_minute_in_cron():
    job = fanout_job(name="t", hour=3, minute=30, population=lambda: [], action=lambda x: None)
    assert job.cron == "30 3 * * *"


def test_one_item_failure_isolated():
    done = []

    def action(item):
        if item == "a":
            raise RuntimeError("boom")
        done.append(item)

    fanout_job(name="t", hour=8, population=lambda: ["a", "b"], action=action).run()
    assert done == ["b"]


def test_action_can_skip_via_early_return():
    done = []

    def action(item):
        if item % 2 == 0:
            return
        done.append(item)

    fanout_job(name="t", hour=8, population=lambda: [1, 2, 3, 4], action=action).run()
    assert done == [1, 3]


def test_item_id_used_for_failure_log(caplog):
    import logging

    def action(_item):
        raise RuntimeError("boom")

    with caplog.at_level(logging.ERROR):
        fanout_job(
            name="med",
            hour=8,
            population=lambda: [("e1", ["藥"])],
            action=action,
            item_id=lambda it: it[0],
        ).run()
    assert "e1" in caplog.text
```

- [ ] **Step 2：確認失敗** Run：`uv run pytest tests/test_fanout.py -q`
  Expected：FAIL（`ModuleNotFoundError: kinsun.scheduler.fanout`）。

- [ ] **Step 3：實作** Create `src/kinsun/scheduler/fanout.py`：
```python
"""扇出執行器：每天某點遍歷一個母體，逐筆隔離失敗。"""

from __future__ import annotations

import logging
from collections.abc import Callable, Iterable
from typing import TypeVar

from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.scheduler")

T = TypeVar("T")


def fanout_job(
    *,
    name: str,
    hour: int,
    population: Callable[[], Iterable[T]],
    action: Callable[[T], None],
    minute: int = 0,
    item_id: Callable[[T], str] = str,
    logger: logging.Logger = logger,
) -> Job:
    """組一個每日 cron job：遍歷 population()，對每筆呼叫 action，逐筆隔離失敗。

    過濾/守門請在 action 內提早 return；母體前處理（分組等）請在 population 內完成。
    """
    cron = f"{minute} {hour} * * *"

    def run() -> None:
        for item in population():
            try:
                action(item)
            except Exception:  # noqa: BLE001 - 單一對象失敗不影響其他
                logger.exception("job %s 處理失敗：%s", name, item_id(item))

    return Job(name=name, cron=cron, run=run)
```

- [ ] **Step 4：確認通過** Run：`uv run pytest tests/test_fanout.py -q` → PASS
- [ ] **Step 5：commit**
```bash
git add src/kinsun/scheduler/fanout.py tests/test_fanout.py
git commit -m "feat: fanout_job 扇出執行器（遍歷+逐筆失敗隔離）"
```

---

## Task 2：build_consolidation_job 改用 fanout_job

**Files:**
- Modify: `src/kinsun/scheduler/jobs.py`

**Interfaces:**
- Consumes：`fanout_job`
- Produces：`build_consolidation_job(*, sessions, run_one, hour, minute=0, name="daily-consolidation") -> Job`（簽名不變）

- [ ] **Step 1：確認既有測試現綠** Run：`uv run pytest tests/test_scheduler_jobs.py -q` → PASS（重構前基準）
- [ ] **Step 2：實作** 改 `src/kinsun/scheduler/jobs.py` 全檔為：
```python
"""排程 job 組裝。"""

from __future__ import annotations

import logging
from collections.abc import Callable

from kinsun.scheduler.fanout import fanout_job
from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.scheduler")


def build_consolidation_job(
    *,
    sessions: Callable[[], list[str]],
    run_one: Callable[[str], object],
    hour: int,
    minute: int = 0,
    name: str = "daily-consolidation",
) -> Job:
    return fanout_job(
        name=name, hour=hour, minute=minute, population=sessions, action=run_one, logger=logger
    )
```
- [ ] **Step 3：確認通過** Run：`uv run pytest tests/test_scheduler_jobs.py -q`、`uv run ruff check src/kinsun/scheduler/jobs.py` → PASS、ruff 通過
- [ ] **Step 4：commit**
```bash
git add src/kinsun/scheduler/jobs.py
git commit -m "refactor: build_consolidation_job 改用 fanout_job"
```

---

## Task 3：主動關懷兩個 job 改用 fanout_job

**Files:**
- Modify: `src/kinsun/proactive/jobs.py`

**Interfaces:**
- Consumes：`fanout_job`
- Produces：`build_greeting_job(*, sessions, greet_one, hour, minute=0, name="daily-greeting")`、`build_inactivity_job(*, sessions, last_active, clock, threshold_seconds, care_one, hour, minute=0, name="inactivity-care")`（簽名不變）

- [ ] **Step 1：確認既有測試現綠** Run：`uv run pytest tests/test_proactive_jobs.py -q` → PASS
- [ ] **Step 2：實作** 改 `src/kinsun/proactive/jobs.py` 全檔為：
```python
"""主動關懷的排程 job。"""

from __future__ import annotations

import logging
from collections.abc import Callable
from datetime import datetime

from kinsun.scheduler.fanout import fanout_job
from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.proactive")

GREETING_INTENT = "早安問候，關心長者今天的狀況"
INACTIVITY_INTENT = "長者已經一段時間沒有互動了，主動表達想念與關心"


def build_greeting_job(
    *,
    sessions: Callable[[], list[str]],
    greet_one: Callable[[str], object],
    hour: int,
    minute: int = 0,
    name: str = "daily-greeting",
) -> Job:
    return fanout_job(
        name=name, hour=hour, minute=minute, population=sessions, action=greet_one, logger=logger
    )


def build_inactivity_job(
    *,
    sessions: Callable[[], list[str]],
    last_active: Callable[[str], float | None],
    clock: Callable[[], datetime],
    threshold_seconds: float,
    care_one: Callable[[str], object],
    hour: int,
    minute: int = 0,
    name: str = "inactivity-care",
) -> Job:
    def action(session_id: str) -> None:
        last = last_active(session_id)
        if last is not None and clock().timestamp() - last >= threshold_seconds:
            care_one(session_id)

    return fanout_job(
        name=name, hour=hour, minute=minute, population=sessions, action=action, logger=logger
    )
```
> 行為註記：原本 `now_ts` 在迴圈前算一次，現改為每筆 `clock()` 一次；對門檻判斷等價，差異可忽略。

- [ ] **Step 3：確認通過** Run：`uv run pytest tests/test_proactive_jobs.py -q`、`uv run ruff check src/kinsun/proactive/jobs.py` → PASS、ruff 通過
- [ ] **Step 4：commit**
```bash
git add src/kinsun/proactive/jobs.py
git commit -m "refactor: 問候/失聯關心 job 改用 fanout_job（過濾移入 action）"
```

---

## Task 4：用藥時段 job 改用 fanout_job

**Files:**
- Modify: `src/kinsun/medication/jobs.py`

**Interfaces:**
- Consumes：`fanout_job`、`SLOT_LABELS`、`Medication`、`MedicationSlot`
- Produces：`build_medication_slot_job(*, slot, meds_at_slot, lookup_elder, is_consented, push, hour, minute=0, name) -> Job`（簽名不變）

- [ ] **Step 1：確認既有測試現綠** Run：`uv run pytest tests/test_medication_jobs.py -q` → PASS
- [ ] **Step 2：實作** 改 `src/kinsun/medication/jobs.py` 全檔為：
```python
"""用藥提醒的時段排程 job。"""

from __future__ import annotations

import logging
from collections.abc import Callable

from kinsun.medication.models import SLOT_LABELS, Medication, MedicationSlot
from kinsun.scheduler.fanout import fanout_job
from kinsun.scheduler.scheduler import Job

logger = logging.getLogger("kinsun.medication")


def build_medication_slot_job(
    *,
    slot: MedicationSlot,
    meds_at_slot: Callable[[], list[Medication]],
    lookup_elder: Callable[[str], object],
    is_consented: Callable[[str], bool],
    push: Callable[[str, str], None],
    hour: int,
    minute: int = 0,
    name: str,
) -> Job:
    label = SLOT_LABELS[slot]

    def population() -> list[tuple[str, list[str]]]:
        by_elder: dict[str, list[str]] = {}
        for med in meds_at_slot():
            by_elder.setdefault(med.elder_id, []).append(med.name)
        return list(by_elder.items())

    def action(item: tuple[str, list[str]]) -> None:
        elder_id, names = item
        elder = lookup_elder(elder_id)
        if elder is None or not elder.line_user_id:
            return
        if not is_consented(elder.line_user_id):
            return
        push(elder.line_user_id, f"{elder.name}，{label}該吃藥囉：{'、'.join(names)}")

    return fanout_job(
        name=name,
        hour=hour,
        minute=minute,
        population=population,
        action=action,
        item_id=lambda item: item[0],
        logger=logger,
    )
```
- [ ] **Step 3：確認通過** Run：`uv run pytest tests/test_medication_jobs.py -q`、`uv run ruff check src/kinsun/medication/jobs.py` → PASS、ruff 通過
- [ ] **Step 4：commit**
```bash
git add src/kinsun/medication/jobs.py
git commit -m "refactor: 用藥時段 job 改用 fanout_job（分組移入 population、守門移入 action）"
```

---

## Task 5：品質收尾

- [ ] **Step 1：全測試 + 品質** Run：`uv run pytest -q`、`uv run ruff check .`、`uv run ruff format --check .` → 全綠
- [ ] **Step 2：確認 `git status` 僅含預期變更**
- [ ] **Step 3：push + PR**（待使用者指示）

---

## Self-Review

**Spec coverage：** 共用扇出本質 → Task 1（`fanout_job`）。四個 job 全收 → Task 2（consolidation）、Task 3（greeting + inactivity，過濾入 action）、Task 4（medication，分組入 population、守門入 action）。失敗隔離一處 → Task 1。各 builder 簽名不變、既有測試保持綠 → Task 2–4 各以既有測試把關。

**Placeholder scan：** 無 TODO；每個 builder 改寫皆含完整檔案內容；測試步驟引用既有測試檔的實際名稱。

**Type consistency：** `fanout_job(*, name, hour, population, action, minute=0, item_id=str, logger=...)` 於 Task 1 定義，Task 2–4 呼叫一致；`population` 回 `Iterable[T]`、`action` 收 `T`、`item_id` 收 `T` 回 `str`；medication 的 `item` 型別 `tuple[str, list[str]]` 在 population／action／item_id 三處一致。各 builder 對外簽名與 `scheduler/worker.py` 現有呼叫一致（未改呼叫端）。
