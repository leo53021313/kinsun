# 開場轉場改「光暈綻放」實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `web/` 開場頁「開始使用」的撕裂展開轉場換成從按鈕綻放的暖黃光暈，並把元件從 `TearTransition` 更名為 `BloomTransition`。

**Architecture:** 沿用現有架構——舞台由 `App.tsx` 在動畫期間就掛在後方，動畫只是一層 `aria-hidden` 的 `fixed` overlay 蓋在上面淡出。差別在 overlay 內容：從「children 複製兩份、各自裁一半滑開」改成「children 單一份淡出放大 ＋ 一顆從按鈕中心放大的光暈」。純 `transform`／`opacity`，走合成器。

**Tech Stack:** React 19、TypeScript、Tailwind CSS v4、Vitest 4（jsdom）、@testing-library/react。

## Global Constraints

- 設計依據：`docs/superpowers/specs/2026-08-13-開場轉場改光暈綻放-design.md`
- **不得新增任何第三方套件**（AGENTS.md）。
- 動畫長度 `BLOOM_DURATION_MS = 700`，`REDUCED_MOTION_MS = 200`，兩者皆不改。
- `prefers-reduced-motion` 為 `true` 時只做 200 毫秒淡出、**完全不掛光暈**（W-11 無障礙要求）。
- overlay 一律掛 `aria-hidden`：裡面是裝飾，真正的內容是後方已掛載的舞台。
- `onDone` 必須存進 ref、**不得**放進 `useEffect` 相依陣列（父層在動畫期間會因舞台載入而重繪）。
- 光暈顏色用主題 token `--color-action`（`#F9DF77`），不寫死色碼。
- 光暈圓心一律由 `getBoundingClientRect()` 算出，**不得**用 `event.clientX`／`clientY`（鍵盤 Enter 觸發時兩者為 `0`）。
- 全程繁體中文註解，用語依 AGENTS.md。
- 驗收指令：`cd web && npm test && npm run typecheck && npm run lint && npm run build`。

## File Structure

| 檔案 | 責任 |
| :-- | :-- |
| `web/src/stage/BloomTransition.tsx`（新增，取代 `TearTransition.tsx`） | 轉場動畫元件。收 `active`／`onDone`／`origin`／`children`，播完通知呼叫端 |
| `web/src/stage/BloomTransition.test.tsx`（新增，取代 `TearTransition.test.tsx`） | 動畫的**契約**測試（動畫本身在 jsdom 驗不了） |
| `web/src/gate/GatePage.tsx`（修改） | 按鈕被按時算出自己的中心點，交給呼叫端 |
| `web/src/App.tsx`（修改） | 保存 `origin`、改用 `BloomTransition`、狀態更名 |
| `web/src/App.test.tsx`（修改） | 接線層的既有斷言改指新的 testid 與常數 |
| `web/src/notify/NotificationBanner.tsx`／`stage/reducedMotion.ts`／`stage/reducedMotion.test.ts`（修改） | 僅註解裡提到 `TearTransition`，更名 |
| `docs/dev/08`／`12`／`17`／`README.md`（修改） | 敘述同步與升版（docs/dev 鐵律） |

---

### Task 1: `BloomTransition` 元件與契約測試

新元件先獨立落地並測過，這一步**還不接線**——`TearTransition` 仍在原地、`App.tsx` 仍用舊的，畫面行為完全不變。這樣壞了也只壞在一個沒人用的新檔案裡。

**Files:**
- Create: `web/src/stage/BloomTransition.tsx`
- Create: `web/src/stage/BloomTransition.test.tsx`

**Interfaces:**
- Consumes: `prefersReducedMotion()` from `web/src/stage/reducedMotion.ts`（既有，無參數，回 `boolean`）
- Produces:
  - `export const BLOOM_DURATION_MS = 700`
  - `export const REDUCED_MOTION_MS = 200`
  - `export type BloomOrigin = { x: number; y: number }`
  - `export function BloomTransition(props: { active: boolean; onDone: () => void; origin?: BloomOrigin; children: ReactNode }): ReactNode`
  - DOM 契約：啟動且未關動態效果時，overlay 內有 `data-testid="bloom-page"`（children 單一份）與 `data-testid="bloom-glow"`（光暈）

- [ ] **Step 1: 寫失敗的測試**

建立 `web/src/stage/BloomTransition.test.tsx`：

```tsx
/** 光暈綻放動畫。
 *
 * 動畫本身沒辦法用 jsdom 驗（沒有版面與合成器），所以測的是**它的契約**：
 * 該不該播、播完有沒有通知、以及「使用者關了動態效果時要走短路」。
 * 最後一條是無障礙要求（W-11），漏掉的話會讓對動態敏感的人不舒服。
 */

import { render, screen } from "@testing-library/react";
import { afterEach, describe, expect, it, vi } from "vitest";

import { BLOOM_DURATION_MS, BloomTransition, REDUCED_MOTION_MS } from "./BloomTransition";

function mockReducedMotion(reduced: boolean) {
  vi.stubGlobal(
    "matchMedia",
    vi.fn().mockImplementation((query: string) => ({
      matches: reduced && query.includes("prefers-reduced-motion"),
      media: query,
      addEventListener: vi.fn(),
      removeEventListener: vi.fn(),
    })),
  );
}

afterEach(() => {
  vi.useRealTimers();
  vi.unstubAllGlobals();
});

describe("BloomTransition", () => {
  it("未啟動時原樣顯示內容，不掛 overlay", () => {
    mockReducedMotion(false);
    render(
      <BloomTransition active={false} onDone={vi.fn()}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(screen.getByText("開場")).toBeInTheDocument();
    expect(screen.queryByTestId("bloom-glow")).not.toBeInTheDocument();
  });

  it("啟動時掛上開場頁副本與光暈", () => {
    mockReducedMotion(false);
    render(
      <BloomTransition active onDone={vi.fn()}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(screen.getByTestId("bloom-page")).toBeInTheDocument();
    expect(screen.getByTestId("bloom-glow")).toBeInTheDocument();
  });

  it("動畫播完後通知呼叫端", () => {
    vi.useFakeTimers();
    mockReducedMotion(false);
    const onDone = vi.fn();
    render(
      <BloomTransition active onDone={onDone}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(onDone).not.toHaveBeenCalled();
    vi.advanceTimersByTime(BLOOM_DURATION_MS);
    expect(onDone).toHaveBeenCalledOnce();
  });

  it("使用者關了動態效果時改走短的淡出", () => {
    vi.useFakeTimers();
    mockReducedMotion(true);
    const onDone = vi.fn();
    render(
      <BloomTransition active onDone={onDone}>
        <p>開場</p>
      </BloomTransition>,
    );
    vi.advanceTimersByTime(REDUCED_MOTION_MS);
    expect(onDone).toHaveBeenCalledOnce();
  });

  it("關了動態效果時不掛光暈，只淡出", () => {
    mockReducedMotion(true);
    render(
      <BloomTransition active onDone={vi.fn()}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(screen.queryByTestId("bloom-glow")).not.toBeInTheDocument();
    expect(screen.getByText("開場")).toBeInTheDocument();
  });

  it("父層在動畫期間重繪也不會重排計時器", () => {
    // ⚠️ 這正是最可能發生的情境：舞台在動畫期間掛載並發請求，資料回來就是一次重繪。
    // onDone 若留在相依陣列，計時器會被反覆重排，動畫播完卻沒有人被通知。
    vi.useFakeTimers();
    mockReducedMotion(false);
    const first = vi.fn();
    const second = vi.fn();
    const { rerender } = render(
      <BloomTransition active onDone={first}>
        <p>開場</p>
      </BloomTransition>,
    );
    vi.advanceTimersByTime(BLOOM_DURATION_MS - 100);
    rerender(
      <BloomTransition active onDone={second}>
        <p>開場</p>
      </BloomTransition>,
    );
    vi.advanceTimersByTime(100);
    expect(second).toHaveBeenCalledOnce();
    expect(first).not.toHaveBeenCalled();
  });

  it("overlay 對讀螢幕的人隱藏", () => {
    // 開場頁的內容在 DOM 裡出現兩次（後方舞台旁邊多一份動畫用的副本），那份是
    // 裝飾。不隱藏的話會被唸兩遍。
    mockReducedMotion(false);
    const { container } = render(
      <BloomTransition active onDone={vi.fn()}>
        <p>開場</p>
      </BloomTransition>,
    );
    const overlay = container.querySelector("[aria-hidden]");
    expect(overlay).not.toBeNull();
    expect(overlay?.querySelector("[data-testid='bloom-glow']")).not.toBeNull();
  });

  it("給了 origin 時光暈以那個點為圓心", () => {
    mockReducedMotion(false);
    render(
      <BloomTransition active onDone={vi.fn()} origin={{ x: 120, y: 340 }}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(screen.getByTestId("bloom-glow")).toHaveStyle({ left: "120px", top: "340px" });
  });

  it("沒給 origin 時光暈退回畫面正中央", () => {
    // ⚠️ 呼叫端不一定給得出座標（例如未來從別處觸發轉場），元件不該假設它一定在。
    mockReducedMotion(false);
    render(
      <BloomTransition active onDone={vi.fn()}>
        <p>開場</p>
      </BloomTransition>,
    );
    expect(screen.getByTestId("bloom-glow")).toHaveStyle({ left: "50%", top: "50%" });
  });
});
```

- [ ] **Step 2: 執行測試，確認它失敗**

Run: `cd web && npx vitest run src/stage/BloomTransition.test.tsx`
Expected: FAIL — `Failed to resolve import "./BloomTransition"`

- [ ] **Step 3: 寫實作**

建立 `web/src/stage/BloomTransition.tsx`：

```tsx
/**
 * 開場頁退進一圈光裡：從使用者按下的按鈕中心綻放暖黃光暈，開場頁同時淡出並
 * 微幅放大，光散去就是後方已經掛好的雙欄舞台（spec 2026-08-13）。
 *
 * ⚠️ 這裡原本是「撕裂展開」（沿鋸齒裂痕切兩半滑開）。換掉的理由是調性不是技術：
 * 「撕開」帶破壞感，與陪伴長輩的產品衝突。動作的隱喻改成「你伸手一按，畫面為
 * 你亮起來」——起點是使用者的手指，而不是一道裂痕。
 *
 * ⚠️ 舞台由呼叫端在動畫**期間**就掛載（見 App.tsx）：等動畫播完才開始請求，
 * 使用者會平白多等 700 毫秒，而那正是他最沒有耐心的時刻。
 *
 * ⚠️ 尊重 prefers-reduced-motion（W-11）：純視覺享受不該讓對動態敏感的人不舒服。
 * 判斷式抽到 `stage/reducedMotion.ts`，與 `notify/NotificationBanner.tsx` 共用同一份。
 */

import { useEffect, useRef, useState, type ReactNode } from "react";

import { prefersReducedMotion } from "./reducedMotion";

export const BLOOM_DURATION_MS = 700;
export const REDUCED_MOTION_MS = 200;

/** 光暈圓心，視窗座標（overlay 是 fixed，兩者同一個座標系）。 */
export type BloomOrigin = { x: number; y: number };

/**
 * 光暈的緩速：起手快、尾巴慢慢散開。
 * 這是「綻放」與「爆開」的分野——等速或 ease-in 都會變成後者。
 */
const BLOOM_EASING = "cubic-bezier(0.22, 1, 0.36, 1)";

/**
 * 光暈底色。用主題 token 而非寫死色碼，品牌色改了它會跟著改。
 * 邊緣柔化到透明，讀起來才是「光」而不是「一塊黃色圓形」。
 */
const BLOOM_GRADIENT = [
  "radial-gradient(circle,",
  "var(--color-action) 0%,",
  "color-mix(in srgb, var(--color-action) 55%, transparent) 45%,",
  "transparent 70%)",
].join(" ");

export function BloomTransition(props: {
  active: boolean;
  onDone: () => void;
  origin?: BloomOrigin;
  children: ReactNode;
}) {
  const { active, onDone, origin, children } = props;
  const [reduced] = useState(prefersReducedMotion);
  // 掛上之後才加位移，否則會直接出現在終點、看不到過程。
  const [moved, setMoved] = useState(false);

  // ⚠️ onDone 存進 ref、不進相依陣列：舞台在動畫**期間**就掛載並發請求（見上方
  // 說明），那正是父層最容易重新渲染的時刻。onDone 若留在相依陣列，父層一重繪就
  // 會清掉計時器、重排一顆新的——「播完通知呼叫端」這個契約會被延後，極端情況下
  // 永遠不觸發。用獨立的 effect 同步而非 render 期間直接賦值：後者會被
  // react-hooks 的 refs 規則抓到。
  const onDoneRef = useRef(onDone);
  useEffect(() => {
    onDoneRef.current = onDone;
  }, [onDone]);

  useEffect(() => {
    if (!active) {
      return;
    }
    const raf = requestAnimationFrame(() => setMoved(true));
    const timer = setTimeout(
      () => onDoneRef.current(),
      reduced ? REDUCED_MOTION_MS : BLOOM_DURATION_MS,
    );
    return () => {
      cancelAnimationFrame(raf);
      clearTimeout(timer);
    };
  }, [active, reduced]);

  if (!active) {
    return <>{children}</>;
  }

  if (reduced) {
    return (
      <div
        aria-hidden
        className="transition-opacity ease-out"
        style={{ opacity: moved ? 0 : 1, transitionDuration: `${REDUCED_MOTION_MS}ms` }}
      >
        {children}
      </div>
    );
  }

  return (
    <div aria-hidden className="pointer-events-none fixed inset-0 z-50 overflow-hidden">
      {/* 開場頁副本：淡出並微幅**放大**。縮小讀起來是「被推走」，放大才是
          「退進光裡」；幅度只有 4%，再大就變成 zoom 特效、蓋過光暈本身。 */}
      <div
        data-testid="bloom-page"
        className="absolute inset-0 transition-[opacity,transform] ease-out"
        style={{
          opacity: moved ? 0 : 1,
          transform: moved ? "scale(1.04)" : "scale(1)",
          transitionDuration: `${BLOOM_DURATION_MS}ms`,
        }}
      >
        {children}
      </div>
      {/* 光暈：40vmax 見方的圓，用 vmax 而非固定 px，直式與橫式螢幕都蓋得滿。
          放大到 4 倍必定覆蓋整個視窗。 */}
      <div
        data-testid="bloom-glow"
        className="absolute size-[40vmax] rounded-full transition-[opacity,transform]"
        style={{
          left: origin ? `${origin.x}px` : "50%",
          top: origin ? `${origin.y}px` : "50%",
          background: BLOOM_GRADIENT,
          opacity: moved ? 0 : 0.9,
          transform: `translate(-50%, -50%) scale(${moved ? 4 : 0.2})`,
          transitionDuration: `${BLOOM_DURATION_MS}ms`,
          transitionTimingFunction: BLOOM_EASING,
        }}
      />
    </div>
  );
}
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `cd web && npx vitest run src/stage/BloomTransition.test.tsx`
Expected: PASS（9 passed）

- [ ] **Step 5: 型別與 lint**

Run: `cd web && npm run typecheck && npm run lint`
Expected: 兩者皆無輸出（`TearTransition.tsx` 此時仍在、仍被 `App.tsx` 使用，不會有未使用匯出的錯誤）

- [ ] **Step 6: Commit**

```bash
git add web/src/stage/BloomTransition.tsx web/src/stage/BloomTransition.test.tsx
git commit -m "feat(web): 新增光暈綻放轉場元件"
```

---

### Task 2: 接線並移除 `TearTransition`

**Files:**
- Modify: `web/src/gate/GatePage.tsx`
- Modify: `web/src/App.tsx`
- Modify: `web/src/App.test.tsx`
- Modify: `web/src/notify/NotificationBanner.tsx`（僅註解）
- Modify: `web/src/stage/reducedMotion.ts`（僅註解）
- Modify: `web/src/stage/reducedMotion.test.ts`（僅註解）
- Delete: `web/src/stage/TearTransition.tsx`
- Delete: `web/src/stage/TearTransition.test.tsx`

**Interfaces:**
- Consumes: Task 1 的 `BloomTransition`、`BLOOM_DURATION_MS`、`BloomOrigin`
- Produces: `GatePage` 的 props 由 `{ state: GateState; onStart: () => void }` 改為 `{ state: GateState; onStart: (origin: BloomOrigin) => void }`

- [ ] **Step 1: 改 `GatePage` 交出按鈕中心點**

`web/src/gate/GatePage.tsx` 全檔改為：

```tsx
/** 開場：先看服務狀態，可用才讓人進去（spec §5.1）。
 *
 * ⚠️ **純展示元件**：狀態由 props 收，不自己呼叫 `useDemoStatus`。轉場動畫啟動時
 * 本元件會被卸載、在 overlay 底下重掛一份（見 BloomTransition），狀態若住在這裡，
 * 動畫開始的瞬間就會歸零成「正在確認服務狀態…」。狀態的擁有者是 App.tsx 的 Demo。
 */

import { strings } from "@/strings";
import type { BloomOrigin } from "@/stage/BloomTransition";

import { StatusCard } from "./StatusCard";
import { canEnter, type GateState } from "./useDemoStatus";

/**
 * 光暈的圓心＝按鈕中心。
 *
 * ⚠️ **不可用 `event.clientX`／`clientY`**：鍵盤按 Enter／Space 觸發 click 時兩者
 * 都是 `0`，光暈會從畫面左上角冒出來。`getBoundingClientRect()` 對滑鼠、觸控、
 * 鍵盤三種操作都給同一個正確答案。
 */
function centerOf(element: HTMLElement): BloomOrigin {
  const rect = element.getBoundingClientRect();
  return { x: rect.left + rect.width / 2, y: rect.top + rect.height / 2 };
}

export function GatePage(props: { state: GateState; onStart: (origin: BloomOrigin) => void }) {
  const enterable = canEnter(props.state);

  return (
    <main className="flex min-h-dvh flex-col items-center justify-center gap-8 bg-background p-8">
      <div className="text-center">
        <h1 className="text-elder-huge font-extrabold text-primary">{strings.gate.brand}</h1>
        <p className="mt-2 text-base text-ink-soft">{strings.gate.slogan}</p>
      </div>

      <StatusCard state={props.state} />

      <button
        type="button"
        disabled={!enterable}
        onClick={(event) => props.onStart(centerOf(event.currentTarget))}
        className="min-h-14 rounded-2xl bg-primary px-10 text-lg font-bold text-white transition-colors enabled:hover:bg-primary-pressed disabled:cursor-not-allowed disabled:opacity-40"
      >
        {strings.gate.start}
      </button>
    </main>
  );
}
```

⚠️ `import type`（非一般 import）：型別限定匯入在編譯期抹除，`gate/` 不會因此在執行期依賴 `stage/`。同樣手法已用於 `strings.ts` 匯入 `PhoneOs`。

- [ ] **Step 2: 改 `App.tsx` 接上新元件**

在 `web/src/App.tsx` 做四處修改：

其一，檔頭第三段 ⚠️ 的 `TearTransition` 改成 `BloomTransition`，並把「重掛兩份」改成「重掛一份」：

```
 * ⚠️ **運營狀態的 hook 住在這裡而不是 GatePage 裡面**：BloomTransition 在 active
 * 翻轉時會把 children 從原位換到 overlay 底下的副本，那會卸載活著的 GatePage、
 * 掛上一個全新實例。狀態留在 GatePage 裡的話，動畫開始的那一瞬間畫面會閃回
 * 「正在確認服務狀態…」、按鈕變灰，並且對後端多打一次——而那正是整個展示的
 * 門面動畫，看的人會以為它壞了。
```

其二，匯入改為：

```tsx
import { BloomTransition, type BloomOrigin } from "./stage/BloomTransition";
```

其三，狀態宣告（原 `const [tearing, setTearing] = useState(false);`）改為：

```tsx
  const [blooming, setBlooming] = useState(false);
  // 光暈的圓心由 GatePage 的按鈕交出來（見該檔 centerOf）。沒有值時
  // BloomTransition 會退回畫面正中央，不會壞。
  const [origin, setOrigin] = useState<BloomOrigin | undefined>(undefined);
```

其四，`tearing` 的三處使用一併更名，並把 `origin` 與新的 `onStart` 接上。註解裡的「撕裂」改成「轉場」：

```tsx
  if (blooming && onStage) {
    setBlooming(false);
  }

  return (
    <>
      {blooming || onStage ? <StagePage /> : null}
      {onStage ? null : (
        <BloomTransition active={blooming} onDone={enterStage} origin={origin}>
          <GatePage
            state={gate}
            onStart={(pressed) => {
              setOrigin(pressed);
              setBlooming(true);
            }}
          />
        </BloomTransition>
      )}
```

- [ ] **Step 3: 更新 `App.test.tsx` 的接線斷言**

`web/src/App.test.tsx` 五處替換：

1. 匯入：`import { TEAR_DURATION_MS } from "./stage/TearTransition";` → `import { BLOOM_DURATION_MS } from "./stage/BloomTransition";`
2. 全檔 `TEAR_DURATION_MS` → `BLOOM_DURATION_MS`（連同上一項的匯入共 4 處命中；使用點 3 處，在第 148、161、172 行附近）
3. 兩處 `expect(screen.queryByTestId("tear-left")).not.toBeInTheDocument();` → `expect(screen.queryByTestId("bloom-glow")).not.toBeInTheDocument();`
4. 測試名稱「撕裂當下畫面仍是已載入的狀態，不會閃回確認中、按鈕也不會變灰」→「轉場當下畫面仍是已載入的狀態，不會閃回確認中、按鈕也不會變灰」；該測試內的註解「成因是 TearTransition 在 active 翻轉時把 children 換到 overlay 底下的兩份副本——React 會卸載活著的 GatePage、掛兩個全新實例」→「成因是 BloomTransition 在 active 翻轉時把 children 換到 overlay 底下的副本——React 會卸載活著的 GatePage、掛一個全新實例」
5. 該測試取內容的三行改為指向開場頁副本：

```tsx
    // overlay 裡那份開場頁副本就是使用者當下看到的畫面。
    // ⚠️ 用 getByText 而非 getByRole：overlay 掛了 aria-hidden（副本是裝飾，
    // 不該被讀螢幕唸兩遍），role 查詢走的是無障礙樹，查不到裡面的東西。
    const overlay = within(screen.getByTestId("bloom-page"));
    expect(overlay.queryByText(strings.gate.checking)).not.toBeInTheDocument();
    expect(overlay.getByText(strings.gate.overall.available)).toBeInTheDocument();
    expect(overlay.getByText(strings.gate.start)).toBeEnabled();
```

另把第 153 行測試內註解的「撕裂旗標」改為「轉場旗標」。

- [ ] **Step 4: 更新三個檔案註解裡的元件名**

- `web/src/stage/reducedMotion.ts`：`` `stage/TearTransition.tsx` `` → `` `stage/BloomTransition.tsx` ``
- `web/src/stage/reducedMotion.test.ts`：同上
- `web/src/notify/NotificationBanner.tsx`：兩處 `stage/TearTransition.tsx` → `stage/BloomTransition.tsx`

- [ ] **Step 5: 刪除舊檔**

```bash
git rm web/src/stage/TearTransition.tsx web/src/stage/TearTransition.test.tsx
```

- [ ] **Step 6: 全套驗收**

Run: `cd web && npm test && npm run typecheck && npm run lint && npm run build`
Expected: 測試全綠（總數比改動前少 7、多 9，淨 +2）、typecheck 與 lint 無輸出、build 成功。

⚠️ 若 `npm run lint` 抱怨 `import type` 的排序，依 eslint 的 `import/order` 自動修正提示調整匯入順序，不要改成一般 import。

- [ ] **Step 7: Commit**

```bash
git add -A web/src
git commit -m "refactor(web): 開場轉場改用光暈綻放，移除撕裂展開"
```

---

### Task 3: `docs/dev` 同步與升版

依 AGENTS.md 的 docs/dev 鐵律：程式改動完成的**同一時間**更新受影響文件，不可事後補。

**Files:**
- Modify: `docs/dev/08_專案結構指南.md`
- Modify: `docs/dev/12_前端架構規範.md`
- Modify: `docs/dev/17_前端資訊架構.md`
- Modify: `docs/dev/README.md`

- [ ] **Step 1: 改內文四處**

1. `docs/dev/08_專案結構指南.md` §6 目錄樹那一行：`撕裂展開動畫＋雙欄舞台：TearTransition／PhoneFrame／StagePage` → `光暈綻放轉場＋雙欄舞台：BloomTransition／PhoneFrame／StagePage`
2. `docs/dev/12_前端架構規範.md`：`prefersReducedMotion` 段落中的 `TearTransition.tsx` → `BloomTransition.tsx`
3. `docs/dev/17_前端資訊架構.md` §7 路由表：`直接開啟不播撕裂動畫、按重整不 404` → `直接開啟不播轉場動畫、按重整不 404`
4. `docs/dev/17_前端資訊架構.md` §7 下方 ⚠️ 附註：`含撕裂動畫期間先掛好的舞台` → `含轉場動畫期間先掛好的舞台`

- [ ] **Step 2: 確認沒有漏網**

Run: `grep -rn "TearTransition\|撕裂" docs/dev/ web/src/`
Expected: 只剩各文件底部「修訂紀錄」表格裡的歷史條目（那是當時的事實，不改）。`web/src/` 應完全沒有命中。

- [ ] **Step 3: 升版頭與 README 狀態表**

三份文件的版頭 `**版本:**` 各加一個次版號、`**更新:**` 改 `2026-08-13`、`**狀態:**` 括號內改為「開場轉場改光暈綻放」；`docs/dev/README.md` 文件狀態表對應三列同步。

- [ ] **Step 4: Commit**

```bash
git add docs/dev
git commit -m "docs(dev): 同步開場轉場改為光暈綻放"
```

---

## 人工驗收（自動化測不到的部分）

jsdom 沒有版面與合成器，動畫**看起來對不對**只能用眼睛驗。實作完成後在瀏覽器做這四項：

1. `cd web && npm run dev`，開 `/demo`，按「開始使用」——光暈應從按鈕位置向外綻放、開場頁退進光裡。
2. 用鍵盤 Tab 到按鈕、按 Enter——光暈仍應從按鈕位置綻放（**不是**畫面左上角）。這條驗的是 `getBoundingClientRect()` 那個決定。
3. 作業系統開啟「減少動態效果」後重整再按——應只有短淡出、看不到光暈。
4. 在實際要展示的那台筆電上看一次，確認不卡頓，並確認暖黃光暈配靛藍按鈕不顯髒。若顯髒，改用 `--color-background-warm-top`（`#FFF9EC`）。
