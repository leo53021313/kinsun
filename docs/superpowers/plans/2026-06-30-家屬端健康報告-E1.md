# 家屬端健康報告（E1）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓家屬在 LIFF 檢視某長輩近 30 天的健康報告（危急事件 D1 + 提醒紀錄 D2）。

**Architecture:** `create_api_router` 加 `risk_events`/`reminder_logs` 兩個 store 參數 + `GET /health-report`（近 30 天篩選、授權沿用 `assert_manages`）；前端新增 `HealthReportPage`。

**Tech Stack:** FastAPI / pytest / ruff（後端）；React + Vite + TS（前端）。

## Global Constraints

- 一律台灣繁體中文：文件、註解、commit 訊息。
- Python 3.12；ruff `E,W,F,I,B,UP`，line-length 100；不新增第三方套件。
- 授權/存在性失敗 404（`assert_manages`）；無/失效 token 401。
- 近 30 天以 `created_at >= (clock() - 30d).timestamp()` 篩選；兩清單 store 已 desc。
- `tier` 回 int（前端映射中文）；`created_at` 回 epoch float（前端格式化）。
- commit 型別 `feat/fix/docs/refactor/test/chore`，結尾加
  `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 後端測試全離線（TestClient + fake/stub）；前端以 typecheck + build 把關。

---

## 檔案結構（File Structure）

**修改（後端）：** `src/kinsun/web/api.py`（+2 參數 + 端點）、`src/kinsun/app.py`（接線）、`tests/test_api_elders.py`、`tests/test_api_medications.py`、`tests/test_api_appointments.py`、`tests/test_api_accounts.py`（補 store 參數）。
**新增（後端）：** `tests/test_api_health_report.py`。
**新增/修改（前端）：** `frontend/src/api.ts`、`frontend/src/report.ts`、`frontend/src/pages/HealthReportPage.tsx`、`frontend/src/pages/EldersPage.tsx`、`frontend/src/App.tsx`。

---

## Task 1：健康報告 REST 端點（web/api.py）

**Files:**
- Modify: `src/kinsun/web/api.py`、`tests/test_api_elders.py`、`tests/test_api_medications.py`、`tests/test_api_appointments.py`、`tests/test_api_accounts.py`
- Test: `tests/test_api_health_report.py`

**Interfaces:**
- Consumes: `RiskEventStore.list_for_session`、`ReminderLogStore.list_for_elder`、`accounts.get_elder`、`current_guardian`/`assert_manages`（既有）
- Produces: `create_api_router(*, verifier, accounts, medications, appointments, clock, risk_events: RiskEventStore, reminder_logs: ReminderLogStore)`；`GET /api/elders/{elder_id}/health-report`

- [ ] **Step 1：寫失敗測試** `tests/test_api_health_report.py`

```python
from datetime import datetime, timedelta, timezone
from itertools import count

from fastapi import FastAPI
from fastapi.testclient import TestClient

from kinsun.accounts.service import AccountService
from kinsun.appointment.service import AppointmentService
from kinsun.medication.service import MedicationService
from kinsun.reports.reminders import ReminderLog
from kinsun.safety.events import RiskEvent
from kinsun.safety.tiers import RiskTier
from kinsun.web.api import create_api_router
from kinsun.web.auth import AuthError
from tests.fakes import FakeAccountRepository, FakeAppointmentStore, FakeMedicationStore

TPE = timezone(timedelta(hours=8))
NOW = datetime(2026, 7, 30, 12, 0, tzinfo=TPE)
RECENT = (NOW - timedelta(days=5)).timestamp()
OLD = (NOW - timedelta(days=40)).timestamp()


class _FakeVerifier:
    def __init__(self, user_id="U-son"):
        self._user_id = user_id

    def verify(self, id_token):
        return self._user_id


class _RiskEvents:
    def __init__(self, events):
        self._events = events

    def list_for_session(self, session_id):
        return self._events


class _Reminders:
    def __init__(self, logs):
        self._logs = logs

    def list_for_elder(self, elder_id):
        return self._logs


def _client(user_id, *, risks, reminders, bind_elder=True):
    repo = FakeAccountRepository()
    ids = (f"id{i}" for i in count(1))
    accounts = AccountService(
        repo, clock=lambda: NOW, new_id=lambda: next(ids), new_code=lambda: "c"
    )
    elder = accounts.create_elder("U-son", "兒子", "阿公")
    if bind_elder:
        from kinsun.accounts.models import Elder

        repo.save_elder(Elder(elder.elder_id, "阿公", "U-elder"))
    app = FastAPI()
    app.include_router(
        create_api_router(
            verifier=_FakeVerifier(user_id),
            accounts=accounts,
            medications=MedicationService(FakeMedicationStore()),
            appointments=AppointmentService(FakeAppointmentStore()),
            clock=lambda: NOW,
            risk_events=_RiskEvents(risks),
            reminder_logs=_Reminders(reminders),
        )
    )
    return TestClient(app), elder.elder_id


def _auth():
    return {"Authorization": "Bearer tok"}


def test_health_report_recent_only():
    risks = [
        RiskEvent("r1", "U-elder", RiskTier.L3, "昏倒", RECENT),
        RiskEvent("r0", "U-elder", RiskTier.L2, "舊事件", OLD),
    ]
    reminders = [
        ReminderLog("m1", "e", "medication", "早上用藥：A", RECENT),
        ReminderLog("m0", "e", "appointment", "舊提醒", OLD),
    ]
    client, elder_id = _client("U-son", risks=risks, reminders=reminders)
    res = client.get(f"/api/elders/{elder_id}/health-report", headers=_auth())
    assert res.status_code == 200
    body = res.json()
    assert [e["reason"] for e in body["risk_events"]] == ["昏倒"]
    assert body["risk_events"][0]["tier"] == 3
    assert [r["content"] for r in body["reminders"]] == ["早上用藥：A"]


def test_health_report_rejects_unmanaged():
    client, elder_id = _client("U-stranger", risks=[], reminders=[])
    assert client.get(f"/api/elders/{elder_id}/health-report", headers=_auth()).status_code == 404


def test_health_report_requires_token():
    client, elder_id = _client("U-son", risks=[], reminders=[])
    assert client.get(f"/api/elders/{elder_id}/health-report").status_code == 401


def test_health_report_unbound_elder_has_no_risks():
    reminders = [ReminderLog("m1", "e", "medication", "早上用藥：A", RECENT)]
    client, elder_id = _client("U-son", risks=[], reminders=reminders, bind_elder=False)
    res = client.get(f"/api/elders/{elder_id}/health-report", headers=_auth())
    assert res.status_code == 200
    assert res.json()["risk_events"] == []
    assert [r["content"] for r in res.json()["reminders"]] == ["早上用藥：A"]
```

- [ ] **Step 2：跑測試確認失敗**

Run: `uv run pytest tests/test_api_health_report.py -v`
Expected: FAIL（`create_api_router()` 不接受 `risk_events`/`reminder_logs`）

- [ ] **Step 3：既有 4 個 API 測試補 store 參數**

在 `tests/test_api_elders.py`、`tests/test_api_medications.py`、`tests/test_api_appointments.py`、`tests/test_api_accounts.py` 各檔：
1. 把 `from tests.fakes import (...)` 補上 `FakeReminderLogStore, FakeRiskEventStore`（維持字母序）。
2. 該檔的 `create_api_router(...)` 呼叫在 `clock=lambda: NOW,` 之後補：
   ```python
            risk_events=FakeRiskEventStore(),
            reminder_logs=FakeReminderLogStore(),
   ```

- [ ] **Step 4：實作** `src/kinsun/web/api.py`

(1) import 區：把 `from datetime import datetime` 改為 `from datetime import datetime, timedelta`；並在 `from kinsun.medication.service import MedicationService` 之後加：
```python
from kinsun.reports.reminders import ReminderLogStore
from kinsun.safety.events import RiskEventStore
```

(2) `create_api_router` 簽章在 `clock: Callable[[], datetime],` 之後加：
```python
    risk_events: RiskEventStore,
    reminder_logs: ReminderLogStore,
```

(3) 在 `return router` 之前新增端點：
```python
    @router.get("/elders/{elder_id}/health-report")
    def health_report(elder_id: str, line_user_id: str = Depends(current_guardian)) -> dict:
        assert_manages(line_user_id, elder_id)
        cutoff = (clock() - timedelta(days=30)).timestamp()
        elder = accounts.get_elder(elder_id)
        session_id = elder.line_user_id if elder else None
        risks = (
            [e for e in risk_events.list_for_session(session_id) if e.created_at >= cutoff]
            if session_id
            else []
        )
        reminders = [r for r in reminder_logs.list_for_elder(elder_id) if r.created_at >= cutoff]
        return {
            "risk_events": [
                {"tier": int(e.tier), "reason": e.reason, "created_at": e.created_at}
                for e in risks
            ],
            "reminders": [
                {"kind": r.kind, "content": r.content, "created_at": r.created_at}
                for r in reminders
            ],
        }
```

- [ ] **Step 5：跑測試確認通過**

Run: `uv run pytest tests/test_api_health_report.py tests/test_api_elders.py tests/test_api_medications.py tests/test_api_appointments.py tests/test_api_accounts.py -q`
Expected: PASS（health 4 + 既有 4 檔全綠）

- [ ] **Step 6：commit**

```bash
git add src/kinsun/web/api.py tests/test_api_health_report.py tests/test_api_elders.py \
        tests/test_api_medications.py tests/test_api_appointments.py tests/test_api_accounts.py
git commit -m "feat: 家屬端健康報告 REST GET /health-report（近 30 天危急+提醒）

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 2：app.py 接線

**Files:**
- Modify: `src/kinsun/app.py`

**Interfaces:**
- Consumes: `create_api_router`（Task 1）、`PgRiskEventStore`（既有 import）、`PgReminderLogStore`（新 import）

- [ ] **Step 1：實作接線**

(1) import 區加：
```python
from kinsun.reports.reminders import PgReminderLogStore
```

(2) 把 pipeline 內聯的 `risk_events=PgRiskEventStore(...)` 提為變數：在 `pipeline = VoicePipeline(` 之前加：
```python
    risk_events = PgRiskEventStore(
        db, clock=lambda: datetime.now(tz), new_id=lambda: uuid.uuid4().hex
    )
    reminder_logs = PgReminderLogStore(
        db, clock=lambda: datetime.now(tz), new_id=lambda: uuid.uuid4().hex
    )
```
並把 `VoicePipeline(...)` 內的
```python
        risk_events=PgRiskEventStore(
            db, clock=lambda: datetime.now(tz), new_id=lambda: uuid.uuid4().hex
        ),
```
改為 `risk_events=risk_events,`。

(3) `create_api_router(...)` 呼叫在 `medications=medications,`/`appointments=appointments,`/`clock=...,` 之後補：
```python
            risk_events=risk_events,
            reminder_logs=reminder_logs,
```

- [ ] **Step 2：匯入驗證 + 全量測試**

Run:
```bash
PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"
uv run pytest -q
```
Expected: `import ok`；pytest 全綠。

- [ ] **Step 3：commit**

```bash
git add src/kinsun/app.py
git commit -m "feat: 組裝層把 risk_events/reminder_logs 傳入家屬端 API

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 3：前端健康報告頁

**Files:**
- Modify: `frontend/src/api.ts`、`frontend/src/pages/EldersPage.tsx`、`frontend/src/App.tsx`
- Create: `frontend/src/report.ts`、`frontend/src/pages/HealthReportPage.tsx`

**Interfaces:**
- Consumes: `GET /api/elders/{id}/health-report`（Task 1）
- 交付物：`npm --prefix frontend run typecheck` 與 `build` 皆成功

- [ ] **Step 1：`api.ts` 加健康報告**（接在既有函式之後）

```typescript
export type RiskEventItem = { tier: number; reason: string; created_at: number };
export type ReminderItem = { kind: string; content: string; created_at: number };

export async function getHealthReport(
  elderId: string,
): Promise<{ risk_events: RiskEventItem[]; reminders: ReminderItem[] }> {
  const res = await apiFetch(`/api/elders/${elderId}/health-report`);
  return (await res.json()) as { risk_events: RiskEventItem[]; reminders: ReminderItem[] };
}
```

- [ ] **Step 2：建立 `frontend/src/report.ts`**

```typescript
const TIER_LABELS: Record<number, string> = { 2: "警示", 3: "緊急" };
const KIND_LABELS: Record<string, string> = { medication: "用藥", appointment: "回診" };

export function tierLabel(tier: number): string {
  return TIER_LABELS[tier] ?? `L${tier}`;
}

export function kindLabel(kind: string): string {
  return KIND_LABELS[kind] ?? kind;
}

export function formatTime(epoch: number): string {
  return new Date(epoch * 1000).toLocaleString("zh-TW");
}
```

- [ ] **Step 3：建立 `frontend/src/pages/HealthReportPage.tsx`**

```tsx
import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";

import { type ReminderItem, type RiskEventItem, getHealthReport } from "../api";
import { formatTime, kindLabel, tierLabel } from "../report";

type Report = { risk_events: RiskEventItem[]; reminders: ReminderItem[] };

export function HealthReportPage() {
  const { elderId = "" } = useParams();
  const [report, setReport] = useState<Report | null>(null);
  const [error, setError] = useState<string | null>(null);

  useEffect(() => {
    getHealthReport(elderId)
      .then(setReport)
      .catch(() => setError("載入失敗，請稍後再試"));
  }, [elderId]);

  if (error) return <p>{error}</p>;
  if (!report) return <p>載入中…</p>;
  return (
    <main>
      <p>
        <Link to="/">← 返回長輩清單</Link>
      </p>
      <h1>健康報告（近 30 天）</h1>
      <h2>危急事件</h2>
      {report.risk_events.length === 0 ? (
        <p>近 30 天無危急事件</p>
      ) : (
        <ul>
          {report.risk_events.map((e, i) => (
            <li key={i}>
              {formatTime(e.created_at)} · {tierLabel(e.tier)} · {e.reason}
            </li>
          ))}
        </ul>
      )}
      <h2>提醒紀錄</h2>
      {report.reminders.length === 0 ? (
        <p>近 30 天無提醒紀錄</p>
      ) : (
        <ul>
          {report.reminders.map((r, i) => (
            <li key={i}>
              {formatTime(r.created_at)} · {kindLabel(r.kind)} · {r.content}
            </li>
          ))}
        </ul>
      )}
    </main>
  );
}
```

- [ ] **Step 4：`EldersPage` 加健康報告連結**

把清單中每個 `<li>` 換成：
```tsx
        {elders.map((e) => (
          <li key={e.elder_id}>
            {e.name}：<Link to={`/elders/${e.elder_id}/medications`}>用藥</Link>
            {" / "}
            <Link to={`/elders/${e.elder_id}/appointments`}>回診</Link>
            {" / "}
            <Link to={`/elders/${e.elder_id}/health-report`}>健康報告</Link>
            {" / "}
            <button type="button" onClick={() => invite(e.elder_id)}>
              邀請家屬
            </button>
          </li>
        ))}
```

- [ ] **Step 5：`App.tsx` 加路由**

import 區加：
```tsx
import { HealthReportPage } from "./pages/HealthReportPage";
```
`<Routes>` 內加（在 appointments 路由之後）：
```tsx
        <Route path="/elders/:elderId/health-report" element={<HealthReportPage />} />
```

- [ ] **Step 6：型別檢查 + 建置**

Run:
```bash
npm --prefix frontend run typecheck
npm --prefix frontend run build
```
Expected: 皆成功。

- [ ] **Step 7：commit**

```bash
git add frontend/src/api.ts frontend/src/report.ts frontend/src/pages/HealthReportPage.tsx \
        frontend/src/pages/EldersPage.tsx frontend/src/App.tsx
git commit -m "feat: 前端健康報告頁 + 長輩健康報告連結

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Task 4：全量品質閘門

**Files:** 無（純驗證）。

- [ ] **Step 1：後端 lint / format / 測試**

Run:
```bash
uv run ruff check .
uv run ruff format --check .
uv run pytest
```
Expected: ruff 無錯、format 已格式化、pytest 全綠。format 報未格式化 → `uv run ruff format .` 後重跑並把變更納入收尾 commit。

- [ ] **Step 2：前端 typecheck + build**

Run:
```bash
npm --prefix frontend run typecheck
npm --prefix frontend run build
```
Expected: 皆成功。

- [ ] **Step 3：pre-commit 全鉤子**

Run: `uv run pre-commit run --all-files`
Expected: 全 Passed。

- [ ] **Step 4：靜態匯入驗證**

Run: `PYTHONPATH=src uv run python -c "import kinsun.app; print('import ok')"`
Expected: `import ok`。

（若 Step 1 有 format 修整，補一個 `chore: 套用格式化` commit。）

---

## Self-Review（計畫對 spec 覆蓋檢查）

- `create_api_router` +2 store 參數 + `GET /health-report`（spec §3）→ Task 1 ✅
- 近 30 天篩選、tier int、created_at epoch、未綁 LINE 無危急（§3,§6）→ Task 1 測試 ✅
- 既有 4 API 測試補參數（§3,§6）→ Task 1 Step 3 ✅
- app.py 接線（§5）→ Task 2 ✅
- 前端 api/report.ts/HealthReportPage/EldersPage 連結/路由（§4）→ Task 3 ✅
- fail-safe 404/401（§7）→ Task 1 測試 ✅

型別/命名一致性：`create_api_router(..., risk_events, reminder_logs)`、回應 `{risk_events:[{tier,reason,created_at}], reminders:[{kind,content,created_at}]}`、前端 `getHealthReport` ↔ 端點 ↔ 頁面對齊；`tierLabel/kindLabel/formatTime`。無佔位符。
