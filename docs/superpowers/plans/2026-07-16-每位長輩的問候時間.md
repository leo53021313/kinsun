# 每位長輩的問候時間（自適應） 實作計畫

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓每位長輩的早安問候時間由她自己的活躍資料決定，而不是全系統統一寫死在 `PROACTIVE_GREETING_HOUR`。

**Architecture:** 純資料驅動、不經 LLM、不動反思與守則系統。每晚批次讀「她每天第一則主動訊息的時刻」取中位數，用**死區**（差 30 分內不動）與四道護欄算出偏好時間，存進 `greeting_preferences` 表。問候 job 改為每半小時掃描，判斷「已過她的時間且今天沒問候過」。

**Tech Stack:** Python 3.12＋uv、psycopg（Postgres/Supabase）、croniter、pytest。

**設計文件：** `docs/superpowers/specs/2026-07-16-每位長輩的問候時間-design.md`

## Global Constraints

- 台灣繁體中文：文件、註解、commit 訊息、面向使用者的字串一律台灣繁體中文，全形標點。
- 命名規範（`AGENTS.md`）：資料表複數名詞、時間戳 `<動詞過去分詞>_at` 且型別 `DOUBLE PRECISION`（epoch 秒）；查詢用 `get_*`（單筆）／`list_for_<維度>`；`save` 為 upsert 語意。
- 持久層三件套：Protocol＋`Pg<領域>Store`＋`Fake<領域>Store` 同住一檔（D-42 例外：單一狀態表可依語意命名檔案，本次為 `proactive/preferences.py`）。
- 環境變數：`Settings` 欄位名＝環境變數鍵小寫，一一對應、不設別名；每個鍵都要列進 `.env.example` 並附預設值與一行中文註解。本次前綴一律 `PROACTIVE_`。
- TDD：先寫失敗測試（RED）→ 最小實作（GREEN）→ 重構。既有 1079 個測試必須全綠，不得放寬任何斷言。
- 整合測試以 `KINSUN_IT=1`＋`KINSUN_TEST_DATABASE_URL` 啟用，禁連正式庫。
- **安全界線（不可協商）**：本機制只碰 `proactive-greeting`。用藥提醒（`medications/jobs.py`）與回診提醒（`appointments/jobs.py`）是各自獨立的 cron，**一行都不准碰**。`safety/`、`agent.py`、`strategies/` 同樣零改動。
- 分支：`feat/agent-reflection`（接在反思功能之後，一起合併）。不 push。

## 檔案結構

| 檔案 | 職責 |
| :--- | :--- |
| `src/kinsun/proactive/preferences.py` | `GreetingPreference` 模型＋三件套持久層 |
| `src/kinsun/proactive/greeting_time.py` | `next_greeting_time()` 純函式（死區＋四道護欄） |
| `src/kinsun/memory/shortterm.py` | 新增 `first_user_turn_per_day`（訊號讀取） |
| `src/kinsun/proactive/jobs.py` | 問候 job 改每半小時＋偏好判斷 |
| `src/kinsun/db.py` | `GREETING_PREFERENCES_DDL` |
| `src/kinsun/config.py` | 6 個 `PROACTIVE_GREETING_*` 設定＋跨欄位驗證 |
| `src/kinsun/composition.py` | 組 store、`Core` 加欄位 |
| `src/kinsun/scheduler/worker.py` | `run_one` 第四步＋問候 job 接線 |

---

### Task A: 偏好資料模型與持久層

**Files:**
- Create: `src/kinsun/proactive/preferences.py`
- Modify: `src/kinsun/db.py`（加 `GREETING_PREFERENCES_DDL`＋`ensure_schema` 一行）
- Modify: `tests/fakes.py`（匯入轉出）
- Test: `tests/test_proactive_preferences_store_contract.py`

**Interfaces:**
- Produces: `GreetingPreference` dataclass（`elder_id`／`hour`／`minute`／`computed_at`／`sample_days`／`median_minute_of_day`）；`GreetingPreferenceStore` Protocol，方法 `save(pref: GreetingPreference) -> None`（upsert）、`get_for_elder(elder_id: str) -> GreetingPreference | None`、`list_all() -> list[GreetingPreference]`。

- [ ] **Step 1: 寫失敗的合約測試**

`tests/test_proactive_preferences_store_contract.py`：

```python
"""GreetingPreferenceStore 合約：Fake 與 Pg 兩個 adapter 必須對同一情境給出相同結果。

Fake 每次都跑；Pg 需 `KINSUN_IT=1`（連真庫）。斷言一律以 `ns` 前綴 scope 到
本測試自己的資料，才能在共用真庫上以「成員／排除」關係斷言而互不干擾。
"""

from __future__ import annotations

import pytest

from kinsun.proactive.preferences import (
    FakeGreetingPreferenceStore,
    GreetingPreference,
    PgGreetingPreferenceStore,
)


@pytest.fixture(params=["fake", "pg"])
def store(request):
    if request.param == "pg":
        return PgGreetingPreferenceStore(request.getfixturevalue("pg_database"))
    return FakeGreetingPreferenceStore()


def _pref(elder_id: str, hour: int = 9, minute: int = 30) -> GreetingPreference:
    return GreetingPreference(
        elder_id=elder_id,
        hour=hour,
        minute=minute,
        computed_at=1_784_000_000.0,
        sample_days=7,
        median_minute_of_day=600,
    )


def test_save_then_get_returns_the_preference(store, ns):
    store.save(_pref(f"{ns}e1"))
    got = store.get_for_elder(f"{ns}e1")
    assert got is not None
    assert (got.hour, got.minute, got.sample_days, got.median_minute_of_day) == (9, 30, 7, 600)


def test_get_for_an_unknown_elder_returns_none(store, ns):
    assert store.get_for_elder(f"{ns}nobody") is None


def test_save_is_upsert(store, ns):
    store.save(_pref(f"{ns}e1", hour=8, minute=0))
    store.save(_pref(f"{ns}e1", hour=10, minute=30))
    got = store.get_for_elder(f"{ns}e1")
    assert (got.hour, got.minute) == (10, 30)
    assert len([p for p in store.list_all() if p.elder_id == f"{ns}e1"]) == 1


def test_list_all_spans_elders(store, ns):
    store.save(_pref(f"{ns}e1"))
    store.save(_pref(f"{ns}e2"))
    ids = {p.elder_id for p in store.list_all()}
    assert {f"{ns}e1", f"{ns}e2"} <= ids
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_preferences_store_contract.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.proactive.preferences'`

- [ ] **Step 3: 實作**

`src/kinsun/proactive/preferences.py`：

```python
"""每位長輩的問候時間偏好：由夜間批次依她的活躍資料算出，問候 job 讀取。

單一狀態表（一位長輩一列），故 `save` 為 upsert 而非 append-only 事件流水帳
（D-42 例外：依語意命名檔案，三件套結構不變）。

sample_days 與 median_minute_of_day 是**可解釋性欄位**：要能看懂系統為什麼
決定九點半，而不是面對一個沒有來由的數字。
"""

from __future__ import annotations

from dataclasses import dataclass
from typing import Protocol

from kinsun.db import Database, _Errors

_COLUMNS = "elder_id, hour, minute, computed_at, sample_days, median_minute_of_day"


@dataclass(frozen=True)
class GreetingPreference:
    elder_id: str
    hour: int
    minute: int  # 對齊半小時：0 或 30
    computed_at: float
    sample_days: int  # 憑幾天的資料算的（可解釋性）
    median_minute_of_day: int  # 她的中位活躍時刻，當天第幾分鐘（可解釋性）


class GreetingPreferenceError(Exception):
    """問候偏好讀寫失敗。"""


class GreetingPreferenceStore(Protocol):
    def save(self, pref: GreetingPreference) -> None: ...
    def get_for_elder(self, elder_id: str) -> GreetingPreference | None: ...
    def list_all(self) -> list[GreetingPreference]: ...


class PgGreetingPreferenceStore:
    def __init__(self, db: Database) -> None:
        self._db = _Errors(db, lambda m: GreetingPreferenceError(f"問候偏好存取失敗：{m}"))

    def save(self, pref: GreetingPreference) -> None:
        self._db.execute(
            f"INSERT INTO greeting_preferences ({_COLUMNS}) VALUES (%s, %s, %s, %s, %s, %s) "
            "ON CONFLICT (elder_id) DO UPDATE SET hour = EXCLUDED.hour, "
            "minute = EXCLUDED.minute, computed_at = EXCLUDED.computed_at, "
            "sample_days = EXCLUDED.sample_days, "
            "median_minute_of_day = EXCLUDED.median_minute_of_day",
            (
                pref.elder_id,
                pref.hour,
                pref.minute,
                pref.computed_at,
                pref.sample_days,
                pref.median_minute_of_day,
            ),
        )

    def get_for_elder(self, elder_id: str) -> GreetingPreference | None:
        row = self._db.query_one(
            f"SELECT {_COLUMNS} FROM greeting_preferences WHERE elder_id = %s", (elder_id,)
        )
        return GreetingPreference(*row) if row else None

    def list_all(self) -> list[GreetingPreference]:
        rows = self._db.query(f"SELECT {_COLUMNS} FROM greeting_preferences ORDER BY elder_id")
        return [GreetingPreference(*r) for r in rows]


class FakeGreetingPreferenceStore:
    """GreetingPreferenceStore 的記憶體替身（測試用，不碰 DB）。

    以 elder_id 為鍵做 upsert；list_all 依 elder_id 排序，對齊 Pg 的 ORDER BY。
    """

    def __init__(self) -> None:
        self._rows: dict[str, GreetingPreference] = {}

    def save(self, pref: GreetingPreference) -> None:
        self._rows[pref.elder_id] = pref

    def get_for_elder(self, elder_id: str) -> GreetingPreference | None:
        return self._rows.get(elder_id)

    def list_all(self) -> list[GreetingPreference]:
        return [self._rows[k] for k in sorted(self._rows)]
```

- [ ] **Step 4: 加 DDL**

`src/kinsun/db.py`，在 `STRATEGIES_DDL` 之後插入：

```python
# 每位長輩的問候時間偏好（spec 2026-07-16）：夜間批次算、問候 job 讀。
# 全新表、無舊欄位要遷移，故建表可單批（既有庫首次跑時一起建起來）。
GREETING_PREFERENCES_DDL = (
    "CREATE TABLE IF NOT EXISTS greeting_preferences ("
    "elder_id TEXT PRIMARY KEY, hour INTEGER NOT NULL, minute INTEGER NOT NULL, "
    "computed_at DOUBLE PRECISION NOT NULL, sample_days INTEGER NOT NULL, "
    "median_minute_of_day INTEGER NOT NULL);"
)
```

`ensure_schema` 內，`conn.execute(STRATEGIES_DDL)` 之後加：

```python
        conn.execute(GREETING_PREFERENCES_DDL)
```

- [ ] **Step 5: `tests/fakes.py` 加匯入轉出**

```python
from kinsun.proactive.preferences import (
    FakeGreetingPreferenceStore as FakeGreetingPreferenceStore,
)
```

- [ ] **Step 6: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_preferences_store_contract.py -v`
Expected: PASS（fake 4 個；`KINSUN_IT=1` 時 fake＋pg 共 8 個）

- [ ] **Step 7: Commit**

```bash
git add src/kinsun/proactive/preferences.py src/kinsun/db.py tests/fakes.py tests/test_proactive_preferences_store_contract.py
git commit -m "feat(proactive): 新增每位長輩的問候時間偏好持久層

問候時間目前是全域 cron（PROACTIVE_GREETING_HOUR），全體長輩同一個時間。
要讓它因人而異，得先有地方存每位長輩自己的時間。

單一狀態表（一位長輩一列），故 save 為 upsert 而非 append-only 事件流水帳。
帶 sample_days 與 median_minute_of_day 兩個可解釋性欄位——自動調整的東西
若說不出「憑什麼是九點半」，就沒人敢信它、也沒人查得動它。

影響：新增 greeting_preferences 表。尚無任何呼叫端，對現行行為零影響。"
```

---

### Task B: 訊號讀取（`first_user_turn_per_day`）

**Files:**
- Modify: `src/kinsun/memory/shortterm.py`（Protocol＋Pg＋Fake）
- Test: `tests/test_memory_shortterm_store_contract.py`（擴充既有檔）

**Interfaces:**
- Produces: `MemoryStore.first_user_turn_per_day(elder_id: str, *, since: float, before: float) -> list[float]` — 回傳 `[since, before)` 內**每天第一則 `role='user'` 訊息**的 epoch 秒（升序、每天至多一筆）。

**為什麼**：這是整個機制的唯一訊號。「她幾點起床」的證據本來就在 `turns` 表裡——她從來不在八點前傳訊息。

**⚠️ 不改既有的 `list_for_range`／`list_recent_in_range`**（consolidation 與反思在用，改它有回歸風險）。

- [ ] **Step 1: 寫失敗的合約測試**

在 `tests/test_memory_shortterm_store_contract.py` 末尾加（依該檔既有 fixture 命名調整）：

```python
def test_first_user_turn_per_day_returns_one_per_day(store, ns):
    day1_early = datetime(2026, 7, 10, 9, 15, tzinfo=TPE)
    day1_late = datetime(2026, 7, 10, 14, 0, tzinfo=TPE)
    day2 = datetime(2026, 7, 11, 8, 30, tzinfo=TPE)
    for at, text in ((day1_early, "早"), (day1_late, "午"), (day2, "隔天早")):
        store.append(f"{ns}e1", Message(role="user", content=text), at=at)

    since = datetime(2026, 7, 10, tzinfo=TPE).timestamp()
    before = datetime(2026, 7, 12, tzinfo=TPE).timestamp()
    got = store.first_user_turn_per_day(f"{ns}e1", since=since, before=before)

    assert got == [day1_early.timestamp(), day2.timestamp()]


def test_first_user_turn_per_day_ignores_assistant_turns(store, ns):
    bot_first = datetime(2026, 7, 10, 8, 0, tzinfo=TPE)
    user_later = datetime(2026, 7, 10, 10, 0, tzinfo=TPE)
    store.append(f"{ns}e1", Message(role="assistant", content="早安"), at=bot_first)
    store.append(f"{ns}e1", Message(role="user", content="嗯"), at=user_later)

    since = datetime(2026, 7, 10, tzinfo=TPE).timestamp()
    before = datetime(2026, 7, 11, tzinfo=TPE).timestamp()
    got = store.first_user_turn_per_day(f"{ns}e1", since=since, before=before)

    assert got == [user_later.timestamp()]


def test_first_user_turn_per_day_is_empty_when_no_turns(store, ns):
    since = datetime(2026, 7, 10, tzinfo=TPE).timestamp()
    before = datetime(2026, 7, 11, tzinfo=TPE).timestamp()
    assert store.first_user_turn_per_day(f"{ns}silent", since=since, before=before) == []
```

> 若既有合約測試檔的 `store` fixture 沒有注入可控時鐘或 `append(..., at=)`，請比照該檔既有測試的寫法調整；`FakeMemoryStore.append` 已支援 `at=`（`memory/shortterm.py`），`PgMemoryStore.append` 用 `clock()`——Pg 側請用 fixture 的可控時鐘逐筆寫入，或直接以 SQL 佈資料（照該檔既有做法）。

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_memory_shortterm_store_contract.py -k first_user_turn -v`
Expected: FAIL — `AttributeError: ... has no attribute 'first_user_turn_per_day'`

- [ ] **Step 3: 實作**

`MemoryStore` Protocol 加：

```python
    def first_user_turn_per_day(
        self, elder_id: str, *, since: float, before: float
    ) -> list[float]: ...
```

`PgMemoryStore`：

```python
    def first_user_turn_per_day(
        self, elder_id: str, *, since: float, before: float
    ) -> list[float]:
        """回傳 [since, before) 內每天第一則長輩發言的時刻（epoch 秒，升序、每天至多一筆）。

        自適應問候時間的唯一訊號（spec 2026-07-16）：她幾點起床，看她幾點開始講話。
        日界依配置時區切分（台灣無日光節約，一天固定 86400 秒）；分組在 Python 端做，
        避免在 SQL 裡寫死時區轉換。
        """
        rows = self._db.query(
            "SELECT created_at FROM turns "
            "WHERE elder_id = %s AND role = 'user' AND created_at >= %s AND created_at < %s "
            "ORDER BY created_at ASC",
            (elder_id, since, before),
        )
        return _first_per_day([r[0] for r in rows], self._clock().tzinfo)
```

模組層級私有函式（Pg 與 Fake 共用，保證兩者語意等價）：

```python
def _first_per_day(timestamps: list[float], tz) -> list[float]:
    """把時刻依所在日期分組，每天只留最早的一筆（升序）。"""
    first: dict[float, float] = {}
    for ts in sorted(timestamps):
        day = (
            datetime.fromtimestamp(ts, tz)
            .replace(hour=0, minute=0, second=0, microsecond=0)
            .timestamp()
        )
        if day not in first:
            first[day] = ts
    return [first[d] for d in sorted(first)]
```

`FakeMemoryStore`：

```python
    def first_user_turn_per_day(
        self, elder_id: str, *, since: float, before: float
    ) -> list[float]:
        stamps = [
            ts
            for ts, m in self._turns.get(elder_id, [])
            if m.role == "user" and since <= ts < before
        ]
        return _first_per_day(stamps, self._now.tzinfo)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_memory_shortterm_store_contract.py -v`
Expected: PASS

- [ ] **Step 5: 跑全套**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: 1079 passed ＋ 新增數

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/memory/shortterm.py tests/test_memory_shortterm_store_contract.py
git commit -m "feat(memory): 新增每天第一則長輩發言的查詢，供自適應問候時間取訊號

「她幾點起床」的證據本來就在 turns 表裡——她從來不在八點前傳訊息。讓 LLM 從
對話裡猜幾點，不如直接看她幾點開始講話。這是自適應問候時間的唯一訊號。

只取 role='user'：金孫自己的問候不算「她醒了」，否則訊號會被系統自己污染。
分組邏輯抽成 _first_per_day 供 Pg 與 Fake 共用，保證兩個 adapter 的日界切分
語意一字不差。

未動既有的 list_for_range／list_recent_in_range（consolidation 與反思在用）。

影響：MemoryStore 多一個查詢方法。尚無呼叫端。"
```

---

### Task C: 問候設定（6 個 `PROACTIVE_GREETING_*`）

**Files:**
- Modify: `src/kinsun/config.py`
- Modify: `.env.example`
- Test: `tests/test_config.py`

**Interfaces:**
- Produces: `Settings.proactive_greeting_adaptive_enabled: bool`、`proactive_greeting_lookback_days: int`、`proactive_greeting_min_sample_days: int`、`proactive_greeting_earliest_hour: int`、`proactive_greeting_latest_hour: int`、`proactive_greeting_max_shift_minutes: int`。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_config.py` 加（`BASE_ENV` 沿用該檔既有寫法）：

```python
def test_adaptive_greeting_settings_have_defaults():
    settings = load_settings(BASE_ENV)
    assert settings.proactive_greeting_adaptive_enabled is True
    assert settings.proactive_greeting_lookback_days == 14
    assert settings.proactive_greeting_min_sample_days == 5
    assert settings.proactive_greeting_earliest_hour == 6
    assert settings.proactive_greeting_latest_hour == 11
    assert settings.proactive_greeting_max_shift_minutes == 30


def test_adaptive_greeting_can_be_switched_off():
    settings = load_settings({**BASE_ENV, "PROACTIVE_GREETING_ADAPTIVE_ENABLED": "false"})
    assert settings.proactive_greeting_adaptive_enabled is False


def test_greeting_hour_bounds_must_be_ordered():
    with pytest.raises(ConfigError, match="PROACTIVE_GREETING_EARLIEST_HOUR"):
        load_settings(
            {
                **BASE_ENV,
                "PROACTIVE_GREETING_EARLIEST_HOUR": "11",
                "PROACTIVE_GREETING_LATEST_HOUR": "6",
            }
        )


def test_greeting_sample_days_must_fit_the_lookback_window():
    with pytest.raises(ConfigError, match="PROACTIVE_GREETING_MIN_SAMPLE_DAYS"):
        load_settings(
            {
                **BASE_ENV,
                "PROACTIVE_GREETING_LOOKBACK_DAYS": "3",
                "PROACTIVE_GREETING_MIN_SAMPLE_DAYS": "5",
            }
        )
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_config.py -k greeting -v`
Expected: FAIL — `AttributeError: 'Settings' object has no attribute 'proactive_greeting_adaptive_enabled'`

- [ ] **Step 3: 改 `config.py`**

`Settings` 加六個欄位（擺在既有 `proactive_*` 附近）：

```python
    proactive_greeting_adaptive_enabled: bool
    proactive_greeting_lookback_days: int
    proactive_greeting_min_sample_days: int
    proactive_greeting_earliest_hour: int
    proactive_greeting_latest_hour: int
    proactive_greeting_max_shift_minutes: int
```

`load_settings` 加：

```python
        # 自適應問候時間（spec 2026-07-16）：預設開；關閉則全體回退 PROACTIVE_GREETING_HOUR。
        proactive_greeting_adaptive_enabled=_parse_bool(
            env.get("PROACTIVE_GREETING_ADAPTIVE_ENABLED", "true")
        ),
        proactive_greeting_lookback_days=_require_positive_int(
            env, "PROACTIVE_GREETING_LOOKBACK_DAYS", "14"
        ),
        proactive_greeting_min_sample_days=_require_positive_int(
            env, "PROACTIVE_GREETING_MIN_SAMPLE_DAYS", "5"
        ),
        proactive_greeting_earliest_hour=int(env.get("PROACTIVE_GREETING_EARLIEST_HOUR", "6")),
        proactive_greeting_latest_hour=int(env.get("PROACTIVE_GREETING_LATEST_HOUR", "11")),
        proactive_greeting_max_shift_minutes=_require_positive_int(
            env, "PROACTIVE_GREETING_MAX_SHIFT_MINUTES", "30"
        ),
```

跨欄位驗證（比照既有 `REFLECTION_` 的作法，放在同一處）：

```python
    # 上下限顛倒＝夾取區間為空，問候時間會被夾成一個荒謬的值；啟動即失敗，不要等到夜裡才發現。
    if earliest_hour >= latest_hour:
        raise ConfigError(
            f"PROACTIVE_GREETING_EARLIEST_HOUR（{earliest_hour}）"
            f"必須小於 PROACTIVE_GREETING_LATEST_HOUR（{latest_hour}）。"
        )
    # 樣本門檻超出回顧視野＝永遠湊不到樣本，問候時間永遠不會調整，且不會報錯。
    if min_sample_days > lookback_days:
        raise ConfigError(
            f"PROACTIVE_GREETING_MIN_SAMPLE_DAYS（{min_sample_days}）不得大於 "
            f"PROACTIVE_GREETING_LOOKBACK_DAYS（{lookback_days}）："
            "樣本門檻超出回顧視野時，永遠湊不到足夠樣本，問候時間永遠不會調整卻不報錯。"
        )
```

（實作時請沿用該檔既有的驗證擺放位置與變數取值方式。）

- [ ] **Step 4: 補 `.env.example`**

依該檔既有的分隔線樣式加一區塊：

```bash
# 自適應問候時間總開關（spec 2026-07-16）；關閉則全體長輩回退 PROACTIVE_GREETING_HOUR
PROACTIVE_GREETING_ADAPTIVE_ENABLED=true
# 回顧幾天的活躍紀錄來推算問候時間
PROACTIVE_GREETING_LOOKBACK_DAYS=14
# 至少幾天有活躍才調整；不足則不動（須 ≤ PROACTIVE_GREETING_LOOKBACK_DAYS）
PROACTIVE_GREETING_MIN_SAMPLE_DAYS=5
# 問候時間下限（幾點）；統計再怎麼算都不會早於此
PROACTIVE_GREETING_EARLIEST_HOUR=6
# 問候時間上限（幾點）；統計再怎麼算都不會晚於此
PROACTIVE_GREETING_LATEST_HOUR=11
# 單次最大調整幅度（分鐘），同時是死區門檻：與現行時間差距在此之內就不動
PROACTIVE_GREETING_MAX_SHIFT_MINUTES=30
```

- [ ] **Step 5: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_config.py -v`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/config.py .env.example tests/test_config.py
git commit -m "feat(config): 新增自適應問候時間的六個設定

自適應問候需要四道護欄（上下限、單次幅度、樣本門檻）與一個總開關。護欄比
演算法重要——這東西會自動改變系統對長輩的行為時間，算錯了沒有護欄就是半夜
三點吵醒人。

兩個跨欄位不變量在啟動時驗證，因為兩者的失效都是靜默的：上下限顛倒會把時間
夾成荒謬的值；樣本門檻超出回顧視野則永遠湊不到樣本，問候時間永遠不調整卻不
報錯，看起來就像「這位長輩的作息很穩定」。

影響：Settings 加 6 個欄位；.env.example 同步。尚無讀取端。"
```

---

### Task D: 計算核心（`next_greeting_time` 純函式）

**Files:**
- Create: `src/kinsun/proactive/greeting_time.py`
- Test: `tests/test_proactive_greeting_time.py`

**Interfaces:**
- Consumes: Task C 的設定值（由呼叫端傳入，本函式不讀 `Settings`）。
- Produces: `next_greeting_time(*, first_turns: list[float], current: tuple[int, int], tz: tzinfo, min_sample_days: int, earliest_hour: int, latest_hour: int, max_shift_minutes: int) -> tuple[int, int] | None`（`None` ＝不調整）；`median_minute_of_day(first_turns: list[float], tz: tzinfo) -> int`（供呼叫端寫可解釋性欄位）。

**這是本功能的核心，也是唯一擋住自我實現漂移的地方。** 純函式、無 DB、無 IO。

- [ ] **Step 1: 寫失敗的測試**

`tests/test_proactive_greeting_time.py`：

```python
"""自適應問候時間的計算核心。

最重要的一條是 test_the_time_does_not_drift_when_she_only_answers_our_greeting
——它守的是「死區」，那是唯一擋住自我實現漂移的設計。
"""

from __future__ import annotations

from datetime import datetime, timedelta, timezone

from kinsun.proactive.greeting_time import median_minute_of_day, next_greeting_time

TPE = timezone(timedelta(hours=8))
GUARDS = dict(
    tz=TPE, min_sample_days=5, earliest_hour=6, latest_hour=11, max_shift_minutes=30
)


def _turns_at(hour: int, minute: int = 0, days: int = 7) -> list[float]:
    """連續 days 天、每天在 hour:minute 各一則。"""
    return [
        datetime(2026, 7, 1 + d, hour, minute, tzinfo=TPE).timestamp() for d in range(days)
    ]


def test_too_few_samples_means_no_change():
    assert next_greeting_time(first_turns=_turns_at(10, days=4), current=(8, 0), **GUARDS) is None


def test_shifts_later_when_she_wakes_much_later():
    # 八點問候、她通常十一點才第一次講話 → 往後調一格（30 分）
    assert next_greeting_time(first_turns=_turns_at(11), current=(8, 0), **GUARDS) == (8, 30)


def test_shifts_earlier_when_she_is_already_up_before_us():
    # 八點問候、她通常七點就自己來 → 往前調一格
    assert next_greeting_time(first_turns=_turns_at(7), current=(8, 0), **GUARDS) == (7, 30)


def test_inside_the_dead_zone_nothing_moves():
    # 她八點二十活躍、八點問候 → 差 20 分（< 30）→ 不動
    assert next_greeting_time(first_turns=_turns_at(8, 20), current=(8, 0), **GUARDS) is None


def test_the_result_is_clamped_to_the_earliest_hour():
    # 她凌晨四點就活躍（失眠？）→ 也不准把問候排到六點以前
    got = next_greeting_time(first_turns=_turns_at(4), current=(6, 30), **GUARDS)
    assert got == (6, 0)


def test_the_result_is_clamped_to_the_latest_hour():
    got = next_greeting_time(first_turns=_turns_at(15), current=(10, 30), **GUARDS)
    assert got == (11, 0)


def test_the_result_is_aligned_to_the_half_hour():
    # 她通常 10:47 活躍 → 往後調的結果必須落在整點或半點（job 每半小時才跑）
    got = next_greeting_time(first_turns=_turns_at(10, 47), current=(8, 0), **GUARDS)
    assert got is not None
    assert got[1] in (0, 30)


def test_the_median_ignores_the_odd_early_bird_day():
    # 六天十點、一天五點（偶爾早起）→ 中位數仍是十點，不被離群值拉走
    turns = _turns_at(10, days=6) + [datetime(2026, 7, 7, 5, tzinfo=TPE).timestamp()]
    assert median_minute_of_day(turns, TPE) == 10 * 60


def test_the_time_does_not_drift_when_she_only_answers_our_greeting():
    """自我實現漂移的回歸測試——本功能最重要的一條。

    她每天都在被問候後五分鐘才講話（活躍是我們自己觸發的）。若沒有死區，
    時間會逐日往前漂到下限六點。有死區就該穩定不動。
    """
    current = (9, 0)
    for _ in range(10):
        turns = [
            datetime(2026, 7, 1 + d, current[0], current[1], tzinfo=TPE).timestamp() + 300
            for d in range(7)
        ]
        nxt = next_greeting_time(first_turns=turns, current=current, **GUARDS)
        if nxt is None:
            break
        current = nxt
    assert current == (9, 0), f"問候時間漂移到 {current}"


def test_it_converges_and_then_stops_when_she_wakes_late():
    """八點問候、她十一點活躍：逐日往後，最後停在死區邊緣，不會無限往後。"""
    current = (8, 0)
    seen = []
    for _ in range(20):
        nxt = next_greeting_time(first_turns=_turns_at(11), current=current, **GUARDS)
        if nxt is None:
            break
        assert nxt != current, "回傳了與現值相同的時間，應該回 None"
        current = nxt
        seen.append(current)
    assert current == (10, 30), f"收斂在 {current}"
    assert len(seen) == 5  # 8:00 → 10:30，一次 30 分
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_greeting_time.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'kinsun.proactive.greeting_time'`

- [ ] **Step 3: 實作**

`src/kinsun/proactive/greeting_time.py`：

```python
"""自適應問候時間的計算核心：從長輩的活躍時刻推算下一次的問候時間。

⚠️ 安全界線（不可協商）：本模組只決定「早安問候」的時間。用藥提醒
（medications/jobs.py）與回診提醒（appointments/jobs.py）是各自獨立的 cron，
時間由 MEDICATION_*_HOUR／APPOINTMENT_REMINDER_HOUR 決定，永遠不受本模組影響。

⚠️ 死區（dead zone）是唯一擋住自我實現漂移的設計，不要拿掉：
若規則是「問候＝她第一次活躍的時刻」，而她的活躍根本是被我們的問候觸發的
（八點問候 → 她八點五分講話），時間會逐日往前漂：
    08:00 → 她 08:05 活躍 → 07:50 → 她 07:55 → 07:40 → ... → 一路漂到下限。
故只有當中位活躍時刻與現行問候時間**相差超過 max_shift_minutes** 才調整。
兩個方向都因此收斂：她十一點才活躍 → 逐日往後停在 10:30；她七點就自己來 →
逐日往前停在 07:00。
"""

from __future__ import annotations

from datetime import datetime, tzinfo
from statistics import median

# 問候 job 每半小時掃一次（cron 0,30 * * * *），故偏好時間必須落在整點或半點——
# 存 07:45 卻在 08:00 問候，是對後台說謊。
_SLOT_MINUTES = 30


def median_minute_of_day(first_turns: list[float], tz: tzinfo) -> int:
    """把每筆時刻換算成「當天的第幾分鐘」後取中位數。

    用中位數不用平均數：偶爾的熬夜或早起是離群值，中位數對它有抗性。
    """
    minutes = []
    for ts in first_turns:
        local = datetime.fromtimestamp(ts, tz)
        minutes.append(local.hour * 60 + local.minute)
    return int(median(minutes))


def _align(minute_of_day: int) -> int:
    return round(minute_of_day / _SLOT_MINUTES) * _SLOT_MINUTES


def next_greeting_time(
    *,
    first_turns: list[float],
    current: tuple[int, int],
    tz: tzinfo,
    min_sample_days: int,
    earliest_hour: int,
    latest_hour: int,
    max_shift_minutes: int,
) -> tuple[int, int] | None:
    """算出下一次的問候時間；回 None 代表不調整（樣本不足、或已在死區內）。

    first_turns 為她每天第一則主動訊息的時刻（epoch 秒），current 為現行問候時間。
    """
    if len(first_turns) < min_sample_days:
        return None

    target = median_minute_of_day(first_turns, tz)
    current_minutes = current[0] * 60 + current[1]
    diff = target - current_minutes
    if abs(diff) <= max_shift_minutes:
        return None  # 死區：夠近了就不動，這是擋住自我實現漂移的關鍵

    step = max_shift_minutes if diff > 0 else -max_shift_minutes
    moved = _align(current_minutes + step)
    clamped = max(earliest_hour * 60, min(latest_hour * 60, moved))
    if clamped == current_minutes:
        return None  # 已在護欄邊界上，無處可去
    return divmod(clamped, 60)
```

- [ ] **Step 4: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_greeting_time.py -v`
Expected: PASS（10 個）

- [ ] **Step 5: 突變驗證（確認漂移測試有牙齒）**

暫時把 `if abs(diff) <= max_shift_minutes: return None` 改成 `if False:`，跑：

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_greeting_time.py -k drift -v`
Expected: **FAIL**（證明漂移測試確實在守死區）。確認後**還原**。

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/proactive/greeting_time.py tests/test_proactive_greeting_time.py
git commit -m "feat(proactive): 新增問候時間的計算核心，用死區擋住自我實現漂移

本功能的核心。從長輩每天第一則主動訊息的時刻取中位數，往那個方向逐步收斂。

最大的坑是自我實現漂移：若規則是「問候＝她第一次活躍的時刻」，而她的活躍
根本是被我們的問候觸發的（八點問候、她八點五分講話），時間會逐日往前漂到
下限六點。死區解決了它——與現行時間相差 30 分內就不動。兩個方向都收斂：
她十一點才活躍就逐日往後停在 10:30；她七點自己來就逐日往前停在 07:00。
漂移的回歸測試是這支檔案最重要的一條，已用突變驗證確認它會咬。

用中位數不用平均數：偶爾熬夜或早起是離群值，中位數對它有抗性。
結果對齊半小時：job 每半小時才掃，存 07:45 卻在 08:00 問候是對後台說謊。

純函式、無 DB、無 IO。安全界線寫進 docstring：本模組只決定早安問候，用藥與
回診提醒是各自獨立的 cron，永遠不受影響。

影響：新增純函式模組。尚無呼叫端。"
```

---

### Task E: 接線（問候 job 改每半小時 ＋ 夜間批次第四步）

**Files:**
- Modify: `src/kinsun/proactive/jobs.py`（`build_greeting_job` 改每半小時＋偏好判斷）
- Modify: `src/kinsun/scheduler/worker.py`（`run_one` 第四步、問候 job 接線）
- Modify: `src/kinsun/composition.py`（組 store、`Core` 加 `greeting_prefs`）
- Test: `tests/test_proactive_jobs.py`、`tests/test_scheduler_worker.py`

**Interfaces:**
- Consumes: Task A 的 `GreetingPreferenceStore`；Task B 的 `first_user_turn_per_day`；Task C 的六個設定；Task D 的 `next_greeting_time`／`median_minute_of_day`。

**⚠️ 三個硬需求**：
1. **只碰 `proactive-greeting`**。`build_inactivity_job`、`build_medication_slot_job`、`build_appointment_reminder_job` 一行都不准動。
2. **每晚計算失敗不得影響其他三步**（整理／摘要／反思）與其他長輩——沿用 `run_one` 既有的 try/except 慣例。
3. **查「今天問候過沒」失敗時跳過該長輩本次問候**（寧可漏問候，不可重複轟炸）。

- [ ] **Step 1: 寫失敗的測試（問候 job）**

`tests/test_proactive_jobs.py` 加（依既有 helper 調整）：

```python
def test_the_greeting_job_scans_every_half_hour():
    job = build_greeting_job(
        sessions=lambda: [],
        greet_one=lambda _e: None,
        default_hour=8,
        prefs=FakeGreetingPreferenceStore(),
        greeted_today=lambda _e: False,
        clock=lambda: datetime(2026, 7, 16, 8, 0, tzinfo=TPE),
    )
    assert job.cron == "0,30 * * * *"


def test_it_greets_once_her_preferred_time_has_passed():
    prefs = FakeGreetingPreferenceStore()
    prefs.save(GreetingPreference("e1", 9, 30, 0.0, 7, 570))
    greeted = []
    job = build_greeting_job(
        sessions=lambda: ["e1"],
        greet_one=greeted.append,
        default_hour=8,
        prefs=prefs,
        greeted_today=lambda _e: False,
        clock=lambda: datetime(2026, 7, 16, 9, 30, tzinfo=TPE),
    )
    job.run()
    assert greeted == ["e1"]


def test_it_stays_quiet_before_her_preferred_time():
    prefs = FakeGreetingPreferenceStore()
    prefs.save(GreetingPreference("e1", 9, 30, 0.0, 7, 570))
    greeted = []
    job = build_greeting_job(
        sessions=lambda: ["e1"],
        greet_one=greeted.append,
        default_hour=8,
        prefs=prefs,
        greeted_today=lambda _e: False,
        clock=lambda: datetime(2026, 7, 16, 9, 0, tzinfo=TPE),
    )
    job.run()
    assert greeted == []


def test_it_does_not_greet_twice_in_one_day():
    prefs = FakeGreetingPreferenceStore()
    prefs.save(GreetingPreference("e1", 8, 0, 0.0, 7, 480))
    greeted = []
    job = build_greeting_job(
        sessions=lambda: ["e1"],
        greet_one=greeted.append,
        default_hour=8,
        prefs=prefs,
        greeted_today=lambda _e: True,  # 今天已經問候過
        clock=lambda: datetime(2026, 7, 16, 10, 0, tzinfo=TPE),
    )
    job.run()
    assert greeted == []


def test_an_elder_without_a_preference_falls_back_to_the_global_hour():
    greeted = []
    job = build_greeting_job(
        sessions=lambda: ["newbie"],
        greet_one=greeted.append,
        default_hour=8,
        prefs=FakeGreetingPreferenceStore(),  # 空的
        greeted_today=lambda _e: False,
        clock=lambda: datetime(2026, 7, 16, 8, 0, tzinfo=TPE),
    )
    job.run()
    assert greeted == ["newbie"]


def test_a_failure_to_check_today_skips_that_elder_rather_than_double_greeting():
    def explode(_elder_id: str) -> bool:
        raise RuntimeError("db down")

    prefs = FakeGreetingPreferenceStore()
    prefs.save(GreetingPreference("e1", 8, 0, 0.0, 7, 480))
    greeted = []
    job = build_greeting_job(
        sessions=lambda: ["e1"],
        greet_one=greeted.append,
        default_hour=8,
        prefs=prefs,
        greeted_today=explode,
        clock=lambda: datetime(2026, 7, 16, 9, 0, tzinfo=TPE),
    )
    job.run()  # 不應拋出
    assert greeted == []  # 寧可漏問候，不可重複轟炸
```

- [ ] **Step 2: 執行測試，確認失敗**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_jobs.py -v`
Expected: FAIL — `TypeError: build_greeting_job() got an unexpected keyword argument 'prefs'`

- [ ] **Step 3: 改 `proactive/jobs.py`**

`build_greeting_job` 改為：

```python
def build_greeting_job(
    *,
    sessions: Callable[[], list[str]],
    greet_one: Callable[[str], object],
    default_hour: int,
    prefs: GreetingPreferenceStore,
    greeted_today: Callable[[str], bool],
    clock: Callable[[], datetime],
    name: str = "daily-greeting",
) -> Job:
    """每半小時掃描：已過她的偏好時間、且今天還沒問候過她，才問候（spec 2026-07-16）。

    為什麼是「已過且今天沒問候過」而非「精確比對時段」：worker 半夜當機重啟後，
    Scheduler 的補跨語意只會補跑一次，精確比對會讓該時段的長輩整天被漏掉。
    冪等由 greeted_today（讀 reminder_logs）保證。
    """

    def action(elder_id: str) -> None:
        now = clock()
        pref = prefs.get_for_elder(elder_id)
        target = pref.hour * 60 + pref.minute if pref else default_hour * 60
        if now.hour * 60 + now.minute < target:
            return
        if greeted_today(elder_id):
            return
        greet_one(elder_id)

    return fanout_job(
        name=name, hour=0, minute=0, population=sessions, action=action, logger=logger
    )
```

> `fanout_job` 目前用 `hour`／`minute` 組出 `f"{minute} {hour} * * *"`。本 job 需要 `0,30 * * * *`——請在 `fanout_job` 加一個 `cron: str | None = None` 參數（給定時直接採用、不組），**不要**改變既有呼叫端的行為。`build_greeting_job` 傳 `cron="0,30 * * * *"`。
>
> 讀偏好失敗須回退全域預設（記 warning，不可中斷）；`greeted_today` 失敗須跳過該長輩（`fanout_job` 的 per-item try/except 已提供隔離，但**回退語意**要自己寫對——讓例外冒到 fanout 等於跳過該長輩，符合「寧可漏問候」，測試已釘死）。

- [ ] **Step 4: 改 `scheduler/worker.py`**

`run_one` 加第四步：

```python
        # 自適應問候時間（spec 2026-07-16）：純統計、不經 LLM；失敗不影響前三步。
        if settings.proactive_greeting_adaptive_enabled:
            try:
                update_greeting_time(
                    elder_id,
                    short_term=memory,
                    prefs=greeting_prefs,
                    clock=clock,
                    default_hour=settings.proactive_greeting_hour,
                    lookback_days=settings.proactive_greeting_lookback_days,
                    min_sample_days=settings.proactive_greeting_min_sample_days,
                    earliest_hour=settings.proactive_greeting_earliest_hour,
                    latest_hour=settings.proactive_greeting_latest_hour,
                    max_shift_minutes=settings.proactive_greeting_max_shift_minutes,
                )
            except Exception:  # noqa: BLE001 - 問候時間計算失敗不影響整理、摘要與反思
                logger.warning("問候時間計算失敗 elder=%s", elder_id)
```

`update_greeting_time` 是 `proactive/greeting_time.py` 的薄協調函式（讀訊號 → 呼叫純函式 → 存偏好），請一併加入該檔並補測試：

```python
def update_greeting_time(
    elder_id: str,
    *,
    short_term,
    prefs: GreetingPreferenceStore,
    clock: Callable[[], datetime],
    default_hour: int,
    lookback_days: int,
    min_sample_days: int,
    earliest_hour: int,
    latest_hour: int,
    max_shift_minutes: int,
) -> None:
    now = clock()
    tz = now.tzinfo
    _, end = previous_day_bounds(now)
    start = end - lookback_days * 86400.0
    first_turns = short_term.first_user_turn_per_day(elder_id, since=start, before=end)
    existing = prefs.get_for_elder(elder_id)
    current = (existing.hour, existing.minute) if existing else (default_hour, 0)
    nxt = next_greeting_time(
        first_turns=first_turns,
        current=current,
        tz=tz,
        min_sample_days=min_sample_days,
        earliest_hour=earliest_hour,
        latest_hour=latest_hour,
        max_shift_minutes=max_shift_minutes,
    )
    if nxt is None:
        return
    prefs.save(
        GreetingPreference(
            elder_id=elder_id,
            hour=nxt[0],
            minute=nxt[1],
            computed_at=now.timestamp(),
            sample_days=len(first_turns),
            median_minute_of_day=median_minute_of_day(first_turns, tz),
        )
    )
    logger.info(
        "問候時間調整 elder=%s %02d:%02d → %02d:%02d（%d 天樣本）",
        elder_id, current[0], current[1], nxt[0], nxt[1], len(first_turns),
    )
```

問候 job 的接線（`build_jobs` 內）：

```python
    def greeted_today(elder_id: str) -> bool:
        now = clock()
        start = now.replace(hour=0, minute=0, second=0, microsecond=0).timestamp()
        logs = reminder_logs.list_for_range(elder_id, start=start, end=now.timestamp())
        return any(log.kind == REMINDER_KIND_PROACTIVE_GREETING for log in logs)
```

- [ ] **Step 5: 改 `composition.py`**

組 store 並收進 `Core`：

```python
    greeting_prefs = PgGreetingPreferenceStore(db)
```

`Core` dataclass 加 `greeting_prefs: PgGreetingPreferenceStore`（註解說明：夜間批次寫、問候 job 讀，故收進 Core）。

- [ ] **Step 6: 執行測試，確認通過**

Run: `PYTHONPATH=src uv run pytest tests/test_proactive_jobs.py tests/test_scheduler_worker.py -v`
Expected: PASS

- [ ] **Step 7: 跑全套 ＋ lint**

Run: `PYTHONPATH=src uv run pytest -q`
Expected: 全綠（1079 ＋ 新增數）

Run: `uv run ruff check src tests && uv run ruff format --check src tests`
Expected: 全過

- [ ] **Step 8: Commit**

```bash
git add src/kinsun/proactive/ src/kinsun/scheduler/worker.py src/kinsun/composition.py tests/
git commit -m "feat(proactive): 問候時間改為每位長輩各自決定，閉環接通

routine 類守則原本在架構上做不到它字面說的事——「九點半再問候」會注入
system prompt，但問候時間是全域 cron，守則只改得了措辭、改不了時間。這個
commit 讓時間真的因人而異。

問候 job 從「每天固定一點鐘扇出」改為「每半小時掃描，已過她的偏好時間且今天
沒問候過才問候」。不用精確比對時段的理由：worker 半夜當機重啟後，Scheduler
的補跨語意只會補跑一次，精確比對會讓該時段的長輩整天被漏掉；用「已過＋今天
沒問候過」則會補問候（晚一點，但有）。冪等由 reminder_logs 保證。

計算掛在既有夜間批次的第四步（整理 → 摘要 → 反思 → 問候時間），不新增 cron。
四者互不拖累：任一失敗只記 warning，不影響其他三步與其他長輩。查「今天問候過
沒」失敗時跳過該長輩——寧可漏一次問候，不可重複轟炸長輩。

安全界線：只碰 proactive-greeting。用藥提醒、回診提醒、失聯關心都是各自獨立
的 cron，一行未動。

影響：長輩的問候時間從今晚起會依她的活躍資料逐步調整（每次最多 30 分鐘、
夾在 6-11 點、樣本不足五天不動）。PROACTIVE_GREETING_ADAPTIVE_ENABLED=false
可全體回退全域設定。"
```

---

### Task F: 文件同步

**Files:**
- Modify: `CONTEXT.md`（加 `greeting_preferences` 與自適應問候的領域語彙）
- Modify: `docs/superpowers/specs/2026-07-14-agent自我進化-每晚反思與策略記憶-design.md`（`routine` 類守則的定位釐清：描述作息事實、影響措辭；時間由 `proactive/greeting_time.py` 決定）

- [ ] **Step 1: 更新 `CONTEXT.md`**

依既有格式加語彙：問候偏好（GreetingPreference）、自適應問候時間。務必寫進：**只碰早安問候，用藥與回診提醒永遠不受影響**；**死區是擋住自我實現漂移的設計**。

- [ ] **Step 2: 更新反思 spec 的 `routine` 定位**

在四類白名單的說明處加註：`routine` 守則描述作息事實、影響金孫的措辭；**問候時間本身由 `proactive/greeting_time.py` 依統計決定**（spec 2026-07-16），兩者各走各的路、都用到同一個事實。

- [ ] **Step 3: 跑全套確認文件變更無影響**

Run: `PYTHONPATH=src uv run pytest -q`

- [ ] **Step 4: Commit**

```bash
git add CONTEXT.md docs/superpowers/specs/
git commit -m "docs: 同步自適應問候時間，並釐清 routine 守則的定位

routine 守則原本讀起來像排程指令（「九點半再問候」），實際上只影響措辭。
現在時間由 proactive/greeting_time.py 依統計決定，兩者的分工必須寫清楚，
否則下一個人（或下一個 AI）會以為守則能改排程，或以為問候時間是寫死的。

影響：純文件。"
```

---

## 完成標準（Definition of Done）

- [ ] 全套測試通過：`PYTHONPATH=src uv run pytest -q`（不得少於 1079 ＋ 新增數）
- [ ] 整合測試通過：`KINSUN_IT=1 PYTHONPATH=src uv run pytest -q`
- [ ] Lint 通過：`uv run ruff check src tests` ＋ `ruff format --check`
- [ ] **`agent.py`／`safety/`／`strategies/` 零改動**：`git diff --stat 9bc8094..HEAD -- src/kinsun/agent.py src/kinsun/safety/ src/kinsun/strategies/` 僅顯示反思功能既有的變更，本次不新增
- [ ] **用藥與回診提醒零改動**：`git diff --stat -- src/kinsun/medications/jobs.py src/kinsun/appointments/jobs.py` 為空
- [ ] **漂移回歸測試有牙齒**：拿掉死區後 `test_the_time_does_not_drift_when_she_only_answers_our_greeting` 必須 FAIL
- [ ] 手動驗證：`/admin` 手動觸發 consolidation job，確認 `greeting_preferences` 有寫入（或 log 顯示「樣本不足」等預期結果）
