# 照護抽取 prompt 接線（custom_instructions）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 讓 `provenance.CUSTOM_FACT_EXTRACTION_PROMPT` 經 Mem0 config 的 `custom_instructions` 真正套用到記憶抽取（目前為死碼）。

**Architecture:** 在 [mem0_factory.py](../../../src/kinsun/mem0_factory.py) 的 `build_mem0_config` 回傳 dict 加一個頂層 key `custom_instructions`，值為照護抽取 prompt（原文逐字）。`Memory.from_config` 會把它當最高優先級附加規則疊進 Mem0 預設抽取 prompt。單一檔案、單一 key 變更。

**Tech Stack:** Python 3.12、mem0ai（2.0.10）、pytest。

## Global Constraints

- 一律台灣繁體中文（文件、註解、commit）。
- 最小修改：只動完成需求所必須的檔案；本案僅 `mem0_factory.py` + `tests/test_mem0_factory.py`。
- 不新增第三方套件、不新增環境變數、不改資料表。
- 全程 fail-safe：不改任何記憶讀寫控制流。
- commit 規範：`feat/fix/docs/refactor/test/chore`，結尾加 `Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>`。
- 品質閘門：`uv run pytest`、`uv run ruff check .`、`uv run ruff format .`、pre-commit 全綠。
- 設計依據：[docs/superpowers/specs/2026-06-29-照護抽取prompt接線-design.md](../specs/2026-06-29-照護抽取prompt接線-design.md)。

---

### Task 1: 把 custom_instructions 接進 build_mem0_config

**Files:**
- Modify: `src/kinsun/mem0_factory.py`
- Test: `tests/test_mem0_factory.py`

**Interfaces:**
- Consumes: `kinsun.longterm.provenance.CUSTOM_FACT_EXTRACTION_PROMPT`（既有字串常數）、`build_mem0_config(settings: Settings) -> dict`（既有）。
- Produces: `build_mem0_config` 回傳的 dict 多一個頂層 key `"custom_instructions"`，型別 `str`，值 `== provenance.CUSTOM_FACT_EXTRACTION_PROMPT`。

- [ ] **Step 1: Write the failing test**

在 `tests/test_mem0_factory.py` 既有 import 下方新增 import，並於檔尾新增一個測試：

```python
from kinsun.longterm.provenance import CUSTOM_FACT_EXTRACTION_PROMPT
```

```python
def test_build_mem0_config_includes_custom_instructions():
    cfg = build_mem0_config(load_settings(_ENV))
    assert cfg["custom_instructions"] == CUSTOM_FACT_EXTRACTION_PROMPT
```

- [ ] **Step 2: Run test to verify it fails**

Run: `uv run pytest tests/test_mem0_factory.py::test_build_mem0_config_includes_custom_instructions -v`
Expected: FAIL，`KeyError: 'custom_instructions'`（目前 config 無此 key）。

- [ ] **Step 3: Write minimal implementation**

在 `src/kinsun/mem0_factory.py` 頂部 import 區加入 provenance：

```python
from kinsun.config import Settings
from kinsun.longterm import provenance
```

在 `build_mem0_config` 的回傳 dict 末尾、`graph_store` 之後加入新 key：

```python
        "graph_store": {
            "provider": "neo4j",
            "config": {
                "url": settings.neo4j_uri,
                "username": settings.neo4j_username,
                "password": settings.neo4j_password,
            },
        },
        "custom_instructions": provenance.CUSTOM_FACT_EXTRACTION_PROMPT,
    }
```

> 注意可能的 import 循環：`provenance.py` 不 import `mem0_factory`，且 `mem0_factory` 既無 longterm 相依，新增此 import 安全。若 ruff 報未使用或排序問題，依 ruff 自動修正。

- [ ] **Step 4: Run test to verify it passes**

Run: `uv run pytest tests/test_mem0_factory.py -v`
Expected: PASS（新測試與既有 `test_build_mem0_config_shape` 皆綠）。

- [ ] **Step 5: Run full quality gate**

Run: `uv run pytest`
Expected: 全部通過（既有 86 passed / 3 skipped 之上 +1 passed）。

Run: `uv run ruff check . && uv run ruff format --check .`
Expected: 無錯誤。

- [ ] **Step 6: Commit**

```bash
git add src/kinsun/mem0_factory.py tests/test_mem0_factory.py
git commit -m "feat: 照護抽取 prompt 經 custom_instructions 接上 Mem0

provenance.CUSTOM_FACT_EXTRACTION_PROMPT 原為死碼；
build_mem0_config 加 custom_instructions 讓其在記憶抽取生效。

Co-Authored-By: Claude Opus 4.8 <noreply@anthropic.com>"
```

---

## Self-Review

**1. Spec coverage：**
- spec §2.1「`build_mem0_config` 新增頂層 key `custom_instructions` = 原文」→ Task 1 Step 3 ✅
- spec §6「`test_mem0_factory` 補一條，斷言含 key 且 == 原文，全離線」→ Task 1 Step 1 ✅
- spec §2.2 不在範圍（分類/升級/per-call prompt/改寫文字）→ 計畫未觸及 ✅

**2. Placeholder scan：** 無 TBD/TODO；測試與實作皆為完整程式碼 ✅

**3. Type consistency：** `build_mem0_config(settings) -> dict`、`CUSTOM_FACT_EXTRACTION_PROMPT: str`、新 key `"custom_instructions"` 在測試與實作一致 ✅

**4. 抽取品質實機驗證**：依 spec §6 為 deferred（需雲端/金鑰），不在本計畫；本計畫驗收＝「設定確實接上」。
